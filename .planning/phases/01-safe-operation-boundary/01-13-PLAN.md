---
phase: 01-safe-operation-boundary
plan: 13
type: execute
wave: 5
depends_on: [01-03, 01-09, 01-10]
files_modified:
  - src/core/mcp.ts
  - src/core/owned-skills.ts
  - src/core/skill-policy.ts
  - src/core/isolation.ts
  - src/core/update.ts
  - test/mcp.test.ts
  - test/skill-policy.test.ts
  - test/isolation.test.ts
  - test/update.test.ts
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-03, SAFE-04, SAFE-05]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every existing MCP/skill/policy/isolation/update mutation has a read-only complete plan and consumes one MutationSession for standalone or nested apply."
    - "D-01~D-03: direct remove/write/rename paths use the same operation-wide PathProof and immediate recheck as transactions."
    - "D-04~D-07: active/needs-repair state blocks these mutations but not their dry-run/status/doctor paths."
    - "D-08~D-14: native input, errors, plans, journals and results are strict, bounded, and redacted."
  artifacts:
    - path: src/core/mcp.ts
      provides: "Session-bound native MCP merge/apply"
    - path: src/core/isolation.ts
      provides: "Session-bound existing manifest/runtime mutations and clean"
    - path: src/core/update.ts
      provides: "Session-bound candidate stage apply with non-mutating preview"
  key_links:
    - from: src/core/mcp.ts
      to: src/core/transaction.ts
      via: "caller/session-owned file transaction"
      pattern: "MutationSession|applyFileTransaction"
    - from: src/core/update.ts
      to: src/core/path-boundary.ts
      via: "checkout/temporary/target proof before rename"
      pattern: "proveOperationPaths|recheck"
  prohibitions:
    - statement: "Hardening an existing isolation mutation MUST NOT introduce project-pack selection, tree-off inheritance, sealed fallback, or claims beyond the already supported adapter behavior."
      requirement_id: SAFE-01
      category: transparency
      status: unverified
      flagged: true
---

<objective>
남은 existing file mutation callers를 shared proof/session/transaction boundary로 이동한다.

Purpose: direct rm/write/rename과 standalone sync paths가 Phase 1 safety guarantees를 우회하지 못하게 한다.
Output: session-bound MCP, skill, policy, existing isolation, and update mutations with focused regressions.
</objective>

<execution_context>
@C:/Users/alpha/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safe-operation-boundary/01-CONTEXT.md
@.planning/phases/01-safe-operation-boundary/01-RESEARCH.md
@.planning/phases/01-safe-operation-boundary/01-PATTERNS.md
@.planning/phases/01-safe-operation-boundary/01-03-SUMMARY.md
@.planning/phases/01-safe-operation-boundary/01-09-SUMMARY.md
@.planning/phases/01-safe-operation-boundary/01-10-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: MCP standalone preview/apply의 one-session tracer</name>
  <files>src/core/mcp.ts, test/mcp.test.ts</files>
  <read_first>
    src/core/mcp.ts
    src/core/transaction.ts
    src/core/writer-lock.ts
    src/core/path-boundary.ts
    test/mcp.test.ts
  </read_first>
  <behavior>
    - plan includes native target, state/journal/snapshot/temp/source/package paths and source/target hashes.
    - standalone apply acquires one session after strict input/full preflight; caller-provided session is reused.
    - active/needs-repair writer blocks apply while plan remains readable.
  </behavior>
  <action>D-01~D-07에 따라 `planMcpSync`의 complete proof inputs/digest를 만들고 `applyMcpSync`에 optional MutationSession을 추가한다. Strict native parse from Plan 01-10 must finish before session acquisition. Pass session to applyFileTransaction and recheck target/source proof immediately before write. Preserve current harness-specific renderer and unsupported parity rather than simulating config support.</action>
  <acceptance_criteria>dry-run changes no bytes; valid standalone apply uses one lock; nested session reuses token; escape/drift/active/needs-repair cases invoke zero target writes.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/mcp.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, or an unsafe/active-state fixture mutates native config</fails_when>
  </verify>
  <reversibility rating="costly">MCP apply signature/session flow changes while preserving renderer behavior.</reversibility>
  <done>one native MCP path exercises strict input, complete proof, one writer, and journaled write.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Owned/ECC-adjacent skills와 Claude policy mutation 통합</name>
  <files>src/core/owned-skills.ts, src/core/skill-policy.ts, test/skill-policy.test.ts</files>
  <read_first>
    src/core/owned-skills.ts
    src/core/skill-policy.ts
    src/core/ecc-skills.ts
    src/core/transaction.ts
    test/skill-policy.test.ts
  </read_first>
  <action>Owned skill and Claude policy plan/apply paths에 complete path/source/config proof, reviewed plan digest, optional MutationSession을 추가한다. Exact `applyOwnedSkillSync`와 `applyClaudeSkillPolicy` APIs는 standalone에서 complete preflight 뒤 own session을 acquire하고, managed install에서 caller session/operation digest를 require하여 lock을 다시 잡지 않는다. Existing native merge semantics and unrelated user settings preservation을 유지하며 invalid/changed input은 transaction 전에 refuse한다.</action>
  <acceptance_criteria>standalone/nested applies share correct lock behavior, dry-runs are byte-identical, drift/escape/invalid native config leaves target unchanged, and unrelated settings survive.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/skill-policy.test.js dist/test/install.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, or user-owned settings are removed</fails_when>
  </verify>
  <reversibility rating="costly">internal apply contracts gain session/proof requirements while preserving rendered content.</reversibility>
  <done>skill/policy writers cannot bypass the shared safety boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Existing isolation/update direct mutations를 session-bound transaction으로 전환</name>
  <files>src/core/isolation.ts, src/core/update.ts, test/isolation.test.ts, test/update.test.ts</files>
  <read_first>
    src/core/isolation.ts
    src/core/update.ts
    src/core/transaction.ts
    src/core/path-boundary.ts
    test/isolation.test.ts
    test/update.test.ts
  </read_first>
  <action>Existing `applyIsolationManifest`, `syncIsolationRuntime`, `cleanIsolationRuntime`, `writeCandidate` direct write/rm/rename paths를 complete plan plus optional MutationSession and shared transaction primitives로 이동한다. Update preview reads and compares only; stage apply binds checkout/package root/temp/target/source hashes. Preserve existing supported isolation semantics only; do not add project-pack apply/remove, tree-off preload, full uninstall, general repair, or sealed fallback.</action>
  <acceptance_criteria>preview/status/doctor work during active writer with unchanged digests; apply/clean/stage block on unsafe/active/needs-repair/drift and valid operations journal under one session.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/isolation.test.js dist/test/update.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, or preview/blocked cases alter checkout, runtime, state, or native config</fails_when>
  </verify>
  <reversibility rating="costly">direct mutations become journaled/session-bound; user-visible existing commands remain unchanged.</reversibility>
  <done>all remaining existing mutation seams honor proof, serialization, journaling, and preview guarantees.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| standalone sync/clean/stage→owned and user-managed files | multiple existing commands mutate native/config/runtime/checkout state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-28 | Tampering/Elevation | direct file mutation callers | critical | mitigate | complete proof, one session, transaction primitives, immediate recheck |
| T-01-29 | Information Disclosure | native merge/update results | high | mitigate | strict input and central redacted serialization contracts |
</threat_model>

## Artifacts this phase produces

| Kind | Artifact | Contract |
|------|----------|----------|
| Extended APIs | MCP/owned-skill/policy/isolation/update apply functions | reviewed plan plus optional caller `MutationSession` |

## Probe coverage notes

- Generic path/config attacks stay in STRIDE; avoiding silent Phase 2+ feature expansion is retained as a descriptor-less product prohibition.

<verification>
Focused MCP, policy, isolation, update, install, transaction suites pass; no direct unproven mutation path remains in named callers.
</verification>

<success_criteria>
- all existing public mutations default to read-only plans.
- standalone/nested applies use the correct single session lifetime.
- later-phase behavior remains absent.
</success_criteria>

<output>
Create `.planning/phases/01-safe-operation-boundary/01-13-SUMMARY.md` when done.
</output>
