---
phase: 01-safe-operation-boundary
plan: 16
type: execute
wave: 7
depends_on: [01-14, 01-15]
files_modified:
  - scripts/build-artifact.mjs
  - scripts/install.ps1
  - scripts/install.sh
  - scripts/update.ps1
  - scripts/update.sh
  - package.json
  - test/preview.test.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-03, SAFE-04, SAFE-05, SAFE-06]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "All four wrapper surfaces use a verified existing artifact for preview and apply, and wrappers themselves execute no git/npm/build/link or managed/native mutation."
    - "D-02/D-04: explicit wrapper apply delegates its complete reviewed plan to the core bootstrap service, which acquires one MutationSession before every external or native mutation."
    - "Three-OS CI proves byte-identical preview, fail-closed prerequisites, one-session apply ordering, redaction, validation, path, process, and crash behavior."
  artifacts:
    - path: scripts/build-artifact.mjs
      provides: "Deterministic build-input manifest writer/checker"
    - path: scripts/install.ps1
      provides: "Thin verified-artifact install wrapper"
    - path: scripts/update.sh
      provides: "Thin verified-artifact update wrapper"
    - path: .github/workflows/ci.yml
      provides: "Windows/macOS/Linux wrapper and Phase 1 behavioral gate"
  key_links:
    - from: scripts/install.sh
      to: src/cli.ts
      via: "artifact check followed only by bootstrap plan/apply CLI invocation"
      pattern: "build-artifact.*check|bootstrap.*install"
    - from: scripts/update.ps1
      to: src/core/bootstrap.ts
      via: "CLI bootstrap update route owns all git/npm/build/link and managed mutation"
      pattern: "bootstrap.*update"
    - from: .github/workflows/ci.yml
      to: test/preview.test.ts
      via: "native wrapper digest and session-order tests on three OSes"
      pattern: "preview.test"
    - from: .github/workflows/ci.yml
      to: src/core/mcp-proxy.ts
      via: "focused proxy and fixture process-policy tests execute on the three-OS matrix"
      pattern: "process.test|mcp-proxy|fixture"
  prohibitions:
    - statement: "A wrapper MUST NOT fall back to direct git, npm, build, link, or native mutation when the verified artifact or complete bootstrap plan is unavailable."
      requirement_id: SAFE-01
      category: safety
      status: unverified
      flagged: true
---

<objective>
verified-artifact-only thin wrappers와 three-OS behavioral gate로 Phase 1의 user-visible safety boundary를 닫는다.

Purpose: wrapper가 core PathProof/MutationSession orchestration을 우회할 수 없고 실제 OS별 evidence가 이를 검증하게 한다.
Output: artifact checker, four thin wrappers, complete preview/session tests, and cross-platform CI gate.
</objective>

<execution_context>
@C:/Users/alpha/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-safe-operation-boundary/01-CONTEXT.md
@.planning/phases/01-safe-operation-boundary/01-RESEARCH.md
@.planning/phases/01-safe-operation-boundary/01-VALIDATION.md
@.planning/phases/01-safe-operation-boundary/01-14-SUMMARY.md
@.planning/phases/01-safe-operation-boundary/01-15-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Verified artifact에서 install wrapper core delegation까지 tracer</name>
  <files>scripts/build-artifact.mjs, scripts/install.ps1, scripts/install.sh, package.json, test/preview.test.ts</files>
  <read_first>
    scripts/install.ps1
    scripts/install.sh
    scripts/build-artifact.mjs
    package.json
    src/cli.ts
    src/core/bootstrap.ts
    test/preview.test.ts
  </read_first>
  <behavior>
    - artifact write/check binds the exact source/build inputs and check mode changes no bytes.
    - install preview checks the existing artifact and invokes only `bootstrap install` plan.
    - install explicit apply invokes only `bootstrap install --apply`; git/npm/build/link and managed/native calls are observed solely inside the already-acquired core session.
  </behavior>
  <action>SAFE-01 and D-02/D-04에 따라 deterministic `scripts/build-artifact.mjs write|check`를 완성하고 build가 manifest를 생성하게 한다. Both install wrapper families validate Node and the existing dist/source manifest, then delegate preserved target/skip-link arguments to the CLI `bootstrap install` route. Wrappers contain no direct npm ci/build/link, file cleanup, or native apply branch. Missing/stale artifacts or refusal from the core plan exit with a stable prerequisite code and unchanged checkout/package/link/config/state/harness digests.</action>
  <acceptance_criteria>PowerShell/POSIX install preview and apply command spies observe only artifact check plus one CLI delegation; all mutation subprocesses occur after the core session event and share its token.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/preview.test.js --test-name-pattern="install wrapper|artifact|session"</automated>
    <fails_when>non-zero exit, output reports 0 matching tests, wrapper invokes a mutation command directly, or any preview digest changes</fails_when>
  </verify>
  <reversibility rating="reversible">wrapper and manifest generation changes can be reverted together without touching user state.</reversibility>
  <done>both install wrappers are verified-artifact launchers for the core one-session operation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update wrappers를 verified-artifact core delegation으로 전환</name>
  <files>scripts/update.ps1, scripts/update.sh, test/preview.test.ts</files>
  <read_first>
    scripts/update.ps1
    scripts/update.sh
    scripts/build-artifact.mjs
    src/cli.ts
    src/core/bootstrap.ts
    test/preview.test.ts
  </read_first>
  <behavior>
    - update preview checks the current verified artifact and invokes only `bootstrap update` plan; checkout and object database remain unchanged.
    - explicit apply invokes only `bootstrap update --apply`; git pull, npm ci, build, link, and managed reconcile are executed by core after full preflight/session acquisition.
    - dirty checkout, unavailable exact target object, stale artifact, unsafe path, active writer, and needs-repair all refuse without wrapper fallback.
  </behavior>
  <action>SAFE-01 and D-01~D-07에 따라 both update wrappers를 artifact check plus CLI `bootstrap update` delegation으로 제한한다. Preserve apply/target argument forms, but remove direct dirty-check/git pull/npm ci/build/link/reconcile execution from shell code; these checks and actions belong to the complete core plan/apply service. Propagate stable structured refusal status and never attempt an alternate shell mutation path.</action>
  <acceptance_criteria>PowerShell/POSIX update command spies see no direct mutation command; preview, refusal, interruption, and concurrent cases preserve every digest; valid apply observes session-before-git and one shared token through reconcile.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/preview.test.js --test-name-pattern="update wrapper|concurrent|interrupted"</automated>
    <fails_when>non-zero exit, output reports 0 matching tests, wrapper executes git/npm directly, or a refused case changes bytes</fails_when>
  </verify>
  <reversibility rating="reversible">thin wrapper routing is source-controlled and carries no independent persistent state.</reversibility>
  <done>both update wrappers delegate their entire operation to the reviewed core safe-apply boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Three-OS Phase 1 behavioral and source-coverage gate</name>
  <files>.github/workflows/ci.yml, test/preview.test.ts</files>
  <read_first>
    .github/workflows/ci.yml
    test/preview.test.ts
    .planning/phases/01-safe-operation-boundary/01-VALIDATION.md
    src/core/mcp-proxy.ts
    src/core/gsd-fixture.ts
    src/core/ecc-fixture.ts
    src/core/mcp-fixture.ts
    test/process.test.ts
    test/install.test.ts
    package.json
  </read_first>
  <action>Existing ubuntu/macos/windows matrix에서 `npm run check`, full suite, native install/update wrapper preview/apply ordering, and focused path/process/crash/redaction/validation tests를 실행한다. Process coverage에는 fake upstream을 사용한 MCP proxy explicit env-name allowlist, bounded redacted protocol/stderr evidence, whole-output fingerprint, initialize/request/idle timeout termination을 포함하고, install coverage에는 GSD/ECC/MCP fixture의 complete preflight→one session→first mkdir/package/MCP process ordering을 포함한다. Wrapper syntax checks를 checkout/package/link/config/state/harness digest 및 session-event behavior assertions로 확장한다. Privileged reparse fixture unavailable은 structured not-run artifact로 남기고 pass count에서 제외하며, CI log/result는 central redacted aliases only를 사용한다.</action>
  <acceptance_criteria>each OS executes native wrapper behavior, full tests, MCP proxy policy tests, and all-three-fixture preflight/session-order tests; changed digest, default/full environment inheritance, raw/inherited stderr, missing timeout/fingerprint evidence, pre-session fixture mutation, direct wrapper mutation, leaked sentinel, 0-test selection, or unsupported-as-pass fails CI.</acceptance_criteria>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, summary reports 0 tests, any required suite is omitted, or a not-run privileged fixture is counted as passed</fails_when>
  </verify>
  <reversibility rating="reversible">CI/test changes create evidence only and do not deploy or release artifacts.</reversibility>
  <done>Phase 1 has executable Windows/macOS/Linux evidence for wrappers and every safety boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wrapper→verified CLI artifact | shell entrypoints choose plan or explicit apply without owning mutation |
| CI host→support claim | OS-specific tests determine whether a behavior is passed, refused, or not-run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-34 | Elevation/Tampering | install/update wrappers | critical | mitigate | verified artifact check and CLI-only delegation to core session boundary |
| T-01-35 | Repudiation/Information Disclosure | CI evidence | high | mitigate | digest/session assertions, explicit not-run state, central redaction |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan(s) | Status |
|--------|----|----------------------|---------|--------|
| GOAL | — | preview, safe refusal, recoverable state, no observable secrets | 01-01~01-16 | COVERED |
| REQ | SAFE-01 | zero-mutation preview for every mutating surface | 01-01, 01-13~01-16 | COVERED |
| REQ | SAFE-02 | link/reparse/alias/parent-change containment | 01-02, 01-07, 01-09, 01-12~01-16 | COVERED |
| REQ | SAFE-03 | serialized durable crash-diagnosable writes | 01-02, 01-09, 01-12~01-16 | COVERED |
| REQ | SAFE-04 | redaction across every observable/persisted surface | 01-01, 01-03, 01-08~01-16 | COVERED |
| REQ | SAFE-05 | strict ambiguous/versioned document refusal | 01-01, 01-04~01-06, 01-10, 01-13, 01-15~01-16 | COVERED |
| REQ | SAFE-06 | shell-free bounded timeout/env-allowlisted process | 01-02, 01-08, 01-11, 01-12, 01-14~01-16 | COVERED |
| RESEARCH | — | Wave 0 six adversarial suites and three-OS behavior | 01-01, 01-02, 01-16 | COVERED |
| RESEARCH | — | parse→schema→domain/version/extension architecture | 01-05, 01-06, 01-10 | COVERED |
| RESEARCH | — | canonical proof and operation-scoped writer session | 01-07, 01-09, 01-12~01-16 | COVERED |
| RESEARCH | — | central redaction, opaque snapshots, local support bundle | 01-03, 01-15 | COVERED |
| RESEARCH | — | unified process adapter, SDK MCP proxy transport, and migrated fixture/install callers | 01-08, 01-11, 01-14~01-16 | COVERED |
| RESEARCH | — | wrappers use verified artifact without preview bootstrap | 01-01, 01-14~01-16 | COVERED |
| CONTEXT | D-01 | fail closed when filesystem safety is unprovable | 01-02, 01-07, 01-14~01-16 | COVERED |
| CONTEXT | D-02 | complete preflight before any mutation | 01-02, 01-07, 01-09, 01-12~01-16 | COVERED |
| CONTEXT | D-03 | dual containment and immediate stability recheck | 01-02, 01-07, 01-09, 01-12~01-15 | COVERED |
| CONTEXT | D-04 | one state-root writer, readers remain available | 01-02, 01-09, 01-12~01-16 | COVERED |
| CONTEXT | D-05 | competing mutation fails immediately with metadata | 01-02, 01-09, 01-14~01-16 | COVERED |
| CONTEXT | D-06 | no PID/age stale unlock; needs-repair | 01-02, 01-09, 01-14~01-16 | COVERED |
| CONTEXT | D-07 | recovery is preview then explicit repair --apply | 01-02, 01-09, 01-15~01-16 | COVERED |
| CONTEXT | D-08 | closed core schemas; namespaced extensions only | 01-01, 01-05, 01-06, 01-09, 01-10 | COVERED |
| CONTEXT | D-09 | unregistered extensions preserved/displayed/inert | 01-01, 01-05, 01-06, 01-10 | COVERED |
| CONTEXT | D-10 | old read-only migration; unknown newer refusal | 01-01, 01-05, 01-06, 01-10 | COVERED |
| CONTEXT | D-11 | bounded sorted coded redacted validation issues | 01-01, 01-05, 01-06, 01-10 | COVERED |
| CONTEXT | D-12 | bounded redacted process excerpts/fingerprint/metadata | 01-01~01-03, 01-08, 01-11, 01-12, 01-14~01-16 | COVERED |
| CONTEXT | D-13 | exact/name/URL/PEM/JWT/API-key typed redaction | 01-01, 01-03, 01-15 | COVERED |
| CONTEXT | D-14 | consistent home/temp/project aliases | 01-01, 01-03, 01-15~01-16 | COVERED |
| CONTEXT | D-15 | explicit local bundle; never auto-upload | 01-01, 01-03, 01-15 | COVERED |

<verification>
`npm test` passes locally, all focused commands named in 01-VALIDATION.md pass, and three-OS CI executes native wrapper behavior rather than syntax checks alone.
</verification>

<success_criteria>
- wrappers are verified-artifact launchers and contain no mutation path outside core safe apply.
- explicit apply creates one session before git/npm/build/link or managed/native mutation.
- source audit covers the ROADMAP goal, SAFE-01~06, research constraints, and D-01~D-15 without Phase 2+ behavior.
</success_criteria>

<output>
Create `.planning/phases/01-safe-operation-boundary/01-16-SUMMARY.md` when done.
</output>
