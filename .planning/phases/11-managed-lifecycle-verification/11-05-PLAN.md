---
phase: 11-managed-lifecycle-verification
plan: 05
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - .planning/phases/11-managed-lifecycle-verification/evidence/probes/life04-receipts.mjs
  - .planning/phases/11-managed-lifecycle-verification/evidence/life04-receipts.txt
  - .planning/phases/11-managed-lifecycle-verification/evidence/LIFE-04-findings.md
  - .planning/phases/11-managed-lifecycle-verification/evidence/probes/life07-external.mjs
  - .planning/phases/11-managed-lifecycle-verification/evidence/life07-external.txt
  - .planning/phases/11-managed-lifecycle-verification/evidence/LIFE-07-findings.md
autonomous: true
requirements: [LIFE-09]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "LIFE-04 is judged on its full text (D-09): removal must be limited to receipted alpha-AOS-owned bytes or semantic entries that still match their recorded applied state, and drifted or user-modified resources must cause a refusal; a refusal that only covers syntactic tampering is recorded as partial coverage, not as the clause."
    - "Research leads P1, P2 and P4 are re-run through the packed CLI and become gap candidates only if this plan's own transcript reproduces them (D-06): P1 same-named user-authored MCP entries in a state root with no journal, for all five harnesses (D-10); P2 a user-edited managed ECC skill; P4 an edit made between the uninstall preview and its apply."
    - "The syntactic-tamper refusal is re-proven with a byte fingerprint (exit 2 and zero changed bytes), so the existing `tampering and missing closing markers` oracle is confirmed at CLI level (D-04)."
    - "LIFE-07 'shown verified external package changes' is judged on a real external change in a sandbox (the locked ECC runtime installed by alpha-AOS itself, not pre-seeded), comparing the printed change list with the installed package version on disk."
    - "LIFE-07 'component-specific recovery instructions' is judged on an induced install failure after an external change and on the uninstall compensation output and recovery receipt, each compared with the stable lock's real package names and install paths (`ecc-universal`, `pi-mcp-adapter`, `<config root>/gsd-core`, `alpha-aos`)."
    - "`LIFE-04-findings.md` cites `.planning/debug/ci02-windows-alpha-aos-host-leak.md` as the Phase 10 test-isolation-leak classification with fix commits 8f17c7e and 7a3ee28, and names no product violation for it (D-13)."
    - "A defect that fails both a LIFE-03 and a LIFE-04 clause is recorded as a separate LIFE-04 gap candidate that cross-references the LIFE-03 key, never merged and never dropped (adjacency edge, LIFE-09)."
    - "`src/` is untouched (D-05)."
  prohibitions:
    - statement: "MUST NOT use real MCP credentials, API keys or authentication files in the user-authored fixtures; every user-authored entry uses synthetic command names such as `p11-user-context7.js` and no environment values."
      category: privacy
      status: resolved
      verification: judgment
    - statement: "MUST NOT promote a code-reading suspicion to a gap candidate: every LIFE-04 and LIFE-07 candidate quotes its own reproduction from this plan's transcripts (D-06)."
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT run install, uninstall or the induced-failure setup against real harness roots or the real global npm prefix; external package installs land only in the sandbox npm prefix."
      category: safety
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/phases/11-managed-lifecycle-verification/evidence/life04-receipts.txt"
      provides: "Receipt and applied-state evidence for uninstall: no-receipt removal, user-modified entries, edit-after-preview, syntactic tamper refusal"
      contains: "life04.no-receipt."
    - path: ".planning/phases/11-managed-lifecycle-verification/evidence/life07-external.txt"
      provides: "External change reporting, induced failure guidance, compensation commands versus the stable lock"
      contains: "life07.compensation."
    - path: ".planning/phases/11-managed-lifecycle-verification/evidence/LIFE-04-findings.md"
      provides: "LIFE-04 clause table, D-13 citation, gap candidates"
      contains: "ci02-windows-alpha-aos-host-leak.md"
    - path: ".planning/phases/11-managed-lifecycle-verification/evidence/LIFE-07-findings.md"
      provides: "LIFE-07 clause table per component (GSD, ECC, npm-link, Pi bridge), gap candidates"
      contains: "LIFE-07/C"
  key_links:
    - from: "life07-external.mjs compensation checks"
      to: "installed package catalog/stack.lock.json"
      via: "expected package names and paths are read from the packed stable lock, not typed by hand"
      pattern: "stack.lock.json"
    - from: "LIFE-04-findings.md"
      to: "LIFE-03-findings.md"
      via: "cross-referenced candidate keys for shared defects"
      pattern: "LIFE-03/C"
---

<objective>
Produce the LIFE-04 evidence (removal limited to receipted, still-matching alpha-AOS state; refusal on drift or user modification) and the LIFE-07 evidence (verified external package changes and component-specific recovery instructions for GSD, ECC, npm-link and the Pi bridge).

Purpose: research reproduced, at module level, uninstall removing same-named user entries with no receipt, removing a user-edited skill, ignoring the previewed hash, and compensation commands naming packages that are not in the stable lock. Under D-06 these are leads until this phase reproduces them through the packed CLI.

Output: two probes and transcripts, `LIFE-04-findings.md`, `LIFE-07-findings.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-managed-lifecycle-verification/11-CONTEXT.md
@.planning/phases/11-managed-lifecycle-verification/11-RESEARCH.md
@.planning/phases/11-managed-lifecycle-verification/evidence/README.md
@.planning/phases/11-managed-lifecycle-verification/11-01-SUMMARY.md
@.planning/phases/11-managed-lifecycle-verification/evidence/seed-selfcheck.txt
@.planning/debug/ci02-windows-alpha-aos-host-leak.md
@src/core/uninstall.ts
@src/core/recovery-receipt.ts
</context>

<interfaces>
Paths: EV = `.planning/phases/11-managed-lifecycle-verification/evidence`, PROBES = `EV/probes`.

From plan 11-01 (read-only here; do not modify `probe-lib.mjs` or `test/helpers/packed-sandbox.ts`): `newSandbox`, `assertSandboxEnv`, `runCli`, `fingerprintRoots`, `compareFingerprints`, `withHostGuard`, `createTranscript`, `packOnce`; `installPackedRelease`, `seedHarnessPrerequisites`, `harnessPaths`, `hostNpmCache`, `snapshotTargets`, `describeHostDrift`.

Product facts (research, to be re-observed, not assumed):
- src/core/uninstall.ts:225-226 fixed-name lists; line 320 records `currentHash` in the plan; lines 486-512 apply recomputes the prune from current content; `SemanticPruneDriftError` for missing TOML end marker, malformed Markdown markers, invalid JSON/YAML; line 417 GSD compensation `rm -rf ~/.claude/get-shit-done ~/.codex/get-shit-done`; lines 566-568 `--all` deletes `snapshots/`.
- src/core/mcp.ts:296-314 `stripManagedToml` also strips unmarked `[mcp_servers.context7|exa|firecrawl]` tables.
- src/core/recovery-receipt.ts: `createRecoveryReceipt`, `writeRecoveryReceipt`, `compensateGsd`, `compensateEcc`, `compensatePiBridge`, `compensateNpmLink`; research found `writeRecoveryReceipt` referenced only from src/core/uninstall.ts.
- src/cli.ts:509 prints `External verified changes retained outside journal rollback: <ids or none>`; src/core/install.ts:862-876 failure message `External verified changes retained: <ids>`.
- Stable lock (read from the installed package): ECC package `components.ecc.package`, Pi bridge `components.mcpBridges.pi.package`@`version`, GSD `components.gsd.package`@`version`; GSD installs under `<config root>/gsd-core` (src/core/install.ts:120-126); npm package name `alpha-aos` (package.json).
- test/recovery-receipt.test.ts:109-140 pins `npm uninstall -g @enterprise-coding-companion/companion` and `npm unlink @modelcontextprotocol/server-pi`.

User-authored fixture recipe (synthetic, no credentials): derive each harness's native MCP shape from the packed `renderMcpConfig(harness, "", lock)` output, then rewrite each managed server (context7, exa, firecrawl) to a user-authored definition whose command is `node` and whose single argument is `p11-user-<server>.js`, and remove any alpha-AOS ownership markers where the format has them (TOML and Markdown). User-authored ECC-named skills: `<eccSkillRoot>/<skill>/SKILL.md` with the synthetic text `p11 user-authored skill`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: LIFE-04 — receipt, applied-state and refusal evidence through the packed CLI, and the LIFE-04 findings</name>
  <files>.planning/phases/11-managed-lifecycle-verification/evidence/probes/life04-receipts.mjs, .planning/phases/11-managed-lifecycle-verification/evidence/life04-receipts.txt, .planning/phases/11-managed-lifecycle-verification/evidence/LIFE-04-findings.md</files>
  <read_first>
    src/core/uninstall.ts (whole file)
    src/core/mcp.ts (lines 280-320 managed TOML stripping)
    test/uninstall.test.ts (lines 105-222 semantic pruning and tampering tests, 250-312 journal and purge tests)
    .planning/phases/11-managed-lifecycle-verification/11-RESEARCH.md (Evidence Map LIFE-04, Research-Time Lead Reproductions P1, P2, P4)
    .planning/phases/11-managed-lifecycle-verification/evidence/README.md
    .planning/phases/11-managed-lifecycle-verification/evidence/suites-local.txt
    .planning/phases/11-managed-lifecycle-verification/evidence/ci-35818198049-lifelines.txt
  </read_first>
  <action>
    Per D-01, D-04, D-06, D-09, D-10 and D-13, write PROBES/life04-receipts.mjs inside `withHostGuard` with one packed tarball.
    1. No receipt (P1), one fresh sandbox per harness in the order claude, codex, antigravity, pi, hermes: `installPackedRelease` only (no alpha-AOS install, so the state root has no journal); write the user-authored MCP config and the user-authored ECC-named skills from the interfaces recipe; record that `<state>/journal` is absent or empty. Run `uninstall --target <h> --json` (preview) and `uninstall --target <h> --yes --apply --json`, fingerprinting before and after. Checks: `life04.no-receipt.<h>.planned` OBSERVED (which user-authored names the preview planned to prune), `life04.no-receipt.<h>.entries` (HOLDS when every user-authored MCP entry is byte-for-byte still present; VIOLATED naming the removed entries) and `life04.no-receipt.<h>.skills` (same for the ECC-named user skills).
    2. User-modified managed skill (P2): a sandbox with `seedHarnessPrerequisites(..., "codex", { eccCache: hostNpmCache() })` and `install --target codex --apply --json`; then run `ecc-skills sync --target codex --apply --json` and record whether it journaled the skills (OBSERVED `life04.modified.skill-receipted`; if the command cannot complete offline, say so and continue with the seeded skills). Append the synthetic line `p11 user edit` to `<home>/.agents/skills/deep-research/SKILL.md`, then `uninstall --target codex --yes --apply --json`: check `life04.modified.skill` HOLDS when the command refuses (non-zero exit) and the file is byte-identical, VIOLATED when the edited file is removed.
    3. User-modified managed MCP entry: in a fresh codex sandbox set up as in step 2, change one argument of the managed `exa` entry in `<CODEX_HOME>/config.toml` to `p11-user-arg` while keeping every alpha-AOS marker intact; `uninstall --target codex --yes --apply --json`: check `life04.modified.mcp-entry` (HOLDS on refusal with zero changed bytes, VIOLATED if the edited entry is removed).
    4. Edit after preview (P4): fresh codex sandbox as in step 2; run `uninstall --target codex --json` and record the planned `currentHash` values; then add a user server `p11-user-server` to `config.toml` outside the managed markers; run `uninstall --target codex --yes --apply --json`: check `life04.preview-hash.enforced` HOLDS when apply refuses because the file no longer matches the preview, VIOLATED when it proceeds (detail: planned hash prefix versus pre-apply hash prefix, and whether `p11-user-server` survived).
    5. Syntactic tamper (positive control): fresh codex sandbox as in step 2; delete the managed end marker line from `config.toml`; `uninstall --target codex --yes --apply`: checks `life04.tamper.refuses` (exit 2 and stderr names the drift) and `life04.tamper.zero-bytes-changed` (fingerprint of home and state unchanged).
    6. Write EV/life04-receipts.txt. Then write EV/LIFE-04-findings.md from the README template: contract verbatim from .planning/milestones/v0.1.0-REQUIREMENTS.md:80; clause rows `only receipted alpha-AOS-owned bytes`, `or semantic entries`, `that still match their recorded applied state`, `drifted resources cause a refusal`, `user-modified resources cause a refusal`, each citing CHECK ids and the Phase 6 test titles with their CI job lines; oracle audit (D-04): `semantic pruning fidelity across TOML, JSON, YAML, and Markdown` and `tampering and missing closing markers safely refuse with SemanticPruneDriftError` cover syntax only, and `full stack uninstall preserves historical transaction journals by default` is sound but `--all` deletes `snapshots/` (record as an observation); a `## Host safety (D-13)` paragraph citing `.planning/debug/ci02-windows-alpha-aos-host-leak.md`, commits 8f17c7e and 7a3ee28, and this plan's host-guard results; core clause `only receipted ... that still match their recorded applied state`; provisional verdict per D-02; one `LIFE-04/C<k>` candidate per failing clause with its reproduction, cross-referencing a `LIFE-03/C<k>` key by name where plan 11-04 records the same defect (read EV/LIFE-03-findings.md if it already exists; otherwise note the expected overlap for plan 11-07 to reconcile).
  </action>
  <verify>
    <automated>npm run build && node .planning/phases/11-managed-lifecycle-verification/evidence/probes/life04-receipts.mjs && T=.planning/phases/11-managed-lifecycle-verification/evidence/life04-receipts.txt && grep -q '^host-guard: unchanged' "$T" && for h in claude codex antigravity pi hermes; do grep -qE "^CHECK life04\.no-receipt\.$h\.entries (HOLDS|VIOLATED|NOT-OBSERVED) " "$T" || exit 1; done && for c in modified.skill modified.mcp-entry preview-hash.enforced tamper.refuses tamper.zero-bytes-changed; do grep -qE "^CHECK life04\.$c (HOLDS|VIOLATED|NOT-OBSERVED) " "$T" || exit 1; done</automated>
    <fails_when>non-zero exit, no unchanged host guard line, a harness without its no-receipt entries check, or any of the five named checks missing</fails_when>
    <automated>F=.planning/phases/11-managed-lifecycle-verification/evidence/LIFE-04-findings.md && grep -q '^## Provisional verdict' "$F" && grep -q '^## Host safety (D-13)' "$F" && grep -q 'ci02-windows-alpha-aos-host-leak.md' "$F" && grep -q 'still match their recorded applied state' "$F" && test "$(grep -cE 'life04\.(no-receipt|modified|preview-hash|tamper)\.' "$F")" -ge 8 && test -z "$(git status --porcelain -- src)"</automated>
    <fails_when>verdict heading, D-13 section or citation, or the contract clause missing, fewer than eight check citations, or src/ changed</fails_when>
  </verify>
  <acceptance_criteria>
    - EV/life04-receipts.txt carries fifteen `life04.no-receipt.*` checks (planned, entries and skills for five harnesses) and the five named single checks.
    - No transcript line carries a user-authored fixture body; fixtures appear as path plus hash, and the synthetic command names appear only inside CLI output the product printed.
    - Each P1, P2 and P4 lead is either a `LIFE-04/C<k>` candidate with this plan's reproduction or listed under `## Leads (not reproduced)` with the observed output that failed to reproduce it.
    - `git status --porcelain -- src` prints nothing.
  </acceptance_criteria>
  <done>LIFE-04 has CLI-level evidence for every clause and a findings file whose candidates rest on this phase's own reproductions.</done>
</task>

<task type="auto">
  <name>Task 2: LIFE-07 — verified external changes and component-specific recovery instructions for GSD, ECC, npm-link and the Pi bridge</name>
  <precondition>The npm registry is reachable (alpha-AOS installs the locked ECC runtime into the sandbox prefix itself; the Pi bridge attempt runs `pi install` against the sandbox PI_CODING_AGENT_DIR). An unreachable registry makes the dependent checks NOT-OBSERVED with the output, not VIOLATED.</precondition>
  <files>.planning/phases/11-managed-lifecycle-verification/evidence/probes/life07-external.mjs, .planning/phases/11-managed-lifecycle-verification/evidence/life07-external.txt, .planning/phases/11-managed-lifecycle-verification/evidence/LIFE-07-findings.md</files>
  <read_first>
    src/core/install.ts (lines 500-700 installGsd, installEccRuntime, installPiBridge; 700-878 applyManagedInstall and its failure message)
    src/core/recovery-receipt.ts (whole file)
    src/core/uninstall.ts (lines 405-430 compensation, receipt writing in apply)
    src/cli.ts (lines 489-513 install output)
    test/recovery-receipt.test.ts (lines 21-140)
    .planning/phases/11-managed-lifecycle-verification/11-RESEARCH.md (Evidence Map LIFE-07, P7 and P7b)
    .planning/phases/11-managed-lifecycle-verification/evidence/README.md
  </read_first>
  <action>
    Per D-01, D-04, D-06 and D-09 (LIFE-07: "shown verified external package changes"), write PROBES/life07-external.mjs inside `withHostGuard` with one packed tarball; read every expected package name and version from the installed package's `catalog/stack.lock.json`.
    1. Real external change (ECC runtime): a fresh sandbox, `installPackedRelease`; seed only the codex GSD files and the codex MCP config (via `harnessPaths` and the packed `renderMcpConfig`), leaving the ECC runtime absent from the prefix. Run `install --target codex --apply` (human) in one sandbox and `--json` in a twin sandbox. Checks: `life07.external.shown` (HOLDS when the human output's `External verified changes retained outside journal rollback:` line and the JSON `externalChanges` name the ECC runtime step), `life07.external.verified` (HOLDS when `<prefix>/node_modules/<ecc package>/package.json` on win32, or `<prefix>/lib/node_modules/...` elsewhere, has the locked version; VIOLATED when the reported change does not match the disk), `life07.external.recovery-shown` (HOLDS when the success output names how to reverse or recover the ECC runtime change; VIOLATED otherwise).
    2. Induced failure after an external change: a fresh sandbox prepared as in step 1; read the dry-run `install --target codex --json` step order; before applying, create a directory at the target path of the first alpha-AOS-owned step that runs after `ecc:runtime` (for example `<home>/.agents/skills/deep-research/SKILL.md`), so that step fails after the ECC runtime was installed. Run `install --target codex --apply`: checks `life07.failure.names-external` (non-zero exit and stderr contains `External verified changes retained` naming the ECC runtime), `life07.failure.component-instructions` (HOLDS when stderr or a written receipt gives an ECC-specific recovery instruction naming the locked package; VIOLATED otherwise), `life07.failure.receipt-written` OBSERVED (any recovery receipt file under the sandbox state root, by path and hash). If the chosen target does not fail, try the next alpha-AOS-owned step after `ecc:runtime` and record which one was used.
    3. Pi bridge attempt: a fresh sandbox, `installPackedRelease`, `seedHarnessPrerequisites(..., "pi", { eccCache: hostNpmCache() })`, then delete the seeded Pi bridge manifest so the bridge is not current; `install --target pi --apply --json`: check `life07.external.pi-bridge-shown` (HOLDS when `externalChanges` names the bridge step and the manifest now carries the locked version; NOT-OBSERVED when `pi install` fails for an environmental reason, quoting stderr).
    4. Compensation commands: a sandbox seeded for codex and pi with `install --target codex,pi --apply --json`; run `uninstall --all --json` (preview) and `uninstall --all --yes --apply --json`; locate the recovery receipt written by the apply (sandbox state root). From the preview JSON and the receipt, record the verbatim compensation command strings (product-generated instructions, not user data) and checks: `life07.compensation.ecc-package` (HOLDS when the ECC command names the lock's ECC package), `life07.compensation.pi-bridge` (HOLDS when the Pi command names the lock's bridge package and its install route), `life07.compensation.gsd-path` (HOLDS when the GSD command names `<config root>/gsd-core`, the path the seeded GSD install actually occupies), `life07.compensation.npm-link` (HOLDS when the command names `alpha-aos`), `life07.receipt.written` (receipt present).
    5. Write EV/life07-external.txt. Then write EV/LIFE-07-findings.md from the README template: contract verbatim from .planning/milestones/v0.1.0-REQUIREMENTS.md:83; clause rows `shown verified external package changes`, and `component-specific recovery instructions when managed-file rollback cannot safely reverse` one row each for GSD, ECC, npm-link and the Pi bridge, citing CHECK ids and the recovery-receipt test titles with CI job lines; oracle audit (D-04): the compensation-generator tests pin `@enterprise-coding-companion/companion` and `@modelcontextprotocol/server-pi`, which are not the stable lock's packages, so a passing test there proves string formatting, not correctness; note under Observations whether any install or update path reaches the compensation generators (the transcript answers this, not code reading alone); core clause; provisional verdict per D-02; one `LIFE-07/C<k>` candidate per failing clause with its reproduction. For npm-link, where no probe in this phase changes a global link, record the clause from the compensation output only and mark the install-path half NOT-OBSERVED with the reason.
  </action>
  <verify>
    <automated>npm run build && node .planning/phases/11-managed-lifecycle-verification/evidence/probes/life07-external.mjs && T=.planning/phases/11-managed-lifecycle-verification/evidence/life07-external.txt && grep -q '^host-guard: unchanged' "$T" && for c in external.shown external.verified external.recovery-shown failure.names-external failure.component-instructions external.pi-bridge-shown compensation.ecc-package compensation.pi-bridge compensation.gsd-path compensation.npm-link receipt.written; do grep -qE "^CHECK life07\.$c (HOLDS|VIOLATED|NOT-OBSERVED|OBSERVED) " "$T" || exit 1; done</automated>
    <fails_when>non-zero exit, no unchanged host guard line, or any of the eleven named checks missing</fails_when>
    <automated>F=.planning/phases/11-managed-lifecycle-verification/evidence/LIFE-07-findings.md && grep -q '^## Provisional verdict' "$F" && for c in GSD ECC npm-link 'Pi bridge'; do grep -q "$c" "$F" || exit 1; done && grep -q 'enterprise-coding-companion' "$F" && test "$(grep -cE 'life07\.(external|failure|compensation|receipt)\.' "$F")" -ge 8 && test -z "$(git status --porcelain -- src)"</automated>
    <fails_when>verdict heading missing, a component row missing, the mismatched-oracle note missing, fewer than eight check citations, or src/ changed</fails_when>
  </verify>
  <acceptance_criteria>
    - Every expected package name in the compensation checks was read from the installed package's stable lock, and the transcript notes which lock field each came from.
    - The induced failure left no change outside the sandbox (unchanged host guard) and the transcript names the step that was made to fail.
    - LIFE-07-findings.md has one row per component (GSD, ECC, npm-link, Pi bridge) plus the shown-verified row.
    - `git status --porcelain -- src` prints nothing.
  </acceptance_criteria>
  <done>LIFE-07 has real external-change evidence, an induced-failure observation and lock-grounded compensation checks, with a findings file ready for plan 11-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe to npm registry | alpha-AOS installs the locked ECC runtime and the Pi bridge from the registry into the sandbox prefix |
| probe to real global npm prefix | A missing `npm_config_prefix` override would install into the user's real global prefix |
| fixtures to transcripts | User-authored MCP entries must not carry credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-18 | Tampering | life07 installs | critical | mitigate | `runCli` containment assertion includes `npm_config_prefix`; host guard wraps the probe; installs only through the packed CLI with the sandbox env |
| T-11-19 | Tampering | locked packages from registry | medium | accept | Only the stable-lock versions the product itself pins are installed, by the product's own fixture-gated installer, into a disposable prefix; no new dependency enters the repository |
| T-11-20 | Information disclosure | user-authored fixtures | medium | mitigate | Synthetic command names only, no env values; transcripts show fixture paths and hashes |
| T-11-21 | Repudiation | gap candidates | medium | mitigate | D-06: each candidate quotes this plan's own reproduction; unreproduced research leads stay under Leads |
| T-11-SC | Tampering | npm installs of `ecc-universal` and `pi-mcp-adapter` | low | accept | Not new packages: both are already pinned with integrity hashes in `catalog/stack.lock.json` and installed only into a sandbox by the product under test (research Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Both transcripts end with a `RESULT:` line and an unchanged host guard.
- Both findings files pass their grep gates.
- `git status --porcelain -- src` prints nothing.
</verification>

<success_criteria>
- Every LIFE-04 and LIFE-07 clause has a HOLDS row or a named gap candidate with this plan's reproduction.
- ROADMAP Phase 11 success criterion 2 holds for LIFE-04 (Phase 10 classification cited, no product violation named for it).
</success_criteria>

## Artifacts this phase produces

- Probes `PROBES/life04-receipts.mjs`, `PROBES/life07-external.mjs`.
- Transcripts `EV/life04-receipts.txt`, `EV/life07-external.txt`.
- Findings `EV/LIFE-04-findings.md` (with `## Host safety (D-13)`), `EV/LIFE-07-findings.md`.
- Check ids `life04.no-receipt.<harness>.*`, `life04.modified.*`, `life04.preview-hash.enforced`, `life04.tamper.*`, `life07.external.*`, `life07.failure.*`, `life07.compensation.*`, `life07.receipt.written`; gap candidate keys `LIFE-04/C<k>`, `LIFE-07/C<k>`.
- Synthetic fixture names `p11-user-<server>.js`, `p11 user-authored skill`, `p11 user edit`, `p11-user-arg`, `p11-user-server`.

<output>
Create `.planning/phases/11-managed-lifecycle-verification/11-05-SUMMARY.md` when done. Include the per-harness no-receipt table, the P2/P4/tamper outcomes, the external-change and failure-path outcomes, the compensation command table against the lock, and both provisional verdicts.
</output>
