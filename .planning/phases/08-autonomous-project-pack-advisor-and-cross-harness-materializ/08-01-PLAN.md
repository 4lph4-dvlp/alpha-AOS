---
phase: 08-autonomous-project-pack-advisor-and-cross-harness-materializ
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/owned-skills.ts
  - src/core/install.ts
  - test/owned-skills.test.ts
autonomous: true
requirements: [PACK-03]

must_haves:
  truths:
    - "The owned skill engine resolves canonical and environment-overridden skill destinations for all five harnesses: claude, codex, antigravity, pi, and hermes (PACK-03, D-03)."
    - "renderOwnedSkill supports both 'explicit' (user-invocable with disable-model-invocation on Claude) and 'automatic' (model-invocable without disabling model execution across harnesses) invocation styles (PACK-03, D-03)."
    - "planInstallation and createInstallOperationPlan plan owned skill steps for every active harness target declared in catalog.components.ownedSkills, rather than gating exclusively on Claude (PACK-03, D-04)."
    - "All multi-target owned skill file operations route through applyFileTransaction with boundary proofs, journaling, and snapshot rollback capabilities (PACK-03, D-03)."
  prohibitions:
    - statement: "MUST NOT hardcode Claude as the only supported target in owned-skills.ts or install.ts (PACK-03, D-03, D-04)."
      category: architectural
      status: resolved
      verification: automated
    - statement: "MUST NOT write owned skills to paths outside proven allowed roots or without transactional snapshots (PACK-03, D-03)."
      category: safety
      status: resolved
      verification: automated
  artifacts:
    - path: "src/core/owned-skills.ts"
      provides: "Multi-harness owned skill destination resolution, frontmatter rendering, and transactional operations"
      exports: ["destinationFor", "renderOwnedSkill", "planOwnedSkillSync", "planOwnedSkillOperation", "applyOwnedSkillSync"]
    - path: "src/core/install.ts"
      provides: "Universal installation planner and apply orchestrator iterating over all active harness targets for owned skills"
      exports: ["planInstallation", "createInstallOperationPlan", "applyInstallation"]
    - path: "test/owned-skills.test.ts"
      provides: "Unit and integration tests verifying multi-harness owned skill resolution, frontmatter rendering, and transactional sync"
---

<objective>
Generalize the owned-skills engine and installation loop to support all five harnesses (Claude, Codex, Antigravity, Pi, Hermes) and automatic model invocation (PACK-03, D-03, D-04).

Purpose: Enable repository-owned skills like `alpha-aos-pack-advisor` to be cleanly rendered, planned, verified, and transactionally applied to any active agent harness on the user's workstation.
Output: Generalized `src/core/owned-skills.ts`, multi-target `src/core/install.ts`, and test suite `test/owned-skills.test.ts`.
</objective>

<execution_context>
Execution via GSD workflow adhering to anti-shallow deep work rules.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-autonomous-project-pack-advisor-and-cross-harness-materializ/08-CONTEXT.md
@.planning/phases/08-autonomous-project-pack-advisor-and-cross-harness-materializ/08-RESEARCH.md
@.planning/phases/08-autonomous-project-pack-advisor-and-cross-harness-materializ/08-VALIDATION.md
@src/core/owned-skills.ts
@src/core/install.ts
@src/core/ecc-skills.ts
</context>

<tasks>

<task id="08-01-01" wave="1">
<title>Task 1: Generalize owned-skills.ts for Multi-Harness Resolution & Automatic Invocability (PACK-03, D-03)</title>
<read_first>
- src/core/owned-skills.ts
- src/core/ecc-skills.ts
- src/types.ts
</read_first>
<action>
1. In `src/core/owned-skills.ts`:
   - Replace the Claude-only `destinationFor` implementation with a complete mapping across all 5 harnesses:
     - `claude`: `join(options.customHome ?? options.claudeHome ?? (process.env.CLAUDE_CONFIG_DIR?.trim() || join(homedir(), ".claude")), "skills", id, "SKILL.md")`
     - `codex`: `join(options.customHome ?? (process.env.CODEX_HOME?.trim() || join(homedir(), ".agents")), "skills", id, "SKILL.md")`
     - `antigravity`: `join(options.customHome ?? (process.env.ANTIGRAVITY_CONFIG_DIR?.trim() || join(homedir(), ".gemini", "config")), "skills", id, "SKILL.md")`
     - `pi`: `join(options.customHome ?? join(homedir(), ".agents"), "skills", id, "SKILL.md")`
     - `hermes`: `join(options.customHome ?? (process.env.HERMES_HOME?.trim() || join(homedir(), ".hermes")), "skills", id, "SKILL.md")`
   - Update `OwnedSkillPlanOptions` interface to accept `customHome?: string` in addition to deprecated `claudeHome?: string`.
   - Update `renderOwnedSkill(source, target, invocation, argumentHint)`:
     - Verify source starts with `---\n`.
     - When `invocation === "explicit"`:
       - For `claude`: insert `disable-model-invocation: true`.
     - When `argumentHint` is supplied:
       - For `claude`: insert `argument-hint: ${JSON.stringify(argumentHint)}`.
     - For `codex`, `antigravity`, `pi`, `hermes`: preserve valid frontmatter with `name` and `description`; when `invocation === "automatic"`, do NOT inject `disable-model-invocation: true`, ensuring the skill is visible and invocable by the agent.
   - Ensure `skillRoot` and `allowedRoots` calculation in `planOwnedSkillOperation` correctly bounds the destination directory.
</action>
<verify>
<automated>npm run check && npm run build</automated>
</verify>
<acceptance_criteria>
- `destinationFor` does not throw for any of "claude" | "codex" | "antigravity" | "pi" | "hermes".
- `renderOwnedSkill` renders correctly for all five targets without throwing.
- TypeScript compiler passes with zero diagnostic errors.
</acceptance_criteria>
</task>

<task id="08-01-02" wave="1">
<title>Task 2: Update install.ts & Implement test/owned-skills.test.ts (PACK-03, D-04)</title>
<read_first>
- src/core/install.ts
- src/core/plan.ts
- test/catalog.test.ts
</read_first>
<action>
1. In `src/core/install.ts`:
   - In `planInstallation` (around line 392):
     Replace:
     ```typescript
     if (selection.targets.includes("claude")) {
       for (const skill of options.catalog.components.ownedSkills) {
         if (!skill.targets.includes("claude")) continue;
         ...
     ```
     With a loop over `selection.targets`:
     ```typescript
     for (const target of selection.targets) {
       for (const skill of options.catalog.components.ownedSkills) {
         if (!skill.targets.includes(target)) continue;
         const plan = await planOwnedSkillSync(options.root, options.catalog, options.lock, skill.id, target, installStateOptions(options));
         const action = plan.action === "replace" ? "update" : plan.action;
         steps.push({ id: `owned-skill:${skill.id}:${target}`, component: "owned-skill", target, action, external: false, note: plan.destination });
       }
     }
     ```
   - In `createInstallOperationPlan` (around line 648):
     Replace:
     ```typescript
     if (install.targets.includes("claude")) {
       for (const skill of options.catalog.components.ownedSkills) { ... }
     }
     ```
     With iterating over all `install.targets` and accumulating plans for every supported target.
   - In `applyInstallation` (around line 839):
     Ensure every planned owned skill across all targets is executed and recorded into component sync results.
2. Create `test/owned-skills.test.ts`:
   - Verify `destinationFor` returns correct paths for all 5 harnesses with and without custom home overrides.
   - Verify `renderOwnedSkill` for all 5 harnesses with both explicit and automatic invocation.
   - Verify `planOwnedSkillSync` and `applyOwnedSkillSync` in a temporary test directory for all 5 harnesses.
   - Verify transaction rollback safely undoes owned skill writes across non-Claude harnesses.
</action>
<verify>
<automated>npm run check && npm run build && node scripts/run-tests.mjs --files dist/test/owned-skills.test.js</automated>
</verify>
<acceptance_criteria>
- `test/owned-skills.test.ts` passes with 100% assertions green.
- `planInstallation` properly emits steps for any harness configured in `catalog.components.ownedSkills`.
- Existing tests in `test/catalog.test.ts` continue to pass.
</acceptance_criteria>
</task>

</tasks>
