---
phase: quick
plan: 260922-om1
type: execute
autonomous: true
---

# Reinstate Claude Code into the active v0.1.0 harness scope

<objective>
Reverse quick task 260914-x2c: the Claude-account verification blocker that motivated
D-17's exclusion of Claude Code from active v0.1.0 scope is now resolved (a working Claude
Code account exists and is driving this very session). Add decision D-18 to Phase 3's
`03-CONTEXT.md` explicitly superseding D-17 — without erasing D-17 or D-09 — and update
`PROJECT.md`, `REQUIREMENTS.md`, and `ROADMAP.md` so Claude Code is named as a fifth active
v0.1.0 harness on equal footing with Codex, Antigravity, Pi, and Hermes.

Purpose: Reinstating the *scope claim* is not itself evidence. This task must not silently
mark any requirement, support-matrix tier, or capability Complete/PROVEN for Claude — it
only removes the categorical exclusion so Claude is judged by the same
`unverified`/`blocked`/`supported` rules D-10 and D-12 already apply to every other harness.
The concrete remaining implementation (the `support-matrix.ts` RESIDUE hardcode, the
`Exclude<HarnessId, "claude">` type exclusion, restoring a Claude `canaryHarness` path, and
actually running a real Claude canary) is explicitly NOT done here and is routed to
follow-up planning.

Output: Aligned `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`,
and `03-CONTEXT.md` (new D-18). `03-UAT.md` and `03-VERIFICATION.md` confirmed to need no
change (or narrowly corrected only if factually-wrong permanent-exclusion wording is found).
No product source, catalog, schema, adapter, or test file is changed by this quick task.
</objective>

<constraints>
- Do NOT modify product source code (`src/**`), catalog files (`catalog/**`), schemas,
  adapters, or tests. This quick task is documentation/planning realignment only.
- Do NOT run or claim to run any canary/verification, and do NOT implement the
  `support-matrix.ts` fix, the `canaryHarness`/catalog restoration, or a real Claude
  invocation. Only flag and route this follow-up work.
- Do NOT mark any CAPA/REL requirement Complete or any support-matrix tier PROVEN for
  Claude. Only the *scope inclusion* language changes; every existing checkbox and
  Traceability status in `REQUIREMENTS.md` (including the already-stale-looking
  `CAPA-02/03/05 = Gaps Found` rows this task does not own) stays byte-identical.
- Preserve requirement IDs CAPA-01 through CAPA-08 and existing phase boundaries. Do not
  move Phase 5/6/7 implementation into this task.
- Keep prior decisions (D-01 through D-17) auditable — supersede D-17 explicitly, never
  delete or rewrite it or the D-09 annotation beneath it.
- Do not reopen or reword Phase 3's already-verified success criteria, gap records
  (G-03-1..4), or `03-VERIFICATION.md`/`03-UAT.md` pass/status fields; only add
  clearly-dated pointer notes, and only narrowly correct wording that is now factually
  wrong (a claim of *permanent* Claude exclusion) if one is actually found.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Add decision D-18 to 03-CONTEXT.md, explicitly superseding D-17</name>
  <files>.planning/phases/03-transactional-project-packs-and-native-optional-use/03-CONTEXT.md</files>
  <action>
In the `<decisions>` "Surface Coverage and Capability State" section, annotate the existing
D-17 bullet exactly the way D-09 was annotated when D-17 superseded it: prepend "(D-17 is
superseded by D-18 on 2026-09-22). Formerly:" before its current opening sentence, keep
every one of D-17's four sub-bullets (Codex anchor, CAPA-03 two-harness rule, Antigravity/Pi/Hermes
promotion rule, stable-ordering rule) completely unchanged beneath it, and close the block
with "Preserved for audit." — mirroring D-09's own closing line. Do not touch the D-09
bullet itself.

Immediately after the annotated D-17 block (before D-10 continues), add a new **D-18**
bullet. It must state: on 2026-09-22, user decision established that the Claude-account
verification blocker underlying D-17 is resolved (a working Claude Code account now exists
and is the active session driving this repository); D-17 is superseded and Claude Code is
reinstated into the active v0.1.0 harness scope on equal footing with Codex, Antigravity,
Pi, and Hermes; reinstating the scope claim is not itself evidence — Claude remains subject
to the same D-10 ceiling/ledger split and D-12 `blocked`/`unverified` distinction as every
other harness, and promotion past `unverified`/RESIDUE to `supported`/PROVEN still requires
its own real invocation-receipt evidence gathered the same way D-01 requires for any
harness. Restate the operative pass-bar rules as: Codex remains the currently proven anchor
for CAPA-01, CAPA-02, and CAPA-05 not because Claude is excluded but because Codex is the
one surface with retained real evidence today, and Claude may become an equally valid
anchor once its own real evidence exists; CAPA-03's "two distinct active harnesses"
requirement drops its "non-Claude" qualifier (any two distinct active harnesses, including
Claude, may satisfy it once both sides carry real evidence), and the already-closed G-03-2
evidence (Hermes to Codex) remains valid and is not reopened by this decision; Antigravity,
Pi, Hermes, and now Claude all promote only from their own deterministic/native evidence and
otherwise remain visibly `unverified` or `unsupported`, with no special path for Claude;
stable equal-proof ordering stays the D-17 offline deterministic fixture obligation,
unaffected by this decision.

End the D-18 bullet with an explicit non-implementation and routing note: this quick task
does not implement (a) removing the `evaluateMatrixCell` `harnessId === "claude"` RESIDUE
hardcode and the `Exclude<HarnessId, "claude">` type exclusion in
`src/core/support-matrix.ts`, (b) restoring a Claude `canaryHarness` option and a Claude
canary run path in `catalog/stack.yaml` / `catalog/canaries.yaml`, or (c) actually executing
a real Claude Code canary invocation to generate genuine PROVEN-tier evidence.
`src/core/support-matrix.ts` and the real-host canary ledger are owned by Phase 7 (REL-02),
which is already marked `passed`/complete, so the correct next step is `/gsd-verify-work 7`
to formally register the now-stale "every claimed harness/surface has current real-host
evidence" truth (Claude re-entered the claimed set with none), followed by
`$gsd-plan-phase 7 --gaps`. Name `$gsd-plan-phase 3 --gaps` as the alternate route if that
verification instead finds the work better scoped to Phase 3's CAPA-01/02/03/05 canary
mechanics. Do not invent a gap ID that does not exist yet.

Do not edit `<canonical_refs>`, `<code_context>`, `<specifics>`, `<deferred>`, or
`<open_questions>` — this task touches only the `<decisions>` section.
  </action>
  <verify>
    <automated>node -e "const s=require('node:fs').readFileSync('.planning/phases/03-transactional-project-packs-and-native-optional-use/03-CONTEXT.md','utf8');for(const x of ['D-18','D-17 is superseded by D-18','D-09 is superseded by D-17','Formerly: The phase pass bar is','Codex is the required active anchor','reinstated','support-matrix.ts','Exclude<HarnessId','canaryHarness'])if(!s.includes(x))throw Error('03-CONTEXT missing '+x);if(!(s.includes('D-18')&&(s.includes('gsd-verify-work 7')||s.includes('gsd-plan-phase 7'))))throw Error('D-18 missing Phase 7 follow-up routing');"</automated>
  </verify>
  <done>03-CONTEXT.md retains D-09 and D-17 fully readable for audit, records D-18 as the user's reinstatement decision with the non-implementation/routing note attached, and does not touch any other section.</done>
</task>

<task type="auto">
  <name>Task 2: Reinstate Claude Code as a fifth active harness across PROJECT.md, REQUIREMENTS.md, and ROADMAP.md</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Reverse 260914-x2c's scope-exclusion language in all three files so Claude Code reads as a
fifth active v0.1.0 harness alongside Codex, Antigravity, Pi, and Hermes, citing D-18.
Everywhere the current text lists the harness set as "Codex, Antigravity, Pi, and/[,] Hermes"
and separately states Claude is excluded/"compatibility residue"/"outside active
support/release gates", change the enumeration to include Claude Code and replace the
exclusion clause with a reinstatement clause noting Claude requires its own real
invocation-receipt evidence before promotion, exactly like every other harness — do not
imply Claude is already proven.

In `PROJECT.md`: update the "What This Is" opening (the active-harness list plus the
following sentence about Claude being compatibility residue and excluded from obligations,
support, pass bars, and release gates); the three "Active" requirements bullets that name
the four-harness parenthetical (the native-invocation bullet, the repository-owned-workflow
bullet whose "active non-Claude targets" phrase should read as "each active supported
target" including Claude, and the cross-platform-validation bullet, which should additionally
note Claude's own real-host evidence is the specific outstanding item per D-18); remove the
"Out of Scope" bullet that names Claude Code's exclusion entirely; update the "Context"
paragraph's "repository-owned skill distribution remains Claude compatibility residue while
active work targets non-Claude native surfaces" clause to describe Claude as historically
the first surface with active work now extending to the others on equal footing; and update
the "Harness scope" Constraints bullet's four-harness list and its Claude-exclusion sentence.

In `REQUIREMENTS.md`: update the "Active Harness Scope" header line (currently four harnesses
plus a Claude-exclusion sentence) to five harnesses plus a D-18 reinstatement sentence noting
Claude's own evidence is still required for promotion; update both "Definition of Done"
bullets that name the four-harness parenthetical and the Claude-compatibility-residue
sentence; and remove the "Out of Scope" table row naming Claude Code's exclusion. Do NOT
touch the Traceability table, any requirement checkbox, or the Coverage counts — every
CAPA-01..08, GATE, OPTO, LIFE, and REL row (including the CAPA-02/03/05 "Gaps Found" rows,
which reflect pre-existing staleness unrelated to this task) must remain byte-identical.

In `ROADMAP.md`: update the Overview paragraph's five-harness enumeration and its
Claude-compatibility-residue sentence, replacing it with a D-18 reinstatement sentence. In
the Phase 3 section, add one new standalone note (after the numbered success-criteria list,
before the "**Plans**:" line) dated 2026-09-22 stating D-18 reinstates Claude without
reopening Phase 3's already-verified success criteria (Codex-anchored evidence from plans
03-19..03-22 remains valid) and that Claude's own evidence is outstanding follow-up, not a
reopened Phase 3 obligation; also update the Phase 3 "Planning note" line's "active
non-Claude targets" phrase to name all five active targets and add one sentence noting it
describes the original D-17-era planning scope. In the Phase 7 section, add one new
standalone note (after its numbered success-criteria list, before its "**Plans**:" line)
dated 2026-09-22 stating D-18 reinstates Claude, that Phase 7's Observable Truth 2 ("every
claimed harness/surface has current real-host... evidence") was verified true only while
Claude was excluded under D-17, that Claude re-entering the claimed set with no real-host
canary evidence of its own is the concrete outstanding gap, and that the next step is
`/gsd-verify-work 7` to formally register it followed by `$gsd-plan-phase 7 --gaps` —
covering the `src/core/support-matrix.ts` RESIDUE hardcode, the `Exclude<HarnessId,
"claude">` exclusion, and restoring/running a real Claude canary — explicitly stating this
quick task does not implement any of that.
  </action>
  <verify>
    <automated>node -e "const f=require('node:fs');const ps=['.planning/PROJECT.md','.planning/REQUIREMENTS.md','.planning/ROADMAP.md'];const bodies={};for(const p of ps){const s=f.readFileSync(p,'utf8');bodies[p]=s;for(const n of ['Claude Code','Codex','Antigravity','Pi','Hermes','D-18'])if(!s.includes(n))throw Error(p+' missing '+n);if(/shipped compatibility residue/i.test(s))throw Error(p+' still frames Claude as residue');}if(/Active v0\.1\.0 development, support claims, or release gates for Claude Code/.test(bodies['.planning/PROJECT.md']))throw Error('PROJECT Out of Scope still excludes Claude');if(/Active v0\.1\.0 development, support commitments, or release gates for Claude Code/.test(bodies['.planning/REQUIREMENTS.md']))throw Error('REQUIREMENTS Out of Scope still excludes Claude');const req=bodies['.planning/REQUIREMENTS.md'];for(const id of ['CAPA-02','CAPA-03','CAPA-05'])if(!new RegExp('\\| '+id+' \\| Phase 3 \\| Gaps Found \\|').test(req))throw Error(id+' traceability changed');for(const id of ['CAPA-01','CAPA-04','CAPA-06','CAPA-07','CAPA-08'])if(!new RegExp('\\| '+id+' \\| Phase 3 \\| Complete \\|').test(req))throw Error(id+' traceability changed');const road=bodies['.planning/ROADMAP.md'];if(!(road.includes('D-18')&&(road.includes('gsd-verify-work 7')||road.includes('gsd-plan-phase 7'))))throw Error('ROADMAP missing Phase 7 routing note');"</automated>
  </verify>
  <done>PROJECT.md, REQUIREMENTS.md, and ROADMAP.md all name Claude Code as a fifth active v0.1.0 harness alongside Codex, Antigravity, Pi, and Hermes, cite D-18, note Claude still needs its own evidence, and no longer describe Claude as excluded compatibility residue anywhere; every existing requirement checkbox and Traceability status is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm 03-UAT.md and 03-VERIFICATION.md need no change (narrow correction only if a permanent-exclusion claim is found)</name>
  <files>.planning/phases/03-transactional-project-packs-and-native-optional-use/03-UAT.md, .planning/phases/03-transactional-project-packs-and-native-optional-use/03-VERIFICATION.md</files>
  <action>
Re-read both files in full. They record Phase 3's already-closed gap-closure work
(G-03-1..4), which proved CAPA-02/03/05 on Codex under D-17's now-superseded rules; that
evidence remains valid regardless of Claude's reinstatement and must not be edited,
reworded, reopened, or have its `gap_id`, `status`, `truth`, `evidence`, or `closure_plan`
fields touched. Search specifically for wording that asserts Claude is *permanently* or
irreversibly out of scope, as opposed to describing the D-17-dated exclusion that was in
effect when that gap closure ran. The planner's own reading found none: 03-UAT.md's
`status: complete` summary carries no Claude-exclusion wording at all in its six test
records, and 03-VERIFICATION.md's "D-17 excludes Claude from active verification" sentence
and "Claude remains RESIDUE" phrasing both describe the dated, D-17-scoped state of that
specific verification run, not a permanent claim.

If your own fresh read confirms no permanent-exclusion claim exists (the expected outcome),
make zero edits to either file. If one is found, correct only that specific sentence in
place to note the exclusion was scoped to the D-17 window (2026-09-14 to 2026-09-22,
superseded by D-18) — under no circumstance touch any gap's status, the phase's overall
`status: passed`/`complete` field, or any evidence/verification text describing what was
actually tested.
  </action>
  <verify>
    <automated>node -e "const f=require('node:fs');const uatPath='.planning/phases/03-transactional-project-packs-and-native-optional-use/03-UAT.md';const verPath='.planning/phases/03-transactional-project-packs-and-native-optional-use/03-VERIFICATION.md';const uat=f.readFileSync(uatPath,'utf8');const ver=f.readFileSync(verPath,'utf8');if(!/status: complete/.test(uat))throw Error('UAT status changed');for(const id of ['G-03-1','G-03-2','G-03-3','G-03-4'])if(!new RegExp(id+'[\\s\\S]{0,120}status: closed').test(uat))throw Error('UAT gap '+id+' status changed');if(!/^status: passed$/m.test(ver))throw Error('VERIFICATION status changed');"</automated>
  </verify>
  <done>03-UAT.md and 03-VERIFICATION.md's gap records and phase-pass status are confirmed unchanged (or, only if a permanent-exclusion claim was actually found, narrowly corrected without touching any status/evidence field).</done>
</task>

</tasks>

<verification>
Run `git diff --check -- .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/phases/03-transactional-project-packs-and-native-optional-use/03-CONTEXT.md .planning/phases/03-transactional-project-packs-and-native-optional-use/03-UAT.md .planning/phases/03-transactional-project-packs-and-native-optional-use/03-VERIFICATION.md`, then run all three task-level automated checks. Inspect `git diff --name-only` and confirm this quick task changed only `PROJECT.md`, `REQUIREMENTS.md`, `ROADMAP.md`, and `03-CONTEXT.md` (03-UAT.md/03-VERIFICATION.md showing zero diff is the expected, correct outcome per Task 3) plus its own quick PLAN/SUMMARY and normal GSD state bookkeeping. Do not run product source tests or any canary — this plan changes no product source, catalog, schema, adapter, or test file; the flagged follow-up (support-matrix.ts hardcode removal, canaryHarness/Claude canary restoration, real Claude canary execution) is explicitly NOT implemented here.
</verification>

<success_criteria>
- PROJECT.md, REQUIREMENTS.md, and ROADMAP.md name Claude Code as a fifth active v0.1.0 harness on equal footing with Codex, Antigravity, Pi, and Hermes; none of them describe Claude as excluded "compatibility residue" anymore.
- D-18 in 03-CONTEXT.md explicitly supersedes D-17 without erasing D-17 or the D-09 audit trail beneath it, and states that reinstating scope is not itself evidence — Claude still requires its own invocation-receipt proof like every other harness.
- Every requirement checkbox and Traceability status in REQUIREMENTS.md (CAPA-01..08, GATE, OPTO, LIFE, REL) is byte-identical to its pre-task value; nothing is silently marked Complete or PROVEN by this scope change.
- The unimplemented follow-up (support-matrix.ts RESIDUE hardcode + `Exclude<HarnessId,"claude">` removal, canaryHarness/Claude canary path restoration, real Claude canary execution) is explicitly named and routed to a concrete next command (`/gsd-verify-work 7` then `$gsd-plan-phase 7 --gaps`, or `$gsd-plan-phase 3 --gaps` as the named alternate) — no gap ID is invented.
- 03-UAT.md and 03-VERIFICATION.md's gap records (G-03-1..4) and phase-pass status are unchanged; the planner's own review found no permanent-exclusion wording requiring correction.
- No source, catalog, schema, adapter, or test file is touched.
</success_criteria>

## Source coverage audit

| Source | Item | Task | Status |
|---|---|---:|---|
| GOAL | Reinstate Claude Code into active v0.1.0 harness scope on equal footing with Codex, Antigravity, Pi, Hermes now that the account-verification blocker is resolved | 1-2 | COVERED |
| PROJECT | What This Is, Active requirements, Out of Scope, Context, harness-scope constraint reinstatement | 2 | COVERED |
| REQ | Active Harness Scope header, Definition of Done, Out of Scope row removal; all CAPA/GATE/OPTO/LIFE/REL traceability and checkboxes preserved unchanged (no new evidence generated by this task) | 2 | COVERED |
| ROADMAP | Overview harness list and compatibility-residue sentence; Phase 3 post-verification note; Phase 7 post-verification note routing the outstanding real-host-evidence follow-up | 2 | COVERED |
| CONTEXT | D-18 added, explicitly superseding D-17 without erasing D-09 or D-17; non-implementation of support-matrix.ts/canaryHarness/real-canary work explicitly flagged and routed | 1 | COVERED |
| UAT / VERIFICATION | Confirmed no permanent-exclusion wording exists in 03-UAT.md/03-VERIFICATION.md; G-03-1..4 and phase-passed status preserved untouched | 3 | COVERED |
| RESEARCH | Quick mode explicitly excludes a research phase; no new library or external integration is selected here | - | EXCLUDED |

<output>
Create `.planning/quick/260922-om1-reverse-quick-task-260914-x2c-reinstate-/260922-om1-SUMMARY.md` when execution completes. The next implementation-planning command is `/gsd-verify-work 7` (to formally register the outstanding Claude real-host-canary gap against REL-02), followed by `$gsd-plan-phase 7 --gaps` — or `$gsd-plan-phase 3 --gaps` if that verification instead finds the work better scoped to Phase 3's CAPA-01/02/03/05 canary mechanics. This quick task must not execute those gaps or the follow-up implementation itself.
</output>
