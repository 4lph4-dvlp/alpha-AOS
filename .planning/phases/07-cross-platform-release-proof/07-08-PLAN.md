---
phase: 07-cross-platform-release-proof
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/support-matrix.ts
  - test/support-matrix.test.ts
  - docs/SUPPORT_MATRIX.md
autonomous: true
gap_closure: true
gap_ids: [G-07-1]
requirements: [REL-02]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "src/core/support-matrix.ts judges Claude Code by the same evidence-based rule as Codex, Antigravity, Pi, and Hermes: evaluateMatrixCell contains no harness-identity branch that fixes a tier before the shared unsupported/undetected/no-ledger/no-receipt/matching-receipt evaluation runs (REL-02, D-18)."
    - "Claude has four real, evidence-backed surfaces in the support matrix (GSD Core, Context7, Unified Memory, Project Packs) structured identically to Codex's, grounded in catalog/stack.yaml's harnesses.claude block and src/core/project-plan.ts's SURFACE_CEILING entry for claude (REL-02, D-18)."
    - "docs/SUPPORT_MATRIX.md and the compiled `alpha-aos doctor --matrix` output reflect Claude's evidence-based cells and no longer assert a fixed compatibility-only tier for Claude anywhere in the table or the JSON envelope (REL-02)."
    - "catalog/stack.yaml and catalog/canaries.yaml are confirmed (not silently assumed) to already declare Claude as canary-eligible on equal footing with Codex; no structural catalog change was needed to close that half of the gap (REL-02, D-18)."
  artifacts:
    - path: "src/core/support-matrix.ts"
      provides: "Evidence-based evaluateMatrixCell and activeSurfaces covering all five HarnessId values, claude included"
    - path: "test/support-matrix.test.ts"
      provides: "Assertions proving Claude reaches UNVERIFIED without a receipt and PROVEN with a real matching receipt, and that no cell reports the compatibility-only tier"
    - path: "docs/SUPPORT_MATRIX.md"
      provides: "Regenerated, byte-identical-to-source support matrix reflecting Claude's evidence-based rows"
  key_links:
    - "support-matrix.ts activeSurfaces.claude -> evaluateMatrixCell -> matchingReceipt -> capabilities/ledger.json (the identical evidence path already used by codex, antigravity, pi, and hermes)"
    - "catalog/stack.yaml harnesses.claude / catalog/canaries.yaml harnesses:[claude,...] -> the canary readiness and invocation machinery in src/core/canary.ts and src/adapters/capability-oracle.ts, already wired for claude before this plan"
---

<objective>
Close gap `G-07-1` (07-VERIFICATION.md): D-18 (03-CONTEXT.md, 2026-09-22) reinstated Claude Code into the active v0.1.0 harness scope on equal footing with Codex, Antigravity, Pi, and Hermes, but `src/core/support-matrix.ts` still hardcodes `harnessId === "claude"` to a fixed compatibility-only tier and types `activeSurfaces` to exclude claude entirely — a structural exclusion that makes Claude incapable of ever reaching the top tier no matter what evidence exists. This plan removes that hardcode and widens the type so Claude is judged by the exact same evidence rule as every other harness: no free pass to the top tier, and no structural ceiling below it.

Purpose: Make the v0.1.0 support matrix honest about D-18. Claude's own real invocation-receipt evidence is a separate matter (see the explicit deferral in Task 2) — this plan only removes the code-level impossibility of Claude ever being judged on that evidence.

Output: `src/core/support-matrix.ts` with claude flowing through the same `activeSurfaces` / `evaluateMatrixCell` path as codex, antigravity, pi, and hermes; `test/support-matrix.test.ts` asserting the corrected behavior; `docs/SUPPORT_MATRIX.md` regenerated to match.

Explicitly NOT in this plan (see Task 2 for the reasoning, recorded rather than silently dropped):
- Running a real Claude Code canary invocation. Achievable from an authenticated host, but this phase's own precedent (07-02, 07-05) never spends a real model turn inside a deterministic, repeatable gap-closure plan.
- Editing `.planning/REQUIREMENTS.md`'s REL-02 body text. The gap's own record says not to touch it until real evidence exists.
- Editing `docs/RELEASE_NOTES_v0.1.0.md` or any other REL-06 release-notes artifact — out of scope for this gap.
- Any change to Truths 1, 3, 4, 5 or Requirements REL-01, REL-03, REL-04, REL-05, REL-06.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-cross-platform-release-proof/07-CONTEXT.md
@.planning/phases/07-cross-platform-release-proof/07-VERIFICATION.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-CONTEXT.md
@src/core/support-matrix.ts
@test/support-matrix.test.ts
@docs/SUPPORT_MATRIX.md
@catalog/stack.yaml
@catalog/canaries.yaml
</context>

<interfaces>
Existing exports `evaluateMatrixCell`, `evaluateSupportMatrix`, `BASE_SUPPORT_MATRIX`, `renderSupportMatrixMarkdown`, and the `SupportMatrixEntry` / `SupportTier` / `EvaluatedMatrixCell` types in `src/core/support-matrix.ts` keep their current signatures — this plan changes what data flows through them and one internal branch, not their shape. `Inventory`, `CapabilityLedger`, and `CapabilityProof` (imported from `../types.js` and `./capability-ledger.js`) are read-only inputs and are not modified.

Grounding facts already established in the codebase, cited so the new claude entries are not invented:
- `catalog/stack.yaml` `harnesses.claude` declares `gsdTarget: claude` and `eccTarget: claude` — the identical shape `harnesses.codex` declares.
- `catalog/canaries.yaml` already lists the claude harness identifier in the `harnesses:` array of all five declared canaries (`DOCUMENTATION_VERSION_SCOPED`, `RESEARCH_MULTI_SOURCE`, `PACK_EXERCISE_FRONTEND_A11Y`, `CROSS_HARNESS_HANDOFF`, `ORDINARY_LOOKUP_CONTROL`), alongside codex on every one.
- `src/core/project-plan.ts`'s `PROJECT_SKILL_ROOTS` maps claude to `.claude/skills`, and its `SURFACE_CEILING` map records claude's project-pack delivery ceiling as `"supported"` — the strongest of the four non-hermes/antigravity ceilings recorded there (codex and pi are both `"unverified"` at that same ceiling).
- `src/core/canary.ts`'s `CanaryHarness` type, `HARNESS_COMMANDS`, and `READINESS_DEFINITIONS` already include claude as a full canary target, and `src/adapters/capability-oracle.ts`'s `INVOCATION_DEFINITIONS.claude` already declares a real non-interactive invocation vector (`claude -p <prompt> --output-format stream-json --verbose`) — none of this canary machinery is touched by this plan; it already treats claude as first-class.
</interfaces>

<tasks>

<task type="auto" id="07-08-01">
  <name>Task 1: Remove Claude's hardcoded tier and judge it by the same evidence rule as every other harness</name>
  <read_first>
    src/core/support-matrix.ts
    test/support-matrix.test.ts
    docs/SUPPORT_MATRIX.md
    catalog/stack.yaml
    catalog/canaries.yaml
    src/core/project-plan.ts (PROJECT_SKILL_ROOTS at ~line 603, SURFACE_CEILING at ~line 644)
  </read_first>
  <files>src/core/support-matrix.ts, test/support-matrix.test.ts, docs/SUPPORT_MATRIX.md</files>
  <action>
    In `src/core/support-matrix.ts`:
    1. Widen the type constraint on the `activeSurfaces` object literal so it is keyed over every value of `HarnessId` rather than excluding the claude identifier, and add a `claude` entry to `activeSurfaces` with exactly four surfaces, in this order, mirroring `codex`'s own four rows in shape: GSD Core bound to receipt-capability id `GSD_CORE` with the note "Lifecycle controller and state writer"; Context7 bound to `CAPA-01` with the note "Version-sensitive documentation lookup"; Unified Memory bound to `CAPA-03` with the note "Cross-harness handoff sender and receiver"; Project Packs bound to `CAPA-05` with the note "Evidence-selected project capability materialization". This mirrors codex's own four entries exactly, grounded in the interfaces section above: claude has the same catalog ceiling as codex (`gsdTarget`/`eccTarget` both declared), the same canary-catalog eligibility (present on all five declared canaries), and a project-pack delivery ceiling in `project-plan.ts` that is at least as strong as codex's.
    2. Update the type annotation on the derived `activeEntries` computation (the `Object.entries(activeSurfaces)` cast just below it) so its key type matches the widened `activeSurfaces` type from step 1.
    3. Remove the standalone entry for the claude harness from `BASE_SUPPORT_MATRIX` — the one entry that sits outside `activeEntries` and names every managed surface at once with a fixed compatibility-only tier. Claude's real surfaces now flow entirely through `activeEntries`, exactly like codex, antigravity, pi, and hermes.
    4. Delete the branch at the very top of `evaluateMatrixCell` that special-cases the claude harness identifier and returns a fixed tier before any evidence check runs. After deletion, every cell — claude's included — reaches the shared unsupported-check, undetected-harness-check, unavailable-ledger-check, and matching-receipt-check in the order they already run for every other harness today.
    5. Update the one-sentence taxonomy summary printed at the top of `renderSupportMatrixMarkdown`'s output (immediately above the table) so it no longer states that Claude Code always lands on the compatibility-only tier. Restate the sentence to describe all four tiers generically — a matching inspectable receipt promotes a cell to the top tier, an unmatched claim is unverified, a surface offered only for legacy compatibility is the compatibility tier, and a structurally incompatible surface is unsupported — without naming any specific harness identifier. Leave the "## Taxonomy" bullet list further down the rendered document unchanged; it already defines each tier generically and needs no claude-specific edit.

    In `test/support-matrix.test.ts`:
    6. In the "support taxonomy refuses false PROVEN states" test, replace the lookup that finds the removed standalone claude entry and its fixed-tier assertion with a lookup of claude's Context7 surface, following the exact same pattern the test already uses two lines above it to look up codex's Context7 surface. Assert that this claude/Context7 cell evaluates to the unverified tier both (a) when claude is absent from the inventory and (b) when claude is present in the inventory but the ledger carries no matching receipt — proving the hardcode is gone without granting claude a free pass to the top tier.
    7. Add one new test, placed immediately after "inspectable matching invocation receipt promotes Antigravity active cell to PROVEN" and following that test's exact structure (a synthetic `SupportMatrixEntry` object, one evaluation against an empty ledger, one evaluation against a ledger containing a single matching proof): build a claude/Context7 `SupportMatrixEntry` bound to receipt-capability `CAPA-01`, assert the unreceipted evaluation is unverified with an evidence summary mentioning a canary, and assert the receipted evaluation reaches the top tier with a receipt reference that names the ledger file and an `observedAt` matching the fixture's timestamp.
    8. In "compiled doctor exposes table and structured matrix diagnostics", replace the assertion matching the old claude row and the assertion that some cell reports the compatibility-only tier with: an assertion that the rendered table contains a claude / GSD Core row reporting the unverified tier (this is deterministic in this test's isolated, receipt-free state root regardless of whether the claude executable itself is detected on the host running the test, because the ledger read from that isolated state root is always empty), an assertion that the compatibility-only tier string never appears anywhere in the table output, a JSON assertion that no cell's tier is the compatibility-only value, and a JSON assertion that at least one cell's `entry.harnessId` is claude. Widen the test's local JSON response type for this assertion (currently typed with only `tier` per cell) to also expose `entry.harnessId`.

    Regenerate `docs/SUPPORT_MATRIX.md`:
    9. After building, write and run a short one-off Node ESM script (delete it when done; it is scratch, not a committed artifact) that imports `evaluateSupportMatrix` and `renderSupportMatrixMarkdown` from the compiled `dist/src/core/support-matrix.js`, calls `evaluateSupportMatrix` with an inventory built the same way the test file's own `inventory([])` helper builds one (all five harnesses present but every one's `detected` false), an empty ledger (`proofs: []`) matching the test file's `ledger()` helper, and the same fixed `generatedAt` and `platform: "linux"` values the "support matrix markdown stays byte-identical to the structured source" test asserts against. Overwrite `docs/SUPPORT_MATRIX.md` with the rendered markdown so that test keeps passing after the source change.
  </action>
  <verify>
    <automated>npm run check && npm run build && node scripts/run-tests.mjs --files dist/test/support-matrix.test.js && ! grep -q 'harnessId === "claude"' src/core/support-matrix.ts</automated>
    <fails_when>non-zero exit code from check, build, or the test run; any support-matrix test failing; or the grep finding the removed claude special-case branch still present in src/core/support-matrix.ts</fails_when>
  </verify>
  <done>src/core/support-matrix.ts contains no claude-specific tier override anywhere in evaluateMatrixCell; activeSurfaces (and its derived activeEntries) type-checks over the full HarnessId union including claude; claude carries four evidence-driven surfaces identical in shape to codex's; docs/SUPPORT_MATRIX.md is regenerated and byte-identical to the structured source; and the support-matrix test suite passes with assertions proving claude reaches unverified without a receipt and the top tier with a real matching receipt, exactly like every other active harness.</done>
</task>

<task type="auto" id="07-08-02">
  <name>Task 2: Confirm catalog canary-eligibility, run the full regression suite, and record what stays honestly deferred</name>
  <read_first>
    catalog/stack.yaml
    catalog/canaries.yaml
    src/core/project-plan.ts (PROJECT_SKILL_ROOTS, SURFACE_CEILING)
    .planning/REQUIREMENTS.md (the REL-02 bullet under "Cross-Platform Release Proof")
    .planning/phases/07-cross-platform-release-proof/07-VERIFICATION.md (the G-07-1 gap record)
  </read_first>
  <files>catalog/stack.yaml, catalog/canaries.yaml</files>
  <action>
    1. Confirm — without modifying either file — that `catalog/stack.yaml`'s `harnesses.claude` block already declares the same `gsdTarget`/`eccTarget` shape as `harnesses.codex`, and that the claude harness identifier already appears in the `harnesses:` array of every one of `catalog/canaries.yaml`'s five declared canaries, exactly alongside codex. Both catalog files already restore Claude's canary-eligible run path in full; no structural catalog edit closes any part of this gap, because none is missing. State this confirmation explicitly in the plan's execution summary, citing the exact line numbers checked.
    2. Run the complete existing automated suite once, after Task 1's change, to confirm the widened `HarnessId` coverage in `activeSurfaces` and the removed branch introduce zero regressions anywhere else — the canary readiness/invocation definitions, the project-pack ceiling table, and the capability-ledger schema already treated claude as a first-class harness before this plan, so this is a regression check on the wider codebase, not new wiring.
    3. Do NOT run a real Claude Code canary invocation as part of this task, and do NOT edit `.planning/REQUIREMENTS.md`'s REL-02 body text. Both are deliberately left open, for the same reason: `catalog/canaries.yaml`'s claude entries and the canary runtime's pre-existing readiness/invocation definitions for claude make a real invocation technically reachable from an authenticated host running `alpha-aos doctor --canary --harness claude`, but this phase's own established precedent (plans 07-02 and 07-05, both of which explicitly declined to spend a real model turn inside their own automated verification) treats a real, cost-bearing canary run as a separate, explicitly human-triggered action — never something a deterministic, repeatable gap-closure plan spends silently on the executor's behalf. Record in the plan's execution summary that: (a) Claude's support-matrix cells remain honestly unverified after this plan lands, exactly as they were structurally incapable of anything BUT a fixed tier before it; (b) the only legitimate way to promote them is a real invocation producing a genuine inspectable receipt, which a human can trigger with `alpha-aos doctor --canary --harness claude --no-spend` first (readiness only, zero cost) and then `alpha-aos doctor --canary --harness claude --capability CAPA-01` (or another declared capability) when ready to spend; and (c) the REL-02 wording reconciliation in REQUIREMENTS.md stays deferred until that real evidence exists, per the gap's own instruction not to touch it before then.
  </action>
  <verify>
    <automated>[ "$(grep -c 'claude' catalog/canaries.yaml)" -eq 5 ] && [ "$(grep -c 'claude' catalog/stack.yaml)" -eq 7 ] && npm test</automated>
    <fails_when>either grep count differs from the value confirmed during planning (5 in catalog/canaries.yaml, 7 in catalog/stack.yaml), signaling the catalog files changed shape and the confirmation in this task's action is stale; or `npm test` exits non-zero or reports any failing test in the full suite</fails_when>
  </verify>
  <done>catalog/stack.yaml and catalog/canaries.yaml are confirmed unchanged and already claude-inclusive (no edit was needed); the full test suite passes with zero regressions from Task 1's change; and the plan's execution summary explicitly records that a real Claude canary invocation and the REQUIREMENTS.md REL-02 wording reconciliation are deliberately deferred, naming the exact follow-up commands, rather than being silently skipped or fabricated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `capabilities/ledger.json` (user state root) → `evaluateMatrixCell` | A locally-writable JSON file is read as evidence that promotes a support-matrix cell to the top tier. This boundary is unchanged by this plan; claude now crosses it identically to every other harness instead of being structurally barred from it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08-01 | Elevation of Privilege | `evaluateMatrixCell` (`src/core/support-matrix.ts`) | low | mitigate | Removing claude's hardcoded tier makes claude reachable by the top tier for the first time, but only through the identical `matchingReceipt`/`receiptIsInspectable` gate already required of codex, antigravity, pi, and hermes — positive polarity, invoked native use, zero exit code, a held invocation verdict, and at least one retained observation. No new promotion path is introduced; the existing rigorous gate now simply applies to a fifth harness instead of being bypassed by an early return for it. |
| T-07-08-02 | Repudiation | `docs/SUPPORT_MATRIX.md`, `alpha-aos doctor --matrix` | low | accept | A stale claim that Claude "remains compatibility residue" is exactly the repudiation risk this plan closes; once removed, the rendered claim always traces back to a concrete ledger row a reader can inspect (`capabilities/ledger.json#proof-N`), or honestly states unverified when none exists. Accepted at low severity because the fix is the mitigation itself and no residual gap remains after Task 1. |
</threat_model>

<verification>
Both tasks' `<automated>` commands must exit 0. After Task 1, `test/support-matrix.test.ts` proves the hardcode is gone and no free pass was granted. After Task 2, the full suite (`npm test`) confirms zero regressions elsewhere, and the catalog confirmation plus the explicit deferral of the real invocation and the REQUIREMENTS.md wording are recorded in the execution summary rather than silently done or silently skipped.
</verification>

<success_criteria>
- `src/core/support-matrix.ts` judges claude through the exact same `evaluateMatrixCell` evidence path as codex, antigravity, pi, and hermes — no harness-identity branch fixes claude's tier.
- `test/support-matrix.test.ts` and `docs/SUPPORT_MATRIX.md` reflect the corrected behavior and pass/stay byte-identical respectively.
- `catalog/stack.yaml` and `catalog/canaries.yaml` are confirmed already claude-inclusive; no catalog edit was needed or made.
- `.planning/REQUIREMENTS.md`'s REL-02 body text is untouched, and the plan's summary explicitly records why (deferred pending real Claude invocation evidence, per the gap's own instruction).
- No real Claude Code canary invocation was run or fabricated by this plan; Claude's support-matrix cells remain honestly unverified pending that separate, human-triggered action.
- `npm test` passes with zero regressions introduced by the widened `HarnessId` coverage.
</success_criteria>

<output>
Create `.planning/phases/07-cross-platform-release-proof/07-08-SUMMARY.md` when done
</output>
