---
phase: quick
plan: 260925-s1a
type: execute
autonomous: true
---

# Fix Install Guidance, Actionable Error Messages, and Source Installation README Docs (S-1A)

<objective>
Resolve installation clarity and developer experience issues across fresh clone environments:
1. Enhance `scripts/build-artifact.mjs`, `scripts/install.ps1`, and `scripts/install.sh` to output clear, actionable remediation commands (`npm ci`, `npm run build`) when `dist/build-artifact.json` is missing on fresh checkouts.
2. Refactor `README.md` (English) and `README.ko.md` (Korean) to clearly distinguish "Installation from Source (Git Clone)" from package-manager installation, making the prerequisite `npm ci` and `npm run build` steps explicit before running `./scripts/install.ps1 -Apply`.
3. Clarify npm package distribution status (currently source-based; official registry distribution planned for future release).
</objective>

<constraints>
- Preserve existing string patterns (e.g. `no build artifact manifest`) in `scripts/build-artifact.mjs` to keep `test/install.test.ts` and `test/preview.test.ts` intact.
- Maintain 1:1 structural symmetry and parity between `README.md` and `README.ko.md`.
- Keep scripts portable across Windows PowerShell and POSIX shells.
- All checks (`npm run check`, `npm run build`, `npm run build:check`, `npm test`) must pass.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Enhance build artifact check and wrapper error guidance</name>
  <files>scripts/build-artifact.mjs, scripts/install.ps1, scripts/install.sh</files>
  <action>
1. In `scripts/build-artifact.mjs`, update `check()` when target manifest is missing to output actionable remediation guidance while retaining the `no build artifact manifest` pattern.
2. In `scripts/install.ps1` and `scripts/install.sh`, enhance the refusal message when artifact check fails to point directly to running `npm ci` and `npm run build`.
  </action>
  <verify>
    <automated>npm run check && npm run build:check</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Refactor README.md and README.ko.md installation sections</name>
  <files>README.md, README.ko.md</files>
  <action>
1. In `README.md`, restructure "Quick Start (Workstation Setup)" into "Installation":
   - Clarify distribution status (installed from source; npm registry distribution planned).
   - "Installation from Source (Git Clone)" section explicitly detailing `git clone` -> `npm ci` -> `npm run build` -> `.\scripts\install.ps1 -Apply`.
2. In `README.ko.md`, mirror the exact same structure in Korean.
  </action>
  <verify>
    <automated>npm run check && npm run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Run comprehensive verification and record quick task completion</name>
  <files>.planning/quick/260925-s1a-fix-install-guidance-and-readme/260925-s1a-SUMMARY.md, .planning/STATE.md</files>
  <action>
1. Run `npm test` and wrapper syntax validations.
2. Create `260925-s1a-SUMMARY.md`.
3. Update `.planning/STATE.md` Quick Tasks Completed table.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
</task>

</tasks>
