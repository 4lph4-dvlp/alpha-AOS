---
phase: 11-managed-lifecycle-verification
plan: 06
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - test/rollback-cli-verification.test.ts
  - .planning/phases/11-managed-lifecycle-verification/evidence/probes/life05-rollback.mjs
  - .planning/phases/11-managed-lifecycle-verification/evidence/life05-rollback.txt
  - .planning/phases/11-managed-lifecycle-verification/evidence/LIFE-05-findings.md
  - .planning/phases/11-managed-lifecycle-verification/evidence/probes/life06-recovery.mjs
  - .planning/phases/11-managed-lifecycle-verification/evidence/life06-recovery.txt
  - .planning/phases/11-managed-lifecycle-verification/evidence/LIFE-06-findings.md
autonomous: true
requirements: [LIFE-09]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "LIFE-05 'restore exact prior bytes' is judged by sha256 equality between each pre-transaction file and the file after `rollback <id> --apply`, and by the absence of every file the transaction created, through the packed CLI (D-01, D-04)."
    - "LIFE-05 'any post-transaction drift prevents all rollback writes before mutation begins' is judged by a fingerprint of the sandbox home and the state root (targets, journal and snapshots) taken before and after a refused `rollback --apply`, which is stronger than the Phase 6 test that compares target contents only (D-04)."
    - "LIFE-05 'preview' is judged on both no-mutation (fingerprint) and on whether the preview reflects drift that the apply will refuse; the findings decide under D-09 whether a preview that promises a restore the apply refuses fails the LIFE-05 text."
    - "Rollback drift diagnostics never enter a transcript as file content: unified-diff bodies are replaced by a line stating their line count and a 12-hex sha256 (research Pitfall 4)."
    - "LIFE-06 is judged on real interruptions: `ALPHA_AOS_FAILPOINT` at each of the five durable boundaries (after-snapshot-sync, after-intent-journal-sync, after-target-rename, after-step-sync, after-final-sync) on a single-transaction command, followed by `status`, `repair`, `repair --apply`, `status`, a restart of the original command, and a second `repair --apply` (D-01, D-04)."
    - "LIFE-06 'no false success' is judged per failpoint: `status` reporting OK is acceptable only when the journal is `applied` and the target bytes equal the post-transaction state; OK with any other journal state or with target bytes matching neither side is VIOLATED."
    - "Research open question 2 is resolved by rule: a `failed` journal counts as 'interrupted' under LIFE-06 only if a real command path produces it with target bytes matching neither the pre- nor the post-state; the executor makes at most two reproduction attempts, and without such a reproduction the `failed`-journal behavior stays a lead with the synthetic observation recorded (D-06, D-09)."
    - "LIFE-06 'restart-safe recovery guidance' is judged on the verbatim `status` and `repair` output: whether it names the repair command, the `--apply` step, and re-running the interrupted operation."
    - "The committed test `test/rollback-cli-verification.test.ts` passes on this host and under WSL before it is committed; a failing assertion is recorded as a gap reproduction instead (D-12)."
    - "`src/` is untouched (D-05)."
  prohibitions:
    - statement: "MUST NOT print unified-diff bodies or any other file content from rollback drift diagnostics into transcripts, findings or the SUMMARY."
      category: privacy
      status: resolved
      verification: judgment
    - statement: "MUST NOT set ALPHA_AOS_FAILPOINT, run `rollback --apply` or `repair --apply` in any environment other than a sandbox proven by the containment assertion; the real `~/.alpha-aos` journals are never read into evidence or modified."
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT record a hand-written synthetic journal observation as a reproduced gap; synthetic journals support leads and corrupt-journal diagnosis only (D-06)."
      category: transparency
      status: resolved
      verification: judgment
  artifacts:
    - path: "test/rollback-cli-verification.test.ts"
      provides: "CI guard: CLI rollback preview changes nothing, apply restores exact bytes, drift refusal changes no byte, journal or snapshot"
      contains: "LIFE-05"
    - path: ".planning/phases/11-managed-lifecycle-verification/evidence/life05-rollback.txt"
      provides: "Packed-CLI rollback preview, exact restore, drift refusal and LIFO evidence"
      contains: "life05.drift.zero-writes"
    - path: ".planning/phases/11-managed-lifecycle-verification/evidence/life06-recovery.txt"
      provides: "Failpoint interruption, repair and restart evidence for five boundaries plus corrupt-journal cases"
      contains: "life06.after-target-rename."
    - path: ".planning/phases/11-managed-lifecycle-verification/evidence/LIFE-05-findings.md"
      provides: "LIFE-05 clause table and verdict"
      contains: "## Provisional verdict"
    - path: ".planning/phases/11-managed-lifecycle-verification/evidence/LIFE-06-findings.md"
      provides: "LIFE-06 clause table, open-question-2 resolution, verdict"
      contains: "## Provisional verdict"
  key_links:
    - from: "test/rollback-cli-verification.test.ts"
      to: "dist/src/core/transaction.js applyFileTransaction"
      via: "creates the transaction the CLI then rolls back"
      pattern: "applyFileTransaction"
    - from: "life06-recovery.mjs"
      to: "src/core/transaction.ts configuredFailpoint"
      via: "ALPHA_AOS_FAILPOINT environment override on one CLI step"
      pattern: "ALPHA_AOS_FAILPOINT"
---

<objective>
Produce the LIFE-05 evidence (preview and apply rollback to exact prior bytes; drift blocks all writes before mutation) and the LIFE-06 evidence (corrupt or interrupted journals diagnosed as needs-repair with restart-safe guidance and no false success).

Purpose: the Phase 6 oracles use synthetic journals and compare target contents only. This plan uses real interruptions through the product's own deterministic failpoints and fingerprints the whole state root, and it commits one cheap CLI-level rollback guard (D-12).

Output: one committed test, two probes and transcripts, `LIFE-05-findings.md`, `LIFE-06-findings.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-managed-lifecycle-verification/11-CONTEXT.md
@.planning/phases/11-managed-lifecycle-verification/11-RESEARCH.md
@.planning/phases/11-managed-lifecycle-verification/evidence/README.md
@.planning/phases/11-managed-lifecycle-verification/11-01-SUMMARY.md
@src/core/transaction.ts
@src/core/repair.ts
</context>

<interfaces>
Paths: EV = `.planning/phases/11-managed-lifecycle-verification/evidence`, PROBES = `EV/probes`.

From plan 11-01 (read-only here; do not modify `probe-lib.mjs` or `test/helpers/packed-sandbox.ts`): `newSandbox`, `assertSandboxEnv`, `runCli`, `fingerprintRoots`, `compareFingerprints`, `withHostGuard`, `createTranscript`, `packOnce`; `createIsolatedSandbox`, `installPackedRelease`, `seedHarnessPrerequisites`, `harnessPaths`, `runInstalledCli`, `hostNpmCache`, `snapshotTargets`, `describeHostDrift`.

Product surfaces:
- `rollback [operation-id] [--apply] [--json]` (src/cli.ts:1352-1387): no id lists journals `id status files createdAt`; preview prints `Rollback transaction:`, `Status:`, `RESTORE <target>` or `REMOVE <target>` lines and `Dry-run only. Pass --apply to restore only if every target still matches its post-transaction hash.`; `RollbackDriftError` exits 2, JSON form prints `{ ok: false, error, diagnostics }`, human form writes `formatDriftDiagnostics` to stderr (unified diffs, src/core/transaction.ts:24-45).
- `applyFileTransaction({ stateRoot, allowedRoots, operations, session?, failpoint? })` (src/core/transaction.ts:180-215) returns a `TransactionJournal` with `id`.
- Failpoints (src/core/transaction.ts:104-134): env ALPHA_AOS_FAILPOINT with one of `after-snapshot-sync`, `after-intent-journal-sync`, `after-target-rename`, `after-step-sync`, `after-final-sync`; a hit writes `alpha-aos: deterministic failpoint reached: <name>` to stderr and exits 70.
- `status [--json]` exit 1 when needsRepair; `repair [--apply] [--json]` (src/cli.ts:1263-1294): crash plan first (`planCrashRepair`, action other than `none`), else writer repair (`Diagnosis only. Pass --apply to perform the one transition this evidence proves.`).
- `owned-skills sync alpha-aos-control --target codex [--apply] [--json]` is the candidate single-transaction command (confirm with a normal run that it adds exactly one journal).

Diff redaction rule for rollback drift steps (both JSON and human forms): run through a probe-local runner that calls `assertSandboxEnv` then `runInstalledCli`, and write the step block with every unified-diff body (lines after a `---`/`+++` header pair that start with `+`, `-`, a space, or `@@`) replaced by one line `[diff body redacted: <n> lines, sha256=<12-hex>]`; for the JSON form, replace every string field carrying a diff with the same metadata string before printing.

Existing oracles to audit (D-04): test/rollback.test.ts:18-66 (asserts target contents after refusal, not journal or snapshots), 115-166 LIFO, 167+ directory sweep; test/transaction.test.ts byte-restore tests; test/status.test.ts:87-146 synthetic incomplete/corrupt/stale journals; test/repair.test.ts:15-180; test/transaction-crash.test.ts:322-500 durable-boundary and repair tests.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LIFE-05 — packed rollback probe, a CLI rollback guard test, and the LIFE-05 findings</name>
  <files>test/rollback-cli-verification.test.ts, .planning/phases/11-managed-lifecycle-verification/evidence/probes/life05-rollback.mjs, .planning/phases/11-managed-lifecycle-verification/evidence/life05-rollback.txt, .planning/phases/11-managed-lifecycle-verification/evidence/LIFE-05-findings.md</files>
  <read_first>
    src/cli.ts (lines 1352-1387)
    src/core/transaction.ts (lines 24-45 diff generation, 180-215 options, 300-400 rollback preflight and restore)
    test/rollback.test.ts (whole file)
    test/transaction.test.ts (byte-restore tests)
    test/helpers/packed-sandbox.ts
    .planning/phases/11-managed-lifecycle-verification/evidence/probes/probe-lib.mjs
    .planning/phases/11-managed-lifecycle-verification/evidence/README.md
    .planning/phases/11-managed-lifecycle-verification/evidence/suites-local.txt
    .planning/phases/11-managed-lifecycle-verification/evidence/ci-35818198049-lifelines.txt
  </read_first>
  <behavior>
    - Test `CLI rollback preview changes no byte and apply restores exact prior bytes (LIFE-05)`: in a `createIsolatedSandbox`, `existing.txt` holds `p11 before` plus newline; `applyFileTransaction` rewrites it to `p11 after` plus newline and creates `created.txt`; `node dist/src/cli.js rollback <id>` exits 0, names both targets, and leaves the home and state fingerprints unchanged; `rollback <id> --apply --json` exits 0, `existing.txt` equals `p11 before` plus newline byte-for-byte, and `created.txt` is absent.
    - Test `CLI rollback refuses on post-transaction drift and changes no byte, journal or snapshot (LIFE-05)`: after a fresh two-file transaction, `created.txt` is modified; `rollback <id> --apply --json` exits 2 with `ok` false, and the fingerprints of the home and the whole state root are unchanged.
  </behavior>
  <action>
    Per D-01, D-04, D-09 and D-12:
    1. Write test/rollback-cli-verification.test.ts with the two behaviors, using `createIsolatedSandbox` and the fingerprint helpers from `./helpers/packed-sandbox.js`, `applyFileTransaction` from `../src/core/transaction.js` with `stateRoot` the sandbox state and `allowedRoots` the sandbox home, and `process.execPath` running `dist/src/cli.js` resolved from `import.meta.dirname` with the sandbox env. Synthetic contents only; build paths with `join`; no platform-absolute literals. Run it on this host and in WSL; commit it only if it passes both, otherwise record the failing assertion as a VIOLATED check in the probe transcript.
    2. Write PROBES/life05-rollback.mjs inside `withHostGuard` with one packed tarball and one sandbox (`installPackedRelease`, `seedHarnessPrerequisites(..., "codex", { eccCache: hostNpmCache() })`). Choose a single-transaction alpha-AOS command that modifies at least one pre-existing file and creates at least one new file (candidate: pre-write the synthetic file `<CODEX_HOME>/AGENTS.md` with `p11 pre-existing synthetic policy` plus newline, then `codex-policy sync --apply --json`; otherwise `install --target codex --apply --json` and the newest journal with two or more files); record the chosen command, the journal id from `rollback --json`, and the pre-state sha256 of every journal target. Steps and checks: `rollback <id>` and `rollback <id> --json` with `life05.preview.no-mutation` (home and state fingerprints unchanged) and `life05.preview.lists-targets` (one RESTORE or REMOVE line per journal file); `rollback <id> --apply --json` with `life05.apply.exact-bytes` (every pre-existing target's sha256 equals its pre-state) and `life05.apply.created-removed`. Drift: run the command again for a new id, append `p11 drift line` plus newline to one of its targets, fingerprint home and state, run `rollback <id2>` (preview) with `life05.preview.reflects-drift` (HOLDS when the preview reports the drift or refuses; VIOLATED when it prints RESTORE lines and the dry-run footer as if the restore would succeed), then `rollback <id2> --apply --json` and `rollback <id2> --apply` through the probe-local redacting runner with `life05.drift.refuses` (exit 2, JSON `ok` false) and `life05.drift.zero-writes` (home and whole state root fingerprints unchanged, including journal status and snapshot bytes). LIFO: two successive transactions on the same target, then `rollback <older id> --apply --json`: `life05.lifo.enforced` (refused, zero bytes changed). Write EV/life05-rollback.txt.
    3. Write EV/LIFE-05-findings.md from the README template: contract verbatim from .planning/milestones/v0.1.0-REQUIREMENTS.md:81; clause rows `preview ... rollback`, `apply rollback to restore exact prior bytes`, `any post-transaction drift prevents all rollback writes`, `before mutation begins`, each citing CHECK ids, the committed test titles, and the Phase 6 test titles with CI job lines; oracle audit (D-04): `all-or-nothing preflight refusal on drift performs zero writes` checks target contents only (test/rollback.test.ts:61-63); decide under D-09 whether `life05.preview.reflects-drift` VIOLATED fails the `preview` clause and say why; record the research lead that CLI rollback takes no writer session under `## Leads (not reproduced)` unless this task reproduced a consequence; core clause; provisional verdict per D-02; `LIFE-05/C<k>` candidates with reproductions.
  </action>
  <verify>
    <automated>npm run check && npm run build && node scripts/run-tests.mjs --files dist/test/rollback-cli-verification.test.js dist/test/path-literal-guard.test.js</automated>
    <fails_when>non-zero exit or `fail` above 0 in the summary</fails_when>
    <automated>wsl.exe --cd "$(cygpath -w "$PWD")" -e sh -c 'node scripts/run-tests.mjs --files dist/test/rollback-cli-verification.test.js'</automated>
    <fails_when>non-zero exit or `fail` above 0 in the summary (the test is not portable to Linux)</fails_when>
    <automated>node .planning/phases/11-managed-lifecycle-verification/evidence/probes/life05-rollback.mjs && T=.planning/phases/11-managed-lifecycle-verification/evidence/life05-rollback.txt && grep -q '^host-guard: unchanged' "$T" && for c in preview.no-mutation preview.lists-targets apply.exact-bytes apply.created-removed preview.reflects-drift drift.refuses drift.zero-writes lifo.enforced; do grep -qE "^CHECK life05\.$c (HOLDS|VIOLATED|NOT-OBSERVED) " "$T" || exit 1; done && ! grep -q '^+p11 drift line' "$T"</automated>
    <fails_when>non-zero exit, no unchanged host guard line, any of the eight checks missing, or a raw diff line of the drift fixture appears in the transcript</fails_when>
    <automated>F=.planning/phases/11-managed-lifecycle-verification/evidence/LIFE-05-findings.md && grep -q '^## Provisional verdict' "$F" && grep -q 'restore exact prior bytes' "$F" && test "$(grep -cE 'life05\.(preview|apply|drift|lifo)\.' "$F")" -ge 6 && test -z "$(git status --porcelain -- src)"</automated>
    <fails_when>verdict heading or contract clause missing, fewer than six check citations, or src/ changed</fails_when>
  </verify>
  <acceptance_criteria>
    - test/rollback-cli-verification.test.ts exists only if both tests passed on this host and in WSL; otherwise the SUMMARY names the failing assertion and the transcript carries it as VIOLATED.
    - EV/life05-rollback.txt names the chosen single-transaction command and contains eight life05 checks.
    - No transcript, findings file or SUMMARY contains a unified-diff body line.
    - `git status --porcelain -- src` prints nothing.
  </acceptance_criteria>
  <done>LIFE-05 is evidenced through the packed CLI with whole-state-root fingerprints, and CI guards CLI rollback restore and refusal.</done>
</task>

<task type="auto">
  <name>Task 2: LIFE-06 — real interruptions at five durable boundaries, corrupt-journal cases, guidance judgment, and the LIFE-06 findings</name>
  <files>.planning/phases/11-managed-lifecycle-verification/evidence/probes/life06-recovery.mjs, .planning/phases/11-managed-lifecycle-verification/evidence/life06-recovery.txt, .planning/phases/11-managed-lifecycle-verification/evidence/LIFE-06-findings.md</files>
  <read_first>
    src/core/transaction.ts (lines 100-135 failpoints; journal status transitions)
    src/core/repair.ts (whole file: planCrashRepair, applyCrashRepair, the outer catch at lines 90-100)
    src/core/status.ts
    src/cli.ts (lines 1263-1294 repair, 515-520 status)
    src/format.ts (lines 900-990 formatOfflineStatus, formatCrashRepairPlan, formatCrashRepairResult)
    test/transaction-crash.test.ts (lines 322-500)
    test/repair.test.ts (whole file)
    test/status.test.ts (lines 87-146)
    .planning/phases/11-managed-lifecycle-verification/11-RESEARCH.md (Evidence Map LIFE-06, Pattern 3, P5, P6, Open Question 2)
  </read_first>
  <action>
    Per D-01, D-04, D-06 and D-09, write PROBES/life06-recovery.mjs inside `withHostGuard` with one packed tarball.
    1. Command check: a sandbox (`installPackedRelease`), run `owned-skills sync alpha-aos-control --target codex --apply --json` normally and record `life06.command.single-transaction` OBSERVED (journal count before and after). If it adds more or fewer than one journal, pick another single-transaction command from the CLI help and record which.
    2. For each failpoint in the order after-snapshot-sync, after-intent-journal-sync, after-target-rename, after-step-sync, after-final-sync, in a fresh sandbox: record the target's pre-state sha256 (or absent); run the command with the env override ALPHA_AOS_FAILPOINT set to the failpoint (through `runCli` `envOverrides`), check `life06.<fp>.interrupted` (exit 70 and the failpoint stderr line); `status` and `status --json`, check `life06.<fp>.status-needs-repair` OBSERVED (the reported state) and `life06.<fp>.no-false-success` per the must_haves rule (read the journal status field and the target sha256 to decide); `repair` and `repair --json` (record the guidance lines verbatim), `repair --apply --json`, check `life06.<fp>.repair-apply` (exit 0); `status --json`, check `life06.<fp>.status-ok-after-repair`; check `life06.<fp>.target-consistent` (target sha256 equals the pre-state or the post-state, never neither); rerun the command without the override, check `life06.<fp>.restart-succeeds` (exit 0, target equals the post-state, status OK); fingerprint home and state, `repair --apply --json` again, check `life06.<fp>.repair-idempotent` (exit 0 and fingerprints unchanged).
    3. Guidance, judged over the outputs collected in step 2: `life06.guidance.names-repair-command` (the NEEDS-REPAIR status text names `alpha-aos repair`), `life06.guidance.names-apply` (the repair preview says to pass `--apply`), `life06.guidance.says-rerun` (status, repair plan or repair result tells the user to re-run the interrupted operation afterwards). Each HOLDS or VIOLATED, quoting the lines that decided it.
    4. Corrupt journals: (a) a sandbox with one real applied journal; write `<state>/journal/p11-corrupt.json` containing `{not json`, then `status --json`, `repair --json`, `repair --apply --json`, `status --json`: checks `life06.corrupt.flagged` and `life06.corrupt.resolved`. (b) P5: copy a real journal from a normal run, set its status to `applying` and delete its `files` array, write it as `<state>/journal/p11-nofiles.json`, then the same four steps: checks `life06.p5.flagged` (status needs repair) and `life06.p5.resolved` (HOLDS when repair clears it; VIOLATED when repair plans `none` and status still needs repair: a diagnosis with no working recovery path).
    5. P6, bounded (open question 2): make at most two attempts to drive a real CLI command into a journal whose status is `failed`. Attempt A: a command that journals two or more files (for example `install --target codex --apply` in a freshly seeded sandbox), with the path of a target written after the first one pre-created as a directory so that write throws after an earlier target was already written; observe the journal status and whether the compensating rollback restored the earlier target. Attempt B (only if A ends `rolled-back` or `applied`): a variation of A that also obstructs the compensating rollback (for example a read-only snapshot directory, where the platform honors it). Record each attempt's design and outcome under check `life06.p6.real-failed-journal` OBSERVED (`produced` or `not produced`). Only if produced: `life06.p6.no-false-success` by the must_haves rule. If not produced, write a synthetic `failed` journal, record `status --json` and `repair --json` as check `life06.p6.synthetic` OBSERVED, and leave P6 as a lead.
    6. Write EV/life06-recovery.txt. Then write EV/LIFE-06-findings.md from the README template: contract verbatim from .planning/milestones/v0.1.0-REQUIREMENTS.md:82; clause rows `diagnose corrupt journals as needs-repair`, `diagnose interrupted journals as needs-repair` (one row per failpoint), `restart-safe recovery guidance` (guidance checks plus restart and idempotent-repair checks), `instead of a false success state` (no-false-success checks), each citing CHECK ids and the Phase 6 test titles with CI job lines; oracle audit (D-04): the status and repair tests use hand-written journals, while `every durable boundary leaves the target diagnosable as before, after, or neither` uses failpoints in-process; state what the packed-CLI failpoint runs add; record the open-question-2 rule and the P6 outcome; core clause; provisional verdict per D-02; `LIFE-06/C<k>` candidates with reproductions.
  </action>
  <verify>
    <automated>npm run build && node .planning/phases/11-managed-lifecycle-verification/evidence/probes/life06-recovery.mjs && T=.planning/phases/11-managed-lifecycle-verification/evidence/life06-recovery.txt && grep -q '^host-guard: unchanged' "$T" && for fp in after-snapshot-sync after-intent-journal-sync after-target-rename after-step-sync after-final-sync; do test "$(grep -cE "^CHECK life06\.$fp\.(interrupted|status-needs-repair|no-false-success|repair-apply|status-ok-after-repair|target-consistent|restart-succeeds|repair-idempotent) " "$T")" -eq 8 || exit 1; done && for c in guidance.names-repair-command guidance.names-apply guidance.says-rerun corrupt.flagged corrupt.resolved p5.flagged p5.resolved p6.real-failed-journal; do grep -qE "^CHECK life06\.$c " "$T" || exit 1; done</automated>
    <fails_when>non-zero exit, no unchanged host guard line, a failpoint without exactly eight checks, or any of the eight named checks missing</fails_when>
    <automated>F=.planning/phases/11-managed-lifecycle-verification/evidence/LIFE-06-findings.md && grep -q '^## Provisional verdict' "$F" && grep -q 'restart-safe recovery guidance' "$F" && grep -qi 'open question 2' "$F" && test "$(grep -cE 'life06\.' "$F")" -ge 10 && test -z "$(git status --porcelain -- src)"</automated>
    <fails_when>verdict heading, contract clause or the open-question-2 resolution missing, fewer than ten check citations, or src/ changed</fails_when>
  </verify>
  <acceptance_criteria>
    - EV/life06-recovery.txt has forty failpoint checks (eight for each of five failpoints) and the guidance, corrupt, P5 and P6 checks.
    - The P6 outcome is recorded as produced or not produced, with each attempt's design; a not-produced outcome appears in LIFE-06-findings.md under `## Leads (not reproduced)`, not as a gap candidate.
    - `git status --porcelain -- src` prints nothing.
  </acceptance_criteria>
  <done>LIFE-06 is judged on real interruptions at every durable boundary with restart and idempotence evidence, and the findings resolve open question 2 by rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| failpoint env to host | ALPHA_AOS_FAILPOINT kills a writer mid-transaction; on real state it would leave a real interrupted journal |
| drift diagnostics to transcripts | The product prints unified diffs of managed files |
| committed tests to CI | New tests run on three OS legs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-22 | Tampering | failpoint and repair steps | critical | mitigate | Failpoint set only through `runCli` `envOverrides`, whose containment assertion runs first; host guard before and after; real journals never read |
| T-11-23 | Information disclosure | rollback drift output | high | mitigate | Probe-local redacting runner replaces diff bodies with line count and hash; acceptance greps for a raw drift-fixture diff line |
| T-11-24 | Denial of service | `main` CI | medium | mitigate | The rollback guard test is committed only after passing on Windows and in WSL (D-12) |
| T-11-25 | Repudiation | P6 judgment | low | mitigate | The open-question-2 rule is fixed in this plan before evidence exists, so the verdict cannot be tuned to the outcome |
</threat_model>

<verification>
- `node scripts/run-tests.mjs --files dist/test/rollback-cli-verification.test.js` passes on this host and in WSL.
- Both transcripts end with a `RESULT:` line and an unchanged host guard.
- Host-safe full suite: `npm run build && H=$(mktemp -d) && WH=$(cygpath -w "$H" 2>/dev/null || printf '%s' "$H") && env -u ALPHA_AOS_STATE_DIR -u CODEX_HOME -u CLAUDE_CONFIG_DIR -u HERMES_HOME -u PI_CODING_AGENT_DIR -u ANTIGRAVITY_CONFIG_DIR HOME="$WH" USERPROFILE="$WH" ALPHA_AOS_REQUIRE_UPSTREAM_MCP=1 node scripts/run-tests.mjs --files $(ls dist/test/*.test.js | grep -v capability-oracle) && test ! -e "$H/.alpha-aos"` exits 0.
- `git status --porcelain -- src` prints nothing.
</verification>

<success_criteria>
- Every LIFE-05 and LIFE-06 clause has a HOLDS row or a named gap candidate with a reproduction.
- Research open question 2 is resolved by the stated rule, with the P6 attempts recorded.
</success_criteria>

## Artifacts this phase produces

- `test/rollback-cli-verification.test.ts` with tests `CLI rollback preview changes no byte and apply restores exact prior bytes (LIFE-05)` and `CLI rollback refuses on post-transaction drift and changes no byte, journal or snapshot (LIFE-05)` (committed only if passing).
- Probes `PROBES/life05-rollback.mjs`, `PROBES/life06-recovery.mjs`; transcripts `EV/life05-rollback.txt`, `EV/life06-recovery.txt`; findings `EV/LIFE-05-findings.md`, `EV/LIFE-06-findings.md`.
- Check ids `life05.preview.*`, `life05.apply.*`, `life05.drift.*`, `life05.lifo.enforced`, `life06.command.single-transaction`, `life06.<failpoint>.*`, `life06.guidance.*`, `life06.corrupt.*`, `life06.p5.*`, `life06.p6.*`; gap candidate keys `LIFE-05/C<k>`, `LIFE-06/C<k>`.
- Redaction line format `[diff body redacted: <n> lines, sha256=<12-hex>]`.

<output>
Create `.planning/phases/11-managed-lifecycle-verification/11-06-SUMMARY.md` when done. Include the chosen rollback command, the eight LIFE-05 outcomes, the five-failpoint table, the guidance outcomes, the P5 and P6 results, whether the rollback test was committed, and both provisional verdicts.
</output>
