---
phase: 03-transactional-project-packs-and-native-optional-use
plan: 16
type: execute
wave: 1
depends_on: []
gap_closure: true
gap_ids: [G-03-1]
files_modified:
  - catalog/canaries.yaml
  - src/adapters/capability-oracle.ts
  - src/adapters/isolation.ts
  - src/core/canary.ts
  - test/canary.test.ts
  - test/isolation.test.ts
autonomous: true
requirements: [CAPA-01, CAPA-07]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A user with a working Codex login can run the CAPA-01 invocation canary on Codex and prove ordered, version-scoped Context7 use without any Claude account or Claude model turn."
    - "Codex invocation is a separate, costed native `codex exec --json --ephemeral` path; free discovery remains on `codex debug prompt-input` and is not misreported as invocation."
    - "The Codex canary loads only runtime-local MCP entries that point through the alpha-AOS observation front, while user-managed authentication is reused by reference and never copied, rendered, journaled, or deleted."
    - "Readiness evaluates the validated runtime-local Codex MCP registration, so Context7 is neither rejected for lacking a global listing nor assumed present without a rendered entry."
    - "The existing Claude declaration and supported-harness code remain present; the newer G-03-1 constraint changes the active verification path, not compatibility claims."
  artifacts:
    - path: "src/adapters/capability-oracle.ts"
      provides: "separate discovery and costed invocation definitions for Codex"
      exports: ["ORACLE_DEFINITIONS"]
    - path: "src/adapters/isolation.ts"
      provides: "Codex canary launch isolation using ignore flags and runtime-derived MCP overrides"
      exports: ["createIsolationLaunchSpec"]
    - path: "src/core/canary.ts"
      provides: "Codex runtime override construction, readiness evidence, cost reporting, and native launch wiring"
      exports: ["createCanaryRuntime", "canaryPromptArgs", "probeReadiness", "runCanary"]
    - path: "catalog/canaries.yaml"
      provides: "a declared Codex leg for DOCUMENTATION_VERSION_SCOPED while retaining Claude"
      contains: "DOCUMENTATION_VERSION_SCOPED"
    - path: "test/canary.test.ts"
      provides: "offline end-to-end regression coverage for the Codex CAPA-01 path"
    - path: "test/isolation.test.ts"
      provides: "focused auth/config isolation and everyday-path non-regression coverage"
  key_links:
    - from: "catalog/canaries.yaml"
      to: "src/core/canary.ts"
      via: "selectCanaries chooses the Codex DOCUMENTATION_VERSION_SCOPED leg and runCanary uses the invocation definition"
      pattern: "DOCUMENTATION_VERSION_SCOPED|canaryPromptArgs"
    - from: "src/core/canary.ts"
      to: "src/adapters/isolation.ts"
      via: "the runtime-derived MCP override vector is passed into the one isolation launch-spec builder"
      pattern: "createIsolationLaunchSpec"
    - from: "src/adapters/isolation.ts"
      to: "src/core/mcp-proxy.ts"
      via: "every Codex MCP override resolves to the runtime rendering whose commands invoke the observe front"
      pattern: "--ignore-user-config|mcp_servers"
    - from: "src/core/canary.ts"
      to: "user-managed Codex authentication"
      via: "Codex's authenticated home is passed through only for auth while configuration is ignored and session state is ephemeral"
      pattern: "CODEX_HOME|--ephemeral"
---

<objective>
Close the executable half of G-03-1 with one production Codex tracer and its isolation invariants.

Purpose: Phase 3 already has a harness-independent observation matcher that accepts the required ordered
`resolve-library-id` then `query-docs` sequence and the version-scoped identifier shape. The missing path is
before that matcher: the catalog selects no Codex leg, invocation reuses the free discovery vector, readiness
cannot see runtime-local registrations, and the isolation adapter rejects Codex. This plan connects those seams
without using Claude.

Output: a declared, costed Codex CAPA-01 leg driven by native `codex exec`, a runtime-local observation-front
configuration that preserves user-managed authentication without copying it, Codex-aware readiness, and focused
offline plus native verification.
</objective>

<execution_context>
@C:/Users/alpha/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-CONTEXT.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-UAT.md
@.planning/debug/codex-native-verification-gap.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-05-SUMMARY.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-06-SUMMARY.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-08-SUMMARY.md
</context>

## Artifacts this plan produces

| Kind | Name | Location |
|------|------|----------|
| adapter contract | costed Codex invocation definition | `src/adapters/capability-oracle.ts` |
| launch vector | native Codex `exec` arguments | `src/core/canary.ts` |
| isolation composition | ignored user config plus runtime MCP overrides | `src/adapters/isolation.ts` |
| declared canary leg | `DOCUMENTATION_VERSION_SCOPED` on Codex | `catalog/canaries.yaml` |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end Codex CAPA-01 invocation through the observation front</name>
  <precondition>`codex login status` exits zero, and `codex exec --help` lists `--json`, `--ephemeral`, `--ignore-user-config`, `--ignore-rules`, `--sandbox`, and `-c/--config`; if this measured surface changed, stop before editing and report the unsupported parity rather than substituting a simulated driver.</precondition>
  <reversibility rating="costly">The invocation-definition and launch-isolation contracts are shared by later canaries, so undoing their shape would require coordinated changes across the runner, adapter, and tests.</reversibility>
  <read_first>
    - .planning/debug/codex-native-verification-gap.md — confirmed root cause, eliminated hypotheses, the measured Codex 0.154 surface, and the explicit blind spot requiring an implementation-time isolation/auth proof.
    - .planning/phases/03-transactional-project-packs-and-native-optional-use/03-CONTEXT.md — D-01, D-02, D-04, D-09, and D-12. The user's later G-03-1 direction supersedes D-09 only for the active development/verification harness; preserve the existing Claude declaration and compatibility path.
    - src/adapters/capability-oracle.ts — `ORACLE_DEFINITIONS`, `ORACLE_PLACEHOLDER_PROMPT`, and the free Codex `debug prompt-input` vector. That table remains the discovery authority.
    - src/adapters/isolation.ts — `CANARY_MCP_ISOLATION`, the Codex project-only branch, and `createIsolationLaunchSpec`'s canary option.
    - src/core/canary.ts — `CanaryRuntime`, `createCanaryRuntime`, `READINESS_DEFINITIONS`, `probeReadiness`, `canaryCosts`, `canaryPromptArgs`, `canaryEnvironmentPolicy`, `assertCanaryLaunchIsolation`, `createCanaryLaunchSpec`, and `runCanary`.
    - catalog/canaries.yaml — `DOCUMENTATION_VERSION_SCOPED`; retain its prompt and expectation contract byte-for-byte except for adding the Codex harness leg.
    - test/canary.test.ts — the cost tests, runtime rendering tests, invocation-axis test, CAPA-01 matcher fixtures, no-spend sweep, and the current Codex-blocked assertions that G-03-1 invalidates.
  </read_first>
  <behavior>
    - Test 1: the shipped catalog selects exactly one `DOCUMENTATION_VERSION_SCOPED` Codex pair when filtered by harness and capability, while the existing Claude pair remains declared.
    - Test 2: Codex discovery still uses `debug prompt-input` and costs no model turn; Codex invocation uses `exec --json --ephemeral`, costs a model turn, and never contains `debug prompt-input`.
    - Test 3: a Codex runtime with Context7 produces config overrides for exactly the runtime's fronted Context7 entry; no override names an ambient or project MCP entry.
    - Test 4: Codex readiness is ready when every required MCP id exists in the validated runtime registration, and returns `MCP_SERVER_NOT_REGISTERED` when Context7 is absent.
    - Test 5: a supplied Codex launcher that records ordered Context7 observations produces `nativeUse: invoked`, `verdict.held: true`, `costsModelTurn: true`, and an oracle fingerprint without consulting model text.
    - Test 6: serialized launch args, readiness, result, and runtime files contain credential names where required but contain no credential values and no copied authentication bytes.
  </behavior>
  <files>catalog/canaries.yaml, src/adapters/capability-oracle.ts, src/adapters/isolation.ts, src/core/canary.ts, test/canary.test.ts</files>
  <action>
Start RED with the six behaviors above, then implement one Codex CAPA-01 path through every existing layer.

In `src/adapters/capability-oracle.ts`, add an invocation-definition contract separate from
`ORACLE_DEFINITIONS`. Keep the latter and its free Codex discovery vector unchanged. The Codex invocation
definition must use the measured non-interactive `exec` surface with JSONL output, ephemeral session state,
read-only sandboxing, ignored user config, ignored exec-policy rules, and a substituted ordinary prompt; mark
it as spending a model turn. Keep the Claude invocation vector equivalent to its current behavior so this is
an additive harness adapter, not a compatibility removal. Make `canaryCosts`, `canaryCostsModelTurn`, and
`canaryPromptArgs` consume the invocation definition per D-01 and G-03-1; discovery continues consuming the
free oracle table.

In `createCanaryRuntime`, derive Codex `-c mcp_servers.<id>...` overrides from the same already-rendered TOML
document written into the runtime. Parse with the existing pinned `smol-toml` dependency; do not hand-parse
TOML and do not add a package. Accept only the closed server fields the renderer emits, preserve array and
string types through TOML-safe values, and refuse a missing, extra, malformed, or unfronted server before a
launch. Store the derived override vector on the runtime so `createCanaryLaunchSpec` passes measured runtime
data into the existing adapter rather than reconstructing server commands elsewhere. Per D-02, every command
must still invoke `alpha-aos mcp-proxy <id> --observe` with this run's observation path.

For the Codex canary branch in `src/adapters/isolation.ts`, use the runtime override vector together with
`--ignore-user-config` and `--ignore-rules`; do not set the ordinary isolated `CODEX_HOME`, `HOME`, or
`USERPROFILE` literals on this branch. Authentication resolution remains Codex-owned: pass through an existing
`CODEX_HOME`, or the platform home name Codex already uses when that override is absent, only as an allowlisted
source value. Do not inspect, copy, serialize, journal, delete, or print `auth.json` or any credential value.
Keep `--ephemeral` and runtime-pinned cache/data/write roots so alpha-AOS creates no session state under the
authenticated root. Update `assertCanaryLaunchIsolation` so this narrow Codex auth-root exception is allowed
only when the invocation carries both ignore flags and the complete runtime-derived MCP override vector; every
other harness config root outside the runtime remains a refusal.

Teach readiness to accept validated runtime-local registrations as evidence for Codex only. `runCanary` must
pass its runtime server set into the pre-probe; a required server absent from that set produces the existing
actionable registration code, while a present server is registered/pending until the native launch proves the
call. Do not invent a global Codex `mcp list` oracle.

Finally add `codex` to the `DOCUMENTATION_VERSION_SCOPED` harness list only. Preserve the prompt, ordered tool
expectations, identifier-shape expectation, Claude leg, and harness-independent matcher. This implements D-01,
D-02, D-04 and D-12 under the user's newer active-harness direction for G-03-1; it does not broaden the other
UAT canaries or claim unsupported parity.
  </action>
  <verify>
    <automated>npm run check &amp;&amp; npm run build &amp;&amp; node scripts/run-tests.mjs --files dist/test/canary.test.js</automated>
    <fails_when>non-zero exit, any target test failure, or the Codex invocation tests do not execute</fails_when>
    <automated>node -e "const{spawnSync}=require('node:child_process');const r=spawnSync(process.execPath,['dist/src/cli.js','doctor','--canary','.','--harness','codex','--capability','CAPA-01','--json'],{encoding:'utf8'});if(r.status!==0)throw new Error(r.stderr||r.stdout);const j=JSON.parse(r.stdout);const x=j.results?.[0];if(x?.harness!=='codex'||x?.nativeUse!=='invoked'||x?.verdict?.held!==true)throw new Error('Codex CAPA-01 did not hold');console.log('Codex CAPA-01 invoked')"</automated>
    <fails_when>Codex does not launch natively, Context7 observations are absent/out of order/unscoped, the result is not invoked, or any Claude authentication is requested</fails_when>
  </verify>
  <done>The shipped Codex CAPA-01 leg runs through native `codex exec`, and only observation-front evidence promotes it to invoked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the Codex auth/config boundary and preserve everyday isolation</name>
  <read_first>
    - test/isolation.test.ts — the canary launch tests added by 03-06 and the everyday Codex project-only launch tests.
    - test/canary.test.ts — environment allowlist, runtime containment, credential sentinel, single-use runtime, and source-level forbidden-secret-flag tests.
    - src/adapters/isolation.ts — the completed Task 1 Codex canary branch and the untouched ordinary Codex branch.
    - src/core/canary.ts — the completed Task 1 runtime override and narrow auth-root isolation assertions.
  </read_first>
  <behavior>
    - Test 1: the Codex canary branch preserves a supplied `CODEX_HOME` reference, emits both ignore flags once, uses only runtime-derived MCP overrides, and has no blocked isolation reason.
    - Test 2: with no `CODEX_HOME`, the platform home source remains available for Codex authentication while every alpha-AOS-controlled write-root points under the canary runtime.
    - Test 3: omitting either ignore flag, adding an ambient server override, changing an observe-front command, or pointing an alpha-AOS-controlled write root outside the runtime is refused before the launcher runs.
    - Test 4: the ordinary non-canary Codex project-only launch remains byte-for-byte equivalent in args/env/guarantees and still isolates `CODEX_HOME` under its runtime.
    - Test 5: Claude, Pi, Antigravity, and Hermes retain their prior launch support or explicit unsupported reasons; no test requires a Claude login or model turn.
  </behavior>
  <files>test/canary.test.ts, test/isolation.test.ts</files>
  <action>
Expand the offline tests around the production tracer. Use synthetic auth-root paths and credential sentinels;
never read a real auth file. Prove the narrow exception, each refusal branch, the runtime write roots, and the
exact preservation of the ordinary Codex branch. Replace the obsolete assertion that every non-Claude harness
must be blocked with a table that expects Codex to hold the new measured composition and expects the other
three harnesses to retain their explicit reasons. Keep all tests platform-independent by supplying source
environments and paths rather than branching only on the developer host.
  </action>
  <verify>
    <automated>npm run check &amp;&amp; npm run build &amp;&amp; node scripts/run-tests.mjs --files dist/test/canary.test.js dist/test/isolation.test.js</automated>
    <fails_when>non-zero exit, any target test failure, a refusal branch reaches its launcher, or an unrelated harness contract changes</fails_when>
  </verify>
  <done>The Codex exception is mechanically limited to authentication lookup, runtime MCP overrides, and ephemeral read-only invocation; everyday and other-harness behavior is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated Codex home → ephemeral canary | user-managed authentication is reused while user configuration and persistent session state must stay outside the run |
| runtime TOML → Codex `-c` overrides | a root-bounded rendered document becomes native CLI configuration arguments |
| Codex model → MCP observation front | untrusted model behavior reaches only the fronted Context7 process; model text is not proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-130 | Information Disclosure | Codex authenticated home | high | mitigate | Pass only the existing auth-root environment name/value to Codex, never read/copy/render/journal auth bytes, retain redacted bounded process capture, and assert credential sentinels are absent from all observable results |
| T-03-131 | Tampering | runtime MCP override vector | high | mitigate | Derive overrides from validated runtime TOML, require the exact declared server set, require each command to carry this run's observe front, and refuse any extra/unfronted entry before launch |
| T-03-132 | Spoofing | invocation verdict | high | mitigate | D-01 remains authoritative: only ordered MCP-side observations and the version-scoped identifier shape produce `invoked`; Codex JSONL/model text is diagnostic only |
| T-03-133 | Elevation of Privilege | Codex execution | medium | mitigate | Pin the native invocation to read-only sandbox, ignored user config/rules, declared environment names, bounded output, and an ephemeral session |
| T-03-134 | Repudiation | discovery/invocation cost split | medium | mitigate | Keep separate definition tables and assert the exact Codex command family and cost bit for each path |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package; `smol-toml` is already pinned and locked in the repository |
</threat_model>

## Source Coverage Audit

| SOURCE | ID | Item | Plan | Status | Notes |
|--------|----|------|------|--------|-------|
| GOAL | — | optional capability use is proven natively, not inferred from file presence | 03-16 | COVERED | native Codex run plus MCP-side observations |
| REQ | CAPA-01 | version-sensitive native Context7 selection and meaningful read-only call | 03-16 | COVERED | ordered/version-scoped CAPA-01 tracer |
| REQ | CAPA-07 | distinguish blocked, unverified, and invoked from evidence | 03-16 | COVERED | runtime-local readiness and observation verdict |
| RESEARCH | — | Codex invocation uses `codex exec --json` while discovery uses `debug prompt-input` | 03-16 | COVERED | separate driver definitions |
| CONTEXT | D-01 | non-interactive run plus MCP-side observation | 03-16 | COVERED | tracer preserves observation authority |
| CONTEXT | D-02 | observing front only in canary runtime | 03-16 | COVERED | ignore flags plus exact runtime overrides; everyday path unchanged |
| CONTEXT | D-04 | evidence binds the exact harness/server inputs | 03-16 | COVERED | existing proof binding now receives the Codex version and observed server version |
| CONTEXT | D-09 | original Claude pass bar | 03-16 | COVERED WITH USER OVERRIDE | newer G-03-1 direction makes Codex the active verification surface; Claude compatibility is retained but not invoked |
| CONTEXT | D-12 | blocked is named/actionable; unverified is absence/no attempt | 03-16 | COVERED | runtime registration decides the readiness branch |
| GAP | G-03-1 | Phase 3 native-use verification runs on Codex without Claude | 03-16 | COVERED | direct gap truth |

All other CAPA requirements and D-03/D-05…D-08/D-10/D-11/D-13…D-16 are already delivered Phase 3
contracts with no diagnosed missing item in G-03-1. They remain unchanged. Deferred ideas remain excluded.

<verification>
- `npm run check &amp;&amp; npm run build &amp;&amp; node scripts/run-tests.mjs --files dist/test/canary.test.js dist/test/isolation.test.js` passes.
- The native Codex CAPA-01 command exits zero and reports one Codex result with `nativeUse: invoked` and `verdict.held: true`.
- No verification command invokes Claude or requires Claude authentication.
</verification>

<success_criteria>
- Codex is a declared, costed invocation surface for CAPA-01.
- Runtime-local Context7 registration passes readiness and every actual call crosses the observation front.
- User-managed Codex authentication is preserved without copying secret material.
- Discovery, everyday isolation, and all non-Codex compatibility paths retain their prior contracts.
</success_criteria>

<output>
Create `.planning/phases/03-transactional-project-packs-and-native-optional-use/03-16-SUMMARY.md` when done
</output>
