---
phase: 07-cross-platform-release-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .npmignore
  - scripts/audit-tarball.mjs
  - .github/workflows/ci.yml
  - test/tarball-fixture.test.ts
autonomous: true
requirements: [REL-01, REL-05]

must_haves:
  truths:
    - "CI builds and packs a single authoritative alpha-aos-0.1.0.tgz on Linux, uploads it as a workflow artifact, and all three OS runners download and test the exact same byte-identical archive (REL-01, REL-05, D-01)."
    - "The release tarball contains only allowlisted files and strictly excludes catalog/candidate.lock.json, .planning/, test/, .ts sources, .git, and local dev state via triple-gate protection (REL-05, D-02)."
    - "scripts/audit-tarball.mjs decompresses and inspects tarball archive manifests using pure Node.js built-ins, failing closed on any unallowlisted entry (REL-05, D-02)."
    - "Cross-platform fixture tests run inside an isolated temporary sandbox with HOME, USERPROFILE, ALPHA_AOS_STATE_DIR, harness configs, and npm_config_prefix redirected to temp roots, causing zero host mutations (REL-01, D-03)."
    - "The fixture suite sequentially executes prefix install -> install --apply -> second install verifying CURRENT idempotency -> status and doctor -> uninstall --all --yes verifying clean baseline restoration (REL-01, D-04)."
  prohibitions:
    - statement: "MUST NOT allow catalog/candidate.lock.json, .planning/, test/, or uncompiled .ts files into the packed release tarball (REL-05, D-02)."
      category: safety
      status: resolved
      verification: automated
    - statement: "MUST NOT mutate real user home directories, user configuration roots, or machine shared roots during fixture lifecycle tests (REL-01, D-03)."
      category: safety
      status: resolved
      verification: automated
    - statement: "MUST NOT hand-roll shell-dependent tar decompression commands; all archive parsing must use pure Node.js (node:zlib) built-ins (REL-05, D-02)."
      category: portability
      status: resolved
      verification: automated
  artifacts:
    - path: "scripts/audit-tarball.mjs"
      provides: "Pure Node.js fail-closed tarball content validator and allowlist auditor"
      exports: ["listTarballFiles", "auditTarballEntries"]
    - path: ".npmignore"
      provides: "Secondary archive exclusion filter preventing accidental bundling of candidate lock or dev state"
    - path: ".github/workflows/ci.yml"
      provides: "Multi-OS CI pipeline with authoritative Linux pack job and 3-OS artifact download matrix"
    - path: "test/tarball-fixture.test.ts"
      provides: "End-to-end lifecycle verification of packed tarball in isolated temporary sandbox"
---

<objective>
Establish the authoritative release packaging pipeline, automated tarball content audit, and multi-OS fixture lifecycle verification (REL-01, REL-05, D-01, D-02, D-03, D-04).

Purpose: Guarantee that alpha-AOS produces a strictly allowlisted, byte-identical release package (`alpha-aos-0.1.0.tgz`) that excludes candidate locks and local state, and verify that the identical archive installs, reconciles, diagnoses, and uninstalls cleanly across Windows, macOS, and Linux sandboxes without host root mutation.
Output: `scripts/audit-tarball.mjs`, updated `package.json`, `.npmignore`, updated `.github/workflows/ci.yml`, and integration test suite `test/tarball-fixture.test.ts`.
</objective>

<execution_context>
Execution via GSD workflow adhering to anti-shallow deep work rules.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-cross-platform-release-proof/07-CONTEXT.md
@.planning/phases/07-cross-platform-release-proof/07-RESEARCH.md
@.planning/phases/07-cross-platform-release-proof/07-PATTERNS.md
@.planning/phases/07-cross-platform-release-proof/07-VALIDATION.md
@package.json
@.github/workflows/ci.yml
@scripts/build-artifact.mjs
@scripts/run-tests.mjs
@test/lifecycle.test.ts
@test/uninstall.test.ts
</context>

<interfaces>
```typescript
// Proposed exports from scripts/audit-tarball.mjs
export interface TarballEntry {
  readonly path: string;
  readonly size: number;
}

export function listTarballFiles(tarballPath: string): TarballEntry[];
export function auditTarballEntries(files: readonly TarballEntry[]): string[];
```
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Allowlisted Tarball Packaging Pipeline, Content Audit & Authoritative CI Matrix (REL-05, D-01, D-02)</name>
  <read_first>
    .planning/phases/07-cross-platform-release-proof/07-CONTEXT.md
    .planning/phases/07-cross-platform-release-proof/07-RESEARCH.md
    .planning/phases/07-cross-platform-release-proof/07-PATTERNS.md
    package.json
    .github/workflows/ci.yml
    scripts/build-artifact.mjs
  </read_first>
  <files>package.json, .npmignore, scripts/audit-tarball.mjs, .github/workflows/ci.yml</files>
  <behavior>
    - Tarball content audit unpacks gzip tar archives using pure Node.js (node:zlib.gunzipSync) and 512-byte POSIX tar header parsing without external shell utilities.
    - Tarball audit strictly fails closed with non-zero exit code if catalog/candidate.lock.json, .planning/ files, test/ directories, uncompiled .ts files, .git files, or .log files are present in the tarball.
    - Tarball audit validates that every included file matches an explicit positive allowlist (package.json, README.md, LICENSE, CHANGELOG.md, dist/build-artifact.json, dist/src/**, catalog/stack.yaml, catalog/stack.lock.json, catalog/facts.yaml, catalog/canaries.yaml, catalog/packs/**, schemas/**, scripts/**, skills/**, docs/**).
    - package.json files array is tightened from broad catalog/ directory to individual catalog files and directories, preventing accidental inclusion of candidate locks.
    - Root .npmignore provides secondary exclusion of catalog/candidate.lock.json, .planning/, test/, scratch/, and dev artifacts.
    - CI workflow in .github/workflows/ci.yml builds and packs alpha-aos-0.1.0.tgz on an authoritative Linux runner (ubuntu-latest), uploads it as a workflow artifact along with its SHA-256 digest, and matrix test jobs download and verify the exact same archive.
  </behavior>
  <action>
Implement the allowlisted packaging and audit pipeline adhering to D-01, D-02, and REL-05:

1. Create `scripts/audit-tarball.mjs`:
   - Implement `listTarballFiles(tarballPath)` using pure Node.js built-ins (`node:fs` and `node:zlib.gunzipSync`).
   - Decode standard 512-byte POSIX ustar blocks. Extract entry filenames, strip the npm standard leading `package/` prefix, handle ustar prefix fields up to 155 characters, extract file sizes from octal header fields, and return an array of `{ path, size }` objects.
   - Implement `auditTarballEntries(files)`:
     - Define forbidden regex patterns: `candidate\.lock\.json`, `^\.planning/`, `^test/`, `\.ts$`, `^\.git`, `\.log$`, and `^scratch/`.
     - Define allowed exact files: `package.json`, `README.md`, `LICENSE`, `CHANGELOG.md`, `dist/build-artifact.json`.
     - Define allowed prefix paths: `dist/src/`, `catalog/`, `schemas/`, `scripts/`, `skills/`, `docs/`.
     - Within `catalog/`, explicitly disallow `candidate.lock.json`.
     - Return an array of violation strings. If violations exist, format actionable error diagnostics and exit with code 3.
   - Support CLI invocation: accept an optional tarball path argument. If no tarball path argument is passed and `alpha-aos-0.1.0.tgz` is not found in the root directory (or if `--prepack` is supplied), `scripts/audit-tarball.mjs` automatically packs a temporary tarball via `npm pack` before auditing, ensuring standalone execution such as `node scripts/audit-tarball.mjs` succeeds even before an explicit pack step. Exit 0 on success or exit 3 with detailed violation diagnostics on failure.

2. Update `package.json`:
   - Refine the `files` array from `"catalog/"` to explicit catalog entries: `"catalog/stack.yaml"`, `"catalog/stack.lock.json"`, `"catalog/facts.yaml"`, `"catalog/canaries.yaml"`, `"catalog/packs/"`. Ensure `"dist/src/"`, `"dist/build-artifact.json"`, `"schemas/"`, `"scripts/"`, `"skills/"`, `"docs/"`, `"README.md"`, `"CHANGELOG.md"`, and `"LICENSE"` are listed.
   - Add `"audit-tarball": "node scripts/audit-tarball.mjs"` to `scripts`.
   - Update `prepack` script to run `npm run build && node scripts/audit-tarball.mjs --prepack`.

3. Create `.npmignore`:
   - Explicitly list exclusions: `catalog/candidate.lock.json`, `.planning/`, `test/`, `scratch/`, `*.ts`, `tsconfig.json`, `.github/`, `*.log`.

4. Update `.github/workflows/ci.yml`:
   - Add an authoritative `package` job running on `ubuntu-latest`. It checks out code, sets up Node 24, runs `npm ci`, `npm run check`, `npm run build`, `npm run build:check`, executes `npm pack`, audits the generated `alpha-aos-0.1.0.tgz` via `node scripts/audit-tarball.mjs alpha-aos-0.1.0.tgz`, generates `alpha-aos-0.1.0.tgz.sha256` via `sha256sum`, and uploads both files using `actions/upload-artifact@v4` under artifact name `release-tarball`.
   - Update the existing `test` matrix job to declare `needs: package`, run on `ubuntu-latest`, `macos-latest`, and `windows-latest`, download the `release-tarball` artifact, verify byte-level SHA-256 checksum matching, and execute the test suites including the tarball fixture suite.
  </action>
  <acceptance_criteria>
    - `scripts/audit-tarball.mjs` executes using pure Node.js (automatically packing a tarball if none exists or is specified), and fails closed with code 3 if candidate.lock.json or unallowlisted files exist in the tarball (REL-05, D-02).
    - `package.json` files field and `.npmignore` exclude candidate locks and test/planning files while including dist/build-artifact.json and CHANGELOG.md (REL-05, REL-06, D-02, D-13).
    - CI workflow builds once authoritatively on Linux, uploads artifact, and 3-OS matrix downloads the identical tarball (REL-01, D-01).
    - `npm run check && npm run build && node scripts/audit-tarball.mjs` exits with code 0 on the clean build.
  </acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node scripts/audit-tarball.mjs</automated>
    <fails_when>non-zero exit code, or audit reports violations</fails_when>
  </verify>
  <done>Authoritative tarball packaging pipeline, fail-closed content audit, and multi-OS CI workflow configuration are operational.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multi-OS Tarball Fixture Lifecycle Suite in Temp-Root Sandbox (REL-01, D-03, D-04)</name>
  <read_first>
    .planning/phases/07-cross-platform-release-proof/07-CONTEXT.md
    .planning/phases/07-cross-platform-release-proof/07-RESEARCH.md
    .planning/phases/07-cross-platform-release-proof/07-PATTERNS.md
    test/lifecycle.test.ts
    test/uninstall.test.ts
    scripts/run-tests.mjs
    src/core/install.ts
    src/core/status.ts
  </read_first>
  <files>test/tarball-fixture.test.ts</files>
  <behavior>
    - Creates a completely isolated temporary sandbox using mkdtemp, redirecting HOME, USERPROFILE, ALPHA_AOS_STATE_DIR, CODEX_HOME, ANTIGRAVITY_CONFIG_DIR, CLAUDE_CONFIG_DIR, PI_CODING_AGENT_DIR, HERMES_HOME, and npm_config_prefix into the sandbox.
    - Resolves cross-platform binary paths correctly on both Windows (prefix/alpha-aos.cmd or prefix/bin/alpha-aos.cmd) and POSIX systems (prefix/bin/alpha-aos).
    - Performs isolated prefix installation (npm install -g tarball --prefix sandbox/prefix) without network access and without touching shared system directories.
    - Executes initial install --apply and verifies that all managed components are laid down cleanly inside the sandbox.
    - Executes a second install and verifies that all components report CURRENT, 0 writes occur, and exit code is 0 (idempotency).
    - Executes status and doctor commands, asserting all diagnostic checks pass cleanly.
    - Executes uninstall --all --yes, verifying clean restoration of the sandbox baseline with zero lingering alpha-AOS files.
    - Verifies that zero files were created or modified in the actual user home directory or outside the sandbox root.
  </behavior>
  <action>
Implement `test/tarball-fixture.test.ts` adhering to D-03, D-04, and REL-01:

1. In `test/tarball-fixture.test.ts`:
   - Implement `createIsolatedSandbox(context)` helper:
     - Allocate a fresh temporary directory via `mkdtemp(join(tmpdir(), "alpha-aos-fixture-"))`.
     - Register `context.after()` cleanup hook to recursively remove the sandbox using `rm` with retries.
     - Create subdirectories for `home`, `state`, `prefix`, and `repo`.
     - Construct an environment object overriding `HOME`, `USERPROFILE`, `ALPHA_AOS_STATE_DIR`, `CODEX_HOME`, `ANTIGRAVITY_CONFIG_DIR`, `CLAUDE_CONFIG_DIR`, `PI_CODING_AGENT_DIR`, `HERMES_HOME`, and `npm_config_prefix`.
     - Implement `resolveCli(prefix)` resolving `join(prefix, "alpha-aos.cmd")` on Windows (falling back to `join(prefix, "bin", "alpha-aos.cmd")` if present) and `join(prefix, "bin", "alpha-aos")` on macOS and Linux.
     - Implement `runCli(args, env)` executing the resolved CLI using `child_process.spawnSync` with `windowsHide: true` and returning exit status, stdout, and stderr.

2. Write the sequential lifecycle test suite:
   - Test 1 (Pack and Prefix Install): Pack the local package to a temporary archive using `npm pack`, then run `npm install -g <tarball> --prefix <prefixDir>` inside the isolated sandbox. Verify exit status is 0 and the resolved binary exists on disk.
   - Test 2 (Initial Install Apply): Invoke `alpha-aos install --apply` using the installed binary. Assert exit status is 0, state directory contains valid journals and locked catalog references, and native harness configurations are generated within the sandboxed home directory.
   - Test 3 (Reconciliation Idempotency): Invoke a second `alpha-aos install` without modifications. Assert that output indicates all components are `CURRENT`, 0 mutations were written, and exit status is 0.
   - Test 4 (Diagnostics Health): Invoke `alpha-aos status` and assert exit status is 0 and `needsRepair` is false. Invoke `alpha-aos doctor` and assert all health findings pass without critical failures.
   - Test 5 (Complete Clean Uninstall): Invoke `alpha-aos uninstall --all --yes --apply`. Assert exit status is 0. Inspect the sandboxed home and state directories, verifying that all managed sidecars, shims, and journals are completely removed, restoring the baseline cleanly.
   - Test 6 (Zero Host Leaks): Record directory listings of the real host user directories before and after the test run; assert that zero files or directories were created or modified outside the sandbox.
  </action>
  <acceptance_criteria>
    - `test/tarball-fixture.test.ts` executes the complete lifecycle (install -> current -> status -> doctor -> uninstall) in an isolated sandbox (REL-01, D-04).
    - Host shared roots and actual home directory have zero mutations (REL-01, D-03).
    - Resolves executable binary correctly across Windows and POSIX platforms (REL-01).
    - `node scripts/run-tests.mjs --files dist/test/tarball-fixture.test.js` passes completely.
  </acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node scripts/run-tests.mjs --files dist/test/tarball-fixture.test.js</automated>
    <fails_when>non-zero exit code, or lifecycle step failure</fails_when>
  </verify>
  <done>Cross-platform tarball fixture lifecycle test suite verified and passing across isolated sandboxes.</done>
</task>

</tasks>

## Artifacts this phase produces

### Scripts
- `scripts/audit-tarball.mjs`: Pure Node.js tarball content inspector and fail-closed allowlist auditor

### Configuration
- `.npmignore`: Package archive exclusion filter
- `package.json`: Refined files allowlist and audit-tarball script hook
- `.github/workflows/ci.yml`: Multi-OS authoritative pack and matrix test workflow

### Tests
- `test/tarball-fixture.test.ts`: Cross-platform sandboxed tarball lifecycle integration suite
