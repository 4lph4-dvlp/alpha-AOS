---
phase: quick
plan: 260914-x2c
type: execute
autonomous: true
---

# Rebaseline v0.1.0 around active non-Claude harnesses

<objective>
Make the milestone and Phase 3 authority agree that Codex, Antigravity, Pi, and Hermes are the active v0.1.0 development targets, while Claude Code is compatibility residue outside active support claims and release gates. Replace the four Claude-bound Phase 3 UAT blockers with non-Claude acceptance paths and diagnosed implementation/evidence gaps that `$gsd-plan-phase 3 --gaps` can consume.

Purpose: The lack of a Claude account must no longer block Phase 3 or silently waive CAPA behavior. The authoritative documents must distinguish shipped compatibility code from active v0.1.0 claims and route missing non-Claude proof back into implementation planning.

Output: Aligned `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `03-CONTEXT.md`, `03-UAT.md`, and `03-VERIFICATION.md`. No product source, catalog, schema, adapter, or user-managed harness file is changed by this quick task.
</objective>

<constraints>
- Preserve existing Claude adapters, schemas, tests, lock data, and managed-file behavior as compatibility residue. Do not plan or perform uninstall, deletion, or mutation of user-managed Claude files.
- Remove Claude from active v0.1.0 development commitments, support claims, phase pass bars, and release gates. A retained Claude proof may document compatibility history, but it is neither required nor sufficient for an active milestone gate.
- Preserve requirement IDs CAPA-01 through CAPA-08 and the existing phase boundaries. Do not move Phase 4, 5, 6, or 7 implementation into this task.
- Do not represent the non-Claude canary, handoff, pack exercise, or stable-ordering work as complete. Record missing source/test work as diagnosed Phase 3 gaps for a later `$gsd-plan-phase 3 --gaps` run.
- Keep prior Phase 3 decisions and evidence auditable. Supersede the Claude-bound D-09 explicitly; do not rewrite history so that it appears never to have existed.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Establish one milestone-wide active harness scope</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Define the exact active v0.1.0 harness set as Codex, Antigravity, Pi, and Hermes in all three authority files. State separately that Claude Code integration remains shipped compatibility residue but is excluded from active development obligations, advertised support, acceptance/pass bars, release matrices, and release gates. Preserve factual historical statements about already implemented five-target adapters only when they are labeled as implementation history rather than a current support claim.

In PROJECT.md, update What This Is, Active requirements, Out of Scope, Context, the harness-scope constraint, and affected key decisions so “supported harness” and “complete managed stack” claims resolve through the active non-Claude set. Reframe the repository-owned workflow item so the current Claude-only surface is a compatibility fact and active work concerns non-Claude native surfaces.

In REQUIREMENTS.md, add the scope distinction to the user-facing contract and Definition of Done without renaming IDs. Keep CAPA-02, CAPA-03, and CAPA-05 open and mark their Phase 3 traceability as `Gaps Found`. Align the lifecycle state with the verifier evidence by marking CAPA-01, CAPA-04, CAPA-06, CAPA-07, and CAPA-08 complete in both their requirement checkboxes and traceability rows. Retain CAPA-06's recorded inference and its qualification rather than upgrading it to an unqualified live invocation claim. Ensure REL-02 through REL-06 and the support-matrix language cannot pull Claude back into the active release bar.

In ROADMAP.md, apply the same active-scope definition to the overview and relevant Phase 3–7 claims. Phase 3 must say 18/18 existing plans executed and that new gaps are pending planning, not that an already executed gap plan is pending. Rewrite Phase 3 success criteria so Codex is the active single-surface canary anchor where a single harness suffices, CAPA-03 requires a genuine two-harness non-Claude handoff, and every other active surface remains `unverified` or `unsupported` until its own evidence promotes it. Claude absence must not be a blocker or a release limitation for v0.1.0.
  </action>
  <verify>
    <automated>node -e "const f=require('node:fs');const ps=['.planning/PROJECT.md','.planning/REQUIREMENTS.md','.planning/ROADMAP.md'];for(const p of ps){const s=f.readFileSync(p,'utf8');for(const n of ['Codex','Antigravity','Pi','Hermes','Claude'])if(!s.includes(n))throw Error(p+' missing '+n);if(!/Claude[\s\S]{0,240}compatib|compatib[\s\S]{0,240}Claude/i.test(s))throw Error(p+' lacks Claude compatibility boundary');}const r=f.readFileSync('.planning/REQUIREMENTS.md','utf8');for(const id of ['CAPA-02','CAPA-03','CAPA-05'])if(!new RegExp('\\| '+id+' \\| Phase 3 \\| Gaps Found \\|').test(r))throw Error(id+' is not Gaps Found');for(const id of ['CAPA-01','CAPA-04','CAPA-06','CAPA-07','CAPA-08'])if(!new RegExp('\\| '+id+' \\| Phase 3 \\| Complete \\|').test(r))throw Error(id+' is not Complete');"</automated>
  </verify>
  <done>The three milestone authority files name the same four active targets, exclude Claude from active support/release gates without deleting compatibility commitments, and give every CAPA requirement a lifecycle state consistent with the current deterministic evidence and the new gaps.</done>
</task>

<task type="auto">
  <name>Task 2: Supersede the Claude-bound Phase 3 pass bar without erasing it</name>
  <files>.planning/phases/03-transactional-project-packs-and-native-optional-use/03-CONTEXT.md</files>
  <action>
Add a dated scope-rebaseline decision D-17 and mark D-09 explicitly as superseded by D-17. D-17 must state that Claude is outside active v0.1.0 development, support, verification, and release-gate scope; Claude implementation and prior evidence remain compatibility history only.

Replace the operative pass-bar rules with non-Claude outcomes: Codex is the required active anchor for CAPA-01, CAPA-02, and CAPA-05 because the repository already has a retained Codex CAPA-01 proof and a bounded Codex canary isolation path; CAPA-03 must use two distinct active non-Claude harnesses and may prefer Hermes→Codex only when both entrypoints and the Codex receiving runtime are proven; Antigravity, Pi, and Hermes promote only from their own deterministic/native evidence and otherwise remain visibly `unverified` or `unsupported`. Define stable equal-proof ordering as an offline deterministic fixture obligation rather than a requirement for two paid live surfaces.

Update the phase boundary, canonical-reference annotations, specifics, and open verification notes that still treat `policy.canaryHarness: claude`, Claude-only canary declarations, Claude-only receiving pairs, or a Claude account as acceptance authority. Preserve D-01 through D-08 and D-10 through D-16 unless D-17 directly narrows their harness scope; preserve deferred ideas and the control-plane/no-proxy, secrets, transactional, and no-auto-deletion decisions.
  </action>
  <verify>
    <automated>node -e "const s=require('node:fs').readFileSync('.planning/phases/03-transactional-project-packs-and-native-optional-use/03-CONTEXT.md','utf8');for(const x of ['D-17','D-09 is superseded by D-17','Codex','Antigravity','Pi','Hermes','compatibility'])if(!s.includes(x))throw Error('03-CONTEXT missing '+x);if(!/CAPA-03[\s\S]{0,500}(two distinct|two-harness)[\s\S]{0,300}non-Claude/i.test(s))throw Error('non-Claude CAPA-03 bar missing');if(!/stable[\s\S]{0,250}(offline|deterministic fixture)/i.test(s))throw Error('deterministic ordering bar missing');"</automated>
  </verify>
  <done>03-CONTEXT retains the original D-09 for audit, records D-17 as the user's superseding scope decision, and provides executable non-Claude acceptance rules without treating unsupported surfaces as supported.</done>
</task>

<task type="auto">
  <name>Task 3: Convert obsolete Claude blockers into four diagnosed Phase 3 gaps</name>
  <files>.planning/phases/03-transactional-project-packs-and-native-optional-use/03-UAT.md, .planning/phases/03-transactional-project-packs-and-native-optional-use/03-VERIFICATION.md</files>
  <action>
Rebaseline 03-UAT.md as the latest human authority. Change tests 1–4 from third-party blocked results to source/evidence issues under D-17, keep the interrupted-sink and fourteen-prohibition results as passes, set the session to `status: diagnosed`, clear Current Test to a completed state, and make the summary exactly 6 total / 2 passed / 4 issues / 0 pending / 0 skipped / 0 blocked. Replace the empty Gaps section with four `status: failed` entries, each carrying `gap_id`, truth, reason, severity, test, root_cause, artifacts, missing, and a diagnosis note suitable for `$gsd-plan-phase 3 --gaps`:

1. `G-03-1` — CAPA-02 must run its Exa→bounded-Firecrawl positive and ordinary-lookup negative on an active non-Claude surface. Record that `catalog/canaries.yaml` declares both CAPA-02 canaries only for Claude, `catalog/stack.yaml` still names `policy.canaryHarness: claude`, and `CANARY_INSTRUCTION_ROOT.codex` is null even though Codex has a bounded observation-front launch path. The later gap plan must supply the active Codex declaration/runtime instruction path and deterministic ordered/fan-out assertions without deleting Claude adapter compatibility.
2. `G-03-2` — CAPA-03 must complete a real handoff between two distinct active non-Claude harnesses with the receiving model using the sentinel and the planning tree unchanged. Record that the shipped declaration is Claude-only, both default `HANDOFF_HARNESS_PAIRS` target Claude, the CLI fallback resolves an absent target as Claude, and Codex cannot currently receive the owned handoff instruction because its canary instruction root is null. The later gap plan must establish an evidence-backed non-Claude pair and receiving runtime; Hermes→Codex is a preference only if live availability and isolation evidence support it.
3. `G-03-3` — CAPA-05 must exercise the materialized exact-hash project pack on an active non-Claude harness with native selection, project-local provenance, and the paired outside negative. Record that `PACK_EXERCISE_FRONTEND_A11Y` is Claude-only and there is no retained active non-Claude native-selection proof. The later gap plan must add a supported declaration/run and tests that preserve locked skill bytes and the existing CAPA-06 absence boundary.
4. `G-03-4` — equal-strength proof rendering must be deterministic without a second live vendor surface. Record that the prior UAT expected a Claude proof only because no held-out automated test existed. The later gap plan must add a fixture using at least two active non-Claude harness IDs, feed equivalent proofs in different input orders, and assert repeated byte-identical harness-id-ascending output; change production sorting only if the red test exposes a defect.

Update 03-VERIFICATION.md frontmatter to `status: gaps_found` and describe this as a scope-authority rebaseline after the earlier `human_needed` report, not a regression in the already verified transactional and CAPA-01 evidence. Replace Claude-bound human-needed wording in observable truths, requirement coverage, human verification, and the gaps summary with the four gap IDs and active non-Claude acceptance. Preserve the earlier retained Claude/Codex evidence as historical observations, but label only the Codex CAPA-01 proof active. Treat CAPA-04, CAPA-06, CAPA-07, CAPA-08, the interrupted-sink backstop, and all fourteen prohibition decisions as resolved unless new implementation changes later invalidate their evidence.
  </action>
  <verify>
    <automated>node -e "const f=require('node:fs');const u=f.readFileSync('.planning/phases/03-transactional-project-packs-and-native-optional-use/03-UAT.md','utf8');const v=f.readFileSync('.planning/phases/03-transactional-project-packs-and-native-optional-use/03-VERIFICATION.md','utf8');if(!/^status: diagnosed$/m.test(u))throw Error('UAT is not diagnosed');if(/result: blocked/.test(u))throw Error('obsolete blocked result remains');for(const x of ['total: 6','passed: 2','issues: 4','pending: 0','skipped: 0','blocked: 0'])if(!u.includes(x))throw Error('UAT summary missing '+x);const ids=[...u.matchAll(/^- gap_id: (G-03-[1-4])$/gm)].map(m=>m[1]);if(ids.join(',')!=='G-03-1,G-03-2,G-03-3,G-03-4')throw Error('expected four ordered Phase 3 gaps');if(!/^status: gaps_found$/m.test(v))throw Error('verification is not gaps_found');for(const id of ids)if(!v.includes(id))throw Error('verification missing '+id);"</automated>
  </verify>
  <done>UAT contains no Claude-account blocker, four fully diagnosed gap records identify the exact missing non-Claude source/test work, and VERIFICATION routes Phase 3 to gap planning while preserving already earned evidence.</done>
</task>

</tasks>

<verification>
Run `git diff --check -- .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/phases/03-transactional-project-packs-and-native-optional-use/03-CONTEXT.md .planning/phases/03-transactional-project-packs-and-native-optional-use/03-UAT.md .planning/phases/03-transactional-project-packs-and-native-optional-use/03-VERIFICATION.md`, then run all three task-level automated checks. Inspect `git diff --name-only` and confirm this quick task changed only those six authority files plus its own quick PLAN/SUMMARY and normal GSD state bookkeeping. Do not run product source tests because this plan changes no product source; gap plans will own their targeted and full-suite gates.
</verification>

<success_criteria>
- PROJECT, REQUIREMENTS, and ROADMAP name one active non-Claude v0.1.0 scope and explicitly separate it from Claude compatibility residue.
- D-17 supersedes D-09 without erasing either the original decision or prior evidence.
- CAPA-02, CAPA-03, and CAPA-05 are Phase 3 gaps; CAPA-01, CAPA-04, CAPA-06, CAPA-07, and CAPA-08 retain the statuses supported by current evidence.
- The four former Claude-account blockers are diagnosed implementation/evidence gaps with exact artifacts and missing behavior, ready for `$gsd-plan-phase 3 --gaps`.
- No source implementation, catalog entry, schema, adapter, test, lock, or user-managed harness file is changed or claimed complete by this documentation rebaseline.
</success_criteria>

## Source coverage audit

| Source | Item | Task | Status |
|---|---|---:|---|
| GOAL | Exclude Claude from active v0.1.0 development, support, verification, and release gates while retaining compatibility residue | 1–3 | COVERED |
| PROJECT | Supported-harness, active-work, constraints, validation, and release-claim authority | 1 | COVERED |
| REQ | CAPA-01…08 lifecycle/traceability and REL active-surface release wording | 1, 3 | COVERED |
| ROADMAP | Phase 3 pass bar and Phase 4–7 active/release scope | 1 | COVERED |
| CONTEXT | D-09 Claude pass bar superseded audibly; D-01…08 and D-10…16 preserved or narrowly scoped | 2 | COVERED |
| UAT / VERIFICATION | Four Claude-bound blockers redesigned as non-Claude acceptance plus deterministic evidence gaps | 3 | COVERED |
| RESEARCH | Quick mode explicitly excludes a research phase; no new library or external integration is selected here | — | EXCLUDED |

<output>
Create `.planning/quick/260914-x2c-exclude-claude-from-the-v0-1-0-active-de/260914-x2c-SUMMARY.md` when execution completes. The next implementation-planning command is `$gsd-plan-phase 3 --gaps`; this quick task must not execute those gaps.
</output>
