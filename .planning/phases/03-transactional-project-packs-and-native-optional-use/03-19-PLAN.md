---
phase: 03-transactional-project-packs-and-native-optional-use
plan: 19
type: execute
gap_closure: true
wave: 1
depends_on: [03-18]
files_modified:
  - src/core/canary.ts
  - test/canary.test.ts
  - test/capability-ledger.test.ts
autonomous: true
requirements: [CAPA-01, CAPA-07]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "When two active non-Claude harness surfaces (e.g. codex and pi, or codex and hermes) produce equally strong documentation proofs, capability reporting and ledger views render them in a stable order keyed by harness-id ascending."
    - "Repeated rendering over the same evidence produces byte-identical output across consecutive runs."
    - "Supplying equivalent proofs in permuted, reversed, or shuffled input orders yields identical harness-id ascending results (invariant sorting)."
    - "Equal-strength proof ordering is proven deterministically through an offline automated fixture without live vendor API calls, paid model spend, or Claude Code dependencies."
  artifacts:
    - path: src/core/canary.ts
      provides: "Deterministic harness-id ascending sorting for capability report rows and proof collections"
    - path: test/canary.test.ts
      provides: "Offline fixture testing multi-harness equal-strength proof rendering in varied input orders"
    - path: test/capability-ledger.test.ts
      provides: "Multi-harness proof persistence and stable retrieval ordering coverage"
  key_links:
    - from: src/core/canary.ts
      to: src/format.ts
      via: "formatCapabilityReport receives rows ordered deterministically by harness ID"
      pattern: "formatCapabilityReport|canaryRow|discoveryRow"
    - from: test/canary.test.ts
      to: src/core/canary.ts
      via: "offline fixture supplies permuted active non-Claude proofs and asserts byte-identical output"
      pattern: "formatCapabilityReport|resolveCapabilityState"
  prohibitions:
    - statement: "Offline proof ordering verification must not invoke live vendor APIs, spawn network requests, or spend model turns."
      status: unverified
      flagged: true
    - statement: "Proof rendering order must not depend on array insertion order or filesystem directory iteration order."
      status: unverified
      flagged: true
    - statement: "Claude Code compatibility residue must not be removed or broken when verifying active non-Claude surfaces."
      status: unverified
      flagged: true
---

<objective>
Close gap G-03-4 by implementing a deterministic offline fixture that proves stable, byte-identical equal-strength proof rendering in harness-id ascending order across active non-Claude surfaces (e.g. Codex, Pi, Hermes) without live vendor dependencies.

Purpose: Under D-17, Claude Code is excluded from active verification and release gates. The prior 03-08 ordering backstop was marked non-inferable and left without an automated test only because it originally assumed a second live vendor surface (Claude). Equal-strength proof ordering is a structural contract of the reporting and ledger layer, and must be proven deterministically offline across active non-Claude harnesses.
Output: Invariant harness-id ascending sorting in canary/reporting aggregation, and comprehensive offline fixture tests in `test/canary.test.ts` and `test/capability-ledger.test.ts` proving ordering stability across permutations.
</objective>

<execution_context>
@C:/Users/alpha/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-CONTEXT.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-UAT.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-VERIFICATION.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-08-PLAN.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-18-SUMMARY.md
@src/core/canary.ts
@src/core/capability-ledger.ts
@src/core/project-plan.ts
@src/format.ts
@test/canary.test.ts
@test/capability-ledger.test.ts
</context>

<interfaces>
- `CapabilityReportRow` in `src/core/canary.ts`: carries capability, harness, completeness, axes, and notes.
- `formatCapabilityReport(title, rows, notes)` in `src/format.ts`: formats a human-readable table with one summary line per capability and tagged detail lines.
- `resolveCapabilityState(input, ledger, ceiling)` in `src/core/project-plan.ts`: resolves weakest support and strongest nativeUse across sorted harnesses.
- `CapabilityProof` in `src/core/capability-ledger.ts`: keyed by `(projectId, harness, capability, polarity)`.
- `byCodePoint(left, right)`: canonical lexicographical sorting helper ensuring platform-independent ascending order.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ensure deterministic harness-id ascending sorting in canary reporting and proof aggregation</name>
  <files>src/core/canary.ts, test/canary.test.ts</files>
  <read_first>
    - .planning/phases/03-transactional-project-packs-and-native-optional-use/03-UAT.md — Item 4: Stable Ordering Backstop diagnosis and D-17 rationale.
    - .planning/phases/03-transactional-project-packs-and-native-optional-use/03-VERIFICATION.md — G-03-4 non-inferable item requirement.
    - src/core/canary.ts — inspect `canaryRow`, `discoveryRow`, `runCanarySweep`, and row collection helpers.
    - src/format.ts — inspect `formatCapabilityReport` table rendering.
    - src/core/project-plan.ts — inspect `resolveCapabilityState` harness sorting.
  </read_first>
  <action>
Review `src/core/canary.ts` where report rows and sweep entries are collected or returned. Ensure that when multiple rows or proofs are processed across harnesses, they are consistently ordered by harness ID ascending using code-point comparison (`byCodePoint` or `(a, b) => a.localeCompare(b)` with code-point semantics), so rendering order is never dependent on async completion order, Map iteration order, or caller array order.

Verify that `formatCapabilityReport` renders rows deterministically, and if rows are passed in unsorted order, ensure there is an explicit sorting helper or contract in `src/core/canary.ts` (e.g., `sortCapabilityReportRows(rows)`) that sorts by capability then harness-id ascending.

Preserve existing Claude compatibility residue: do not remove Claude types or references, but ensure active non-Claude harnesses (codex, pi, hermes, antigravity) are first-class sorting targets.
  </action>
  <acceptance_criteria>
    - `src/core/canary.ts` exports or applies a deterministic ordering contract ensuring rows and proofs sort by harness ID ascending.
    - No live vendor APIs or authentication tokens are required to assemble or sort rows.
    - Shipped Claude types and historical evidence remain valid and untouched.
  </acceptance_criteria>
  <verify>
    <automated>npm run check &amp;&amp; npm run build &amp;&amp; node scripts/run-tests.mjs --files dist/test/canary.test.js</automated>
    <fails_when>The command exits nonzero, type checking fails, or any canary test fails.</fails_when>
  </verify>
  <done>Row and proof aggregation functions guarantee stable, harness-id ascending order for all active non-Claude surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the offline fixture proving invariant equal-strength proof rendering across active non-Claude surfaces</name>
  <files>test/canary.test.ts, test/capability-ledger.test.ts</files>
  <read_first>
    - .planning/phases/03-transactional-project-packs-and-native-optional-use/03-08-PLAN.md — Truth 32: "When two harness surfaces produce equally strong documentation proofs, the ledger renders them in a stable order keyed by harness id ascending, so two runs over the same evidence print identically".
    - test/canary.test.ts — existing `formatCapabilityReport` and discovery row tests around lines 1930-2050.
    - test/capability-ledger.test.ts — existing ledger fixture and round-trip tests.
  </read_first>
  <action>
Add a dedicated test suite in `test/canary.test.ts` for the 03-08 Stable Ordering Backstop under D-17:
1. Construct equivalent documentation proofs (`CAPA-01`) for at least two active non-Claude harnesses: `codex` and `pi` (and optionally `hermes`). Both proofs have equal strength (`nativeUse = "invoked"`, complete bound inputs, valid `invocationEvidence`).
2. Construct corresponding capability report rows for `codex` and `pi`.
3. In Test Case A: Pass `[codexRow, piRow]` to `formatCapabilityReport`, record `renderedA`.
4. In Test Case B: Pass `[piRow, codexRow]` (reversed input order) to `formatCapabilityReport` (after sorting or through the canonical row formatter), record `renderedB`.
5. Assert that both outputs place `codex` before `pi` (since `"codex" < "pi"` in code-point order).
6. Assert that `renderedA === renderedB` (byte-identical rendering across runs regardless of input order).
7. In `test/capability-ledger.test.ts`, add test asserting that when multiple active non-Claude proofs are stored in the capability ledger, querying or iterating proofs yields stable deterministic order across permutations.
8. Verify that zero model turns are spent, no network calls are made, and no live credentials are required.
  </action>
  <acceptance_criteria>
    - Test asserts equal-strength documentation proofs across active non-Claude harnesses (`codex`, `pi`) render in harness-id ascending order (`codex` preceding `pi`).
    - Test asserts byte-identical output across repeated runs and across reversed/shuffled input permutations.
    - Test runs entirely offline with zero model spend and zero network access.
    - All tests in `test/canary.test.ts` and `test/capability-ledger.test.ts` pass cleanly.
  </acceptance_criteria>
  <verify>
    <automated>npm run check &amp;&amp; npm run build &amp;&amp; node scripts/run-tests.mjs --files dist/test/canary.test.js dist/test/capability-ledger.test.js</automated>
    <fails_when>The command exits nonzero, any test fails, or equal-strength proofs across codex and pi do not render in byte-identical harness-id ascending order.</fails_when>
  </verify>
  <done>G-03-4 is closed with a permanent, offline deterministic fixture proving stable equal-strength proof rendering across active non-Claude surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Test runner → reporting formatter | Offline fixture inputs must faithfully reproduce production proof and row shapes. |
| Memory → disk persistence | Proof ordering must be invariant across serialize / deserialize round trips. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-143 | Tampering | `formatCapabilityReport` row ordering | low | mitigate | Enforce code-point sorting on rows to eliminate host-locale or insertion-order nondeterminism. |
| T-03-144 | Repudiation | Equal-strength proof resolution | medium | mitigate | Assert deterministic tie-breaking in `resolveCapabilityState` keyed by harness ID. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit
- 03-UAT.md Item 4: Diagnosed gap G-03-4 addressed directly by offline active non-Claude fixture.
- 03-VERIFICATION.md: Non-inferable backstop closed with automated test evidence.
- D-17: Excludes Claude from active release gates; anchors on Codex and Pi.
</source_audit>

<verification>
- Automated check: `npm run check && npm run build && node scripts/run-tests.mjs --files dist/test/canary.test.js dist/test/capability-ledger.test.js`
- Passing criteria: 0 errors, 0 failures, deterministic byte-identical rendering for permuted equal-strength proofs.
</verification>

<success_criteria>
- Equal-strength proofs across active non-Claude harnesses render in harness-id ascending order deterministically.
- Byte-identical output is verified across input permutations.
- No live vendor dependencies or Claude tokens are required.
</success_criteria>

<output>
Create `.planning/phases/03-transactional-project-packs-and-native-optional-use/03-19-SUMMARY.md` when execution completes.
</output>
