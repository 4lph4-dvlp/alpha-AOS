---
phase: 13-dependency-candidate-promotion
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - catalog/stack.lock.json
  - catalog/candidate.lock.json
  - src/core/mcp-proxy.ts
  - catalog/canaries.yaml
  - AGENTS.md
  - test/catalog.test.ts
  - test/mcp-proxy.test.ts
  - .planning/phases/13-dependency-candidate-promotion/PROMOTION-EVIDENCE.md
  - docs/how-to/promote-dependencies.md
autonomous: true
requirements: [DEP-01, DEP-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`catalog/stack.lock.json` contains the verified candidate versions: @opengsd/gsd-core 1.14.0, ecc-universal 2.2.1, @upstash/context7-mcp 4.1.1, firecrawl-mcp 3.25.2, and pi-mcp-adapter 2.36.0, with exact Subresource Integrity hashes verified against the registry (DEP-02, D-01, D-04)."
    - "`catalog/candidate.lock.json` on `main` is reset to `{ schemaVersion: 1, channel: "candidate", generatedAt: null, status: "empty", components: {} }` (DEP-02, D-03)."
    - "`scripts/pin-pack-skills.mjs` re-derives `sourceSha256` for all 22 skills in ECC 2.2.1 and verifies that `deep-research` target rendered hash remains `fdd97098a3a4a02513baf0428faacbe390b42fd4f86362387938e3455c80d90c` across all 5 harnesses (DEP-02, D-06, D-08)."
    - "Runtime constants and unit test assertions referencing `ecc-universal@2.2.0` in `src/core/mcp-proxy.ts`, `catalog/canaries.yaml`, `AGENTS.md`, `test/catalog.test.ts`, and `test/mcp-proxy.test.ts` are synchronized to `2.2.1`, while historical release docs preserve their 2.2.0 record (DEP-01, DEP-02, D-07, D-09)."
    - "`src/core/gsd-compat.ts` adapter and renderer hashes are verified against GSD Core 1.14.0, confirming Codex hook helpers are byte-identical and require no renderer changes (DEP-01, DEP-02, D-15)."
    - "`catalog/stack.lock.json` maintains the standard profile spine invariant with `\"profile\": \"standard\"` for GSD Core (DEP-02, D-16)."
    - "`.planning/phases/13-dependency-candidate-promotion/PROMOTION-EVIDENCE.md` documents complete 3-tier promotion receipts, 4-harness GSD metrics, and ECC diff evidence (DEP-01, DEP-02, D-02, D-17)."
    - "`docs/how-to/promote-dependencies.md` establishes the official standard operating procedure for future component promotions (DEP-01, DEP-02, D-18, D-19)."
    - "Strategy B-1 release hygiene is validated via `npm pack` and `scripts/audit-tarball.mjs`, proving `.planning/` and `candidate.lock.json` are strictly excluded from distribution tarballs (DEP-02, D-05, D-21)."
  prohibitions:
    - statement: "MUST NOT include unverified candidate dependencies or candidate lock records in `catalog/stack.lock.json`; all promoted components must have passing 3-tier verification."
      category: safety
      status: resolved
      verification: automated
    - statement: "MUST NOT leave active candidate components in `catalog/candidate.lock.json` on `main`; candidate channel must be reset to empty status upon promotion."
      category: correctness
      status: resolved
      verification: automated
    - statement: "MUST NOT alter historical documentation (`docs/alpha-vibe-stack-*`) when synchronizing active code constants to 2.2.1."
      category: correctness
      status: resolved
      verification: automated
    - statement: "MUST NOT package `.planning/`, `candidate.lock.json`, or test files into release distribution archives."
      category: safety
      status: resolved
      verification: automated
  artifacts:
    - path: "catalog/stack.lock.json"
      provides: "Authoritative stable dependency lock containing verified 1.14.0/2.2.1/4.1.1/3.25.2/2.36.0 package versions"
    - path: "catalog/candidate.lock.json"
      provides: "Empty candidate lock channel ready for future automated discovery runs"
    - path: "src/core/mcp-proxy.ts"
      provides: "Updated routing policy finding constant referencing ecc-universal@2.2.1"
      exports: ["ROUTING_CONTRACT_MISMATCH", "createMcpPolicyProxy"]
    - path: ".planning/phases/13-dependency-candidate-promotion/PROMOTION-EVIDENCE.md"
      provides: "Comprehensive formal verification ledger and audit trail for Phase 13"
    - path: "docs/how-to/promote-dependencies.md"
      provides: "Official maintainer developer guide detailing the end-to-end dependency promotion lifecycle"
  key_links:
    - from: "catalog/stack.lock.json"
      to: "schemas/lock.schema.json"
      via: "validated by loadLock during catalog loading and testing"
      pattern: "loadLock\\(root, \"stable\"\\)"
    - from: "scripts/pin-pack-skills.mjs"
      to: "catalog/stack.lock.json"
      via: "writes updated sourceSha256 hash map atomically into locked ECC component"
      pattern: "ecc\\.sourceSha256 = mergeSorted"
    - from: "test/catalog.test.ts"
      to: "catalog/stack.lock.json"
      via: "asserts exact promoted versions 1.14.0, 2.2.1, 2.36.0"
      pattern: "assert\\.equal\\(lock\\.components\\.ecc\\?\\.version, \"2\\.2\\.1\"\\)"
---

<objective>
Execute fine-grained partial promotion of verified candidate components into `catalog/stack.lock.json`, atomically re-pin ECC 2.2.1 pack skill hashes, synchronize runtime and test literals, document the verification evidence, author the official promotion guide, and verify Strategy B-1 release hygiene.

Specifically:
1. Run `scripts/promote-candidate.mjs` to promote the 5 verified components (@opengsd/gsd-core 1.14.0, ecc-universal 2.2.1, context7-mcp 4.1.1, firecrawl-mcp 3.25.2, pi-mcp-adapter 2.36.0) into `catalog/stack.lock.json` and reset `catalog/candidate.lock.json` to empty.
2. Run `scripts/pin-pack-skills.mjs write` to re-derive all 19 pack skill source hashes and verify `deep-research` target hash invariance across all 5 harnesses.
3. Synchronize runtime constants in `src/core/mcp-proxy.ts`, `catalog/canaries.yaml`, `AGENTS.md`, `test/catalog.test.ts`, and `test/mcp-proxy.test.ts` to `2.2.1`, while preserving historical docs per D-07.
4. Author the comprehensive promotion ledger `.planning/phases/13-dependency-candidate-promotion/PROMOTION-EVIDENCE.md` and developer guide `docs/how-to/promote-dependencies.md`.
5. Run `npm pack` and `scripts/audit-tarball.mjs` to prove Strategy B-1 release hygiene (0 violations, `.planning/` strictly excluded) and verify `npm test` passes with 0 failures.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-dependency-candidate-promotion/13-CONTEXT.md
@.planning/phases/13-dependency-candidate-promotion/13-RESEARCH.md
@.planning/phases/13-dependency-candidate-promotion/13-PATTERNS.md
@.planning/phases/13-dependency-candidate-promotion/13-VALIDATION.md
@catalog/stack.lock.json
@catalog/candidate.lock.json
@scripts/promote-candidate.mjs
@scripts/pin-pack-skills.mjs
@scripts/audit-tarball.mjs
@src/core/mcp-proxy.ts
@test/catalog.test.ts
@test/mcp-proxy.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute candidate promotion and atomically pin ECC 2.2.1 pack skill hashes</name>
  <files>catalog/stack.lock.json, catalog/candidate.lock.json</files>
  <read_first>
    catalog/stack.lock.json
    catalog/candidate.lock.json
    scripts/promote-candidate.mjs
    scripts/pin-pack-skills.mjs
    .planning/phases/13-dependency-candidate-promotion/13-RESEARCH.md (Section 1, 3, Pitfall 3 & 5)
    .planning/phases/13-dependency-candidate-promotion/13-PATTERNS.md (Pattern 1 & 3)
  </read_first>
  <action>
    1. Execute maintainer promotion CLI to update stable lock and reset candidate lock:
       `node scripts/promote-candidate.mjs --verify-integrity --reset-candidate`
    2. Verify `catalog/stack.lock.json` contains:
       - `components.gsd.version`: `"1.14.0"` (integrity: `sha512-e05sV2c8KlcQ2hoJ4U3U1OBjqswQOon4C29Cs75fEAr+tq7qJ/XyFYrN/nwblREWUkPUUoxmVzLVj4Im2KjQxQ==`, profile: `"standard"`)
       - `components.ecc.version`: `"2.2.1"` (integrity: `sha512-D8vFXQ++Aub1CD2RPyYfPEXVt51eEHzvImLd1XaOos/gUxYRWU+BRDXMDnmRrjDfWwX5+uFEJULYpqPYFPSGuw==`)
       - `components.mcp.context7.version`: `"4.1.1"` (integrity: `sha512-fUARTIZGVKzlzQKDhKUg4aIQ3yEU/12STdfxJDDnLMSN7yyHiPvNHI05iFwxkv3vC8G76Wp27MwK6Jucl/C5pA==`)
       - `components.mcp.firecrawl.version`: `"3.25.2"` (integrity: `sha512-Z0PWA9UD757p8w1ahAh6Fyqm28uDc8ng7OkTE3NE0x1F85vAeU4/MMmx1l+xtmDm8OSpybriMjNJZavca4ijfA==`)
       - `components.mcpBridges.pi.version`: `"2.36.0"` (integrity: `sha512-aX7Hf1FMGoHFjjx2Y4t12Hkue83CiBQmZaeQQ0mg+2wxOzbIOYdWzEkh1bmKlLHKiH6zRWlxWitsr+RaiXvoRQ==`)
       - `components.mcp.exa`: unchanged at `"3.4.1"`.
    3. Verify `catalog/candidate.lock.json` is reset to:
       ```json
       {
         "schemaVersion": 1,
         "channel": "candidate",
         "generatedAt": null,
         "status": "empty",
         "components": {}
       }
       ```
    4. Run `node scripts/pin-pack-skills.mjs write --all` (or `--update-all`) to re-derive all 19 pack skills and global skills for ECC 2.2.1:
       - Confirms 21 skills retain their exact byte content and hashes.
       - Confirms `deep-research` source hash is updated to `72e184f5d0f31ca4d45286753409d8c8431b025e8ac3800dcc9f32d9e930f58a`.
       - Confirms target rendered hash for `deep-research` remains `fdd97098a3a4a02513baf0428faacbe390b42fd4f86362387938e3455c80d90c` across all 5 harnesses.
    5. Run `node scripts/pin-pack-skills.mjs check` to verify lock consistency.
  </action>
  <verify>
    <automated>node scripts/pin-pack-skills.mjs check</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `catalog/stack.lock.json` contains promoted candidate versions and passes schema validation.
    - `catalog/candidate.lock.json` is reset to empty.
    - `scripts/pin-pack-skills.mjs check` exits with 0.
  </acceptance_criteria>
  <done>Stable lock is promoted, candidate lock is reset, and pack skills are pinned.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize code constants and update test assertions</name>
  <files>src/core/mcp-proxy.ts, catalog/canaries.yaml, AGENTS.md, test/catalog.test.ts, test/mcp-proxy.test.ts</files>
  <read_first>
    src/core/mcp-proxy.ts (lines 160-175)
    catalog/canaries.yaml (lines 15-22)
    AGENTS.md (lines 55-65)
    test/catalog.test.ts (lines 60-120)
    test/mcp-proxy.test.ts (lines 1450-1465)
    .planning/phases/13-dependency-candidate-promotion/13-RESEARCH.md (Pitfall 4)
    .planning/phases/13-dependency-candidate-promotion/13-PATTERNS.md (Section 2.5, 2.6)
  </read_first>
  <action>
    1. In `src/core/mcp-proxy.ts`:
       - Update `ROUTING_CONTRACT_MISMATCH.runtime` from `"ecc-universal@2.2.0"` to `"ecc-universal@2.2.1"` (preserving the `narrow` policy reconciliation per D-07, D-09).
    2. In `catalog/canaries.yaml`:
       - Update comment at line 15 to reference `ecc-universal@2.2.1`.
    3. In `AGENTS.md`:
       - Update line 61 reference from `ecc-universal 2.2.0` to `ecc-universal 2.2.1`.
    4. In `test/catalog.test.ts`:
       - Update line 67: `assert.equal(lock.components.gsd?.version, "1.14.0");`
       - Update line 68: `assert.equal(lock.components.ecc?.version, "2.2.1");`
       - Update line 73: `assert.equal(lock.components.ecc?.sourceSha256["deep-research"], "72e184f5d0f31ca4d45286753409d8c8431b025e8ac3800dcc9f32d9e930f58a");`
       - Update line 76: `assert.equal(lock.components.mcpBridges?.pi?.version, "2.36.0");`
       - Update line 108: `assert.match(plan.find((action) => action.id === "ecc:runtime")?.command ?? "", /ecc-universal@2\.2\.1/u);`
       - Update line 111: `assert.equal(plan.find((action) => action.id === "mcp-bridge:pi")?.command, "pi install npm:pi-mcp-adapter@2.36.0");`
    5. In `test/mcp-proxy.test.ts`:
       - Update line 1457: `assert.equal(ROUTING_CONTRACT_MISMATCH.runtime, "ecc-universal@2.2.1", "the mismatch finding names the locked runtime version");`
    6. Confirm historical documentation files (`docs/alpha-vibe-stack-*`) are untouched per D-07.
    7. Rebuild and test:
       `npm run check && npm run build && node --test dist/test/catalog.test.js dist/test/mcp-proxy.test.js`
  </action>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/catalog.test.js dist/test/mcp-proxy.test.js</automated>
    <fails_when>non-zero exit, a `not ok` line, or `# fail` above 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/mcp-proxy.ts` specifies `ecc-universal@2.2.1`.
    - `test/catalog.test.ts` and `test/mcp-proxy.test.ts` pass cleanly with promoted version assertions.
    - Historical docs remain unchanged.
  </acceptance_criteria>
  <done>Code constants and test assertions are fully synchronized.</done>
</task>

<task type="auto">
  <name>Task 3: Author documentation, complete evidence ledger, and verify Strategy B-1 release hygiene</name>
  <files>.planning/phases/13-dependency-candidate-promotion/PROMOTION-EVIDENCE.md, docs/how-to/promote-dependencies.md</files>
  <read_first>
    .planning/phases/13-dependency-candidate-promotion/13-CONTEXT.md (D-02, D-05, D-18, D-19, D-21)
    .planning/phases/13-dependency-candidate-promotion/13-RESEARCH.md (Section 2, 3, 4, 6)
    .planning/phases/13-dependency-candidate-promotion/13-PATTERNS.md (Section 2.7, 2.8, Pattern 4)
    scripts/audit-tarball.mjs (allowlist and forbidden patterns)
  </read_first>
  <action>
    1. Complete `.planning/phases/13-dependency-candidate-promotion/PROMOTION-EVIDENCE.md`:
       - Append Section 4: GSD Core 1.14.0 4-Harness Fixture Receipt (metrics, sentinels, and Codex stop-hook smoke pass).
       - Append Section 5: ECC 2.2.1 Skills Diff & Hash Pinning Receipt (hash table of all 19 pack skills + 3 global skills, diff analysis of `deep-research`, and target hash invariance).
       - Append Section 6: MCP Servers & Pi Bridge Fixture Receipts (tools/list outputs for context7 4.1.1 and firecrawl 3.25.2, Pi bridge install output for 2.36.0).
       - Append Section 7: Final Promotion Verdict & Candidate Channel Reset Confirmation.
    2. Author `docs/how-to/promote-dependencies.md`:
       - Overview & Principles: Staged discovery, independent 3-tier gates, immutable supply chain locks.
       - Architecture: Candidate channel (`candidate.lock.json`) vs. Stable authority (`stack.lock.json`).
       - Red-Main Guard Gating: Using `check-main-baseline.mjs` to prevent promotion onto red commits.
       - The 3-Tier Promotion Gate Sequence (Registry SRI, Isolated Fixtures, 3-OS CI Matrix).
       - Category-Specific Gates (MCP stdio, Skill packs, Harness bridges, Workflows per D-18).
       - Step-by-Step Operator Runbook: PR rebase, local pre-flight, `promote-candidate.mjs`, `pin-pack-skills.mjs`, code synchronization.
       - Strategy B-1 Release Sanitation: Positive allowlist tarball auditing (`scripts/audit-tarball.mjs`) and clean clone repository hygiene.
    3. Verify Strategy B-1 Release Sanitation:
       - Run `npm pack --ignore-scripts` to build production tarball.
       - Run `node scripts/audit-tarball.mjs <pack-name>` to verify 0 violations: proves `.planning/` and `candidate.lock.json` are strictly excluded from distribution tarballs.
       - Clean up generated tarball.
    4. Execute full test suite:
       `npm test`
       Confirm all suites pass with 0 failures.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, a `not ok` line, or `# fail` above 0</fails_when>
    <automated>node --check scripts/audit-tarball.mjs</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `PROMOTION-EVIDENCE.md` is complete with detailed receipts across all components.
    - `docs/how-to/promote-dependencies.md` is authored and covers the complete promotion lifecycle.
    - Tarball packaging audit passes with 0 violations and verifies Strategy B-1 release hygiene.
    - Full test suite (`npm test`) passes with 0 failures.
  </acceptance_criteria>
  <done>Promotion evidence, developer documentation, and release hygiene verification complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public Release Tarball / Distribution → User | Release tarball must contain ONLY runtime code and verified stable lock; candidate staging and development planning files must be strictly excluded. |
| Pinned Catalog Lock → alpha-AOS Runtime | Runtime commands (`alpha-aos install`, `alpha-aos update --apply`) consume only `catalog/stack.lock.json`; unverified candidate data is inaccessible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-10 | Information Disclosure | npm pack / release tarball | high | mitigate | Strategy B-1 release hygiene enforced via `scripts/audit-tarball.mjs`: `.planning/`, `candidate.lock.json`, and internal scratch files are explicitly forbidden and rejected. |
| T-13-11 | Tampering | catalog/stack.lock.json | critical | mitigate | All promoted components have their exact SHA-512 SRI hashes verified against registry metadata and conform to `schemas/lock.schema.json`. |
| T-13-12 | Elevation of Privilege | catalog/candidate.lock.json | medium | mitigate | Candidate lock on `main` is reset to `status: "empty"`, preventing accidental application of unverified components. |
| T-13-13 | Repudiation | PROMOTION-EVIDENCE.md | low | mitigate | Complete audit document preserves full provenance of fixture runs, registry hashes, CI run IDs, and semantic diff findings. |
</threat_model>

<verification>
- `catalog/stack.lock.json` carries exact candidate versions and passes `loadLock(root, "stable")`.
- `catalog/candidate.lock.json` has `status: "empty"` and empty components.
- `node scripts/pin-pack-skills.mjs check` exits 0.
- `npm run check && npm run build` compiles with 0 errors.
- `npm test` passes all tests with 0 failures.
- `npm pack` + `node scripts/audit-tarball.mjs <tarball>` reports 0 violations.
- `docs/how-to/promote-dependencies.md` and `PROMOTION-EVIDENCE.md` exist and are populated.
</verification>

<success_criteria>
- Verified candidate components (@opengsd/gsd-core 1.14.0, ecc-universal 2.2.1, context7-mcp 4.1.1, firecrawl-mcp 3.25.2, pi-mcp-adapter 2.36.0) are safely promoted into `catalog/stack.lock.json`.
- Candidate lock channel is reset to empty.
- Pack skill hashes and code literals are synchronized and proven by tests.
- Promotion evidence and operating procedure documentation are complete.
- Full test suite and release sanitation audits pass.
</success_criteria>

## Artifacts this phase produces

- Modified file `catalog/stack.lock.json` with promoted candidate versions.
- Modified file `catalog/candidate.lock.json` reset to empty channel.
- Modified file `src/core/mcp-proxy.ts` updated to ecc-universal@2.2.1.
- Modified file `catalog/canaries.yaml` updated comment.
- Modified file `AGENTS.md` updated runtime reference.
- Modified file `test/catalog.test.ts` updated version and command assertions.
- Modified file `test/mcp-proxy.test.ts` updated version assertion.
- Completed file `.planning/phases/13-dependency-candidate-promotion/PROMOTION-EVIDENCE.md`.
- New file `docs/how-to/promote-dependencies.md`.

<output>
Create `.planning/phases/13-dependency-candidate-promotion/13-03-SUMMARY.md` when done.
</output>
