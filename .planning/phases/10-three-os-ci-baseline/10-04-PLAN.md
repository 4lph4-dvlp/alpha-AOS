---
phase: 10-three-os-ci-baseline
plan: 04
type: execute
wave: 3
depends_on: [10-01, 10-03]
files_modified:
  - .planning/phases/10-three-os-ci-baseline/CI_RUN.md
  - .planning/debug/ci02-windows-alpha-aos-host-leak.md
autonomous: true
requirements: [CI-03]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "One push-triggered `main` CI run (event `push`, branch `main`) whose head SHA contains every Phase 10 fix commit concludes `success`, with `ubuntu-latest / Node 24`, `macos-latest / Node 24`, `windows-latest / Node 24` and `Authoritative Release Package (Linux)` all `success` in the same attempt (CI-03, D-12, ROADMAP success criterion 5)."
    - "In that run the ubuntu and macOS legs report `destinationFor resolves destinations across all five harnesses` ok, every leg reports `no test under test/ asserts against a platform-specific absolute path literal` ok, and the windows leg reports `packed release completes the isolated install, reconcile, diagnose, and uninstall lifecycle` ok (ROADMAP success criteria 1 and 2, observed on CI)."
    - "If any re-run was needed, it was GitHub `Re-run all jobs` (`gh run rerun RUN` without `--failed`) on the same run after a recorded transient pinned-upstream failure; the accepted attempt number is recorded and every job in that attempt started after that attempt began (D-13)."
    - "`.planning/phases/10-three-os-ci-baseline/CI_RUN.md` records run id, attempt, head SHA, event, URL and per-leg and per-step conclusions exactly as returned by `gh run view`, and the CI-02 debug record is marked resolved citing it (D-14, D-07). Both records reach origin/main through a plain fast-forward docs-only push. That push's own CI run is not the proof run and is never written into CI_RUN.md, which stays pinned to the recorded run id and attempt."
  prohibitions:
    - statement: "MUST NOT accept as proof a `workflow_dispatch` run, a run where any job was re-run individually (`gh run rerun --failed` or a per-job re-run), or legs taken from different runs or different attempts (D-12, D-13)."
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT force-push, rewrite `main` history, or edit `.github/workflows/ci.yml` to obtain a green run."
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT transcribe run facts by hand; every run id, attempt, SHA, event, URL and conclusion in CI_RUN.md comes from `gh` output captured in this plan (D-14)."
      category: transparency
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/phases/10-three-os-ci-baseline/CI_RUN.md"
      provides: "CI-03 proof record: run metadata with attempt, matrix job table, Phase 10 observations, single-run integrity, contained fix commits"
      contains: "Attempt"
    - path: ".planning/debug/ci02-windows-alpha-aos-host-leak.md"
      provides: "CI-02 record closed with the proof run"
      contains: "status: resolved"
  key_links:
    - from: ".planning/phases/10-three-os-ci-baseline/CI_RUN.md"
      to: "gh run view RUN --attempt N --json databaseId,attempt,headSha,event,conclusion,url,headBranch,jobs"
      via: "captured JSON quoted in the record"
      pattern: "gh run view"
    - from: ".planning/debug/ci02-windows-alpha-aos-host-leak.md"
      to: ".planning/phases/10-three-os-ci-baseline/CI_RUN.md"
      via: "Resolution section cites the proof run id and attempt"
      pattern: "CI_RUN\\.md"
    - from: "10-VERIFICATION.md (written by the phase verifier)"
      to: ".planning/phases/10-three-os-ci-baseline/CI_RUN.md"
      via: "verification cites CI_RUN.md as the CI-03 evidence (D-14)"
      pattern: "CI_RUN\\.md"
---

<objective>
Prove CI-03: publish the Phase 10 fixes to `main` and capture one push-triggered CI run in which ubuntu-latest, macos-latest and windows-latest are green together, then record it from `gh` output.

Purpose: Phase 10's closing evidence is a single run id, as it was for G-01-1 in v0.1.0. A green ubuntu or macOS leg is also the authoritative POSIX observation of the CI-01 fix, and a green windows leg is the CI observation of the CI-02 fix under the unchanged host-immutability oracle.

Output: the pushed `main` head that the proof run built, `.planning/phases/10-three-os-ci-baseline/CI_RUN.md`, and the CI-02 debug record marked resolved. Both records are published to origin/main by a follow-up docs-only push whose run is not the proof run.
</objective>

<assumption_delta_decision>
noun: CI run id (single-run proof)
decision: no-change
rationale: The detector's trigger word "another" (in "no leg individually re-run or taken from another run") refers to run provenance, not to a second instance of an identity or model noun; no identity model generalizes, so the plan keeps exactly one run id as the proof.
</assumption_delta_decision>

## Planner Assumptions (flagged)

These resolve the unclassified CI-03 edge-probe item; they are stated rather than silently assumed, and the verifier should confirm them against CI_RUN.md.

- A-10-04-1: "A single `main` CI run id" is satisfied by one attempt of one run. When D-13's `Re-run all jobs` is used, the accepted evidence is attempt N's complete job list; jobs from different attempts are never mixed.
- A-10-04-2: The `Authoritative Release Package (Linux)` job is a precondition of the three OS legs (they download its tarball) and must also be `success` in the accepted attempt, although CI-03 names only the three OS legs.
- A-10-04-3: D-13's "three registry-touching MCP tests" is read as the pinned-upstream startup path in mcp-proxy.test.js: `the context7 proxy starts and lists exactly its two tools`, `the exa proxy starts and lists exactly its two tools`, `the firecrawl proxy starts and lists its allowlisted tools`, plus `a proxy launch writes nothing into the user's home`, which starts pinned context7 through the same `startPinnedServer`. A transient registry failure in that fourth test is treated the same way and recorded as this interpretation.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-three-os-ci-baseline/10-CONTEXT.md
@.planning/phases/10-three-os-ci-baseline/10-RESEARCH.md
@.planning/phases/10-three-os-ci-baseline/10-01-SUMMARY.md
@.planning/phases/10-three-os-ci-baseline/10-03-SUMMARY.md
@.planning/debug/ci02-windows-alpha-aos-host-leak.md
@.planning/milestones/v0.1.0-phases/07-cross-platform-release-proof/CI_RUN.md
@.github/workflows/ci.yml
@test/helpers/upstream-gate.ts
</context>

<interfaces>
- Workflow `CI` (.github/workflows/ci.yml, unchanged since c6415a2): triggers on push to main, pull_request and workflow_dispatch; job `package` named `Authoritative Release Package (Linux)`; matrix job `test` named `${{ matrix.os }} / Node 24` for ubuntu-latest, macos-latest, windows-latest with `fail-fast: false`, `ALPHA_AOS_REQUIRE_UPSTREAM_MCP: "1"`, steps including `npm test` and `Safety boundary suites`.
- Remote: `origin` = github.com/4lph4-dvlp/alpha-AOS; `origin/main` is c6415a2 at planning time and local `main` carries the phase commits on top of it (fast-forward).
- gh 2.100.0 (10-RESEARCH Finding 8): `gh run list --workflow CI --branch main --event push --json databaseId,headSha,status,conclusion,event`; `gh run watch RUN --exit-status`; `gh run view RUN [--attempt N] --json databaseId,attempt,headSha,event,conclusion,url,headBranch,jobs` (jobs carry name, conclusion, databaseId, startedAt, steps); `gh run view RUN --log-failed`; `gh run view --job JOB --log`; `gh run rerun RUN` re-runs ALL jobs, `--failed` re-runs only failed jobs (forbidden here); `gh api repos/4lph4-dvlp/alpha-AOS/actions/runs/RUN/attempts/N` returns `run_started_at`.
- Registry-unreachable vocabulary: `REGISTRY_UNREACHABLE` in test/helpers/upstream-gate.ts; a pinned startup failure message contains `did not answer a tools listing` and `upstream gate: ALPHA_AOS_REQUIRE_UPSTREAM_MCP`.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — publish the Phase 10 commits to main and drive the push-triggered run to a green single attempt</name>
  <precondition>`gh auth status` exits 0 for github.com with push access to 4lph4-dvlp/alpha-AOS, and plans 10-01 and 10-03 have their SUMMARY files in the phase directory.</precondition>
  <reversibility rating="costly">Publishing the phase commits to origin/main can only be undone by revert commits every clone sees; no force-push is permitted, so a mistake costs a follow-up commit and another CI run.</reversibility>
  <files>(no repository files; pushes local main and captures gh output into the session scratchpad)</files>
  <read_first>
    .github/workflows/ci.yml
    .planning/phases/10-three-os-ci-baseline/10-01-SUMMARY.md
    .planning/phases/10-three-os-ci-baseline/10-03-SUMMARY.md
    .planning/phases/10-three-os-ci-baseline/10-RESEARCH.md (Finding 8, CI-03 evidence capture)
    test/helpers/upstream-gate.ts (REGISTRY_UNREACHABLE)
  </read_first>
  <action>
    Implement D-12 and D-13 end to end.

    1. Preflight, all local: `git status --porcelain` shows no modified or staged tracked file (the pre-existing untracked `nul` file is left alone and never added); `git log --oneline origin/main..main` lists the 10-01 and 10-03 commits; `git diff --quiet c6415a2 -- .github/workflows/ci.yml` exits 0; `npm run check && npm run build && npm run build:check` pass; and the quick run on a scratch home passes: `node --test dist/test/path-literal-guard.test.js dist/test/owned-skills.test.js dist/test/ecc-skills.test.js dist/test/gate-engine.test.js dist/test/gate-lifecycle.test.js` plus `node --test --test-name-pattern="test-owned state root|host (fingerprint|drift)" dist/test/mcp-proxy.test.js dist/test/tarball-fixture.test.js`, each under the scratch-home prefix used in 10-03 (HOME and USERPROFILE redirected, ALPHA_AOS_STATE_DIR and harness root names unset). Then run the POSIX preflight check. Read the `POSIX observation:` line in 10-01-SUMMARY.md. When `wsl.exe -e node --version` exits 0, run from the repository root under WSL, against the dist just built, `wsl.exe --cd "$(cygpath -w "$PWD")" -e sh -c 'node --test --test-name-pattern="destinationFor resolves|global ECC roots" dist/test/owned-skills.test.js dist/test/ecc-skills.test.js && node --test dist/test/path-literal-guard.test.js'`. Run it whichever form the 10-01 line takes, because 10-02 and 10-03 landed after that observation. If WSL is still unavailable and the 10-01 line records the WSL fallback, write in this plan's SUMMARY that the proof run's ubuntu and macOS legs are the first POSIX observation of CI-01. A red `destinationFor` or guard result there follows step 6's non-D-13 branch. Do not push if any preflight step fails, including a failing WSL run.
    2. Publish with a plain fast-forward `git push origin main`. Record `git rev-parse origin/main` as the proof SHA.
    3. Find the push-triggered run: poll `gh run list --workflow CI --branch main --event push --limit 10 --json databaseId,headSha,status,event` until the entry whose headSha equals the proof SHA appears; record its databaseId as RUN. A `workflow_dispatch` run is never used (D-12).
    4. Wait for completion with `gh run watch RUN --exit-status` (run it in the background and monitor; the windows leg takes several minutes).
    5. Evaluate with `gh run view RUN --json databaseId,attempt,headSha,event,conclusion,url,headBranch,jobs`, saved to the session scratchpad. Green means event `push`, conclusion `success`, and all four jobs `success` in that attempt.
    6. If a job is red, read `gh run view RUN --log-failed`. D-13 applies only when every failing test is on the pinned-upstream startup path listed in assumption A-10-04-3 and each failure text shows `did not answer a tools listing` with registry or network vocabulary in the upstream stderr. Then save the failure excerpt, run `gh run rerun RUN` (never with `--failed`, never a single-job re-run), wait again, and evaluate `gh run view RUN --attempt N --json ...` for the new attempt number N. Allow at most two re-run-all attempts. For ANY other failure (the packed lifecycle, the path-literal guard, `destinationFor`, the regression tests, or anything else) do not re-run: capture the failing leg, step and test name (and, for the packed lifecycle, the D-05 drift report lines from the log), let Task 2 record the run as NOT CLOSED, and stop the plan with a blocker in the SUMMARY so the phase verifier routes a gap closure.
    7. For the accepted attempt, save per job: name, databaseId, conclusion, startedAt and the step conclusions for `npm test` and `Safety boundary suites`; the attempt's `run_started_at` from `gh api repos/4lph4-dvlp/alpha-AOS/actions/runs/RUN/attempts/N`; and, from each leg's `gh run view --job JOB --log`, the test summary lines (tests, pass, fail, skipped) and the result lines for the Phase 10 tests named in this plan's must_haves.
  </action>
  <verify>
    <automated>SHA=$(git rev-parse origin/main) && ID=$(gh run list --workflow CI --branch main --event push --limit 20 --json databaseId,headSha --jq ".[] | select(.headSha == \"$SHA\") | .databaseId" | head -1) && test -n "$ID" && gh run view "$ID" --json event,conclusion,jobs --jq '[.event, .conclusion, ([.jobs[] | select(.name | test("-latest / Node 24")) | .conclusion] | join(","))] | join(" ")'</automated>
    <fails_when>no run id is found for the pushed SHA (empty ID, `test -n` fails), or the printed line is anything other than `push success success,success,success` (wrong event, a non-success conclusion, a red leg, or fewer than three legs listed)</fails_when>
    <automated>for f in test/owned-skills.test.ts test/ecc-skills.test.ts test/tarball-fixture.test.ts test/mcp-proxy.test.ts test/gate-engine.test.ts test/gate-lifecycle.test.ts; do git diff --quiet c6415a2 origin/main -- "$f" && { echo "unchanged on origin/main: $f"; exit 1; }; done; git cat-file -e origin/main:test/path-literal-guard.test.ts && git cat-file -e origin/main:test/helpers/path-literal-scan.ts && git diff --quiet c6415a2 origin/main -- .github/workflows/ci.yml</automated>
    <fails_when>a line `unchanged on origin/main:` is printed (a Phase 10 fix is missing from the proof SHA), `git cat-file -e` exits non-zero (a guard file is missing), or the final diff exits 1 (ci.yml changed)</fails_when>
  </verify>
  <acceptance_criteria>
    - The proof run's event is `push`, its branch is `main`, its head SHA equals the pushed `origin/main`, and its conclusion is `success`.
    - `ubuntu-latest / Node 24`, `macos-latest / Node 24`, `windows-latest / Node 24` and `Authoritative Release Package (Linux)` are all `success` in the accepted attempt.
    - Every Phase 10 test file change and both new guard files are present at the proof SHA, and ci.yml is unchanged from c6415a2.
    - If attempt N is greater than 1, the scratchpad holds the earlier attempt's failure excerpt showing only pinned-upstream registry failures, and no `--failed` re-run was issued (`gh run view RUN --json attempt` equals the number of re-run-all invocations plus one).
    - No force-push was used (`git reflog show origin/main` shows only fast-forward updates).
    - The SUMMARY records the preflight POSIX check: the WSL pass lines, or (only when WSL is unavailable and 10-01 took the fallback) the statement that the proof run's ubuntu and macOS legs are the first POSIX observation of CI-01.
  </acceptance_criteria>
  <done>One push-triggered main run is green on all three OS legs and the package job in a single attempt, or the plan has stopped with the failing leg captured and no disallowed re-run.</done>
</task>

<task type="auto">
  <name>Task 2: Record the proof in CI_RUN.md from gh output and close the CI-02 debug record</name>
  <files>.planning/phases/10-three-os-ci-baseline/CI_RUN.md, .planning/debug/ci02-windows-alpha-aos-host-leak.md</files>
  <read_first>
    .planning/milestones/v0.1.0-phases/07-cross-platform-release-proof/CI_RUN.md (record convention)
    .planning/debug/ci02-windows-alpha-aos-host-leak.md
    the session scratchpad JSON and log excerpts captured in Task 1
  </read_first>
  <action>
    Implement D-14 and close D-07's record. Every value comes from the Task 1 `gh` captures; nothing is typed from memory or from the web UI.

    1. Write `.planning/phases/10-three-os-ci-baseline/CI_RUN.md` following the Phase 7 CI_RUN.md layout:
       - `## 1. Run Metadata` table with rows Run ID, Attempt, Workflow URL, Workflow File (`.github/workflows/ci.yml`), Trigger (the `event` value), Commit SHA (`headSha`), Branch (`headBranch`), Conclusion. Below it, the exact `gh run view RUN --attempt N --json databaseId,attempt,headSha,event,conclusion,url,headBranch,jobs` command and the captured JSON reduced to those fields plus each job's name, databaseId, conclusion and startedAt, in a fenced block.
       - `## 2. Matrix Jobs` table: job name, job id, conclusion, `npm test` step conclusion, `Safety boundary suites` step conclusion, and the test summary counts from each leg's log.
       - `## 3. Phase 10 Observations` — per leg, the log result lines for `destinationFor resolves destinations across all five harnesses`, `no test under test/ asserts against a platform-specific absolute path literal`, `packed release completes the isolated install, reconcile, diagnose, and uninstall lifecycle`, `pinned upstream startups keep their npx cache under the test-owned state root, never the host state root`, and the gate journal regression tests' parent test names.
       - `## 4. Single-Run Integrity (D-12, D-13)` — the event is push (not workflow_dispatch); the attempt number; if N is greater than 1, the earlier attempt's failure excerpt and cause, the statement that `Re-run all jobs` (`gh run rerun` without `--failed`) was used, and a table showing each job's startedAt after the attempt's `run_started_at`. Also state assumptions A-10-04-1 to A-10-04-3 and how this run satisfies them.
       - `## 5. Phase 10 Commits Contained` — the Phase 10 commit shas from `git log --oneline c6415a2..SHA` and a `git merge-base --is-ancestor` result for each fix commit.
       - `## 6. Conclusion` — `CI-03: closed` with the run id and attempt; or, if Task 1 stopped, `CI-03: NOT CLOSED` with the failing leg, step, test and captured excerpt. End the section with this sentence, filling in the run id and attempt: `This record is published by a later docs-only push to main; that push's CI run is not the proof run and does not replace run RUN attempt N.`
    2. In the debug record: set front matter `status: resolved` (only when the run is green) and `updated` to today, and add a `## Resolution` section at the top of the body citing the run id, attempt, head SHA and `CI_RUN.md`, noting that Phase 11 (LIFE-03, LIFE-04) cites this record. When Task 1 stopped red, keep `status: diagnosed` and add the red run under Evidence instead.
    3. Commit both files as `docs(10-04): record three-OS proof run`.
    4. Publish the record with a plain fast-forward `git push origin main`, with no force and no other commit on top. Phase 11 (LIFE-03, LIFE-04) and 10-VERIFICATION.md cite CI_RUN.md and the debug record, so they must be on origin/main, not only local. Do this in both outcomes (closed or NOT CLOSED). The push triggers a new push-triggered CI run for the record commit. That run is not the proof run. CI_RUN.md stays pinned to RUN and attempt N from Task 1 and is never edited to reference the new run. The new run's outcome neither closes nor reopens CI-03, and it is not re-run to change the proof. Record only in the SUMMARY the new run's id (the `gh run list --workflow CI --branch main --event push --json databaseId,headSha` entry for the record commit SHA) and its conclusion if it has finished. Waiting for it is not required.
    5. The phase verifier writes 10-VERIFICATION.md and must cite CI_RUN.md as the CI-03 evidence (D-14). Say so in the SUMMARY's next-steps line, and say that the proof must be re-checked through the run id in CI_RUN.md, not through the current origin/main head.
  </action>
  <verify>
    <automated>F=.planning/phases/10-three-os-ci-baseline/CI_RUN.md && ID=$(grep -oE 'actions/runs/[0-9]+' "$F" | head -1 | cut -d/ -f3) && test -n "$ID" && OUT=$(gh run view "$ID" --json event,conclusion,headSha --jq '.event + " " + .conclusion + " " + .headSha') && echo "$OUT" && case "$OUT" in "push success "*) grep -q "${OUT##* }" "$F" ;; *) false ;; esac && grep -q 'Attempt' "$F"</automated>
    <fails_when>no run URL in CI_RUN.md (empty ID), the run's event or conclusion is not `push success`, the recorded file does not contain the run's head SHA, or no Attempt row exists</fails_when>
    <automated>grep -q '^status: resolved' .planning/debug/ci02-windows-alpha-aos-host-leak.md && grep -q 'CI_RUN.md' .planning/debug/ci02-windows-alpha-aos-host-leak.md && grep -q '## Resolution' .planning/debug/ci02-windows-alpha-aos-host-leak.md</automated>
    <fails_when>any grep exits non-zero (debug record not resolved, not citing CI_RUN.md, or missing the Resolution section)</fails_when>
    <automated>F=.planning/phases/10-three-os-ci-baseline/CI_RUN.md && git fetch origin main --quiet && test "$(git rev-parse origin/main)" = "$(git rev-parse main)" && test "$(git log -1 --format=%s origin/main)" = "docs(10-04): record three-OS proof run" && git diff --quiet origin/main -- "$F" .planning/debug/ci02-windows-alpha-aos-host-leak.md && ID=$(grep -oE 'actions/runs/[0-9]+' "$F" | head -1 | cut -d/ -f3) && test -n "$ID" && test "$(gh run view "$ID" --json headSha --jq .headSha)" != "$(git rev-parse origin/main)"</automated>
    <fails_when>any `test` or `git diff --quiet` exits non-zero: origin/main differs from local main, the origin/main head is not the record commit, either record file at origin/main differs from the committed file, CI_RUN.md holds no run id, or the recorded run's head SHA equals the record commit (CI_RUN.md was repointed to the docs-push run)</fails_when>
  </verify>
  <acceptance_criteria>
    - CI_RUN.md contains the six sections named in the action, a Run ID equal to the run found for the pushed SHA, its Attempt, Trigger `push`, and the head SHA returned by `gh run view`.
    - Every job row's conclusion in CI_RUN.md matches `gh run view RUN --attempt N --json jobs`.
    - The debug record is `status: resolved`, has `## Resolution` citing the run id, attempt and CI_RUN.md.
    - `git log -1 --format=%s` is `docs(10-04): record three-OS proof run`, and `git status --porcelain` shows no modified tracked file afterwards.
    - After the push, origin/main equals local main, its head is the record commit, and both record files at origin/main are byte-identical to the committed files. CI_RUN.md still names the Task 1 run id and attempt, not the run triggered by the record push.
    - CI_RUN.md section 6 ends with the sentence stating that the docs-only push's run is not the proof run. The SUMMARY records that run's id, and its conclusion if it has finished.
  </acceptance_criteria>
  <done>CI_RUN.md is the inspectable CI-03 evidence retrieved from gh, and the CI-02 record is closed with it. Both are published to origin/main by a docs-only push. That push's own run is not the proof run, and CI_RUN.md stays pinned to the recorded run id and attempt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repository → origin/main | Pushing publishes commits every clone and every later CI run builds on |
| GitHub Actions API → phase record | Run facts cross from the API into a committed record that later phases treat as evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Repudiation | CI_RUN.md proof | high | mitigate | Every field comes from `gh run view`/`gh api` JSON captured in this plan; the verify step re-reads the run by id and compares event, conclusion and head SHA; attempt number and job start times prove no per-job re-run or stitched legs |
| T-10-08 | Tampering | git push to origin/main | medium | mitigate | Preflight (clean tree, ci.yml unchanged, check/build/build:check, scratch-home quick run, WSL POSIX check when available) before a plain fast-forward push; force-push forbidden. The later docs-only record push is also a plain fast-forward, carries only CI_RUN.md and the debug record, and its run is never substituted for the proof run (Task 2 verify pins the recorded run id) |
| T-10-09 | Information disclosure | CI log excerpts copied into CI_RUN.md | low | mitigate | Only test names, pass/fail lines, summary counts, step conclusions and D-05 metadata-only drift lines are copied; no environment values or file contents |
</threat_model>

<verification>
- `gh run view RUN --json event,conclusion,jobs` for the recorded run prints event `push`, conclusion `success`, and three `-latest / Node 24` legs `success`.
- CI_RUN.md's run id, attempt and head SHA match `gh` output; the debug record is resolved and cites CI_RUN.md.
- Both records are on origin/main through the docs-only push. After that push, origin/main points at the record commit, so Task 1's origin/main-based verify no longer targets the proof run. Re-check the proof through the run id in CI_RUN.md (Task 2's first verify), never through the current origin/main head.
- Phase verifier: 10-VERIFICATION.md must cite `.planning/phases/10-three-os-ci-baseline/CI_RUN.md` as the CI-03 evidence (D-14) and `.planning/debug/ci02-windows-alpha-aos-host-leak.md` as the CI-02 root-cause record.
</verification>

<success_criteria>
- ROADMAP success criterion 5 holds: a single `main` CI run id shows ubuntu-latest, macos-latest and windows-latest green, with no leg individually re-run or taken from another run.
- Success criteria 1 and 2 are observed on the CI legs in that same run.
</success_criteria>

## Artifacts this phase produces

- New file `.planning/phases/10-three-os-ci-baseline/CI_RUN.md` with sections `## 1. Run Metadata` (including an Attempt row), `## 2. Matrix Jobs`, `## 3. Phase 10 Observations`, `## 4. Single-Run Integrity (D-12, D-13)`, `## 5. Phase 10 Commits Contained`, `## 6. Conclusion`.
- Debug record `.planning/debug/ci02-windows-alpha-aos-host-leak.md` moved to `status: resolved` with a `## Resolution` section.
- The pushed `origin/main` head that the proof run built. Commit `docs(10-04): record three-OS proof run`, pushed afterwards as a docs-only fast-forward. Its CI run is not the proof run.

<output>
Create `.planning/phases/10-three-os-ci-baseline/10-04-SUMMARY.md` when done. Include:
- the run id, attempt, head SHA and per-leg conclusions
- any D-13 re-run, with its recorded cause
- the preflight POSIX check result (WSL pass, or first POSIX observation deferred to the proof run)
- the docs-only record push's run id and, if finished, its conclusion, labelled as not the proof run
</output>
