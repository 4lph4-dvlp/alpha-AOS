---
phase: quick
plan: 260911-w4s
type: execute
autonomous: true
status: complete
---

# Reproducible Codex GSD execution policy

User authorized recovery, portable policy implementation, fresh-install validation, usage measurement, then Phase 3 resumption. GSD remains the lifecycle/state owner. No third-party managed workflow is patched.

## Task 1 — Recover and measure

Record the interrupted 03-13 task boundary without claiming a complete plan. Preserve ae3a713 and the two uncommitted Task 3 files. Add a read-only, bounded local usage audit that aggregates per-response records without printing prompts, credentials, or double-counting cache tokens. Validate duplicate-response and time-window handling.

## Task 2 — Install owned Codex execution guidance

Provide a short repository-owned policy through Codex's native global instruction surface, preserving user text with one explicit owned block. Use the existing preview, operation-boundary, transaction, drift-refusal and rollback contracts. Integrate into managed install/update and a narrow CLI preview/apply command. Detect instruction overrides and report unsupported rather than claiming effectiveness. Policy limits reads, prohibits full-history delegation by default, avoids redundant parent work and repeated test output, preserves all GSD gates and requires honest quota recovery. Do not silently change the user's selected model or override higher-priority instructions.

## Task 3 — Validate and resume

Test clean install, idempotence, changed user text preservation, malformed ownership markers, reviewed-plan drift and rollback. Confirm native instruction discovery through a free meaningful invocation if available. Measure a bounded read-only smoke task using a fresh context against the recorded parent/executor baseline, recording limits of comparison. Document reinstall behavior and upstream work separately; do not claim GSD-only installs carry alpha-AOS policy. Commit quick summary and state before resuming 03-13, then 03-14 and 03-15 with phase verification; paid canary exclusions remain in force.
