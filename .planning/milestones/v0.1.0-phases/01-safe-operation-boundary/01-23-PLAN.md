---
phase: 01-safe-operation-boundary
plan: 23
type: execute
wave: 14
depends_on: [01-19, 01-20, 01-21, 01-22]
files_modified: []
autonomous: true
gap_closure: true
gap_ids: [G-01-1]
requirements: [SAFE-01, SAFE-02, SAFE-06]
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Gap `G-01-1` is closed only by an observed green three-OS CI matrix run — the same matrix, run 33937610401, that exposed all four root causes. Local green on one Windows developer host is necessary and not sufficient, and this plan is where that distinction is enforced."
    - "The ubuntu-latest and macos-latest legs pass `npm run check`, `npm test`, `npm run build:check` and the named ten-file `Safety boundary suites` step, and `preview.test.js` reports an empty not-run list for both POSIX wrapper families."
    - "The windows-latest leg passes the same four steps; its 46 `npm test` failures in the observed run were RC-1, and their disappearance is the only proof that the RC-1 fix covers the 8.3 short-name shape this host cannot produce."
    - "Two predictions made by the diagnosis are checked rather than assumed: that macOS shared the RC-3 home write with Linux, and that the darwin platform floor is exactly `__CF_USER_TEXT_ENCODING` and nothing else. A leg that disagrees disproves the diagnosis and reopens the gap instead of being explained away."
  artifacts: []
  key_links:
    - from: .github/workflows/ci.yml
      to: test/preview.test.ts
      via: "the per-OS safety step names the ten Phase 1 suites, so a suite that silently stops running is a failure rather than an omission"
      pattern: "preview.test"
  prohibitions:
    - statement: "This gap MUST NOT be recorded as closed on local evidence alone; the closing evidence is an observed CI run id with three green legs, quoted in the summary."
      requirement_id: SAFE-01
      category: correctness
      status: unverified
      flagged: true
    - statement: "A red leg MUST NOT be closed by removing a suite from the CI file, by marking a test as not-run, or by relaxing an assertion; a red leg is new information about the product."
      requirement_id: SAFE-02
      category: safety
      status: unverified
      flagged: true
---

<objective>
`G-01-1` 의 실제 수용 게이트를 실행한다. 이 gap 의 `truth` 는 "ubuntu-latest 와 macos-latest leg 가 `npm run check`, `npm test`, `npm run build:check` 및 명명된 10개 Safety boundary 스위트를 통과하고, `preview.test.js` 가 두 POSIX wrapper 계열에 대해 빈 not-run 목록을 보고한다" 이다. 그것은 로컬에서 관측할 수 없다.

Purpose: 01-19..01-22 가 네 개의 근본 원인을 각각 닫았지만, 넷 중 셋은 이 개발 호스트에서 원리적으로 재현되지 않는다. RC-1 의 8.3 단축명 형태는 Windows CI 러너에만 있고, RC-2 는 macOS 에만 있고, RC-4 는 부하 의존적이며 POSIX 전용이다. 3-OS 매트릭스가 이 gap 을 드러낸 도구였으므로 그것이 이 gap 을 닫는 도구다.
Output: 관측된 CI run id 와 세 leg 의 결과. 초록이면 gap 종료, 빨간불이면 새로운 진단 입력.
</objective>

<execution_context>
@C:/Users/alpha/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safe-operation-boundary/01-UAT.md
@.planning/phases/01-safe-operation-boundary/01-VALIDATION.md
@.github/workflows/ci.yml
</context>

## Gap being closed

`01-UAT.md` gap `G-01-1` in full. The four root-cause plans each state, in their own `<verification>` section, that local green is necessary but not sufficient. This plan is the place that statement is cashed in.

The observed failure being replaced:

| Leg | `npm test` | Later steps | Cause |
|-----|-----------|-------------|-------|
| ubuntu-latest | pass 190 / fail 0 | `Safety boundary suites` pass 109 / fail 4 | RC-3 (three failures, one defect) and RC-4 (one failure) |
| macos-latest | pass 140 / fail 50 | not run | RC-1 and RC-2 |
| windows-latest | pass 144 / fail 46 | not run | RC-1 |

Observed run: `https://github.com/4lph4-dvlp/alpha-AOS/actions/runs/33937610401`.

## Wave placement

This plan runs in wave 14, after the four root-cause plans have each run in a wave of their own — `01-19` in 10, `01-20` in 11, `01-21` in 12, `01-22` in 13. The rationale for serializing them is in `01-19-PLAN.md` `## Wave placement`: each opens with a committed RED, and `01-20`'s RED is a type error that breaks the build for the whole tree.

One consequence lands here. This plan is now the **only** place a full-suite figure is meaningful across all four fixes at once, and the per-plan chain of SUMMARY totals is what makes the arithmetic checkable rather than asserted.

## Multi-Source Coverage Audit

| Source | Item | Status | Where |
|--------|------|--------|-------|
| GOAL (ROADMAP Phase 1) | the phase goal is only true if it is true on all three supported operating systems | COVERED | Task 2 |
| UAT `G-01-1.truth` | the full truth statement, which names the legs, the steps, the ten suites and the empty not-run list | COVERED | Task 2 |
| UAT `missing[4]` | "floor 를 3-OS 에서 실제 관측과 대조하는 테스트가 CI 매트릭스 전 leg 에서 돌도록 한다" | COVERED | Task 2 (the macOS leg running at all is the missing half) |
| UAT `missing[12]` | "macOS 레그도 같은 결함을 가질 것으로 예측된다 … 아니라면 이 진단이 반증된다" | COVERED | Task 2 (recorded as a falsifiable prediction, checked against the leg) |
| 01-VALIDATION.md | the three-OS CI suite must be green for phase verification to hold | COVERED | Task 1, Task 2 |
| CONTEXT D-01 | fail closed — an unobserved leg is not a passing leg | COVERED | Task 2 (a not-run leg is not a pass) |

No source item is MISSING. Nothing is deferred by this plan.

## Flagged Assumptions (spec-less edge probe)

The deterministic edge probe for this phase returned **6 applicable, 0 resolved, 6 unresolved**. The six rows are enumerated in full in `01-17-PLAN.md` and are one shared ledger across every gap-closure plan of this phase, so they are not restated here and are not double-counted.

**No-silent-drop equality:** 6 probe-surfaced edge items == 0 authored into `must_haves.truths` + 6 surfaced as flagged assumptions in the shared ledger. This plan authors no new edge predicates; it observes the ones the four preceding plans authored. 2 prohibitions kept and authored descriptor-less into `must_haves.prohibitions`.

<tasks>

<task type="auto">
  <name>Task 1: 로컬에서 CI 와 동일한 네 스텝을 그대로 돌려 밀어 넣을 값을 확정한다</name>
  <files>(no files modified — this task runs commands and records results)</files>
  <read_first>
    .github/workflows/ci.yml
    .planning/phases/01-safe-operation-boundary/01-19-SUMMARY.md
    .planning/phases/01-safe-operation-boundary/01-20-SUMMARY.md
    .planning/phases/01-safe-operation-boundary/01-21-SUMMARY.md
    .planning/phases/01-safe-operation-boundary/01-22-SUMMARY.md
  </read_first>
  <precondition>All four of `01-19-SUMMARY.md`, `01-20-SUMMARY.md`, `01-21-SUMMARY.md` and `01-22-SUMMARY.md` exist and record a completed plan. Halt and report if any is missing — this plan observes their combined effect and has nothing to observe otherwise.</precondition>
  <action>
CI 워크플로가 정의한 네 스텝을 이 호스트에서 같은 순서로 그대로 돌린다. 순서를 바꾸거나 스텝을 건너뛰지 않는다 — `npm run build:check` 가 `npm test` 뒤에 오는 것은 `npm test` 가 방금 만든 아티팩트를 검증하기 위해서다.

  1. `npm run check`
  2. `npm test`
  3. `npm run build:check`
  4. `.github/workflows/ci.yml` 의 `Safety boundary suites` 스텝에 적힌 10개 파일을 그 순서 그대로 `node --test` 에 넘긴다. 파일 목록은 워크플로에서 읽어 오고, 기억에 의존해 다시 타이핑하지 않는다.

네 스텝의 결과를 기록한다: 각 스텝의 exit code, `npm test` 의 `tests`/`pass`/`fail` 수, 10-파일 실행의 `tests`/`pass`/`fail` 수, 그리고 `preview.test.js` 가 진단으로 출력하는 wrapper not-run 목록. 이 Windows 호스트에서 `bash` 가 있으면 POSIX wrapper 두 계열의 not-run 목록이 비어 있어야 하고, 없으면 그 사실 자체를 기록한다 — CI 의 ubuntu/macos leg 에서는 반드시 비어 있어야 하는 값이므로, 로컬 관측은 CI 관측을 대신하지 않고 비교 기준이 된다.

`01-18-SUMMARY.md` 가 기록한 테스트 총수를 기준선으로 삼아, 네 plan 이 더한 새 테스트 수만큼 실제로 늘었는지 확인한다. 네 plan 은 직렬 웨이브(10, 11, 12, 13)로 돌았으므로 각 SUMMARY 의 총수가 다음 plan 의 기준선이고, 사슬 전체가 맞아떨어져야 한다: `01-19` +4, `01-20` +1, `01-21` +4, `01-22` +2 — 합계 **+11**. 각 SUMMARY 가 기록한 총수를 순서대로 늘어놓아 그 증가폭이 실제로 그렇게 나뉘는지 확인한다. 사슬 어딘가가 어긋나면 그 plan 의 테스트가 실행되지 않고 있는 것이므로 push 하지 말고 그것부터 찾는다.

**`resolveGlobalNodeModulesRoot` 의 관측 빈도를 기록한다 (후속 phase 입력).** `01-21` 은 전역 npm 루트를 spawn 없이 해석하며, 사용자 `.npmrc` 의 `prefix` 는 읽지 않는다. 그래서 nvm/volta 사용자나 `prefix` 를 명시한 사용자는 프리뷰의 `ecc:runtime` 노트에서 영구적으로 `unverified` 를 보게 된다. 이것은 D-01 위반이 아니다 — 증명할 수 없는 것을 `current` 로 주장하지 않고 install 을 계획하는 쪽이 안전한 쪽이고, plan 이 그 한계를 정직하게 적었다. 다만 얼마나 흔한지는 아무도 모른다. 이 호스트에서 `alpha-aos install --target claude` (`--apply` 없이) 를 실행해 `ecc:runtime` 스텝의 `action` 과 `note` 를 그대로 기록하고, Task 2 에서 세 leg 의 같은 값을 관측할 수 있으면 함께 기록한다. 이 값은 이 phase 를 막지 않는다. 요약에 "관측 빈도: <n>/<total> 호스트에서 unverified" 한 줄로 남겨, 후속 phase 가 "spawn 없이 `.npmrc` 의 `prefix` 를 읽는 파서를 만들 가치가 있는가" 를 근거 위에서 판단하게 한다.

한 스텝이라도 빨간불이면 여기서 멈춘다. Task 2 로 넘어가지 않는다 — 로컬에서 빨간 것을 3-OS 매트릭스가 고쳐 주지 않는다.
  </action>
  <acceptance_criteria>
    - All four steps exit 0 on this host
    - The recorded `npm test` total is at least eleven greater than the total recorded in `01-18-SUMMARY.md`, and the per-plan chain adds up: `01-19` +4, `01-20` +1, `01-21` +4, `01-22` +2, each read from that plan's own SUMMARY
    - The `ecc:runtime` step's `action` and `note` from a real `alpha-aos install` preview on this host are recorded verbatim, so the frequency of the `unverified` global-root state has an observation behind it rather than a guess. This blocks nothing; it is an input to a later phase
    - The ten-file run reports `# fail 0`
    - The wrapper not-run list observed locally is recorded verbatim, including the case where a launcher is unavailable on this host
    - The file list passed to the ten-file run was read from `.github/workflows/ci.yml` rather than retyped
  </acceptance_criteria>
  <verify>
    <automated>npm run check &amp;&amp; npm test &amp;&amp; npm run build:check</automated>
    <fails_when>non-zero exit, `fail 0` absent from the `npm test` summary line, or the line `build artifact verified` absent from the output</fails_when>
    <automated>node --test dist/test/preview.test.js dist/test/path-boundary.test.js dist/test/transaction-crash.test.js dist/test/redaction.test.js dist/test/validation.test.js dist/test/process.test.js dist/test/protocol-session.test.js dist/test/mcp-proxy.test.js dist/test/install.test.js dist/test/update.test.js</automated>
    <fails_when>non-zero exit, or `# fail 0` absent from the summary</fails_when>
  </verify>
  <done>The four CI steps are green on this host and their numbers are recorded, so a red leg in CI can be attributed to a platform rather than to an untested change.</done>
</task>

<task type="auto">
  <name>Task 2: CI 매트릭스 계약을 기계로 확인하고 3-OS 관측 항목을 end-of-phase 로 넘긴다</name>
  <files>(no files modified — this task checks the workflow contract and states the human observation)</files>
  <read_first>
    .github/workflows/ci.yml
    .planning/phases/01-safe-operation-boundary/01-VALIDATION.md
  </read_first>
  <action>
`workflow.human_verify_mode` 가 `end-of-phase` 이므로 이 관측은 mid-flight checkpoint 로 멈추지 않는다. 이 task 는 기계가 확인할 수 있는 부분을 확인하고, 사람만 할 수 있는 부분을 `<human-check>` 로 남겨 phase 종료 시 `01-UAT.md` 로 수확되게 한다. 원래의 `G-01-1` 도 정확히 그 경로로 표면화됐다.

**기계가 확인하는 것.** `.github/workflows/ci.yml` 이 세 leg(`ubuntu-latest`, `macos-latest`, `windows-latest`), 네 스텝(`npm run check`, `npm test`, `npm run build:check`, `Safety boundary suites`), 그리고 10개 스위트 파일 이름을 모두 담고 있는지 확인한다. 하나라도 빠지면 매트릭스가 관측하겠다고 약속한 표면이 줄어든 것이므로 실패시킨다. 이 확인은 `<verify>` 의 첫 automated 커맨드가 수행한다.

**사람이 관측하는 것.** 아래 `<human-check>` 에 적힌 절차를 그대로 수행한다 — push 하고, 매트릭스 실행을 끝까지 보고, run id 와 세 leg 의 결과를 기록한다.

push 한 뒤 CI 매트릭스 실행을 끝까지 관측하고, 세 leg 각각에 대해 아래를 확인한다.

**확인할 것 (leg 마다):**

  - `npm run check` — exit 0
  - `npm test` — `fail 0`
  - `npm run build:check` — `build artifact verified`
  - `Safety boundary suites` — `# fail 0`, 그리고 10개 파일이 모두 실행됐는지(파일 하나가 조용히 빠지면 통과처럼 보인다)
  - ubuntu-latest 와 macos-latest 에서 `preview.test.js` 의 wrapper not-run 목록이 비어 있는지 — `install.sh` 와 `update.sh` 두 계열이 실제로 실행됐다는 뜻이다
  - `Validate POSIX wrapper syntax` / `Validate PowerShell wrapper syntax` — exit 0

**같이 확인할 두 예측 (진단의 반증 가능성):**

  1. macos-latest 가 RC-3 의 홈 쓰기를 Linux 와 공유했는가. 01-UAT.md 는 npm 캐시 기본값이 두 플랫폼에서 동일하다는 이유로 그렇게 예측했다. 이번 실행이 초록이면 이 예측은 확인도 반증도 되지 않으므로, 판단은 이전 실행(33937610401)의 macOS leg 가 `npm test` 단계에서 멈춰 `Safety boundary suites` 에 도달조차 못 했다는 사실 위에서 내린다. 이 점을 그대로 기록한다 — 예측이 검증되지 않은 채 남았다는 것이 정직한 결론이며, 초록을 예측 확인으로 바꿔 적지 않는다.
  2. darwin platform floor 가 정확히 `__CF_USER_TEXT_ENCODING` 하나인가. macos-latest 의 `the declared platform floor must match what the OS actually delivers` 가 초록이면 확인된 것이고, 다른 이름을 보고하며 실패하면 floor 를 관측에 맞춰 넓힌다 — 테스트가 옳고 선언이 따라간다.

**빨간 leg 를 만났을 때 할 수 있는 것과 할 수 없는 것.** 빨간 leg 는 제품에 대한 새 정보다. CI 파일에서 스위트를 빼거나, 테스트를 not-run 으로 표시하거나, 단언을 완화해 초록으로 만드는 것은 금지된다. 실패 텍스트와 leg 이름과 run id 를 기록하고 진단으로 되돌아간다.

**기록할 것:** 관측한 run id 와 URL, 세 leg 각각의 결론(pass/fail 과 실패 시 정확한 실패 이름), 그리고 위 두 예측에 대한 결론. 이 값들이 `01-UAT.md` 의 `G-01-1` 을 닫는 증거이며, 요약에 그대로 옮겨 적는다.
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` still names all three legs, all four steps, and all ten Phase 1 suite files
    - The human observation procedure is present in this task's `<verify><human-check>` so the end-of-phase verifier harvests it into `01-UAT.md`
    - No suite name was removed from the workflow by any plan in this gap-closure round
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('node:fs');const s=fs.readFileSync('.github/workflows/ci.yml','utf8');const need=['ubuntu-latest','macos-latest','windows-latest','npm run check','npm test','npm run build:check','Safety boundary suites','dist/test/preview.test.js','dist/test/path-boundary.test.js','dist/test/transaction-crash.test.js','dist/test/redaction.test.js','dist/test/validation.test.js','dist/test/process.test.js','dist/test/protocol-session.test.js','dist/test/mcp-proxy.test.js','dist/test/install.test.js','dist/test/update.test.js'];const miss=need.filter(n=>!s.includes(n));if(miss.length){console.error('ci matrix contract missing: '+miss.join(', '));process.exit(1);}console.log('ci matrix contract intact: 3 legs, 4 steps, 10 suites');"</automated>
    <fails_when>non-zero exit, or the line `ci matrix contract intact: 3 legs, 4 steps, 10 suites` absent from the output</fails_when>
    <human-check>Push the branch, watch the three-OS matrix run to completion, and record the run id and URL. Confirm for every leg: `npm run check` exit 0, `npm test` `fail 0`, `npm run build:check` prints `build artifact verified`, and `Safety boundary suites` reports `# fail 0` with all ten named files actually executed. Confirm the wrapper not-run list is empty on ubuntu-latest and macos-latest. Answer both diagnostic predictions — the macOS RC-3 prediction may legitimately be answered "not verifiable from this run", and that non-answer must be recorded as such rather than upgraded to a confirmation by a green leg. If any leg is red, record the leg name, the exact failing test names and the run id, and return to diagnosis: do not remove a suite from the workflow, mark a test as not-run, or relax an assertion.</human-check>
  </verify>
  <done>The workflow still promises the full matrix surface, and the three-OS observation is queued as an end-of-phase human item with an explicit red-leg protocol.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer host → supported operating systems | Three of the four root causes in this gap are invisible on this host by construction. Anything asserted only here is an assumption about the other two platforms. |
| CI configuration → what is actually executed | A suite removed from the named list, or a leg that stops before its later steps, reads as an absence rather than a failure unless someone checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-23-01 | Repudiation | recording the gap as closed on local evidence | high | mitigate | Task 2's `<human-check>` requires an observed run id and is harvested into `01-UAT.md` at end of phase, which is the same path that surfaced this gap originally; the prohibition is stated in `must_haves.prohibitions`. |
| T-01-23-02 | Tampering | making a red leg green by shrinking what CI runs | high | mitigate | Task 2 states explicitly what may not be done, and requires the ten named files to be confirmed as actually executed rather than merely listed. |
| T-01-23-03 | Repudiation | a leg that stops early being read as a pass | medium | mitigate | Task 2 checks all four steps per leg; the observed run's macOS and Windows legs never reached their later steps, which is exactly the shape being guarded against. |
| T-01-23-04 | Repudiation | converting an unverifiable prediction into a confirmed one because the run was green | medium | mitigate | Task 2 requires "not verifiable from this run" to be recorded as such for prediction 1. |
| T-01-23-SC | Tampering | npm / pip / cargo installs | low | accept | This plan adds no dependency and runs no package-manager install task. No new legitimacy checkpoint is required. |
</threat_model>

<verification>
- The four CI steps are green locally and their numbers are recorded (Task 1).
- A three-OS CI run is observed to completion, its id and URL recorded, and every leg green across all four steps (Task 2).
- The wrapper not-run list is empty on ubuntu-latest and macos-latest.
- Both diagnostic predictions have a recorded answer, including a recorded non-answer where the run cannot decide.
- The `ecc:runtime` preview state (`current` / `install` + `unverified`) is recorded from an actual preview run, as the observation a later phase needs to decide whether a spawn-free `.npmrc` `prefix` reader is worth building.
</verification>

<success_criteria>
1. Gap `G-01-1` is closed by an observed CI run id with three green legs, not by local evidence.
2. Every one of the ten named Phase 1 suites is confirmed to have executed on every leg.
3. `preview.test.js` reports an empty not-run list for both POSIX wrapper families on the two POSIX legs.
4. The macOS platform-floor prediction is confirmed against the leg, or the floor is corrected to match what the OS delivers.
5. No red leg was closed by removing a suite, skipping a test, or relaxing an assertion.
</success_criteria>

<output>
Create `.planning/phases/01-safe-operation-boundary/01-23-SUMMARY.md` when done.
</output>
