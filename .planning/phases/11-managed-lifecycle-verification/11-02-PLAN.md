---
phase: 11-managed-lifecycle-verification
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - test/helpers/io-trap-preload.ts
  - test/status-offline-verification.test.ts
  - .planning/phases/11-managed-lifecycle-verification/evidence/probes/life02-status.mjs
  - .planning/phases/11-managed-lifecycle-verification/evidence/probes/life02-discovery.mjs
  - .planning/phases/11-managed-lifecycle-verification/evidence/life02-status.txt
  - .planning/phases/11-managed-lifecycle-verification/evidence/life02-discovery.txt
  - .planning/phases/11-managed-lifecycle-verification/evidence/LIFE-02-findings.md
autonomous: true
requirements: [LIFE-09]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "LIFE-02 'offline status' is judged by a CLI-level trap, not by the in-process default-export patch the Phase 6 test uses (D-04): `node --import dist/test/helpers/io-trap-preload.js <packed cli> status --json` logs zero child_process calls and zero network calls, and the same preload logs at least one child_process call for `inventory --json` and at least one `fetch` for a loopback fetch control, so a silent trap cannot read as a pass."
    - "LIFE-02 'fast' is judged on a populated state root (after a packed Codex install), with the in-process `getOfflineStatus` median of 10 calls compared against the Phase 6 50 ms interpretation and the packed CLI wall times recorded as notes, never inside CHECK details (D-04, D-09)."
    - "LIFE-02 native discovery and meaningful read-only execution evidence come from actually running the packed CLI `doctor --discovery` and `doctor --canary --no-spend` against a scratch clone of the repository with ALPHA_AOS_STATE_DIR in the sandbox; spending legs are recorded 'not executed (cost)' and never run (D-11)."
    - "Each of the five harnesses (claude, codex, antigravity, pi, hermes) gets one discovery check and one no-spend execution check; a harness with no no-spend execution evidence is a named gap candidate, not a pass, and a harness missing from the discovery rows is VIOLATED, not skipped (D-10, D-11)."
    - "A Claude Code real-host invocation receipt is not required and is not attempted; Claude's cost-excluded legs cite G-07-2 / REL-02 as the future requirement that owns them (D-10)."
    - "`doctor --json` findings are judged for a `code` on every finding and for a runnable next action on the error-level finding produced by a deliberately corrupt journal in the sandbox (actionable coded findings clause, D-09)."
    - "`evidence/LIFE-02-findings.md` follows the evidence README template: every clause row cites a transcript CHECK id, a named test, or a CI job id; each gap candidate carries clause, missing, close condition and a reproduction with observed output (D-02, D-06)."
    - "The committed test `test/status-offline-verification.test.ts` passes on this host and under WSL before it is committed; a failing trap assertion is recorded as a gap reproduction instead of committed (D-12)."
    - "The real repository work tree and the real host state root are unchanged across the discovery and canary runs: `git status --porcelain` output is identical before and after, and the probe host guard reports unchanged (D-13 host safety)."
    - "`src/` is untouched (D-05)."
  prohibitions:
    - statement: "MUST NOT run any canary leg that spends a model turn, and MUST NOT pass a spend-enabling flag or credential to `doctor --canary` (D-11)."
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT run `doctor --discovery` or `doctor --canary` against the real repository tree or with ALPHA_AOS_STATE_DIR unset; the canary writes memory-vault files into the project it is given."
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT print harness configuration bodies, authentication files or environment values into the transcripts; discovery output is recorded as the CLI printed it through its redaction seam, aliased."
      category: privacy
      status: resolved
      verification: judgment
  artifacts:
    - path: "test/helpers/io-trap-preload.ts"
      provides: "Preload that records child_process and network calls to the file named by ALPHA_AOS_IO_TRAP_LOG, propagated to ESM named imports with syncBuiltinESMExports"
      contains: "syncBuiltinESMExports"
    - path: "test/status-offline-verification.test.ts"
      provides: "CI guard: CLI `status` spawns nothing and opens no connection, with positive controls"
      contains: "ALPHA_AOS_IO_TRAP_LOG"
    - path: ".planning/phases/11-managed-lifecycle-verification/evidence/life02-discovery.txt"
      provides: "doctor --discovery and --canary --no-spend transcripts with per-harness checks"
      contains: "CHECK life02.discovery.antigravity"
    - path: ".planning/phases/11-managed-lifecycle-verification/evidence/LIFE-02-findings.md"
      provides: "LIFE-02 clause table, oracle audit, provisional verdict, gap candidates"
      contains: "LIFE-02/C"
  key_links:
    - from: "test/status-offline-verification.test.ts"
      to: "dist/test/helpers/io-trap-preload.js"
      via: "node --import <file URL> dist/src/cli.js status --json"
      pattern: "--import"
    - from: ".planning/phases/11-managed-lifecycle-verification/evidence/probes/life02-status.mjs"
      to: ".planning/phases/11-managed-lifecycle-verification/evidence/probes/probe-lib.mjs"
      via: "import of the plan 11-01 probe library (read-only reuse)"
      pattern: "probe-lib.mjs"
---

<objective>
Produce the LIFE-02 evidence: offline, fast `status`; explicit `doctor` / canary modes with native discovery and meaningful read-only execution evidence; actionable coded findings. Every clause is judged on the packed CLI (D-01) with stronger oracles than the Phase 6 tests (D-04), and the no-spend limits of D-11 are recorded honestly per harness.

Purpose: the Phase 6 LIFE-02 coverage cites only `status` unit tests; discovery and execution evidence have never been produced for this requirement.

Output: two probes and transcripts, an I/O trap preload plus one pass-only CI test (D-12), and `evidence/LIFE-02-findings.md` for plan 11-07.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-managed-lifecycle-verification/11-CONTEXT.md
@.planning/phases/11-managed-lifecycle-verification/11-RESEARCH.md
@.planning/phases/11-managed-lifecycle-verification/evidence/README.md
@.planning/phases/11-managed-lifecycle-verification/11-01-SUMMARY.md
@test/status.test.ts
@src/core/status.ts
</context>

<interfaces>
Paths: EV = `.planning/phases/11-managed-lifecycle-verification/evidence`, PROBES = `EV/probes`.

From plan 11-01 (read-only here; this plan must not modify `probe-lib.mjs` or `test/helpers/packed-sandbox.ts`, because four plans run in parallel in this wave):
- probe-lib.mjs: `newSandbox`, `assertSandboxEnv`, `runCli`, `fingerprintRoots`, `compareFingerprints`, `hostGuardTargets`, `withHostGuard`, `createTranscript`, `packOnce`. Transcript vocabulary and outcome words (`HOLDS`, `VIOLATED`, `NOT-OBSERVED`, `OBSERVED`) are in EV/README.md.
- packed-sandbox.ts: `createIsolatedSandbox`, `openSandbox`, `installPackedRelease`, `seedHarnessPrerequisites`, `hostNpmCache`, `snapshotTargets`, `describeHostDrift`.

Product surfaces:
- `status [--json]` (src/cli.ts:515-520): `getOfflineStatus(userStateRoot(), catalog, lock)`; exit 1 when `needsRepair`. Human text from `formatOfflineStatus` (src/format.ts:909-931).
- `doctor [--json]` (src/cli.ts:770 onward): findings `{ level, code, ... }`, exit 2 on an error finding.
- `doctor --discovery [path] [--json]` and `doctor --canary [path] [--harness <id>] [--capability <id>] [--no-spend] [--json]` (help text src/cli.ts:128-175). Research run: discovery 36.6 s with COMPLETE rows for codex and pi only, claude NOT-RUN (spends a model turn), hermes unsupported, antigravity absent; canary --no-spend 9.1 s, CAPA-01/02/05 legs `not attempted` for claude and codex, CAPA-03 handoff free legs COMPLETE for codex; the canary writes `.ecc/memory/project/...` into the given project.
- `inventory --json` spawns version probes (positive control for the child_process trap).

New files:
- `test/helpers/io-trap-preload.ts`: when ALPHA_AOS_IO_TRAP_LOG is set, wraps `node:child_process` (spawn, spawnSync, exec, execSync, execFile, execFileSync, fork), `node:net` (connect, createConnection), `node:tls` (connect), `node:dns` (lookup, resolve), `node:http` and `node:https` (request, get) and `globalThis.fetch`; each call appends one line `<module>.<function>` (or `fetch`) to the log with appendFileSync and then delegates; after patching calls `syncBuiltinESMExports()` from `node:module`. When the variable is unset it patches nothing.
- Trap log line vocabulary: `child_process.<fn>`, `net.<fn>`, `tls.connect`, `dns.<fn>`, `http.<fn>`, `https.<fn>`, `fetch`.
- `--import` takes a file URL on Windows: pass `pathToFileURL(<absolute path>).href`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Offline and fast status, coded doctor findings — packed probe plus a CLI-level I/O trap test</name>
  <precondition>Plan 11-01 is complete: `dist/test/helpers/packed-sandbox.js` builds and EV/seed-selfcheck.txt shows `seed.codex.ecc HOLDS`.</precondition>
  <files>test/helpers/io-trap-preload.ts, test/status-offline-verification.test.ts, .planning/phases/11-managed-lifecycle-verification/evidence/probes/life02-status.mjs, .planning/phases/11-managed-lifecycle-verification/evidence/life02-status.txt</files>
  <read_first>
    test/status.test.ts (lines 20-75: the zero-subprocess patch and the 50 ms benchmark whose oracles this task audits)
    src/core/status.ts (getOfflineStatus signature and fields needsRepair, managedStatePresent)
    src/cli.ts (lines 400-425 catalog and lock loading before dispatch, 477-520 inventory and status, 770-1007 doctor)
    src/format.ts (lines 900-935 formatOfflineStatus)
    src/core/transaction.ts (lines 180-215 applyFileTransaction options)
    .planning/phases/11-managed-lifecycle-verification/evidence/README.md
    .planning/phases/11-managed-lifecycle-verification/evidence/probes/probe-lib.mjs
    test/helpers/packed-sandbox.ts
  </read_first>
  <behavior>
    - Test `offline status spawns no child process and opens no network connection through the CLI (LIFE-02)`: in a `createIsolatedSandbox` whose state root holds one applied journal written by `applyFileTransaction`, `node --import <preload URL> dist/src/cli.js status --json` exits 0, reports `needsRepair` false and `managedStatePresent` true, and the trap log is absent or empty.
    - Positive control in the same test: `inventory --json` under the same preload writes at least one `child_process.` line.
    - Positive control in the same test: `node --import <preload URL> --input-type=module -e` running a loopback fetch to 127.0.0.1 port 9 (error swallowed) writes at least one `fetch` line.
  </behavior>
  <action>
    Per D-01, D-04, D-09 and D-12:
    1. Write test/helpers/io-trap-preload.ts exactly as the interfaces block describes. Keep the original function in a closure and delegate with the same `this` and arguments so behavior is unchanged apart from the log line.
    2. Write test/status-offline-verification.test.ts with the three behaviors above in one `test(...)` titled `offline status spawns no child process and opens no network connection through the CLI (LIFE-02)`. Use `createIsolatedSandbox(context)` from `./helpers/packed-sandbox.js`, the checkout CLI at `dist/src/cli.js` resolved from `import.meta.dirname`, `process.execPath` as the executable, the sandbox env plus ALPHA_AOS_IO_TRAP_LOG pointing at a file inside the sandbox root, and a separate log file per spawn. Build every path with `join` from the sandbox root or `import.meta.dirname`; add no platform-absolute string literal (the path-literal guard scans test/*.ts). Run it on this host and under WSL (verify block); if any assertion fails for a reason that is a product fact rather than a test mistake, do not commit the test: move the failing observation into the probe transcript as a VIOLATED check instead.
    3. Write PROBES/life02-status.mjs using probe-lib: inside `withHostGuard`, pack once, one sandbox, `installPackedRelease`, `seedHarnessPrerequisites(..., "codex")`, `install --target codex --apply --json`. Then: (a) `status` and `status --json` with checks `life02.status.ok-after-install` (exit 0, needsRepair false); (b) the packed CLI `status --json` ten times, writing min, median and max wall milliseconds as a note line, check `life02.status.cli-timing` OBSERVED with detail `wall times in note`; (c) import the INSTALLED package's `dist/src/core/status.js` and `dist/src/core/catalog.js`, load its catalog and stable lock, call `getOfflineStatus(<sandbox state>, catalog, lock)` ten times, note the median, and check `life02.status.in-process-under-50ms` HOLDS when the median is below 50 ms else VIOLATED; (d) run the packed CLI entrypoint as `node --import <preload URL from the checkout dist> <entrypoint> status --json` with ALPHA_AOS_IO_TRAP_LOG in the sandbox, plus the two positive controls, with checks `life02.status.trap-control-spawn`, `life02.status.trap-control-network` (HOLDS when the control logged at least one line; if either control logs nothing the trap is invalid and both following checks become NOT-OBSERVED), `life02.status.zero-subprocess` and `life02.status.zero-network` (HOLDS when the status log is empty, VIOLATED listing the logged lines otherwise); (e) `doctor --json` on the healthy sandbox, check `life02.doctor.coded` HOLDS when every finding has a non-empty string `code` and `level`; (f) write a deliberately corrupt journal file `<sandbox state>/journal/p11-corrupt.json` containing the bytes `{not json`, then `status` (human) and `status --json` with check `life02.status.needs-repair` (exit 1 and needsRepair true), note the verbatim status guidance line, then `doctor --json` with checks `life02.doctor.flags-corrupt-journal` (an error- or warning-level finding names the journal problem: HOLDS, else VIOLATED) and `life02.doctor.actionable` (that finding, or the status guidance, names a runnable `alpha-aos` command: HOLDS, else VIOLATED). Write EV/life02-status.txt.
  </action>
  <verify>
    <automated>npm run check && npm run build && node scripts/run-tests.mjs --files dist/test/status-offline-verification.test.js dist/test/path-literal-guard.test.js</automated>
    <fails_when>non-zero exit or a summary line with `fail` above 0</fails_when>
    <automated>wsl.exe --cd "$(cygpath -w "$PWD")" -e sh -c 'node scripts/run-tests.mjs --files dist/test/status-offline-verification.test.js'</automated>
    <fails_when>non-zero exit or `fail` above 0 in the summary (the trap or the test is not portable to Linux)</fails_when>
    <automated>node .planning/phases/11-managed-lifecycle-verification/evidence/probes/life02-status.mjs && T=.planning/phases/11-managed-lifecycle-verification/evidence/life02-status.txt && grep -q '^host-guard: unchanged' "$T" && for c in life02.status.ok-after-install life02.status.in-process-under-50ms life02.status.trap-control-spawn life02.status.trap-control-network life02.status.zero-subprocess life02.status.zero-network life02.doctor.coded life02.status.needs-repair life02.doctor.flags-corrupt-journal life02.doctor.actionable; do grep -qE "^CHECK $c (HOLDS|VIOLATED|NOT-OBSERVED|OBSERVED) " "$T" || exit 1; done</automated>
    <fails_when>non-zero exit, no unchanged host guard line, or any of the ten named checks is missing</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'syncBuiltinESMExports' test/helpers/io-trap-preload.ts` is at least 1.
    - The committed test file exists only if it passed on this host and in WSL; otherwise the SUMMARY names the failing assertion and EV/life02-status.txt carries it as a VIOLATED check.
    - EV/life02-status.txt holds the ten named checks, and no CHECK detail contains a millisecond figure (timings live in note lines only).
    - `git status --porcelain -- src` prints nothing.
  </acceptance_criteria>
  <done>Offline, fast and coded-finding clauses of LIFE-02 have packed-CLI evidence with valid positive controls, and CI now guards the CLI-level offline property.</done>
</task>

<task type="auto">
  <name>Task 2: Native discovery and no-spend canary evidence for five harnesses, and the LIFE-02 findings</name>
  <precondition>The harness CLIs `codex`, `claude`, `pi`, `hermes` and `agy` resolve on PATH (research Environment Availability); a missing CLI is recorded NOT-OBSERVED with the lookup output, not treated as a product gap.</precondition>
  <files>.planning/phases/11-managed-lifecycle-verification/evidence/probes/life02-discovery.mjs, .planning/phases/11-managed-lifecycle-verification/evidence/life02-discovery.txt, .planning/phases/11-managed-lifecycle-verification/evidence/LIFE-02-findings.md</files>
  <read_first>
    src/cli.ts (lines 770-1007 doctor routing: --discovery, --canary, --no-spend, --json)
    src/core/doctor.ts (DoctorFinding codes)
    test/canary.test.ts (lines 374-425 declared canary harnesses)
    .planning/phases/11-managed-lifecycle-verification/11-RESEARCH.md (Evidence Map LIFE-02, Pitfalls 1-2)
    .planning/phases/11-managed-lifecycle-verification/evidence/life02-status.txt
    .planning/phases/11-managed-lifecycle-verification/evidence/suites-local.txt
    .planning/phases/11-managed-lifecycle-verification/evidence/README.md (findings template)
    .planning/REQUIREMENTS.md (Future Requirements: REL-02 / G-07-2)
  </read_first>
  <action>
    Per D-01, D-04, D-09, D-10 and D-11:
    1. Write PROBES/life02-discovery.mjs. Inside `withHostGuard`: pack once and `installPackedRelease` into a sandbox prefix (no seeding needed). Clone the repository with `git clone --no-hardlinks --quiet <repo root> <sandbox root>/project`. Record `git -C <repo root> status --porcelain` before. Define, inside this probe file only, a runner for the real-home commands: its env is the probe process env with ALPHA_AOS_STATE_DIR set to the sandbox state root (HOME and the harness roots stay real so the harness binaries find their own configuration, research Pitfall 2); it refuses to spawn unless ALPHA_AOS_STATE_DIR resolves inside the sandbox root, the argument list contains no `--apply`, and the command is one of `doctor --discovery <project>`, `doctor --canary <project> --no-spend` (each optionally with `--json`); it writes a step block through the transcript. Fingerprint `<project>/.planning` before and after each canary run.
    2. Steps: `doctor --discovery <project> --json`, `doctor --discovery <project>` (human), `doctor --canary <project> --no-spend --json`, `doctor --canary <project> --no-spend` (human). From the JSON, for each of claude, codex, antigravity, pi and hermes record `life02.discovery.<harness>`: HOLDS when its discovery row is COMPLETE; NOT-OBSERVED with detail `not executed (cost)` when the row states it would spend a model turn; VIOLATED when the row is unsupported or when the harness has no row at all (detail: the row state or `absent from discovery rows`). Record `life02.canary.<harness>.no-spend-execution`: HOLDS when at least one no-spend leg for that harness completed and launched that harness's own native surface; VIOLATED otherwise (detail: each leg's capability id and state). Record one OBSERVED check `life02.canary.spend-legs` listing every leg reported as would-spend, with detail `not executed (cost)`. Record `life02.canary.planning-unchanged` (the `.planning` fingerprint of the scratch project is unchanged: HOLDS or VIOLATED) and `life02.canary.vault-writes` OBSERVED naming the new paths under the scratch project (expected product behavior, not a mutation of the user's tree). After all steps record `life02.repo-unchanged` (the real repository porcelain output is identical: HOLDS or VIOLATED). Write EV/life02-discovery.txt.
    3. Write EV/LIFE-02-findings.md from the template. Contract text verbatim from .planning/milestones/v0.1.0-REQUIREMENTS.md:78. Clause rows: offline `status`; fast inspection; explicit `doctor` or canary mode; native discovery (one row per harness); meaningful read-only execution evidence (one row per harness); actionable coded findings. Each row cites its CHECK id(s), plus the named Phase 6 tests and CI job lines from EV/suites-local.txt and EV/ci-35818198049-lifelines.txt where they apply. Oracle audit column (D-04): state that `offline status strictly executes zero subprocesses` patches the default export without syncBuiltinESMExports and that `offline status completes in under 50ms benchmark` measures an empty state root in-process, and whether the Task 1 probe supplied the missing proof. Name the core clause (native discovery with meaningful read-only execution evidence) and give a provisional verdict per D-02. Gap candidates `LIFE-02/C<k>`: at minimum one per VIOLATED check group (for example per harness lacking discovery or no-spend execution evidence), each with a reproduction quoting the exact command and the observed JSON row. Under Observations note that Claude's cost-excluded legs belong to G-07-2 / REL-02 (D-10) and that the spending canary is deferred (CONTEXT Deferred Ideas).
  </action>
  <verify>
    <automated>npm run build && node .planning/phases/11-managed-lifecycle-verification/evidence/probes/life02-discovery.mjs && T=.planning/phases/11-managed-lifecycle-verification/evidence/life02-discovery.txt && grep -q '^host-guard: unchanged' "$T" && test "$(grep -cE '^CHECK life02\.discovery\.(claude|codex|antigravity|pi|hermes) ' "$T")" -eq 5 && test "$(grep -cE '^CHECK life02\.canary\.(claude|codex|antigravity|pi|hermes)\.no-spend-execution ' "$T")" -eq 5 && grep -qE '^CHECK life02\.repo-unchanged (HOLDS|VIOLATED) ' "$T"</automated>
    <fails_when>non-zero exit, no unchanged host guard line, not exactly five discovery checks or five no-spend execution checks, or the repository-unchanged check is missing</fails_when>
    <automated>F=.planning/phases/11-managed-lifecycle-verification/evidence/LIFE-02-findings.md && grep -qF 'offline `status` for fast inspection' "$F" && test "$(grep -cE 'life02\.(status|doctor|discovery|canary)\.' "$F")" -ge 10 && grep -q 'not executed (cost)' "$F" && grep -q '^## Provisional verdict' "$F" && grep -q '^## Gap candidates' "$F" && test -z "$(git status --porcelain -- src)"</automated>
    <fails_when>the contract text is not quoted, fewer than ten CHECK citations, no cost note, no provisional verdict heading, or src/ changed</fails_when>
  </verify>
  <acceptance_criteria>
    - EV/life02-discovery.txt shows both `doctor --discovery` and `doctor --canary --no-spend` steps with exit codes and aliased output, and no spending leg was launched (the canary step output names spend legs as not attempted).
    - Every VIOLATED check in both LIFE-02 transcripts appears as a `LIFE-02/C<k>` gap candidate or is explicitly argued in the findings as not failing the LIFE-02 text.
    - The real repository `git status --porcelain` output is identical before and after this task apart from files this plan adds under the evidence directory.
    - `git status --porcelain -- src` prints nothing.
  </acceptance_criteria>
  <done>LIFE-02 has per-harness discovery and no-spend execution evidence, cost-excluded legs are recorded honestly, and the findings file is ready for plan 11-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| discovery probe to real harness installations | `doctor --discovery` launches the real codex and pi binaries with the real HOME |
| canary to project tree | The handoff canary writes memory-vault files into the project path it is given |
| transcripts to public repository | Discovery output names harness versions and paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-06 | Tampering | life02-discovery.mjs real-home runner | high | mitigate | Runner refuses unless ALPHA_AOS_STATE_DIR is inside the sandbox, no `--apply` is present, and the command is on a two-entry allowlist; the project is a scratch clone; host guard plus repository porcelain comparison before and after |
| T-11-07 | Elevation of privilege / cost | doctor --canary | high | mitigate | `--no-spend` is mandatory in the allowlist; spend legs are recorded, never launched (D-11) |
| T-11-08 | Information disclosure | transcripts | medium | mitigate | CLI output already passes the redaction seam; probe aliases home, repo, tmp and sandbox paths; no env values printed |
| T-11-09 | Denial of service | `main` CI | medium | mitigate | The trap test is committed only after passing on Windows and in WSL (D-12) |
</threat_model>

<verification>
- `node scripts/run-tests.mjs --files dist/test/status-offline-verification.test.js` passes on this host and in WSL.
- Both LIFE-02 transcripts end with a `RESULT:` line and an unchanged host guard.
- Host-safe full suite: `npm run build && H=$(mktemp -d) && WH=$(cygpath -w "$H" 2>/dev/null || printf '%s' "$H") && env -u ALPHA_AOS_STATE_DIR -u CODEX_HOME -u CLAUDE_CONFIG_DIR -u HERMES_HOME -u PI_CODING_AGENT_DIR -u ANTIGRAVITY_CONFIG_DIR HOME="$WH" USERPROFILE="$WH" ALPHA_AOS_REQUIRE_UPSTREAM_MCP=1 node scripts/run-tests.mjs --files $(ls dist/test/*.test.js | grep -v capability-oracle) && test ! -e "$H/.alpha-aos"` exits 0.
- `git status --porcelain -- src` prints nothing.
</verification>

<success_criteria>
- Every LIFE-02 clause has an evidence row or a named gap candidate with a reproduction.
- The D-11 boundary holds: no model turn was spent, and the cost-excluded legs are named.
</success_criteria>

## Artifacts this phase produces

- `test/helpers/io-trap-preload.ts` (env input `ALPHA_AOS_IO_TRAP_LOG`; log vocabulary `child_process.<fn>`, `net.<fn>`, `tls.connect`, `dns.<fn>`, `http.<fn>`, `https.<fn>`, `fetch`).
- `test/status-offline-verification.test.ts` with test `offline status spawns no child process and opens no network connection through the CLI (LIFE-02)` (committed only if passing).
- Probes `PROBES/life02-status.mjs`, `PROBES/life02-discovery.mjs`; transcripts `EV/life02-status.txt`, `EV/life02-discovery.txt`; findings `EV/LIFE-02-findings.md`.
- Check ids: `life02.status.*`, `life02.doctor.*`, `life02.discovery.<harness>`, `life02.canary.<harness>.no-spend-execution`, `life02.canary.spend-legs`, `life02.canary.planning-unchanged`, `life02.canary.vault-writes`, `life02.repo-unchanged`.
- Gap candidate keys `LIFE-02/C<k>`.

<output>
Create `.planning/phases/11-managed-lifecycle-verification/11-02-SUMMARY.md` when done. Include the per-harness discovery and no-spend table, the trap control results, whether the trap test was committed, and the provisional LIFE-02 verdict.
</output>
