---
phase: 05-persistent-tree-off-preload-isolation
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/types.ts
  - src/adapters/isolation.ts
  - src/adapters/shims.ts
  - src/shim-dispatch.ts
  - test/shims.test.ts
autonomous: true
requirements: [OPTO-02, OPTO-05, OPTO-08]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Terminal CLI launches (codex, pi, hermes, agy) are intercepted via transparent CLI shims in ~/.alpha-aos/shims/ on PATH without requiring an alpha-aos run wrapper (D-05, OPTO-02)."
    - "For off trees, shims inject harness preload exclusion flags (Codex: --strict-config --ignore-user-config --ignore-rules; Pi: --no-skills --no-extensions --no-prompt-templates --no-themes; Hermes: chat --ignore-user-config --ignore-rules) and isolated config roots (~/.alpha-aos/isolated/trees/<tree-id>/) (D-05, D-07, OPTO-05)."
    - "Existing user login credentials and tokens are reused safely via filesystem hardlinks/symlinks without copying credential bytes into state (D-07)."
    - "Preload exclusion enforces fail-closed semantics: unprovable or unsupported surfaces (such as Antigravity GUI) exit immediately with exit code 2 and guidance to alpha-aos tree inspect (D-06, D-08, OPTO-08)."
    - "Upstream binary resolution excludes ~/.alpha-aos/shims from PATH search, preventing recursive fork bomb loops."
    - "Sub-millisecond shim dispatch executes with less than 0.1ms policy lookup overhead before spawning upstream."
  prohibitions:
    - statement: "MUST NOT permit recursive shim invocation; shims MUST resolve upstream binaries by filtering ~/.alpha-aos/shims from PATH."
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT copy credential bytes (auth.json, tokens) into isolated directories; credential reuse MUST use filesystem links or reference-based pointers (D-07)."
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT claim or simulate preload isolation for Antigravity GUI / desktop app launches; it MUST fail closed or report unsupported (D-06, D-08)."
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT invoke full heavy alpha-AOS CLI for fast-path shim execution; dispatch MUST remain sub-millisecond (<0.1ms policy lookup) via focused shim-dispatch.ts."
      category: performance
      status: resolved
      verification: judgment
  artifacts:
    - path: "src/adapters/shims.ts"
      provides: "Transparent CLI shim generator, recursion-free upstream binary locator, and zero-copy auth link builder"
      exports: ["generateShimScripts", "findUpstreamBinary", "linkAuthWithoutCopying", "prepareIsolatedTreeRoot", "SHIMMED_COMMANDS"]
    - path: "src/shim-dispatch.ts"
      provides: "Ultra-lightweight fast-path standalone CLI dispatcher (<0.1ms policy lookup overhead)"
    - path: "test/shims.test.ts"
      provides: "Automated test coverage for shim generation, loop-free binary resolution, preload flag injection, and fail-closed termination"
  key_links:
    - from: "src/shim-dispatch.ts"
      to: "src/core/tree-policy.ts"
      via: "resolveEffectivePolicy and loadTreeRegistry for sub-millisecond CWD policy resolution"
      pattern: "resolveEffectivePolicy"
    - from: "src/shim-dispatch.ts"
      to: "src/adapters/shims.ts"
      via: "findUpstreamBinary and prepareIsolatedTreeRoot for execution environment setup"
      pattern: "findUpstreamBinary|prepareIsolatedTreeRoot"
    - from: "src/shim-dispatch.ts"
      to: "src/adapters/isolation.ts"
      via: "getHarnessPreloadExclusion for flag and env injection"
      pattern: "getHarnessPreloadExclusion"
---

<objective>
Deliver transparent CLI shims in `~/.alpha-aos/shims/`, sub-millisecond dispatch (<0.1ms CWD lookup), harness preload exclusion injection, zero-copy authentication referencing, and fail-closed guardrails for Phase 5 (OPTO-02, OPTO-05, OPTO-08).

Purpose: Allow users to run ordinary harness CLI commands (`codex`, `pi`, `hermes`, `agy`) natively. In `off` trees, shims isolate configuration roots and inject flags to prevent global customization preloading. If isolation is unprovable (e.g. Antigravity GUI), dispatch halts immediately with exit code 2 (D-05, D-06, D-07, D-08).
Output: Updated `src/types.ts` and `src/adapters/isolation.ts`, new adapter `src/adapters/shims.ts`, standalone dispatch entrypoint `src/shim-dispatch.ts`, and test suite in `test/shims.test.ts`.
</objective>

<execution_context>
@C:/Users/alpha/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-persistent-tree-off-preload-isolation/05-CONTEXT.md
@.planning/phases/05-persistent-tree-off-preload-isolation/05-RESEARCH.md
@.planning/phases/05-persistent-tree-off-preload-isolation/05-DISCUSSION-LOG.md
@src/adapters/isolation.ts
@src/adapters/harnesses.ts
@src/core/tree-policy.ts
@src/core/process.ts
@src/core/paths.ts
</context>

<interfaces>
- `HarnessPreloadExclusion`:
  ```typescript
  export interface HarnessPreloadExclusion {
    readonly args: readonly string[];
    readonly env: Readonly<Record<string, string>>;
    readonly provable: boolean;
    readonly unsupportedReason?: string;
  }
  ```
- `ShimGenerationResult`:
  ```typescript
  export interface ShimGenerationResult {
    readonly shimsDir: string;
    readonly createdShims: readonly string[];
    readonly commands: readonly string[];
  }
  ```
- `UpstreamBinaryResolution`:
  ```typescript
  export interface UpstreamBinaryResolution {
    readonly executable: string;
    readonly isDirectScript: boolean;
    readonly scriptPath?: string;
  }
  ```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harness Preload Exclusion Vectors, Zero-Copy Auth Referencing, and Fail-Closed Support Logic</name>
  <read_first>
    - .planning/phases/05-persistent-tree-off-preload-isolation/05-CONTEXT.md — D-06: Antigravity GUI unsupported; D-07: Isolated roots & zero-copy auth; D-08: Fail-closed exit code 2.
    - .planning/phases/05-persistent-tree-off-preload-isolation/05-RESEARCH.md — Section "Harness Preload Exclusion Arguments Vector" & Pattern 3 "Reference-Based Credential Reuse".
    - src/adapters/isolation.ts — `createIsolationLaunchSpec`, `CANARY_MCP_ISOLATION`.
    - src/adapters/harnesses.ts — harness commands and discovery.
  </read_first>
  <files>src/types.ts, src/adapters/isolation.ts, src/adapters/shims.ts</files>
  <action>
1. In `src/types.ts`:
   - Export `HarnessPreloadExclusion`:
     ```typescript
     export interface HarnessPreloadExclusion {
       readonly args: readonly string[];
       readonly env: Readonly<Record<string, string>>;
       readonly provable: boolean;
       readonly unsupportedReason?: string;
     }
     ```

2. In `src/adapters/isolation.ts`:
   - Implement `getHarnessPreloadExclusion(harness: HarnessId, isolatedRoot: string, options?: { surface?: "cli" | "gui" }): HarnessPreloadExclusion`:
     - `codex`:
       `args: ["--strict-config", "--ignore-user-config", "--ignore-rules"]`
       `env: { CODEX_HOME: join(isolatedRoot, "codex") }`
       `provable: true`
     - `pi`:
       `args: ["--no-skills", "--no-extensions", "--no-prompt-templates", "--no-themes"]`
       `env: { PI_CODING_AGENT_DIR: join(isolatedRoot, "pi") }`
       `provable: true`
     - `hermes`:
       `args: ["chat", "--ignore-user-config", "--ignore-rules"]`
       `env: { HERMES_HOME: join(isolatedRoot, "hermes") }`
       `provable: true`
     - `antigravity`:
       If `options?.surface === "gui"`:
         `provable: false`
         `unsupportedReason: "Antigravity IDE/GUI desktop launches do not support pre-launch preload exclusion interception (D-06). Use CLI 'agy' or see 'alpha-aos tree inspect'."`
       Else (`surface === "cli"` or default for `agy`):
         `args: []`
         `env: { HOME: join(isolatedRoot, "antigravity", "home"), USERPROFILE: join(isolatedRoot, "antigravity", "home") }`
         `provable: true`
     - `claude`:
       `args: ["--setting-sources", "project,local", "--strict-mcp-config"]`
       `env: { CLAUDE_CONFIG_DIR: join(isolatedRoot, "claude") }`
       `provable: true` (compatibility residue under D-17)
     - Any other harness: `provable: false`, `unsupportedReason: "Unsupported or unprovable preload isolation for harness"`.

3. In `src/adapters/shims.ts`:
   - Implement `linkAuthWithoutCopying(sourceAuthFile: string, targetAuthFile: string): Promise<{ linked: boolean; method: "hardlink" | "symlink" | "none" }>`:
     - Check if `sourceAuthFile` exists; if not, return `{ linked: false, method: "none" }`.
     - Ensure target parent directory exists (`mkdir(dirname(targetAuthFile), { recursive: true })`).
     - If target already exists, remove it (`rm(targetAuthFile, { force: true })`).
     - Try `fs.link(sourceAuthFile, targetAuthFile)` (hardlink, preferred on Windows & POSIX without elevated privileges on same drive volume).
     - If hardlink fails, try `fs.symlink(sourceAuthFile, targetAuthFile, "file")`.
     - If both fail, return `{ linked: false, method: "none" }`.
     - Under NO circumstance copy file bytes or read secret tokens into memory.
   - Implement `prepareIsolatedTreeRoot(treeId: string, harness: HarnessId, stateRoot?: string): Promise<{ isolatedRoot: string; env: Record<string, string> }>`:
     - Path: `join(stateRoot ?? userStateRoot(), "isolated", "trees", treeId)`.
     - Create directory structure.
     - Call `getHarnessPreloadExclusion(harness, isolatedRoot)`.
     - If `harness === "codex"`: resolve global user `~/.codex/auth.json` (or from `CODEX_HOME`); link to `join(isolatedRoot, "codex", "auth.json")` via `linkAuthWithoutCopying`.
     - Return `{ isolatedRoot, env: exclusion.env }`.
  </action>
  <acceptance_criteria>
    - `getHarnessPreloadExclusion` returns exact preload flags for Codex, Pi, Hermes, and agy.
    - Antigravity GUI is reported as `provable: false` with explicit D-06 explanation.
    - `linkAuthWithoutCopying` reuses native auth via link without reading or copying credential bytes.
    - `prepareIsolatedTreeRoot` provisions isolated directories under `~/.alpha-aos/isolated/trees/<tree-id>/`.
  </acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build</automated>
    <fails_when>The command exits nonzero or TypeScript typecheck fails.</fails_when>
  </verify>
  <done>Preload exclusion flags and zero-copy auth referencing implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Transparent CLI Shim Generation and Recursion-Proof Upstream Binary Resolution</name>
  <read_first>
    - .planning/phases/05-persistent-tree-off-preload-isolation/05-CONTEXT.md — D-05: Transparent CLI shims on PATH for codex, pi, hermes, agy.
    - .planning/phases/05-persistent-tree-off-preload-isolation/05-RESEARCH.md — Pattern 2 "Recursion-Free Transparent CLI Shim Execution" & Pitfall 1 "CLI Shim Recursion Loop".
    - src/core/paths.ts — `userStateRoot`.
    - src/core/process.ts — executable resolution and environment helpers.
  </read_first>
  <files>src/adapters/shims.ts</files>
  <action>
In `src/adapters/shims.ts`:
1. Define `SHIMMED_COMMANDS = ["codex", "pi", "hermes", "agy"] as const`.

2. Implement `findUpstreamBinary(commandName: string, options?: { pathEnv?: string; shimsDir?: string }): UpstreamBinaryResolution | null`:
   - Obtain `pathEnv` (default `process.env.PATH ?? ""`).
   - Resolve `canonicalShimsDir = resolve(options?.shimsDir ?? join(userStateRoot(), "shims"))`.
   - Split `pathEnv` by `delimiter` (`:` or `;`), filtering out empty strings.
   - Filter entries: `resolve(entry) !== canonicalShimsDir` (CRITICAL: prevents infinite recursive fork loop).
   - On Windows, search extensions: `[".exe", ".cmd", ".bat", ""]`. On POSIX: `[""]`.
   - Iterate directories and test `existsSync(candidate)`.
   - If candidate is a `.cmd` or `.bat` wrapper on Windows, inspect if it is an npm/npx runner that wraps a `.js` CLI (e.g. `node_modules/@openai/codex/bin/codex.js`). If so, return `{ executable: process.execPath, isDirectScript: true, scriptPath: resolvedScript }` to preserve shell-free execution; otherwise return `{ executable: candidate, isDirectScript: false }`.
   - Return null if no upstream binary is found.

3. Implement `generateShimScripts(targetDir: string, options?: { nodePath?: string; dispatchScriptPath?: string }): Promise<ShimGenerationResult>`:
   - Ensure `targetDir` exists (`mkdir(targetDir, { recursive: true })`).
   - Node binary path: `options?.nodePath ?? process.execPath`.
   - Standalone dispatch script path: `options?.dispatchScriptPath ?? join(packageRoot(), "dist", "shim-dispatch.js")`.
   - For each command in `SHIMMED_COMMANDS`:
     - POSIX shell script (`targetDir/<cmd>`):
       ```sh
       #!/bin/sh
       exec "<nodePath>" "<dispatchScriptPath>" "<cmd>" "$@"
       ```
       Set permissions `0o755`.
     - Windows CMD script (`targetDir/<cmd>.cmd`):
       ```cmd
       @echo off
       "<nodePath>" "<dispatchScriptPath>" <cmd> %*
       ```
     - Windows PowerShell script (`targetDir/<cmd>.ps1`):
       ```ps1
       & "<nodePath>" "<dispatchScriptPath>" "<cmd>" @args
       ```
   - Return `{ shimsDir: targetDir, createdShims: [...], commands: SHIMMED_COMMANDS }`.

4. Implement `verifyShimsPrecedence(shimsDir: string, pathEnv?: string): { isFirstOnPath: boolean; position: number; issues: string[] }`:
   - Checks whether `shimsDir` is at the beginning of `PATH` or shadowed by another entry.
  </action>
  <acceptance_criteria>
    - `findUpstreamBinary` strictly excludes `shimsDir` from PATH search, preventing recursive fork bomb loops.
    - `generateShimScripts` creates executable shims for POSIX (`sh`) and Windows (`.cmd`, `.ps1`) for `codex`, `pi`, `hermes`, `agy`.
    - Shims invoke `shim-dispatch.js` passing the targeted command name and all arguments.
  </acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build</automated>
    <fails_when>The command exits nonzero or TypeScript typecheck fails.</fails_when>
  </verify>
  <done>Transparent shim generation and recursion-proof binary resolution implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Standalone Sub-Millisecond Shim Dispatcher and Automated Test Suite</name>
  <read_first>
    - .planning/phases/05-persistent-tree-off-preload-isolation/05-CONTEXT.md — D-05: Sub-millisecond dispatch (<1ms); D-08: Fail-closed exit code 2.
    - .planning/phases/05-persistent-tree-off-preload-isolation/05-RESEARCH.md — Section "Component Responsibilities Table" (shim-dispatch.ts) & Pitfalls 1, 6.
    - src/core/tree-policy.ts — `resolveEffectivePolicy`, `loadTreeRegistry`.
    - src/adapters/shims.ts — `findUpstreamBinary`, `prepareIsolatedTreeRoot`.
    - src/adapters/isolation.ts — `getHarnessPreloadExclusion`.
  </read_first>
  <files>src/shim-dispatch.ts, test/shims.test.ts</files>
  <action>
1. Create `src/shim-dispatch.ts`:
   - Purpose: Fast-path CLI dispatcher invoked by shims. Minimal imports to boot in <80ms and perform CWD policy resolution in <0.1ms.
   - Parse `command = process.argv[2]` and `rawArgs = process.argv.slice(3)`.
   - If `command` is missing, exit 1 with usage.
   - Map `command` to `HarnessId` (`"codex" -> "codex"`, `"pi" -> "pi"`, `"hermes" -> "hermes"`, `"agy" -> "antigravity"`).
   - Resolve CWD policy:
     ```typescript
     const startLookup = performance.now();
     const registry = await loadTreeRegistry();
     const policy = await resolveEffectivePolicy(process.cwd(), registry.trees);
     const lookupMs = performance.now() - startLookup;
     ```
   - Resolve upstream binary: `findUpstreamBinary(command)`. If null, print `alpha-aos: Upstream binary '${command}' not found on PATH` to stderr and exit 127.
   - If `policy.effectiveMode === "managed"`:
     - Spawn upstream binary with original `rawArgs`, inheriting `stdio: "inherit"`, `shell: false`.
     - Exit with child's exit code upon completion.
   - If `policy.effectiveMode === "off"`:
     - Check provable support: call `getHarnessPreloadExclusion(harness, isolatedRoot, { surface: "cli" })`.
     - If `!exclusion.provable`:
       - Write error to stderr: `alpha-aos: Preload isolation cannot be proven on this surface (${exclusion.unsupportedReason ?? "unsupported"}). Refusing launch. See 'alpha-aos tree inspect'.\n`.
       - Exit with status code 2 (Fail-Closed, D-08, OPTO-08).
     - Prepare isolated tree root via `prepareIsolatedTreeRoot(policy.entry?.id ?? "default", harness)`.
     - Construct isolated arguments: `[...exclusion.args, ...rawArgs]`.
     - Spawn upstream binary with isolated env and arguments, `stdio: "inherit"`, `shell: false`.
     - Exit with child's exit code upon completion.

2. Create `test/shims.test.ts`:
   - Test 1: `generateShimScripts` creates POSIX and Windows scripts for `codex`, `pi`, `hermes`, `agy` with correct shebang and argument passing.
   - Test 2: `findUpstreamBinary` finds mock binary in system directory while ignoring mock binary in `shimsDir` (no recursion).
   - Test 3: Zero-copy auth referencing: `linkAuthWithoutCopying` links source to target using hardlink/symlink; verify file contents match and updates reflect without byte copying.
   - Test 4: `getHarnessPreloadExclusion` returns correct flags:
     - Codex has `--strict-config`, `--ignore-user-config`, `--ignore-rules`.
     - Pi has `--no-skills`, `--no-extensions`, `--no-prompt-templates`, `--no-themes`.
     - Hermes has `chat`, `--ignore-user-config`, `--ignore-rules`.
     - Antigravity GUI returns `provable: false` with exit code 2 requirement.
   - Test 5: Shim dispatch performance: measure `resolveEffectivePolicy` benchmark on mock registry; verify execution completes in <0.5ms (target <0.1ms).
   - Test 6: Fail-closed dispatch: simulated launch of unprovable surface halts immediately with exit code 2 and outputs guidance to `tree inspect`.
  </action>
  <acceptance_criteria>
    - `src/shim-dispatch.ts` handles managed passthrough and off isolation seamlessly.
    - Unsupported/unprovable surfaces fail closed with exit code 2.
    - Upstream binary lookup never re-invokes shims.
    - All tests in `test/shims.test.ts` pass with 0 failures.
  </acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node scripts/run-tests.mjs --files dist/test/shims.test.js</automated>
    <fails_when>The command exits nonzero, TypeScript typecheck fails, or any test in dist/test/shims.test.js fails.</fails_when>
  </verify>
  <done>Fast-path shim dispatcher, preload exclusion flags, and test suite verified.</done>
</task>

</tasks>
