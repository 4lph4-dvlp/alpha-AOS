---
phase: 01-safe-operation-boundary
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/preview.test.ts
  - test/redaction.test.ts
  - test/validation.test.ts
autonomous: true
requirements: [SAFE-01, SAFE-04, SAFE-05]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Spec-less edge assumption (SAFE-01, resolved conservatively from the phase goal): every preview surface is judged by before/after checkout, package, configuration, state, and harness-resource digests, including an interrupted preview invocation."
    - "Spec-less edge assumption (SAFE-04, resolved conservatively from D-12 through D-15): absence of secrets is asserted over human output, JSON, errors, persisted evidence, and diagnostic bundle previews rather than one formatter alone."
    - "Spec-less edge assumption (SAFE-05, resolved conservatively from D-08 through D-11): syntax ambiguity, version routing, closed-schema validation, extension inertness, and deterministic bounded issues are separate observable cases."
  artifacts:
    - path: test/preview.test.ts
      provides: "Zero-mutation CLI and wrapper digest contract"
    - path: test/redaction.test.ts
      provides: "Cross-surface secret and private-path corpus"
    - path: test/validation.test.ts
      provides: "Managed-document rejection and extension-inertness matrix"
  key_links:
    - from: test/preview.test.ts
      to: scripts/install.ps1
      via: "temporary checkout digest before and after preview"
      pattern: "install.*preview|tree.*digest"
    - from: test/redaction.test.ts
      to: src/cli.ts
      via: "spawned human/JSON/error surfaces"
      pattern: "--json|stderr|support"
    - from: test/validation.test.ts
      to: src/core/catalog.ts
      via: "table-driven malformed and versioned documents"
      pattern: "duplicate|schemaVersion|extensions"
  prohibitions:
    - statement: "A preview MUST NOT bootstrap, pull, install, build, link, create state, or touch harness configuration before it proves that a runnable artifact already exists."
      requirement_id: SAFE-01
      category: safety
      status: unverified
      flagged: true
    - statement: "A diagnostic preview MUST NOT imply that a local support bundle was uploaded or shared; the sharing decision belongs exclusively to the user."
      requirement_id: SAFE-04
      category: transparency
      status: unverified
      flagged: true
---

<objective>
Wave 0에서 preview, redaction, strict validation의 실패 방향을 현재 CLI 표면에 고정한다.

Purpose: 안전 구현이 자기 자신을 green으로 만드는 느슨한 검사로 축소되지 않게 한다.
Output: 실제 CLI/파일시스템을 사용하는 세 개의 회귀 테스트 파일.
</objective>

<execution_context>
@C:/Users/alpha/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-safe-operation-boundary/01-CONTEXT.md
@.planning/phases/01-safe-operation-boundary/01-RESEARCH.md
@.planning/phases/01-safe-operation-boundary/01-VALIDATION.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Preview 무변이 tracer 계약 작성</name>
  <files>test/preview.test.ts</files>
  <read_first>
    test/transaction.test.ts
    test/install.test.ts
    src/cli.ts
    scripts/install.ps1
    scripts/install.sh
    scripts/update.ps1
    scripts/update.sh
  </read_first>
  <behavior>
    - 설치·업데이트 CLI와 PowerShell/POSIX wrapper의 apply 없는 호출 전후에 checkout, package/global-link sentinel, native config, ALPHA_AOS_STATE_DIR, harness root의 byte digest가 동일하다.
    - build artifact가 없거나 source digest와 맞지 않으면 stable prerequisite/refusal을 출력하고 어떤 digest도 바꾸지 않는다.
    - preview가 중단되거나 동시에 두 번 실행돼도 state root나 lock을 만들지 않는다.
  </behavior>
  <action>D-01, D-02와 SAFE-01을 검증하는 end-to-end tracer를 작성한다. OS temp 아래 격리된 checkout과 fake HOME/config/state/package roots를 만들고 실제 `dist/src/cli.js` 및 네 wrapper를 child process로 호출한다. 각 표면의 전후 SHA-256 tree digest, outside sentinel, 종료 코드, stable refusal 문구를 기록한다. wrapper가 지원되지 않는 host에서는 pass로 세지 말고 명시적 not-run evidence를 assertion에 남긴다. 테스트는 현재 동작에서 mutation을 관찰해 RED가 되도록 하되 source checkout 밖의 실제 사용자 root는 절대 사용하지 않는다.</action>
  <acceptance_criteria>`test/preview.test.ts`가 모든 mutating CLI preview와 install/update 두 shell family를 열거하고, 각 case가 다섯 종류의 mutation surface digest 동일성과 preview 중단/병렬 호출을 관찰한다.</acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
    <fails_when>non-zero exit, or TypeScript reports that test/preview.test.ts cannot be compiled</fails_when>
  </verify>
  <reversibility rating="reversible">테스트 전용 변경이며 production bytes를 바꾸지 않는다.</reversibility>
  <done>SAFE-01의 zero-mutation 계약이 한 end-to-end 파일에 실행 가능한 assertions로 존재한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 모든 observable surface의 redaction corpus 작성</name>
  <files>test/redaction.test.ts</files>
  <read_first>
    test/transaction.test.ts
    test/doctor.test.ts
    src/core/paths.ts
    src/core/process.ts
    src/core/doctor.ts
    src/cli.ts
  </read_first>
  <behavior>
    - exact credential, secret-bearing property/name, URL userinfo/token parameter, PEM, JWT, API-key가 stable typed placeholder로 치환된다.
    - 1-byte chunk 경계, Error/object/array/cycle/depth/size 경계에서도 원문 secret이 human, JSON, stderr, journal metadata, support preview에 없다.
    - home, temp, project root는 segment-aware alias가 되고 repository-relative suffix와 environment-variable name은 남는다.
  </behavior>
  <action>D-12, D-13, D-14, D-15와 SAFE-04의 adversarial corpus를 작성한다. 고정 sentinel 값을 CLI human/JSON, thrown Error, process excerpt, diagnostic finding, journal metadata, snapshot inspection metadata, support-bundle dry-run에 주입하고 전체 captured bytes에서 sentinel 및 secret-bearing URL 원문이 0회인지 검사한다. snapshot content 자체는 출력 검사를 하지 않고 opaque bytes의 hash/size/permission metadata만 노출되는지 검사한다.</action>
  <acceptance_criteria>각 secret family와 path alias가 positive/negative control을 가지며, chunk 분할·circular object·oversize output case가 원문 미노출과 bounded output을 함께 검증한다.</acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
    <fails_when>non-zero exit, or TypeScript reports that test/redaction.test.ts cannot be compiled</fails_when>
  </verify>
  <reversibility rating="reversible">테스트 corpus는 제거 가능하고 외부 상태를 변경하지 않는다.</reversibility>
  <done>SAFE-04의 모든 명시 표면이 동일한 sentinel corpus로 검증된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: strict document validation 및 migration matrix 작성</name>
  <files>test/validation.test.ts</files>
  <read_first>
    test/catalog.test.ts
    test/project.test.ts
    test/mcp.test.ts
    src/core/catalog.ts
    src/core/project.ts
    src/core/mcp.ts
    schemas/project-stack.schema.json
  </read_first>
  <behavior>
    - catalog, lock, manifest, evidence, receipt, journal, writer lock, install state, alpha-AOS-owned native subtree가 malformed/duplicate/ambiguous/unknown core field에서 mutation 전에 거부된다.
    - supported older version은 read-only parse와 migration plan을 반환하고 unknown newer version은 stable refusal code를 반환한다.
    - unknown namespaced extension은 보존·표시되지만 operation plan과 apply input에는 영향을 주지 않는다.
    - validation issues는 path/code/expected 순으로 안정 정렬되고 상한을 넘지 않으며 actual은 redacted shape summary다.
  </behavior>
  <action>D-08, D-09, D-10, D-11과 SAFE-05의 table-driven matrix를 작성한다. JSON duplicate key, YAML multi-document/duplicate/alias bomb, TOML duplicate/dotted/quoted key ambiguity, closed core objects, malformed namespace, old/current/newer schema version을 포함한다. 동일 invalid input을 두 번 검증한 결과의 deep equality와 issue cap을 검사하고, plan/apply spy가 호출되지 않았음을 함께 확인한다.</action>
  <acceptance_criteria>모든 managed document kind에 current-valid, malformed, ambiguous, closed-world, unknown-extension, supported-old, unknown-newer case가 있고 invalid case마다 stable code와 zero-mutation assertion이 있다.</acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
    <fails_when>non-zero exit, or TypeScript reports that test/validation.test.ts cannot be compiled</fails_when>
  </verify>
  <reversibility rating="reversible">검증 계약만 추가하며 runtime 문서를 변경하지 않는다.</reversibility>
  <done>SAFE-05의 parser→version→schema→domain 순서와 extension inertness가 executable contract로 고정된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/wrapper→filesystem | preview라는 신뢰 주장과 실제 checkout/config/state bytes 사이 경계 |
| untrusted diagnostic input→observable output | credential/path/process data가 human·JSON·evidence로 이동하는 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | preview entry points | high | mitigate | 전후 tree digest와 outside sentinel을 actual subprocess invocation마다 검증 |
| T-01-02 | Information Disclosure | output/evidence surfaces | high | mitigate | cross-surface secret corpus와 whole-output negative assertions |
| T-01-03 | Denial of Service | validation issue generation | medium | mitigate | deterministic cap/order assertions와 alias/duplicate stress fixtures |
</threat_model>

## Artifacts this phase produces

| Kind | Artifact | Contract |
|------|----------|----------|
| New file | `test/preview.test.ts` | CLI/wrapper zero-mutation digest matrix |
| New file | `test/redaction.test.ts` | recursive redaction and alias corpus |
| New file | `test/validation.test.ts` | strict managed-document matrix |

## Probe coverage notes

- Canonical path traversal, command injection, and generic secret-logging concerns are recorded in the STRIDE register and are not duplicated as spec-less prohibitions.
- SAFE-01, SAFE-04, SAFE-05의 unclassified probe rows are conservatively resolved above from the roadmap and D-01/D-02/D-08~D-15; no row is dismissed.

<verification>
Wave 0 종료 시 `npm run check`가 성공하고 세 test source가 컴파일된다. 테스트의 production assertions는 후속 계획의 focused command가 green으로 만든다.
</verification>

<success_criteria>
- preview matrix가 모든 current mutating entry point와 네 wrapper surface를 열거한다.
- redaction corpus가 human/JSON/error/persistence/support surface를 모두 포함한다.
- validation matrix가 모든 Phase 1 managed document kind와 version/extension edge를 포함한다.
</success_criteria>

<output>
Create `.planning/phases/01-safe-operation-boundary/01-01-SUMMARY.md` when done.
</output>
