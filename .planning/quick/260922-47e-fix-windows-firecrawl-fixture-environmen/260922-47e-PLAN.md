---
phase: quick
plan: 260922-47e
type: execute
autonomous: true
---

# Fix Windows Firecrawl fixture environment propagation

<objective>
Ensure the bounded Node/npm environment used by MCP fixtures preserves the Windows launcher variables required by nested npm package bins, and add regression coverage at both the policy and `discoverTools()` boundaries.

Purpose: `alpha-aos install --apply` must not fail its Firecrawl canary because the outer fixture process removed `PATHEXT`/`COMSPEC` before the proxy could pass them to upstream npx.

Output: A minimal environment-policy correction, regression tests, a rebuilt linked CLI, a successful live Firecrawl fixture, and a completed Codex/Pi/Hermes managed install.
</objective>

<constraints>
- Preserve the explicit environment allowlist; do not inherit the ambient environment wholesale.
- Pass only the two non-secret Windows launcher names already approved by the process and MCP proxy policies.
- Keep behavior portable: optional names remain absent on hosts whose source environment does not define them.
- Do not modify catalog locks, package dependencies, or MCP tool allowlists.
- Preserve the pre-existing untracked `nul` entry.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Repair the runtime allowlist and prove the fixture boundary</name>
  <files>src/core/install.ts, test/process.test.ts, test/protocol-session.test.ts</files>
  <action>
Add `PATHEXT` and `COMSPEC` to `NODE_RUNTIME_ENVIRONMENT_NAMES` next to `PATH`, matching the existing `commandProbeEnvironment` and `upstreamEnvironmentPolicy` contracts. Extend the node-child environment unit test with explicit source values and assertions for both names. Make the fake MCP server used by the migrated fixture probe advertise its expected tool only when a Windows child received both launcher variables; keep non-Windows behavior unchanged. This makes the production `discoverTools()` call path fail if the outer fixture boundary drops either variable again.
  </action>
  <verify>
    <automated>npm run check &amp;&amp; npm run build &amp;&amp; node --test dist/test/process.test.js dist/test/protocol-session.test.js &amp;&amp; npm test</automated>
    <live>alpha-aos fixture mcp firecrawl codex --apply --json, followed by alpha-aos install --target codex,pi,hermes --apply --json and a final dry-run/status check</live>
  </verify>
  <done>The policy and fixture tests carry both launcher variables, the full suite passes, Firecrawl exposes the four reviewed tools, and Hermes reaches current state without bypassing the fixture gate.</done>
</task>

</tasks>

<verification>
Run `git diff --check`, TypeScript checking, the focused process/protocol tests, and the repository full suite. Confirm the live Firecrawl fixture returns exactly the four allowed tool names. Apply the reviewed managed install for Codex, Pi, and Hermes, then confirm all planned steps are current and offline status reports a free writer with no incomplete or corrupt journals.
</verification>

<success_criteria>
- `NODE_RUNTIME_ENVIRONMENT_NAMES` declares `PATHEXT` and `COMSPEC` without widening to ambient inheritance.
- Unit and production-boundary regression tests fail if either Windows launcher variable is removed.
- Check, build, focused tests, and full suite pass.
- The live Firecrawl MCP fixture completes initialize and tools/list with four allowed tools.
- Codex, Pi, and Hermes managed install finishes and a subsequent dry-run reports every step current.
</success_criteria>
