---
phase: 07-cross-platform-release-proof
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/core/support-matrix.ts
  - src/cli.ts
  - src/format.ts
  - docs/SUPPORT_MATRIX.md
  - test/support-matrix.test.ts
  - test/release-controls.test.ts
  - docs/RELEASE_CONTROLS.md
autonomous: true
requirements: [REL-02, REL-03]

must_haves:
  truths:
    - "The official versioned support matrix is defined as structured immutable data in src/core/support-matrix.ts and exposed via alpha-aos doctor --matrix and docs/SUPPORT_MATRIX.md (REL-02, D-05)."
    - "Every harness surface is classified into a strict 4-tier mutually exclusive taxonomy: PROVEN, RESIDUE, UNVERIFIED, or UNSUPPORTED, with false success states strictly prohibited (REL-02, D-08)."
    - "Active claimed release targets (Codex, Antigravity, Pi, Hermes) evaluate against inspectable real-host canary receipts, while Claude Code remains locked to RESIDUE (REL-02, D-06, D-08)."
    - "Paired positive and negative controls in test/release-controls.test.ts prove behavior across 4 domains: optional capability invocation, project pack scope, mandatory gate blocking, and tree-off opt-out (REL-03, D-07)."
    - "docs/RELEASE_CONTROLS.md is produced as an inspectable evidence ledger verifying paired acceptance criteria using deterministic timestamps, with disk writes guarded to prevent dirtying the git working tree during normal test runs (REL-03, D-07, D-16)."
    - "Automated drift tests guarantee that docs/SUPPORT_MATRIX.md matches the structured matrix code generator byte-for-byte (REL-02, D-05)."
  prohibitions:
    - statement: "MUST NOT report an unverified or unprobed harness as PROVEN; without an inspectable receipt on disk it must remain UNVERIFIED or UNSUPPORTED (REL-02, D-08)."
      category: integrity
      status: resolved
      verification: automated
    - statement: "MUST NOT promote Claude Code to an active or gated harness; it must strictly remain classified as RESIDUE (REL-02, D-08)."
      category: boundary
      status: resolved
      verification: automated
    - statement: "MUST NOT assert positive capability behavior without an accompanying negative exclusion control in test/release-controls.test.ts (REL-03, D-07)."
      category: verification
      status: resolved
      verification: automated
    - statement: "MUST NOT dirty-mark the git working tree with live timestamps during normal test runs; docs/RELEASE_CONTROLS.md disk writes must be guarded behind UPDATE_RELEASE_CONTROLS=1 and use deterministic timestamps (REL-03, D-07, D-16)."
      category: safety
      status: resolved
      verification: automated
  artifacts:
    - path: "src/core/support-matrix.ts"
      provides: "Structured support matrix ledger, 4-tier taxonomy evaluator, and markdown generator"
      exports: ["BASE_SUPPORT_MATRIX", "SupportTier", "SupportMatrixEntry", "evaluateMatrixCell", "evaluateSupportMatrix", "renderSupportMatrixMarkdown"]
    - path: "docs/SUPPORT_MATRIX.md"
      provides: "Public markdown specification of alpha-AOS v0.1.0 support matrix"
    - path: "docs/RELEASE_CONTROLS.md"
      provides: "Public acceptance controls verification evidence ledger"
    - path: "test/support-matrix.test.ts"
      provides: "Automated verification for support matrix taxonomy, canary overlay, and doc sync"
    - path: "test/release-controls.test.ts"
      provides: "Automated verification for paired positive and negative acceptance controls"
---

<objective>
Implement the versioned support matrix ledger with 4-tier taxonomy diagnostics and automated paired positive/negative acceptance controls (REL-02, REL-03, D-05, D-06, D-07, D-08).

Purpose: Guarantee transparent, honest reporting of platform and harness capabilities where active targets (Codex, Antigravity, Pi, Hermes) are proven by real-host receipts, Claude Code remains compatibility residue, and all critical control surfaces are validated through paired positive and negative tests.
Output: `src/core/support-matrix.ts`, updated `src/cli.ts` and `src/format.ts`, `docs/SUPPORT_MATRIX.md`, `docs/RELEASE_CONTROLS.md`, `test/support-matrix.test.ts`, and `test/release-controls.test.ts`.
</objective>

<execution_context>
Execution via GSD workflow adhering to anti-shallow deep work rules.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-cross-platform-release-proof/07-CONTEXT.md
@.planning/phases/07-cross-platform-release-proof/07-RESEARCH.md
@.planning/phases/07-cross-platform-release-proof/07-PATTERNS.md
@.planning/phases/07-cross-platform-release-proof/07-VALIDATION.md
@src/core/doctor.ts
@src/core/capability-ledger.ts
@src/core/canary.ts
@src/core/tree-policy.ts
@src/core/gate-lifecycle.ts
@src/core/mcp-proxy.ts
@src/cli.ts
@src/format.ts
</context>

<interfaces>
```typescript
// Proposed exports from src/core/support-matrix.ts
export type SupportTier = "PROVEN" | "RESIDUE" | "UNVERIFIED" | "UNSUPPORTED";

export interface SupportMatrixEntry {
  readonly harnessId: string;
  readonly surface: string;
  readonly platform: "win32" | "darwin" | "linux" | "all";
  readonly baselineTier: SupportTier;
  readonly notes: string;
}

export interface EvaluatedMatrixCell {
  readonly entry: SupportMatrixEntry;
  readonly tier: SupportTier;
  readonly evidenceSummary: string;
}

export interface SupportMatrixReport {
  readonly generatedAt: string;
  readonly platform: NodeJS.Platform;
  readonly cells: readonly EvaluatedMatrixCell[];
}

export function evaluateMatrixCell(entry: SupportMatrixEntry, inventory: Inventory, ledger: CapabilityLedger): EvaluatedMatrixCell;
export function evaluateSupportMatrix(inventory: Inventory, ledger: CapabilityLedger): SupportMatrixReport;
export function renderSupportMatrixMarkdown(report: SupportMatrixReport): string;
```
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Versioned Support Matrix Ledger, 4-Tier Taxonomy, Canary Overlay & CLI Diagnostics (REL-02, D-05, D-06, D-08)</name>
  <read_first>
    .planning/phases/07-cross-platform-release-proof/07-CONTEXT.md
    .planning/phases/07-cross-platform-release-proof/07-RESEARCH.md
    .planning/phases/07-cross-platform-release-proof/07-PATTERNS.md
    src/core/doctor.ts
    src/core/capability-ledger.ts
    src/core/inventory.ts
    src/cli.ts
    src/format.ts
  </read_first>
  <files>src/core/support-matrix.ts, src/cli.ts, src/format.ts, docs/SUPPORT_MATRIX.md, test/support-matrix.test.ts</files>
  <behavior>
    - Defines BASE_SUPPORT_MATRIX structured ledger covering active harnesses (Codex, Antigravity, Pi, Hermes) and compatibility residue (Claude Code) across all key surfaces (GSD Core, Unified Memory, Context7, Project Packs, Mandatory Gates, Tree-Off, Managed Lifecycle).
    - Classifies capabilities into mutually exclusive 4-tier taxonomy: PROVEN (verifiable canary receipt on disk), RESIDUE (Claude Code compatibility only), UNVERIFIED (harness detected but canary not run), and UNSUPPORTED (incompatible OS/version).
    - Overlays live host inventory and capability ledger receipts without mutating disk state or executing expensive model turns during matrix inspection.
    - CLI supports "alpha-aos doctor --matrix" rendering colored ANSI table (green for PROVEN, yellow for RESIDUE, cyan for UNVERIFIED, red for UNSUPPORTED) and structured "--json" envelope.
    - Markdown generator renders canonical docs/SUPPORT_MATRIX.md; unit tests enforce zero drift between generated markdown and committed document.
  </behavior>
  <action>
Implement the support matrix ledger and diagnostics adhering to D-05, D-06, D-08, and REL-02:

1. Create `src/core/support-matrix.ts`:
   - Define type `SupportTier` ("PROVEN" | "RESIDUE" | "UNVERIFIED" | "UNSUPPORTED").
   - Define interface `SupportMatrixEntry` containing `harnessId`, `surface`, `platform`, `baselineTier`, and `notes`.
   - Export constant `BASE_SUPPORT_MATRIX` array detailing the official v0.1.0 matrix:
     - Codex: GSD Core (PROVEN), Context7 (PROVEN), Unified Memory (PROVEN).
     - Antigravity: GSD Core (PROVEN), Unified Memory (PROVEN), GUI Desktop Preload (UNSUPPORTED).
     - Pi: GSD Core (PROVEN).
     - Hermes: Worker Authority (PROVEN), GSD State Controller (UNSUPPORTED).
     - Claude Code: All surfaces (RESIDUE).
   - Implement `evaluateMatrixCell(entry, inventory, ledger)`:
     - If entry.harnessId === "claude", return tier "RESIDUE".
     - If entry.baselineTier === "UNSUPPORTED", return tier "UNSUPPORTED".
     - If harness is not detected in inventory, return tier "UNVERIFIED" with reason "Harness executable not detected".
     - Look for matching verified canary receipts in `ledger.proofs`. If an outcome === "verified" proof exists, return "PROVEN" with proof timestamp and receipt reference. Otherwise return "UNVERIFIED" with reason "Canary unrun on host".
   - Implement `evaluateSupportMatrix(inventory, ledger)` returning `SupportMatrixReport`.
   - Implement `renderSupportMatrixMarkdown(report)` producing formatted GitHub Flavored Markdown table with status badges and explanatory footnotes.

2. Update `src/format.ts`:
   - Implement `formatSupportMatrixTable(report: SupportMatrixReport): string`:
     - Format a responsive ASCII/Unicode table displaying columns: Harness, Surface, Platform, Status, Evidence / Notes.
     - Apply ANSI color styling (unless NO_COLOR or non-tty): PROVEN green, RESIDUE yellow, UNVERIFIED cyan, UNSUPPORTED red.

3. Update `src/cli.ts`:
   - In the `command === "doctor"` handler, add parsing for the `--matrix` flag.
   - When `--matrix` is provided:
     - Collect inventory and read capability ledger from user state root.
     - Call `evaluateSupportMatrix(inventory, ledger)`.
     - If `--json` is specified, emit the JSON report envelope and exit 0.
     - Otherwise, print `formatSupportMatrixTable(report)` and exit 0.

4. Generate `docs/SUPPORT_MATRIX.md`:
   - Render the canonical baseline markdown using `renderSupportMatrixMarkdown`.

5. Create `test/support-matrix.test.ts`:
   - Test 1 (Taxonomy Classification): Verify correct tier assignment for proven, unverified, residue, and unsupported cells.
   - Test 2 (Receipt Overlay): Inject synthetic verified canary receipts; assert status transitions from UNVERIFIED to PROVEN.
   - Test 3 (Claude Residue Invariant): Assert that Claude Code is strictly classified as RESIDUE under all conditions.
   - Test 4 (CLI Output Formatting): Execute CLI with `--matrix` and `--matrix --json`; assert valid table output and valid JSON schema.
   - Test 5 (Zero Documentation Drift): Assert byte-for-byte equality between `renderSupportMatrixMarkdown()` output and `docs/SUPPORT_MATRIX.md`.
  </action>
  <acceptance_criteria>
    - `src/core/support-matrix.ts` defines structured support matrix and evaluates 4-tier taxonomy (REL-02, D-05, D-08).
    - `alpha-aos doctor --matrix` and `alpha-aos doctor --matrix --json` provide terminal table and structured envelope (REL-02, D-05).
    - Claude Code is strictly classified as RESIDUE; active harnesses evaluate via canary receipts (REL-02, D-06, D-08).
    - `docs/SUPPORT_MATRIX.md` has zero drift against structured matrix generator (REL-02, D-05).
    - `node scripts/run-tests.mjs --files dist/test/support-matrix.test.js` passes completely.
  </acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node scripts/run-tests.mjs --files dist/test/support-matrix.test.js</automated>
    <fails_when>non-zero exit code, or documentation drift detected</fails_when>
  </verify>
  <done>Support matrix ledger, 4-tier taxonomy evaluation, CLI doctor integration, and doc synchronization are verified and passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Paired Positive/Negative Acceptance Controls Suite & Evidence Ledger (REL-03, D-07)</name>
  <read_first>
    .planning/phases/07-cross-platform-release-proof/07-CONTEXT.md
    .planning/phases/07-cross-platform-release-proof/07-RESEARCH.md
    .planning/phases/07-cross-platform-release-proof/07-PATTERNS.md
    test/gate-lifecycle.test.ts
    test/tree-policy.test.ts
    test/mcp-proxy.test.ts
    test/helpers/pack-fixtures.ts
    src/core/gate-lifecycle.ts
    src/core/tree-policy.ts
    src/core/mcp-proxy.ts
  </read_first>
  <files>test/release-controls.test.ts, docs/RELEASE_CONTROLS.md</files>
  <behavior>
    - Evaluates 4 explicit Positive and Negative control pairs:
      1. Optional Capability Invocation: Positive (documentation intent triggers Context7 MCP calls) vs Negative (general task does not invoke Context7).
      2. Project Pack Scope: Positive (tools discoverable inside canonical project root) vs Negative (tools undiscoverable outside project root).
      3. Mandatory Gate Blocking: Positive (auth/migration diff blocks lifecycle progression until gate receipt is provided) vs Negative (low-risk doc diff passes silently with 0 blocking).
      4. Tree-Off Opt-Out: Positive (managed directory loads alpha-AOS customizations) vs Negative (tree marked off completely suppresses customizations, returning vanilla harness baseline).
    - Generates inspectable acceptance report for docs/RELEASE_CONTROLS.md documenting test evidence, cryptographic receipts, and deterministic verification timestamps.
    - Prohibits one-sided assertions; every positive control must have an accompanying negative counterpart.
    - Guards disk writes to docs/RELEASE_CONTROLS.md behind UPDATE_RELEASE_CONTROLS=1; normal test runs verify in-memory and assert zero drift against the committed document without dirtying the git working tree.
  </behavior>
  <action>
Implement `test/release-controls.test.ts` and evidence generator adhering to D-07, D-16, and REL-03:

1. In `test/release-controls.test.ts`:
   - Structure tests around interface `ControlPairResult` with fields: `domain`, `positive` (`name`, `passed`, `receipt`), `negative` (`name`, `passed`, `receipt`).
   - Implement Pair 1 (Optional Capability Invocation):
     - Positive: Invoke canary simulator with library documentation lookup prompt ("lookup react hooks api"). Verify Context7 MCP proxy intercepts request and calls `resolve-library-id` and `query-docs`. Record verified observation receipt.
     - Negative: Invoke canary simulator with general algorithmic task ("implement binary search"). Verify Context7 MCP tools are never invoked (call count === 0). Record verified non-invocation receipt.
   - Implement Pair 2 (Project Pack Scope):
     - Positive: Set up repository matching Node/TypeScript Web API pack evidence (`express` in package.json, `tsconfig.json`). Run project plan; assert pack is selected and project-scoped tools are discoverable.
     - Negative: In a sibling directory outside the canonical project root, run project plan or tool discovery; assert pack tools are completely undiscoverable and not loaded.
   - Implement Pair 3 (Mandatory Gate Blocking):
     - Positive: Create Git commit modifying authentication boundary (`src/auth/jwt.ts`). Run `evaluateLifecycleGates` at `execute:post`; assert execution is blocked (`blocked: true`) and `blockingObligations` includes `security-review`.
     - Negative: Create Git commit modifying non-risk documentation (`README.md`). Run `evaluateLifecycleGates`; assert execution is not blocked (`blocked: false`) and `silentPass` is true.
   - Implement Pair 4 (Tree-Off Opt-Out):
     - Positive: Directory without tree-off registration loads alpha-AOS configurations and MCP servers.
     - Negative: Register directory as `off` in tree registry (`trees.json`). Resolve launch policy; assert all global customizations, MCP servers, and skills are suppressed, returning clean vanilla harness baseline.
   - Implement automated reporter function `generateReleaseControlsMarkdown(results, options)` that aggregates all 4 control pairs into GitHub Flavored Markdown with pass/fail badges, evidence hashes, and deterministic timestamps (e.g. fixed release timestamp or commit date).
   - In test verification: normal test runs evaluate all 4 pairs in-memory and assert zero drift against the committed `docs/RELEASE_CONTROLS.md` without modifying disk. Disk writing is strictly guarded behind `process.env.UPDATE_RELEASE_CONTROLS === "1"`, ensuring test execution never dirty-marks the git working tree.

2. Author initial `docs/RELEASE_CONTROLS.md`:
   - Initialize the markdown file with the expected schema, documenting the four paired domains, methodology, verification criteria, and initial verified receipts generated with deterministic timestamps.
  </action>
  <acceptance_criteria>
    - `test/release-controls.test.ts` executes all 4 paired positive/negative acceptance controls (REL-03, D-07).
    - `docs/RELEASE_CONTROLS.md` is committed with inspectable receipts for all 4 control domains, and test runs assert zero drift without dirtying the git working tree (REL-03, D-07, D-16).
    - Zero false positives or unverified claims across all test cases.
    - `node scripts/run-tests.mjs --files dist/test/release-controls.test.js` passes completely.
  </acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node scripts/run-tests.mjs --files dist/test/release-controls.test.js</automated>
    <fails_when>non-zero exit code, or any control pair assertion fails</fails_when>
  </verify>
  <done>Paired positive/negative acceptance controls and public evidence ledger verified and passing.</done>
</task>

</tasks>

## Artifacts this phase produces

### Core Modules
- `src/core/support-matrix.ts`: Support matrix ledger, 4-tier taxonomy engine, and doc generator

### CLI & UI
- `src/cli.ts`: `alpha-aos doctor --matrix [--json]` dispatch
- `src/format.ts`: Support matrix terminal table formatter with ANSI status badges

### Documentation
- `docs/SUPPORT_MATRIX.md`: Versioned public support matrix
- `docs/RELEASE_CONTROLS.md`: Paired acceptance controls evidence ledger

### Tests
- `test/support-matrix.test.ts`: Support matrix taxonomy and drift unit suite
- `test/release-controls.test.ts`: Paired acceptance controls integration suite
