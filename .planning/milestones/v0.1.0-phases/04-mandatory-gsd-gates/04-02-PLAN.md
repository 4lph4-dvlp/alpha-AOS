---
phase: 04-mandatory-gsd-gates
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - schemas/gate-receipt.schema.json
  - src/types.ts
  - src/core/validation.ts
  - src/core/gate-receipt.ts
  - src/core/gate.ts
  - test/gate-engine.test.ts
autonomous: true
requirements: [GATE-02]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Gate engine resolution follows Native-First precedence: native project scripts in package.json or native CLI configurations are chosen before ECC skill fallbacks (D-04)."
    - "Overlapping check engines are visibly suppressed in the gate execution report with cited reasons, guaranteeing exactly one check engine runs per obligation and zero duplicate reviews occur (D-05)."
    - "Structured gate receipts strictly conform to schemas/gate-receipt.schema.json validated using Ajv Draft 2020-12 in src/core/validation.ts (D-06)."
    - "Gate receipts record obligation, selected engine, status ('passed' | 'failed'), exit code, execution timestamp, target Git HEAD SHA, working-tree digest, suppressed alternatives, and evidence SHA-256 (D-06)."
    - "Receipt persistence is transactional and atomic using applyFileTransaction, ensuring pre-image snapshots and crash-resilient journal logging."
  prohibitions:
    - statement: "MUST NOT execute multiple engines for the same obligation or perform duplicate reviews of the same risk concern (D-05)."
      category: behavioral
      status: resolved
      verification: judgment
    - statement: "MUST NOT write gate receipts directly to disk with raw fs.writeFile; all receipt mutations must go through applyFileTransaction (SAFE-03)."
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT ignore or downgrade non-zero exit codes from check engines; non-zero exit MUST record status: 'failed' (SAFE-05)."
      category: safety
      status: resolved
      verification: judgment
  artifacts:
    - path: "schemas/gate-receipt.schema.json"
      provides: "Draft 2020-12 closed JSON schema for structured gate receipts"
      contains: "schemaVersion"
    - path: "src/core/gate-receipt.ts"
      provides: "Gate receipt data models, serialization, atomic persistence, and strict validation"
      exports: ["createGateReceipt", "writeGateReceipt", "readGateReceiptStrict", "computeRiskSurfaceDigest"]
    - path: "src/core/gate.ts"
      provides: "Single-engine obligation resolution with native-first precedence and visible suppression"
      exports: ["selectGateEngine", "executeGateCheck"]
    - path: "test/gate-engine.test.ts"
      provides: "Verification of native-first selection, suppression reporting, receipt validation, and atomic writing"
  key_links:
    - from: "src/core/gate-receipt.ts"
      to: "src/core/transaction.ts"
      via: "applyFileTransaction writes receipts atomically to .alpha-aos/receipts/gates/"
      pattern: "applyFileTransaction"
    - from: "src/core/gate-receipt.ts"
      to: "src/core/validation.ts"
      via: "validateManagedDocument validates receipts against gate-receipt.schema.json"
      pattern: "validateManagedDocument"
    - from: "src/core/gate.ts"
      to: "src/core/process.ts"
      via: "runProcess executes selected check engine with bounded output and timeouts"
      pattern: "runProcess"
---

<objective>
Implement Single-Engine Obligation Resolution, Native-First Dispatch, and Structured Gate Receipt Persistence (GATE-02).

Purpose: When a mandatory obligation is triggered, alpha-AOS selects exactly one check engine (favoring project-native tools over ECC skills), suppresses duplicate reviews visibly, executes the check in a bounded shell-free subprocess, and writes an atomic, schema-validated structured gate receipt (D-04, D-05, D-06).
Output: `schemas/gate-receipt.schema.json`, `src/core/gate-receipt.ts`, updated `src/core/validation.ts` and `src/core/gate.ts`, and test suite in `test/gate-engine.test.ts`.
</objective>

<execution_context>
@C:/Users/alpha/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-mandatory-gsd-gates/04-CONTEXT.md
@.planning/phases/04-mandatory-gsd-gates/04-RESEARCH.md
@.planning/phases/04-mandatory-gsd-gates/04-01-PLAN.md
@schemas/receipt.schema.json
@src/core/process.ts
@src/core/transaction.ts
@src/core/validation.ts
@src/core/gate.ts
</context>

<interfaces>
- `GateReceipt` in `src/types.ts` and `src/core/gate-receipt.ts`:
  ```typescript
  export interface GateReceipt {
    readonly schemaVersion: 1;
    readonly obligation: GateObligationType;
    readonly status: "passed" | "failed";
    readonly engine: {
      readonly id: string;
      readonly kind: "native" | "ecc";
      readonly command?: string;
      readonly skill?: string;
    };
    readonly targetCommitSha: string;
    readonly workingTreeDigest: string;
    readonly exitCode: number;
    readonly executedAt: string;
    readonly suppressedEngines: readonly {
      readonly engine: string;
      readonly reason: string;
    }[];
    readonly evidenceHash: string;
    readonly diagnostics?: string;
  }
  ```
- `EngineSelection` in `src/core/gate.ts`:
  ```typescript
  export interface EngineSelection {
    readonly obligation: GateObligationType;
    readonly selectedEngine: {
      readonly id: string;
      readonly kind: "native" | "ecc";
      readonly command?: string;
      readonly skill?: string;
    };
    readonly suppressedEngines: readonly {
      readonly engine: string;
      readonly reason: string;
    }[];
  }
  ```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Structured gate receipt schema and closed Draft 2020-12 validation</name>
  <read_first>
    - .planning/phases/04-mandatory-gsd-gates/04-CONTEXT.md — D-06: Structured Gate Receipts schema and fields.
    - schemas/receipt.schema.json — reference schema structure in Draft 2020-12.
    - src/core/validation.ts — `ManagedDocumentKind` and `validateManagedDocument`.
    - src/types.ts — `GateObligationType`.
  </read_first>
  <files>schemas/gate-receipt.schema.json, src/types.ts, src/core/validation.ts</files>
  <action>
Create `schemas/gate-receipt.schema.json`:
- JSON Schema Draft 2020-12 (`$schema: "https://json-schema.org/draft/2020-12/schema"`).
- Closed world (`unevaluatedProperties: false` or `additionalProperties: false`).
- Required fields: `schemaVersion`, `obligation`, `status`, `engine`, `targetCommitSha`, `workingTreeDigest`, `exitCode`, `executedAt`, `suppressedEngines`, `evidenceHash`.
- `schemaVersion`: enum `[1]`.
- `obligation`: enum `["security-review", "database-migration", "release-check"]`.
- `status`: enum `["passed", "failed"]`.
- `engine`: object requiring `id` and `kind` (enum `["native", "ecc"]`), optional `command` and `skill`.
- `targetCommitSha`: string matching 40-character hex pattern (`^[0-9a-f]{40}$`).
- `workingTreeDigest`: string matching 64-character hex pattern (`^[0-9a-f]{64}$`).
- `exitCode`: integer.
- `executedAt`: string date-time format.
- `suppressedEngines`: array of objects each having `engine` (string) and `reason` (string).
- `evidenceHash`: string matching 64-character hex pattern (`^[0-9a-f]{64}$`).
- `diagnostics`: optional string.

In `src/types.ts`:
- Define `GateReceipt` matching the schema.

In `src/core/validation.ts`:
- Add `"gate-receipt"` to `ManagedDocumentKind`.
- Wire `schemas/gate-receipt.schema.json` into schema loader.
  </action>
  <acceptance_criteria>
    - `schemas/gate-receipt.schema.json` defines a valid Draft 2020-12 schema.
    - `ManagedDocumentKind` includes `"gate-receipt"`.
    - `validateManagedDocument` successfully validates conforming gate receipts and rejects invalid or unknown properties.
  </acceptance_criteria>
  <verify>
    <automated>npm run check &amp;&amp; npm run build</automated>
    <fails_when>The command exits nonzero or TypeScript typecheck fails.</fails_when>
  </verify>
  <done>Gate receipt schema and validation registration are implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Native-first engine resolver with visible suppression and bounded subprocess dispatch</name>
  <read_first>
    - .planning/phases/04-mandatory-gsd-gates/04-CONTEXT.md — D-04: Native-First precedence; D-05: Visible suppression reporting.
    - .planning/phases/04-mandatory-gsd-gates/04-RESEARCH.md — Pattern 2: Single-Engine Resolution with Native Precedence & Suppression.
    - src/core/process.ts — `runProcess` shell-free bounded execution adapter.
    - src/core/gate.ts — functions created in Plan 04-01.
  </read_first>
  <files>src/core/gate.ts</files>
  <action>
In `src/core/gate.ts`:
- Implement `selectGateEngine(obligation: GateObligationType, projectRoot: string, packageJsonScripts?: Record<string, string>): EngineSelection`:
  1. For `security-review`:
     - Inspect `packageJsonScripts` for native script keys: `["security", "audit", "security-check", "test:security", "lint:security"]`.
     - If matched, return selected native engine (`id: "npm-run-" + scriptName`, `kind: "native"`, `command: "npm run " + scriptName`), and suppressed ECC engine (`engine: "ecc-universal:security-review"`, `reason: "native script found in package.json: npm run " + scriptName`).
     - If no native script found, select ECC fallback (`id: "ecc-universal:security-review"`, `kind: "ecc"`, `skill: "security-review"`), and suppressed `[]`.
  2. For `database-migration`:
     - Inspect `packageJsonScripts` for native keys: `["migrate:check", "migrate:status", "db:status", "prisma:status"]`.
     - Inspect project root for ORM tools (e.g. `prisma`, `alembic`, `knex`, `drizzle`).
     - If native command or script exists, select native engine and suppress ECC fallback.
     - If absent, select fallback ECC migration check skill (`id: "ecc-universal:migration-check"`, `kind: "ecc"`, `skill: "migration-check"`).
  3. For `release-check`:
     - Inspect `packageJsonScripts` for native keys: `["release:check", "release:verify", "changeset:status"]`.
     - If native script exists, select native engine and suppress fallback.
     - If absent, select fallback release check engine (`id: "alpha-aos:release-check"`, `kind: "native"`, `command: "node scripts/build-artifact.mjs check"` or version consistency check).
- Implement `executeGateCheck(selection: EngineSelection, projectRoot: string): Promise<{ exitCode: number; stdout: string; stderr: string; timedOut: boolean }>`:
  - Executes the selected engine's command via `runProcess` with:
    - `cwd: projectRoot`
    - `timeoutMs: 60_000` (60s ceiling)
    - `maxOutputBytes: 262_144` (256 KiB bounded capture)
    - Environment scrubbed via `nodeRuntimeEnvironment` with explicit allowlist
  - Returns exit code, redacted excerpts, and timeout flag.
  </action>
  <acceptance_criteria>
    - `selectGateEngine` strictly selects 1 engine per obligation (D-04).
    - When native tools exist, they are chosen over ECC skills and ECC skills are visibly cited in `suppressedEngines` (D-05).
    - Subprocess execution uses `runProcess` with bounded timeout and byte caps without a shell.
  </acceptance_criteria>
  <verify>
    <automated>npm run check &amp;&amp; npm run build</automated>
    <fails_when>The command exits nonzero or TypeScript typecheck fails.</fails_when>
  </verify>
  <done>Native-first engine resolution and bounded execution dispatch are implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Atomic gate receipt writing and persistence verification suite</name>
  <read_first>
    - .planning/phases/04-mandatory-gsd-gates/04-CONTEXT.md — D-06: Structured Gate Receipts persistence under .alpha-aos/receipts/gates/.
    - src/core/transaction.ts — `applyFileTransaction` atomic write engine.
    - src/core/validation.ts — `validateManagedDocument`.
    - test/project-pack-sync.test.ts — transaction and receipt test precedents.
  </read_first>
  <files>src/core/gate-receipt.ts, test/gate-engine.test.ts</files>
  <action>
Create `src/core/gate-receipt.ts`:
- Implement `computeRiskSurfaceDigest(projectRoot: string, riskFiles: readonly string[]): Promise<string>`:
  - Sorts `riskFiles` in ascending code-point order.
  - Computes SHA-256 over `${relPath}:${sha256(content)}\n`. If a file is missing or unreadable, records `${relPath}:MISSING\n`.
  - Returns resulting 64-hex digest.
- Implement `createGateReceipt(params: { obligation: GateObligationType; engineSelection: EngineSelection; executionResult: { exitCode: number; stdout: string; stderr: string }; headCommitSha: string; workingTreeDigest: string; evidenceHash: string }): GateReceipt`:
  - Determines status: `executionResult.exitCode === 0 ? "passed" : "failed"`.
  - Records ISO 8601 `executedAt` timestamp.
  - Combines stdout/stderr into bounded diagnostic excerpt if failed.
- Implement `writeGateReceipt(projectRoot: string, receipt: GateReceipt): Promise<{ receiptPath: string; transactionId: string }>`:
  - Target path: `.alpha-aos/receipts/gates/${receipt.obligation}.json`.
  - Serializes receipt as formatted JSON.
  - Validates document via `validateManagedDocument({ text, format: "json", kind: "gate-receipt" })`.
  - Writes atomically via `applyFileTransaction` with `.alpha-aos` as allowed root.
- Implement `readGateReceiptStrict(projectRoot: string, obligation: GateObligationType): Promise<GateReceipt | null>`:
  - Reads `.alpha-aos/receipts/gates/${obligation}.json`. Returns `null` if ENOENT.
  - Validates document strictly via `validateManagedDocument`. Throws if invalid or schema-corrupt.

In `test/gate-engine.test.ts`:
- Author comprehensive tests:
  1. Native-First precedence selects native `package.json` script over ECC fallback.
  2. Suppressed engine list contains ECC skill and cites exact reason.
  3. Non-zero exit code or timeout results in `status: "failed"` receipt without crashing.
  4. Receipt validates strictly against `schemas/gate-receipt.schema.json`.
  5. `writeGateReceipt` writes atomically using `applyFileTransaction`.
  6. Tampered or invalid receipt is rejected by `readGateReceiptStrict`.
  </action>
  <acceptance_criteria>
    - Receipts are written atomically under `.alpha-aos/receipts/gates/<obligation>.json`.
    - All written receipts validate against `schemas/gate-receipt.schema.json`.
    - All tests in `test/gate-engine.test.ts` pass with 0 failures.
  </acceptance_criteria>
  <verify>
    <automated>npm run check &amp;&amp; npm run build &amp;&amp; node scripts/run-tests.mjs --files dist/test/gate-engine.test.js</automated>
    <fails_when>The command exits nonzero, type checking fails, or any assertion in dist/test/gate-engine.test.js fails.</fails_when>
  </verify>
  <done>Structured gate receipt persistence and single-engine dispatch are verified with automated tests.</done>
</task>

</tasks>
