---
phase: 07-cross-platform-release-proof
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - test/helpers/brownfield-fixture.ts
  - test/brownfield-gsd-cycle.test.ts
  - scripts/run-brownfield-proof.mjs
  - src/core/worker-authority.ts
autonomous: true
requirements: [REL-04]

must_haves:
  truths:
    - "A realistic brownfield Git fixture workspace containing existing code, dependencies, and .planning/ state is synthesized in a clean sandbox (REL-04, D-09)."
    - "The brownfield E2E cycle exercises all 5 representative benchmark elements: (1) Context7 documentation lookup, (2) TypeScript Web API pack matching, (3) Mandatory authentication security gate, (4) Unified Memory context handoff between Codex and Antigravity, and (5) Safe project pack cleanup restoring baseline (REL-04, D-10)."
    - "The single GSD writer invariant is strictly enforced throughout the cycle: only the active controller holds writer.lock, and worker harnesses (Hermes, Pi) executing inside witnessWorkerDelegation are forbidden from mutating .planning/ files (REL-04, D-11)."
    - "Any unauthorized worker attempt to mutate .planning/ files immediately fails closed with WorkerAuthorityError ('planning-mutation-detected') and triggers byte-for-byte rollback of modified planning files (REL-04, D-11)."
    - "The brownfield verification cycle is executable both as an automated CI regression suite (test/brownfield-gsd-cycle.test.ts) and as a standalone CLI auditor runner (scripts/run-brownfield-proof.mjs) (REL-04, D-12)."
  prohibitions:
    - statement: "MUST NOT allow worker harnesses or cross-harness memory handoffs to mutate .planning/ files or acquire controller writer locks (REL-04, D-11)."
      category: safety
      status: resolved
      verification: automated
    - statement: "MUST NOT allow Hermes to act as a GSD state controller (REL-04, D-11)."
      category: safety
      status: resolved
      verification: automated
    - statement: "MUST NOT mutate files outside the sandboxed brownfield repository during GSD lifecycle execution (REL-04, D-09)."
      category: safety
      status: resolved
      verification: automated
  artifacts:
    - path: "test/helpers/brownfield-fixture.ts"
      provides: "Brownfield Git repository fixture synthesizer with pre-existing commits, code, and planning state"
      exports: ["setupBrownfieldFixture", "BrownfieldFixture"]
    - path: "test/brownfield-gsd-cycle.test.ts"
      provides: "Automated 5-element brownfield GSD benchmark and single-writer enforcement integration suite"
    - path: "scripts/run-brownfield-proof.mjs"
      provides: "Standalone executable script for human auditor reproduction of the brownfield GSD cycle"
    - path: "src/core/worker-authority.ts"
      provides: "Single GSD writer enforcement engine with cryptographic delegation witnessing and rollback"
---

<objective>
Execute and verify the full brownfield GSD lifecycle benchmark across active harnesses with strict single-writer protection (REL-04, D-09, D-10, D-11, D-12).

Purpose: Prove that alpha-AOS operates cleanly on realistic brownfield codebases through all phases (discuss -> plan -> execute -> verify -> ship) exercising 5 representative benchmark elements while cryptographically enforcing the Single GSD Writer invariant during cross-harness worker delegation.
Output: `test/helpers/brownfield-fixture.ts`, `test/brownfield-gsd-cycle.test.ts`, `scripts/run-brownfield-proof.mjs`, and verified integrations in `src/core/worker-authority.ts`.
</objective>

<execution_context>
Execution via GSD workflow adhering to anti-shallow deep work rules.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-cross-platform-release-proof/07-CONTEXT.md
@.planning/phases/07-cross-platform-release-proof/07-RESEARCH.md
@.planning/phases/07-cross-platform-release-proof/07-PATTERNS.md
@.planning/phases/07-cross-platform-release-proof/07-VALIDATION.md
@src/core/worker-authority.ts
@src/core/writer-lock.ts
@src/core/project-plan.ts
@src/core/gate-lifecycle.ts
@src/adapters/unified-memory.ts
@test/helpers/git-fixture.ts
@test/helpers/pack-fixtures.ts
</context>

<interfaces>
```typescript
// Proposed exports from test/helpers/brownfield-fixture.ts
export interface BrownfieldFixture {
  readonly path: string;
  readonly headCommit: string;
  readonly baselinePlanningDigest: string;
  cleanup(): Promise<void>;
}

export function setupBrownfieldFixture(targetDir: string): Promise<BrownfieldFixture>;
```
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Brownfield Git Fixture Synthesizer, 5-Element E2E Benchmark Suite & Standalone Runner (REL-04, D-09, D-10, D-12)</name>
  <read_first>
    .planning/phases/07-cross-platform-release-proof/07-CONTEXT.md
    .planning/phases/07-cross-platform-release-proof/07-RESEARCH.md
    .planning/phases/07-cross-platform-release-proof/07-PATTERNS.md
    test/helpers/git-fixture.ts
    test/helpers/pack-fixtures.ts
    src/core/project-plan.ts
    src/core/gate-lifecycle.ts
    src/adapters/unified-memory.ts
  </read_first>
  <files>test/helpers/brownfield-fixture.ts, test/brownfield-gsd-cycle.test.ts, scripts/run-brownfield-proof.mjs</files>
  <behavior>
    - Synthesizes an offline, realistic brownfield Git repository with initial commits, package.json (with express dependency), tsconfig.json, auth routes (src/auth/jwt.ts), and pre-existing .planning/ state (PROJECT.md, STATE.md, ROADMAP.md).
    - Drives the complete GSD lifecycle: discuss -> plan -> execute -> verify -> ship.
    - Exercises the 5 representative benchmark elements in sequence:
      1. Global Optional Capability: Context7 documentation lookup resolves library IDs and queries docs.
      2. Project Pack: Node/TypeScript Web API pack is selected by repository evidence and projected into workspace.
      3. Mandatory Gate: Modification to authentication file (src/auth/jwt.ts) triggers security-review gate, blocking advance until gate receipt is verified.
      4. Cross-Harness Handoff: Context and memory handoff between Codex and Antigravity executes via Unified Memory without corrupting planning state.
      5. Safe Cleanup: alpha-aos uninstall --project cleanly removes project pack artifacts while leaving brownfield source code untouched.
    - Standalone runner scripts/run-brownfield-proof.mjs reproduces the complete benchmark and emits structured terminal output for human auditors.
  </behavior>
  <action>
Implement the brownfield fixture synthesizer, E2E benchmark suite, and standalone runner adhering to D-09, D-10, D-12, and REL-04:

1. Create `test/helpers/brownfield-fixture.ts`:
   - Implement `setupBrownfieldFixture(targetDir)`:
     - Initialize a Git repository with `git init` and configure local committer identity (`user.name`, `user.email`).
     - Create brownfield codebase files: `package.json` with `"dependencies": { "express": "^4.21.0" }`, `tsconfig.json`, `src/index.ts` (Express server setup), and `src/auth/jwt.ts` (token verification function).
     - Create realistic `.planning/` directory with `PROJECT.md`, `ROADMAP.md`, `STATE.md`, and `phases/01-auth/01-CONTEXT.md`.
     - Stage all files (`git add .`) and commit with deterministic message "feat: initial brownfield application".
     - Compute and record baseline planning tree digest.
     - Return `BrownfieldFixture` object containing `path`, `headCommit`, `baselinePlanningDigest`, and `cleanup()` hook.

2. Implement `test/brownfield-gsd-cycle.test.ts`:
   - Test 1 (Full 5-Element Cycle):
     - Step 1 (Discuss): Ingest brownfield repository requirements and verify initial planning state.
     - Step 2 (Plan): Execute `createProjectPlan` inside fixture; assert Node/TypeScript Web API pack is evidence-matched and selected; assert tool projections are generated.
     - Step 3 (Execute with Context7 & Memory Handoff): Trigger Context7 documentation lookup for Express router API; execute cross-harness handoff from Codex to Antigravity using `recordHandoff` and `consumeHandoff` in Unified Memory adapter; assert Memory Vault entries are created without altering `.planning/` files.
     - Step 4 (Verify Mandatory Gate): Introduce modification to `src/auth/jwt.ts`; invoke `evaluateLifecycleGates`; assert lifecycle advance is blocked by `security-review` gate obligation; generate valid `GateReceipt` and assert lifecycle unblocks.
     - Step 5 (Ship & Safe Cleanup): Reconcile plan application; execute `uninstallProjectPack(fixture.path)`; assert pack shims, projections, and sidecars are cleanly removed, while original `package.json`, `tsconfig.json`, and `src/index.ts` remain intact.

3. Create `scripts/run-brownfield-proof.mjs`:
   - Standalone CLI executable with shebang `#!/usr/bin/env node`.
   - Allocates a temporary directory, sets up the brownfield fixture, and runs each of the 5 benchmark steps with informative terminal logging.
   - Logs step elapsed times, gate evaluation statuses, and hash checks.
   - Cleans up temporary files upon completion and exits 0 on success or 1 on failure.
  </action>
  <acceptance_criteria>
    - `test/helpers/brownfield-fixture.ts` synthesizes realistic brownfield repository with Git history and planning state (REL-04, D-09).
    - `test/brownfield-gsd-cycle.test.ts` executes all 5 benchmark elements end-to-end (REL-04, D-10).
    - `scripts/run-brownfield-proof.mjs` executes standalone for manual auditor reproduction (REL-04, D-12).
    - `node scripts/run-tests.mjs --files dist/test/brownfield-gsd-cycle.test.js` passes completely.
  </acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node scripts/run-tests.mjs --files dist/test/brownfield-gsd-cycle.test.js</automated>
    <fails_when>non-zero exit code, or any benchmark step fails</fails_when>
  </verify>
  <done>Brownfield Git fixture synthesizer, 5-element E2E benchmark suite, and standalone runner verified and passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Single GSD Writer Enforcement during Cross-Harness Worker Delegation (REL-04, D-11)</name>
  <read_first>
    .planning/phases/07-cross-platform-release-proof/07-CONTEXT.md
    .planning/phases/07-cross-platform-release-proof/07-RESEARCH.md
    .planning/phases/07-cross-platform-release-proof/07-PATTERNS.md
    src/core/worker-authority.ts
    src/core/writer-lock.ts
    test/worker-authority.test.ts
    test/canary.test.ts
  </read_first>
  <files>src/core/worker-authority.ts, test/brownfield-gsd-cycle.test.ts</files>
  <behavior>
    - Strictly enforces that only the designated GSD controller holds writer.lock and mutates .planning/ state.
    - Prohibits Hermes from ever acquiring a controller role or writer.lock (GATE-05, D-11).
    - Wraps all worker delegations in witnessWorkerDelegation, taking cryptographic snapshots of .planning/ before delegating.
    - If a worker harness attempts to create, modify, or delete any file in .planning/, witnessWorkerDelegation intercepts the violation, rolls back modified planning files byte-for-byte to their snapshot state, and throws WorkerAuthorityError with code "planning-mutation-detected".
    - Verifies that cross-harness Unified Memory handoffs between Codex and Antigravity never modify .planning/ state.
  </behavior>
  <action>
Enforce the Single GSD Writer invariant adhering to D-11 and REL-04:

1. In `src/core/worker-authority.ts`:
   - Audit `assertControllerRole`, `resolveHarnessRole`, and `witnessWorkerDelegation`.
   - Ensure `witnessWorkerDelegation` takes a deep cryptographic snapshot (in-memory map of file paths to byte buffers) of all files in `.planning/` before invoking the worker action.
   - After the worker action completes, re-hash `.planning/`.
   - If any difference is detected:
     - Restore all altered files from the in-memory snapshot.
     - Delete any newly created files in `.planning/`.
     - Throw `WorkerAuthorityError("planning-mutation-detected", harnessId, message, modifiedPaths)`.
   - Ensure that `assertControllerRole("hermes")` always throws `WorkerAuthorityError("unauthorized-controller")`.

2. In `test/brownfield-gsd-cycle.test.ts`:
   - Add dedicated test cases verifying Single Writer enforcement during the brownfield cycle:
     - Test A: Hermes Controller Prohibition. Assert that attempting to launch a GSD cycle with Hermes as controller immediately throws `WorkerAuthorityError("unauthorized-controller")`.
     - Test B: Worker Planning Tamper Defense. Delegate a task to Hermes or Pi inside `witnessWorkerDelegation`. Inside the worker task, attempt to write `tampered: true` to `.planning/STATE.md` and create a rogue file `.planning/rogue.md`. Assert that `witnessWorkerDelegation` rejects with `WorkerAuthorityError("planning-mutation-detected")`. Verify that `.planning/STATE.md` was restored to its exact original bytes and `.planning/rogue.md` was purged.
     - Test C: Controller Writer Lock Isolation. Assert that while the controller holds `writer.lock`, concurrent attempts by another harness to acquire the lock fail with `WriterLockError`.
     - Test D: Cross-Harness Unified Memory Handoff Immutability. Execute context handoff between Codex and Antigravity; assert that `.planning/` tree digest before and after handoff is identical.
  </action>
  <acceptance_criteria>
    - Only active GSD controller holds writer.lock; Hermes is strictly forbidden from controller role (REL-04, D-11).
    - `witnessWorkerDelegation` catches unauthorized worker writes to `.planning/`, rolls back files byte-for-byte, and raises `WorkerAuthorityError` (REL-04, D-11).
    - Cross-harness memory handoffs leave `.planning/` completely immutable (REL-04, D-10, D-11).
    - `node scripts/run-tests.mjs --files dist/test/brownfield-gsd-cycle.test.js` passes completely.
  </acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node scripts/run-tests.mjs --files dist/test/brownfield-gsd-cycle.test.js</automated>
    <fails_when>non-zero exit code, or writer-lock / worker-authority assertion fails</fails_when>
  </verify>
  <done>Single GSD writer enforcement and worker authority defense verified and passing across cross-harness operations.</done>
</task>

</tasks>

## Artifacts this phase produces

### Core Engine
- `src/core/worker-authority.ts`: Guarded single-writer authority and cryptographic delegation witness

### Test Helpers
- `test/helpers/brownfield-fixture.ts`: Brownfield Git fixture workspace synthesizer

### Test Suites
- `test/brownfield-gsd-cycle.test.ts`: Brownfield 5-element GSD cycle and single-writer verification suite

### Scripts
- `scripts/run-brownfield-proof.mjs`: Standalone executable brownfield proof runner for human auditors
