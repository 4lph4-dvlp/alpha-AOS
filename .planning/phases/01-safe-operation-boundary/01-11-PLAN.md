---
phase: 01-safe-operation-boundary
plan: 11
type: execute
wave: 4
depends_on: [01-02, 01-08, 01-09]
files_modified:
  - src/core/mcp-fixture.ts
  - src/core/gsd-fixture.ts
  - src/core/ecc-fixture.ts
  - src/core/gsd-compat.ts
  - src/core/install.ts
  - src/core/ecc-skills.ts
  - src/cli.ts
  - test/process.test.ts
  - test/install.test.ts
  - test/ecc-skills.test.ts
autonomous: true
requirements: [SAFE-04, SAFE-06]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-02/D-03: every GSD, ECC, and MCP fixture receives caller-precomputed fixture/temp/source/package/cache/config/target paths plus a complete immutable PathProof set; no mutating fixture API calls mkdtemp or discovers a writable path after preflight."
    - "D-04: every mutating fixture API requires the caller's one MutationSession and approved ProcessSpecs, performs no nested lock acquisition, and executes neither mkdir/write/remove nor a package/MCP child before full proof revalidation and session acquisition."
    - "Every fixture and installer child process uses ProcessSpec with shell false, an operation-specific environment-name allowlist, timeout, and output cap."
    - "D-12: fixture/install failures preserve only fingerprints, bounded redacted excerpts, exit/timeout metadata; raw stdout/stderr never enters errors or results."
    - "MCP protocol sessions use the same process boundary and no longer spread process.env into child environments."
  artifacts:
    - path: src/core/mcp-fixture.ts
      provides: "Preplanned, proof-bound MCP fixture executor consuming a caller MutationSession"
    - path: src/core/gsd-fixture.ts
      provides: "Preplanned, proof-bound GSD fixture executor consuming a caller MutationSession"
    - path: src/core/ecc-fixture.ts
      provides: "Preplanned, proof-bound ECC fixture executor consuming a caller MutationSession"
    - path: src/core/install.ts
      provides: "ProcessSpec-based external package inspection/install calls"
  key_links:
    - from: src/core/mcp-fixture.ts
      to: src/core/process.ts
      via: "openProtocolProcess"
      pattern: "openProtocolProcess"
    - from: src/core/mcp-fixture.ts
      to: src/core/writer-lock.ts
      via: "caller-provided MutationSession gates the first fixture directory/package/protocol mutation"
      pattern: "MutationSession|OperationPathProofSet"
    - from: src/core/install.ts
      to: src/core/gsd-fixture.ts
      via: "preplanned fixture operation passed with the caller session; aggregate ownership is completed in Plan 01-14"
      pattern: "GsdFixtureOperationPlan|MutationSession"
    - from: src/core/install.ts
      to: src/core/process.ts
      via: "normalized npm/node ProcessSpec"
      pattern: "runProcess|normalizeProcessSpec"
  prohibitions:
    - statement: "A fixture result MUST NOT claim native verification when its child timed out, exceeded capture bounds, inherited an unapproved credential name, or produced only unparsed raw output."
      requirement_id: SAFE-06
      category: transparency
      status: unverified
      flagged: true
---

<objective>
fixture와 installer subprocess callers를 중앙 ProcessSpec 경계로 이동한다.

Purpose: high-risk external process surfaces에서 shell, ambient env, raw-output escape를 제거한다.
Output: preplanned/session-required MCP/GSD/ECC fixture APIs, migrated compatibility/installer process calls, and caller seams ready for aggregate ownership.
</objective>

<execution_context>
@C:/Users/alpha/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safe-operation-boundary/01-RESEARCH.md
@.planning/phases/01-safe-operation-boundary/01-PATTERNS.md
@.planning/phases/01-safe-operation-boundary/01-08-SUMMARY.md
@.planning/phases/01-safe-operation-boundary/01-09-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: MCP initialize/list-tools session을 ProcessSpec으로 연결</name>
  <files>src/core/mcp-fixture.ts, test/process.test.ts</files>
  <read_first>
    src/core/mcp-fixture.ts
    src/core/process.ts
    src/core/path-boundary.ts
    src/core/writer-lock.ts
    test/mcp.test.ts
    test/process.test.ts
  </read_first>
  <behavior>
    - initialize and tools/list messages complete through openProtocolProcess with absolute deadline and byte cap.
    - only required runtime plus fixture credential names enter the child; unrelated environment sentinel is absent.
    - timeout/protocol failure emits coded bounded redacted evidence, never raw stderr.
    - the caller supplies exact fixtureRoot, pack/archive/source/package/cache/temp/config/target/state/journal/snapshot paths, their complete PathProof set, approved npm/Pi/MCP ProcessSpecs, reviewed digest, and one MutationSession before the first directory or child-process mutation.
  </behavior>
  <action>SAFE-04/SAFE-06, D-02~D-04, D-12에 따라 read-only `createMcpFixtureOperationPlan`과 mutating `runMcpFixture(reviewedPlan, session)` 경계를 만든다. Planner는 caller가 선택한 exact non-existing fixture root 아래 pack/archive/source/package/cache/temp/config/target/state/journal/snapshot paths와 npm pack, Pi bridge, MCP protocol ProcessSpecs를 모두 확정하고 complete `OperationPathProofSet`/source hashes/plan digest를 반환한다. Executor는 reviewed digest와 전체 proof/spec set을 재검증하고 caller `MutationSession`의 operation/plan digest를 확인한 뒤에만 첫 mkdir/package write/process launch를 허용하며, apply 중 `mkdtemp`를 호출하거나 writable path/process를 추가하거나 nested writer lock을 획득하지 않는다. Cleanup도 동일 session과 predeclared proofs 아래에서만 수행한다. Direct spawn block은 `openProtocolProcess`로 교체하고 server별 exact executable/argv/env-name list 및 npm cache/temp roots를 선언한다. Fixture sentinel values는 RedactionContext exact credentials로 등록하고 protocol deadline, stderr cap, whole-output fingerprint, termination metadata만 result에 남긴다.</action>
  <acceptance_criteria>representative MCP fixture completes a meaningful initialize/list-tools exchange; incomplete path role/proof/spec, changed source/parent, absent or mismatched session, timeout/env/redaction cases run zero mkdir/write/remove/package/Pi/MCP actions before refusal, while valid execution records the same caller token on every mutation and no raw sentinel.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/process.test.js --test-name-pattern="protocol|MCP"</automated>
    <fails_when>non-zero exit, output reports 0 matching tests, any fixture mutation/process launch precedes full preflight and caller-session acquisition, a nested lock is acquired, or diagnostics contain raw stderr/credential sentinel</fails_when>
  </verify>
  <reversibility rating="reversible">existing fixture result contract is preserved with safer metadata.</reversibility>
  <done>one MCP fixture path is end-to-end preplanned, proof-bound, caller-session-controlled, and process-policy-controlled.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GSD/ECC/compat fixture process migration</name>
  <files>src/core/gsd-fixture.ts, src/core/ecc-fixture.ts, src/core/gsd-compat.ts, test/process.test.ts</files>
  <read_first>
    src/core/gsd-fixture.ts
    src/core/ecc-fixture.ts
    src/core/gsd-compat.ts
    src/core/process.ts
    src/core/path-boundary.ts
    src/core/writer-lock.ts
    test/gsd-compat.test.ts
    test/process.test.ts
  </read_first>
  <behavior>
    - createGsdFixtureOperationPlan and createEccFixtureOperationPlan precompute exact fixture/temp/source/package/cache/config/target/state/journal/snapshot paths, complete proofs, source/lock hashes, and every npm/npx/smoke ProcessSpec without mutation.
    - runGsdFixture and runEccFixture require those reviewed plans plus the caller MutationSession; the first mkdir, npm pack/install, compatibility write, rendered-skill write, or cleanup happens only after the complete proof/session gate.
    - every fixture mutation and subprocess records the caller token; neither executor calls mkdtemp, creates a nested writer, spreads process.env, or derives a new writable destination from subprocess output.
  </behavior>
  <action>D-02~D-04/D-12에 따라 GSD/ECC fixtures에도 MCP tracer와 같은 read-only operation-plan plus required caller-session API를 적용한다. Exact archive name/source tree/render target and npm cache/temp roots를 stable lock metadata에서 사전 계산하고, package output은 expected preproved archive path와 일치할 때만 소비한다. GSD compatibility의 package extraction temp path도 caller plan/proof/session에 포함한다. 모든 npm pack/install, smoke, monitor, compatibility subprocess를 approved ProcessSpec으로 전환하고 operation마다 runtime/PATH/TEMP 및 named fixture variables만 allowlist한다. mkdir/write/remove/cleanup은 session-aware filesystem primitive와 predeclared proof를 사용하고 raw output은 ProcessResult excerpt/fingerprint로만 소비한다. Existing integrity, renderer hash, global sentinel checks를 유지하며 direct spawnSync/runCommandCapture/process.env spread를 제거한다.</action>
  <acceptance_criteria>GSD/ECC/compat fixtures preserve meaningful invocation/integrity behavior; each incomplete proof, unexpected archive/source path, changed parent, absent/mismatched session case records zero filesystem/process mutations, and valid cases observe one token with no mkdtemp/direct child_process/process.env seam.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/gsd-compat.test.js dist/test/process.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, any GSD/ECC/compat mutation or child precedes full preflight/session acquisition, a fixture creates a nested lock, or a migrated fixture exposes a raw subprocess sentinel</fails_when>
  </verify>
  <reversibility rating="reversible">callers retain their domain result types while process transport changes.</reversibility>
  <done>all three fixture families share precomputed paths/proofs/specs, the caller's one MutationSession, and the centralized subprocess policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Install inspection/external command process migration</name>
  <files>src/core/install.ts, src/core/ecc-skills.ts, src/cli.ts, test/install.test.ts, test/ecc-skills.test.ts</files>
  <read_first>
    src/core/install.ts
    src/core/process.ts
    test/install.test.ts
    src/core/gsd-compat.ts
    src/core/gsd-fixture.ts
    src/core/ecc-fixture.ts
    src/core/mcp-fixture.ts
    src/core/ecc-skills.ts
    src/cli.ts
  </read_first>
  <action>`inspectGsdInstall`, global npm root/version inspection, and external installer command runners를 ProcessSpec으로 전환한다. Plan 단계의 command specs에는 absolute executable, argv, cwd, required/optional env names, timeout, byte cap이 모두 존재해야 한다. ECC skill sync와 standalone CLI fixture routes는 먼저 complete fixture operation plan을 만들고 proof를 끝낸 뒤 one caller session을 acquire하여 session-required fixture executor에 전달한다. Managed install helpers는 preplanned GSD/ECC/MCP fixture operations and required caller-session parameters를 받도록 바꾸고, aggregate install/bootstrap의 전체 path/process ordering 및 single-session ownership은 Plan 01-14의 명시적 계약으로 둔다. 이 task에서는 fixture/external process boundary와 caller signature를 닫고, apply 중 임의 temp path 생성이나 session 없는 legacy fixture call을 남기지 않는다.</action>
  <acceptance_criteria>install/ECC/CLI fixture call sites use the preplanned session-required APIs, no call site passes raw path options or invokes a fixture without complete proofs/approved specs/caller session, plans expose env names not values, and existing target/version/preview behavior remains.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/install.test.js dist/test/ecc-skills.test.js dist/test/process.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, an install plan contains an environment value, a fixture caller omits its proof/session contract, or a child/filesystem mutation occurs before its caller session</fails_when>
  </verify>
  <reversibility rating="reversible">installation semantics remain unchanged until the later session integration plan.</reversibility>
  <done>all known fixture/install process seams use SAFE-06 policy and all three mutating fixture APIs are callable only with precomputed proof-bound plans and a caller MutationSession; Plan 01-14 owns aggregate install/bootstrap orchestration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture/install orchestration→third-party executable | package and MCP processes receive argv/env and return untrusted output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-24 | Elevation | fixture/install children | high | mitigate | exact ProcessSpec, shell false, named env allowlist |
| T-01-25 | Information Disclosure | process failures | high | mitigate | bounded redacted excerpts and fingerprint-only raw evidence |
</threat_model>

## Artifacts this phase produces

| Kind | Artifact | Contract |
|------|----------|----------|
| Migrated surface | MCP/GSD/ECC fixture APIs | precomputed temp/source/package/cache/config/target paths, complete PathProofs, approved ProcessSpecs, and required caller MutationSession |
| Migrated surface | compatibility/install process calls | unified ProcessSpec and ProcessResult semantics with Plan 01-14 aggregate-session caller seam |

## Probe coverage notes

- Generic injection is canon and tracked in STRIDE; false native-verification reporting is retained as a descriptor-less product prohibition.

<verification>
Focused process, install, compatibility, and MCP protocol tests pass without direct shell/raw-output seams.
</verification>

<success_criteria>
- every Phase 1 fixture/install process caller uses the central adapter and each mutating fixture consumes a precomputed operation plan plus caller session.
- ambient env and raw stderr/stdout do not cross the boundary.
- timeouts/caps are explicit operation metadata.
- no fixture filesystem or process mutation occurs before its complete proof set is revalidated and the caller session is acquired.
</success_criteria>

<output>
Create `.planning/phases/01-safe-operation-boundary/01-11-SUMMARY.md` when done.
</output>
