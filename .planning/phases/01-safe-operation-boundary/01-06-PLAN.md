---
phase: 01-safe-operation-boundary
plan: 06
type: execute
wave: 3
depends_on: [01-05]
files_modified:
  - schemas/stack.schema.json
  - schemas/lock.schema.json
  - schemas/project-stack.schema.json
  - src/core/catalog.ts
  - src/core/project.ts
  - test/catalog.test.ts
  - test/project.test.ts
autonomous: true
requirements: [SAFE-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Catalog, stable/candidate lock, and project manifest core fields are closed at every owned object level per D-08."
    - "Unknown namespaced extensions round-trip but remain inert per D-09."
    - "Loaders return supported-old read-only migration plans and reject unknown newer versions with bounded deterministic issues per D-10/D-11."
  artifacts:
    - path: schemas/stack.schema.json
      provides: "Closed current stack catalog contract"
    - path: schemas/lock.schema.json
      provides: "Closed stable/candidate lock contract"
    - path: schemas/project-stack.schema.json
      provides: "Closed project manifest contract"
    - path: src/core/catalog.ts
      provides: "Strict catalog and lock loaders"
    - path: src/core/project.ts
      provides: "Strict project manifest consumer"
  key_links:
    - from: src/core/catalog.ts
      to: src/core/validation.ts
      via: "managed document route before cast/domain use"
      pattern: "parseManagedDocument|validateManagedDocument"
    - from: src/core/project.ts
      to: schemas/project-stack.schema.json
      via: "strict YAML manifest route"
      pattern: "project-stack|migration"
  prohibitions:
    - statement: "A candidate lock or unregistered extension MUST NOT become authoritative merely because its JSON/YAML is syntactically valid."
      requirement_id: SAFE-05
      category: safety
      status: unverified
      flagged: true
---

<objective>
catalog, lock, project manifest loaders를 strict managed-document engine으로 전환한다.

Purpose: 현재 cast/minimum guards를 closed schema와 explicit version semantics로 교체한다.
Output: closed schemas and strict loader results for the repository's primary policy documents.
</objective>

<execution_context>
@C:/Users/alpha/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safe-operation-boundary/01-RESEARCH.md
@.planning/phases/01-safe-operation-boundary/01-PATTERNS.md
@.planning/phases/01-safe-operation-boundary/01-05-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: stack catalog bytes에서 typed catalog까지 strict tracer</name>
  <files>schemas/stack.schema.json, src/core/catalog.ts, test/catalog.test.ts</files>
  <read_first>
    schemas/stack.schema.json
    schemas/project-stack.schema.json
    src/core/catalog.ts
    src/core/validation.ts
    test/catalog.test.ts
  </read_first>
  <behavior>
    - valid current catalog loads unchanged through strict YAML and compiled schema.
    - duplicate/multi-doc/alias overflow/unknown core properties refuse before returning StackCatalog.
    - unknown namespaced extension remains visible in preservedExtensions and cannot alter catalog policy/components.
  </behavior>
  <action>D-08/D-09에 따라 `schemas/stack.schema.json`의 모든 alpha-AOS-owned object를 closed-world로 만들고 explicit namespaced `extensions` map만 연다. `loadCatalog`을 YAML strict parse→version route→Ajv schema→domain invariants 순으로 변경한다. 기존 public success return을 current valid document에서 유지하되 validation failure는 stable issues를 가진 typed error/result boundary로 전달한다.</action>
  <acceptance_criteria>existing catalog loads, all unknown-core/duplicate fixtures refuse before cast, and unknown extension changes neither selected components nor plan digest.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/catalog.test.js --test-name-pattern="catalog"</automated>
    <fails_when>non-zero exit, output reports 0 matching tests, or the valid repository catalog fails to load</fails_when>
  </verify>
  <reversibility rating="costly">closed schema may expose previously tolerated invalid user documents, but current repository fixtures remain supported.</reversibility>
  <done>stack.yaml has a complete strict typed path and no implicit unknown-field authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stable/candidate lock authority와 version refusal 고정</name>
  <files>schemas/lock.schema.json, src/core/catalog.ts, test/catalog.test.ts</files>
  <read_first>
    schemas/lock.schema.json
    catalog/stack.lock.json
    catalog/candidate.lock.json
    src/core/catalog.ts
    test/catalog.test.ts
  </read_first>
  <behavior>
    - stable and candidate channels parse against the same closed current schema but authority remains explicit in the caller.
    - supported old lock returns read-only migration plan; unknown newer refuses.
    - exact package version/integrity/domain invariants reject incomplete or ambiguous entries.
  </behavior>
  <action>D-08, D-10, D-11에 따라 lock schema의 nested packages/components를 닫고 exact version/integrity and stable-channel invariants를 domain validation에 둔다. `loadLock(root, channel)`은 channel을 authority input으로 보존하고 candidate를 stable apply input으로 승격하지 않는다. old/newer version result and bounded errors를 validation engine contract로 노출한다.</action>
  <acceptance_criteria>repository stable/candidate fixtures validate in their own channel; candidate-as-stable, duplicate package, missing integrity, old/current/newer cases have distinct deterministic results.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/catalog.test.js --test-name-pattern="lock"</automated>
    <fails_when>non-zero exit, output reports 0 matching tests, or candidate lock is accepted as stable authority</fails_when>
  </verify>
  <reversibility rating="costly">lock validation tightens accepted external input but does not rewrite any lock automatically.</reversibility>
  <done>lock parsing is strict, channel-aware, version-aware, and non-promoting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Project manifest strict read 및 previewable migration</name>
  <files>schemas/project-stack.schema.json, src/core/project.ts, test/project.test.ts</files>
  <read_first>
    schemas/project-stack.schema.json
    src/core/project.ts
    src/core/isolation.ts
    test/project.test.ts
    test/isolation.test.ts
  </read_first>
  <behavior>
    - current manifest retains existing detection behavior after strict validation.
    - supported old manifest is inspection-only with migration plan; detection/apply does not consume it as current.
    - unregistered extensions cannot select capabilities, harnesses, or isolation behavior.
  </behavior>
  <action>D-08~D-11에 따라 project manifest schema와 loader를 strict route에 연결한다. Existing repository-local user-owned resources remain outside the owned subtree and are not deleted or normalized. `detectProject` and manifest readers surface validation/migration result without implementing Phase 2 evidence selection or Phase 3 pack materialization.</action>
  <acceptance_criteria>valid current project tests retain their result; invalid/old/newer/extension cases are deterministic and no project pack or detection evidence workflow is introduced.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/project.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, or an unregistered extension changes project detection</fails_when>
  </verify>
  <reversibility rating="costly">manifest acceptance is tightened while user-owned fields outside alpha-AOS ownership remain preserved.</reversibility>
  <done>project manifest core is closed, versioned, and extension-inert without absorbing later phases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository policy files→install planning | committed or local YAML/JSON controls package and harness operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Tampering | catalog/lock loaders | high | mitigate | closed schemas, strict syntax, channel/domain invariants |
| T-01-13 | Elevation | extensions | high | mitigate | unregistered namespaces preserved but excluded from domain results |
</threat_model>

## Artifacts this phase produces

| Kind | Artifact | Contract |
|------|----------|----------|
| Schema | `schemas/stack.schema.json` | closed catalog core + namespaced extensions |
| Schema | `schemas/lock.schema.json` | closed exact stable/candidate lock |
| Schema | `schemas/project-stack.schema.json` | closed versioned manifest |

## Probe coverage notes

- Generic malformed-input security is canon and tracked in STRIDE; authority promotion and extension influence remain phase-specific descriptor-less prohibitions.

<verification>
Catalog, lock, and project focused suites plus `npm run check` pass with repository fixtures and negative matrices.
</verification>

<success_criteria>
- all three primary documents reject ambiguity/unknown core fields before domain use.
- old/newer versions follow D-10 exactly.
- extension data cannot affect planning/apply without a registered adapter.
</success_criteria>

<output>
Create `.planning/phases/01-safe-operation-boundary/01-06-SUMMARY.md` when done.
</output>
