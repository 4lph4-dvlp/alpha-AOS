---
phase: 11-managed-lifecycle-verification
plan: 04
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - .planning/phases/11-managed-lifecycle-verification/evidence/probes/life03-uninstall.mjs
  - .planning/phases/11-managed-lifecycle-verification/evidence/life03-uninstall.txt
  - .planning/phases/11-managed-lifecycle-verification/evidence/probes/life03-project.mjs
  - .planning/phases/11-managed-lifecycle-verification/evidence/life03-project.txt
  - .planning/phases/11-managed-lifecycle-verification/evidence/probes/life03-tty.mjs
  - .planning/phases/11-managed-lifecycle-verification/evidence/life03-tty.txt
  - .planning/phases/11-managed-lifecycle-verification/evidence/LIFE-03-findings.md
autonomous: true
requirements: [LIFE-09]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every LIFE-03 clause is judged separately through the packed CLI (D-01, D-09): preview; remove a project pack; clean an isolated runtime; uninstall one target; uninstall the full managed stack; without removing harness applications; without removing authentication; without removing unrelated user resources."
    - "Previews (`uninstall --target <h>`, `uninstall --all`, `uninstall --all --purge`, `uninstall --project <p>`, `project isolate clean <p>`) are judged by byte fingerprints of the sandbox, not by the absence of an error (D-04)."
    - "`uninstall --target <h>` is judged for each of the five harnesses at sandbox file level (Claude Code included, D-10): managed MCP entries, ECC skills and the owned `alpha-aos-control` skill for that harness, a synthetic authentication sentinel file, a user-authored MCP server entry, a user-authored skill directory, and the other harnesses' files."
    - "Full-stack uninstall is judged by a leftover scan: every target recorded in the sandbox journals by the install that is still present after `uninstall --all --yes --apply` is listed, so an unremoved alpha-AOS-owned file (for example an owned skill) cannot pass silently (D-04)."
    - "Research open question 3 is resolved: both project-pack removal routes are judged — the approve route (`project status` removal digest, then `project approve <p> --plan-digest <removal-digest> --apply`) for 'remove a project pack', and `uninstall --project <p>` for the same clause and for the unrelated-user-resources clause."
    - "The isolated-runtime clean is judged through `project isolate init|sync|clean` in a scratch project, and the unmarked removal of `<state>/runtimes` by `uninstall --all` is recorded as an observation unless a user resource is shown removed."
    - "The interactive `[y/N]` confirmation is exercised through a real pseudo-terminal with util-linux `script` under WSL (answer `n` aborts with no byte change, answer `y` proceeds), instead of being left manual-only; if WSL cannot run it the clause is NOT-OBSERVED and the non-TTY refusal test is cited (06-VALIDATION manual-only row)."
    - "The LIFE-03 findings cite `.planning/debug/ci02-windows-alpha-aos-host-leak.md`: the Windows `~/.alpha-aos` change was a test-isolation leak in test code, fixed by commits 8f17c7e and 7a3ee28 and pinned by Phase 10 regression tests; it names no product requirement violation, and the product's host safety is judged afresh by the probe host guard (D-13)."
    - "`src/` is untouched (D-05)."
  prohibitions:
    - statement: "MUST NOT run any uninstall, project approve, project isolate clean or TTY confirmation step outside a sandbox proven by the containment assertion; the real harness roots, authentication files and `~/.alpha-aos` are never a target."
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT use real credentials or real authentication files as preservation fixtures; authentication sentinels are synthetic files whose content is the fixed string `p11-synthetic-auth-sentinel`, and transcripts record only their hashes."
      category: privacy
      status: resolved
      verification: judgment
    - statement: "MUST NOT present a removal that took an unrelated user file as HOLDS because the file was inside an alpha-AOS directory; unrelated user resources are judged by what the user wrote, not by where it lives (D-09)."
      category: transparency
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/phases/11-managed-lifecycle-verification/evidence/life03-uninstall.txt"
      provides: "Preview, per-target (five harnesses) and full-stack uninstall evidence with preservation and leftover checks"
      contains: "life03.target.claude."
    - path: ".planning/phases/11-managed-lifecycle-verification/evidence/life03-project.txt"
      provides: "Both project-pack removal routes and the isolated-runtime clean"
      contains: "life03.pack.approve-route"
    - path: ".planning/phases/11-managed-lifecycle-verification/evidence/life03-tty.txt"
      provides: "Pseudo-terminal [y/N] confirmation evidence from WSL"
      contains: "life03.tty."
    - path: ".planning/phases/11-managed-lifecycle-verification/evidence/LIFE-03-findings.md"
      provides: "LIFE-03 clause table, oracle audit, D-13 citation, gap candidates"
      contains: "ci02-windows-alpha-aos-host-leak.md"
  key_links:
    - from: "LIFE-03-findings.md"
      to: ".planning/debug/ci02-windows-alpha-aos-host-leak.md"
      via: "D-13 classification citation"
      pattern: "test-isolation leak"
    - from: "life03-project.mjs"
      to: "dist/test/helpers/pack-fixtures.js"
      via: "createDomainFixture builds a project whose evidence selects a pack"
      pattern: "createDomainFixture"
---

<objective>
Produce the LIFE-03 evidence: preview and remove a project pack, clean an isolated runtime, uninstall one target, uninstall the full managed stack, each without removing harness applications, authentication or unrelated user resources.

Purpose: the Phase 6 tests cover one harness (claude) at unit level, one full-stack run without a leftover check, and a project-pack test whose title claims a directory sweep it never asserts. This plan judges every clause on the packed CLI across all five harnesses (D-10) and both removal routes.

Output: three probes and transcripts (one run under WSL for the pseudo-terminal) and `evidence/LIFE-03-findings.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-managed-lifecycle-verification/11-CONTEXT.md
@.planning/phases/11-managed-lifecycle-verification/11-RESEARCH.md
@.planning/phases/11-managed-lifecycle-verification/evidence/README.md
@.planning/phases/11-managed-lifecycle-verification/11-01-SUMMARY.md
@.planning/phases/11-managed-lifecycle-verification/evidence/seed-selfcheck.txt
@.planning/debug/ci02-windows-alpha-aos-host-leak.md
@src/core/uninstall.ts
</context>

<interfaces>
Paths: EV = `.planning/phases/11-managed-lifecycle-verification/evidence`, PROBES = `EV/probes`.

From plan 11-01 (read-only here; do not modify `probe-lib.mjs` or `test/helpers/packed-sandbox.ts`): `newSandbox`, `assertSandboxEnv`, `runCli(transcript, sandbox, args, { label, envOverrides, input, cwd })`, `fingerprintRoots`, `compareFingerprints`, `withHostGuard`, `createTranscript`, `packOnce`; `installPackedRelease`, `seedHarnessPrerequisites`, `harnessPaths(sandbox, harness)` (gsdRoot, eccSkillRoot, mcpConfigPath, piBridgeManifest), `hostNpmCache`, `snapshotTargets`, `describeHostDrift`, `openSandbox`.

Product surfaces:
- `uninstall [--target <h> | --project <path> | --all] [--purge] [--yes|-y] [--apply] [--json]` (src/cli.ts:1389-1469). Without `--apply` prints the plan; with `--apply` and no `--yes`: non-TTY stdin exits 2 with `Non-interactive environment requires --yes to apply uninstall.`; TTY stdin prints `Proceed with uninstall? [y/N] ` and aborts with `Uninstall aborted.` unless the answer is `y` or `yes`. `SemanticPruneDriftError` exits 2.
- src/core/uninstall.ts: `MANAGED_SERVERS = ["context7", "exa", "firecrawl"]`, `MANAGED_SKILLS = ["unified-memory", "documentation-lookup", "deep-research"]` (lines 225-226); per-harness pruners; `--project` scans every file under `<project>/.alpha-aos/` (lines 385-404); a claude owned-skill name `gsd-code-review` at line 345; `--all` removes `<state>/runtimes` (lines 566-569).
- Project pack flow (src/cli.ts:1117-1260): `project approve <p>` (preview, prints the plan digest and the paste-ready command), `project approve <p> --plan-digest <d> --apply` (writes `.alpha-aos/plan.json`), `project sync <p> --apply` (materializes pack targets and receipts), `project status <p> --json` (`{ reconciliation, removals, provenance }`, each removal carrying `removalDigest`), `project approve <p> --plan-digest <removalDigest> --apply` (removes the pack; prints `Removed pack:` and a transaction id). Fixture helper: `createDomainFixture(domain, root)` in `dist/test/helpers/pack-fixtures.js` with domains `web`, `api-data`, `infrastructure`, `agent-ai`, `security`, `scientific`; the working sequence is exercised in test/project-pack-sync.test.ts.
- Isolation (src/cli.ts:1043-1089): `project isolate init <p> --mode project-only --harness codex --trust --apply`, `project isolate sync <p> --apply`, `project isolate clean <p>` (preview: `Would remove generated runtime ...`), `project isolate clean <p> --apply` (`cleanIsolationRuntime`, marker check and journaled removal, src/core/isolation.ts:661-690).
- WSL: util-linux `script` is at `/usr/sbin/script`; WSL node is v26 with npm 12 (checked at planning time). Pseudo-terminal invocation: spawn `script` with arguments `-q`, `-e`, `-c`, `<command line>`, `/dev/null` and the answer (`n` or `y` plus newline) on stdin.

Synthetic preservation fixtures (content is fixed synthetic text, never real credentials):
- Authentication sentinels, content `p11-synthetic-auth-sentinel`: claude `<CLAUDE_CONFIG_DIR>/.credentials.json`, codex `<CODEX_HOME>/auth.json`, antigravity `<home>/.gemini/oauth_creds.json`, pi `<PI_CODING_AGENT_DIR>/auth.json`, hermes `<HERMES_HOME>/auth.json`.
- User MCP server named `p11-user-server` added to each harness MCP config through the packed `renderMcpConfig(harness, <config holding the user server>, lock)` so the managed entries and the user entry coexist in the native format.
- User skill `<eccSkillRoot>/p11-user-skill/SKILL.md` with synthetic content per harness.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Uninstall previews, one-target uninstall for five harnesses, and full-stack uninstall with a leftover scan</name>
  <precondition>EV/seed-selfcheck.txt from plan 11-01 shows `seed.<harness>.mcp HOLDS` for all five harnesses.</precondition>
  <files>.planning/phases/11-managed-lifecycle-verification/evidence/probes/life03-uninstall.mjs, .planning/phases/11-managed-lifecycle-verification/evidence/life03-uninstall.txt</files>
  <read_first>
    src/core/uninstall.ts (whole file: plan, pruners, apply, compensation)
    src/cli.ts (lines 1389-1469)
    src/format.ts (formatUninstallPlan, formatUninstallResult)
    test/uninstall.test.ts (lines 21-104 claude target test, 224-312 pack removal, full stack and purge tests)
    .planning/phases/11-managed-lifecycle-verification/evidence/seed-selfcheck.txt
    .planning/phases/11-managed-lifecycle-verification/evidence/probes/probe-lib.mjs
  </read_first>
  <action>
    Per D-01, D-04, D-09 and D-10, write PROBES/life03-uninstall.mjs; everything runs inside `withHostGuard` with one packed tarball.
    1. Setup (one sandbox, all five harnesses): `installPackedRelease`; `seedHarnessPrerequisites` for each harness with `eccCache: hostNpmCache()`; then `install --target claude,codex,antigravity,pi,hermes --apply --json` so alpha-AOS itself writes and journals its owned files (codex policy, owned skills, claude skill policy, anything not already current). If that install exits non-zero, record check `life03.setup.install` NOT-OBSERVED or VIOLATED with the stderr (environmental versus product, as in the evidence README) and continue with the seeded files only, saying so in a note. Add the synthetic preservation fixtures from the interfaces block to every harness. Record check `life03.setup.fixtures` OBSERVED listing each fixture path (aliased) and its 12-hex hash.
    2. Previews: fingerprint the sandbox home, state and prefix; run `uninstall --target codex`, `uninstall --target codex --json`, `uninstall --all --json`, `uninstall --all --purge --json`; fingerprint after each: checks `life03.preview.target.no-mutation`, `life03.preview.target-json.no-mutation`, `life03.preview.all.no-mutation`, `life03.preview.purge.no-mutation`. Keep the `--all` preview JSON for the leftover scan.
    3. Non-interactive guard: `uninstall --target codex --apply` without `--yes` (stdin is a pipe): check `life03.guard.non-tty-refuses` HOLDS when exit is 2, stderr carries the `--yes` message and the fingerprint is unchanged.
    4. One-target uninstall, in the order hermes, pi, antigravity, codex, claude, each `uninstall --target <h> --yes --apply --json`, fingerprinting before and after. Per harness record: `life03.target.<h>.managed-mcp-removed` (context7, exa and firecrawl entries gone from that harness config, parsed natively: JSON, TOML, YAML), `life03.target.<h>.ecc-removed` (the three ECC SKILL.md files gone from that harness skill root, or OBSERVED `shared root` when another still-installed harness uses the same root, naming it), `life03.target.<h>.owned-removed` (the `alpha-aos-control` SKILL.md for that harness gone; for claude also `alpha-aos-ship`), `life03.target.<h>.auth-kept` (sentinel hash unchanged), `life03.target.<h>.user-mcp-kept` (the `p11-user-server` entry still present and equal to its pre-uninstall value), `life03.target.<h>.user-skill-kept`, `life03.target.<h>.others-untouched` (the other harnesses' fixture and managed files unchanged, excluding the shared `.agents/skills` root already reported). Each check is HOLDS or VIOLATED; VIOLATED details name the paths.
    5. Full stack, in a fresh sandbox set up as in step 1: `uninstall --all --yes --apply --json` then the leftover scan: collect every `files[].target` from every journal the install wrote (read before uninstall), and list those that still exist afterwards and are not a preservation fixture: check `life03.all.leftovers` HOLDS when the list is empty, VIOLATED listing the aliased paths. Also `life03.all.auth-kept`, `life03.all.user-mcp-kept`, `life03.all.user-skill-kept`, `life03.all.journals-kept` (journal files still present without `--purge`). Then `uninstall --all --yes --apply --purge --json`: check `life03.all.purge-removes-state`. Finally `inventory --json` before and after in the same sandbox: check `life03.all.harness-apps-kept` HOLDS when the detected harness command list is identical (the harness binaries live outside the sandbox and are never touched).
    6. Write EV/life03-uninstall.txt.
  </action>
  <verify>
    <automated>npm run build && node .planning/phases/11-managed-lifecycle-verification/evidence/probes/life03-uninstall.mjs && T=.planning/phases/11-managed-lifecycle-verification/evidence/life03-uninstall.txt && grep -q '^host-guard: unchanged' "$T" && for h in claude codex antigravity pi hermes; do test "$(grep -cE "^CHECK life03\.target\.$h\.(managed-mcp-removed|ecc-removed|owned-removed|auth-kept|user-mcp-kept|user-skill-kept|others-untouched) " "$T")" -eq 7 || exit 1; done && grep -qE '^CHECK life03\.all\.leftovers (HOLDS|VIOLATED) ' "$T" && test "$(grep -cE '^CHECK life03\.preview\.[a-z-]+\.no-mutation ' "$T")" -eq 4</automated>
    <fails_when>non-zero exit, no unchanged host guard line, any harness without exactly seven target checks, no leftover check, or not exactly four preview checks</fails_when>
  </verify>
  <acceptance_criteria>
    - EV/life03-uninstall.txt holds 35 `life03.target.*` checks, four preview checks, the non-TTY guard check and the `life03.all.*` checks.
    - No transcript line contains the sentinel string `p11-synthetic-auth-sentinel` (fixtures are reported by hash only).
    - `git status --porcelain -- src` prints nothing.
  </acceptance_criteria>
  <done>Preview, one-target and full-stack uninstall are judged per harness with preservation and leftover evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Both project-pack removal routes and the isolated-runtime clean</name>
  <files>.planning/phases/11-managed-lifecycle-verification/evidence/probes/life03-project.mjs, .planning/phases/11-managed-lifecycle-verification/evidence/life03-project.txt</files>
  <read_first>
    test/project-pack-sync.test.ts (the approve, sync and removal sequence)
    test/helpers/pack-fixtures.ts (createDomainFixture, domain evidence files)
    src/cli.ts (lines 1041-1260 project and isolate routing)
    src/core/uninstall.ts (lines 380-410 project scope, 560-575 runtimes removal)
    src/core/isolation.ts (lines 640-700 cleanIsolationRuntime)
    test/isolation.test.ts (clean tests at lines 144 and 414)
    test/uninstall.test.ts (lines 224-248 pack removal test)
  </read_first>
  <action>
    Per D-01, D-04 and D-09 (research open question 3: judge both routes), write PROBES/life03-project.mjs inside `withHostGuard` with one packed tarball and one sandbox (`installPackedRelease`, codex seeded, `install --target codex --apply --json`).
    1. Approve route: create a scratch project inside the sandbox root with `createDomainFixture` for a domain whose pack materializes files (start with `web`; use the domain test/project-pack-sync.test.ts uses if `web` selects nothing), then `git init` it. Run `project approve <p> --json` to read the plan digest, `project approve <p> --plan-digest <d> --apply --json`, `project sync <p> --apply --json`. Write a user file `<p>/.alpha-aos/p11-user-notes.txt` and a user file beside one materialized pack file (inside a pack-created directory). Fingerprint the project and the sandbox home. Remove the fixture's selecting evidence so the pack becomes STALE, run `project status <p>` and `project status <p> --json`, check `life03.pack.removal-offered` (a removal with a `removalDigest` exists for the pack) and `life03.pack.status-no-mutation` (fingerprints unchanged). Run `project approve <p> --plan-digest <removalDigest> --apply --json`: check `life03.pack.approve-route` HOLDS when every receipted pack target is gone, `life03.pack.user-files-kept` (both user files byte-identical), `life03.pack.manifest-kept` (`.alpha-aos/stack.yaml` or plan artifact that the user approved is still present, if it existed), `life03.pack.empty-dirs-swept` (no empty directory left where only pack files lived; VIOLATED naming them), and `life03.pack.reversible` (the printed transaction id is listed by `rollback --json`).
    2. Uninstall route: set up a second project the same way through `project sync <p2> --apply`, add `<p2>/.alpha-aos/p11-user-notes.txt`, run `uninstall --project <p2> --json` (preview) and check `life03.project.preview-no-mutation`, then `uninstall --project <p2> --yes --apply --json`: check `life03.project.pack-removed` (pack targets gone), `life03.project.user-file-kept` (HOLDS only if the user notes file survived; VIOLATED otherwise), `life03.project.manifest-kept` (the reviewed `.alpha-aos/stack.yaml`, if present, survived).
    3. Isolated runtime: a third scratch project, `project isolate init <p3> --mode project-only --harness codex --trust --apply`, `project isolate sync <p3> --apply --json` (record the runtime root), fingerprint the sandbox state and the project, `project isolate clean <p3>` (preview): check `life03.runtime.preview-no-mutation`; `project isolate clean <p3> --apply --json`: checks `life03.runtime.clean-removes` (runtime root gone), `life03.runtime.project-kept` (project tree including `.alpha-aos/stack.yaml` unchanged), `life03.runtime.journaled` (a new journal lists the removed files). Then observation only: create an unmarked directory `<state>/runtimes/p11-unmarked/keep.txt`, run `uninstall --all --yes --apply --json`, and record `life03.runtime.uninstall-all-unmarked` OBSERVED stating whether the unmarked directory was removed.
    4. Write EV/life03-project.txt.
  </action>
  <verify>
    <automated>npm run build && node .planning/phases/11-managed-lifecycle-verification/evidence/probes/life03-project.mjs && T=.planning/phases/11-managed-lifecycle-verification/evidence/life03-project.txt && grep -q '^host-guard: unchanged' "$T" && for c in pack.removal-offered pack.approve-route pack.user-files-kept pack.empty-dirs-swept project.preview-no-mutation project.pack-removed project.user-file-kept runtime.preview-no-mutation runtime.clean-removes runtime.project-kept runtime.journaled; do grep -qE "^CHECK life03\.$c (HOLDS|VIOLATED|NOT-OBSERVED|OBSERVED) " "$T" || exit 1; done</automated>
    <fails_when>non-zero exit, no unchanged host guard line, or any of the eleven named checks is missing</fails_when>
  </verify>
  <acceptance_criteria>
    - EV/life03-project.txt shows the full approve-route sequence including the removal digest printed by `project status` and consumed by `project approve`.
    - If the domain fixture selected no pack, the transcript records `life03.pack.approve-route` NOT-OBSERVED with the plan output, and the SUMMARY names the domains tried.
    - `git status --porcelain -- src` prints nothing.
  </acceptance_criteria>
  <done>Both project-pack removal routes and the isolated-runtime clean are judged on the packed CLI with user-file preservation checks.</done>
</task>

<task type="auto">
  <name>Task 3: Pseudo-terminal [y/N] confirmation under WSL, and the LIFE-03 findings</name>
  <precondition>`wsl.exe -e sh -c 'command -v script && node --version'` prints a `script` path and a Node major of 24 or higher.</precondition>
  <files>.planning/phases/11-managed-lifecycle-verification/evidence/probes/life03-tty.mjs, .planning/phases/11-managed-lifecycle-verification/evidence/life03-tty.txt, .planning/phases/11-managed-lifecycle-verification/evidence/LIFE-03-findings.md</files>
  <read_first>
    src/cli.ts (lines 1424-1452 confirmation guard)
    .planning/milestones/v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VALIDATION.md (manual-only TTY row)
    .planning/debug/ci02-windows-alpha-aos-host-leak.md (classification and fix commits)
    .planning/phases/11-managed-lifecycle-verification/evidence/life03-uninstall.txt
    .planning/phases/11-managed-lifecycle-verification/evidence/life03-project.txt
    .planning/phases/11-managed-lifecycle-verification/evidence/suites-local.txt
    .planning/phases/11-managed-lifecycle-verification/evidence/ci-35818198049-lifelines.txt
    .planning/phases/11-managed-lifecycle-verification/evidence/README.md (findings template)
  </read_first>
  <action>
    Per D-01, D-04, D-09 and D-13:
    1. Write PROBES/life03-tty.mjs to run under WSL Linux node. Inside `withHostGuard` (its host guard targets resolve against the WSL home, which the note in the transcript states), pack once from the repository path, open a sandbox under the Linux `os.tmpdir()`, `installPackedRelease` with the WSL npm cache, and create one journaled managed file with `codex-policy sync --apply` (no ECC or network needed). Fingerprint the sandbox home and state. Spawn `script` with the arguments in the interfaces block, the command line being the sandbox CLI `uninstall --target codex --apply` (no `--yes`), the sandbox env (checked with `assertSandboxEnv` first), and stdin `n` plus newline: check `life03.tty.prompt-shown` (stdout contains `Proceed with uninstall? [y/N]`), `life03.tty.no-aborts` (stdout contains `Uninstall aborted.` and the fingerprint is unchanged). Repeat with stdin `y` plus newline: check `life03.tty.yes-proceeds` (exit 0 and the codex policy file is gone). Write EV/life03-tty.txt (the WSL run writes into the shared checkout path, so the file appears in the Windows work tree).
    2. Write EV/LIFE-03-findings.md from the README template. Contract verbatim from .planning/milestones/v0.1.0-REQUIREMENTS.md:79. Clause rows, each citing CHECK ids plus the relevant Phase 6 test titles and CI job lines: preview (uninstall and isolate previews); remove a project pack (approve route and uninstall route, both); clean an isolated runtime; uninstall one target (five harness rows); uninstall the full managed stack (leftover scan); without removing harness applications; without removing authentication; without removing unrelated user resources (user MCP entries, user skills, user files under `.alpha-aos/`); interactive confirmation (the TTY checks, or NOT-OBSERVED citing `non-interactive environment requires --yes to apply uninstall (LIFE-03, D-02)`). Oracle audit (D-04): `project pack removal removes sidecars and sweeps empty directories` asserts only that the file is gone (test/uninstall.test.ts:245-247); the packed lifecycle full-stack uninstall asserts named removals and never scans for leftovers; `target-level uninstall ...` covers claude only at unit level. Add a `## Host safety (D-13)` paragraph citing `.planning/debug/ci02-windows-alpha-aos-host-leak.md` (`classification: test-isolation leak`), fix commits 8f17c7e and 7a3ee28, the Phase 10 regression tests, and the host-guard results of every LIFE-03 transcript as the fresh product judgment. Name the core clause (remove alpha-AOS-managed state without removing unrelated user resources), give the provisional verdict per D-02, and write one gap candidate `LIFE-03/C<k>` per failing clause with its reproduction; when one defect fails a LIFE-03 clause and a LIFE-04 clause (for example a user-authored entry removed), keep it here as its own candidate and cross-reference the LIFE-04 key rather than merging (adjacency edge).
  </action>
  <verify>
    <automated>npm run build && wsl.exe --cd "$(cygpath -w "$PWD")" -e sh -c 'node .planning/phases/11-managed-lifecycle-verification/evidence/probes/life03-tty.mjs' && T=.planning/phases/11-managed-lifecycle-verification/evidence/life03-tty.txt && grep -q '^platform: linux' "$T" && grep -q '^host-guard: unchanged' "$T" && for c in prompt-shown no-aborts yes-proceeds; do grep -qE "^CHECK life03\.tty\.$c (HOLDS|VIOLATED|NOT-OBSERVED) " "$T" || exit 1; done</automated>
    <fails_when>non-zero exit, the transcript was not produced on linux, no unchanged host guard line, or any of the three TTY checks is missing</fails_when>
    <automated>F=.planning/phases/11-managed-lifecycle-verification/evidence/LIFE-03-findings.md && grep -q '^## Provisional verdict' "$F" && grep -q 'ci02-windows-alpha-aos-host-leak.md' "$F" && grep -q '8f17c7e' "$F" && grep -q '^## Host safety (D-13)' "$F" && test "$(grep -cE 'life03\.(target|all|preview|pack|project|runtime|tty)\.' "$F")" -ge 15 && test -z "$(git status --porcelain -- src)"</automated>
    <fails_when>verdict heading, D-13 citation, fix commit or host-safety heading missing, fewer than fifteen check citations, or src/ changed</fails_when>
  </verify>
  <acceptance_criteria>
    - EV/life03-tty.txt header shows `platform: linux` and the three TTY checks.
    - LIFE-03-findings.md has one clause row per clause named in action step 2 and five one-target rows.
    - Every VIOLATED check across the three LIFE-03 transcripts maps to a `LIFE-03/C<k>` candidate or to an explicit argument that it does not fail the LIFE-03 text.
    - `git status --porcelain -- src` prints nothing.
  </acceptance_criteria>
  <done>The interactive confirmation is evidenced through a real pseudo-terminal, and LIFE-03 has a complete findings file with the D-13 citation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uninstall probes to host | `uninstall --all --apply` and `--purge` are the most destructive commands in the product; a missing override would remove real managed state |
| WSL probe to Linux home | The TTY probe runs under the WSL user's home |
| preservation fixtures to transcripts | Synthetic auth sentinels must never look like, or be, real credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-14 | Tampering | life03 uninstall steps | critical | mitigate | Every step goes through probe-lib `runCli` (containment assertion over twelve location names) or calls `assertSandboxEnv` before spawning `script`; host guard before and after each probe, exit 3 on drift |
| T-11-15 | Information disclosure | auth sentinels | medium | mitigate | Fixed synthetic content, reported by hash only; acceptance greps the transcript for the sentinel string |
| T-11-16 | Tampering | WSL home | medium | mitigate | Sandbox under the Linux tmpdir; host guard over the WSL home's managed targets |
| T-11-17 | Repudiation | findings | low | accept | Clause-to-check mapping is re-derivable from the transcripts by the D-17 verifier |
</threat_model>

<verification>
- The three LIFE-03 transcripts end with a `RESULT:` line and an unchanged host guard.
- LIFE-03-findings.md passes its grep gate.
- `git status --porcelain -- src` prints nothing.
</verification>

<success_criteria>
- Every LIFE-03 clause carries a HOLDS row or a named gap candidate with a reproduction (ROADMAP Phase 11 success criteria 1 and 3 for LIFE-03).
- ROADMAP Phase 11 success criterion 2 holds for LIFE-03: the verdict cites the Phase 10 classification and does not name a product violation for the test-isolation leak.
</success_criteria>

## Artifacts this phase produces

- Probes `PROBES/life03-uninstall.mjs`, `PROBES/life03-project.mjs`, `PROBES/life03-tty.mjs` (run under WSL).
- Transcripts `EV/life03-uninstall.txt`, `EV/life03-project.txt`, `EV/life03-tty.txt`.
- Findings `EV/LIFE-03-findings.md` with a `## Host safety (D-13)` section.
- Check ids `life03.setup.*`, `life03.preview.*`, `life03.guard.non-tty-refuses`, `life03.target.<harness>.*`, `life03.all.*`, `life03.pack.*`, `life03.project.*`, `life03.runtime.*`, `life03.tty.*`; gap candidate keys `LIFE-03/C<k>`.
- Synthetic fixture names `p11-synthetic-auth-sentinel`, `p11-user-server`, `p11-user-skill`, `p11-user-notes.txt`, `p11-unmarked`.

<output>
Create `.planning/phases/11-managed-lifecycle-verification/11-04-SUMMARY.md` when done. Include the per-harness one-target table, the leftover list, both project-route outcomes, the runtime clean outcome, the TTY outcome, and the provisional LIFE-03 verdict.
</output>
