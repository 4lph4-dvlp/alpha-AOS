---
phase: 01-safe-operation-boundary
plan: 07
type: execute
wave: 1
depends_on: [01-02]
files_modified:
  - src/core/path-boundary.ts
  - test/path-boundary.test.ts
autonomous: true
requirements: [SAFE-02]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01: an unprovable boundary/reparse/filesystem capability returns a stable refusal while inspection remains available and no override exists."
    - "D-02: PathProof covers targets, state, journals, snapshots, temp files, install sources, and package root before mutation."
    - "D-03: known links require configured and canonical containment and ancestor identity stability immediately before apply."
  artifacts:
    - path: src/core/path-boundary.ts
      provides: "Cross-platform PathProof creation, capability result, and immediate recheck"
      exports: [provePathBoundary, proveOperationPaths, recheckPathProof]
  key_links:
    - from: src/core/path-boundary.ts
      to: test/path-boundary.test.ts
      via: "real symlink/junction/reparse and parent-swap fixtures"
      pattern: "PathProof|recheck"
  prohibitions:
    - statement: "Filesystem support MUST NOT be reported as proven when the platform probe could not classify a reparse point or verify the required durability/identity semantics."
      requirement_id: SAFE-02
      category: transparency
      status: unverified
      flagged: true
---

<objective>
lexical containment을 configured+canonical component proof와 mutation 직전 recheck로 강화한다.

Purpose: symlink, junction, reparse, alias, parent swap이 allowed root를 우회하지 못하게 한다.
Output: PathProof value object, capability diagnostics, operation-wide proof set.
</objective>

<execution_context>
@C:/Users/alpha/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safe-operation-boundary/01-CONTEXT.md
@.planning/phases/01-safe-operation-boundary/01-RESEARCH.md
@.planning/phases/01-safe-operation-boundary/01-PATTERNS.md
@.planning/phases/01-safe-operation-boundary/01-02-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One target의 configured/canonical containment tracer</name>
  <files>src/core/path-boundary.ts, test/path-boundary.test.ts</files>
  <read_first>
    src/core/transaction.ts
    src/core/paths.ts
    test/path-boundary.test.ts
    test/transaction.test.ts
  </read_first>
  <behavior>
    - existing and not-yet-existing target paths resolve via component lstat and nearest existing ancestor realpath.
    - configured and canonical relations must both remain under the same allowed root.
    - known in-root symlink works, escaping symlink refuses, unknown reparse refuses with stable code.
  </behavior>
  <action>D-01/D-03에 따라 `PathProof`, `PathComponentIdentity`, `FilesystemCapability`, `PathBoundaryError`와 `provePathBoundary`를 구현한다. Normalize absolute configured path, walk each existing component with lstat/realpath, record device/inode or supported Windows identity/reparse classification, append non-existing suffix to canonical ancestor, and require configured plus canonical containment. Windows probe가 권한 없이 unknown이면 unsupported result를 반환하고 mutation override option을 정의하지 않는다.</action>
  <acceptance_criteria>inside regular path and inside known link pass; escape link, unknown reparse, configured-inside/canonical-outside cases return stable codes and preserve outside sentinel.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/path-boundary.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, or an outside sentinel hash changes</fails_when>
  </verify>
  <reversibility rating="reversible">new utility is additive and performs read-only proof operations.</reversibility>
  <done>one target is safely allowed or refused from actual filesystem evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Operation-wide proof set와 immediate recheck 완성</name>
  <files>src/core/path-boundary.ts, test/path-boundary.test.ts</files>
  <read_first>
    src/core/path-boundary.ts
    src/core/install.ts
    src/core/transaction.ts
    src/core/ecc-skills.ts
    test/path-boundary.test.ts
  </read_first>
  <behavior>
    - proveOperationPaths requires named targets/state/journal/snapshot/temp/source/package-root roles and refuses incomplete sets.
    - recheck compares canonical path and all recorded ancestor identities immediately before rename/remove/process mutation.
    - changed parent or support capability yields refusal before target bytes change.
  </behavior>
  <action>D-02/D-03에 따라 `OperationPathInput` role union과 `OperationPathProofSet`을 추가한다. Required roles를 operation kind별로 선언하고 unknown/unplanned path를 apply 중 계산할 수 없게 immutable proof set을 반환한다. `recheckPathProof`는 lock 획득 뒤 및 각 rename/remove/external package step 직전에 original ancestor identity/canonical endpoint를 다시 읽어 mismatch code를 반환한다.</action>
  <acceptance_criteria>missing role, newly discovered apply path, parent swap, replaced ancestor cases all refuse before mutation; unchanged full proof set rechecks successfully.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/path-boundary.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, or any parent-swap case writes outside its allowed root</fails_when>
  </verify>
  <reversibility rating="reversible">read-only proof contracts can be evolved before release.</reversibility>
  <done>all D-02 path roles are explicit and mutation-time identity drift is detectable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| configured path→canonical object | aliases and reparse semantics can redirect a nominal child outside ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-14 | Tampering/Elevation | path-boundary.ts | critical | mitigate | component proof, dual containment, identity recheck, outside sentinel |
| T-01-15 | Repudiation | filesystem support reporting | medium | mitigate | typed capability/refusal evidence and explicit not-run fixture reporting |
</threat_model>

## Artifacts this phase produces

| Kind | Artifact | Contract |
|------|----------|----------|
| New file | `src/core/path-boundary.ts` | `provePathBoundary`, `proveOperationPaths`, `recheckPathProof` |
| New types | `PathProof`, `PathComponentIdentity`, `FilesystemCapability`, `OperationPathProofSet` | immutable canonical evidence |

## Probe coverage notes

- Generic path traversal is canon and covered in STRIDE; unproven-support advertising is retained as the product-specific descriptor-less prohibition.

<verification>
Focused path-boundary tests pass on supported host cases; unavailable privileged probes emit not-run evidence and never count as pass.
</verification>

<success_criteria>
- dual configured/canonical containment is enforced.
- every D-02 role can be proven before mutation.
- parent identity drift refuses immediately before mutation.
</success_criteria>

<output>
Create `.planning/phases/01-safe-operation-boundary/01-07-SUMMARY.md` when done.
</output>
