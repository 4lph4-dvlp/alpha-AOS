---
phase: 03-transactional-project-packs-and-native-optional-use
plan: 22
type: execute
gap_closure: true
wave: 4
depends_on: [03-20]
files_modified:
  - catalog/canaries.yaml
  - src/core/canary.ts
  - test/canary.test.ts
  - test/capability-oracle.test.ts
autonomous: true
requirements: [CAPA-05]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "catalog/canaries.yaml declares PACK_EXERCISE_FRONTEND_A11Y for Codex as the active verification anchor while preserving Claude compatibility."
    - "The representative project pack (WEB_REACT) materializes skill files into .agents/skills/ for Codex with unchanged locked skill bytes from catalog/stack.lock.json."
    - "The representative intent prompt names neither the pack name (WEB_REACT) nor any skill/tool name, ensuring native intent-driven selection."
    - "The exercised pack maintains project-local provenance in .alpha-aos/receipts/ and is proven absent and undiscoverable outside the project scope via paired negative discovery (CAPA-06)."
  artifacts:
    - path: catalog/canaries.yaml
      provides: "Active Codex declaration for PACK_EXERCISE_FRONTEND_A11Y"
    - path: src/core/canary.ts
      provides: "Codex representative pack exercise runner integration"
    - path: test/canary.test.ts
      provides: "Canary catalog and pack exercise selection assertions for Codex"
    - path: test/capability-oracle.test.ts
      provides: "Paired inside-project discovery, locked-byte integrity, and outside-project absence tests"
  key_links:
    - from: catalog/canaries.yaml
      to: src/core/project-pack-sync.ts
      via: "PACK_EXERCISE_FRONTEND_A11Y targets WEB_REACT skills materialized under .agents/skills"
      pattern: "PACK_EXERCISE_FRONTEND_A11Y|WEB_REACT"
    - from: src/core/canary.ts
      to: src/core/capability-ledger.ts
      via: "paired evidence unit joins positive in-project pack exercise with negative out-of-project control"
      pattern: "pairEvidence|completeness"
  prohibitions:
    - statement: "The pack exercise prompt must never name the pack (WEB_REACT) or the skill (frontend-design / frontend-a11y)."
      status: unverified
      flagged: true
    - statement: "Materializing or exercising a project pack must not modify locked skill source bytes or inject unreviewed headers."
      status: unverified
      flagged: true
    - statement: "A positive pack exercise must not be reported as complete without a paired outside-project negative discovery control."
      status: unverified
      flagged: true
---

<objective>
Close gap G-03-3 by declaring active non-Claude (Codex) representative pack exercise in the canary catalog, verifying intent-matched pack selection and project-local provenance in `.agents/skills`, and asserting paired out-of-scope absence.

Purpose: Under D-17, Claude Code is excluded from active development and release gates. `PACK_EXERCISE_FRONTEND_A11Y` in `catalog/canaries.yaml` was previously declared for Claude only, and no active non-Claude pack exercise evidence was recorded. Active non-Claude representative pack exercise must be declared for Codex, integrated with the pack sync engine, and validated with paired inside/outside scope evidence.
Output: Active Codex pack exercise declaration in `catalog/canaries.yaml`, test runner integration in `src/core/canary.ts`, and comprehensive provenance/paired-absence tests in `test/canary.test.ts` and `test/capability-oracle.test.ts`.
</objective>

<execution_context>
@C:/Users/alpha/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-CONTEXT.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-UAT.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-VERIFICATION.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-01-SUMMARY.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-04-SUMMARY.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-11-SUMMARY.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-20-PLAN.md
@catalog/canaries.yaml
@catalog/packs/web.yaml
@src/core/canary.ts
@src/core/project-pack-sync.ts
@src/core/project-plan.ts
@test/canary.test.ts
@test/capability-oracle.test.ts
</context>

<interfaces>
- `PACK_EXERCISE_FRONTEND_A11Y` in `catalog/canaries.yaml`: intent prompt for web accessibility work, mapped to capability `CAPA-05`.
- `PROJECT_SKILL_ROOTS.codex = ".agents/skills"`: project-local root where skills are materialized.
- `applyProjectPackSync(plan, session)`: atomic transaction writing skills and receipts under `.alpha-aos/receipts/`.
- `pairEvidence(positive, negative)`: assembles complete evidence unit pairing in-project exercise with out-of-project control.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declare active Codex for PACK_EXERCISE_FRONTEND_A11Y in catalog/canaries.yaml and update canary runner selection</name>
  <files>catalog/canaries.yaml, src/core/canary.ts, test/canary.test.ts</files>
  <read_first>
    - .planning/phases/03-transactional-project-packs-and-native-optional-use/03-UAT.md — Item 3: CAPA-05 Representative Pack Exercise diagnosis.
    - catalog/canaries.yaml — inspect `PACK_EXERCISE_FRONTEND_A11Y` declaration at line 80.
    - src/core/canary.ts — inspect `selectCanaries` and `canaryPromptArgs`.
  </read_first>
  <action>
In `catalog/canaries.yaml`:
- Add `codex` to `harnesses` for `PACK_EXERCISE_FRONTEND_A11Y` (preserving `claude`).
- Verify that the declared prompt text contains zero occurrences of the pack name (`WEB_REACT`) or the skill name (`frontend-design`, `frontend-a11y`).

In `src/core/canary.ts`:
- Ensure `selectCanaries` resolves `PACK_EXERCISE_FRONTEND_A11Y` when `--harness codex` is specified or when running full canary sweeps.
- Ensure `canaryPromptArgs` derives the invocation vector for Codex with the accessibility intent prompt.

In `test/canary.test.ts`:
- Assert that `PACK_EXERCISE_FRONTEND_A11Y` in `catalog/canaries.yaml` declares `codex` and `claude`.
- Assert that prompt hint checking (`findPromptHints`) passes with zero forbidden tokens.
- Add test verifying that selecting `--capability CAPA-05 --harness codex` selects `PACK_EXERCISE_FRONTEND_A11Y`.
  </action>
  <acceptance_criteria>
    - `catalog/canaries.yaml` declares `codex` for `PACK_EXERCISE_FRONTEND_A11Y`.
    - Prompt contains no hints and passes `findPromptHints`.
    - Codex canary selector returns `PACK_EXERCISE_FRONTEND_A11Y` for CAPA-05.
    - Claude declarations remain intact.
  </acceptance_criteria>
  <verify>
    <automated>npm run check &amp;&amp; npm run build &amp;&amp; node scripts/run-tests.mjs --files dist/test/canary.test.js</automated>
    <fails_when>The command exits nonzero, type checking fails, or canary selection assertions fail.</fails_when>
  </verify>
  <done>Active Codex pack exercise is declared in catalog and integrated into canary selection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove representative pack materialization, project-local .agents/skills provenance, and paired CAPA-06 absence</name>
  <files>src/core/canary.ts, test/canary.test.ts, test/capability-oracle.test.ts</files>
  <read_first>
    - .planning/phases/03-transactional-project-packs-and-native-optional-use/03-VERIFICATION.md — Truth 4 (CAPA-05 / G-03-3).
    - src/core/project-pack-sync.ts — `applyProjectPackSync`, receipt generation, and `.agents/skills` target paths.
    - test/capability-oracle.test.ts — existing paired discovery and ancestor freedom assertions.
  </read_first>
  <action>
In `test/canary.test.ts` and `test/capability-oracle.test.ts`:
1. Build an offline fixture verifying the end-to-end representative pack exercise lifecycle on Codex:
   - Create a synthetic project root matching `WEB_REACT` evidence (e.g. `package.json` with React dependency).
   - Generate reviewed plan and apply pack sync using `applyProjectPackSync`.
   - Verify that the pack's skill is written into `.agents/skills/<skill>/SKILL.md` (the canonical project-local skill root for Codex).
   - Verify that the written skill bytes match the pinned `sourceSha256` from `catalog/stack.lock.json` with zero modifications (preserving D-07 and locked identity).
   - Verify that `.alpha-aos/receipts/<pack>.json` records project-local provenance including `harness: "codex"`, `sourceHash`, and exact target path.
2. Verify paired discovery and absence boundary:
   - Run positive discovery on Codex in the project root: asserts the skill is discovered in `.agents/skills/`.
   - Run negative discovery on Codex in an ancestor-free non-project directory: asserts the skill is NOT discovered.
   - Join the positive and negative proofs via `pairEvidence`: asserts `completeness === "COMPLETE"`, satisfying both CAPA-05 provenance and CAPA-06 absence bounds.
3. Wire opt-in live test structure for Codex pack exercise under `ALPHA_AOS_LIVE_CANARY='1'` with `codex login status` precondition.
  </action>
  <acceptance_criteria>
    - Representative pack materializes into `.agents/skills/` with unchanged locked bytes.
    - `.alpha-aos/receipts/` records durable project-local provenance for Codex.
    - Paired evidence unit proves in-scope discovery and out-of-scope absence with `completeness === "COMPLETE"`.
    - All tests run offline and pass deterministically.
  </acceptance_criteria>
  <verify>
    <automated>npm run check &amp;&amp; npm run build &amp;&amp; node scripts/run-tests.mjs --files dist/test/canary.test.js dist/test/capability-oracle.test.js</automated>
    <fails_when>The command exits nonzero, any pack sync or paired discovery test fails, or locked bytes are modified.</fails_when>
  </verify>
  <done>G-03-3 is closed with active Codex representative pack exercise, project-local provenance, and paired absence verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Project pack sync → `.agents/skills/` | Synced skill files must stay strictly inside the project root and match locked hashes. |
| Inside project → Outside project | Capabilities installed for a project must remain undiscoverable and uninvokable outside it. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-151 | Elevation of Privilege | Ambient skill leakage outside project root | high | mitigate | Verify ancestor freedom and assert paired negative discovery outside project directory. |
| T-03-152 | Tampering | Modification of third-party skill bytes | high | mitigate | Assert `targetSha256 === sourceSha256` under `PLAN_RENDERER_ID = "ecc-skill/identity"`. |
| T-03-153 | Repudiation | Unproven provenance claim | medium | mitigate | Store atomic receipts in `.alpha-aos/receipts/` recording exact source hashes and target paths. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit
- 03-UAT.md Item 3: Resolves missing active non-Claude pack exercise declaration and native selection proofs.
- 03-VERIFICATION.md: Truth 4 (CAPA-05) satisfied on active non-Claude surface (Codex).
- D-17: Replaces Claude with Codex as active anchor for CAPA-05 while preserving Claude compatibility residue.
</source_audit>

<verification>
- Automated check: `npm run check && npm run build && node scripts/run-tests.mjs --files dist/test/canary.test.js dist/test/capability-oracle.test.js`
- Acceptance: Codex is declared, pack materializes into `.agents/skills`, receipts record provenance, and paired absence is verified.
</verification>

<success_criteria>
- Codex is declared for `PACK_EXERCISE_FRONTEND_A11Y`.
- Representative pack materializes into `.agents/skills` with exact locked hashes.
- Durable provenance is established in project receipts.
- Paired negative control confirms absence outside the project (CAPA-06).
</success_criteria>

<output>
Create `.planning/phases/03-transactional-project-packs-and-native-optional-use/03-22-SUMMARY.md` when execution completes.
</output>
