---
phase: 11-managed-lifecycle-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/helpers/packed-sandbox.ts
  - test/tarball-fixture.test.ts
  - .planning/phases/11-managed-lifecycle-verification/evidence/README.md
  - .planning/phases/11-managed-lifecycle-verification/evidence/probes/probe-lib.mjs
  - .planning/phases/11-managed-lifecycle-verification/evidence/probes/life01-reconcile.mjs
  - .planning/phases/11-managed-lifecycle-verification/evidence/probes/seed-selfcheck.mjs
  - .planning/phases/11-managed-lifecycle-verification/evidence/probes/suites-and-ci.mjs
  - .planning/phases/11-managed-lifecycle-verification/evidence/life01-codex-reconcile.txt
  - .planning/phases/11-managed-lifecycle-verification/evidence/seed-selfcheck.txt
  - .planning/phases/11-managed-lifecycle-verification/evidence/suites-local.txt
  - .planning/phases/11-managed-lifecycle-verification/evidence/ci-35818198049-lifelines.txt
  - .planning/milestones/v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md
autonomous: true
requirements: [LIFE-09]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Tracer (D-01, D-17): after `npm run build`, `node .planning/phases/11-managed-lifecycle-verification/evidence/probes/life01-reconcile.mjs` packs the repository, installs the tarball into an isolated sandbox with the `createIsolatedSandbox` env shape, drives the packed CLI through initial `install --target codex --apply`, a dry-run `install --target codex`, and a second `install --target codex --apply`, and writes `evidence/life01-codex-reconcile.txt` whose CHECK lines a re-run reproduces line-for-line."
    - "Research open question 1 is resolved: `alpha-aos install` without `--apply` is treated as 'the plan' for LIFE-01 because it is the stateful plan that prints `CURRENT` step lines (src/cli.ts:495-500); the static `alpha-aos plan` table is captured in the same transcript as an OBSERVED check, and is raised as a gap candidate only if README.md or docs/ direct users to `alpha-aos plan` to see reconcile state (D-09)."
    - "Every probe CLI step runs only after a containment assertion proves HOME, USERPROFILE, APPDATA, LOCALAPPDATA, XDG_CONFIG_HOME, ALPHA_AOS_STATE_DIR, CODEX_HOME, CLAUDE_CONFIG_DIR, ANTIGRAVITY_CONFIG_DIR, PI_CODING_AGENT_DIR, HERMES_HOME and npm_config_prefix all resolve inside the sandbox root, and every probe fingerprints the real host guard targets before and after and exits 3 on any drift (D-13 host safety judged afresh)."
    - "Transcripts carry the command (aliased), exit code, duration and verbatim aliased stdout and stderr; they never contain file contents, environment variable values, or an unaliased host home path in either plain or JSON-escaped form."
    - "The packed lifecycle test keeps its Phase 10 host-immutability oracle: the `hostMutationTargets` block is byte-identical to commit 35d4c15 and the line `assert.deepEqual(afterHost, beforeHost, describeHostDrift(beforeManifests, afterManifests));` is unchanged."
    - "The packed lifecycle test additionally asserts (D-12, pass-only) that a dry-run `install --target codex` lists only `CURRENT` step lines and does not change any byte under the sandbox home, state and prefix, and that the second `--apply` changes no sandbox byte; each assertion is committed only if the tracer transcript recorded the matching check as HOLDS."
    - "`evidence/suites-local.txt` records the six Phase 6 suites (`lifecycle`, `status`, `recovery-receipt`, `repair`, `rollback`, `uninstall`) and the supporting suites run on this host with a summary line showing fail 0 and equal tests/pass counts, and `evidence/ci-35818198049-lifelines.txt` records, for jobs 107044319989 (ubuntu), 107044319988 (macOS) and 107044319963 (windows), the CI log line for every Phase 6 test title plus a staleness diff proving `src`, `scripts`, `catalog`, `package.json`, `package-lock.json` and every cited suite file are byte-identical between 35d4c15 and HEAD (D-01, D-03)."
    - "`06-VERIFICATION.md` exists beside the archived Phase 6 artifacts with the report skeleton (frontmatter, verdict matrix of eight LIFE rows, Phase 13 gating section, evidence records, per-requirement clause tables, gap register, leads, observations, re-run index) and its LIFE-01 section already carries the Codex clause rows citing transcript CHECK ids (tracer end-to-end, D-01, D-17)."
    - "`src/` is untouched: `git status --porcelain -- src` is empty and `git diff --quiet 35d4c15 HEAD -- src` exits 0 (D-05)."
    - "Ordering edge (LIFE-09): CHECK lines are written in step execution order, each check id appears exactly once per transcript, and check details carry no timing, temp path or hash that varies between runs, so the D-17 re-run compares CHECK lines line-for-line."
    - "Empty edge (LIFE-09): a probe whose step could not run for an environmental reason records `NOT-OBSERVED` with the verbatim output, never `HOLDS`; a transcript with zero CHECK lines is a probe error (exit 1), not a completed run."
  prohibitions:
    - statement: "MUST NOT run any probe step with the developer's real home, real `~/.alpha-aos`, real harness configuration roots or the real repository tree as a mutation target; every `--apply` step runs inside a sandbox proven by the containment assertion."
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT loosen, narrow or reorder the Phase 10 packed-lifecycle host-immutability oracle while moving helpers out of test/tarball-fixture.test.ts."
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT write file contents, credential values, or environment variable values into any evidence transcript, findings file or report; evidence is path, kind, size, hash and exit code only (CONTEXT specifics)."
      category: privacy
      status: resolved
      verification: judgment
    - statement: "MUST NOT modify anything under `src/` (D-05) and MUST NOT commit a failing test (D-12)."
      category: safety
      status: resolved
      verification: judgment
  artifacts:
    - path: "test/helpers/packed-sandbox.ts"
      provides: "Context-free packed sandbox, packed release install, five-harness prerequisite seeding, byte fingerprint and drift description shared by tarball-fixture and every Phase 11 probe"
      exports: ["openSandbox", "createIsolatedSandbox", "packRelease", "installPackedRelease", "seedHarnessPrerequisites", "harnessPaths", "hostNpmCache", "fingerprintManifest", "snapshotTargets", "describeHostDrift", "vanishedEntryCount", "parseJsonResult", "runInstalledCli", "npmInvocation", "commandResult"]
    - path: ".planning/phases/11-managed-lifecycle-verification/evidence/probes/probe-lib.mjs"
      provides: "Transcript writer, CHECK vocabulary, path aliasing, sandbox containment assertion, host guard"
      contains: "hostGuardTargets"
    - path: ".planning/phases/11-managed-lifecycle-verification/evidence/life01-codex-reconcile.txt"
      provides: "Tracer transcript: Codex idempotent reconcile through the packed CLI"
      contains: "CHECK life01.codex.second-apply-noop"
    - path: ".planning/milestones/v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md"
      provides: "Report skeleton with LIFE-01 Codex rows; completed by plan 11-07"
      contains: "life01-codex-reconcile.txt"
  key_links:
    - from: "test/tarball-fixture.test.ts"
      to: "test/helpers/packed-sandbox.ts"
      via: "named imports replace the moved definitions"
      pattern: "from \"./helpers/packed-sandbox.js\""
    - from: ".planning/phases/11-managed-lifecycle-verification/evidence/probes/probe-lib.mjs"
      to: "dist/test/helpers/packed-sandbox.js"
      via: "dynamic import resolved five directories above the probe file"
      pattern: "dist/test/helpers/packed-sandbox.js"
    - from: "06-VERIFICATION.md LIFE-01 section"
      to: "evidence/life01-codex-reconcile.txt"
      via: "clause rows cite transcript CHECK ids"
      pattern: "life01\\.codex\\."
---

<objective>
Build the Phase 11 evidence harness and prove it end-to-end on one lifecycle promise: LIFE-01 idempotent reconcile for Codex, driven through the packed CLI in an isolated sandbox, captured as a re-runnable transcript, and cited from the first rows of the Phase 6 verification report.

Purpose: every later evidence plan (11-02..11-06) reuses this harness, so its sandbox isolation, host guard, aliasing and transcript format must be proven before five plans run it in parallel. The same plan re-runs the Phase 6 suites locally and pulls their POSIX results from CI run 35818198049 (D-01, D-03), strengthens the one LIFE-01 oracle that CI already runs (D-04, D-12), and seeds the report skeleton that plan 11-07 completes.

Output: `test/helpers/packed-sandbox.ts`, the probe library and three probes under `.planning/phases/11-managed-lifecycle-verification/evidence/probes/`, four transcripts, a strengthened `test/tarball-fixture.test.ts`, and `06-VERIFICATION.md` (skeleton plus LIFE-01 Codex rows).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-managed-lifecycle-verification/11-CONTEXT.md
@.planning/phases/11-managed-lifecycle-verification/11-RESEARCH.md
@.planning/phases/11-managed-lifecycle-verification/11-PATTERNS.md
@.planning/phases/11-managed-lifecycle-verification/11-VALIDATION.md
@.planning/milestones/v0.1.0-REQUIREMENTS.md
@.planning/phases/10-three-os-ci-baseline/CI_RUN.md
@test/tarball-fixture.test.ts
</context>

<interfaces>
Phase paths used below: PH = `.planning/phases/11-managed-lifecycle-verification`, EV = `PH/evidence`, PROBES = `EV/probes`, REPORT = `.planning/milestones/v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md`.

Existing, test/tarball-fixture.test.ts (unchanged at 35d4c15):
- Lines 46-78: `npmInvocation`, `commandResult`, `runInstalledCli(cli, prefix, args, env)` (win32 runs `node <prefix>/node_modules/alpha-aos/dist/src/cli.js`, 600 s timeout).
- Lines 80-149: `export async function createIsolatedSandbox(context)` — mkdtemp `alpha-aos-fixture-`, dirs `home state prefix repo temp npm-cache npm-logs home/.codex`, env = process.env minus every `npm_*` name, plus HOME, USERPROFILE, XDG_CONFIG_HOME, APPDATA, LOCALAPPDATA, ALPHA_AOS_STATE_DIR, CODEX_HOME, ANTIGRAVITY_CONFIG_DIR (home/.gemini/antigravity), CLAUDE_CONFIG_DIR (home/.claude), PI_CODING_AGENT_DIR (home/.pi/agent), HERMES_HOME (home/.hermes), npm_config_prefix/cache/logs_dir/userconfig/audit/fund/update_notifier, TMPDIR/TEMP/TMP.
- Lines 151-310: `HostEntry`, `HostFingerprint`, `isMissingEntry`, `fingerprintManifest`, `hostMutationTargets` (lines 217-226, eight targets, `hostStateRoot` first), `snapshotTargets`, `hostSnapshot`, `HOST_DRIFT_LISTING_LIMIT` = 50, `HOST_DRIFT_HASH_PREFIX` = 12, `compareCodeUnits`, `describeHostEntry`, `describeDigest`, `describeHostDrift(before, after, limit)` (first line `the packed lifecycle must not change any real managed host path`), `vanishedEntryCount`.
- Line 312: `parseJsonResult<T>(result, label)`.
- Lines 585-785: the packed lifecycle test; install recipe lines 590-714 (npm cache lookup, `npm pack <repo> --ignore-scripts --json --pack-destination`, `npm install --global <tgz> --prefix <prefix> --ignore-scripts --no-audit --no-fund --prefer-offline` with the host npm cache, lock channel `stable`, GSD seeding lines 664-668, locked ECC install lines 670-683 with the sandbox cache, ECC skill pre-render with `renderEccSkill` and the `This Alpha Vibe rendering` fallback lines 687-705, Codex MCP pre-render with `renderMcpConfig("codex", "", lock)` lines 706-714); reconcile assertions lines 741-753; host assertion line 781.

Harness path table (sandbox env values, from src/core/install.ts:101-127, src/core/ecc-skills.ts:53-57, src/core/mcp.ts:105-113 and 491-493):
- GSD config root: claude CLAUDE_CONFIG_DIR; codex CODEX_HOME; antigravity ANTIGRAVITY_CONFIG_DIR; pi PI_CODING_AGENT_DIR; hermes has no GSD. Files: `<root>/gsd-core/VERSION` = lock gsd.version + newline, `<root>/gsd-core/.gsd-runtime` = harness id + newline, `<root>/.gsd-profile` = lock gsd.profile + newline.
- ECC skill root: claude `<CLAUDE_CONFIG_DIR>/skills`; codex `<home>/.agents/skills`; antigravity `<ANTIGRAVITY_CONFIG_DIR>/skills`; pi `<home>/.agents/skills`; hermes `<HERMES_HOME>/skills`. Skill file `<root>/<skill>/SKILL.md`, skills `unified-memory`, `documentation-lookup`, `deep-research`; expected hash `lock.components.ecc.targetSha256[skill][harness]`.
- MCP config: claude `<home>/.claude.json`; codex `<CODEX_HOME>/config.toml`; antigravity `<home>/.gemini/config/mcp_config.json`; pi `<PI_CODING_AGENT_DIR>/mcp.json`; hermes `<HERMES_HOME>/config.yaml`.
- Pi bridge manifest: `<dirname(pi mcp.json)>/npm/node_modules/<lock.components.mcpBridges.pi.package>/package.json` with `version` = lock `components.mcpBridges.pi.version`.
- Owned skills (catalog/stack.yaml:49-61): `alpha-aos-control` targets all five harnesses; `alpha-aos-ship` targets claude only.

New in test/helpers/packed-sandbox.ts (all exported, type-only import of the node:test context type so importing the compiled helper never registers a test):
- `interface CommandResult`, `interface FixtureSandbox` (existing fields plus readonly `temp`, `cache`, `npmLogs`; `runCli(args, options?)`).
- `npmInvocation()`, `commandResult(result)`, `runInstalledCli(cli, prefix, args, env, options?)` where options = `{ input?: string; timeoutMs?: number; cwd?: string }`.
- `openSandbox(root: string): Promise<FixtureSandbox>` — creates the same directories and env under `root` (no cleanup registration).
- `createIsolatedSandbox(context)` — mkdtemp + `context.after` cleanup + `openSandbox(root)`; observable behavior identical to today.
- `HostEntry`, `HostFingerprint`, `fingerprintManifest`, `snapshotTargets`, `HOST_DRIFT_LISTING_LIMIT`, `HOST_DRIFT_HASH_PREFIX`, `describeHostDrift(before, after, limit?, header?)` (header defaults to the existing first line string), `vanishedEntryCount`, `parseJsonResult`.
- `hostNpmCache(): string` (the `npm config get cache` lookup from the lifecycle test, asserting the directory exists).
- `packRelease(sandbox, repositoryRoot): { tarballPath: string }`.
- `installPackedRelease(sandbox, options: { tarballPath: string; hostNpmCache: string }): Promise<PackedRelease>` where `PackedRelease = { tarballPath; tarballSha256; installedPackage; installedLock }` (asserts lock channel `stable` and no `catalog/candidate.lock.json`).
- `harnessPaths(sandbox, harness)` returning `{ gsdRoot: string | null; eccSkillRoot; mcpConfigPath; piBridgeManifest: string | null }` from the table above.
- `seedHarnessPrerequisites(sandbox, release, harness, options?: { eccCache?: string })` — seeds GSD files (not hermes), installs the locked ECC package into the prefix once if absent (default cache: the sandbox cache, matching the lifecycle test), pre-renders the three ECC skills for that harness asserting each hash equals the lock target hash (with the lifecycle test's deep-research fallback), pre-renders the harness MCP config with the packed `renderMcpConfig(harness, "", lock)`, and seeds the Pi bridge manifest for pi. Imports `ecc-fixture.js` and `mcp.js` from the INSTALLED package, never from the checkout.

Transcript vocabulary (probe-lib.mjs; every Phase 11 probe uses it verbatim):
- Header lines: `probe: <id>`, `head: <40-hex>`, `dirty-tracked: <count of git status --porcelain lines, untracked excluded>`, `node: <version>`, `npm: <version>`, `platform: <process.platform> <process.arch>`, `tarball-sha256: <64-hex or n/a>`, `started: <ISO date>`.
- Step block: `## step <n>: <label>`, `$ <aliased command line>`, `env-overrides: <names only, comma-separated, or none>`, `exit: <code>`, `duration-ms: <n>`, `--- stdout (<bytes>B sha256=<12-hex>) ---`, aliased stdout (first 65536 bytes, then `[truncated: <n> bytes total]`), `--- stderr (<bytes>B sha256=<12-hex>) ---`, aliased stderr.
- Fingerprint comparison line: `fingerprint <label>: <12-hex before> -> <12-hex after>` followed by the `describeHostDrift` lines when they differ.
- Check line: `CHECK <check-id> <OUTCOME> <detail>` with OUTCOME one of `HOLDS` (the clause behavior was observed as required), `VIOLATED` (observed behavior contradicts the clause: a gap reproduction), `NOT-OBSERVED` (environment prevented observation; detail names why), `OBSERVED` (neutral fact).
- Host guard lines: `host-guard: unchanged (<n> targets)` on success; on drift the literal marker `HOST-DRIFT` followed by the drift description, and the process exits 3.
- Footer: `RESULT: holds=<n> violated=<n> not-observed=<n> observed=<n>`.
- Aliases (longest match first, case-insensitive on win32, applied to both the plain and the JSON-escaped double-backslash form and to forward-slash form): sandbox root to `<sandbox>`, repository root to `<repo>`, `os.homedir()` to `<home>`, `os.tmpdir()` to `<tmp>`.
- Exit codes: 0 completed (whatever the outcomes), 1 probe error or zero CHECK lines, 3 host drift.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — packed sandbox helper, probe library and the Codex LIFE-01 reconcile probe, transcript and first report rows</name>
  <precondition>The npm registry is reachable or the host npm cache already holds `ecc-universal@2.2.0` and the runtime dependencies of this package (the probe installs them into the sandbox prefix).</precondition>
  <files>test/helpers/packed-sandbox.ts, .planning/phases/11-managed-lifecycle-verification/evidence/README.md, .planning/phases/11-managed-lifecycle-verification/evidence/probes/probe-lib.mjs, .planning/phases/11-managed-lifecycle-verification/evidence/probes/life01-reconcile.mjs, .planning/phases/11-managed-lifecycle-verification/evidence/life01-codex-reconcile.txt, .planning/milestones/v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md</files>
  <read_first>
    test/tarball-fixture.test.ts (lines 1-149 sandbox, 151-319 fingerprint and drift helpers, 585-785 packed lifecycle test)
    src/cli.ts (lines 483-513: `plan` and `install` output formats)
    src/core/install.ts (lines 101-127 GSD paths, 373-463 managed install plan steps)
    src/core/ecc-skills.ts (lines 48-60 ECC skill roots)
    src/core/mcp.ts (lines 105-113 MCP config paths, 490-493 Pi bridge manifest)
    src/core/paths.ts (userStateRoot)
    .planning/phases/11-managed-lifecycle-verification/11-RESEARCH.md (Evidence Map LIFE-01, Patterns 1-2, Pitfalls 5, 6, 8, 9, Security Domain)
    .planning/milestones/v0.1.0-phases/07-cross-platform-release-proof/07-VERIFICATION.md (lines 1-40 gap frontmatter format)
    .planning/phases/10-three-os-ci-baseline/10-VERIFICATION.md (lines 1-60 evidence and re-check table format)
  </read_first>
  <action>
    Build the thinnest end-to-end evidence path (helper, probe, transcript, report row) per D-01, D-04 and D-17. Do not touch `src/` (D-05) and do not edit test/tarball-fixture.test.ts in this task (Task 2 dedupes it).

    1. Create test/helpers/packed-sandbox.ts with every export named in the interfaces block. Copy the bodies from test/tarball-fixture.test.ts verbatim, changing only: `openSandbox(root)` holds everything `createIsolatedSandbox` does after its mkdtemp and cleanup registration; `createIsolatedSandbox` becomes mkdtemp plus `context.after` cleanup (same rm options: recursive, force, maxRetries 10, retryDelay 50) plus `openSandbox`; `runInstalledCli` and `FixtureSandbox.runCli` accept the optional options object and pass `input`, `timeout` (default 600000) and `cwd` to spawnSync; `describeHostDrift` takes an optional fourth `header` argument whose default is the existing first-line string. Write `hostNpmCache`, `packRelease`, `installPackedRelease`, `harnessPaths` and `seedHarnessPrerequisites` by lifting the lifecycle test's recipe (lines 590-714) into functions, generalized to five harnesses with the harness path table; `seedHarnessPrerequisites` must compute every path from the sandbox env and home, never from `process.env` or `os.homedir()`. Resolve the repository root the same way the test does (two directories above the compiled file's directory, `dist/test/helpers` means three: use `resolve(import.meta.dirname, "..", "..", "..")`). Use double quotes, semicolons, `.js` import extensions, and no platform-absolute string literals.
    2. Create PROBES/probe-lib.mjs (plain ESM, not compiled). It resolves the repository root as five directories above its own file, dynamically imports `dist/test/helpers/packed-sandbox.js` (failing with the message `run npm run build first` if absent), and exports: `newSandbox(label)` (mkdtemp under `os.tmpdir()` named `alpha-aos-p11-<label>-`, then `openSandbox`; cleanup with the retry rm unless the probe was started with `--keep`); `assertSandboxEnv(env, root)` checking the twelve names listed in must_haves resolve inside `root` and throwing before any spawn otherwise; `runCli(transcript, sandbox, args, { label, envOverrides, input, cwd })` that asserts containment (including every override), runs the packed CLI with `runInstalledCli`, times it, and writes a step block; `fingerprintRoots(roots)` and `compareFingerprints(transcript, label, before, after)` over `snapshotTargets` and `describeHostDrift` with a header naming the label; `hostGuardTargets()` returning the real host state root (ALPHA_AOS_STATE_DIR of the probe process or `<home>/.alpha-aos`), the eight `hostMutationTargets` paths of the lifecycle test, `<home>/.agents/skills/alpha-aos-control/SKILL.md`, `<CLAUDE_CONFIG_DIR or home/.claude>/skills/<skill>/SKILL.md` for the three ECC skills plus `alpha-aos-control` and `alpha-aos-ship`, `<PI_CODING_AGENT_DIR or home/.pi/agent>/mcp.json`, `<HERMES_HOME or home/.hermes>/config.yaml` and `<home>/.gemini/config/mcp_config.json`; `withHostGuard(transcript, fn)` that fingerprints those targets before and after `fn`, writes the unchanged line or the host-drift marker plus the drift description, and sets exit code 3 on drift; `createTranscript(probeId, outPath)` implementing the header, step, check, note and footer vocabulary of the interfaces block with the aliasing rules (an `--out <path>` probe argument overrides `outPath`, so the D-17 verifier can re-run a probe into a scratch file and diff its CHECK lines against the committed transcript without overwriting it; header git queries run as `git -c safe.directory=<repo root> -C <repo root> ...` so a WSL run of a probe, as plan 11-04 does, can read the Windows checkout), where `check(id, outcome, detail)` throws on a duplicate id or an outcome outside the four words; and `packOnce(transcript)` that runs `npm pack` once per probe into a scratch directory (or reuses the tarball named by the `ALPHA_AOS_P11_TARBALL` environment variable) and records `tarball-sha256`. Record in the transcript header, as a note, why `<home>/.claude.json` is not a host guard target: the executing Claude Code session rewrites it continuously.
    3. Create PROBES/life01-reconcile.mjs. Inside `withHostGuard`: pack once, open one sandbox, `installPackedRelease` with the host npm cache, `seedHarnessPrerequisites(..., "codex")`, then steps (a) `install --target codex --apply --json` with check `life01.codex.initial-apply` OBSERVED (applied count, journal count); (b) fingerprint the sandbox home, state and prefix; (c) dry-run `install --target codex` (human form) with check `life01.codex.preview-current`: HOLDS when at least one step line exists and every step line (an upper-case action word, a step id, then ` - `, excluding `WARNING` lines) starts with `CURRENT`, else VIOLATED listing the non-current step ids; (d) fingerprint again, check `life01.codex.preview-no-mutation`; (e) dry-run `install --target codex --json`, check `life01.codex.preview-json-current` (every `steps[].action` is `current`); (f) second `install --target codex --apply --json`, check `life01.codex.second-apply-noop` (`applied` and `operationIds` empty, `current` length equals steps length, journal file names unchanged); (g) fingerprint again, check `life01.codex.second-apply-byte-identical` (HOLDS when the three digests equal the step-(b) digests, else VIOLATED with the drift lines above it; the sandbox `temp`, `npm-cache` and `npm-logs` directories are excluded and the note says why: fixture scratch and npm debug logs by design); (h) `plan` and `plan --json`, check `life01.plan-command-static` OBSERVED stating whether any `CURRENT` line appears; (i) grep README.md and docs/ for `alpha-aos plan` and record check `life01.plan-command-docs` OBSERVED with the matching file:line list or `none`. Write the transcript to EV/life01-codex-reconcile.txt.
    4. Create EV/README.md: the directory layout, the transcript vocabulary (copy the interfaces block list), the re-run recipe (`npm run build`, then `node <probe>`; compare CHECK lines), the per-requirement findings file template (`LIFE-0N-findings.md` with exactly these headings in order: `## Contract` (verbatim LIFE text), `## Clauses` (table with columns `# | Clause | Evidence | Oracle audit (D-04) | Result` where Result is HOLDS, VIOLATED, NOT-OBSERVED or UNEVIDENCED), `## Core clause`, `## Provisional verdict` (VERIFIED, PARTIAL or GAP per D-02, one line of reasoning), `## Gap candidates` (one `### LIFE-0N/C<k>` subsection each, with the bullet labels `clause:`, `missing:`, `close condition:`, `reproduction:` per D-06, the reproduction quoting the command, the transcript path, the CHECK id and the observed lines), `## Leads (not reproduced)`, `## Observations`), and the rule that failing reproductions live only here, never in `test/` (D-12).
    5. Create REPORT (06-VERIFICATION.md) as a skeleton: YAML frontmatter with `phase: 06-managed-lifecycle-uninstall-and-recovery`, `verified_in: 11-managed-lifecycle-verification`, `status: TO-BE-COMPILED`, `score: TO-BE-COMPILED`, `phase13_gating: TO-BE-COMPILED`, `gaps: []`; then sections `## Evidence standard` (D-01..D-04 and D-09 in two short paragraphs, local Windows 11 host plus CI run 35818198049 for POSIX per D-03), `## Verdict matrix` (a table `| Requirement | Verdict | Gaps | Primary evidence |` with one row per LIFE-01..LIFE-08 whose cells read TO-BE-COMPILED), `## Phase 13 gating` (TO-BE-COMPILED), `## Evidence records`, one `## LIFE-0N` section per requirement quoting the verbatim contract text from .planning/milestones/v0.1.0-REQUIREMENTS.md:77-84 followed by a clause table, `## Gap register`, `## Leads (not reproduced)`, `## Observations`, `## Re-run index (D-17)`. Fill the LIFE-01 clause table now with the Codex rows from the transcript (clauses: install or reconcile the reviewed stable lock; idempotently; a second unchanged plan reports `CURRENT`), each citing `evidence/life01-codex-reconcile.txt` and its CHECK id and result, plus a row `five-harness scope (D-10)` reading TO-BE-COMPILED (plan 11-03). Every other section body reads TO-BE-COMPILED.
  </action>
  <verify>
    <automated>npm run check && npm run build && node .planning/phases/11-managed-lifecycle-verification/evidence/probes/life01-reconcile.mjs</automated>
    <fails_when>non-zero exit: 1 means the probe itself failed or wrote zero CHECK lines, 3 means the real host guard targets changed</fails_when>
    <automated>T=.planning/phases/11-managed-lifecycle-verification/evidence/life01-codex-reconcile.txt && grep -qE '^head: [0-9a-f]{40}$' "$T" && grep -qE '^tarball-sha256: [0-9a-f]{64}$' "$T" && grep -q '^host-guard: unchanged' "$T" && ! grep -q 'HOST-DRIFT' "$T" && grep -q '^RESULT: holds=' "$T" && test "$(grep -c '^CHECK life01\.' "$T")" -ge 7 && ! grep -qiF "$(node -p "require('os').homedir()")" "$T" && ! grep -qiF "$(node -p "JSON.stringify(require('os').homedir()).slice(1,-1)")" "$T"</automated>
    <fails_when>any test exits non-zero: a header field is missing, the host guard line is absent or the drift marker is present, fewer than 7 life01 CHECK lines exist, or the real home path appears unaliased in plain or JSON-escaped form</fails_when>
    <automated>R=.planning/milestones/v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md && test "$(grep -cE '^\| LIFE-0[1-8] \|' "$R")" -eq 8 && grep -q 'life01-codex-reconcile.txt' "$R" && grep -q 'life01.codex.second-apply-noop' "$R" && grep -q '^## Re-run index (D-17)' "$R" && test -z "$(git status --porcelain -- src)" && git diff --quiet 35d4c15 HEAD -- src</automated>
    <fails_when>the matrix does not have exactly eight LIFE rows, the LIFE-01 rows do not cite the transcript and its check id, the re-run index section is missing, or anything under src/ differs from 35d4c15</fails_when>
  </verify>
  <acceptance_criteria>
    - `node .planning/phases/11-managed-lifecycle-verification/evidence/probes/life01-reconcile.mjs` exits 0 after `npm run build`, and running it a second time with `--out <scratch file>` produces the same sequence of `CHECK` lines (`diff <(grep '^CHECK' <committed>) <(grep '^CHECK' <scratch>)` prints nothing) while leaving the committed transcript untouched.
    - The transcript contains the checks `life01.codex.initial-apply`, `life01.codex.preview-current`, `life01.codex.preview-no-mutation`, `life01.codex.preview-json-current`, `life01.codex.second-apply-noop`, `life01.codex.second-apply-byte-identical`, `life01.plan-command-static` and `life01.plan-command-docs`, each exactly once.
    - `test/helpers/packed-sandbox.ts` compiles under `npm run check` and exports every name in the interfaces block; importing `dist/test/helpers/packed-sandbox.js` from a plain `node --input-type=module -e` script registers no test (the probe run proves it).
    - The transcript contains no environment variable values and no file contents: stdout blocks are CLI output only, and no line other than a CHECK detail or fingerprint line carries a full 64-hex digest of a user file.
    - 06-VERIFICATION.md exists at the archived Phase 6 path with the section list from action step 5 and the LIFE-01 Codex rows filled.
    - `git status --porcelain -- src` prints nothing.
  </acceptance_criteria>
  <done>A single command reproduces LIFE-01 Codex reconcile evidence through the packed CLI with a host guard, and the report skeleton cites it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move the shared helpers out of tarball-fixture, strengthen its LIFE-01 oracle, and self-check five-harness seeding</name>
  <files>test/tarball-fixture.test.ts, test/helpers/packed-sandbox.ts, .planning/phases/11-managed-lifecycle-verification/evidence/probes/seed-selfcheck.mjs, .planning/phases/11-managed-lifecycle-verification/evidence/seed-selfcheck.txt</files>
  <read_first>
    test/tarball-fixture.test.ts (whole file)
    test/helpers/packed-sandbox.ts (as written by Task 1)
    .planning/phases/11-managed-lifecycle-verification/evidence/life01-codex-reconcile.txt (outcome of life01.codex.second-apply-byte-identical)
    .planning/phases/10-three-os-ci-baseline/10-02-PLAN.md (prohibitions on the host-immutability oracle)
    test/path-literal-guard.test.ts (what the guard rejects in test/*.ts)
  </read_first>
  <behavior>
    - The existing tests `host fingerprint keeps the aggregate digest byte stream`, `host drift report names changed entries by metadata and never by content` and `host drift report bounds its listing and keeps per-target totals` pass unchanged against the imported helpers.
    - The packed lifecycle test passes and now also asserts, each only where the tracer recorded the matching check as HOLDS: dry-run `install --target codex` exits 0 and every step line starts with `CURRENT` (`life01.codex.preview-current`); the sandbox home, state and prefix digests are equal before and after that dry-run (`life01.codex.preview-no-mutation`); and equal before and after the second `--apply` (`life01.codex.second-apply-byte-identical`).
  </behavior>
  <action>
    Per D-12 (pass-only) and the Phase 10 oracle prohibitions:
    1. In test/tarball-fixture.test.ts delete the definitions now exported by test/helpers/packed-sandbox.ts (`CommandResult`, `FixtureSandbox`, `npmInvocation`, `commandResult`, `runInstalledCli`, `createIsolatedSandbox`, `HostEntry`, `HostFingerprint`, `isMissingEntry`, `fingerprintManifest`, `snapshotTargets`, the two drift constants and their private helpers, `describeHostDrift`, `vanishedEntryCount`, `parseJsonResult`) and import them with named imports from `./helpers/packed-sandbox.js`. Keep `hostHome`, `hostStateRoot`, `hostCodexRoot`, the `hostMutationTargets` block and `hostSnapshot` in the test file byte-for-byte; keep `export` on nothing in the test file (the helper is now the only exporter of `createIsolatedSandbox`).
    2. Replace the lifecycle test's install recipe (npm cache lookup through the Codex MCP pre-render) with `hostNpmCache`, `packRelease` (keeping the `ALPHA_AOS_RELEASE_TARBALL` branch in the test), `installPackedRelease` and `seedHarnessPrerequisites(..., "codex")` only if every assertion the recipe made survives, either in the helper or in the test (tarball audit, `dist/src/cli.js` present, no `catalog/candidate.lock.json` in the tarball or the installed package, lock channel `stable`, the three ECC target hashes); the ECC install keeps the sandbox cache. If any assertion would be lost, leave the recipe inline instead and record that choice in the SUMMARY.
    3. Only if the tracer transcript recorded `life01.codex.preview-current HOLDS` and `life01.codex.preview-no-mutation HOLDS` (D-12: commit passing oracles only; a VIOLATED result stays a gap reproduction in the transcript), add to the lifecycle test, between the journal loop and the `reconciled` install: a manifest map and `snapshotTargets([sandbox.home, sandbox.state, sandbox.prefix], ...)`; a dry-run `sandbox.runCli(["install", "--target", "codex"])` asserting exit 0, at least one step line, every step line starting with `CURRENT ` (message: the full stdout); a second snapshot asserted deepEqual to the first with `describeHostDrift(..., undefined, "a dry-run install must not change any sandbox byte")`. After the `reconciled` install and its existing assertions, add the same byte-identity assertion with the header `an idempotent reconcile must not change any sandbox byte` only if the tracer transcript recorded `life01.codex.second-apply-byte-identical HOLDS`; otherwise add nothing there and note in the SUMMARY that the byte-identity clause is a gap candidate for plan 11-03, not a committed test.
    4. Create PROBES/seed-selfcheck.mjs: inside the host guard, one packed sandbox; for each of claude, codex, antigravity, pi, hermes call `seedHarnessPrerequisites` and record checks `seed.<harness>.gsd` (files present with the locked values, or OBSERVED `no GSD for hermes`), `seed.<harness>.ecc` (three hashes equal the lock target hashes), `seed.<harness>.mcp` (config file exists and parses), `seed.pi.bridge` (manifest version equals the lock); then run the dry-run `install --target <harness> --json` per harness and record `seed.<harness>.preview` OBSERVED listing each step id with its action. Write EV/seed-selfcheck.txt. This transcript tells plans 11-03..11-05 which harnesses reach an all-`current` preview from seeding alone.
  </action>
  <verify>
    <automated>npm run check && npm run build && H=$(mktemp -d) && WH=$(cygpath -w "$H" 2>/dev/null || printf '%s' "$H") && env -u ALPHA_AOS_STATE_DIR -u CODEX_HOME -u CLAUDE_CONFIG_DIR -u HERMES_HOME -u PI_CODING_AGENT_DIR -u ANTIGRAVITY_CONFIG_DIR HOME="$WH" USERPROFILE="$WH" node scripts/run-tests.mjs --files dist/test/tarball-fixture.test.js dist/test/path-literal-guard.test.js && test ! -e "$H/.alpha-aos"</automated>
    <fails_when>non-zero exit, a summary line with `fail` above 0, or `.alpha-aos` created in the scratch home</fails_when>
    <automated>diff <(git show 35d4c15:test/tarball-fixture.test.ts | sed -n '/^const hostMutationTargets = \[/,/^\];/p') <(sed -n '/^const hostMutationTargets = \[/,/^\];/p' test/tarball-fixture.test.ts) && test "$(sed -n '/^const hostMutationTargets = \[/,/^\];/p' test/tarball-fixture.test.ts | wc -l)" -eq 10 && test "$(grep -c 'assert.deepEqual(afterHost, beforeHost, describeHostDrift(beforeManifests, afterManifests));' test/tarball-fixture.test.ts)" -eq 1 && grep -q 'from "./helpers/packed-sandbox.js"' test/tarball-fixture.test.ts</automated>
    <fails_when>diff prints a line, the extracted block is not exactly 10 lines, the host deepEqual line count is not 1, or the helper import is missing</fails_when>
    <automated>npm run build && node .planning/phases/11-managed-lifecycle-verification/evidence/probes/seed-selfcheck.mjs && S=.planning/phases/11-managed-lifecycle-verification/evidence/seed-selfcheck.txt && test "$(grep -cE '^CHECK seed\.(claude|codex|antigravity|pi|hermes)\.preview ' "$S")" -eq 5 && grep -q '^host-guard: unchanged' "$S"</automated>
    <fails_when>non-zero exit, fewer or more than five preview checks, or no unchanged host guard line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "^function fingerprintManifest\|^async function fingerprintManifest" test/tarball-fixture.test.ts` is 0 (one implementation, in the helper).
    - The packed lifecycle test passes under the scratch-home command and contains the dry-run `CURRENT` assertion and the header string `a dry-run install must not change any sandbox byte` if and only if the tracer recorded `life01.codex.preview-current HOLDS` and `life01.codex.preview-no-mutation HOLDS`.
    - The second-apply byte-identity assertion is present if and only if EV/life01-codex-reconcile.txt shows `CHECK life01.codex.second-apply-byte-identical HOLDS`.
    - EV/seed-selfcheck.txt has a `seed.<harness>.ecc` check for all five harnesses and a `seed.pi.bridge` check.
    - `git status --porcelain -- src` prints nothing.
  </acceptance_criteria>
  <done>One helper implementation serves the CI lifecycle test and the probes, the CI test guards the dry-run CURRENT promise and sandbox byte identity, and five-harness seeding is proven.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run the Phase 6 suites locally and pull their POSIX results from CI run 35818198049</name>
  <precondition>`gh auth status` exits 0 and `gh run view 35818198049 --json headSha` returns `35d4c15` as the head prefix (logs for the run are still retrievable).</precondition>
  <files>.planning/phases/11-managed-lifecycle-verification/evidence/probes/suites-and-ci.mjs, .planning/phases/11-managed-lifecycle-verification/evidence/suites-local.txt, .planning/phases/11-managed-lifecycle-verification/evidence/ci-35818198049-lifelines.txt</files>
  <read_first>
    scripts/run-tests.mjs (header comment: env scrubbing and the --files contract)
    .planning/phases/10-three-os-ci-baseline/CI_RUN.md (sections 1-2: job ids and per-step results)
    .planning/phases/11-managed-lifecycle-verification/11-RESEARCH.md (Evidence Map intro, Pattern 4)
    .planning/phases/11-managed-lifecycle-verification/evidence/probes/probe-lib.mjs
  </read_first>
  <action>
    Produce the D-01 suite evidence and the D-03 POSIX corroboration with one re-runnable probe, PROBES/suites-and-ci.mjs, using the probe-lib transcript vocabulary (two transcripts).
    1. Suites (EV/suites-local.txt): create a scratch home with mkdtemp; build the child env from `process.env` minus ALPHA_AOS_STATE_DIR, CODEX_HOME, CLAUDE_CONFIG_DIR, HERMES_HOME, PI_CODING_AGENT_DIR and ANTIGRAVITY_CONFIG_DIR, with HOME and USERPROFILE set to the scratch home (the Phase 10 host-safe pattern). Run `node scripts/run-tests.mjs --files dist/test/lifecycle.test.js dist/test/status.test.js dist/test/recovery-receipt.test.js dist/test/repair.test.js dist/test/rollback.test.js dist/test/uninstall.test.js` as step 1, then the supporting oracles `dist/test/transaction.test.js dist/test/transaction-crash.test.js dist/test/isolation.test.js dist/test/update.test.js dist/test/canary.test.js dist/test/doctor.test.js` as step 2. Strip ANSI escapes; keep every per-test result line and the summary lines verbatim. Checks: `suites.phase6` HOLDS when the summary shows fail 0 and tests equals pass (detail: the test count), `suites.supporting` likewise, and `suites.scratch-home-clean` HOLDS when the scratch home has no `.alpha-aos` afterwards. Record the list of Phase 6 test titles (the per-test lines of step 1) for step 2 of this action.
    2. CI (EV/ci-35818198049-lifelines.txt): record `gh run view 35818198049 --json headSha,conclusion,event,attempt` (check `ci.run` HOLDS when conclusion is success, event push, head starts with 35d4c15). For each job id 107044319989 (ubuntu-latest), 107044319988 (macos-latest) and 107044319963 (windows-latest) run `gh run view --job <id> --log`, strip ANSI, and keep only lines containing one of the Phase 6 titles or `packed release completes the isolated install, reconcile, diagnose, and uninstall lifecycle`; write them verbatim under a per-job step. Check `ci.<os>.phase6-titles` HOLDS when every Phase 6 title appears with a pass marker and none with a fail marker (detail: matched count over title count), and `ci.<os>.packed-lifecycle` likewise for the packed lifecycle title. Finally run `git diff --stat 35d4c15 HEAD -- src scripts catalog package.json package-lock.json test/lifecycle.test.ts test/status.test.ts test/recovery-receipt.test.ts test/repair.test.ts test/rollback.test.ts test/uninstall.test.ts test/transaction.test.ts test/transaction-crash.test.ts test/isolation.test.ts test/update.test.ts test/canary.test.ts test/doctor.test.ts` and record check `ci.staleness` HOLDS when it prints nothing; add a note that test/tarball-fixture.test.ts changed in this plan, so the CI run corroborates its 35d4c15 form, and its new oracle is corroborated on POSIX only by the next `main` CI run.
    3. Do not reinterpret results here: the D-04 oracle audit of each test belongs to the per-requirement findings in plans 11-02..11-06.
  </action>
  <verify>
    <automated>npm run build && node .planning/phases/11-managed-lifecycle-verification/evidence/probes/suites-and-ci.mjs && S=.planning/phases/11-managed-lifecycle-verification/evidence/suites-local.txt && C=.planning/phases/11-managed-lifecycle-verification/evidence/ci-35818198049-lifelines.txt && grep -q '^CHECK suites.phase6 HOLDS' "$S" && grep -q '^CHECK suites.supporting HOLDS' "$S" && grep -q '^CHECK ci.run HOLDS' "$C" && test "$(grep -cE '^CHECK ci\.(ubuntu|macos|windows)\.phase6-titles ' "$C")" -eq 3 && grep -q '^CHECK ci.staleness HOLDS' "$C"</automated>
    <fails_when>non-zero exit, the six-suite or supporting run check is not HOLDS, the run check is not HOLDS, there are not exactly three per-OS title checks, or the staleness check is not HOLDS (the cited code moved since the CI proof commit)</fails_when>
  </verify>
  <acceptance_criteria>
    - EV/suites-local.txt contains the verbatim per-test lines of the six Phase 6 suites and a summary with fail 0.
    - EV/ci-35818198049-lifelines.txt names all three job ids and, per job, carries the matching CI log lines verbatim.
    - Any `ci.<os>.phase6-titles` result other than HOLDS is kept as recorded and flagged in the SUMMARY for plan 11-07; it is not re-run or edited to pass.
    - `git status --porcelain -- src` prints nothing.
  </acceptance_criteria>
  <done>The Phase 6 suites have fresh local evidence and per-OS CI corroboration that a verifier can re-derive by job id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe process to real host | Probes run on the developer workstation; a missing env override would point an `--apply` step at real harness roots |
| evidence transcript to public repository | Transcripts are committed under `.planning/`; anything printed into them is published |
| CI job log to evidence | Log lines are copied verbatim from a public CI run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | probe-lib.mjs runCli | critical | mitigate | `assertSandboxEnv` checks twelve location names (including every per-step override) resolve inside the sandbox root before any spawn; `withHostGuard` fingerprints the real state root and managed files before and after and exits 3 on drift |
| T-11-02 | Information disclosure | transcripts | high | mitigate | Aliasing of sandbox, repo, home and tmp paths in plain, JSON-escaped and forward-slash forms; env printed as names only; no file bodies; acceptance greps for the unaliased home path |
| T-11-03 | Repudiation | tarball-fixture host-immutability oracle | high | mitigate | Diff gate: `hostMutationTargets` block byte-identical to 35d4c15 and exactly 10 lines; the host deepEqual line appears exactly once |
| T-11-04 | Denial of service | `main` CI | medium | mitigate | D-12: only passing assertions are committed; the byte-identity assertion is conditional on the tracer's HOLDS |
| T-11-05 | Tampering | CI evidence staleness | medium | mitigate | `ci.staleness` diff proves src, scripts, catalog, package files and every cited suite are unchanged since 35d4c15 |
</threat_model>

<verification>
- `npm run check` passes.
- The scratch-home run of `dist/test/tarball-fixture.test.js` and `dist/test/path-literal-guard.test.js` passes with no `.alpha-aos` in the scratch home.
- Four transcripts exist under the evidence directory, each ending with a `RESULT:` line and none containing the host-drift marker.
- Host-safe full suite (phase gate for this wave): `npm run build && H=$(mktemp -d) && WH=$(cygpath -w "$H" 2>/dev/null || printf '%s' "$H") && env -u ALPHA_AOS_STATE_DIR -u CODEX_HOME -u CLAUDE_CONFIG_DIR -u HERMES_HOME -u PI_CODING_AGENT_DIR -u ANTIGRAVITY_CONFIG_DIR HOME="$WH" USERPROFILE="$WH" ALPHA_AOS_REQUIRE_UPSTREAM_MCP=1 node scripts/run-tests.mjs --files $(ls dist/test/*.test.js | grep -v capability-oracle) && test ! -e "$H/.alpha-aos"` exits 0.
- `git status --porcelain -- src` prints nothing.
</verification>

<success_criteria>
- LIFE-01 Codex evidence exists as a re-runnable packed-CLI transcript and is cited from 06-VERIFICATION.md (ROADMAP Phase 11 success criterion 1, first requirement row).
- The harness every wave-2 plan depends on is proven: sandbox containment, host guard, aliasing, CHECK vocabulary, five-harness seeding.
- The Phase 6 suites have local and three-OS CI evidence tied to an unchanged code base.
</success_criteria>

## Artifacts this phase produces

- `test/helpers/packed-sandbox.ts` exports: `CommandResult`, `FixtureSandbox`, `PackedRelease`, `HostEntry`, `HostFingerprint`, `npmInvocation`, `commandResult`, `runInstalledCli`, `openSandbox`, `createIsolatedSandbox`, `hostNpmCache`, `packRelease`, `installPackedRelease`, `harnessPaths`, `seedHarnessPrerequisites`, `fingerprintManifest`, `snapshotTargets`, `HOST_DRIFT_LISTING_LIMIT`, `HOST_DRIFT_HASH_PREFIX`, `describeHostDrift`, `vanishedEntryCount`, `parseJsonResult`.
- `PROBES/probe-lib.mjs` exports: `newSandbox`, `assertSandboxEnv`, `runCli`, `fingerprintRoots`, `compareFingerprints`, `hostGuardTargets`, `withHostGuard`, `createTranscript`, `packOnce`; env input `ALPHA_AOS_P11_TARBALL`; probe flags `--keep` and `--out <path>`.
- Probes: `PROBES/life01-reconcile.mjs`, `PROBES/seed-selfcheck.mjs`, `PROBES/suites-and-ci.mjs`.
- Transcripts: `EV/life01-codex-reconcile.txt`, `EV/seed-selfcheck.txt`, `EV/suites-local.txt`, `EV/ci-35818198049-lifelines.txt`; convention file `EV/README.md`.
- Check ids: `life01.codex.*`, `life01.plan-command-static`, `life01.plan-command-docs`, `seed.<harness>.*`, `suites.phase6`, `suites.supporting`, `suites.scratch-home-clean`, `ci.run`, `ci.<os>.phase6-titles`, `ci.<os>.packed-lifecycle`, `ci.staleness`.
- Report skeleton `.planning/milestones/v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md` with the placeholder token `TO-BE-COMPILED`.
- New assertions in the packed lifecycle test of `test/tarball-fixture.test.ts` (dry-run `CURRENT`, sandbox byte identity).

<output>
Create `.planning/phases/11-managed-lifecycle-verification/11-01-SUMMARY.md` when done. Include the outcome of every `life01.codex.*` check, whether the second-apply byte-identity assertion was committed, the seed self-check preview table, and the three per-OS CI title results.
</output>
