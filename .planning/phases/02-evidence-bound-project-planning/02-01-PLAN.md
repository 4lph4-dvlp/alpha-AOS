---
phase: 02-evidence-bound-project-planning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/path-boundary.ts
  - src/core/validation.ts
  - schemas/pack-catalog.schema.json
  - src/core/pack-catalog.ts
  - src/core/evidence.ts
  - src/core/project-plan.ts
  - src/types.ts
  - src/format.ts
  - src/cli.ts
  - test/project-plan.test.ts
  - test/preview.test.ts
autonomous: true
requirements: [DETC-01, DETC-02, DETC-04]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`alpha-aos project plan <dir>` on a directory whose package.json declares `react` prints a selected `WEB_REACT` pack line, the `package.json` path that selected it, and three digests, in one invocation."
    - "The same invocation with `--json` emits the same decision as a redacted JSON envelope through the existing `print()` seam."
    - "Two concurrent `alpha-aos project plan` invocations against the same repository both complete, produce byte-identical output, and leave the checkout byte-identical — `project plan` opens no writer lock and creates no temporary file."
    - "The canonical root recorded by the plan is the same value whether the repository is addressed directly or through a symlinked alias, and the 16-hex projectId derived from it is identical in both cases."
  artifacts:
    - src/core/evidence.ts
    - src/core/pack-catalog.ts
    - src/core/project-plan.ts
    - schemas/pack-catalog.schema.json
    - test/project-plan.test.ts
  key_links:
    - "src/cli.ts `project plan` branch -> src/core/project-plan.ts planProjectCapabilities"
    - "src/core/project-plan.ts -> src/core/evidence.ts collectProjectEvidence"
    - "src/core/project-plan.ts -> src/core/pack-catalog.ts loadPackCatalogStrict"
    - "src/core/evidence.ts -> src/core/path-boundary.ts canonicalizeWithMissingTail"
    - "src/core/evidence.ts -> src/core/isolation.ts isolationProjectId"
    - "src/core/pack-catalog.ts -> src/core/validation.ts validateManagedDocument (kind `pack-catalog`)"
    - "src/cli.ts -> src/format.ts formatProjectPlan -> print() redaction seam"
  prohibitions:
    - "`alpha-aos project plan` must not accept an `--apply` flag and must not persist anything (D-12)."
    - "`alpha-aos project sync --apply` must remain refused; Phase 3 opens it."
    - "`project plan` must not spawn a package manager, a git subprocess, or any network call (D-08)."
    - "`catalog/packs/*.yaml` must not be read with a bare `yaml` parse; every managed document goes through validateManagedDocument."
    - "No individual evidence fact may carry a file hash (D-07)."
    - "`isolationProjectId` in src/core/isolation.ts must not be modified — its output names existing runtime roots under `~/.alpha-aos/`."
---

<objective>
Prove the whole Phase 2 machine end-to-end on one path before any layer is built out: a real
repository directory, a canonical boundary-proven root, one collected fact, one YAML-declared
pack loaded through the strict validator, one predicate evaluated, one reviewable plan value
with digests, and one CLI invocation that prints it in text and JSON.

Purpose: every later plan in this phase expands a layer of this slice. If the seam between
`evidence.ts`, `pack-catalog.ts`, `project-plan.ts` and `cli.ts` is wrong, it is wrong once here
rather than after nine committed layers.

Output: `src/core/evidence.ts`, `src/core/pack-catalog.ts`, `src/core/project-plan.ts`,
`schemas/pack-catalog.schema.json`, a working `alpha-aos project plan` subcommand, and
`test/project-plan.test.ts` carrying the end-to-end assertion.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-evidence-bound-project-planning/02-CONTEXT.md
@.planning/phases/02-evidence-bound-project-planning/02-RESEARCH.md
@.planning/phases/02-evidence-bound-project-planning/02-PATTERNS.md
@AGENTS.md
</context>

## Artifacts this phase produces

Symbols and files created by THIS plan (new — not pre-existing, exempt from drift verification):

| Kind | Name | File |
|------|------|------|
| module | `src/core/evidence.ts` | new |
| module | `src/core/pack-catalog.ts` | new |
| module | `src/core/project-plan.ts` | new |
| schema | `schemas/pack-catalog.schema.json` | new |
| test | `test/project-plan.test.ts` | new |
| export | `canonicalizeWithMissingTail` | `src/core/path-boundary.ts` (newly exported, function pre-exists as module-private) |
| type | `CanonicalRoot`, `RootReason` | `src/core/evidence.ts` |
| function | `resolveCanonicalRoot` | `src/core/evidence.ts` |
| function | `collectProjectEvidence` | `src/core/evidence.ts` |
| type | `EvidenceFact`, `EvidenceEnvelope` | `src/types.ts` |
| function | `loadPackCatalogStrict` | `src/core/pack-catalog.ts` |
| type | `PackDeclaration`, `PackCatalog` | `src/types.ts` |
| function | `planProjectCapabilities` | `src/core/project-plan.ts` |
| type | `ProjectCapabilityPlan`, `PackEvaluation`, `LeafResult` | `src/types.ts` |
| function | `formatProjectPlan` | `src/format.ts` |
| document kind | `"pack-catalog"` | `ManagedDocumentKind` in `src/core/validation.ts` |
| CLI subcommand | `alpha-aos project plan [path] [--json]` | `src/cli.ts` |

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "one repository selects one pack" — one path only</name>
  <files>src/core/path-boundary.ts, src/core/validation.ts, schemas/pack-catalog.schema.json, src/core/pack-catalog.ts, src/core/evidence.ts, src/core/project-plan.ts, src/types.ts, src/format.ts, src/cli.ts, test/project-plan.test.ts</files>
  <read_first>
    - src/core/path-boundary.ts (lines 1-160 and 239-310 — `canonicalizeWithMissingTail` at :133 is module-private today; `provePathBoundary` at :245 and `proveOperationPaths` at :419 are the batch contracts)
    - src/core/catalog.ts (whole file — `loadCatalogStrict`, `ManagedDocumentError`, `loadSchema`, `catalogInvariants` are the exact shapes to mirror)
    - src/core/validation.ts (lines 1-140 and 270-330 — `ManagedDocumentKind` union at :12, `OWNED_SUBTREE` at :286, `CORE_SCHEMAS` at :299, `coreObject` helper)
    - src/core/isolation.ts (lines 120-175 — `isolationProjectId` at :132, `discoverSkillPaths` at :161)
    - src/core/component-session.ts (lines 21-70 and 110-170 — `reviewedDigest`, `ReviewedPlanBoundary`, `ComponentPlanError`, `assertPlanUnchanged`)
    - src/core/project.ts (whole file — `detectProject` at :36-96 is the engine being displaced; `inspectProjectManifest` at :149-173 survives)
    - src/cli.ts (lines 130-150 for the `print()` seam, lines 570-605 for the `project` branch and the `repair` branch shape)
    - src/format.ts (lines 33-50 — `formatProject` and `formatUpdate` table conventions)
    - src/types.ts (lines 100-130 — `ProjectDetection` and neighbours)
    - catalog/packs/web.yaml (the declaration this tracer evaluates)
    - schemas/stack.schema.json and schemas/evidence.schema.json (the `$schema` draft, `required` ordering, `additionalProperties: false` and per-property `description` style the new schema must match)
    - test/project.test.ts (lines 1-55 — `mkdtemp` + `context.after` fixture shape and `withManifest`)
    - test/preview.test.ts (lines 220-260 — `runCli` at :231 and `PREVIEW_INVOCATIONS` at :247)
    - .planning/phases/02-evidence-bound-project-planning/02-PATTERNS.md
  </read_first>
  <action>
Wire ONE entry point through every layer of Phase 2 to the far end of the stack. Exactly one pack,
one fact kind, one manifest reader. No other detector kinds, no sub-project discovery, no ignore
list, no near-miss rendering, no approve, no status. Real error handling on the single path.

Start by writing the failing end-to-end assertion in a new `test/project-plan.test.ts`: build a
temporary directory containing a `package.json` whose `dependencies` declare `react`, then spawn
the built CLI (`process.execPath` + `dist/src/cli.js`, mirroring `runCli` in `test/preview.test.ts:231`)
with `["project", "plan", <dir>]` and assert the exit code is 0, stdout names `WEB_REACT`, names
`package.json` as the selecting path, and carries the three digest lines. Add a second case that
runs the same invocation with `--json`, `JSON.parse`s stdout, and asserts the parsed value's
selected pack ids equal `["WEB_REACT"]`. Add a third case asserting two `Promise.all`-launched
concurrent invocations return byte-identical stdout. Add a fourth case that creates a `dir`-kind
symlink to the fixture (`"junction"` on win32, per `test/path-boundary.test.ts:60-72`, with the same
`context.skip` fallback when the host cannot create one) and asserts the plan's `scope.canonicalRoot`
and `scope.projectId` are identical through both addresses.

Then build the slice, in this order:

1. `src/core/path-boundary.ts` — export `canonicalizeWithMissingTail` (it is module-private at :133).
   Change only the `export` keyword and the doc comment; do not alter its body, its ENOENT-continues /
   other-errno-refuses split, or any call site. This is the explicit resolution of the CONTEXT premise
   that named it a reusable asset: the phase EXPORTS it rather than routing the root through
   `provePathBoundary`, because the canonical root has no outer allowed root to be proven against, and
   re-deriving a second canonical form is exactly the 01-19 asymmetry.

2. `src/core/evidence.ts` (new) — export `type RootReason = "explicit-override" | "git-root" | "worktree-root" | "submodule-root" | "project-declaration" | "standalone-directory"`,
   `interface CanonicalRoot { root: string; reason: RootReason; projectId: string }`, and
   `async function resolveCanonicalRoot(inputPath: string, explicit?: string): Promise<CanonicalRoot>`.
   The ladder for this tracer implements only two rungs: nearest ancestor containing a `.git` entry
   tested with `existsSync` (NOT `statSync(...).isDirectory()`) yields `"git-root"`; otherwise the
   directory itself yields `"standalone-directory"`. Canonicalize through `canonicalizeWithMissingTail`
   and feed that already-canonical value to `isolationProjectId` from `src/core/isolation.ts` — call it,
   do not modify it, because `resolve()` in its body does not follow symlinks and only a canonical input
   makes it an identity function.
   Also export `async function collectProjectEvidence(root: CanonicalRoot): Promise<EvidenceEnvelope>`
   with exactly one detector for this tracer: a `dependency` detector that reads `package.json` at the
   root through `parseStrictJson` from `src/core/validation.ts` (never `JSON.parse` — duplicate keys must
   be a refusal, not a coin flip), reads names from `dependencies`, `devDependencies` and
   `peerDependencies` via `Object.keys` on a null-prototype copy, and records a fact per declared
   package. Every fact record is `{ id, kind, detected, path?, version?, reason? }` and carries NO `hash`
   field (D-07). Emit a record for the negative case too. Compute `sourceHash` as sha256 over the sorted
   list of `(relative POSIX path, sha256 of bytes)` for every file actually read, using the
   `createHash("sha256").update(content).digest("hex")` one-liner at `src/core/ecc-skills.ts:40-42`.

3. `src/core/validation.ts` — add `"pack-catalog"` to the `ManagedDocumentKind` union at :12. Add its
   entry to `OWNED_SUBTREE` (value `null`, the whole document is owned) and to `CORE_SCHEMAS` using
   `coreObject(["schemaVersion", "packs"], { schemaVersion: { type: "integer" }, packs: { type: "array" } })`.
   Both tables are `Record<ManagedDocumentKind, …>`, so `tsc` enumerates every omission — do not grep
   for call sites.

4. `schemas/pack-catalog.schema.json` (new) — close the shape `catalog/packs/web.yaml` already uses:
   required `schemaVersion` and `packs`; each pack requires `id` and `evidence`; `evidence` accepts the
   operators `all`, `any`, `anyFiles`, `anyDependencies`, `manifestOptIn`; optional `skills`, `lifecycle`
   and `selectionPolicy` (they are declared in `catalog/packs/brownfield.yaml` and
   `catalog/packs/security.yaml`, and because the validator is closed-world, omitting them makes
   alpha-AOS reject its own catalog). Match `schemas/stack.schema.json` for `$schema`, `$id`,
   `additionalProperties: false` and per-property `description` style. Full-fidelity nesting and the
   fact vocabulary are plan 02-02's work; this file only needs to accept `catalog/packs/web.yaml`.

5. `src/core/pack-catalog.ts` (new) — export `async function loadPackCatalogStrict(root: string, files?: readonly string[]): Promise<StrictLoadResult<PackCatalog>>`.
   Mirror `loadCatalogStrict` in `src/core/catalog.ts:141-157` exactly: cache the schema read from
   `schemas/pack-catalog.schema.json` on disk so that file is the single source of truth, read the YAML
   text, call `validateManagedDocument<PackCatalog>({ text, format: "yaml", kind: "pack-catalog", schema, domain })`,
   and throw the imported `ManagedDocumentError` with `createMigrationPlan(result)` when the result is
   not ok or the value is null. For this tracer the default file list is `["catalog/packs/web.yaml"]`;
   the seven-file merge with cross-file duplicate-id refusal is plan 02-02.

6. `src/core/project-plan.ts` (new) — export
   `async function planProjectCapabilities(options: { path: string; packageRoot: string }): Promise<ProjectCapabilityPlan>`.
   It resolves the canonical root, collects evidence, loads the pack catalog, and evaluates every pack
   without short-circuiting: compute the truth value of every leaf and keep both the satisfied and the
   failed leaf lists as `LeafResult { factId, detected, path, reason }`. For this tracer only the
   `anyDependencies` operator needs an evaluator, and only the `selected` and `silent` statuses need to
   exist. Synthesize the leaf's fact id from operator plus matched literal as `dependency:react`,
   extending the `manifest:scientificResearch` convention already in `src/core/project.ts:86`. Build
   `ProjectCapabilityPlan` with, at minimum for this tracer: `scope { canonicalRoot, rootReason, projectId, subProjectPath }`,
   `selected: PackEvaluation[]`, `inputsDigest`, `evidenceDigest`, `planDigest`. Compute `inputsDigest`
   from the evidence envelope's `sourceHash`; compute `evidenceDigest` over the normalized sorted fact
   set (ids, detected flags, versions, paths); compute `planDigest` with `reviewedDigest("project-capability-plan", digestableView)`
   from `src/core/component-session.ts:65`. Build `digestableView` as its own object literal in one
   place that never contains `createdAt` or any git ref — do not construct it by deleting fields from
   the envelope. Sort every array before it enters a digest.

7. `src/types.ts` — declare `EvidenceFact`, `EvidenceEnvelope`, `PackDeclaration`, `PackCatalog`,
   `LeafResult`, `PackEvaluation`, `ProjectCapabilityPlan`. Leave `ProjectDetection` in place; plan
   02-06 removes it together with its producer.

8. `src/format.ts` — add `formatProjectPlan(plan: ProjectCapabilityPlan): string` beside the existing
   `formatProject`. Emit the project line (`canonicalRoot` and `rootReason`), one `SELECT <packId> — <leaf phrases>`
   line per selected pack, the `Inputs digest:` / `Evidence digest:` / `Plan digest:` lines, and the
   closing sentence stating the run is preview-only and wrote nothing. Follow `formatUpdate`'s
   `rows: string[][]` accumulation style where a table is warranted.

9. `src/cli.ts` — in the `project` branch (currently `if (!["detect", "plan", "sync"].includes(subcommand))`
   at :575), route `plan` to `planProjectCapabilities` and `print(plan, json, formatProjectPlan(plan), context)`
   through the existing seam at :132-142 — never `process.stdout.write` directly, which is what :583 does
   today. `plan` takes NO `--apply` flag. Leave the `detect` route and the `sync --apply` refusal string
   untouched; `detect` retires in plan 02-06 and `sync --apply` opens in Phase 3. Use an
   `observableContext()` redaction context, because dependency and path values reach stdout.
  </action>
  <verify>
    <automated>npm run check &amp;&amp; npm run build &amp;&amp; node --test dist/test/project-plan.test.js</automated>
    <fails_when>non-zero exit, or "fail 0" absent from the node --test summary, or the summary reports fewer than 4 tests</fails_when>
    <automated>node dist/src/cli.js project plan . --json</automated>
    <fails_when>non-zero exit, or stdout is not parseable JSON, or the parsed value has no `scope.projectId` matching /^[0-9a-f]{16}$/</fails_when>
    <automated>grep -c "export async function canonicalizeWithMissingTail" src/core/path-boundary.ts</automated>
    <fails_when>output is 0 — the single canonical form was not exported and a second canonicalization path exists</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0 with no TypeScript error.
    - `node --test dist/test/project-plan.test.js` reports `fail 0` with at least 4 tests.
    - `node dist/src/cli.js project plan <fixture-with-react>` exits 0 and stdout contains `WEB_REACT`, `package.json`, `Inputs digest:`, `Evidence digest:` and `Plan digest:`.
    - `node dist/src/cli.js project plan <fixture-with-react> --json` emits stdout that `JSON.parse` accepts and whose `scope.projectId` matches `/^[0-9a-f]{16}$/`.
    - Two concurrent `project plan` invocations against the same fixture produce byte-identical stdout.
    - The plan's `scope.canonicalRoot` and `scope.projectId` are equal when the fixture is addressed directly and through a directory symlink alias (or the case reports `context.skip` on a host without symlink privilege, following `test/path-boundary.test.ts:169-170`).
    - `grep -c "export async function canonicalizeWithMissingTail" src/core/path-boundary.ts` returns 1.
    - `grep -c "isolationProjectId" src/core/evidence.ts` returns at least 1, and `git diff --stat -- src/core/isolation.ts` is empty.
    - `grep -c "validateManagedDocument" src/core/pack-catalog.ts` returns at least 1.
    - No fact object emitted by `collectProjectEvidence` carries a `hash` property (assert with `Object.hasOwn` in the test).
    - `alpha-aos project plan --apply` is not a recognised flag: the CLI does not branch on `--apply` inside the `plan` route.
  </acceptance_criteria>
  <done>A single repository directory declaring `react` produces a `WEB_REACT` selection with its selecting path and three digests, in both text and JSON, through the real CLI binary, with a canonical root that survives symlink aliasing — and the run writes nothing.</done>
  <reversibility rating="costly">The evidence/pack-catalog/project-plan module split and the `ProjectCapabilityPlan` shape become the read path for every later consumer, test and explanation surface in this phase (carries CONTEXT D-05's own `costly` rating forward).</reversibility>
</task>

<task type="auto">
  <name>Task 2: Register `project plan` in the SAFE-01 mutating-entry-point enumeration</name>
  <files>test/preview.test.ts</files>
  <read_first>
    - test/preview.test.ts (lines 240-270 — `PREVIEW_INVOCATIONS` at :247 and the comment stating that adding a mutating command without adding it here is the regression this enumeration catches; lines 480-520 for how each invocation is exercised)
    - src/cli.ts (the `project plan` route added in Task 1)
  </read_first>
  <action>
Add one entry to the `PREVIEW_INVOCATIONS` array in `test/preview.test.ts` (declared at :247), placed
beside the existing `project isolate plan` / `project isolate sync` entries: name `project plan`,
args `["project", "plan", "."]`.

This is research carryover 4 and it is not bookkeeping. The Phase 1 SAFE-01 tracer asserts that every
listed invocation leaves the source checkout, the package and global-link sentinels, native
configuration, the managed state root and every harness resource root byte-for-byte unchanged.
Registering `project plan` is what makes D-12's "previews and persists nothing" an enforced property
rather than a claim in a document. `project approve` is deliberately NOT added here — it does not exist
until plan 02-09, which owns its registration.

Do not modify `runCli`, the `CHECKOUT_IGNORED` set, or the `LAUNCHER_FOOTPRINT` list.
  </action>
  <verify>
    <automated>npm run check &amp;&amp; npm run build &amp;&amp; node --test dist/test/preview.test.js</automated>
    <fails_when>non-zero exit, or "fail 0" absent from the node --test summary</fails_when>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "fail 0" absent from the summary, or the reported test total is 208 or lower</fails_when>
  </verify>
  <acceptance_criteria>
    - `test/preview.test.ts` contains an entry whose `args` array is exactly `["project", "plan", "."]`.
    - `node --test dist/test/preview.test.js` reports `fail 0`.
    - `npm test` reports `fail 0` and a total strictly greater than 208 (the Phase 2 start baseline recorded in `.planning/phases/02-evidence-bound-project-planning/02-VALIDATION.md`).
  </acceptance_criteria>
  <done>The SAFE-01 preview tracer exercises `alpha-aos project plan` and proves it changes no byte of the checkout, the state root, or any harness resource root.</done>
</task>

</tasks>

## Phase edge-coverage ledger (spec-less probe fallback)

Phase 2 has no SPEC file, so the deterministic edge probe ran in fallback mode and returned 11
applicable rows, all `unresolved`. Every row is accounted for below — nine were authored as explicit
`must_haves.truths` in the plan that owns the behaviour, and two `unclassified` rows stay unresolved
and are surfaced as flagged planner assumptions. No row was dismissed and none was silently dropped.

| # | Requirement | Category | Disposition | Authored in |
|---|-------------|----------|-------------|-------------|
| 1 | DETC-01 | unclassified | **unresolved** — flagged planner assumption | `02-05-PLAN.md` §Flagged planner assumptions |
| 2 | DETC-02 | adjacency | resolved (explicit) | `02-02-PLAN.md` truth — duplicate pack id / duplicate fact id refuse rather than last-wins |
| 3 | DETC-02 | empty | resolved (explicit) | `02-06-PLAN.md` truth — an empty repository yields one `detected: false` record per declared fact, never an empty facts array |
| 4 | DETC-02 | ordering | resolved (explicit) | `02-06-PLAN.md` truth — one total order (fact id, then path), identical across runs |
| 5 | DETC-02 | concurrency | resolved (explicit) | `02-01-PLAN.md` truth — two concurrent `project plan` runs both complete, are byte-identical, and write nothing |
| 6 | DETC-03 | concurrency | resolved (explicit) | `02-07-PLAN.md` truth — near-miss classification is a pure function of the envelope; `plan` holds no lock and leaves no residue |
| 7 | DETC-04 | empty | resolved (explicit) | `02-08-PLAN.md` truth — zero selected packs still yields all nine fields, empty rather than omitted |
| 8 | DETC-04 | encoding | resolved (explicit) | `02-08-PLAN.md` truth — sha256 over UTF-8 bytes of one canonical serialization; non-ASCII path digests stable across runs |
| 9 | DETC-05 | idempotency | resolved (explicit) | `02-09-PLAN.md` truth — a second approve of the same digest reports already-current and writes nothing |
| 10 | DETC-05 | concurrency | resolved (explicit) | `02-09-PLAN.md` truth — concurrent approves serialize on the writer lock; exactly one writes, never a partial artifact |
| 11 | DETC-06 | unclassified | **unresolved** — flagged planner assumption | `02-10-PLAN.md` §Flagged planner assumptions |

Count check: 11 probe rows == 9 authored truths + 2 flagged assumptions.

Prohibitions were recalled per plan and lifted into each plan's `must_haves.prohibitions` block,
authored descriptor-less; each therefore disposes flagged-unverified at verification rather than
claiming an unwritten check.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanned repository → evidence collector | Every byte under the user's repository is untrusted input: `package.json` contents, directory names, symlink targets. |
| `catalog/packs/*.yaml` → pack catalog loader | Repository-owned but still parsed as a managed document; a malformed or duplicated declaration must refuse rather than resolve ambiguously. |
| plan value → stdout | Dependency names and file paths reach an observable surface and a possibly-committed artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `resolveCanonicalRoot` / `collectProjectEvidence` in `src/core/evidence.ts` | high | mitigate | The root is canonicalized through the single `canonicalizeWithMissingTail` form (exported, not re-derived), so a symlinked alias resolves to the same root instead of widening the read surface. Proven by the symlink-alias case in `test/project-plan.test.ts`. |
| T-02-06 | Tampering | `package.json` dependency reader in `src/core/evidence.ts` | medium | mitigate | Parse with `parseStrictJson` (duplicate-key rejecting) and read names via `Object.keys` on a null-prototype copy; never spread the parsed object into a config object. |
| T-02-08 | Spoofing | `catalog/packs/*.yaml` load in `src/core/pack-catalog.ts` | high | mitigate | Load through `validateManagedDocument` with the on-disk `schemas/pack-catalog.schema.json`; a not-ok or null result throws `ManagedDocumentError` rather than partially applying whatever parsed. |
| T-02-10 | Information Disclosure | `print()` seam in `src/cli.ts` | medium | mitigate | Route every emitted byte through `print(value, json, formatted, observableContext())`; no `process.stdout.write` in the new route. |
| T-02-11 | Tampering | `project plan` write surface | high | mitigate | `project plan` is registered in `PREVIEW_INVOCATIONS`, so the SAFE-01 tracer fails if it writes any byte anywhere it is watched. |
</threat_model>

<verification>
- `npm run check` exits 0.
- `npm run build` exits 0 and `npm run build:check` still reports a consistent input/output count.
- `node --test dist/test/project-plan.test.js` reports `fail 0` with at least 4 tests.
- `npm test` reports `fail 0` with a total strictly above 208.
- `git diff --stat -- src/core/isolation.ts` is empty.
</verification>

<success_criteria>
One real repository directory, addressed directly or through a symlink, produces one YAML-declared
pack selection with its selecting evidence path and three digests, in text and JSON, through the
built `alpha-aos` binary — and the SAFE-01 tracer proves the invocation wrote nothing.
</success_criteria>

<output>
Create `.planning/phases/02-evidence-bound-project-planning/02-01-SUMMARY.md` when done.
</output>
