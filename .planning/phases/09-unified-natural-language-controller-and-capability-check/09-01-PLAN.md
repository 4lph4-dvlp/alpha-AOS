---
phase: 09-unified-natural-language-controller-and-capability-check
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/alpha-aos-control/SKILL.md
  - skills/alpha-aos-pack-advisor/SKILL.md
  - catalog/stack.yaml
  - catalog/stack.lock.json
  - test/catalog.test.ts
  - test/owned-skills.test.ts
autonomous: true
requirements: [CTRL-01, CTRL-02, CTRL-03]

must_haves:
  truths:
    - "The alpha-aos-control skill is authored with explicit, fail-closed instructions unifying capability pack advisory, directory tree-off policy, health diagnostics, safe rollback, and crash repair (CTRL-01, CTRL-03)."
    - "The skill explicitly defines activation triggers for the project environment setup completion checkpoint (manifest creation, dependency installs) to resolve the 2 -> 3 trigger gap (CTRL-02)."
    - "alpha-aos-control replaces alpha-aos-pack-advisor in catalog/stack.yaml and catalog/stack.lock.json across all 5 harnesses (claude, codex, antigravity, pi, hermes) with exact SHA-256 verification (CTRL-01)."
    - "test/catalog.test.ts and test/owned-skills.test.ts verify alpha-aos-control registration and multi-harness rendering with 100% assertions passing (CTRL-01)."
  prohibitions:
    - statement: "MUST NOT bypass the 64-character plan digest check or allow fuzzy digest guessing (CTRL-01)."
      category: safety
      status: resolved
      verification: automated
    - statement: "MUST NOT mutate project files or apply sync without prior user approval (CTRL-01)."
      category: safety
      status: resolved
      verification: automated
    - statement: "MUST NOT write files to the project repository when applying tree-off policy (CTRL-03)."
      category: architectural
      status: resolved
      verification: automated
  artifacts:
    - path: "skills/alpha-aos-control/SKILL.md"
      provides: "Unified natural language controller and capability pack advisor skill"
    - path: "catalog/stack.yaml"
      provides: "Updated stack catalog registering alpha-aos-control"
    - path: "catalog/stack.lock.json"
      provides: "Cryptographically pinned hashes for alpha-aos-control across all 5 harnesses"
---

<objective>
Author the unified `alpha-aos-control` skill, register it in `catalog/stack.yaml` and `catalog/stack.lock.json` across all 5 harnesses (Claude, Codex, Antigravity, Pi, Hermes) replacing `alpha-aos-pack-advisor`, and verify with updated catalog and owned-skills tests.

Purpose: Deliver a single, comprehensive agent skill that provides autonomous capability pack advisory, natural language directory tree isolation, health diagnostics, and safe rollback/repair while explicitly solving the 2 -> 3 trigger gap.
Output: `skills/alpha-aos-control/SKILL.md`, updated catalog/lock, and passing tests in `test/catalog.test.ts` and `test/owned-skills.test.ts`.
</objective>

<execution_context>
Execution via GSD workflow adhering to anti-shallow deep work rules.
</execution_context>

<context>
@.planning/phases/09-unified-natural-language-controller-and-capability-check/09-CONTEXT.md
@.planning/phases/09-unified-natural-language-controller-and-capability-check/09-RESEARCH.md
@.planning/phases/09-unified-natural-language-controller-and-capability-check/09-VALIDATION.md
@catalog/stack.yaml
@catalog/stack.lock.json
@skills/alpha-aos-pack-advisor/SKILL.md
</context>

<tasks>

<task id="09-01-01" wave="1">
<title>Task 1: Author skills/alpha-aos-control/SKILL.md and Retire alpha-aos-pack-advisor</title>
<read_first>
- skills/alpha-aos-pack-advisor/SKILL.md
- src/core/owned-skills.ts
</read_first>
<action>
1. Create `skills/alpha-aos-control/SKILL.md`:
   - YAML frontmatter:
     ```markdown
     ---
     name: alpha-aos-control
     description: "Autonomous controller and capability pack advisor for alpha-AOS. Automatically triggers when project environment setup (scaffolding, package manifests, dependency installation) completes, during GSD onboarding or Phase 01, or upon natural language requests for capability packs, directory tree exclusion (tree-off), diagnostics (status/doctor), rollback, or repair."
     ---
     ```
   - Content instructions covering all 5 core operational domains:
     - **Domain 1: Autonomous Capability Pack Advisory & Materialization**:
       - Workspace inspection: `alpha-aos project plan . --json` + `alpha-aos project status . --json`.
       - Automatic Checkpoint Trigger: Highlight that when files like `package.json`, `tsconfig.json`, `pyproject.toml`, `requirements.txt`, `Cargo.toml` are created or dependencies (`npm install`, `pip install`, `cargo add`) are installed, the agent MUST run this check before moving to application coding.
       - Analysis: compare `plan.selected` with `status.reconciliation.packs`.
       - Present recommendation with matched reasons and evidence.
       - Ask user for confirmation.
       - Upon confirmation, extract exact 64-char `plan.planDigest`, run:
         `alpha-aos project approve . --plan-digest <digest> --apply`
         `alpha-aos project sync . --apply`
       - Verify `status === "CURRENT"`.
       - Remind user to restart session or reload tools to activate newly materialized MCP tools and skills.
     - **Domain 2: Directory Tree Isolation (Tree-Off Policy)**:
       - Match natural language requests like "이 프로젝트는 팀원들과 함께 쓰니 alpha-AOS를 쓰지 못하게 격리해줘", "alpha-aos 꺼줘/제외해줘".
       - Run: `alpha-aos tree policy set . --mode off`.
       - Verify: `alpha-aos tree status .`.
       - Explain to user: alpha-AOS global skills/MCPs/memory are excluded for this tree, zero files written in the repository.
     - **Domain 3: Directory Tree Policy Restoration**:
       - Match requests like "alpha-AOS 다시 켜줘", "격리 해제해줘".
       - Run: `alpha-aos tree policy set . --mode inherit`.
       - Verify: `alpha-aos tree status .`.
     - **Domain 4: Diagnostics & Health (Status & Doctor)**:
       - Match requests like "상태 점검해줘", "닥터 실행해줘", "진단해줘".
       - Run `alpha-aos status` (fast offline check) and/or `alpha-aos doctor` (full canary report).
       - Summarize findings and actionable recommendations.
     - **Domain 5: Safe Rollback & Crash Recovery**:
       - Match requests like "방금 작업/설치 롤백해줘", "되돌려줘".
       - Run: `alpha-aos rollback --apply`.
       - Match requests like "크래시 복구해줘", "망가진 상태 수리해줘".
       - Run: `alpha-aos repair --apply`.
     - **Safety Invariants**:
       - Fail-closed on errors or digest drift.
       - No blind manual edits to `.alpha-aos/stack.yaml` or harness configs.
       - Tree-off writes zero files to the target workspace.
2. Remove `skills/alpha-aos-pack-advisor/SKILL.md` (subsumed by `alpha-aos-control`).
</action>
<verify>
<automated>node -e "const fs = require('fs'); assert(fs.existsSync('skills/alpha-aos-control/SKILL.md')); assert(!fs.existsSync('skills/alpha-aos-pack-advisor/SKILL.md'));"</automated>
</verify>
<acceptance_criteria>
- `skills/alpha-aos-control/SKILL.md` exists with complete instructions across all 5 domains and trigger checklist.
- `skills/alpha-aos-pack-advisor/SKILL.md` is removed.
</acceptance_criteria>
</task>

<task id="09-01-02" wave="1">
<title>Task 2: Register alpha-aos-control in Catalog/Lock Channels & Update Tests</title>
<read_first>
- catalog/stack.yaml
- catalog/stack.lock.json
- test/catalog.test.ts
- test/owned-skills.test.ts
</read_first>
<action>
1. In `catalog/stack.yaml`:
   - Replace `alpha-aos-pack-advisor` with `alpha-aos-control`:
     ```yaml
     - id: alpha-aos-control
       source: skills/alpha-aos-control/SKILL.md
       targets: [claude, codex, antigravity, pi, hermes]
       invocation: automatic
     ```
2. Compute SHA-256 for `skills/alpha-aos-control/SKILL.md` source and target renderings via `renderOwnedSkill`.
3. In `catalog/stack.lock.json`:
   - Replace `alpha-aos-pack-advisor` under `components.ownedSkills` with `alpha-aos-control` and its exact computed source and target hashes.
4. Update `test/catalog.test.ts`:
   - Assert `catalog.components.ownedSkills.map((s) => s.id)` equals `["alpha-aos-ship", "alpha-aos-control"]`.
   - Assert `alpha-aos-control` source and target hashes match the lock.
5. Update `test/owned-skills.test.ts` if any assertions reference `alpha-aos-pack-advisor`.
</action>
<verify>
<automated>npm run check && npm run build && node scripts/run-tests.mjs --files dist/test/catalog.test.js dist/test/owned-skills.test.js</automated>
</verify>
<acceptance_criteria>
- TypeScript compiler (`npm run check`) passes with zero errors.
- `catalog/stack.yaml` and `catalog/stack.lock.json` consistently declare and pin `alpha-aos-control`.
- `test/catalog.test.ts` and `test/owned-skills.test.ts` pass with 100% green assertions.
</acceptance_criteria>
</task>

</tasks>
