---
phase: 01-safe-operation-boundary
plan: 03
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - src/core/redaction.ts
  - src/core/paths.ts
  - src/core/support-bundle.ts
  - src/types.ts
  - test/redaction.test.ts
autonomous: true
requirements: [SAFE-04]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-12 and D-13: recursive redaction removes exact and structural secrets before persistence and returns only bounded typed placeholders and fingerprints for process output."
    - "D-14: home, user-identifying, temp, and selected project roots use segment-aware aliases consistently while repository-relative suffixes and environment names remain useful."
    - "D-15: support material has a deterministic non-mutating manifest/redacted preview whose source hashes and destination intent can be bound to an explicit local creation action; no upload or network path exists."
  artifacts:
    - path: src/core/redaction.ts
      provides: "Central recursive redactor, bounded serializer, and output excerpt policy"
      exports: [createRedactionContext, redactValue, serializeObservable, createRedactedExcerpt]
    - path: src/core/paths.ts
      provides: "Segment-aware home/temp/project aliases"
      exports: [createPathAliases, aliasPath]
    - path: src/core/support-bundle.ts
      provides: "Immutable local support-bundle plan and deterministic redacted-byte renderer"
      exports: [planSupportBundle, renderSupportBundleBytes]
  key_links:
    - from: src/core/support-bundle.ts
      to: src/core/redaction.ts
      via: "redact before preview or artifact write"
      pattern: "redact|serializeObservable"
    - from: src/core/redaction.ts
      to: src/core/paths.ts
      via: "path alias stage after secret replacement"
      pattern: "aliasPath|pathAliases"
  prohibitions:
    - statement: "Support diagnostics MUST NOT contain snapshot bytes or create any network request; only alias path, size, hash, permissions, and redacted content previews are shareable."
      requirement_id: SAFE-04
      category: privacy
      status: unverified
      flagged: true
---

<objective>
모든 출력·진단·persistence consumer가 재사용할 중앙 redaction 및 immutable local support-bundle planning 경계를 구현한다.

Purpose: secret 제거를 CLI formatting 단계에만 맡기지 않고 데이터가 저장되기 전에 차단한다.
Output: redaction/path-alias utility, bounded serializers, and hash-bound support-bundle plan/renderer.
</objective>

<execution_context>
@C:/Users/alpha/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safe-operation-boundary/01-CONTEXT.md
@.planning/phases/01-safe-operation-boundary/01-RESEARCH.md
@.planning/phases/01-safe-operation-boundary/01-PATTERNS.md
@.planning/phases/01-safe-operation-boundary/01-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Secret-bearing Error에서 support preview까지 한 redaction path 연결</name>
  <files>src/core/redaction.ts, src/core/support-bundle.ts, test/redaction.test.ts</files>
  <read_first>
    src/core/paths.ts
    src/core/process.ts
    src/core/doctor.ts
    test/redaction.test.ts
    test/transaction.test.ts
  </read_first>
  <behavior>
    - nested Error/object/array containing a credential and tokenized URL becomes typed placeholders before support preview serialization.
    - cycles, depth, item count, string length, and total byte limits produce stable truncation markers.
    - raw input and snapshot bytes are not retained in the returned preview object.
  </behavior>
  <action>D-12, D-13, D-15에 따라 `RedactionContext`, `RedactedValue`, `ObservableEnvelope`, `SupportBundlePlan`을 정의하고 one-path tracer를 구현한다. replacement order는 exact credential→secret-bearing key/name→URL userinfo/token params→PEM/JWT/API-key→path alias다. `planSupportBundle`은 sources의 alias path/size/SHA-256/permissions와 redacted preview만 반환하고 byte write/network call을 수행하지 않는다. exact values는 call-scoped context로만 받고 global cache나 error string에 보존하지 않는다.</action>
  <acceptance_criteria>credential과 URL token을 가진 nested Error를 planSupportBundle에 넣은 test가 stable placeholders, fingerprint, bounds를 확인하고 원문 및 snapshot bytes 0회를 확인한다.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/redaction.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, or any secret sentinel appears in test diagnostics</fails_when>
  </verify>
  <reversibility rating="reversible">새 utility/service는 기존 API를 제거하지 않고 추가된다.</reversibility>
  <done>한 실제 diagnostic object가 중앙 redactor를 거쳐 safe support preview로 변환된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Segment-aware private path alias 확장</name>
  <files>src/core/paths.ts, src/core/redaction.ts, test/redaction.test.ts</files>
  <read_first>
    src/core/paths.ts
    src/core/redaction.ts
    test/paths.test.ts
    test/redaction.test.ts
  </read_first>
  <behavior>
    - exact home/temp/project roots map to stable aliases and descendants retain only relative suffixes.
    - prefix collisions such as /user/al and /user/alpha do not alias.
    - Windows comparison is case-normalized without altering displayed repository-relative suffixes.
  </behavior>
  <action>D-14에 따라 기존 `redactHome` 호환 동작을 `createPathAliases`/`aliasPath`로 확장한다. canonical segment boundary를 비교하고 priority가 높은 selected project→temp→home alias를 적용하며 user name을 포함한 absolute executable/cwd/path metadata에도 같은 function을 사용한다. URL이나 arbitrary message에 substring replace를 하지 말고 parsed path value에만 적용한다.</action>
  <acceptance_criteria>Windows/POSIX separator, case, exact root, descendant, sibling-prefix negative control이 모두 stable `~`, `<temp>`, `<project>` output을 검증한다.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/redaction.test.js dist/test/paths.test.js</automated>
    <fails_when>non-zero exit, or output reports 0 tests for either compiled test file</fails_when>
  </verify>
  <reversibility rating="reversible">기존 redactHome export를 호환 wrapper로 유지한다.</reversibility>
  <done>private path alias가 secret replacement 뒤의 단일 reusable stage가 된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime values→serializer | arbitrary nested values and Errors may contain secrets or identifying paths |
| managed state→support artifact | recovery bytes and process evidence cross into user-shareable material |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Information Disclosure | redaction.ts | high | mitigate | ordered exact/structural rules, recursive bounds, cross-surface corpus |
| T-01-08 | Information Disclosure | support-bundle.ts | high | mitigate | opaque snapshot metadata and no-network module boundary |
| T-01-09 | Tampering | support bundle plan | medium | mitigate | immutable source hashes, destination intent, and deterministic plan digest |
</threat_model>

## Artifacts this phase produces

| Kind | Artifact | Contract |
|------|----------|----------|
| New file | `src/core/redaction.ts` | `createRedactionContext`, `redactValue`, `serializeObservable`, `createRedactedExcerpt` |
| New file | `src/core/support-bundle.ts` | `planSupportBundle`, `renderSupportBundleBytes` |
| New types | `RedactionContext`, `ObservableEnvelope`, `SupportBundlePlan` | bounded redacted observability contracts |

## Probe coverage notes

- Generic secret logging is canon and remains in the STRIDE register; the local-only sharing and opaque-snapshot product constraints remain descriptor-less prohibitions.

<verification>
`npm run check`와 focused redaction/paths tests가 green이며 captured output에 corpus sentinel이 없다.
</verification>

<success_criteria>
- all recursive values are bounded and stable.
- path aliases preserve useful relative information without exposing private roots.
- support bundle preview is non-mutating, deterministic, and binds every source hash plus local destination intent.
</success_criteria>

<output>
Create `.planning/phases/01-safe-operation-boundary/01-03-SUMMARY.md` when done.
</output>
