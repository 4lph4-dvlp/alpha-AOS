---
phase: 07-cross-platform-release-proof
verified: 2026-09-20T05:08:55Z
status: gaps_found
score: 0/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
decision_coverage:
  honored: 16
  total: 16
  not_honored: []
gaps:
  - truth: "The same packed release bytes install, reconcile, diagnose, and uninstall successfully in Windows, macOS, and Linux fixture environments without touching real shared roots."
    status: failed
    reason: "The CI contract exists, but no workflow run exists for current HEAD 89fa676ed40b6e7321ac74e10901dfb2ca61f001. The latest listed CI run predates Phase 07, and the verifier observed only the local Windows lifecycle test. There is no macOS/Linux execution record for the current frozen archive and therefore no three-OS byte-identity proof."
    artifacts:
      - path: ".github/workflows/ci.yml"
        issue: "Defines an authoritative package job and three-OS consumers, but configuration is not observed execution evidence."
      - path: "test/tarball-fixture.test.ts"
        issue: "Passes locally on Windows; no current-head hosted macOS/Linux result exists."
    missing:
      - "Push or otherwise run current release HEAD through the authoritative package plus ubuntu-latest, macos-latest, and windows-latest jobs."
      - "Retain an inspectable run ID showing all three consumers verified the same artifact checksum and passed the packed lifecycle."
  - truth: "A versioned support matrix shows current real-host native discovery and representative invocation evidence for every claimed harness and surface, while every unproven combination is visibly limited or unsupported."
    status: failed
    reason: "The limitation reporting is honest, but the required proof is absent: the committed matrix has ten active cells marked UNVERIFIED, the verifier's Windows evaluation had 0 PROVEN and 10 UNVERIFIED cells, and the implementation explicitly makes every Antigravity receipt unrepresentable."
    artifacts:
      - path: "docs/SUPPORT_MATRIX.md"
        issue: "All ten active Codex, Antigravity, Pi, and Hermes cells are UNVERIFIED."
      - path: "src/core/support-matrix.ts"
        issue: "matchingReceipt returns null for every Antigravity entry, so REL-02 cannot currently be satisfied for an active claimed harness."
    missing:
      - "Extend the closed capability evidence model so Antigravity real-host discovery and invocation receipts are representable."
      - "Run current, cost-bounded native discovery and representative invocation canaries for every claimed harness/surface and retain inspectable redacted receipts."
      - "Regenerate the versioned matrix from those receipts without promoting unsupported combinations."
  - truth: "Paired positive and negative controls prove optional native invocation, project-pack scope, mandatory-gate blocking, and tree-off exclusion instead of inferring success from installed files."
    status: failed
    reason: "Project scope, gate, and tree-policy controls execute real local code, but the optional invocation pair constructs two Context7 observations in memory and defines the negative as an empty observation string. It never invokes a native harness or Context7, so the required native positive/negative behavior is not proven."
    artifacts:
      - path: "test/release-controls.test.ts"
        issue: "optionalInvocationPair supplies synthetic McpObservation values and an empty negative record rather than exercising native intent routing."
      - path: "docs/RELEASE_CONTROLS.md"
        issue: "The deterministic hashes attest to fixture objects generated by the test, not retained native host invocation receipts."
    missing:
      - "Replace or supplement the optional-invocation pair with a real native harness positive invocation and an ordinary-task negative control."
      - "Bind the public control ledger to retained, redacted invocation/non-invocation receipts from that run."
  - truth: "A real brownfield project completes discuss -> plan -> execute -> verify -> ship with one GSD writer while exercising one global optional capability, one project pack, one mandatory gate, one cross-harness handoff, and safe cleanup."
    status: failed
    reason: "The test creates a useful Git fixture and verifies pack/gate/cleanup and writer rollback code, but it labels direct core-function calls as lifecycle stages, injects offline Context7 observations, uses an in-memory Unified Memory runner, and creates the passing security receipt directly. It does not run a real GSD discuss/plan/execute/verify/ship workflow or real harness handoff. The shipped standalone runner is also broken because it imports dist/test/helpers/brownfield-fixture.js, while the npm package includes no dist/test files."
    artifacts:
      - path: "test/helpers/brownfield-fixture.ts"
        issue: "runBrownfieldLifecycle is an offline fixture driver, not a GSD lifecycle execution; Context7, memory, and review evidence are synthesized."
      - path: "scripts/run-brownfield-proof.mjs"
        issue: "Imports ../dist/test/helpers/brownfield-fixture.js, which npm pack excludes."
      - path: "package.json"
        issue: "Publishes dist/src and scripts, but not the runner's dist/test dependency."
    missing:
      - "Execute the actual brownfield GSD lifecycle and current native harness integration, retaining one-controller/writer evidence."
      - "Run the workflow from the authoritative frozen package bytes or otherwise bind every step to their digest."
      - "Move the standalone runner's implementation into a published runtime module or exclude the broken runner until it is self-contained."
  - truth: "The published v0.1.0 tarball excludes candidate/local state, contains only allowlisted files, is byte-identical to the three-OS tested artifact, and exposes provenance, the frozen lock, release notes, limitations, and a fresh public-registry smoke test."
    status: failed
    reason: "Local packing, allowlist, provenance, documentation, and Stage 1 smoke mechanisms work, but alpha-aos@0.1.0 is absent from the public npm registry (npm returned E404), no Stage 2 registry smoke ran, no release tarball/provenance bundle is retained in the repository, and no current-head three-OS run binds a published digest to tested bytes."
    artifacts:
      - path: "scripts/release.mjs"
        issue: "Implements publish and Stage 2 branches, but neither branch was executed."
      - path: "scripts/smoke-test.mjs"
        issue: "Registry mode exists, but the tracked verification ledger records it as unrun."
      - path: "scripts/verify-provenance.mjs"
        issue: "Validator works, but there is no shipped release bundle to validate."
    missing:
      - "After authorization, publish the exact CI-qualified alpha-aos-0.1.0.tgz to npm."
      - "Run node scripts/smoke-test.mjs --registry alpha-aos@0.1.0 and retain the result."
      - "Record registry integrity/tarball digest and prove it matches the authoritative three-OS artifact and provenance bundle."
deferred: []
---

# Phase 7: Cross-Platform Release Proof Verification Report

**Phase Goal:** Users receive one v0.1.0 package whose behavior, contents, provenance, support claims, and complete brownfield workflow are proven against the same frozen bytes.
**Verified:** 2026-09-20T05:08:55Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

The repository contains substantial release tooling and several strong local safety tests. It does not yet contain or point to the external evidence required to call v0.1.0 released or cross-platform-qualified. The distinction is decisive: the phase built mechanisms that can produce evidence, but the roadmap requires the resulting three-OS, real-host, brownfield, published-byte, and public-registry evidence itself.

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | The same packed bytes pass the complete isolated lifecycle on Windows, macOS, and Linux. | ✗ FAILED | Local Windows named test passed in 76.8s. Current HEAD `89fa676` has zero GitHub Actions runs; latest available CI run predates Phase 07. No current macOS/Linux result or shared-digest run exists. |
| 2 | Every claimed harness/surface has current real-host native discovery and representative invocation evidence. | ✗ FAILED | `docs/SUPPORT_MATRIX.md` has 10 active `UNVERIFIED` cells. Live Windows `doctor --matrix --json` produced 0 PROVEN / 10 UNVERIFIED / 2 UNSUPPORTED / 1 RESIDUE. `matchingReceipt()` categorically returns null for Antigravity. |
| 3 | Paired controls prove native optional invocation, project scope, mandatory gating, and tree-off exclusion. | ✗ FAILED | The focused test passed, but the optional-invocation pair feeds a synthetic two-row observation array to the matcher and feeds an empty string to the negative. No native harness or Context7 process runs. |
| 4 | A real brownfield GSD lifecycle completes with one writer and all five representative elements. | ✗ FAILED | The offline test passed and writer rollback is behaviorally proven, but `runBrownfieldLifecycle()` directly calls project/gate/uninstall APIs, fabricates Context7 and memory adapters, and labels the calls as GSD stages. The packaged standalone runner imports a dependency excluded from the package. |
| 5 | The published package is the three-OS-qualified artifact and has verifiable provenance plus a public-registry fresh-install smoke proof. | ✗ FAILED | npm registry lookup for `alpha-aos@0.1.0` returned E404. Stage 2 is explicitly unrun in `.planning/WINDOWS.md`; no current-head CI artifact or retained provenance bundle binds publication to tested bytes. |

**Score:** 0/5 truths verified (0 present-but-behavior-unverified)

### What Is Implemented and Working

- `npm run check` and `npm run build` pass (115 inputs, 224 outputs).
- The local Windows packed lifecycle passes install, reconcile, status, doctor, purge, host-path snapshot, and unchanged-tarball assertions.
- The tarball allowlist excludes candidate lock, planning state, and tests; `npm pack --dry-run` reported 168 files, no candidate lock, no planning files, and no `dist/test` files.
- Provenance verification rejects drift and validates packaged build outputs and the stable lock.
- Support diagnostics fail closed instead of fabricating `PROVEN` status.
- Planning-state tamper rollback is exercised by a passing named behavioral test.
- The release parser defaults to dry-run and refuses combining production publication with the test-only dirty-tree bypass.

These are valid implementation results, but they do not satisfy the release-proof outcomes above.

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `.github/workflows/ci.yml` | One Linux-built archive consumed by all three OS jobs | ⚠️ PARTIAL | Substantive and wired as a CI contract; no execution at current HEAD. |
| `scripts/audit-tarball.mjs` | Pure-Node fail-closed archive allowlist audit | ✓ VERIFIED | Substantive parser, canonical path checks, resource bounds, and focused tests. |
| `.npmignore` / `package.json` | Positive package surface and negative exclusions | ✓ VERIFIED | Dry-run pack excludes candidate, planning, tests, and TypeScript source. |
| `test/tarball-fixture.test.ts` | Installed-artifact lifecycle and host-root isolation | ⚠️ PARTIAL | Passed on Windows locally; no current macOS/Linux run. |
| `src/core/support-matrix.ts` | Receipt-backed four-tier matrix | ⚠️ PARTIAL | Honest evaluator and CLI wiring exist; active proof corpus is empty and Antigravity cannot be promoted. |
| `docs/SUPPORT_MATRIX.md` | Public current evidence ledger | ✗ HOLLOW | Correctly renders limitations, but every active cell is UNVERIFIED, so it documents the missing proof. |
| `test/release-controls.test.ts` | Four native positive/negative control pairs | ⚠️ PARTIAL | Three domains exercise local behavior; optional invocation is synthetic. |
| `docs/RELEASE_CONTROLS.md` | Inspectable receipts for paired controls | ⚠️ PARTIAL | Deterministic test-object hashes, not native host invocation receipts. |
| `test/helpers/brownfield-fixture.ts` | Realistic brownfield fixture and workflow driver | ⚠️ PARTIAL | Realistic repository; lifecycle/capability/handoff evidence is offline and synthetic. |
| `test/brownfield-gsd-cycle.test.ts` | Actual five-element GSD cycle and one-writer tests | ⚠️ PARTIAL | Strong worker and local API tests; not an actual GSD lifecycle or frozen-package run. |
| `scripts/run-brownfield-proof.mjs` | Reproducible standalone auditor | ✗ NOT_WIRED | Included in the package but imports excluded `dist/test/helpers/brownfield-fixture.js`. |
| `src/core/worker-authority.ts` | Worker mutation witness and rollback | ✓ VERIFIED | Named tamper test passed; original bytes restored and rogue file removed. |
| `scripts/verify-provenance.mjs` | Fail-closed release-bundle verifier | ✓ VERIFIED | Named valid-provenance test passed; negative tests exist and are active. |
| `CHANGELOG.md` / `docs/RELEASE_NOTES_v0.1.0.md` | Scope, residue, and limitations | ✓ VERIFIED | Clearly disclose UNVERIFIED support and the unrun registry gate. |
| `scripts/smoke-test.mjs` | Local and public-registry installed CLI smoke modes | ⚠️ PARTIAL | Local mode exists and was exercised; registry target is unpublished. |
| `scripts/release.mjs` | Complete guarded release orchestration | ⚠️ PARTIAL | Preview-first and publish branches are wired; only the test-only non-publish path was exercised. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `.github/workflows/ci.yml` package job | three-OS test matrix | uploaded `release-tarball`, `needs: package`, downloaded checksum | ⚠️ PARTIAL | Static workflow is correct; no current-head run validates it. |
| `.github/workflows/ci.yml` | `test/tarball-fixture.test.ts` | `ALPHA_AOS_RELEASE_TARBALL` consumed by the lifecycle test | ✓ WIRED | The test uses the CI artifact when the environment variable is present. |
| `src/cli.ts` | `src/core/support-matrix.ts` | `doctor --matrix` evaluates inventory plus host ledger | ✓ WIRED | CLI table and JSON named test is active. |
| `src/core/support-matrix.ts` | Antigravity evidence | `matchingReceipt` | ✗ NOT_WIRED | The function returns null for every Antigravity entry. |
| `test/release-controls.test.ts` | native Context7 invocation | optional positive/negative pair | ✗ DISCONNECTED | Static observation objects replace native invocation. |
| `scripts/run-brownfield-proof.mjs` | packaged brownfield driver | import from `dist/test/helpers` | ✗ NOT_WIRED | Import target is absent from npm contents. |
| `scripts/release.mjs` | provenance and Stage 1 smoke | generated tarball path | ✓ WIRED | Non-publish mechanism is complete. |
| `scripts/release.mjs` | npm registry and Stage 2 smoke | explicit `--publish`, then `--registry alpha-aos@0.1.0` | ⚠️ PARTIAL | Code path exists but has never been exercised; the registry package is absent. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| Support matrix | evaluated cells | real host inventory + `capabilities/ledger.json` | Yes when receipts exist | ⚠️ STATIC/EMPTY — current corpus yields no PROVEN active cell. |
| Release controls | receipt hashes | canonicalized test evidence objects | Fixture data | ⚠️ STATIC — optional invocation evidence is constructed in test. |
| Brownfield report | five lifecycle rows | direct fixture functions and synthetic adapters | Fixture data | ⚠️ STATIC — not actual GSD/native execution. |
| Provenance report | tarball/build/lock hashes | archive bytes | Yes | ✓ FLOWING for local/fixture archives; no published release archive exists. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Type/build integrity | `npm run check`; `npm run build` | Both exit 0; 115 inputs / 224 outputs | ✓ PASS |
| False-PROVEN refusal | named `support taxonomy refuses false PROVEN states` | 1 pass, 0 fail | ✓ PASS |
| Paired-control implementation | named `four release-control domains carry paired positive and negative evidence` | 1 pass, 0 fail; test remains synthetic for optional invocation | ✓ PASS (limited evidence) |
| Offline brownfield driver | named `brownfield ... proves all five benchmark elements offline` | 1 pass, 0 fail | ✓ PASS (offline fixture only) |
| Worker planning rollback | named tamper test | 1 pass, 0 fail | ✓ PASS |
| Provenance validator | named valid-provenance test | 1 pass, 0 fail | ✓ PASS |
| Preview-first argument gate | named release-mode test | 1 pass, 0 fail | ✓ PASS |
| Local packed lifecycle | named packed lifecycle test | 1 pass, 0 fail in 76.8s on `win32` | ✓ PASS (Windows only) |
| Packaged runner closure | `npm pack --dry-run --ignore-scripts --json` | runner present; dependency absent; zero `dist/test` files | ✗ FAIL |
| Current-head three-OS CI | `gh run list` matched against `git rev-parse HEAD` | 0 matching runs | ✗ FAIL |
| Public registry release | `npm view alpha-aos@0.1.0 ...` | E404 Not Found | ✗ FAIL |

### Probe Execution

No conventional or phase-declared `probe-*.sh` files exist. Step 7c is not applicable.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| REL-01 | 07-01 | Same packed bytes complete lifecycle on three OSes without host mutation | ✗ BLOCKED | Windows passes locally; no current-head macOS/Linux artifact consumers. |
| REL-02 | 07-02 | Current real-host discovery and invocation evidence for every active claim | ✗ BLOCKED | 0 PROVEN, 10 UNVERIFIED; Antigravity receipts structurally unrepresentable. |
| REL-03 | 07-02 | Paired native optional, project, gate, and opt-out controls | ✗ BLOCKED | Optional invocation is a synthetic observation fixture. |
| REL-04 | 07-03 | Real full brownfield GSD cycle with five representative elements | ✗ BLOCKED | Direct offline fixture driver, no actual GSD lifecycle/native handoff, and packaged runner is broken. |
| REL-05 | 07-01, 07-04 | Published package is allowlisted and equals the three-OS artifact | ✗ BLOCKED | Local allowlist passes; no published artifact and no current three-OS run. |
| REL-06 | 07-04 | Published provenance, frozen lock, notes, limitations, public-registry smoke | ✗ BLOCKED | Tooling/docs pass; npm E404 and Stage 2 unrun. |

No Phase 07 requirements are orphaned from plans. The requirement checkboxes marked complete in `REQUIREMENTS.md` are tracking claims, not verification evidence.

### Test Quality Audit

| Test File | Linked Req | Active | Skipped | Circular | Assertion Level | Verdict |
| --- | --- | ---: | ---: | --- | --- | --- |
| `test/tarball-fixture.test.ts` | REL-01, REL-05 | 7 | 0 | No | Behavioral | Strong locally; lacks current three-OS execution evidence. |
| `test/support-matrix.test.ts` | REL-02 | 4 | 0 | No | Value/integration | Correctly proves fail-closed taxonomy, not existence of real receipts. |
| `test/release-controls.test.ts` | REL-03 | 1 | 0 | No | Behavioral for 3 domains; fixture matching for optional invocation | Insufficient for the native-invocation clause. |
| `test/brownfield-gsd-cycle.test.ts` | REL-04 | 5 | 0 | No | Behavioral fixture | Proves local APIs and rollback, not a real GSD/native/frozen-byte cycle. |
| `test/provenance.test.ts` | REL-06 | 10 | 0 | No | Value/behavioral | Strong validator tests; no publication or registry smoke assertion. |

**Disabled tests on requirements:** 0.  
**Circular expected-value generators:** 0 blocking cases.  
**Insufficient assertions/evidence scope:** 3 — native optional invocation, real GSD brownfield workflow, and public-registry release outcome.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | ---: | --- | --- | --- |
| `scripts/run-brownfield-proof.mjs` | 11 | Published script imports excluded test build output | 🛑 Blocker | The advertised standalone proof cannot execute from the release package. |
| `test/release-controls.test.ts` | 91 | Synthetic MCP observations stand in for native optional invocation | 🛑 Blocker | Passing test cannot prove REL-03 native behavior. |
| `test/helpers/brownfield-fixture.ts` | 264 | Offline Context7 and in-memory memory runner stand in for real integrations | 🛑 Blocker | Passing cycle cannot prove REL-04's real workflow claim. |
| `scripts/release.mjs` | 19 | Test-only dirty-tree bypass | ℹ Info | Correctly guarded and incompatible with `--publish`; it is not shipped release evidence. |

No unreferenced `TBD`, `FIXME`, or `XXX` debt markers and no disabled requirement tests were found in phase-modified files.

### Decision Coverage

All 16 trackable `07-CONTEXT.md` decisions are represented in shipped artifacts. This non-blocking translation check does not establish that the resulting outcome evidence exists.

### Human Verification Required

None for verdict selection. The phase failures are programmatically observable: missing current-head hosted runs, an empty/unrepresentable proof corpus, synthetic test seams, a missing packaged import, and an absent npm registry package.

### Gaps Summary

The phase should not proceed as a completed v0.1.0 release. The central issue is not that release tooling is absent; most tooling is substantive and several local tests are strong. The issue is that all five roadmap truths require evidence outside those mechanisms, and that evidence was either never produced or was replaced with deterministic fixtures:

1. No current authoritative archive has passed all three OS jobs.
2. No active support cell is currently PROVEN, and Antigravity cannot be represented.
3. The optional native control is synthetic.
4. The brownfield workflow is an offline direct-API fixture and its packaged runner is broken.
5. The package is not published and the public-registry smoke gate is unrun.

There are no later milestone phases to which these gaps can be deferred.

---

_Verified: 2026-09-20T05:08:55Z_  
_Verifier: the agent (gsd-verifier)_
