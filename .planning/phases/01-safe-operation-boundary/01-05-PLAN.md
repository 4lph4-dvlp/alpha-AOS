---
phase: 01-safe-operation-boundary
plan: 05
type: execute
wave: 2
depends_on: [01-01, 01-04]
files_modified:
  - package.json
  - package-lock.json
  - src/core/validation.ts
  - test/validation.test.ts
autonomous: true
requirements: [SAFE-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-08: every managed format enters one strict syntax→exact version→closed schema→domain pipeline; only explicit namespaced extensions remain open."
    - "D-09: unregistered extension namespaces are preserved for round-trip/display but excluded from typed planning and apply payloads."
    - "D-10 and D-11: supported old versions produce read-only migration plans, newer versions refuse, and issues are bounded/sorted/coded with redacted actual-shape summaries."
  artifacts:
    - path: src/core/validation.ts
      provides: "Strict multi-format validation/version/extension engine"
      exports: [parseManagedDocument, validateManagedDocument, registerExtensionAdapter, createMigrationPlan]
    - path: package.json
      provides: "Exact Ajv, jsonc-parser, and approved smol-toml dependencies"
  key_links:
    - from: src/core/validation.ts
      to: test/validation.test.ts
      via: "table-driven syntax/version/schema/domain cases"
      pattern: "parseManagedDocument|ValidationIssue"
  prohibitions:
    - statement: "Validation MUST NOT coerce, default, remove, or otherwise rewrite untrusted input while deciding whether it is safe to read or mutate."
      requirement_id: SAFE-05
      category: transparency
      status: unverified
      flagged: true
---

<objective>
managed JSON/YAML/TOML documents를 위한 deterministic strict validation engine을 만든다.

Purpose: duplicate와 unknown field가 parser cast에 묻히기 전에 거부되고 schema evolution이 previewable하게 된다.
Output: exact dependencies, ValidationIssue contracts, parsers/version router/extension registry/migration-plan API.
</objective>

<execution_context>
@C:/Users/alpha/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safe-operation-boundary/01-CONTEXT.md
@.planning/phases/01-safe-operation-boundary/01-RESEARCH.md
@.planning/phases/01-safe-operation-boundary/01-PATTERNS.md
@.planning/phases/01-safe-operation-boundary/01-01-SUMMARY.md
@.planning/phases/01-safe-operation-boundary/01-04-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Duplicate JSON에서 stable ValidationIssue까지 tracer 연결</name>
  <files>src/core/validation.ts, test/validation.test.ts</files>
  <read_first>
    src/core/catalog.ts
    src/core/project.ts
    test/validation.test.ts
    test/catalog.test.ts
  </read_first>
  <behavior>
    - duplicate JSON property is rejected before parsed value reaches schema validation.
    - a current-version valid document reaches strict Ajv 2020 validation and domain callback.
    - issues expose stable code/documentPath/expected/actualShape and sort deterministically with a fixed cap.
  </behavior>
  <action>D-08 and D-11에 따라 `ManagedDocumentKind`, `ValidationIssue`, `ValidationResult`, `ManagedSchemaRoute`와 `parseManagedDocument` tracer를 만든다. jsonc-parser visitor로 duplicate/property offsets를 수집하고, YAML은 `parseDocument`의 unique keys·single document·alias limit을 사용하며, TOML은 approved parser를 호출한다. raw values는 issue message에 넣지 않고 type/length/key-count shape만 중앙 redactor로 요약한다. issue order는 documentPath→code→expected, cap은 50으로 고정한다.</action>
  <acceptance_criteria>duplicate JSON test가 schema/domain callback 호출 0회, stable issue code/path/order, secret sentinel 0회를 확인하고 current valid JSON은 typed value를 반환한다.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/validation.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, or validation test diagnostics contain the secret sentinel</fails_when>
  </verify>
  <reversibility rating="reversible">새 engine은 기존 loaders가 다음 계획에서 opt in하는 additive seam이다.</reversibility>
  <done>untrusted bytes가 typed current document가 되거나 stable bounded issues로 끝나는 한 경로가 동작한다.</done>
</task>

<task type="auto">
  <name>Task 2: Exact strict-validation dependencies 고정</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    package.json
    package-lock.json
    .planning/phases/01-safe-operation-boundary/01-RESEARCH.md
    .planning/phases/01-safe-operation-boundary/01-04-SUMMARY.md
  </read_first>
  <precondition>01-04-SUMMARY.md records explicit approval of smol-toml@1.8.0 with the reviewed integrity.</precondition>
  <action>research-approved `ajv@8.20.0`, `jsonc-parser@3.3.1`, approved `smol-toml@1.8.0`을 exact production dependencies로 설치하고 package-lock integrity를 갱신한다. Ajv는 2020 constructor에 strict/allErrors true, coerceTypes/useDefaults/removeAdditional false로만 생성한다. 추가 parser나 transitive package를 직접 pin하지 않는다.</action>
  <acceptance_criteria>package.json에 세 exact version이 있고 npm lock resolves them with integrity; npm install lifecycle output shows no package install scripts from the audited direct dependencies.</acceptance_criteria>
  <verify>
    <automated>npm install --ignore-scripts && npm ls ajv@8.20.0 jsonc-parser@3.3.1 smol-toml@1.8.0 --depth=0</automated>
    <fails_when>non-zero exit, an invalid dependency tree is reported, or any resolved direct version differs from the three exact versions</fails_when>
  </verify>
  <reversibility rating="reversible">package and lock changes can be reverted together before release.</reversibility>
  <done>strict parsers/schema engine dependencies exactly match the audited versions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Version router, migration plan, extension inertness 완성</name>
  <files>src/core/validation.ts, test/validation.test.ts</files>
  <read_first>
    src/core/validation.ts
    test/validation.test.ts
    src/core/catalog.ts
    src/types.ts
  </read_first>
  <behavior>
    - exact current version selects only its compiled validator.
    - registered supported-old version is read-only and returns deterministic migration operations/digest without mutation.
    - unknown newer and unsupported older versions return distinct stable refusal codes.
    - only a registered namespace+schema+adapter can contribute normalized extension data.
  </behavior>
  <action>D-09, D-10, D-11에 따라 exact schema-version route table과 `createMigrationPlan`을 완성한다. old-version route는 current typed value로 암묵 변환하지 않고 read-only parsed form plus migration operations/source hash/target hash를 반환한다. unknown extensions는 raw subtree를 preserved map으로 분리하고 normalized core value에서 제외한다. registered adapter는 namespace syntax와 its schema validation을 통과한 data만 별도 typed extension result에 넣는다.</action>
  <acceptance_criteria>old/current/newer/unsupported version cases and registered/unregistered extension cases have deterministic deep-equal results across repeated runs; unregistered data cannot change operation plan digest.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/validation.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, or repeated validation outputs differ</fails_when>
  </verify>
  <reversibility rating="reversible">version routes are explicit registrations and no persistent migration runs here.</reversibility>
  <done>schema evolution and extension behavior exactly implement D-09 through D-11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document bytes→typed domain | ambiguous external syntax becomes trusted planning/apply data |
| extension namespace→core operation | third-party fields attempt to influence mutation semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Tampering/DoS | validation.ts | high | mitigate | duplicate/alias limits, closed schema, domain invariants, bounded issues |
| T-01-11 | Elevation | extension registry | high | mitigate | namespace+schema+adapter registration required for influence |
| T-01-SC | Tampering | npm dependencies | high | mitigate | exact audited versions, human checkpoint for SUS package, integrity lock |
</threat_model>

## Artifacts this phase produces

| Kind | Artifact | Contract |
|------|----------|----------|
| New file | `src/core/validation.ts` | strict syntax, version routing, closed validation, domain and extension APIs |
| New types/functions | `ValidationIssue`, `ValidationResult`, `ManagedSchemaRoute`, `parseManagedDocument`, `validateManagedDocument`, `registerExtensionAdapter`, `createMigrationPlan` | deterministic document boundary |
| Dependency | `ajv@8.20.0`, `jsonc-parser@3.3.1`, `smol-toml@1.8.0` | exact audited parsers/validator |

## Probe coverage notes

- Generic parser abuse is canon and recorded in STRIDE; silent input rewriting and extension influence are retained as phase-specific descriptor-less prohibitions.

<verification>
`npm run check`, focused validation tests, and exact dependency tree succeed.
</verification>

<success_criteria>
- syntax ambiguity is rejected before schema evaluation.
- version and extension behavior is deterministic and non-mutating.
- issue lists are capped, sorted, coded, and redacted.
</success_criteria>

<output>
Create `.planning/phases/01-safe-operation-boundary/01-05-SUMMARY.md` when done.
</output>
