---
phase: 10-three-os-ci-baseline
plan: 03
type: execute
wave: 2
depends_on: [10-02]
files_modified:
  - test/mcp-proxy.test.ts
  - test/gate-engine.test.ts
  - test/gate-lifecycle.test.ts
  - .planning/debug/ci02-windows-alpha-aos-host-leak.md
autonomous: true
requirements: [CI-02]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "test/mcp-proxy.test.ts sets `process.env.ALPHA_AOS_STATE_DIR` to `join(tmpdir(), \"alpha-aos-test-mcp-proxy-state\")` at module scope, before any upstream environment policy is built, so the pinned context7, exa and firecrawl startups put their npx cache under a test-owned root and never under the host state root (CI-02, D-02)."
    - "The gate-engine receipt test journals into its own scratch state root through a restored ALPHA_AOS_STATE_DIR redirect, and every gate-lifecycle CLI spawn receives ALPHA_AOS_STATE_DIR as an EnvironmentPolicy literal pointing at a scratch state root beside (never inside) the fixture repository (CI-02, D-02)."
    - "Each of the three regression assertions was observed failing against the unfixed test code, with the command, output excerpt and RED commit recorded in the debug record, and passes after the fix (D-08, ROADMAP success criterion 4)."
    - "Running mcp-proxy.test.js and tarball-fixture.test.js concurrently on a scratch home reports fail 0 and leaves no `.alpha-aos` in that home (ROADMAP success criterion 2, locally)."
    - "The post-fix per-file home-write probe shows zero home entries for mcp-proxy.test.js, gate-engine.test.js and gate-lifecycle.test.js."
    - "`src/` and `.github/workflows/ci.yml` are byte-identical to commit c6415a2: the fix is test-side only because the cause is a test-isolation leak and the D-03 product audit is not triggered."
  prohibitions:
    - statement: "MUST NOT fix the leak by serializing the packed-lifecycle fixture, running it last, adding a suite-wide host guard, or editing `hostMutationTargets` or the host assertion in test/tarball-fixture.test.ts (D-02)."
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT redirect the state root, HOME or USERPROFILE in `.github/workflows/ci.yml`; that would move the oracle's own `hostStateRoot` off the real `~/.alpha-aos`."
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT run any RED observation or probe against the developer's real home, and MUST NOT delete or clean the developer's real `~/.alpha-aos`; every run uses a fresh scratch HOME/USERPROFILE with ALPHA_AOS_STATE_DIR unset."
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT change product code under `src/` in this plan (the `writeGateReceipt` stateRoot option and the `upstreamEnvironmentPolicy` ambient write root stay recorded follow-ups)."
      category: architectural
      status: resolved
      verification: judgment
  artifacts:
    - path: "test/mcp-proxy.test.ts"
      provides: "module-scope hostStateRootAtLoad and testStateRoot, the ALPHA_AOS_STATE_DIR redirect, and the regression test for the pinned-startup cache location"
      contains: "alpha-aos-test-mcp-proxy-state"
    - path: "test/gate-engine.test.ts"
      provides: "redirectStateRoot helper and the journal-location regression assertion"
      contains: "the gate receipt journal must land in the test-owned state root, not the host state root"
    - path: "test/gate-lifecycle.test.ts"
      provides: "gateEnvironment(stateRoot) policy for every CLI spawn and the journal-location regression assertion"
      contains: "gateEnvironment(stateRoot)"
    - path: ".planning/debug/ci02-windows-alpha-aos-host-leak.md"
      provides: "RED observations (D-08) and the local fix evidence"
      contains: "RED commit"
  key_links:
    - from: "test/mcp-proxy.test.ts module scope"
      to: "src/core/paths.ts userStateRoot via src/core/mcp-proxy.ts proxyCacheRoot"
      via: "process.env.ALPHA_AOS_STATE_DIR = testStateRoot, read lazily by userStateRoot()"
      pattern: "process\\.env\\.ALPHA_AOS_STATE_DIR = testStateRoot"
    - from: "test/gate-lifecycle.test.ts gateEnvironment"
      to: "dist/src/cli.js gate check child"
      via: "EnvironmentPolicy.literal ALPHA_AOS_STATE_DIR materialized by runProcess"
      pattern: "ALPHA_AOS_STATE_DIR: stateRoot"
    - from: "test/gate-engine.test.ts redirectStateRoot"
      to: "src/core/gate-receipt.ts writeGateReceipt applyFileTransaction stateRoot userStateRoot()"
      via: "process.env.ALPHA_AOS_STATE_DIR saved, set and restored in context.after"
      pattern: "redirectStateRoot\\(context"
---

<objective>
Fix the diagnosed CI-02 cause at its source: the three test files that write the real host `~/.alpha-aos` while the packed-lifecycle fixture has its snapshot window open (debug record from 10-02, D-01 class 3). Per D-02, each leaking test gets its own state root; the fixture's oracle stays exactly as it is.

Purpose: The windows packed lifecycle leaves every real host managed path byte-identical because nothing else in the suite writes there any more, and three deterministic regression assertions pin where each writer's bytes land. Each assertion is observed red against the unfixed test before the fix (D-08).

Output: redirected state roots and regression assertions in test/mcp-proxy.test.ts, test/gate-engine.test.ts and test/gate-lifecycle.test.ts; the RED observations and local fix evidence appended to `.planning/debug/ci02-windows-alpha-aos-host-leak.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-three-os-ci-baseline/10-CONTEXT.md
@.planning/phases/10-three-os-ci-baseline/10-RESEARCH.md
@.planning/phases/10-three-os-ci-baseline/10-PATTERNS.md
@.planning/phases/10-three-os-ci-baseline/10-02-SUMMARY.md
@.planning/debug/ci02-windows-alpha-aos-host-leak.md
@test/mcp-proxy.test.ts
@test/gate-engine.test.ts
@test/gate-lifecycle.test.ts
@src/core/process.ts
</context>

<interfaces>
Existing, unchanged product contracts:
- `userStateRoot()` (src/core/paths.ts:93-97): returns `resolve(ALPHA_AOS_STATE_DIR)` when that name is set and non-blank, else `join(homedir(), ".alpha-aos")`. It is read lazily at call time.
- `proxyCacheRoot(stateRoot = userStateRoot())` returns `join(stateRoot, "mcp-cache")`; `upstreamEnvironmentPolicy(server, source = process.env)` returns an EnvironmentPolicy whose `literal.npm_config_cache` is `join(proxyCacheRoot(), "npm-cache")` (src/core/mcp-proxy.ts:199-246).
- `writeGateReceipt(projectRoot, receipt)` journals through `applyFileTransaction({ stateRoot: userStateRoot(), ... })` and returns `{ receiptPath, transactionId }`; the journal file is `join(stateRoot, "journal", transactionId + ".json")` (src/core/transaction.ts:224).
- `EnvironmentPolicy { required?, optional?, literal?: Readonly record of string to string, source? }` (src/core/process.ts:32-44). `commandProbeEnvironment({ source })` passes HOME/USERPROFILE through and does NOT pass ALPHA_AOS_STATE_DIR (src/core/process.ts:922-958).

Existing test anchors:
- test/mcp-proxy.test.ts: imports `tmpdir` from node:os (line 17) and `dirname, join, resolve` from node:path (line 18); `userStateRoot` import (line 38); `MCP_SERVER_IDS` and `upstreamEnvironmentPolicy` imports (lines 25-37); `expectedProxyCacheRoot(stateRoot)` (line 669); `startPinnedServer` (702); startup tests (812, 824, 834); `the module pins a proxy cache root under the managed state root` (854); `a proxy launch writes nothing into the user's home` saves and restores ALPHA_AOS_STATE_DIR itself (932-948).
- test/gate-engine.test.ts: `scratchRoot(context, label)` (lines 15-21); the leaking call `writeGateReceipt(root, receipt)` inside `writeGateReceipt writes an atomic receipt that readGateReceiptStrict validates (D-06, SAFE-03)` (line 184).
- test/gate-lifecycle.test.ts: `probeEnv = commandProbeEnvironment({ source: process.env })` (line 20); `scratchRoot` (22-28); seven `runProcess({` calls in four tests (lines 108, 146, 182, 196, 228, 240, 257); each test's repository is `created.fixture.path` under the scratch `parent`.

Scratch-home run environment (Git Bash, developer host; never the real home):
- `H=$(mktemp -d)`, `WH=$(cygpath -w "$H" 2>/dev/null || printf '%s' "$H")`
- prefix `env -u ALPHA_AOS_STATE_DIR -u CODEX_HOME -u CLAUDE_CONFIG_DIR -u HERMES_HOME -u PI_CODING_AGENT_DIR -u ANTIGRAVITY_CONFIG_DIR HOME="$WH" USERPROFILE="$WH"`, plus `ALPHA_AOS_REQUIRE_UPSTREAM_MCP=1` for any run that includes mcp-proxy.test.js.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — pin the mcp-proxy pinned startups to a test-owned state root, RED then GREEN, proven end-to-end on a scratch home</name>
  <files>test/mcp-proxy.test.ts, .planning/debug/ci02-windows-alpha-aos-host-leak.md</files>
  <read_first>
    test/mcp-proxy.test.ts (lines 13-48 imports and module constants, 665-753 expectedProxyCacheRoot and startPinnedServer, 812-972 startup tests and state-root tests)
    src/core/mcp-proxy.ts (lines 199-246)
    src/core/paths.ts (lines 93-97)
    .planning/debug/ci02-windows-alpha-aos-host-leak.md (sections Writer and Regression RED observations)
    .planning/phases/10-three-os-ci-baseline/10-RESEARCH.md (Pattern 3, Pitfalls 4 and 5, Alternatives Considered)
  </read_first>
  <behavior>
    - New test `pinned upstream startups keep their npx cache under the test-owned state root, never the host state root`: `userStateRoot()` equals `testStateRoot`; `testStateRoot` differs from `hostStateRootAtLoad` and is not inside it; for every id in `MCP_SERVER_IDS`, `upstreamEnvironmentPolicy(id).literal?.npm_config_cache` equals `join(expectedProxyCacheRoot(testStateRoot), "npm-cache")` and does not start with `hostStateRootAtLoad`.
    - The test needs no network and writes nothing; it fails deterministically when the module-scope redirect is absent.
    - The existing `the module pins a proxy cache root under the managed state root`, `the upstream child's write locations are declared, never inherited` and `a proxy launch writes nothing into the user's home` tests still pass unchanged.
  </behavior>
  <action>
    Implement D-02 for the primary writer, with the D-08 red-first sequence.

    1. RED. Add `homedir` to the node:os import and `relative` to the node:path import. Immediately after the import block, declare `const hostStateRootAtLoad = resolve(process.env.ALPHA_AOS_STATE_DIR?.trim() || join(homedir(), ".alpha-aos"));` and `const testStateRoot = join(tmpdir(), "alpha-aos-test-mcp-proxy-state");`. Add the new test from the behavior block next to `the module pins a proxy cache root under the managed state root`, using `relative(hostStateRootAtLoad, testStateRoot)` to prove the test root is outside the host root. Do not assert that the host root is unchanged (other files run concurrently, so that assertion would itself race, 10-RESEARCH anti-patterns); assert the positive location instead. Build, then run the regression test on a scratch home (first verify command, prefixed as in the interfaces block) and confirm it FAILS because `userStateRoot()` still resolves the scratch home's `.alpha-aos`. Save the command and a short output excerpt (test name, `not ok`, the actual versus expected root). Commit as `test(10-03): pin mcp-proxy startups to a test-owned state root (RED)`.
    2. GREEN. Directly below the two constants add `process.env.ALPHA_AOS_STATE_DIR = testStateRoot;` with one comment line stating the non-obvious constraint: node --test runs each file in its own process so this cannot reach another file, and the stable tmp root keeps the npx cache warm across the two CI test steps without touching the host root (10-RESEARCH Pitfall 4). Rebuild, rerun: the regression test passes. Commit as `fix(10-03): redirect mcp-proxy test state root out of the host home`.
    3. End-to-end: run the whole mcp-proxy.test.js on a fresh scratch home with `ALPHA_AOS_REQUIRE_UPSTREAM_MCP=1` (second verify command): fail 0, and the scratch home has no `.alpha-aos`.
    4. Append to the debug record's `## Regression RED observations (D-08)` section: test name, file, RED commit sha, the RED command and output excerpt, the GREEN commit sha and the passing result. Metadata only.
  </action>
  <verify>
    <automated>npm run build && H=$(mktemp -d) && WH=$(cygpath -w "$H" 2>/dev/null || printf '%s' "$H") && env -u ALPHA_AOS_STATE_DIR -u CODEX_HOME -u CLAUDE_CONFIG_DIR -u HERMES_HOME -u PI_CODING_AGENT_DIR -u ANTIGRAVITY_CONFIG_DIR HOME="$WH" USERPROFILE="$WH" node --test --test-name-pattern="test-owned state root" dist/test/mcp-proxy.test.js</automated>
    <fails_when>after the GREEN commit: non-zero exit, a `not ok` line, or `# pass 0` (the name filter matched nothing); before the GREEN commit this command is expected to show `not ok` for the new test, and that output is the RED evidence</fails_when>
    <automated>H=$(mktemp -d) && WH=$(cygpath -w "$H" 2>/dev/null || printf '%s' "$H") && env -u ALPHA_AOS_STATE_DIR -u CODEX_HOME -u CLAUDE_CONFIG_DIR -u HERMES_HOME -u PI_CODING_AGENT_DIR -u ANTIGRAVITY_CONFIG_DIR HOME="$WH" USERPROFILE="$WH" ALPHA_AOS_REQUIRE_UPSTREAM_MCP=1 node --test dist/test/mcp-proxy.test.js && test ! -e "$H/.alpha-aos"</automated>
    <fails_when>non-zero exit from node --test (`# fail` above 0), or `test ! -e` fails because `.alpha-aos` exists in the scratch home after the run</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'process.env.ALPHA_AOS_STATE_DIR = testStateRoot;' test/mcp-proxy.test.ts` prints 1, and that line sits above the first `test(` call in the file.
    - `grep -c 'alpha-aos-test-mcp-proxy-state' test/mcp-proxy.test.ts` prints 1.
    - The test name `pinned upstream startups keep their npx cache under the test-owned state root, never the host state root` appears verbatim.
    - `git log --oneline -3 -- test/mcp-proxy.test.ts` shows the RED `test(10-03)` commit before the `fix(10-03)` commit, and the debug record names both shas.
    - The full mcp-proxy.test.js run on a scratch home reports fail 0 and leaves no `.alpha-aos` in that home.
  </acceptance_criteria>
  <done>The primary writer no longer resolves the host state root, a deterministic regression assertion pins it, and the RED-to-GREEN evidence is in the debug record.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Redirect the gate-receipt writers in gate-engine and gate-lifecycle to scratch state roots, RED then GREEN</name>
  <files>test/gate-engine.test.ts, test/gate-lifecycle.test.ts, .planning/debug/ci02-windows-alpha-aos-host-leak.md</files>
  <read_first>
    test/gate-engine.test.ts (lines 1-21 imports and scratchRoot, 159-195 the receipt test)
    test/gate-lifecycle.test.ts (lines 1-28 imports, probeEnv and scratchRoot, 92-264 the four CLI tests)
    test/mcp-proxy.test.ts (lines 941-948: the save, set and restore pattern for ALPHA_AOS_STATE_DIR)
    src/core/gate-receipt.ts (lines 105-121)
    src/core/process.ts (lines 32-44 EnvironmentPolicy, 922-958 commandProbeEnvironment)
    src/core/transaction.ts (line 224 journal path)
  </read_first>
  <behavior>
    - gate-engine: inside `writeGateReceipt writes an atomic receipt that readGateReceiptStrict validates (D-06, SAFE-03)`, the journal file `join(stateRoot, "journal", written.transactionId + ".json")` exists, where `stateRoot = join(root, "state")`; assertion message `the gate receipt journal must land in the test-owned state root, not the host state root`.
    - gate-lifecycle: inside `Execution of gate check passes when engine succeeds, creating receipt and unblocking lifecycle (GATE-02, GATE-03)`, after the first `gate check --obligation security-review --json`, `join(stateRoot, "journal")` holds at least one `.json` file; assertion message `gate check must journal its receipt into the test-owned state root, not the host state root`.
    - All existing assertions in both files keep passing, including the staleness test (the state root sits beside the repository, so the gate's working-tree digest is unaffected).
  </behavior>
  <action>
    Implement D-02 for the two gate-receipt writers, red first per D-08.

    1. RED, gate-engine. Import `existsSync` from node:fs. In the receipt test, declare `const stateRoot = join(root, "state");` and, after `writeGateReceipt`, add the journal-existence assertion from the behavior block. Do not redirect yet.
    2. RED, gate-lifecycle. Add `readdir` to the node:fs/promises import. In the passing-check test, declare `const stateRoot = join(parent, "state");` (a sibling of the repository at `parent/repo`, never inside it) and after the first check's assertions add the journal-directory assertion from the behavior block, reading the directory with a `.catch` that yields an empty list so the failure is an assertion message rather than an ENOENT stack. Do not change any spawn environment yet.
    3. Build and run both files on a scratch home (verify command). Both new assertions must FAIL (the journals land in the scratch home's `.alpha-aos`). Save the command and output excerpts. Commit as `test(10-03): pin gate receipt journals to test-owned state roots (RED)`.
    4. GREEN, gate-engine. Add `function redirectStateRoot(context: TestContext, stateRoot: string): void` that saves the current ALPHA_AOS_STATE_DIR, sets it to `stateRoot`, and restores it (deleting the name when it was absent) in `context.after`, following test/mcp-proxy.test.ts:941-948. Call `redirectStateRoot(context, stateRoot);` in the receipt test before `writeGateReceipt`.
    5. GREEN, gate-lifecycle. Add `type EnvironmentPolicy` to the existing ../src/core/process.js import and `function gateEnvironment(stateRoot: string): EnvironmentPolicy` returning the probe policy with its literal map extended by `ALPHA_AOS_STATE_DIR: stateRoot` (spread `probeEnv`, then `literal` spread from `probeEnv.literal` plus the new name). The spawned CLI never inherits the parent's ALPHA_AOS_STATE_DIR because `commandProbeEnvironment` does not list it, so the literal is the only route. In all four CLI tests declare `const stateRoot = join(parent, "state");` and pass `environment: gateEnvironment(stateRoot)` to every one of the seven `runProcess` calls, so no test in the file can reach the host state root.
    6. Rebuild and rerun on a scratch home: both files pass and the scratch home holds no `.alpha-aos`. Commit as `fix(10-03): journal gate receipts into test-owned state roots`.
    7. Append both RED observations (test name, file, RED commit sha, command, output excerpt) and the GREEN commit sha to the debug record's D-08 section.
  </action>
  <verify>
    <automated>npm run build && H=$(mktemp -d) && WH=$(cygpath -w "$H" 2>/dev/null || printf '%s' "$H") && env -u ALPHA_AOS_STATE_DIR -u CODEX_HOME -u CLAUDE_CONFIG_DIR -u HERMES_HOME -u PI_CODING_AGENT_DIR -u ANTIGRAVITY_CONFIG_DIR HOME="$WH" USERPROFILE="$WH" node --test dist/test/gate-engine.test.js dist/test/gate-lifecycle.test.js && test ! -e "$H/.alpha-aos"</automated>
    <fails_when>after the GREEN commit: non-zero exit from node --test (`# fail` above 0), or `.alpha-aos` exists in the scratch home; before GREEN this command is expected to report the two new assertions as `not ok`, which is the RED evidence</fails_when>
    <automated>test "$(grep -c 'runProcess({' test/gate-lifecycle.test.ts)" -eq "$(grep -c 'environment: gateEnvironment(stateRoot)' test/gate-lifecycle.test.ts)" && test "$(grep -c 'runProcess({' test/gate-lifecycle.test.ts)" -ge 7</automated>
    <fails_when>the two counts differ (some CLI spawn still uses an environment without the state-root literal) or fewer than 7 spawns are counted</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'the gate receipt journal must land in the test-owned state root, not the host state root' test/gate-engine.test.ts` prints 1 and `grep -c 'redirectStateRoot(context, stateRoot);' test/gate-engine.test.ts` prints at least 1.
    - `grep -c 'gate check must journal its receipt into the test-owned state root, not the host state root' test/gate-lifecycle.test.ts` prints 1 and `grep -c 'ALPHA_AOS_STATE_DIR: stateRoot' test/gate-lifecycle.test.ts` prints 1.
    - Every `runProcess({` call in gate-lifecycle.test.ts passes `environment: gateEnvironment(stateRoot)` (second verify command).
    - Both files pass on a scratch home and leave no `.alpha-aos` in it.
    - `git log --oneline -3 -- test/gate-engine.test.ts test/gate-lifecycle.test.ts` shows the RED `test(10-03)` commit before the `fix(10-03)` commit; the debug record names both shas and both RED excerpts.
  </acceptance_criteria>
  <done>Both gate-receipt writers journal into scratch state roots, two deterministic regression assertions pin that, and their RED-to-GREEN evidence is recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the fix locally — pair reproduction, per-file home-write probe, and suite run on a scratch home — and record it</name>
  <files>.planning/debug/ci02-windows-alpha-aos-host-leak.md</files>
  <read_first>
    .planning/debug/ci02-windows-alpha-aos-host-leak.md (Evidence and Follow-ups sections)
    .planning/phases/10-three-os-ci-baseline/10-RESEARCH.md (Code Examples: pair reproduction and per-file probe; Pitfalls 5 and 6; Assumption A2)
    scripts/run-tests.mjs (header: --files usage)
  </read_first>
  <action>
    Close the local half of ROADMAP success criterion 2 and record it per D-07. All runs use fresh scratch homes; nothing touches the real home.

    1. Pair reproduction after the fix (first verify command): `mcp-proxy.test.js` and `tarball-fixture.test.js` with `--test-concurrency=2` on a scratch home and `ALPHA_AOS_REQUIRE_UPSTREAM_MCP=1`. Expected: fail 0, and no `.alpha-aos` in the scratch home.
    2. Per-file home-write probe (10-RESEARCH Pitfall 5): write a throwaway script in the session scratchpad (not committed) that runs each `dist/test/*.test.js` alone under the scratch-home prefix with `ALPHA_AOS_REQUIRE_UPSTREAM_MCP=1`, each with its own fresh scratch home, four at a time, and records per file the exit status, the count of entries under the home (`find HOME -mindepth 1`) and those entry paths up to depth 4. Expected: mcp-proxy.test.js, gate-engine.test.js and gate-lifecycle.test.js write 0 entries. Record capability-oracle.test.js and preview.test.js rows as observed, next to their research classification (dev-host-only, Assumption A2; PowerShell startup cache outside managed paths). If any OTHER file now writes under the home's `.alpha-aos`, stop and report it as an additional same-class writer instead of closing the task.
    3. Suite run on a scratch home (second verify command): every compiled suite except capability-oracle.test.js through `scripts/run-tests.mjs --files`, fail 0, and no `.alpha-aos` in the scratch home. Then run capability-oracle.test.js alone on another scratch home and record its result and home entries; it is excluded from the local gate only because its failures under a redirected home come from this developer host's PATH (10-RESEARCH Finding 3, Pitfall 6); the CI legs in 10-04 run it.
    4. Append a `## Fix (local)` section to the debug record: the three fix commit shas, the pair reproduction command and TAP summary after the fix, the per-file probe table after the fix, the suite result, the capability-oracle observation, and a statement that `src/` and `.github/workflows/ci.yml` are unchanged since c6415a2. Keep `status: diagnosed`; 10-04 sets `resolved` once the proof run is green.
  </action>
  <verify>
    <automated>npm run build && H=$(mktemp -d) && WH=$(cygpath -w "$H" 2>/dev/null || printf '%s' "$H") && env -u ALPHA_AOS_STATE_DIR -u CODEX_HOME -u CLAUDE_CONFIG_DIR -u HERMES_HOME -u PI_CODING_AGENT_DIR -u ANTIGRAVITY_CONFIG_DIR HOME="$WH" USERPROFILE="$WH" ALPHA_AOS_REQUIRE_UPSTREAM_MCP=1 node --test --test-concurrency=2 dist/test/mcp-proxy.test.js dist/test/tarball-fixture.test.js && test ! -e "$H/.alpha-aos"</automated>
    <fails_when>non-zero exit from node --test (the packed lifecycle or any mcp-proxy test `not ok`, `# fail` above 0), or `.alpha-aos` exists in the scratch home</fails_when>
    <automated>H=$(mktemp -d) && WH=$(cygpath -w "$H" 2>/dev/null || printf '%s' "$H") && env -u ALPHA_AOS_STATE_DIR -u CODEX_HOME -u CLAUDE_CONFIG_DIR -u HERMES_HOME -u PI_CODING_AGENT_DIR -u ANTIGRAVITY_CONFIG_DIR HOME="$WH" USERPROFILE="$WH" ALPHA_AOS_REQUIRE_UPSTREAM_MCP=1 node scripts/run-tests.mjs --files $(ls dist/test/*.test.js | grep -v capability-oracle) && test ! -e "$H/.alpha-aos"</automated>
    <fails_when>non-zero exit from the runner (`# fail` above 0 or a missing compiled file), or `.alpha-aos` exists in the scratch home</fails_when>
    <automated>git diff --quiet c6415a2 -- src/ .github/workflows/ci.yml && grep -q '^## Fix (local)' .planning/debug/ci02-windows-alpha-aos-host-leak.md && grep -q 'RED commit' .planning/debug/ci02-windows-alpha-aos-host-leak.md</automated>
    <fails_when>git diff reports a change under src/ or in ci.yml since c6415a2 (exit 1), or the debug record lacks the Fix (local) section or the RED commit references</fails_when>
  </verify>
  <acceptance_criteria>
    - The pair reproduction after the fix reports fail 0 with the packed lifecycle test `ok`, and the scratch home has no `.alpha-aos`.
    - The per-file probe table in the debug record shows 0 entries for mcp-proxy.test.js, gate-engine.test.js and gate-lifecycle.test.js.
    - The scratch-home suite run (all suites except capability-oracle.test.js) reports fail 0; the capability-oracle result is recorded with its reason.
    - `src/` and `.github/workflows/ci.yml` are unchanged since c6415a2.
    - The debug record keeps `status: diagnosed` and contains `## Fix (local)`.
  </acceptance_criteria>
  <done>The leak is gone locally under the exact concurrent condition that reproduced it, no other test writes the host state root on this host except the recorded dev-host-only row, and the evidence is recorded for 10-04 and Phase 11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → real user home | Test code resolving `userStateRoot()` or spawning the CLI without a redirect writes the real managed state (journals, snapshots, npx cache) |
| test process → spawned CLI child | The child receives only the declared EnvironmentPolicy; the parent's ALPHA_AOS_STATE_DIR does not cross unless declared as a literal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02 | Tampering | test/mcp-proxy.test.ts, test/gate-engine.test.ts, test/gate-lifecycle.test.ts | high | mitigate | Module-scope redirect to `join(tmpdir(), "alpha-aos-test-mcp-proxy-state")`; per-test redirect with restore in gate-engine; `gateEnvironment(stateRoot)` literal on all seven CLI spawns in gate-lifecycle; regression assertions pin the positive location; RED observations and probes run only on scratch homes |
| T-10-03 | Repudiation | host-immutability oracle | high | mitigate | No edit to test/tarball-fixture.test.ts, `src/` or ci.yml in this plan (git diff gate against c6415a2); no serialization or suite guard substituted for the fix |
| T-10-07 | Information disclosure | debug record RED excerpts | low | mitigate | Excerpts carry test names, paths under scratch roots, pass/fail lines and commit shas only; no file content or environment values |
</threat_model>

<verification>
- The three regression assertions pass; each has a recorded RED observation and RED commit.
- The pair reproduction on a scratch home reports fail 0 with no `.alpha-aos` written.
- The per-file probe shows zero home writes for the three fixed files.
- `git diff --quiet c6415a2 -- src/ .github/workflows/ci.yml` exits 0.
</verification>

<success_criteria>
- ROADMAP success criterion 4 holds: a regression test pins the diagnosed cause, observed failing against the unfixed code and passing against the fix.
- ROADMAP success criterion 2 holds locally (the windows CI observation is 10-04): the packed lifecycle leaves every real host managed path byte-identical with the former writers running concurrently, and the host-path immutability assertion is unchanged.
</success_criteria>

## Artifacts this phase produces

- In `test/mcp-proxy.test.ts`: module constants `hostStateRootAtLoad`, `testStateRoot` (`join(tmpdir(), "alpha-aos-test-mcp-proxy-state")`), the module-scope redirect `process.env.ALPHA_AOS_STATE_DIR = testStateRoot;`, test `pinned upstream startups keep their npx cache under the test-owned state root, never the host state root`.
- In `test/gate-engine.test.ts`: helper `redirectStateRoot(context, stateRoot)`, assertion message `the gate receipt journal must land in the test-owned state root, not the host state root`.
- In `test/gate-lifecycle.test.ts`: helper `gateEnvironment(stateRoot): EnvironmentPolicy`, assertion message `gate check must journal its receipt into the test-owned state root, not the host state root`.
- Commits: `test(10-03): pin mcp-proxy startups to a test-owned state root (RED)`, `fix(10-03): redirect mcp-proxy test state root out of the host home`, `test(10-03): pin gate receipt journals to test-owned state roots (RED)`, `fix(10-03): journal gate receipts into test-owned state roots`.
- Debug record sections filled: `## Regression RED observations (D-08)`, `## Fix (local)`.

<output>
Create `.planning/phases/10-three-os-ci-baseline/10-03-SUMMARY.md` when done. Include the RED and GREEN commit shas, the post-fix pair reproduction summary, and the per-file probe table.
</output>
