---
phase: 03-transactional-project-packs-and-native-optional-use
plan: 21
type: execute
gap_closure: true
wave: 3
depends_on: [03-20]
files_modified:
  - catalog/canaries.yaml
  - src/core/canary.ts
  - src/cli.ts
  - test/canary.test.ts
autonomous: true
requirements: [CAPA-03]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "HANDOFF_HARNESS_PAIRS prioritizes active non-Claude harness pairs (hermes -> codex, pi -> codex, codex -> hermes) while retaining Claude pairs as legacy fallback."
    - "CROSS_HARNESS_HANDOFF in catalog/canaries.yaml declares active non-Claude receiving harnesses (codex) without removing Claude compatibility."
    - "Codex receiving runtime materializes the alpha-AOS memory-handoff steering instruction into .agents/skills so the receiver consults handed-off Unified Memory context."
    - "An explicit cross-harness handoff produces an auditable ImmutabilityWitness proving the .planning tree hash is byte-for-byte identical before and after the handoff round trip."
    - "GSD lifecycle state remains authoritative; Unified Memory content cannot modify .planning/ or become authoritative project policy."
  artifacts:
    - path: catalog/canaries.yaml
      provides: "Active Codex declaration for CROSS_HARNESS_HANDOFF"
    - path: src/core/canary.ts
      provides: "Active non-Claude HANDOFF_HARNESS_PAIRS and receiving Codex runtime support"
    - path: src/cli.ts
      provides: "Active non-Claude handoff row target resolution"
    - path: test/canary.test.ts
      provides: "Active non-Claude handoff pair resolution, memory round-trip, and .planning immutability witness coverage"
  key_links:
    - from: src/core/canary.ts
      to: src/adapters/unified-memory.ts
      via: "runHandoffCanary calls memoryHandoff and memorySearch for handoff exchange"
      pattern: "memoryHandoff|memorySearch"
    - from: src/core/canary.ts
      to: .planning
      via: "hashPlanningTree records byte-identical tree witness before and after receiver run"
      pattern: "hashPlanningTree|comparePlanningTrees|immutabilityWitness"
    - from: catalog/canaries.yaml
      to: src/core/canary.ts
      via: "CROSS_HARNESS_HANDOFF declaration guides handoff prompt and target verification"
      pattern: "CROSS_HARNESS_HANDOFF"
  prohibitions:
    - statement: "The receiving harness execution must not modify any file beneath .planning/."
      status: unverified
      flagged: true
    - statement: "Unified Memory content must not become authoritative project policy or supersede GSD state."
      status: unverified
      flagged: true
    - statement: "Claude Code must not be required as the receiving harness to satisfy CAPA-03 under D-17."
      status: unverified
      flagged: true
---

<objective>
Close gap G-03-2 by establishing an evidence-backed active non-Claude handoff pair (e.g. Hermes -> Codex), configuring Codex receiving runtime instruction support, declaring active non-Claude handoff targets in the canary catalog, and proving `.planning/` tree immutability and GSD state authority.

Purpose: Under D-17, Claude Code is excluded from active verification and release gates. Prior handoff declarations, pairs, and runtime paths were wired exclusively for Claude as the universal receiver. Active non-Claude handoff between two distinct harnesses (Hermes -> Codex or Pi -> Codex) must be established with verified memory recall and recursive planning tree byte immutability.
Output: Active non-Claude `HANDOFF_HARNESS_PAIRS`, catalog declaration updates, Codex receiving instruction support in `src/core/canary.ts`, CLI fallback updates, and comprehensive immutability tests in `test/canary.test.ts`.
</objective>

<execution_context>
@C:/Users/alpha/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-CONTEXT.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-UAT.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-VERIFICATION.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-12-PLAN.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-12-SUMMARY.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-20-PLAN.md
@catalog/canaries.yaml
@src/core/canary.ts
@src/adapters/unified-memory.ts
@src/cli.ts
@skills/alpha-aos-memory-handoff/SKILL.md
@test/canary.test.ts
</context>

<interfaces>
- `HandoffPair` in `src/core/canary.ts`: `{ source: LedgerHarness, target: LedgerHarness, why: string }`.
- `HANDOFF_HARNESS_PAIRS`: ordered list of candidate handoff pairs evaluated by `resolveHandoffPair`.
- `MEMORY_HANDOFF_INSTRUCTION_ID`: `alpha-aos-memory-handoff` materialized for `CAPA-03`.
- `hashPlanningTree(planningRoot)`: returns SHA-256 recursive tree hash over all files in `.planning/`.
- `comparePlanningTrees(before, after)`: evaluates tree immutability, returns `{ identical, changedPaths }`.
- `ImmutabilityWitness` on `CapabilityProof`: records asserted immutability of the planning tree.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Configure active non-Claude handoff pairs, catalog declarations, and Codex receiving runtime support</name>
  <files>catalog/canaries.yaml, src/core/canary.ts, src/cli.ts, test/canary.test.ts</files>
  <read_first>
    - .planning/phases/03-transactional-project-packs-and-native-optional-use/03-UAT.md — Item 2: CAPA-03 Receiving Handoff diagnosis.
    - catalog/canaries.yaml — inspect `CROSS_HARNESS_HANDOFF` declaration at line 108.
    - src/core/canary.ts — inspect `HANDOFF_HARNESS_PAIRS` at line 2876, `handoffRow` at line 3793, and `CANARY_OWNED_INSTRUCTIONS` at line 1599.
    - src/cli.ts — inspect line 894 handoff row resolution.
  </read_first>
  <action>
In `catalog/canaries.yaml`:
- Add `codex` to `harnesses` for `CROSS_HARNESS_HANDOFF` (preserving `claude`).

In `src/core/canary.ts`:
- Update `HANDOFF_HARNESS_PAIRS` to place active non-Claude pairs first:
  1. `{ source: "hermes", target: "codex", why: "D-17 active non-Claude handoff pair: hermes worker to codex receiver" }`
  2. `{ source: "pi", target: "codex", why: "active non-Claude alternative pair: pi agent to codex receiver" }`
  3. `{ source: "codex", target: "hermes", why: "active non-Claude alternative pair: codex to hermes worker" }`
  4. Followed by legacy pairs targeting Claude for backward compatibility.
- Ensure that `CANARY_INSTRUCTION_ROOT.codex` (established in 03-20) allows `MEMORY_HANDOFF_INSTRUCTION_ID` (`skills/alpha-aos-memory-handoff/SKILL.md`) to be written to `.agents/skills/alpha-aos-memory-handoff/SKILL.md` when `options.capability === "CAPA-03"`.
- Update `handoffRow` in `src/core/canary.ts`: change default fallback from `"claude"` to `"codex"`.

In `src/cli.ts`:
- Update line 894 handoff row mapping to fallback to `"codex"` instead of `"claude"` when `result.pair?.target` is absent.

In `test/canary.test.ts`:
- Assert that `CROSS_HARNESS_HANDOFF` in `catalog/canaries.yaml` declares `codex`.
- Assert that `resolveHandoffPair` selects `hermes -> codex` when both binaries are present on PATH, and falls back to `pi -> codex` or `codex -> hermes`.
- Assert that `createCanaryRuntime({ harness: "codex", capability: "CAPA-03", ... })` writes `skills/alpha-aos-memory-handoff/SKILL.md` into the Codex runtime's `.agents/skills/alpha-aos-memory-handoff/SKILL.md`.
  </action>
  <acceptance_criteria>
    - `catalog/canaries.yaml` declares `codex` for `CROSS_HARNESS_HANDOFF`.
    - `HANDOFF_HARNESS_PAIRS` prioritizes active non-Claude pairs (`hermes -> codex`, `pi -> codex`).
    - `handoffRow` and CLI defaults use active non-Claude targets (`codex`).
    - Codex receiving runtime materializes the memory handoff steering instruction.
    - Shipped Claude handoff pairs remain available as legacy fallback.
  </acceptance_criteria>
  <verify>
    <automated>npm run check &amp;&amp; npm run build &amp;&amp; node scripts/run-tests.mjs --files dist/test/canary.test.js</automated>
    <fails_when>The command exits nonzero, type checking fails, or handoff pair resolution assertions fail.</fails_when>
  </verify>
  <done>Active non-Claude handoff pairs and Codex receiving runtime instruction support are declared and wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove explicit cross-harness handoff execution and byte-identical .planning/ tree immutability</name>
  <files>src/core/canary.ts, test/canary.test.ts</files>
  <read_first>
    - .planning/phases/03-transactional-project-packs-and-native-optional-use/03-VERIFICATION.md — Truth 3 (CAPA-03 / G-03-2).
    - src/core/canary.ts — `runHandoffCanary`, `hashPlanningTree`, `comparePlanningTrees`, and `ImmutabilityWitness`.
    - test/canary.test.ts — existing handoff canary assertions around lines 3800-4050.
  </read_first>
  <action>
In `test/canary.test.ts`:
1. Build an offline integration test simulating an active two-harness handoff (`hermes -> codex`):
   - Set up a fixture project directory containing a genuine `.planning/` directory with multiple files (`STATE.md`, `ROADMAP.md`, `REQUIREMENTS.md`).
   - Run `runHandoffCanary` with a mock or controlled runner executing `memoryHandoff` (source: `hermes`, sentinel: `alpha-aos-handoff-sentinel-12345`) and `memorySearch` on the receiving end (`codex`).
   - Assert that the receiving harness receives and uses the handed-off context sentinel.
   - Assert that `hashPlanningTree` takes a recursive digest of `.planning/` before and after the receiver run.
   - Assert `comparePlanningTrees` reports `identical === true` and `changedPaths === []`.
   - Assert that the returned `EvidenceUnit` has `completeness === "COMPLETE"` and `immutabilityWitness.asserted === true`.
2. Add a negative control test for planning tree corruption:
   - If a file in `.planning/` is mutated or added during the handoff run, `comparePlanningTrees` reports `identical === false` and lists the changed path.
   - The resulting `EvidenceUnit` must fail closed with `completeness === "INCOMPLETE"`, with reasons naming the planning tree modification, and withhold the native-use axis.
3. Assert that GSD state authority is preserved: Unified Memory cannot overwrite or become authoritative project state.
  </action>
  <acceptance_criteria>
    - Handoff between active non-Claude harnesses proves receiver context access.
    - Pre-run and post-run `.planning/` tree hashes are proven byte-for-byte identical.
    - Any tampering with `.planning/` during handoff fails closed as `INCOMPLETE` with withheld axis.
    - All tests run offline and pass deterministically.
  </acceptance_criteria>
  <verify>
    <automated>npm run check &amp;&amp; npm run build &amp;&amp; node scripts/run-tests.mjs --files dist/test/canary.test.js</automated>
    <fails_when>The command exits nonzero, any handoff test fails, or planning tree mutation fails to be caught.</fails_when>
  </verify>
  <done>G-03-2 is closed with verified active non-Claude handoff wiring and byte-identical planning tree immutability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Memory Vault → Harness context | Unified Memory context is passed to the receiving harness; it must inform the model without modifying project state. |
| Receiving harness → `.planning/` | The receiver must NOT modify any file under `.planning/`; GSD is the sole state authority. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-148 | Tampering | `.planning/` files during cross-harness handoff | high | mitigate | Compute recursive SHA-256 tree digest before and after run; fail closed if any file differs. |
| T-03-149 | Elevation of Privilege | Memory content becoming authoritative policy | high | mitigate | Treat Unified Memory as ephemeral context only; never write memory records into `.planning/`. |
| T-03-150 | Repudiation | Single-harness false handoff claim | medium | mitigate | Enforce `pair.source !== pair.target` in `resolveHandoffPair` so a harness cannot hand off to itself. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit
- 03-UAT.md Item 2: Resolves missing active non-Claude handoff pair, receiving runtime support, and immutability verification.
- 03-VERIFICATION.md: Truth 3 (CAPA-03) satisfied on active non-Claude pair (Hermes -> Codex).
- D-17: Replaces Claude with active non-Claude pairs while preserving Claude compatibility residue.
</source_audit>

<verification>
- Automated check: `npm run check && npm run build && node scripts/run-tests.mjs --files dist/test/canary.test.js`
- Acceptance: active non-Claude pairs resolve, receiving runtime materializes instructions, and `.planning/` immutability witness passes.
</verification>

<success_criteria>
- Active non-Claude handoff pair (`hermes -> codex`) is declared and prioritized.
- Codex receiving runtime materializes `alpha-aos-memory-handoff`.
- Immutability witness proves `.planning/` is byte-identical across the handoff round trip.
- GSD state authority is strictly enforced.
</success_criteria>

<output>
Create `.planning/phases/03-transactional-project-packs-and-native-optional-use/03-21-SUMMARY.md` when execution completes.
</output>
