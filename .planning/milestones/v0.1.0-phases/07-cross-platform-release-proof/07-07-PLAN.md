---
phase: 07-cross-platform-release-proof
plan: 07
type: gap_closure
wave: 2
depends_on: ["07-05", "07-06"]
files_modified:
  - .github/workflows/ci.yml
  - .planning/phases/07-cross-platform-release-proof/CI_RUN.md
  - scripts/release.mjs
  - scripts/smoke-test.mjs
  - docs/RELEASE_NOTES_v0.1.0.md
autonomous: false
gap_closure: true
requirements: [REL-01, REL-05, REL-06]

must_haves:
  truths:
    - "A GitHub Actions CI workflow run on current release HEAD builds the authoritative alpha-aos-0.1.0.tgz on Linux, and ubuntu-latest, macos-latest, and windows-latest jobs all verify the identical SHA-256 digest and pass the packed lifecycle test (REL-01, REL-05, D-01)."
    - "CI_RUN.md records the inspectable run ID, commit SHA, workflow URL, artifact digest, and multi-OS verification evidence (REL-01, REL-05)."
    - "alpha-aos@0.1.0 is published to the public npm registry with verifiable provenance matching the CI-tested artifact (REL-05, REL-06, D-15)."
    - "Stage 2 public-registry smoke test installs alpha-aos@0.1.0 from npm into an isolated environment and verifies CLI functionality (REL-06, D-16)."
    - "docs/RELEASE_NOTES_v0.1.0.md reflects verified three-OS qualification, provenance bundle hashes, and public registry release (REL-06)."
  prohibitions:
    - statement: "MUST NOT mark Phase 07 complete without inspectable three-OS CI run evidence for current HEAD (REL-01)."
      category: verification
      status: resolved
      verification: automated
    - statement: "MUST NOT publish an unverified or dirty build to the public npm registry (REL-05, D-15)."
      category: safety
      status: resolved
      verification: automated
  artifacts:
    - path: ".planning/phases/07-cross-platform-release-proof/CI_RUN.md"
      provides: "Inspectable record of three-OS GitHub Actions workflow run and artifact checksum matching"
    - path: "scripts/release.mjs"
      provides: "Guarded release orchestrator with public publish and Stage 2 smoke verification"
    - path: "docs/RELEASE_NOTES_v0.1.0.md"
      provides: "Release notes with verified provenance digests and registry status"
---

<objective>
Close Gap 1 (REL-01), Gap 5 (REL-05, REL-06) identified in 07-VERIFICATION.md:
1. Execute and document the three-OS GitHub Actions CI matrix on current release HEAD, proving byte-identical tarball lifecycle across Windows, macOS, and Linux.
2. Complete authorized publication of `alpha-aos@0.1.0` to the public npm registry and execute Stage 2 public-registry smoke verification.
3. Update release notes and provenance documentation with final published artifact digests.
</objective>

<execution_context>
Execution via GSD workflow with human checkpoint for npm publication authorization.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-cross-platform-release-proof/07-CONTEXT.md
@.planning/phases/07-cross-platform-release-proof/07-VERIFICATION.md
@.github/workflows/ci.yml
@test/tarball-fixture.test.ts
@scripts/release.mjs
@scripts/smoke-test.mjs
@scripts/verify-provenance.mjs
@docs/RELEASE_NOTES_v0.1.0.md
</context>

<interfaces>
```bash
# Release orchestration commands
node scripts/release.mjs --dry-run
node scripts/release.mjs --publish
node scripts/smoke-test.mjs --registry alpha-aos@0.1.0
node scripts/verify-provenance.mjs alpha-aos-0.1.0.tgz
```
</interfaces>

<tasks>

<task type="auto" id="07-07-01">
  <name>Task 1: Three-OS GitHub Actions CI Run & Byte-Identity Evidence Recording (REL-01, REL-05, D-01)</name>
  <read_first>
    .github/workflows/ci.yml
    test/tarball-fixture.test.ts
    .planning/phases/07-cross-platform-release-proof/07-VERIFICATION.md
  </read_first>
  <files>.github/workflows/ci.yml, .planning/phases/07-cross-platform-release-proof/CI_RUN.md</files>
  <behavior>
    - Current release HEAD is pushed to GitHub or executed through the authoritative CI workflow.
    - The `package` job on `ubuntu-latest` builds `alpha-aos-0.1.0.tgz`, computes its SHA-256 digest, and uploads the `release-tarball` artifact.
    - The `test` matrix jobs on `ubuntu-latest`, `macos-latest`, and `windows-latest` download the identical artifact, verify SHA-256 match, and pass `test/tarball-fixture.test.ts`.
    - `.planning/phases/07-cross-platform-release-proof/CI_RUN.md` records the GitHub Actions run ID, workflow URL, commit SHA, artifact checksum, and per-OS test outcomes.
  </behavior>
  <action>
    1. Verify `.github/workflows/ci.yml` is configured to build `alpha-aos-0.1.0.tgz` on `ubuntu-latest` and test across `ubuntu-latest`, `macos-latest`, and `windows-latest`.
    2. Ensure current HEAD (including Gap 2, 3, 4 fixes) is pushed to the repository branch and CI is triggered.
    3. Monitor workflow run using `gh run watch` or `gh run list --commit <HEAD_SHA>`.
    4. Verify all 3 OS matrix jobs pass with matching artifact SHA-256 checksums.
    5. Create `.planning/phases/07-cross-platform-release-proof/CI_RUN.md` documenting:
       - Commit SHA
       - GitHub Actions Run ID and URL
       - Tarball filename and SHA-256 digest
       - Matrix job outcomes (Ubuntu, macOS, Windows: all PASS)
  </action>
  <acceptance_criteria>
    - GitHub Actions CI run completed successfully on all 3 OS platforms for the current release commit.
    - `.planning/phases/07-cross-platform-release-proof/CI_RUN.md` contains the inspectable run ID, commit SHA, and matching SHA-256 checksums.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('node:fs'); const c=fs.readFileSync('.planning/phases/07-cross-platform-release-proof/CI_RUN.md','utf8'); if (!c.includes('Run ID') || !c.includes('SHA-256')) process.exit(1);"</automated>
    <fails_when>non-zero exit code, or CI_RUN.md missing required evidence fields</fails_when>
  </verify>
  <done>Three-OS CI execution evidence is recorded and verified against the identical packed archive.</done>
</task>

<task type="checkpoint:decision" id="07-07-02">
  <name>Task 2: Authorized npm Publication & Stage 2 Public-Registry Smoke Test (REL-05, REL-06, D-15, D-16)</name>
  <read_first>
    scripts/release.mjs
    scripts/smoke-test.mjs
    scripts/verify-provenance.mjs
    docs/RELEASE_NOTES_v0.1.0.md
    .planning/phases/07-cross-platform-release-proof/07-VERIFICATION.md
  </read_first>
  <files>scripts/release.mjs, scripts/smoke-test.mjs, docs/RELEASE_NOTES_v0.1.0.md</files>
  <behavior>
    - Pre-flight preview (`node scripts/release.mjs --dry-run`) passes all checks, build, audit, provenance, and Stage 1 local smoke.
    - Upon user confirmation and under valid npm credentials, `node scripts/release.mjs --publish` publishes `alpha-aos-0.1.0.tgz` to the public npm registry.
    - Stage 2 public-registry smoke test (`node scripts/smoke-test.mjs --registry alpha-aos@0.1.0`) executes and passes.
    - `npm view alpha-aos@0.1.0` confirms package presence on npm.
    - `docs/RELEASE_NOTES_v0.1.0.md` is updated with final published status and provenance digest.
  </behavior>
  <action>
    1. Run `node scripts/release.mjs --dry-run` to confirm all local pre-flight gates pass cleanly.
    2. Prompt user for publication authorization and verify npm authentication (`npm whoami`).
    3. Execute `node scripts/release.mjs --publish`.
    4. Run `node scripts/smoke-test.mjs --registry alpha-aos@0.1.0` in an isolated temp environment.
    5. Verify provenance bundle against published tarball using `node scripts/verify-provenance.mjs`.
    6. Update `docs/RELEASE_NOTES_v0.1.0.md` with final registry verification results.
  </action>
  <acceptance_criteria>
    - `npm view alpha-aos@0.1.0 version` returns `0.1.0` without E404.
    - `node scripts/smoke-test.mjs --registry alpha-aos@0.1.0` exits 0.
    - `docs/RELEASE_NOTES_v0.1.0.md` documents successful public registry release and verified digests.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/smoke-test.mjs --local</automated>
    <fails_when>non-zero exit code, or smoke test fails</fails_when>
  </verify>
  <done>alpha-aos@0.1.0 is published to npm and verified via Stage 2 public-registry smoke test.</done>
</task>

</tasks>

## Artifacts this phase produces
- `.planning/phases/07-cross-platform-release-proof/CI_RUN.md`: Inspectable record of three-OS CI run and checksum matching.
- `docs/RELEASE_NOTES_v0.1.0.md`: Release notes updated with verified public registry status.
