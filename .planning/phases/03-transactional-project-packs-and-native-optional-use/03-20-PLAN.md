---
phase: 03-transactional-project-packs-and-native-optional-use
plan: 20
type: execute
gap_closure: true
wave: 2
depends_on: [03-18]
files_modified:
  - catalog/canaries.yaml
  - catalog/stack.yaml
  - src/core/canary.ts
  - test/canary.test.ts
autonomous: true
requirements: [CAPA-02]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "catalog/canaries.yaml declares RESEARCH_MULTI_SOURCE and ORDINARY_LOOKUP_CONTROL for Codex as an active verification target while keeping Claude compatibility intact."
    - "catalog/stack.yaml designates policy.canaryHarness: codex as the active anchor harness under D-17."
    - "CANARY_INSTRUCTION_ROOT.codex maps to a valid runtime skill root (.agents/skills) so alpha-AOS-owned research routing instructions are materialized into isolated Codex canary runtimes."
    - "Multi-source research on Codex verifies ordered web_search_exa discovery before firecrawl_scrape extraction across exactly two servers, while ordinary lookup touches at most one research server."
    - "Research expectation matching fails closed if tools are out of order, if forbidden tools (firecrawl_search) appear, or if server budgets are exceeded."
  artifacts:
    - path: catalog/canaries.yaml
      provides: "Active Codex declarations for RESEARCH_MULTI_SOURCE and ORDINARY_LOOKUP_CONTROL"
    - path: catalog/stack.yaml
      provides: "policy.canaryHarness: codex active anchor declaration"
    - path: src/core/canary.ts
      provides: "CANARY_INSTRUCTION_ROOT.codex mapping and research expectation matching for Codex"
    - path: test/canary.test.ts
      provides: "Ordered sequence matching, forbidden-tool rejection, fan-out limits, and single-server control coverage for Codex"
  key_links:
    - from: catalog/canaries.yaml
      to: src/core/canary.ts
      via: "Canary declarations provide expectTools, expectOrdered, forbidTools, and maxDistinctServers for Codex"
      pattern: "RESEARCH_MULTI_SOURCE|ORDINARY_LOOKUP_CONTROL"
    - from: src/core/canary.ts
      to: skills/alpha-aos-research-routing/SKILL.md
      via: "createCanaryRuntime materializes owned research routing instruction into Codex runtime root"
      pattern: "CANARY_INSTRUCTION_ROOT|RESEARCH_ROUTING_INSTRUCTION"
    - from: catalog/stack.yaml
      to: src/core/canary.ts
      via: "policy.canaryHarness guides default canary selection and sweeps"
      pattern: "canaryHarness.*codex"
  prohibitions:
    - statement: "Canary prompt text must never name the skill, the server, or the tool it is meant to elicit."
      status: unverified
      flagged: true
    - statement: "Canary execution and diagnostic reporting must not reveal API keys (e.g. EXA_API_KEY, FIRECRAWL_API_KEY) in logs, persistent records, or stderr."
      status: unverified
      flagged: true
    - statement: "Shipped Claude canary declarations and compatibility residue must not be removed or broken."
      status: unverified
      flagged: true
---

<objective>
Close gap G-03-1 by declaring active non-Claude (Codex) multi-source research routing and ordinary-lookup control canaries, updating stack policy to designate Codex as the canary harness, enabling runtime instruction materialization for Codex, and verifying fail-closed ordered discovery and fan-out bounds.

Purpose: Under D-17, Claude Code is excluded from active development and release gates. CAPA-02 canaries were previously Claude-only in `catalog/canaries.yaml`, `catalog/stack.yaml` named Claude, and `CANARY_INSTRUCTION_ROOT.codex` was null. Active non-Claude support anchored by Codex must be declared, configured, and verified without breaking existing Claude compatibility.
Output: Active Codex canary declarations in `catalog/canaries.yaml`, `policy.canaryHarness: codex` in `catalog/stack.yaml`, `CANARY_INSTRUCTION_ROOT.codex` implementation in `src/core/canary.ts`, and strict unit/matrix test coverage in `test/canary.test.ts`.
</objective>

<execution_context>
@C:/Users/alpha/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-CONTEXT.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-UAT.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-VERIFICATION.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-07-SUMMARY.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-08-SUMMARY.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-18-SUMMARY.md
@catalog/canaries.yaml
@catalog/stack.yaml
@src/core/canary.ts
@src/core/mcp-proxy.ts
@skills/alpha-aos-research-routing/SKILL.md
@test/canary.test.ts
</context>

<interfaces>
- `CanaryDeclaration` in `src/core/canary.ts`: declared canary properties including `harnesses: readonly CanaryHarness[]`.
- `CANARY_INSTRUCTION_ROOT`: per-harness function returning relative skill directory under isolated runtime root.
- `RESEARCH_MULTI_SOURCE`: requires ordered `[web_search_exa, firecrawl_scrape]`, forbids `[firecrawl_search]`, `maxDistinctServers: 2`, requires `EXA_API_KEY`.
- `ORDINARY_LOOKUP_CONTROL`: negative control requiring `maxDistinctServers: 1`.
- `matchExpectations(observations, declaration)`: checks ordered sequence, server budget, forbidden tools, and argument patterns.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declare active Codex research canaries, update stack policy, and configure Codex runtime instruction root</name>
  <files>catalog/canaries.yaml, catalog/stack.yaml, src/core/canary.ts, test/canary.test.ts</files>
  <read_first>
    - .planning/phases/03-transactional-project-packs-and-native-optional-use/03-UAT.md — Item 1: CAPA-02 Native Research Routing diagnosis and missing artifacts.
    - catalog/canaries.yaml — inspect `RESEARCH_MULTI_SOURCE` and `ORDINARY_LOOKUP_CONTROL` declarations.
    - catalog/stack.yaml — inspect `policy.canaryHarness` at line 63.
    - src/core/canary.ts — inspect `CANARY_INSTRUCTION_ROOT` at line 1567 and `createCanaryRuntime` at line 1662.
  </read_first>
  <action>
In `catalog/canaries.yaml`:
- Add `codex` to the `harnesses` list of `RESEARCH_MULTI_SOURCE` (preserving `claude`).
- Add `codex` to the `harnesses` list of `ORDINARY_LOOKUP_CONTROL` (preserving `claude`).

In `catalog/stack.yaml`:
- Change `policy.canaryHarness: claude` to `policy.canaryHarness: codex` to designate Codex as the active anchor canary harness in compliance with D-17.

In `src/core/canary.ts`:
- Update `CANARY_INSTRUCTION_ROOT.codex` from `null` to `(harnessRoot: string) => join(harnessRoot, ".agents", "skills")`.
- In `createCanaryRuntime`, ensure that when `options.harness === "codex"`, owned instructions for research routing (`RESEARCH_ROUTING_INSTRUCTION_ID`, source `skills/alpha-aos-research-routing/SKILL.md`) are materialized into the runtime's `.agents/skills` directory so the isolated runtime provides the steering layer without contaminating everyday user configuration.
- Preserve Claude instruction root and ensure Pi and Hermes remain explicit nulls or unverified per D-10/D-17.

In `test/canary.test.ts`:
- Update catalog inspection tests to assert that `RESEARCH_MULTI_SOURCE` and `ORDINARY_LOOKUP_CONTROL` both declare `codex` in their `harnesses` arrays alongside `claude`.
- Assert that `catalog/stack.yaml` declares `policy.canaryHarness: "codex"`.
- Add test verifying that `createCanaryRuntime({ harness: "codex", ... })` writes `SKILL.md` under `.agents/skills/alpha-aos-research-routing/` within the isolated runtime root.
  </action>
  <acceptance_criteria>
    - `catalog/canaries.yaml` declares `codex` for both `RESEARCH_MULTI_SOURCE` and `ORDINARY_LOOKUP_CONTROL`.
    - `catalog/stack.yaml` has `policy.canaryHarness: codex`.
    - `CANARY_INSTRUCTION_ROOT.codex` is a non-null function returning `.agents/skills` path.
    - `createCanaryRuntime` successfully materializes the owned research routing skill for Codex.
    - No existing Claude compatibility declarations are removed or broken.
  </acceptance_criteria>
  <verify>
    <automated>npm run check &amp;&amp; npm run build &amp;&amp; node scripts/run-tests.mjs --files dist/test/canary.test.js</automated>
    <fails_when>The command exits nonzero, types do not compile, or canary catalog / runtime assertions fail.</fails_when>
  </verify>
  <done>Codex CAPA-02 canaries are declared in catalog, stack policy points to Codex, and runtime instruction root is active.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate fail-closed ordered matching, fan-out limits, and single-server control for Codex</name>
  <files>src/core/canary.ts, test/canary.test.ts</files>
  <read_first>
    - .planning/phases/03-transactional-project-packs-and-native-optional-use/03-VERIFICATION.md — Truth 2 (CAPA-02 / G-03-1).
    - src/core/canary.ts — `matchExpectations`, `countDistinctServers`, and `decideInvocation`.
    - test/canary.test.ts — existing research canary matcher tests around lines 2590-2750.
  </read_first>
  <action>
In `test/canary.test.ts`:
1. Build a focused test suite asserting the strict matrix for Codex CAPA-02 multi-source research:
   - Positive case: observation sequence has `web_search_exa` on server `exa`, then `firecrawl_scrape` on server `firecrawl`. Asserts: `held=true`, `expectationsHeld=true`, `ordered=true`, `distinctServers=2`, `withinServerBudget=true`, `nativeUse="invoked"`.
   - Reversed sequence failure: `firecrawl_scrape` before `web_search_exa`. Asserts: `held=false`, `ordered=false`, `nativeUse="unverified"`.
   - Forbidden tool failure: sequence includes `firecrawl_search`. Asserts: `held=false`, `forbiddenSeen=["firecrawl_search"]`, `nativeUse="unverified"`.
   - Server budget exceed failure: observations span 3 distinct servers (e.g. exa, firecrawl, context7). Asserts: `held=false`, `withinServerBudget=false`, `nativeUse="unverified"`.
   - Incomplete route failure: discovery only (`web_search_exa` alone) without extraction leg. Asserts: `held=false`, missing extraction tool, `nativeUse="unverified"`.
2. Test Codex `ORDINARY_LOOKUP_CONTROL` fan-out bounds:
   - Positive control: observation sequence touches 0 or 1 research server (e.g. web search only or local response). Asserts: `withinServerBudget=true`, `held=true`.
   - Negative control failure: observation sequence touches 2 research servers for an ordinary lookup. Asserts: `withinServerBudget=false`, `held=false`.
3. Add opt-in live test structure for Codex research canaries (`RESEARCH_MULTI_SOURCE` and `ORDINARY_LOOKUP_CONTROL`) guarded by `$env:ALPHA_AOS_LIVE_CANARY === '1'` and `codex login status` zero-exit check, ensuring no spend during ordinary test runs.
  </action>
  <acceptance_criteria>
    - Strict matrix proves passing and failing directions for Codex research routing and ordinary control.
    - Reversed order, forbidden tool, and excessive server counts each fail closed with expected diagnostic codes.
    - Single-server bound is enforced for ordinary lookups.
    - Full repository test suite passes with zero unskipped failures.
  </acceptance_criteria>
  <verify>
    <automated>npm run check &amp;&amp; npm run build &amp;&amp; node scripts/run-tests.mjs --files dist/test/canary.test.js</automated>
    <fails_when>The command exits nonzero, any test fails, or any fail-closed control fails to reject improper observation sequences.</fails_when>
  </verify>
  <done>G-03-1 is closed with active Codex CAPA-02 declarations, runtime instruction wiring, and strict fail-closed matching validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Canary declaration → runtime execution | Prompts must remain hint-free and tools allowlisted. |
| External MCP observation → verdict | Only classified tool events decide the verdict; model output text is inadmissible. |
| Credential environment → process | API keys (EXA_API_KEY) must be allowlisted for process launch but redacted from durable output. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-145 | Information Disclosure | `EXA_API_KEY` handling in canary runner | high | mitigate | Redact environment and credential values via `PLATFORM_FLOOR_ENVIRONMENT` and `canaryEnvironmentPolicy`. |
| T-03-146 | Elevation of Privilege | Fan-out tool calls in ordinary lookup | medium | mitigate | Fail closed when `distinctServers > maxDistinctServers` (`ORDINARY_LOOKUP_CONTROL` limit 1). |
| T-03-147 | Tampering | Out-of-order research execution | medium | mitigate | Enforce `expectOrdered=true` so extraction before discovery is rejected. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit
- 03-UAT.md Item 1: Resolves missing active non-Claude declaration, `policy.canaryHarness` setting, and `CANARY_INSTRUCTION_ROOT.codex`.
- 03-VERIFICATION.md: Truth 2 (CAPA-02) satisfied on active non-Claude surface (Codex).
- D-17: Replaces Claude with Codex as active anchor for CAPA-02 while preserving Claude compatibility residue.
</source_audit>

<verification>
- Automated check: `npm run check && npm run build && node scripts/run-tests.mjs --files dist/test/canary.test.js`
- Acceptance: catalog declarations include codex, stack policy names codex, instruction root is configured, and all fail-closed matcher tests pass.
</verification>

<success_criteria>
- Codex is declared for `RESEARCH_MULTI_SOURCE` and `ORDINARY_LOOKUP_CONTROL`.
- Stack policy specifies `canaryHarness: codex`.
- Runtime instruction root for Codex is non-null and materializes `alpha-aos-research-routing`.
- Ordered Exa -> Firecrawl matching and single-server ordinary lookup bounds pass all tests.
</success_criteria>

<output>
Create `.planning/phases/03-transactional-project-packs-and-native-optional-use/03-20-SUMMARY.md` when execution completes.
</output>
