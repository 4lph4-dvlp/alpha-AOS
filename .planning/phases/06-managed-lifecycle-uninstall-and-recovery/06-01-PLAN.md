---
phase: 06-managed-lifecycle-uninstall-and-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/core/status.ts
  - src/cli.ts
  - src/format.ts
  - src/core/install.ts
  - src/core/update.ts
  - test/status.test.ts
  - test/lifecycle.test.ts
autonomous: true
requirements: [LIFE-01, LIFE-02, LIFE-08]

must_haves:
  truths:
    - "Stable-lock reconciliation (`alpha-aos install` / `plan`) against an already-applied stack computes exact SHA-256 hashes, reports all components as CURRENT, executes 0 writes, and exits 0 (LIFE-01, D-15)."
    - "Offline status (`alpha-aos status`) executes strictly 0 subprocesses and 0 network calls, completing in <50ms by evaluating local state, lock metadata, journal health, and tree policies (LIFE-02, D-14)."
    - "Offline status flags needsRepair as true immediately if writer.lock is held by a dead process (ESRCH) or if any journal is incomplete (status: 'applying') or corrupt (LIFE-02, D-09)."
    - "Active tool version discovery (--discovery) and execution canaries (--canary) are strictly reserved for alpha-aos doctor and never run during status (LIFE-02, D-14)."
    - "Update preview (`alpha-aos update --check`) performs zero filesystem writes, package changes, or git mutations (LIFE-08, D-16)."
    - "Update apply (`alpha-aos update --apply`) strictly reconciles committed stable lock (`catalog/stack.lock.json`) and rejects candidate locks (`catalog/candidate.lock.json`) (LIFE-08, D-16)."
  prohibitions:
    - statement: "MUST NOT spawn any child processes (`child_process.spawn`, `exec`, `execSync`, `fork`) or open network sockets in `getOfflineStatus` or `alpha-aos status` (LIFE-02, D-14)."
      category: performance
      status: resolved
      verification: automated
    - statement: "MUST NOT execute file writes or mutate harness configurations when all components match stable-lock SHA-256 hashes (LIFE-01, D-15)."
      category: safety
      status: resolved
      verification: automated
    - statement: "MUST NOT promote or apply `candidate.lock.json` in `alpha-aos update --apply` without committed review (LIFE-08, D-16)."
      category: safety
      status: resolved
      verification: automated
  artifacts:
    - path: "src/core/status.ts"
      provides: "Fast zero-subprocess offline status engine (<50ms)"
      exports: ["getOfflineStatus", "OfflineStatus"]
    - path: "test/status.test.ts"
      provides: "Automated verification for offline status latency, 0-subprocess constraint, and needs-repair diagnostics"
    - path: "test/lifecycle.test.ts"
      provides: "Automated verification for stable-lock idempotency, update preview read-only guarantee, and candidate rejection"
---

<objective>
Implement fast offline status inspection (LIFE-02), strict hash-verified reconciliation idempotency (LIFE-01), and read-only update preview with candidate rejection (LIFE-08).

Purpose: Ensure that users can query stack health in <50ms without subprocess or network overhead, that repeat install/plan runs against matching disk state execute zero mutations, and that updates never mutate state during preview or apply unreviewed candidate locks.
Output: `src/core/status.ts`, updated `src/types.ts`, `src/cli.ts`, `src/format.ts`, `src/core/install.ts`, `src/core/update.ts`, and test suites `test/status.test.ts` and `test/lifecycle.test.ts`.
</objective>

<execution_context>
Execution via GSD workflow adhering to anti-shallow deep work rules.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-managed-lifecycle-uninstall-and-recovery/06-CONTEXT.md
@.planning/phases/06-managed-lifecycle-uninstall-and-recovery/06-RESEARCH.md
@.planning/phases/06-managed-lifecycle-uninstall-and-recovery/06-PATTERNS.md
@.planning/phases/06-managed-lifecycle-uninstall-and-recovery/06-VALIDATION.md
@src/core/writer-lock.ts
@src/core/transaction.ts
@src/core/install.ts
@src/core/update.ts
@src/cli.ts
</context>

<interfaces>
```typescript
// Proposed additions to src/types.ts
export interface OfflineStatus {
  readonly channel: Channel;
  readonly generatedAt: string | null;
  readonly writerStatus: WriterStatus;
  readonly needsRepair: boolean;
  readonly incompleteTransactions: readonly string[];
  readonly corruptJournals: readonly string[];
  readonly treePoliciesCount: number;
  readonly managedStatePresent: boolean;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strict Hash-Verified Reconciliation Idempotency & Pure Read-Only Update Preview</name>
  <read_first>
    - .planning/phases/06-managed-lifecycle-uninstall-and-recovery/06-CONTEXT.md — D-15: Strict hash-verified idempotency; D-16: Pure read-only update preview.
    - .planning/phases/06-managed-lifecycle-uninstall-and-recovery/06-RESEARCH.md — Pattern 7: Strict Hash-Verified Reconciliation Idempotency & Pitfall 8.
    - src/core/install.ts — `createManagedInstallPlan`, `applyManagedInstall`.
    - src/core/update.ts — `planCandidateStage`, `resolveCandidate`, candidate vs stable lock handling.
    - src/cli.ts — Command routing for `install`, `plan`, and `update`.
  </read_first>
  <files>src/core/install.ts, src/core/update.ts, src/cli.ts, test/lifecycle.test.ts</files>
  <action>
1. In `src/core/install.ts`:
   - In `createManagedInstallPlan`:
     - Ensure all components (GSD, ECC, MCP, skill policies, Codex execution policy) compute exact SHA-256 hashes of on-disk files against `catalog/stack.lock.json`.
     - When all components are already installed and file hashes match exactly, mark every step action as `"current"`.
   - In `applyManagedInstall`:
     - Check if `plan.steps.every((step) => step.action === "current")`.
     - If all steps are already current:
       - Do NOT acquire a `MutationSession` or write any lock files.
       - Execute 0 disk writes and 0 external package commands.
       - Return `ManagedInstallResult` with `applied: []`, `current: plan.steps.map((s) => s.id)`, `operationIds: []`, `externalChanges: []`.
       - Exit with status 0.

2. In `src/core/update.ts` and `src/cli.ts`:
   - Enforce pure read-only preview in `alpha-aos update --check`:
     - Mutates zero files on disk (no write to `catalog/candidate.lock.json`, no write to `catalog/stack.lock.json`, no writes to state root).
     - Returns resolved candidate comparison without persistent state mutation.
   - Enforce stable-lock boundary in `alpha-aos update --apply`:
     - Strictly reads and reconciles committed stable lock `catalog/stack.lock.json`.
     - Explicitly ignores and rejects `catalog/candidate.lock.json`. Any candidate lock staged via `--stage` must be reviewed and committed into `stack.lock.json` before apply will reconcile it.

3. Create `test/lifecycle.test.ts`:
   - Test 1: Stable-lock idempotency (`LIFE-01`, `D-15`). Run `createManagedInstallPlan` on applied fixture; assert every step reports `action: "current"`. Run `applyManagedInstall`; assert `result.applied` is empty, `result.current` contains all steps, zero file modifications occurred, and exit status is 0.
   - Test 2: Idempotent repetition. Running reconciliation multiple times in sequence produces identical results without accumulating journals or snapshots.
   - Test 3: Pure read-only update preview (`LIFE-08`, `D-16`). Run `alpha-aos update --check` on a synthetic stack; assert zero files are created or modified across the repository or user state directory.
   - Test 4: Candidate lock rejection (`LIFE-08`, `D-16`). Place a modified `candidate.lock.json` in `catalog/`; run `update --apply`; assert only `stack.lock.json` is applied and `candidate.lock.json` changes are not applied.
  </action>
  <acceptance_criteria>
    - Reconciling an already-applied stack returns all steps as `CURRENT` and executes zero writes (`LIFE-01`).
    - Second sequential install/plan run produces identical output and exits 0.
    - `update --check` mutates 0 files and executes 0 package managers (`LIFE-08`).
    - `update --apply` strictly rejects candidate locks and reconciles only stable locks (`LIFE-08`).
    - `node scripts/run-tests.mjs --files dist/test/lifecycle.test.js` passes completely.
  </acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node scripts/run-tests.mjs --files dist/test/lifecycle.test.js</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <done>Idempotent stable-lock reconciliation and read-only update preview verified and passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fast Zero-Subprocess Offline Status Engine (&lt;50ms) & Doctor Boundary</name>
  <read_first>
    - .planning/phases/06-managed-lifecycle-uninstall-and-recovery/06-CONTEXT.md — D-09: Needs-repair classification; D-14: Zero-subprocess offline status.
    - .planning/phases/06-managed-lifecycle-uninstall-and-recovery/06-RESEARCH.md — Pattern 5: Zero-Subprocess Offline Status vs Probing Doctor & Pitfall 2.
    - src/core/writer-lock.ts — `inspectWriterState`.
    - src/core/transaction.ts — `listManagedTransactions`.
    - src/cli.ts — Current `status` command implementation (line 504).
    - src/format.ts — Existing formatters.
  </read_first>
  <files>src/types.ts, src/core/status.ts, src/cli.ts, src/format.ts, test/status.test.ts</files>
  <action>
1. In `src/types.ts`:
   - Export `OfflineStatus`:
     ```typescript
     export interface OfflineStatus {
       readonly channel: Channel;
       readonly generatedAt: string | null;
       readonly writerStatus: WriterStatus;
       readonly needsRepair: boolean;
       readonly incompleteTransactions: readonly string[];
       readonly corruptJournals: readonly string[];
       readonly treePoliciesCount: number;
       readonly managedStatePresent: boolean;
     }
     ```

2. Create `src/core/status.ts`:
   - Implement `export async function getOfflineStatus(stateRoot: string, catalog: StackCatalog, lock: StackLock): Promise<OfflineStatus>`:
     - Check `inspectWriterState(stateRoot)`.
     - Scan `~/.alpha-aos/journal/` without spawning subprocesses:
       - Read directory entries using `readdir` with `node:fs/promises`.
       - Filter for `.json` files and read them using `readFile`.
       - Any journal where `JSON.parse` fails is recorded in `corruptJournals`.
       - Any journal where `status === "applying"` is recorded in `incompleteTransactions`.
       - Flag `needsRepair = writerState.status === "needs-repair" || incompleteTransactions.length > 0 || corruptJournals.length > 0`.
     - Read `~/.alpha-aos/trees.json` using `readFile` to count registered directory tree policies.
     - Check `existsSync(stateRoot)` for `managedStatePresent`.
     - Strictly ZERO subprocess invocations: no `child_process.spawn`, `exec`, `execFile`, or `probeCommand`.
     - Strictly ZERO network socket requests.

3. In `src/format.ts`:
   - Export `formatOfflineStatus(status: OfflineStatus): string`:
     - Display compact overview: Active channel, lock timestamp, writer status, tree policies count.
     - If `needsRepair` is true: display high-visibility warning:
       `STATUS: NEEDS-REPAIR (Run 'alpha-aos repair' to recover from interrupted operation)`.
       List incomplete transaction IDs and corrupt journal files.
     - If healthy: display `STATUS: OK`.

4. In `src/cli.ts`:
   - Update `command === "status"`:
     - Replace `collectInventory(catalog)` with `await getOfflineStatus(userStateRoot(), catalog, lock)`.
     - Print formatted output or JSON if `--json` flag passed.
     - If `status.needsRepair` is true, set `process.exitCode = 1`.

5. Create `test/status.test.ts`:
   - Test 1: Zero-subprocess enforcement (`LIFE-02`, `D-14`). Instrument/spy on `node:child_process` methods (`spawn`, `exec`, `execFile`, `fork`). Run `getOfflineStatus`; assert 0 calls were made.
   - Test 2: Latency benchmark (`LIFE-02`, `D-14`). Run `getOfflineStatus` 10 times with `performance.now()`; assert median execution time is < 50ms.
   - Test 3: Healthy stack reporting. Assert `needsRepair: false`, channel matches lock, writer status is `"free"`.
   - Test 4: Incomplete transaction diagnosis (`LIFE-02`, `D-09`). Write a synthetic journal with `status: "applying"`; assert `needsRepair: true`, `incompleteTransactions` names the ID, and output instructs user to run `alpha-aos repair`.
   - Test 5: Corrupt journal diagnosis (`LIFE-02`, `D-09`). Write an unparseable JSON file to `journal/`; assert `needsRepair: true` and file is listed in `corruptJournals`.
   - Test 6: Stale lock diagnosis (`LIFE-02`, `D-09`). Write a stale `writer.lock` with a dead PID; assert `needsRepair: true` and `writerStatus: "needs-repair"`.
  </action>
  <acceptance_criteria>
    - `getOfflineStatus` executes 0 subprocesses and 0 network requests (`LIFE-02`).
    - Execution completes in < 50ms (`LIFE-02`).
    - Reports `needs-repair` immediately upon incomplete journal or dead writer lock (`LIFE-02`, `D-09`).
    - Active discovery and canaries remain excluded from `status` and isolated to `doctor`.
    - `node scripts/run-tests.mjs --files dist/test/status.test.js` passes completely.
  </acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node scripts/run-tests.mjs --files dist/test/status.test.js</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <done>Zero-subprocess offline status engine verified with <50ms benchmark and actionable coded findings.</done>
</task>

</tasks>

## Artifacts this phase produces

### Functions
- `getOfflineStatus(stateRoot: string, catalog: StackCatalog, lock: StackLock): Promise<OfflineStatus>` in `src/core/status.ts`
- `formatOfflineStatus(status: OfflineStatus): string` in `src/format.ts`

### Types
- `OfflineStatus` in `src/types.ts`

### Files
- `src/core/status.ts`
- `test/status.test.ts`
- `test/lifecycle.test.ts`
