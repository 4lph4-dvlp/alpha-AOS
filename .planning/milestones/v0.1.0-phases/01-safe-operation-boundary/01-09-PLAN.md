---
phase: 01-safe-operation-boundary
plan: 09
type: execute
wave: 2
depends_on: [01-02, 01-05, 01-07]
files_modified:
  - src/core/writer-lock.ts
  - src/core/transaction.ts
  - schemas/writer-lock.schema.json
  - schemas/transaction-journal.schema.json
  - test/transaction.test.ts
  - test/transaction-crash.test.ts
autonomous: true
requirements: [SAFE-02, SAFE-03, SAFE-04]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-04/D-05: one durable token-bound writer session owns a state root and competitors fail immediately with active operation metadata while readers remain available."
    - "D-06/D-07: abandoned-looking locks become needs-repair until token/journal/hashes are inspected; only explicit repair --apply may perform a proven transition."
    - "D-02/D-03: the session consumes a complete PathProof set and rechecks it before every byte or external mutation."
    - "Transaction intents and snapshots are durable before target mutation, step/final states are synced, and rollback drift protection remains intact."
  artifacts:
    - path: src/core/writer-lock.ts
      provides: "State-root exclusive writer and recovery diagnosis"
      exports: [acquireMutationSession, inspectWriterState, planWriterRepair, applyWriterRepair]
    - path: src/core/transaction.ts
      provides: "Write-ahead durable hash journal consuming MutationSession"
    - path: schemas/writer-lock.schema.json
      provides: "Strict writer record schema"
    - path: schemas/transaction-journal.schema.json
      provides: "Strict durable transaction state schema"
  key_links:
    - from: src/core/transaction.ts
      to: src/core/writer-lock.ts
      via: "caller-owned MutationSession token and lifecycle"
      pattern: "MutationSession"
    - from: src/core/transaction.ts
      to: src/core/path-boundary.ts
      via: "proof-set validation and immediate recheck"
      pattern: "recheckPathProof"
  prohibitions:
    - statement: "Transaction diagnosis MUST NOT label an interrupted operation rolled back, applied, or safe-to-clear when a target hash is neither the recorded before nor after value."
      requirement_id: SAFE-03
      category: transparency
      status: unverified
      flagged: true
---

<objective>
operation-scoped writer lock과 durable write-ahead transaction state machine을 구현한다.

Purpose: concurrent/interrupted writes가 실제 target state를 숨기거나 추측으로 lock을 제거하지 못하게 한다.
Output: MutationSession, strict writer/journal schemas, durable transaction and explicit repair APIs.
</objective>

<execution_context>
@C:/Users/alpha/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safe-operation-boundary/01-CONTEXT.md
@.planning/phases/01-safe-operation-boundary/01-RESEARCH.md
@.planning/phases/01-safe-operation-boundary/01-PATTERNS.md
@.planning/phases/01-safe-operation-boundary/01-02-SUMMARY.md
@.planning/phases/01-safe-operation-boundary/01-05-SUMMARY.md
@.planning/phases/01-safe-operation-boundary/01-07-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One state-root writer session과 competing refusal tracer</name>
  <files>src/core/writer-lock.ts, schemas/writer-lock.schema.json, test/transaction-crash.test.ts</files>
  <read_first>
    src/core/transaction.ts
    src/core/path-boundary.ts
    src/core/validation.ts
    test/transaction-crash.test.ts
    schemas/install-state.schema.json
  </read_first>
  <behavior>
    - open(lock,"wx",0600), random token, operation ID, PID, start time, journal path, plan digest are synced before session use.
    - competitor returns ACTIVE_WRITER immediately with operation metadata and retry guidance and never removes the lock.
    - readers inspect status during active session without acquiring or mutating lock state.
  </behavior>
  <action>D-04/D-05에 따라 strict `WriterLockRecord`, `MutationPlan`, `MutationSession`, `WriterState`를 구현한다. `acquireMutationSession`은 complete read-only preflight result를 요구하고 lock file exclusive create/write/sync plus supported parent sync를 first mutation으로 수행한다. Existing lock은 strict-read/redacted metadata만 반환하며 wait, PID signal, delete를 하지 않는다. Session closes only after transaction final durability proof.</action>
  <acceptance_criteria>two-process test proves bounded fail-fast competitor behavior, exact diagnostic fields, unchanged target, reader availability, and no competitor unlink attempt.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/transaction-crash.test.js --test-name-pattern="writer|compet"</automated>
    <fails_when>non-zero exit, output reports 0 matching tests, competitor waits past the bounded threshold, or the lock disappears</fails_when>
  </verify>
  <reversibility rating="costly">writer/journal format becomes persisted state, so future changes require explicit schema migration.</reversibility>
  <done>state-root mutation serialization works end-to-end and leaves reader operations available.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Durable write-ahead journal과 existing rollback invariant 결합</name>
  <files>src/core/transaction.ts, schemas/transaction-journal.schema.json, test/transaction.test.ts, test/transaction-crash.test.ts</files>
  <read_first>
    src/core/transaction.ts
    src/core/writer-lock.ts
    src/core/path-boundary.ts
    src/core/validation.ts
    test/transaction.test.ts
    test/transaction-crash.test.ts
  </read_first>
  <behavior>
    - complete before/after hashes and PathProofs are gathered before mutation.
    - snapshot bytes and per-file intent are synced before target rename/remove; each step and final status are synced afterward.
    - each mutation rechecks canonical ancestor identity and unknown current hash blocks rollback/repair while preserving user bytes.
  </behavior>
  <action>D-02/D-03 and SAFE-03에 따라 `applyFileTransaction`이 caller-supplied `MutationSession`을 소비하도록 변경한다. 같은-directory temp file sync→rename→supported parent directory sync를 journal, snapshot, target에 적용한다. Per-file intent with before/after hash and proof digest를 target mutation 전에 durable write하고 deterministic failpoint hook을 test-only options에 제공한다. 기존 reverse rollback과 all-target drift preflight를 보존하고 strict journal loader를 사용한다.</action>
  <acceptance_criteria>baseline transaction/rollback tests remain green; every failpoint yields a strict journal whose current hash classification matches bytes; parent swap and neither hash write nothing further.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/transaction.test.js dist/test/transaction-crash.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, rollback overwrites drifted bytes, or a journal intent is absent after target mutation</fails_when>
  </verify>
  <reversibility rating="costly">new journal state is versioned and older readable state needs the explicit migration route.</reversibility>
  <done>durable evidence always precedes mutation and exact-byte rollback protection remains intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: needs-repair diagnosis와 explicit proven repair action 구현</name>
  <files>src/core/writer-lock.ts, src/core/transaction.ts, test/transaction-crash.test.ts</files>
  <read_first>
    src/core/writer-lock.ts
    src/core/transaction.ts
    test/transaction-crash.test.ts
    src/core/doctor.ts
    src/cli.ts
  </read_first>
  <behavior>
    - missing PID or old start time never clears a lock and reports NEEDS_REPAIR until token/journal/schema/hash proof completes.
    - planWriterRepair is read-only and returns before/after/neither classification and permitted transition.
    - applyWriterRepair requires reviewed plan digest and explicit caller action, restores exact proven bytes or releases only fully resolved lock state.
  </behavior>
  <action>D-06/D-07에 따라 `inspectWriterState`, `planWriterRepair`, `applyWriterRepair`를 완성한다. Repair plan은 writer token/journal path/schema/operation ID and every current target hash를 bind한다. Apply는 plan digest와 proof를 revalidate하며 before/after classification으로 안전한 finalization/exact restoration만 허용한다. Neither, missing evidence, external package uncertainty는 mutation 없이 stable needs-repair guidance를 반환한다; Phase 6 compensation/uninstall은 추가하지 않는다.</action>
  <acceptance_criteria>PID-absent and aged locks persist through diagnosis; preview digest is unchanged; explicit apply succeeds only for fully proven fixture and drifted fixture remains untouched/blocked.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/transaction-crash.test.js --test-name-pattern="repair|abandon|drift"</automated>
    <fails_when>non-zero exit, output reports 0 matching tests, diagnosis removes a lock, or drifted bytes are overwritten</fails_when>
  </verify>
  <reversibility rating="costly">repair may restore exact prior bytes or finalize lock state, always behind reviewed digest and explicit apply.</reversibility>
  <done>interrupted operations are diagnosable without heuristic unlock and have a narrow explicit recovery seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent process→state root | multiple commands attempt ownership of the same mutation state |
| journal evidence→repair mutation | persisted metadata authorizes restoration or lock finalization |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-19 | Tampering/Repudiation | writer-lock.ts | high | mitigate | exclusive create, random token, durable metadata, no heuristic unlock |
| T-01-20 | Tampering | transaction.ts | critical | mitigate | write-ahead intent, fsync order, proof recheck, hash classification |
| T-01-21 | Elevation | repair apply | high | mitigate | reviewed plan digest and complete token/schema/hash revalidation |
</threat_model>

## Artifacts this phase produces

| Kind | Artifact | Contract |
|------|----------|----------|
| New file | `src/core/writer-lock.ts` | `acquireMutationSession`, `inspectWriterState`, `planWriterRepair`, `applyWriterRepair` |
| New schema | `schemas/writer-lock.schema.json` | strict token-bound writer record |
| New schema | `schemas/transaction-journal.schema.json` | strict durable write-ahead state |
| New types | `MutationPlan`, `MutationSession`, `WriterLockRecord`, `WriterState`, `WriterRepairPlan` | operation ownership and recovery contracts |

## Probe coverage notes

- Generic race-condition security belongs to STRIDE; false-safe recovery state is retained as the phase-specific descriptor-less prohibition.

<verification>
Transaction baseline, contention, crash failpoint, diagnosis, and repair tests all pass; no unavailable durability capability is counted as supported.
</verification>

<success_criteria>
- exactly one writer session spans an operation.
- durable intent precedes every target mutation.
- recovery never guesses from PID/age and never overwrites drift.
</success_criteria>

<output>
Create `.planning/phases/01-safe-operation-boundary/01-09-SUMMARY.md` when done.
</output>
