---
phase: 01-safe-operation-boundary
plan: 12
type: execute
wave: 4
depends_on: [01-03, 01-06, 01-09, 01-11]
files_modified:
  - src/core/gsd-compat.ts
  - src/core/ecc-skills.ts
  - test/gsd-compat.test.ts
  - test/ecc-skills.test.ts
autonomous: true
requirements: [SAFE-02, SAFE-03, SAFE-04, SAFE-06]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-02/D-03: GSD compatibility and ECC skill plans enumerate every target, state, journal, snapshot, temp, source, and package-root path, bind source/plan hashes, and recheck them immediately before mutation."
    - "D-04/D-05: each standalone apply acquires one MutationSession after complete preflight, while a caller-provided session is consumed without acquiring a nested writer lock."
    - "D-12: failures expose only typed, bounded, redacted evidence and never raw subprocess output or credential values."
  artifacts:
    - path: src/core/gsd-compat.ts
      provides: "Complete GSD compatibility plan plus standalone-or-caller-session apply API"
    - path: src/core/ecc-skills.ts
      provides: "Complete ECC skill plan plus standalone-or-caller-session apply API"
  key_links:
    - from: src/core/gsd-compat.ts
      to: src/core/transaction.ts
      via: "caller-supplied MutationSession and complete OperationPathProofSet"
      pattern: "MutationSession|applyFileTransaction|recheckPathProof"
    - from: src/core/ecc-skills.ts
      to: src/core/writer-lock.ts
      via: "standalone session acquisition only after reviewed plan revalidation"
      pattern: "acquireMutationSession|session"
  prohibitions:
    - statement: "A nested component apply MUST NOT acquire a second writer lock or calculate a new mutation path after its caller has acquired the operation session."
      requirement_id: SAFE-03
      category: safety
      status: unverified
      flagged: true
---

<objective>
GSD compatibility와 ECC skill mutator를 install orchestration보다 먼저 complete proof와 caller-consumable MutationSession API로 전환한다.

Purpose: 후속 install caller가 callee마다 별도 lock을 잡거나 session 없이 파일을 변경할 수 없게 한다.
Output: standalone 및 nested 호출 모두에 사용할 session-aware GSD/ECC plan/apply contracts.
</objective>

<execution_context>
@C:/Users/alpha/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safe-operation-boundary/01-CONTEXT.md
@.planning/phases/01-safe-operation-boundary/01-RESEARCH.md
@.planning/phases/01-safe-operation-boundary/01-PATTERNS.md
@.planning/phases/01-safe-operation-boundary/01-03-SUMMARY.md
@.planning/phases/01-safe-operation-boundary/01-06-SUMMARY.md
@.planning/phases/01-safe-operation-boundary/01-09-SUMMARY.md
@.planning/phases/01-safe-operation-boundary/01-11-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: GSD compatibility standalone/nested one-session tracer</name>
  <files>src/core/gsd-compat.ts, test/gsd-compat.test.ts</files>
  <read_first>
    src/core/gsd-compat.ts
    src/core/transaction.ts
    src/core/writer-lock.ts
    src/core/path-boundary.ts
    test/gsd-compat.test.ts
  </read_first>
  <behavior>
    - planGsdCompatibilitySync enumerates target/state/journal/snapshot/temp/source/package-root roles, source/target hashes, ProcessSpecs, environment names, and a deterministic reviewed digest without mutation.
    - standalone apply revalidates the complete plan before acquiring one MutationSession.
    - nested apply requires and reuses the caller token, acquires no lock, and rejects any path or source absent from the reviewed plan.
  </behavior>
  <action>D-02~D-05에 따라 GSD compatibility plan을 complete `OperationPathProofSet`과 immutable source/process evidence로 확장하고 apply API가 optional caller `MutationSession`을 소비하도록 변경한다. Session이 없을 때만 complete read-only revalidation 후 one writer를 acquire한다. Session이 있으면 operation/plan digest ownership을 검증하고 같은 token을 `applyFileTransaction` 및 모든 cleanup에 전달한다. 각 source read와 target rename/remove 직전에 proof/hash를 재검사하며 raw process output은 D-12 redacted result만 사용한다.</action>
  <acceptance_criteria>standalone case는 lock 1개, nested case는 추가 lock 0개를 관찰하고, incomplete/drifted/escape plan은 process 또는 file mutation 0회와 unchanged target digest를 보인다.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/gsd-compat.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, nested apply acquires another lock, or a blocked fixture changes target bytes</fails_when>
  </verify>
  <reversibility rating="costly">session/proof requirements change the internal apply contract while preserving standalone behavior.</reversibility>
  <done>GSD compatibility mutation is complete-plan-bound and consumes exactly one operation session.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ECC skill sync를 동일 session/proof contract로 전환</name>
  <files>src/core/ecc-skills.ts, test/ecc-skills.test.ts</files>
  <read_first>
    src/core/ecc-skills.ts
    src/core/transaction.ts
    src/core/writer-lock.ts
    src/core/path-boundary.ts
    test/ecc-skills.test.ts
  </read_first>
  <behavior>
    - ECC plan binds every selected source file, destination, state/journal/snapshot/temp/package root, expected hash, and plan digest.
    - standalone and caller-session apply share the same immediate recheck and journal semantics.
    - cleanup/removal is a proven operation under the same session and unknown source/config drift is preserved and refused.
  </behavior>
  <action>D-02~D-04에 따라 ECC skill plan/apply에 GSD tracer와 같은 complete proof, reviewed digest, and optional caller session contract를 적용한다. Selected skill source bytes와 lock integrity를 plan에 bind하고, apply 중 새 path/selection을 계산하지 않는다. 모든 write/remove는 caller token을 `applyFileTransaction`에 전달하고 D-03 immediate recheck 뒤 실행한다. Standalone path도 같은 plan API를 재사용하며 failure evidence는 central redactor를 통과한다.</action>
  <acceptance_criteria>standalone/nested apply의 결과 bytes가 같고 nested lock acquisition은 0회이며, source drift·path swap·active/needs-repair cases는 target과 outside sentinel을 보존한다.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/ecc-skills.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, a nested lock is created, or blocked cleanup removes a target</fails_when>
  </verify>
  <reversibility rating="costly">session threading changes internal signatures while existing rendered skill bytes remain unchanged.</reversibility>
  <done>ECC skill writes and removals cannot bypass complete proof, one writer, or durable journal evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component plan→standalone/nested apply | trusted source bytes and file operations cross into state-root mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-26 | Tampering/Elevation | GSD/ECC component apply | critical | mitigate | complete proof and digest before standalone session; caller-token validation for nested apply |
| T-01-27 | Repudiation/Information Disclosure | component failure evidence | high | mitigate | one journal token and central redacted typed results |
</threat_model>

## Artifacts this phase produces

| Kind | Artifact | Contract |
|------|----------|----------|
| Extended APIs | GSD compatibility/ECC plan and apply functions | complete proof plus standalone-or-caller `MutationSession` |

## Probe coverage notes

- Generic package tampering is covered in STRIDE; nested-lock/session bypass is retained as the descriptor-less product prohibition.

<verification>
GSD compatibility and ECC focused suites pass with one session, complete proofs, immediate rechecks, and redacted failure evidence.
</verification>

<success_criteria>
- both component callees are session-aware before install orchestration is modified.
- standalone and nested paths share one complete plan contract.
- no callee discovers paths or acquires a second writer during nested apply.
</success_criteria>

<output>
Create `.planning/phases/01-safe-operation-boundary/01-12-SUMMARY.md` when done.
</output>
