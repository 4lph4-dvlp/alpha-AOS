---
phase: quick
plan: 260922-47e
status: complete
completed: 2026-09-22
requirements-completed: []
---

# Fix Windows Firecrawl fixture environment propagation

Fixed the Windows-only MCP fixture failure by preserving `PATHEXT` and `COMSPEC` in the explicit Node/npm child environment. The environment boundary remains allowlist-based and continues to omit undeclared ambient values.

Tasks completed:
1. Added the two approved Windows launcher names to `NODE_RUNTIME_ENVIRONMENT_NAMES` in `src/core/install.ts`.
2. Extended `test/process.test.ts` to prove the shared Node child policy materializes both names from an explicit source.
3. Extended the production `discoverTools()` round-trip fixture in `test/protocol-session.test.ts` so its Windows fake MCP server refuses to advertise the expected tool when either launcher name is missing.
4. Rebuilt the globally linked CLI, verified the real Firecrawl MCP fixture exposes exactly the four reviewed tools, and applied the managed Codex/Pi/Hermes install. Hermes received its owned skill, 22 ECC skills, and MCP configuration.

Verification:
- RED evidence: focused tests failed 2/32 before the source correction (`PATHEXT` absent in the unit policy and `missing_windows_launcher_environment` returned by the fixture boundary).
- `npm run check`: pass.
- `npm run build`: pass; 118 inputs / 232 outputs.
- Focused process and protocol suites: 32 passed, 0 failed.
- `npm test`: 906 total, 897 passed, 9 skipped, 0 failed.
- `alpha-aos fixture mcp firecrawl codex --apply --json`: pass; four allowed tools, expected family found, no credential stored.
- `alpha-aos install --target 'codex,pi,hermes' --apply --json`: pass; three Hermes components applied.
- Final install dry-run: every Codex, Pi, and Hermes step is current.
- Final offline status: writer free, no repair required, no incomplete transactions, no corrupt journals.

Code commit: `57e70d1`.

Activation note: restart Hermes and any other running harness process before relying on newly synchronized MCP or skill configuration.
