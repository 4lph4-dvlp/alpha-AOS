---
phase: 01-safe-operation-boundary
plan: 15
type: execute
wave: 6
depends_on: [01-03, 01-09, 01-10, 01-14]
files_modified:
  - src/core/support-bundle.ts
  - src/cli.ts
  - src/format.ts
  - src/core/doctor.ts
  - test/redaction.test.ts
  - test/preview.test.ts
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-03, SAFE-04, SAFE-05, SAFE-06]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-15 with D-02~D-04: support-bundle apply binds complete source and destination PathProofs, reviewed plan/source digests, immediate rechecks, one shared MutationSession, and durable journal evidence."
    - "Every CLI mutation remains preview-first; repair, support-bundle, and bootstrap install/update apply only explicit reviewed plans."
    - "D-12~D-14: human, JSON, doctor, top-level error, process metadata, and support output share one bounded redacted serializer and path aliases."
  artifacts:
    - path: src/core/support-bundle.ts
      provides: "Complete local-only support-bundle plan/apply under MutationSession"
      exports: [planSupportBundle, applySupportBundle]
    - path: src/cli.ts
      provides: "Preview-first repair/support/bootstrap routes and central serialization"
  key_links:
    - from: src/core/support-bundle.ts
      to: src/core/transaction.ts
      via: "complete source/destination proofs and caller session passed into journaled file apply"
      pattern: "PathProof|MutationSession|applyFileTransaction|recheck"
    - from: src/cli.ts
      to: src/core/bootstrap.ts
      via: "verified artifact invokes plan/apply while the core service owns the session"
      pattern: "createBootstrapOperationPlan|applyBootstrapOperation"
  prohibitions:
    - statement: "Support-bundle CLI MUST NOT expose an upload destination or transmit data; it may only preview and explicitly create a local redacted artifact."
      requirement_id: SAFE-04
      category: privacy
      status: unverified
      flagged: true
---

<objective>
support-bundle apply와 모든 Phase 1 core services를 preview-first CLI 및 중앙 redacted serializer에 연결한다.

Purpose: local diagnostic write와 wrapper apply가 shared proof/session 경계를 우회하지 않고 user-visible refusal/recovery semantics가 일치하게 한다.
Output: session-bound support-bundle service and complete CLI/format/doctor closure.
</objective>

<execution_context>
@C:/Users/alpha/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safe-operation-boundary/01-CONTEXT.md
@.planning/phases/01-safe-operation-boundary/01-RESEARCH.md
@.planning/phases/01-safe-operation-boundary/01-PATTERNS.md
@.planning/phases/01-safe-operation-boundary/01-03-SUMMARY.md
@.planning/phases/01-safe-operation-boundary/01-09-SUMMARY.md
@.planning/phases/01-safe-operation-boundary/01-10-SUMMARY.md
@.planning/phases/01-safe-operation-boundary/01-14-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Support-bundle preview→proof→session→journaled local apply tracer</name>
  <files>src/core/support-bundle.ts, test/redaction.test.ts</files>
  <read_first>
    src/core/support-bundle.ts
    src/core/redaction.ts
    src/core/path-boundary.ts
    src/core/writer-lock.ts
    src/core/transaction.ts
    test/redaction.test.ts
  </read_first>
  <behavior>
    - planSupportBundle binds every diagnostic source PathProof/hash/metadata, local destination PathProof, state/journal/snapshot/temp/package-root proofs, rendered bytes hash, and deterministic reviewed digest without mutation.
    - applySupportBundle revalidates every source and destination proof/hash, acquires one MutationSession, immediately rechecks sources and destination, then writes through the same session/journal.
    - drift, parent swap, incomplete proof, active/needs-repair state, and unsupported filesystem write nothing; snapshots remain opaque and no network path exists.
  </behavior>
  <action>D-01~D-04 and D-15에 따라 Plan 01-03의 immutable renderer를 `applySupportBundle(reviewedPlan, optionalSession)`에 연결한다. Plan must include complete named roles for each source, selected destination, state/journal/snapshot/temp and runtime package root; bind source hashes, permissions, redacted-byte hash, and plan digest. Revalidate all evidence before standalone session acquisition, then recheck every source and the destination ancestors immediately before passing the same session to `applyFileTransaction`. Caller session must match plan/operation digest and cannot trigger a nested lock. Journal stores only aliases/hashes/size/permissions and redacted entry metadata, never source or snapshot bytes.</action>
  <acceptance_criteria>valid local apply uses one token and deterministic bytes; each incomplete/drift/swap/active fixture writes no destination; journal/support result contains no raw sentinel, private root, snapshot bytes, network field, or upload action.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/redaction.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, a blocked fixture creates the destination, or any secret/snapshot sentinel appears</fails_when>
  </verify>
  <reversibility rating="reversible">the local artifact is transaction-owned and exact journal evidence supports its safe inverse.</reversibility>
  <done>support-bundle creation consumes complete proofs, reviewed digest, one session, immediate rechecks, and journal evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI repair/support/bootstrap와 central serializer closure</name>
  <files>src/cli.ts, src/format.ts, src/core/doctor.ts, test/preview.test.ts, test/redaction.test.ts</files>
  <read_first>
    src/cli.ts
    src/format.ts
    src/core/doctor.ts
    src/core/redaction.ts
    src/core/support-bundle.ts
    src/core/bootstrap.ts
    src/core/writer-lock.ts
    test/preview.test.ts
    test/redaction.test.ts
  </read_first>
  <behavior>
    - existing mutation commands and repair/support/bootstrap install/update routes return read-only plans unless explicit apply is present.
    - repair uses journal/token/hash proof; support delegates its complete reviewed plan; bootstrap delegates the core service that owns the operation session.
    - human/JSON/doctor/top-level errors use one RedactionContext and serializer with stable refusal codes and path aliases.
  </behavior>
  <action>D-01, D-04~D-07, D-12~D-15에 따라 CLI help/routes for `repair`, `support-bundle`, and internal wrapper `bootstrap install|update`을 추가한다. Preview builds and prints complete plans only. Explicit apply passes the reviewed plan to the relevant core service; CLI 또는 wrapper route가 자체 external mutation/second lock을 수행하지 않는다. Replace raw JSON/string error/output writes with `serializeObservable`, keep status/doctor/preview readable during active/needs-repair states, and expose operation ID/start/PID/retry/repair guidance without raw credentials, roots, or subprocess bytes.</action>
  <acceptance_criteria>spawned human/JSON tests show equivalent plan/apply/refusal semantics, preview digests stay unchanged, apply reaches one core-owned session, and all observable bytes exclude corpus sentinels/private roots.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/preview.test.js dist/test/redaction.test.js dist/test/transaction-crash.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, a preview mutates, a CLI route acquires a nested lock, or a secret sentinel appears</fails_when>
  </verify>
  <reversibility rating="costly">new CLI routes are additive; every mutation remains explicit and hash-bound.</reversibility>
  <done>all Phase 1 user-visible routes share preview-first dispatch, one-session ownership, and central redacted output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| diagnostics→local support artifact | untrusted runtime/state metadata becomes user-shareable bytes |
| user argv→core mutation service | explicit apply selects repair/support/bootstrap mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-32 | Information Disclosure/Tampering | support-bundle apply | high | mitigate | complete proofs, source hash/digest recheck, one session, redacted journal-only metadata |
| T-01-33 | Elevation/Information Disclosure | CLI dispatch/output | critical | mitigate | preview default, reviewed plans, core-owned session and central serializer |
</threat_model>

## Artifacts this phase produces

| Kind | Artifact | Contract |
|------|----------|----------|
| Extended service | `src/core/support-bundle.ts` | complete local-only plan/apply under one MutationSession |
| New CLI routes | repair, support-bundle, internal bootstrap install/update | preview-first reviewed-plan dispatch |

<verification>
CLI, redaction, support-bundle, preview, and repair suites pass with one-session and no-disclosure evidence.
</verification>

<success_criteria>
- support-bundle apply has complete source/destination proof and durable journal wiring.
- CLI never performs bootstrap mutation outside the core safe-apply service.
- every output/error surface uses the shared redaction/alias contract.
</success_criteria>

<output>
Create `.planning/phases/01-safe-operation-boundary/01-15-SUMMARY.md` when done.
</output>
