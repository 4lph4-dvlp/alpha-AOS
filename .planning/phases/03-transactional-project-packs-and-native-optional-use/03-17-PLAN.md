---
phase: 03-transactional-project-packs-and-native-optional-use
plan: 17
type: execute
wave: 2
depends_on: ["03-16"]
gap_closure: true
gap_ids: [G-03-1]
files_modified:
  - src/core/canary.ts
  - src/cli.ts
  - test/canary.test.ts
autonomous: true
requirements: [CAPA-01, CAPA-07]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "An explicit canary harness or capability filter that selects zero declarations exits non-zero with the requested filter and available choices instead of printing a zero-run success."
    - "The refusal happens before any runtime, ledger write, cost announcement, or model turn."
    - "The final verification runs CAPA-01 natively on Codex, parses the JSON result, and requires an invoked/held verdict without consulting Claude."
    - "Targeted Codex canary/isolation tests and the complete repository suite pass after the gap closes."
  artifacts:
    - path: "src/core/canary.ts"
      provides: "fail-closed explicit-filter selection contract"
      exports: ["selectCanaries", "runCanarySweep"]
    - path: "src/cli.ts"
      provides: "user-facing doctor refusal before canary state is created"
      contains: "doctor --canary"
    - path: "test/canary.test.ts"
      provides: "core and compiled-CLI regressions for empty filters and the Codex selection"
  key_links:
    - from: "src/cli.ts"
      to: "src/core/canary.ts"
      via: "doctor validates the explicit selection before creating runtimes or entering run callbacks"
      pattern: "selectCanaries|runCanarySweep"
    - from: "test/canary.test.ts"
      to: "dist/src/cli.js"
      via: "a child-process test proves an explicit empty harness filter exits non-zero without a model turn"
      pattern: "doctor.*--canary"
---

<objective>
Make G-03-1 fail closed at the CLI boundary and finish with the required native and full-suite gates.

Purpose: a Codex implementation is not enough if `doctor --canary --harness <id>` can still select nothing and
report a zero-run success. The explicit filter is a user assertion about what must run; an empty selection must
be a refusal before any state or spend.

Output: a stable core/CLI refusal with offline regression coverage, followed by targeted, full-suite, and live
Codex CAPA-01 verification.
</objective>

<execution_context>
@C:/Users/alpha/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-UAT.md
@.planning/debug/codex-native-verification-gap.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-16-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refuse an explicit filter that selects no canary declarations</name>
  <read_first>
    - src/core/canary.ts — `selectCanaries`, `canaryCostLines`, `RunCanarySweepOptions`, and the first lines of `runCanarySweep` before announcements or callbacks.
    - src/cli.ts — `ledgerHarness`, `resolveCapabilityFilter`, and the complete `doctor --canary` branch, noting that state-root/runtime work must stay after selection validation.
    - test/canary.test.ts — selection, cost-line, no-spend, callback-accounting, runtime-write, and CLI-source assertions.
    - .planning/debug/codex-native-verification-gap.md — the reproduced zero-selection result and the requirement for a useful fail-closed result.
  </read_first>
  <behavior>
    - Test 1: a catalog with no Codex declaration plus an explicit Codex filter is refused with a stable code/message naming `codex` and available harnesses.
    - Test 2: an explicit capability filter with no declaration is refused naming the requested capability and available ids/capabilities.
    - Test 3: no filter plus an empty catalog remains a valid zero-work sweep only if that state is reachable by the catalog contract; explicit filters are the load-bearing distinction.
    - Test 4: the refusal occurs before `announce`, `run`, `runHandoff`, runtime creation, or ledger append can be called.
    - Test 5: the shipped catalog filtered to Codex and CAPA-01 selects exactly one pair and proceeds to the supplied runner.
  </behavior>
  <files>src/core/canary.ts, test/canary.test.ts</files>
  <action>
Start RED for the five behaviors, then add one core selection assertion used by `runCanarySweep`. It must know
whether `harness` and/or `capability` was explicitly supplied, distinguish that from an unfiltered sweep, and
throw before cost lines are built or announced. Use the repository's stable, action-oriented error style: name
the requested filter, say that zero declarations matched, list the declared alternatives in stable catalog
order, and provide a runnable next action. Do not create a fake skipped row; that would turn a configuration
mistake into evidence. Preserve D-12 by keeping the refusal outside `blocked`/`unverified` outcome vocabulary.
  </action>
  <verify>
    <automated>npm run check &amp;&amp; npm run build &amp;&amp; node scripts/run-tests.mjs --files dist/test/canary.test.js</automated>
    <fails_when>non-zero exit, any target failure, or an explicit zero-selection test reaches an announcement/run callback</fails_when>
  </verify>
  <done>Every explicit filter either schedules at least one declared leg or refuses before state and spend.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind the doctor CLI to the fail-closed selection contract</name>
  <read_first>
    - src/cli.ts — the completed `doctor --canary` branch from filter parsing through printed result and exit-code handling.
    - src/core/canary.ts — Task 1's explicit-selection assertion and the completed 03-16 Codex selection.
    - test/canary.test.ts — existing child-process/source tests and temp state-root fixtures; reuse their shell-free bounded subprocess idiom.
  </read_first>
  <behavior>
    - Test 1: compiled `doctor --canary --harness pi --no-spend --json` exits non-zero before creating state when the shipped catalog has no Pi declarations, and stderr names `pi` plus available harnesses.
    - Test 2: compiled `doctor --canary --harness codex --capability CAPA-01 --no-spend --json` does not refuse selection and accounts for the declared spending leg as unverified/not attempted without launching it.
    - Test 3: the explicit-filter refusal output contains no private auth-root path, credential value, raw environment, or runtime path.
  </behavior>
  <files>src/cli.ts, test/canary.test.ts</files>
  <action>
Wire the doctor branch to validate its selection immediately after catalog/filter loading and before resolving
state roots, probing harness versions, creating runtimes, announcing cost, or appending ledger entries. If the
core assertion already runs inside `runCanarySweep`, structure the CLI so the same contract remains the single
authority and prove ordering with injected callbacks or a temporary `ALPHA_AOS_STATE_DIR`; do not duplicate
selection rules in CLI prose. Add shell-free compiled-CLI tests using `process.execPath`, bounded captured output,
and a temporary state root. The negative uses Pi because it is a valid declared harness id with no shipped
canary legs; it spends no turn. The Codex no-spend control proves the new CAPA-01 declaration is selectable
without spending during this regression test.
  </action>
  <verify>
    <automated>npm run check &amp;&amp; npm run build &amp;&amp; node scripts/run-tests.mjs --files dist/test/canary.test.js</automated>
    <fails_when>non-zero exit, any target test failure, the Pi command exits zero, or the Codex no-spend control reports zero selections</fails_when>
  </verify>
  <done>The user-facing command cannot report success for an explicit harness/capability request that ran nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI filters → declared canary selection | user input determines which paid/native legs are eligible to run |
| selection → state/spend callbacks | an empty or malformed request must stop before mutation or model invocation |
| native result → completion claim | only a parsed invoked/held Codex result may close G-03-1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-135 | Repudiation | explicit empty selection | high | mitigate | Refuse zero matches before announcements/callbacks and name the requested filter plus declared alternatives |
| T-03-136 | Denial of Service | malformed or stale filters | low | accept | A bounded non-zero refusal is the intended fail-closed behavior; the message supplies a runnable correction |
| T-03-137 | Information Disclosure | CLI refusal | medium | mitigate | Build the refusal only from catalog ids/harness names and assert no environment, auth path, runtime path, or credential value is serialized |
| T-03-138 | Spoofing | final native-use claim | high | mitigate | Parse the live JSON envelope and require Codex harness, `nativeUse: invoked`, and `verdict.held: true`; model output text is not consulted |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package and changes no dependency or lock file |
</threat_model>

## Source Coverage Audit

| SOURCE | ID | Item | Plan | Status | Notes |
|--------|----|------|------|--------|-------|
| GOAL | — | prove native optional capability use | 03-16, 03-17 | COVERED | native Codex result plus fail-closed command accounting |
| REQ | CAPA-01 | native version-sensitive Context7 invocation | 03-16, 03-17 | COVERED | tracer and final parsed live gate |
| REQ | CAPA-07 | honest blocked/unverified/invoked reporting | 03-16, 03-17 | COVERED | runtime readiness plus zero-selection refusal |
| RESEARCH | — | Codex `exec --json` invocation surface | 03-16 | COVERED | measured native adapter, not the discovery oracle |
| CONTEXT | D-01 | MCP observation is invocation authority | 03-16, 03-17 | COVERED | final gate checks the observation-derived verdict |
| CONTEXT | D-02 | observation proxy exists only for canaries | 03-16 | COVERED | runtime overrides and everyday regression |
| CONTEXT | D-09 | original Claude pass bar | 03-16, 03-17 | COVERED WITH USER OVERRIDE | no Claude execution; compatibility declarations remain intact |
| CONTEXT | D-12 | honest outcome vocabulary | 03-16, 03-17 | COVERED | readiness uses existing states; empty selection is a refusal |
| GAP | G-03-1 | Codex verification without Claude account | 03-16, 03-17 | COVERED | both plans carry the required gap id |

No deferred idea is implemented. Other completed Phase 3 decisions remain unchanged and are not diagnosed gaps.

<verification>
- Targeted gate: `npm run check &amp;&amp; npm run build &amp;&amp; node scripts/run-tests.mjs --files dist/test/canary.test.js dist/test/isolation.test.js`.
- Full-suite gate: `npm test` passes with zero failures; do not pin a recalled test count.
- Native final gate: `node -e "const{spawnSync}=require('node:child_process');const r=spawnSync(process.execPath,['dist/src/cli.js','doctor','--canary','.','--harness','codex','--capability','CAPA-01','--json'],{encoding:'utf8'});if(r.status!==0)throw new Error(r.stderr||r.stdout);const j=JSON.parse(r.stdout);if(j.results?.length!==1)throw new Error('expected one Codex result');const x=j.results[0];if(x.harness!=='codex'||x.nativeUse!=='invoked'||x.verdict?.held!==true)throw new Error('Codex CAPA-01 evidence did not hold');if((j.results??[]).some(y=>y.harness==='claude'))throw new Error('Claude was included');console.log('G-03-1 native Codex verification passed')"`.
- Required negative gate: `node dist/src/cli.js doctor --canary . --harness pi --no-spend --json` exits non-zero and creates no canary runtime or ledger row.
</verification>

<success_criteria>
- An explicit empty filter cannot create a false zero-run success.
- The targeted canary/isolation regression set and `npm test` pass.
- One live Codex-only CAPA-01 run reports invoked/held from ordered, version-scoped Context7 observations.
- No Claude login, Claude model turn, or Claude verification command is required.
</success_criteria>

<output>
Create `.planning/phases/03-transactional-project-packs-and-native-optional-use/03-17-SUMMARY.md` when done
</output>
