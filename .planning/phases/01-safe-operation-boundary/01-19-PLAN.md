---
phase: 01-safe-operation-boundary
plan: 19
type: execute
wave: 10
depends_on: [01-18]
files_modified:
  - src/core/path-boundary.ts
  - test/path-boundary.test.ts
autonomous: true
gap_closure: true
gap_ids: [G-01-1]
requirements: [SAFE-02]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "SAFE-02 / D-01: an operation whose allowed root does not exist yet, and whose ancestor is a junction, a POSIX symlink or an 8.3 short name, is PROVEN rather than refused as `outside-canonical-root`. Failing closed means refusing what cannot be proven, not refusing what is legitimate."
    - "The target path and the allowed root are canonicalized by exactly one function, so a comparison never places a canonicalized target against a non-canonical root."
    - "A canonicalization that cannot be completed is reported in the proof with a stated reason and a refusal code, never absorbed by a catch that leaves a non-canonical string in place of a canonical one."
    - "SAFE-02 is not weakened by the fix: a target that escapes the canonical root is still refused with `outside-canonical-root`, including when the allowed root is itself reached through a link, and the outside sentinel is unchanged."
    - "The regression is reproducible on an ordinary developer host — a Windows junction needs no privilege and a POSIX directory symlink needs none either, so the fixture builds the link itself rather than depending on how CI provisions its temp directory."
  artifacts:
    - path: src/core/path-boundary.ts
      provides: "Symmetric canonical resolution for every path the boundary compares, with an explicit refusal when resolution cannot be completed"
      exports: [provePathBoundary, recheckPathProof, proveOperationPaths, assertPathProven, PathBoundaryError, requiredRolesForFileMutation]
    - path: test/path-boundary.test.ts
      provides: "Regression fixtures that build a real link and place a not-yet-created allowed root under it, plus the negative control that the escape refusal survives the fix"
  key_links:
    - from: test/path-boundary.test.ts
      to: src/core/path-boundary.ts
      via: "the regression fixtures call provePathBoundary directly with a linked, not-yet-existing allowed root"
      pattern: "provePathBoundary"
    - from: src/core/path-boundary.ts
      to: src/core/path-boundary.ts
      via: "provePathBoundary and recheckPathProof resolve the allowed root through the same helper that resolves the target"
      pattern: "canonicalizeWithMissingTail"
  prohibitions:
    - statement: "The boundary MUST NOT compare a canonically resolved path against a root that was not resolved the same way; the two sides of every containment comparison come from one resolution function."
      requirement_id: SAFE-02
      category: safety
      status: unverified
      flagged: true
    - statement: "A failed canonical resolution MUST NOT be absorbed into a fallback that substitutes the unresolved string; the proof must carry a refusal code and a stated reason instead."
      requirement_id: SAFE-02
      category: safety
      status: unverified
      flagged: true
    - statement: "Restoring the legitimate case MUST NOT be achieved by relaxing containment — an escaping target stays refused with `outside-canonical-root` even when the allowed root is itself reached through a link."
      requirement_id: SAFE-02
      category: safety
      status: unverified
      flagged: true
---

<objective>
`provePathBoundary` 가 대상 경로와 허용 루트를 서로 다른 방식으로 정규화하는 비대칭을 제거한다. 대상은 "가장 가까운 존재하는 조상의 realpath + 아직 없는 나머지" 로 정규화되는데 루트는 `realpath(configuredRoot)` 한 번만 부르고 실패하면 조용히 비정규 문자열에 머문다. 그 결과 정상 연산이 `outside-canonical-root` 로 거부된다.

Purpose: 01-UAT.md `G-01-1` RC-1 을 닫는다. CI run 33937610401 에서 macOS 50건 / Windows 46건 실패의 주 원인이고, 테스트 환경 문제가 아니라 제품 결함이다 — 상태 루트가 junction, symlink, 8.3 단축명 아래에 있고 아직 만들어지지 않은 사용자는 정당한 연산을 거부당한다. D-01 은 "증명할 수 없으면 fail closed" 이지 "증명할 수 있는 것까지 거부" 가 아니다.
Output: 한 개의 공유 정규화 헬퍼, 세 곳의 조용한 catch 를 대체하는 명시적 refusal, 그리고 개발 호스트에서 실제로 빨간불이 되는 회귀 픽스처 3종.
</objective>

<execution_context>
@C:/Users/alpha/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safe-operation-boundary/01-UAT.md
@.planning/phases/01-safe-operation-boundary/01-07-SUMMARY.md
@src/core/path-boundary.ts
@test/path-boundary.test.ts
</context>

## Gap being closed

`01-UAT.md` gap `G-01-1`, root cause **RC-1**. The UAT states the mechanism precisely and it is reproduced, not hypothesised:

> 대상은 `src/core/path-boundary.ts:231-241` 에서 "가장 가까운 존재하는 조상의 realpath + 아직 없는 나머지" 로 계산되므로 아직 만들어지지 않은 경로도 정규화된다. 반면 허용 루트는 `:243-248` 에서 `realpath(configuredRoot)` 를 그냥 호출하고, 루트 자체가 아직 없으면 ENOENT 로 throw 하여 catch 가 비정규 문자열을 그대로 쓴다. 그 결과 `:257` 의 `withinRoot(canonicalRootSource, canonical)` 이 정규화된 대상과 비정규 루트를 비교해 정상 연산을 `outside-canonical-root` 로 거부한다.

Local reproduction on this Windows host (2026-09-05): a junction plus a not-yet-existing `<junction>/state` as the allowed root produced `proven=false, code=outside-canonical-root`; `realpath(root)` threw ENOENT while the target expanded through the junction. Removing the junction makes the same call pass.

This plan closes `missing` items 1, 2 and 3 of `G-01-1`.

## Multi-Source Coverage Audit

| Source | Item | Status | Where |
|--------|------|--------|-------|
| GOAL (ROADMAP Phase 1) | "reject unsafe or ambiguous operations" — a refusal must mean unprovable, not merely unresolved-by-one-of-two-methods | COVERED | Task 2 (single resolution function), Task 1 (fixture 1, 2) |
| ROADMAP SC2 | "User sees a refusal when a write or removal could escape an allowed root through a symlink, junction, reparse point, alias, or parent-path change, and an outside sentinel remains untouched" | COVERED | Task 1 fixture 3 (negative control, sentinel hash asserted) |
| REQ SAFE-02 | realpath/reparse-aware root containment | COVERED | Task 1, Task 2 |
| UAT `missing[0]` | "허용 루트를 대상 경로와 동일한 방식으로 정규화한다" | COVERED | Task 2 |
| UAT `missing[1]` | "정규화 실패를 조용한 catch 로 삼키지 않는다" | COVERED | Task 2 (three catch sites) |
| UAT `missing[2]` | "루트가 아직 존재하지 않고 조상이 junction/symlink/8.3 단축명인 경우를 고정하는 회귀 테스트" | COVERED | Task 1 |
| CONTEXT D-01 | "fail closed with inspection preserved" — refusal is for the unprovable | COVERED | Task 2 (`unprovable-filesystem` carries a reason) |
| CONTEXT D-03 | "Symlinks and junctions are allowed only when both configured and canonical paths remain inside the allowed boundary" | COVERED | Task 1 fixtures 1–3 |

No source item is MISSING. Nothing is deferred by this plan.

## Flagged Assumptions (spec-less edge probe)

The deterministic edge probe for this phase returned **6 applicable, 0 resolved, 6 unresolved**. The six rows are enumerated in full in `01-17-PLAN.md` and are one shared ledger across every gap-closure plan of this phase, so they are not restated here and are not double-counted.

**No-silent-drop equality:** 6 probe-surfaced edge items == 0 authored into `must_haves.truths` + 6 surfaced as flagged assumptions in the shared ledger. This plan's share is row 2 (SAFE-02), whose predicates are stated explicitly in `must_haves.truths` above rather than inferred from the probe. 3 prohibitions kept and authored descriptor-less into `must_haves.prohibitions`.

## Artifacts this plan produces

| Symbol | Kind | File | Exported |
|--------|------|------|----------|
| `canonicalizeWithMissingTail` | async function returning `{ canonical, reason }` | `src/core/path-boundary.ts` | no (module-local) |
| `provePathBoundary` | **behaviour change**: root and target resolved by the same helper; an unresolvable side becomes an `unprovable-filesystem` refusal | `src/core/path-boundary.ts` | yes (signature unchanged) |
| `recheckPathProof` | **behaviour change**: the recheck resolves the allowed root through the same helper instead of a bare `realpath` with a silent fallback | `src/core/path-boundary.ts` | yes (signature unchanged) |
| `classifyComponent` | **behaviour change**: a component whose own canonical form cannot be read becomes unprovable rather than silently recording its unresolved path | `src/core/path-boundary.ts` | no (module-local) |

No exported signature changes. `PathBoundaryCode` gains no new member — `unprovable-filesystem` already exists and is the correct code for "support could not be proven".

<!-- planner-discipline-allow: await realpath( -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 개발 호스트에서 실제로 빨간불이 되는 RC-1 회귀 픽스처</name>
  <files>test/path-boundary.test.ts</files>
  <read_first>
    test/path-boundary.test.ts
    src/core/path-boundary.ts
  </read_first>
  <behavior>
    - A junction (Windows) or directory symlink (POSIX) is created by the fixture itself; nothing depends on how the host provisions `os.tmpdir()`, so the defect reproduces on an ordinary developer machine and not only on CI.
    - Fixture 1 — root does not exist yet, ancestor is a link: allowed root `<root>/link/state` where `<root>/link` is a link to `<root>/real` and `state` has never been created. `provePathBoundary` for a target under it must return `proven === true` and `code === "ok"`. Before the fix this returns `outside-canonical-root`.
    - Fixture 2 — root exists but is reached through a link (the macOS `/var` → `/private/var` shape): `<root>/real/state` exists, `<root>/link` links to `<root>/real`, allowed root is `<root>/link/state`. Must be `proven === true`.
    - Fixture 3 — negative control: with the same linked allowed root, a target that resolves outside it is still refused with `code === "outside-canonical-root"`, and the outside sentinel's sha256 is unchanged.
    - Fixture 4 — the refusal in fixture 3 names a canonical root: the `detail` string contains the canonically resolved root, proving both sides of the comparison were resolved rather than only one.
  </behavior>
  <action>
`test/path-boundary.test.ts` 에 RC-1 회귀 테스트를 추가한다. **이 task 는 소스를 고치지 않는다.** 픽스처를 먼저 커밋해 빨간불을 확정한 다음 Task 2 에서 고친다.

기존 파일 규약을 그대로 따른다. 링크 생성은 이미 있는 `tryDirectorySymlink(target, linkPath)` 를 쓴다 — Windows 에서는 `junction`, POSIX 에서는 `dir` 로 만들고 권한이 없으면 `false` 를 돌려준다. `false` 인 경우에만 `notRun.push({ fixture, reason })` 후 `context.skip(...)` 하는 이 파일의 기존 not-run 규약을 그대로 쓴다. Windows junction 은 권한이 필요 없고 POSIX 디렉터리 symlink 도 필요 없으므로 두 플랫폼 모두에서 실제로 실행되어야 한다 — skip 이 정상 경로가 되면 그것은 픽스처 결함이다.

네 개의 테스트를 추가한다.

1. `an allowed root that does not exist yet under a link is still provable`
   `createFixture` 로 얻은 `root` 아래에 `real` 디렉터리를 만들고 `link` 를 `real` 로 향하는 링크로 만든다. 허용 루트는 `join(root, "link", "state")` — 아직 만들지 않는다. 대상은 `join(root, "link", "state", "journal", "entry.json")`. `provePathBoundary({ path, allowedRoots: [allowedRoot], role: "journal" })` 를 호출해 `proof.proven === true` 와 `proof.code === "ok"` 를 단언한다. 실패 메시지에 `proof.detail` 을 넣어 빨간불일 때 실제 사유가 보이게 한다.

2. `an allowed root reached through a link is canonicalized on both sides`
   같은 링크 구조에서 이번에는 `join(root, "real", "state")` 를 먼저 만든다. 허용 루트는 `join(root, "link", "state")`, 대상은 `join(root, "link", "state", "entry.json")`. `proven === true` 를 단언하고, `proof.canonical` 이 링크를 통과한 실제 경로(즉 `real` 쪽)를 가리키는지 — `proof.canonical` 이 `await realpath(join(root, "real", "state"))` 로 시작하는지 — 도 함께 단언한다.

3. `a target that escapes a linked allowed root is still refused`
   링크 구조를 만들고 허용 루트를 `join(root, "link")` 로 둔다. `fixture.outsideDirectory` 를 향하는 링크 `join(root, "link", "escape")` 를 만든 뒤 대상을 `join(root, "link", "escape", "written.txt")` 로 한다. `proof.proven === false` 와 `proof.code === "outside-canonical-root"` 를 단언하고, `sha256File(fixture.outsideSentinel)` 이 호출 전후로 동일한지 단언한다. 이 테스트는 Task 2 이전에도 초록이어야 한다 — 초록이 아니면 픽스처가 틀린 것이고, Task 2 이후에도 반드시 초록이어야 한다. 이것이 "정상 연산 복구" 가 "경계 완화" 로 구현되지 않았다는 유일한 자동 증거다.

4. `a canonical-root refusal names the canonically resolved root`
   3번과 같은 구조에서 얻은 `proof.detail` 이 `await realpath(join(root, "real"))` 문자열을 포함하는지 단언한다. 비대칭이 남아 있으면 detail 에는 링크 경로가 실려 있으므로 이 단언이 두 측면이 모두 정규화됐다는 것을 고정한다.

**RED 확정 절차 (건너뛰지 말 것).** 소스를 고치기 전에 `npm run build` 후 네 테스트를 이름으로 실행해 1번과 4번이 실제로 실패하는 것을 관측한다. 관측한 실패 코드와 `detail` 문자열을 RED 커밋 메시지 본문에 그대로 적는다. 1번이 수정 전에 통과한다면 픽스처가 결함을 재현하지 못한 것이므로 진행하지 말고 픽스처를 고친다 — 이 호스트에서 재현되지 않는 회귀 테스트는 회귀 테스트가 아니다.

RED 커밋: `test(01-19): add failing regression for asymmetric root canonicalization`.
  </action>
  <acceptance_criteria>
    - `test/path-boundary.test.ts` names all four tests: `an allowed root that does not exist yet under a link is still provable`, `an allowed root reached through a link is canonicalized on both sides`, `a target that escapes a linked allowed root is still refused`, `a canonical-root refusal names the canonically resolved root`
    - The RED commit body records the observed `code` and `detail` for the two tests that fail before the fix
    - `src/core/path-boundary.ts` is unchanged by this task — `git show --stat` for the RED commit lists only `test/path-boundary.test.ts`
    - `a target that escapes a linked allowed root is still refused` passes both before and after Task 2
    - The not-run list reported by this suite gains no new entry on this host: the link fixtures execute rather than skip
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; node --test --test-name-pattern "escapes a linked allowed root" dist/test/path-boundary.test.js</automated>
    <fails_when>non-zero exit, or `# fail 0` absent — the negative control must be green before the fix, otherwise the fixture itself is wrong</fails_when>
    <automated>npm run build &amp;&amp; if node --test --test-name-pattern "does not exist yet under a link" dist/test/path-boundary.test.js; then echo "RED-GATE-NOT-MET"; exit 1; else echo "red confirmed"; fi</automated>
    <fails_when>the output contains `RED-GATE-NOT-MET` — the regression test passed against unmodified source, so it does not reproduce RC-1 on this host and must be corrected before Task 2</fails_when>
  </verify>
  <reversibility rating="reversible">Test fixtures only; reverting removes assertions without changing production behaviour.</reversibility>
  <done>Four regression tests exist, the two that pin RC-1 fail against unmodified source with the recorded reason, and the escape negative control is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 대상과 루트를 한 함수로 정규화하고 실패를 proof 에 드러낸다</name>
  <files>src/core/path-boundary.ts</files>
  <read_first>
    src/core/path-boundary.ts
    test/path-boundary.test.ts
  </read_first>
  <behavior>
    - `provePathBoundary` resolves the allowed root and the target through one function, so `withinRoot` never receives one resolved and one unresolved operand.
    - An allowed root that does not exist yet resolves to the realpath of its deepest existing ancestor plus the not-yet-created remainder, exactly as the target already does.
    - When neither side can be resolved at all, the proof returns `proven === false` with code `unprovable-filesystem` and a `detail` that names the path and the reason — not a comparison against an unresolved string.
    - `recheckPathProof` resolves the allowed root the same way; a resolution failure there returns `{ ok: false, code: "unprovable-filesystem" }` rather than continuing against the configured root.
    - Every existing assertion in `test/path-boundary.test.ts` and `test/transaction-crash.test.ts` stays green: containment, escape refusal, unknown reparse, ancestor change and identity checks are unchanged in meaning.
  </behavior>
  <action>
`src/core/path-boundary.ts` 를 고친다. 이 파일에는 정규화 실패를 삼키는 catch 가 정확히 세 곳 있고, 세 곳 모두 다룬다.

**공유 헬퍼를 만든다.** 모듈 로컬 `async function canonicalizeWithMissingTail(pathValue: string): Promise<{ canonical: string; reason: string | null }>` 를 추가한다. `ancestorChain(pathValue)` 를 가장 깊은 쪽부터 거슬러 올라가며 `realpath` 가 성공하는 첫 조상을 찾고, `canonical = suffix === "" ? resolvedAncestor : resolve(resolvedAncestor, suffix)` 로 계산한다. 여기서 `suffix` 는 `relative(ancestor, pathValue)` 다. 어떤 조상도 해결되지 않으면 `{ canonical: pathValue, reason: "no ancestor of <pathValue> could be canonically resolved" }` 를 돌려준다. `reason` 이 null 이 아닌 결과를 정상 값처럼 쓰는 호출자가 없어야 한다.

**첫째 catch — 허용 루트 (현재 243-248번째 줄).** `realpath(configuredRoot)` 한 번 호출과 그 catch 를 통째로 `canonicalizeWithMissingTail(configuredRoot)` 로 바꾼다. `reason` 이 null 이 아니면 `withinRoot` 비교에 도달하지 말고 `unsupported(role, configured, reason, "unprovable-filesystem")` 를 돌려준다. 이것이 RC-1 의 직접 원인이다.

**둘째 catch — 대상 (현재 231-241번째 줄).** 지금은 `components` 배열의 마지막 존재 컴포넌트의 `canonical` 을 써서 계산한다. 같은 결과를 같은 방식으로 얻도록 이 블록도 `canonicalizeWithMissingTail(configured)` 호출로 바꾼다. `reason` 이 null 이 아니면 마찬가지로 `unprovable-filesystem` 로 거부한다. 두 측면이 문자 그대로 같은 함수를 통과하는 것이 이 plan 의 핵심이므로, 대상 쪽 계산을 "지금도 맞으니 그대로 둔다" 로 남기지 않는다 — 비대칭을 코드에서 제거하는 것이 요구사항이다. `components` 배열 자체는 `recheckPathProof` 가 쓰므로 그대로 채운다.

**셋째 catch — 컴포넌트 자기 정규화 (현재 154-159번째 줄).** `classifyComponent` 안의 `try { canonical = await realpath(path); } catch { canonical = path; }` 는 해결 불가 상태를 해결된 것처럼 기록한다. 이것을 `unknownReparse` 와 같은 자리의 실패 신호로 바꾼다 — 반환 타입을 `PathComponentIdentity | { unprovable: true; path: string; reason: string }` 형태로 넓히고, 호출부(218-229번째 줄 루프)에서 그 신호를 받으면 `unsupported(role, configured, reason, "unprovable-filesystem")` 로 거부한다. 기존 `unknownReparse` 분기는 `unknown-reparse` 코드를 계속 쓰고 이 새 분기와 섞지 않는다 — 두 사유는 다른 것이고 코드가 그 차이를 유지해야 한다.

**`recheckPathProof` (현재 294-301번째 줄).** `realpath(canonicalRoot)` 호출과 주석뿐인 catch 를 `canonicalizeWithMissingTail(proof.allowedRoot)` 로 바꾼다. `reason` 이 null 이 아니면 즉시 `{ ok: false, code: "unprovable-filesystem", detail: reason }` 를 돌려준다. 지금처럼 "설정된 루트로 되돌아가 아래에서 어차피 검사한다" 로 이어가면, 검사가 실행되는 시점에는 다시 비정규 루트와 정규화된 endpoint 를 비교하게 되므로 같은 결함이 apply 직전 recheck 경로에 남는다.

**바꾸지 않는 것.** `withinRoot` 의 세그먼트 인지 비교, `comparable` 의 대소문자 정책, `outside-configured-root` 분기, `unknown-reparse` 분기, `ancestor-changed` 판정, `proveOperationPaths` 의 role 검사와 `includes` 색인은 손대지 않는다. 이 plan 은 정규화의 대칭성만 고친다.

Task 1 의 네 테스트가 모두 초록이 되면 GREEN 커밋: `fix(01-19): canonicalize the allowed root the same way as the target`.
  </action>
  <acceptance_criteria>
    - `src/core/path-boundary.ts` contains `canonicalizeWithMissingTail` and calls it for both the target and the allowed root inside `provePathBoundary`
    - `git grep -c 'await realpath(' -- src/core/path-boundary.ts` returns 1 — the only remaining direct call is the one inside the shared helper
    - `provePathBoundary` returns `unprovable-filesystem` with a non-null `detail` when a side cannot be resolved, and never reaches `withinRoot` in that case
    - `recheckPathProof` no longer contains a `realpath` call with an empty or comment-only catch
    - `node --test dist/test/path-boundary.test.js` reports `# fail 0` with every one of Task 1's four tests present in the passing output
    - `node --test dist/test/transaction-crash.test.js dist/test/install.test.js dist/test/update.test.js` reports `# fail 0` — the boundary is consumed by the transaction and installer paths and their meaning must not shift
    - `npm test` reports `fail 0` and a total test count of at least 194 — the 190 baseline recorded by `01-18-SUMMARY.md` plus this plan's 4 new tests. If `01-18-SUMMARY.md` records a higher final count, that number is the baseline instead and the threshold rises with it
  </acceptance_criteria>
  <verify>
    <automated>npm run check &amp;&amp; npm run build &amp;&amp; node --test dist/test/path-boundary.test.js</automated>
    <fails_when>non-zero exit, `# fail 0` absent from the summary, or the test name `an allowed root that does not exist yet under a link is still provable` absent from the passing-test output</fails_when>
    <automated>node --test dist/test/transaction-crash.test.js dist/test/install.test.js dist/test/update.test.js</automated>
    <fails_when>non-zero exit, or `# fail 0` absent from the summary</fails_when>
    <automated>npm test</automated>
    <fails_when>non-zero exit, `fail 0` absent from the summary line, or a reported `tests` count of 193 or lower</fails_when>
  </verify>
  <reversibility rating="reversible">One file, no exported signature change, no caller change. Reverting restores the previous asymmetry and the four regression tests go red again, which is the intended alarm.</reversibility>
  <done>A legitimate operation under a link with a not-yet-created allowed root is proven, an escaping one is still refused, and no comparison in the file places a resolved path against an unresolved one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Configured path → allowed root containment | Every managed write, removal and snapshot crosses here; the proof is the only thing standing between a link and a write outside the boundary. |
| Filesystem state → proof validity | The filesystem can change between preflight and apply; `recheckPathProof` is where that gap is closed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-19-01 | Elevation of Privilege | a fix that restores the legitimate case by relaxing `withinRoot` | critical | mitigate | Task 1 fixture 3 is a mandatory negative control asserted green both before and after Task 2, with the outside sentinel's sha256 compared across the call. Task 2 explicitly forbids touching `withinRoot`, `comparable` or the `outside-configured-root` branch. |
| T-01-19-02 | Denial of Service | over-refusal of legitimate operations (the defect itself) | high | mitigate | Task 2 makes the two sides of every comparison come from one resolution function; Task 1 fixtures 1 and 2 pin the two shapes CI observed (not-yet-existing root under a link; existing root reached through a link). |
| T-01-19-03 | Tampering | an unresolvable component silently recorded with its unresolved path, letting a later recheck compare against a value that was never canonical | medium | mitigate | Task 2's third catch site turns that into an `unprovable-filesystem` refusal carrying the reason. |
| T-01-19-04 | Tampering | the apply-time recheck retaining the asymmetry after preflight is fixed | high | mitigate | Task 2 routes `recheckPathProof` through the same helper and refuses on an unresolvable root instead of continuing against the configured one. |
| T-01-19-SC | Tampering | npm / pip / cargo installs | low | accept | This plan adds no dependency and runs no package-manager install task. No new legitimacy checkpoint is required. |
</threat_model>

<verification>
- `npm run check` exits 0 with no output.
- `node --test dist/test/path-boundary.test.js` reports `# fail 0` and names all four new tests.
- `node --test dist/test/transaction-crash.test.js dist/test/install.test.js dist/test/update.test.js` reports `# fail 0`.
- `npm test` reports `fail 0` with a total strictly greater than the count `01-18-SUMMARY.md` records.
- `git grep -c 'await realpath(' -- src/core/path-boundary.ts` returns 1.

**Local green is necessary but not sufficient.** The authoritative verification for gap `G-01-1` is a green three-OS CI matrix run — the same matrix (run 33937610401) that exposed RC-1. RC-1 was invisible on this developer host until a fixture built the link itself; a macOS or Windows leg is the only place the `os.tmpdir()` shapes occur naturally. That observation is owned by plan `01-23`.
</verification>

<success_criteria>
1. `provePathBoundary` compares a canonically resolved target against a canonically resolved root, produced by the same function.
2. A not-yet-created allowed root whose ancestor is a junction, a POSIX symlink or an 8.3 short name is proven, not refused.
3. A resolution that cannot be completed surfaces in the proof as `unprovable-filesystem` with a stated reason.
4. The escape refusal required by ROADMAP SC2 is unchanged, proven by a negative control that is green before and after the fix, with the outside sentinel byte-identical.
5. `recheckPathProof` carries no residual asymmetry into the moment of mutation.
</success_criteria>

<output>
Create `.planning/phases/01-safe-operation-boundary/01-19-SUMMARY.md` when done.
</output>
