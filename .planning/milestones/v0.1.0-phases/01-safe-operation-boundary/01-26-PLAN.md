---
phase: 01-safe-operation-boundary
plan: 26
type: execute
wave: 17
depends_on: [01-24, 01-25]
files_modified:
  - .planning/WINDOWS.md
autonomous: true
gap_closure: true
gap_ids: [G-01-1]
requirements: [SAFE-01, SAFE-02, SAFE-03, SAFE-04, SAFE-05, SAFE-06]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Gap G-01-1 closes on, and only on, a NEW three-OS CI run id in which ubuntu-latest, macos-latest AND windows-latest are each green across all four steps: `npm run check`, `npm test`, `npm run build:check`, and `Safety boundary suites`. Two of three is not closure and local green on this Windows developer host is not a substitute."
    - "The windows-latest leg passes `Safety boundary suites`, and `the test runner environment carries no npm lifecycle injection` is reported as passing in that step rather than absent from it — the test must be seen running and green, not seen missing."
    - "All ten named Phase 1 suites actually execute on every leg, and `wrapper families executed: 4; not-run: []` still holds on all three."
    - "The step total on each leg is at least as large as the previous run's 125, because `01-24` and `01-25` only add tests; a smaller total means a suite stopped running and is treated as a red result rather than a faster one."
    - "A Broken Window is marked fixed only where a leg produced the evidence for it. A fixture that reported not-run on every leg leaves its ledger entry open, and so does an entry whose evidence has not been observed YET — `not observed yet` and `observed and negative` are different states and the record says which one it is. Only entry #1, which is decided on the written record rather than on a leg, is disposed regardless of run state."
    - "The record states what this run could NOT decide as plainly as what it decided: the Windows unknown-reparse canary and the file-symlink escape canary remain manual-only and unresolved, and a green run does not upgrade them."
    - statement: "If the matrix run is interrupted, cancelled, or a leg is re-run individually, the closing evidence is a single run id whose three legs are all green in that same run — not a stitched-together set of green legs from different runs."
      verification: backstop
  artifacts:
    - path: .planning/WINDOWS.md
      provides: "Ledger entries disposed against observed per-leg evidence: #1 waived on the written record in every state; #3 marked fixed on the green macos-latest floor assertion, #2 and #4 marked fixed only where a leg actually ran the new fixture — and all three left open, with their awaited observation named, when the three-OS run has not been observed yet"
  key_links:
    - from: .github/workflows/ci.yml
      to: scripts/run-tests.mjs
      via: "the observed run is what proves the routing repair works on the platform that exposed RC-5, which no local run on this host can show"
      pattern: "run-tests.mjs"
    - from: .github/workflows/ci.yml
      to: test/preview.test.ts
      via: "the per-OS safety step names the ten Phase 1 suites, so a suite that silently stops running is a failure rather than an omission nobody notices"
      pattern: "preview.test"
  prohibitions:
    - statement: "G-01-1 MUST NOT be recorded as closed on local evidence, on two of three green legs, or on green legs stitched from different runs. The closing evidence is one observed run id whose three legs are all green."
      requirement_id: SAFE-01
      category: safety
      status: unverified
      verification: null
      flagged: true
    - statement: "A red leg MUST NOT be closed by removing a suite from the workflow, marking a test as not-run, or relaxing an assertion. A red leg is new information about the product and returns to diagnosis."
      requirement_id: SAFE-02
      category: safety
      status: unverified
      verification: null
      flagged: true
    - statement: "A red leg MUST NOT be explained away as cosmetic because only a meta-test failed. RC-3 concealed a real SAFE-01 violation for a full verification cycle through exactly that reasoning, and RC-5 is the same defect class at a different seam."
      requirement_id: SAFE-01
      category: safety
      status: unverified
      verification: null
      flagged: true
    - statement: "A Broken Window MUST NOT be marked fixed on a leg where its fixture reported not-run. A not-run fixture is an absence of evidence, not evidence of absence."
      requirement_id: SAFE-02
      category: transparency
      status: unverified
      verification: null
      flagged: true
    - statement: "A manual-only verification MUST NOT be upgraded to observed because an automated run happened to be green. The unknown-reparse and file-symlink canaries stay unresolved until a privileged host actually runs them."
      requirement_id: SAFE-02
      category: transparency
      status: unverified
      verification: null
      flagged: true
---

<objective>
`G-01-1` 의 수용 게이트를 다시 실행한다. `01-VERIFICATION.md` 는 이 gap 이 **오직** 세 leg 이 모두 초록인 새 CI run id 로만 닫힌다고 못 박았고, 이전 라운드에서 이 개발 호스트의 로컬 초록이 POSIX 사용자의 홈에 파일을 쓰던 진짜 SAFE-01 위반을 통과시킨 사실이 그 규칙의 근거다. 그 기록이 verifier 의 표현으로 "로컬 초록을 통과로 승격시키는 것에 반대하는 가장 강한 논거" 다.

Purpose: `01-24` 는 RC-5 를 이 호스트에서 재현하고 수리했지만, 이 호스트는 `npm_config_*` 가 비어 있어 원래 결함을 자연 상태로 만들어 내지 못한다. 수리가 windows-latest 러너 이미지의 머신 스코프 `npm_config_prefix` 아래에서 실제로 통하는지는 그 이미지 위에서만 알 수 있다. `01-25` 가 더한 EACCES 픽스처도 마찬가지로 POSIX leg 에서만 실제로 돈다. 같은 run 이 셋 다 관측한다.
Output: 관측된 새 run id 와 세 leg 의 결과, 그리고 그 증거 위에서 처분된 Broken Windows ledger. 초록이면 gap 종료, 빨간불이면 새로운 진단 입력.
</objective>

<execution_context>
@C:/Users/alpha/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safe-operation-boundary/01-VERIFICATION.md
@.planning/phases/01-safe-operation-boundary/01-UAT.md
@.planning/phases/01-safe-operation-boundary/01-VALIDATION.md
@.planning/WINDOWS.md
@.github/workflows/ci.yml
</context>

## Gap being closed

`01-VERIFICATION.md` `gaps[0].missing[0]`, in full and verbatim:

> A new three-OS CI run id in which all three legs are green. That run id is the closing evidence for G-01-1; nothing else is.

The run being replaced is `33984247757` (head `403e017`), `conclusion: failure`:

| Leg | `npm run check` | `npm test` | `build:check` | `Safety boundary suites` |
|-----|-----------------|-----------|---------------|--------------------------|
| ubuntu-latest | success | 202 / 201 / **fail 0** / 1 skipped | success | 125 / 124 / **fail 0** / 1 skipped |
| macos-latest | success | 202 / 201 / **fail 0** / 1 skipped | success | 125 / 124 / **fail 0** / 1 skipped |
| windows-latest | success | **fail 0** | success | 125 / 123 / **fail 1** / 1 skipped — `the test runner environment carries no npm lifecycle injection`, `actual: ['npm_config_prefix']` |

`01-24` repairs the one red cell. This plan is where that repair is either confirmed on the platform that produced it, or disproven.

## What this plan may not do

Carried forward verbatim from `01-23`'s prohibitions, which `01-VERIFICATION.md` confirms were honored under direct pressure to ship, and which are re-declared in this plan's `must_haves.prohibitions`:

1. The gap is not recorded closed on local evidence, on two of three green legs, or on green legs from different runs.
2. A red leg is not closed by removing a suite from `.github/workflows/ci.yml`, marking a test not-run, or relaxing an assertion. Routing the step through the scrubbing runner was the permitted repair and it already happened in `01-24`; anything that shrinks what the step runs is not.
3. A red leg is not explained away as cosmetic because only a meta-test failed.

## Multi-Source Coverage Audit

| Source | Item | Status | Where |
|--------|------|--------|-------|
| GOAL (ROADMAP Phase 1) | the phase goal is only true if it is true on all three supported operating systems | COVERED | Task 2 |
| REQ SAFE-01 | preview leaves every mutation surface byte-identical | COVERED | Task 2 (`preview.test.js` green on all three legs is the roadmap SC1 evidence) |
| REQ SAFE-02 | escape refusal through links, junctions, reparse points, aliases, parent changes | COVERED | Task 2 (`path-boundary.test.js` incl. the new EACCES fixture), Task 3 (the two manual-only canaries, carried with reasons) |
| REQ SAFE-03 | serialized, hash-checked, journaled managed writes | COVERED | Task 2 (`transaction-crash.test.js` is one of the ten named suites and must report green on every leg) |
| REQ SAFE-04 | redaction across every observable surface; bounded shell-free subprocesses | COVERED | Task 2 (`redaction.test.js`, `process.test.js`, `protocol-session.test.js`, `mcp-proxy.test.js`) |
| REQ SAFE-05 | malformed / ambiguous / unsupported documents rejected before mutation | COVERED | Task 2 (`validation.test.js`) |
| REQ SAFE-06 | approved environment names only, bounded output, timeouts | COVERED | Task 2 (`process.test.js` incl. the new probe-answerability assertion), Task 3 (ledger #4) |
| RESEARCH / 01-VALIDATION.md | the full Windows, macOS and Linux CI suite must be green before `/gsd-verify-work` | COVERED | Task 1, Task 2 |
| CONTEXT D-01 | fail closed — an unobserved leg is not a passing leg | COVERED | Task 2 |
| VERIFICATION `gaps[0].missing[0]` | a new three-OS run id with three green legs | COVERED | Task 2 |
| VERIFICATION `behavior_unverified_items[0]` | Windows unknown-reparse canary | COVERED as an explicit carry-forward | Task 3 `<human-check>` |
| VERIFICATION `## Human Verification Required` 3 | file-symlink escape canary, accepted on the privilege gate rather than an observation | COVERED as an explicit carry-forward | Task 3 `<human-check>` |
| VERIFICATION `## Human Verification Required` 4 | `commandProbeEnvironment` on POSIX | COVERED | Task 3 (disposed on the per-leg diagnostic `01-25` prints) |
| VERIFICATION broken-windows cross-check | "#3 darwin floor off-host is now actually resolved by the green macos-latest leg and should be marked fixed" | COVERED | Task 3 |
| VERIFICATION `## Human Verification Required` 5 | judgment-tier prohibitions already accepted by the human in `01-UAT.md` item 4 | COVERED as an explicit no-action | Task 3 (carried as resolved; no action unless those files change, and none of them are in this round's `files_modified`) |

No source item is MISSING. Nothing is deferred silently; every carry-forward above names its reason.

## Flagged Assumptions (spec-less edge probe)

This phase has no `-SPEC.md`. The deterministic edge probe returned **6 applicable, 0 resolved, 6 unresolved** (5 `unclassified`, 1 `concurrency`). Every row is surfaced here as a flagged planner assumption rather than authored into `must_haves.truths`, per the no-silent-drop rule. Same shared ledger as `01-17` through `01-25`.

| # | Requirement | Probe category | Disposition in this plan |
|---|-------------|----------------|--------------------------|
| 1 | SAFE-01 | concurrency | **Flagged, unresolved.** Partially reached: the run-level concurrency hazard this plan owns — evidence stitched from separate runs or from an individually re-run leg — is authored into `must_haves.truths` as a `backstop` predicate. The product's own concurrency surface is out of scope and stays green under ROADMAP SC1 and SC3. |
| 2 | SAFE-02 | unclassified | **Flagged, unresolved.** Observed here rather than derived: the EACCES fixture and the four green escape vectors run on the legs. Two vectors stay manual-only and are named in Task 3 with reasons. |
| 3 | SAFE-03 | unclassified | **Flagged, unresolved.** Observed rather than derived: `transaction-crash.test.js` is one of the ten named suites and its per-leg result is recorded. |
| 4 | SAFE-04 | unclassified | **Flagged, unresolved.** Observed rather than derived: `redaction.test.js` and the two protocol suites are among the ten. |
| 5 | SAFE-05 | unclassified | **Flagged, unresolved.** Observed rather than derived: `validation.test.js` is among the ten. |
| 6 | SAFE-06 | unclassified | **Flagged, unresolved as an automatic classification.** Live here: `process.test.js` carries both the runtime floor exact-match and the new probe-answerability assertion, and their per-leg results are recorded. |

**No-silent-drop equality:** 6 probe-surfaced edge items == 1 authored into `must_haves.truths` (row 1, as a `backstop` predicate) + 6 surfaced as flagged assumptions above (row 1 appears in both because it is only partially reached). 5 prohibitions kept at the precision stage and authored descriptor-less into `must_haves.prohibitions`. Canon-referral: CI credential handling and workflow permissions are canon (`/gsd-secure-phase`); the workflow already declares `permissions: contents: read` and no prohibition is minted for it here.

## Artifacts this phase produces

| Symbol | Kind | File | Exported |
|--------|------|------|----------|
| the observed run id and per-leg conclusions | recorded evidence | `01-26-SUMMARY.md` | n/a |
| ledger dispositions for entries 1-4 | ledger rows | `.planning/WINDOWS.md` | n/a |

No source file, test file or workflow file is modified by this plan. `files_modified` names only the ledger.

<tasks>

<task type="auto">
  <name>Task 1: push 하기 전에 CI 와 동등한 네 스텝 + 주입된 환경까지 로컬에서 확정한다</name>
  <files>(no files modified — this task runs commands and records results)</files>
  <read_first>
    .github/workflows/ci.yml
    .planning/phases/01-safe-operation-boundary/01-24-SUMMARY.md
    .planning/phases/01-safe-operation-boundary/01-25-SUMMARY.md
    .planning/phases/01-safe-operation-boundary/01-22-SUMMARY.md
  </read_first>
  <precondition>Both `01-24-SUMMARY.md` and `01-25-SUMMARY.md` exist and record a completed plan. This plan observes their combined effect on three operating systems and has nothing to observe otherwise — halt and report if either is missing.</precondition>
  <reversibility rating="reversible">This task runs commands and records numbers; it changes no file and can be re-run.</reversibility>
  <action>
CI 워크플로가 정의한 네 스텝을 이 호스트에서 같은 순서로 그대로 돌린다. 순서를 바꾸거나 스텝을 건너뛰지 않는다 — `npm run build:check` 가 `npm test` 뒤에 오는 것은 `npm test` 가 방금 만든 아티팩트를 검증하기 위해서다.

  1. `npm run check`
  2. `npm test`
  3. `npm run build:check`
  4. `.github/workflows/ci.yml` 의 `Safety boundary suites` 스텝에 적힌 명령을 워크플로에서 **읽어 와서** 그대로 실행한다. `01-24` 이후 그 명령은 `node scripts/run-tests.mjs --files` 로 시작해 10개 파일을 나열하는 형태다. 명령과 파일 목록을 기억에 의존해 다시 타이핑하지 않는다 — 로컬에서 실행한 것이 CI 가 실행할 것과 같다는 보장이 그 읽기다.

그리고 **주입 상태에서 한 번 더** 돌린다. windows-latest 러너 이미지가 머신 스코프로 하는 일을 흉내 내어, `npm_config_prefix` 를 환경에 넣은 채 위 4번의 명령을 다시 실행한다. `fail 0` 이어야 한다. 이것이 이 호스트에서 얻을 수 있는, RC-5 수리에 대한 가장 강한 증거다. 그리고 그것으로도 충분하지 않다는 것이 Task 2 의 존재 이유다.

기록할 것: 각 스텝의 exit code, `npm test` 의 `tests`/`pass`/`fail`/`skipped` 수, 10-파일 실행의 같은 수치(주입 없음과 주입 있음 양쪽), 그리고 `preview.test.js` 가 진단으로 출력하는 wrapper not-run 목록과 `path-boundary.test.js` 의 not-run 목록, `process.test.js` 의 per-command 진단 한 줄.

**기준선 산술.** `01-22-SUMMARY.md` 가 기록한 202 를 기준선으로 삼는다. `01-24` 는 테스트 2개, `01-25` 는 4개(EACCES 3 + probe 1)를 더했으므로 합계 **+6**, 즉 최소 208 이어야 한다. 각 SUMMARY 가 기록한 총수를 순서대로 늘어놓아 증가폭이 실제로 그렇게 나뉘는지 확인한다. 사슬 어딘가가 어긋나면 그 plan 의 테스트가 실행되지 않고 있는 것이므로 push 하지 말고 그것부터 찾는다.

한 스텝이라도 빨간불이면 여기서 멈춘다. Task 2 로 넘어가지 않는다 — 로컬에서 빨간 것을 3-OS 매트릭스가 고쳐 주지 않는다.
  </action>
  <acceptance_criteria>
    - All four steps exit 0 on this host, and the fourth was run from the command read out of `.github/workflows/ci.yml` rather than retyped
    - The ten-file invocation also reports fail 0 when `npm_config_prefix` is injected into the environment
    - The recorded `npm test` total is at least 208, and the per-plan chain adds up: `01-24` +2 and `01-25` +4, each read from that plan's own SUMMARY
    - The `preview.test.js` wrapper not-run list, the `path-boundary.test.js` not-run list, and the `process.test.js` per-command diagnostic line are each recorded verbatim, including the entries that say a fixture did not run on this host
    - No step was skipped and no step order was changed
  </acceptance_criteria>
  <verify>
    <automated>npm run check &amp;&amp; npm test &amp;&amp; npm run build:check</automated>
    <fails_when>non-zero exit, a `fail` count other than 0 in the `npm test` summary, a reported test total below 208, or the line `build artifact verified` absent from the output</fails_when>
    <automated>npm_config_prefix=/tmp/alpha-aos-rc5-probe INIT_CWD="$PWD" node scripts/run-tests.mjs --files dist/test/preview.test.js dist/test/path-boundary.test.js dist/test/transaction-crash.test.js dist/test/redaction.test.js dist/test/validation.test.js dist/test/process.test.js dist/test/protocol-session.test.js dist/test/mcp-proxy.test.js dist/test/install.test.js dist/test/update.test.js</automated>
    <fails_when>non-zero exit, or a `fail` count other than 0 in the summary line — the routed step seeing the injected setting means RC-5 is still open</fails_when>
  </verify>
  <done>The four CI steps are green on this host, they are green again under the injection that turned windows-latest red, and their numbers are recorded — so a red leg in CI can be attributed to a platform rather than to an untested change.</done>
</task>

<task type="auto">
  <name>Task 2: 새 3-OS 매트릭스 실행을 끝까지 관측하고 그 run id 를 종료 증거로 기록한다</name>
  <files>(no files modified — this task checks the workflow contract and states the human observation)</files>
  <read_first>
    .github/workflows/ci.yml
    .planning/phases/01-safe-operation-boundary/01-VERIFICATION.md
    .planning/phases/01-safe-operation-boundary/01-VALIDATION.md
  </read_first>
  <precondition>`gh auth status` succeeds and the repository's origin remote is `4lph4-dvlp/alpha-AOS`. The run must be read back through the GitHub API — a run inferred from a local push, or quoted from a previous session, is not an observation.</precondition>
  <reversibility rating="costly">The push publishes commits to `main`. Rewriting published history on a shared branch is possible but expensive, so the local gate in Task 1 runs first and this task does not start on a red local result.</reversibility>
  <action>
`workflow.human_verify_mode` 가 `end-of-phase` 이므로 이 관측은 mid-flight checkpoint 로 멈추지 않는다. 이 task 는 기계가 확인할 수 있는 부분을 확인하고, 사람만 할 수 있는 부분을 `<human-check>` 로 남겨 phase 종료 시 `01-UAT.md` 로 수확되게 한다. 원래의 `G-01-1` 도 정확히 그 경로로 표면화됐다.

**기계가 확인하는 것.** `.github/workflows/ci.yml` 이 세 leg, 네 스텝, 10개 스위트 파일 이름을 모두 담고 있고, `Safety boundary suites` 스텝이 `scripts/run-tests.mjs` 와 `--files` 를 통해 도달하는지 확인한다. 하나라도 빠지면 매트릭스가 관측하겠다고 약속한 표면이 줄어든 것이므로 실패시킨다. 이 확인은 `<verify>` 의 첫 automated 커맨드가 수행한다.

**사람이 관측하는 것.** push 하고, 매트릭스 실행을 끝까지 보고, run id 와 세 leg 의 결과를 기록한다. 아래 `<human-check>` 에 절차가 있다.

**leg 마다 확인할 것:**

  - `npm run check` — exit 0
  - `npm test` — `fail 0`, 총수 208 이상
  - `npm run build:check` — `build artifact verified`
  - `Safety boundary suites` — `fail 0`, 그리고 10개 파일이 모두 실행됐는지. 스텝 총수가 이전 run 의 125 보다 **작으면** 스위트 하나가 조용히 빠진 것이므로 더 빠른 결과가 아니라 빨간 결과로 취급한다
  - windows-latest 의 `Safety boundary suites` 안에서 `the test runner environment carries no npm lifecycle injection` 이 **통과로 보고되는지**. 그 줄이 아예 없으면 통과가 아니라 스위트가 안 돈 것이다 — 이 구분이 이 run 의 핵심이다
  - `wrapper families executed: 4; not-run: []` 가 세 leg 모두에서 유지되는지
  - ubuntu-latest 와 macos-latest 에서 `preview.test.js` 의 wrapper not-run 목록이 비어 있는지
  - `Validate POSIX wrapper syntax` / `Validate PowerShell wrapper syntax` — exit 0

**이번 run 이 새로 답할 수 있는 두 가지 (Task 3 의 입력):**

  1. `01-25` 의 EACCES 픽스처가 ubuntu-latest 와 macos-latest 에서 **실제로 실행됐는지**, 아니면 not-run 으로 보고했는지. `path-boundary.test.js` 의 not-run 진단 줄을 세 leg 모두에서 그대로 옮겨 적는다. 실행돼 초록이면 Broken Window #2 가 닫힐 근거가 생긴 것이고, 세 leg 모두 not-run 이면 근거가 없는 것이다.
  2. `01-25` 의 per-command 진단이 세 leg 에서 무엇을 보고하는지. `process.test.js` 의 그 한 줄을 leg 마다 옮겨 적는다. 어떤 leg 에서든 찾은 명령의 version 과 unsupportedReason 이 둘 다 null 이면 그것이 Broken Window #4 가 말한 조건이며, 넓혀야 할 것은 pin 이 아니라 passthrough 다.

**빨간 leg 를 만났을 때.** 빨간 leg 는 제품에 대한 새 정보다. CI 파일에서 스위트를 빼거나, 테스트를 not-run 으로 표시하거나, 단언을 완화해 초록으로 만드는 것은 금지된다. "메타 테스트만 실패했으니 표면적이다" 도 금지된다 — RC-3 이 정확히 그 추론으로 한 주기 내내 진짜 SAFE-01 위반을 숨겼다. 실패 텍스트와 leg 이름과 run id 를 기록하고 진단으로 되돌아간다.

**기록할 것:** 관측한 run id 와 URL, head sha, 세 leg 각각의 결론과 네 스텝의 수치, 그리고 위 두 입력. 이 값들이 `G-01-1` 을 닫는 증거이며 요약에 그대로 옮겨 적는다. 세 leg 이 한 run 안에서 모두 초록이 아니면 gap 은 열린 채로 기록한다.
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` still names all three legs, all four steps, and all ten Phase 1 suite files, and its `Safety boundary suites` step reaches them through `scripts/run-tests.mjs --files`
    - The human observation procedure is present in this task's `<verify><human-check>` so the end-of-phase verifier harvests it into `01-UAT.md`
    - The recorded evidence is one run id whose three legs are green in that same run, with head sha and URL — not a set of green legs assembled from different runs and not an individually re-run leg
    - The windows-latest `Safety boundary suites` output is recorded as containing `the test runner environment carries no npm lifecycle injection` reported as passing, not merely as absent
    - Each leg's `Safety boundary suites` total is recorded and is at least the 125 of run 33984247757
    - The `path-boundary.test.js` not-run diagnostic and the `process.test.js` per-command diagnostic are transcribed per leg
    - If any leg is red, the summary records the leg, the exact failing test names and the run id, and states the gap as open — no suite is removed, no test is marked not-run, no assertion is relaxed
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('node:fs');const s=fs.readFileSync('.github/workflows/ci.yml','utf8');const need=['ubuntu-latest','macos-latest','windows-latest','npm run check','npm test','npm run build:check','Safety boundary suites','scripts/run-tests.mjs','--files','dist/test/preview.test.js','dist/test/path-boundary.test.js','dist/test/transaction-crash.test.js','dist/test/redaction.test.js','dist/test/validation.test.js','dist/test/process.test.js','dist/test/protocol-session.test.js','dist/test/mcp-proxy.test.js','dist/test/install.test.js','dist/test/update.test.js'];const miss=need.filter(n=>!s.includes(n));if(miss.length){console.error('ci matrix contract missing: '+miss.join(', '));process.exit(1);}console.log('ci matrix contract intact: 3 legs, 4 steps, 10 suites, routed through the runner');"</automated>
    <fails_when>non-zero exit, or the line `ci matrix contract intact: 3 legs, 4 steps, 10 suites, routed through the runner` absent from the output</fails_when>
    <human-check>Push the branch, watch the three-OS matrix run to completion, and record the run id, URL and head sha from `gh run view &lt;id&gt; --json conclusion,headSha,jobs`. Confirm for every leg: `npm run check` exit 0, `npm test` fail 0 with a total of at least 208, `npm run build:check` prints `build artifact verified`, and `Safety boundary suites` reports fail 0 with a total no lower than 125 and all ten named files actually executed. On windows-latest specifically, confirm `the test runner environment carries no npm lifecycle injection` appears in the `Safety boundary suites` output as a pass — an absent line is a suite that did not run, not a pass. Confirm `wrapper families executed: 4; not-run: []` on all three legs and an empty wrapper not-run list on ubuntu-latest and macos-latest. Transcribe, per leg, the `path-boundary.test.js` not-run diagnostic and the `process.test.js` per-command diagnostic — Task 3 disposes the ledger on those two lines. The closing evidence is ONE run id whose three legs are all green in that run; do not assemble it from separate runs or from an individually re-run leg. If any leg is red, record the leg name, the exact failing test names and the run id, and return to diagnosis: do not remove a suite from the workflow, mark a test as not-run, relax an assertion, or treat a failing meta-test as cosmetic.</human-check>
  </verify>
  <done>The workflow still promises the full matrix surface and routes the safety step through the scrubbing runner, and the three-OS observation is queued as an end-of-phase human item with an explicit red-leg protocol and an explicit single-run-id rule. Because this task is autonomous, that observation does NOT exist when Task 3 begins unless a human has already performed it and recorded the per-leg lines; Task 3's first step is to determine which of those two states it is in, and the summary states which state this execution ran under.</done>
</task>

<task type="auto">
  <name>Task 3: 관측된 증거 위에서 Broken Windows ledger 를 처분하고, 닫히지 않은 것을 닫히지 않은 채로 기록한다</name>
  <files>.planning/WINDOWS.md</files>
  <read_first>
    .planning/WINDOWS.md
    .planning/phases/01-safe-operation-boundary/01-VERIFICATION.md
    .planning/phases/01-safe-operation-boundary/01-UAT.md
    .planning/phases/01-safe-operation-boundary/01-VALIDATION.md
  </read_first>
  <precondition>Determine, before disposing anything, which of three states this execution is in, and record the answer in the summary. Task 2 is autonomous and its three-OS observation is a `&lt;human-check&gt;` harvested at end of phase, so under ordinary autonomous execution state (a) is the expected one — not a fault.
  **(a) NOT YET OBSERVED** — no run id and no per-leg lines exist, because Task 2's `&lt;human-check&gt;` has not been performed. Evidence-dependent entries (#2, #3, #4) stay open; only #1 is disposed.
  **(b) OBSERVED, ALL THREE LEGS GREEN** — a run id and per-leg lines exist in Task 2's record or in `01-UAT.md`. Dispose #2, #3 and #4 against those lines.
  **(c) OBSERVED, A LEG RED** — a run id exists and at least one leg failed. Evidence-dependent entries stay open, the red leg and its failing test names are recorded, and nothing is disposed on the strength of a run that did not go green.
  Do not infer state (b) from a local green run, from a push having happened, or from a run quoted in a previous session. If no run id was read back through the GitHub API in this execution or written into Task 2's record, the state is (a).</precondition>
  <reversibility rating="reversible">Ledger entries are recorded state with an explicit tool for both directions; a wrong disposition is corrected by re-running the tool with the right verdict.</reversibility>
  <action>
`.planning/WINDOWS.md` 는 open 4, waived 0 이다. 네 항목을 **각각 관측된 증거에 대고** 처분한다. 증거가 없으면 열린 채로 둔다 — 열린 항목을 닫는 것보다 근거 없이 닫는 것이 이 phase 에 더 비싸다. 처분은 ledger 를 직접 편집하지 않고 `gsd-tools` 의 `windows fixed <id>` / `windows waive <id> "<reason>"` 서브커맨드로 수행한다. 이 파일에는 표와 JSON 블록이 함께 있어 손으로 고치면 둘이 어긋난다.

**먼저 상태 (a)/(b)/(c) 를 판정한다** (위 `<precondition>`). 항목 #1 만이 leg 증거가 아니라 기록에 대고 결정되므로 세 상태 모두에서 처분된다. #2, #3, #4 는 상태 (b) 에서만 처분된다.

**상태 (a) — 아직 관측되지 않음 (자율 실행의 기본 경로).** 이것은 결함이 아니라 `human_verify_mode: end-of-phase` 의 정상 동작이다. 이 상태에서 하는 일:

  - `windows waive 1` 을 아래 #1 의 이유와 함께 실행한다. 이 항목은 leg 증거를 필요로 하지 않는다.
  - #2, #3, #4 는 **손대지 않는다.** ledger 에서 open 으로 남고, 요약에는 각각 "어떤 관측을 기다리는지" 를 한 줄씩 적는다 (#3 은 macos-latest leg 의 초록, #2 는 POSIX leg 의 `path-boundary.test.js` not-run 진단, #4 는 세 leg 의 `process.test.js` per-command 진단).
  - 요약에 `G-01-1` 을 **열린 채로** 기록하고, 닫히려면 Task 2 의 `<human-check>` 가 수행돼야 한다고 적는다. 로컬 초록이나 push 사실을 관측으로 승격시키지 않는다.
  - 아래 `<verify>` 의 `<human-check>` 가 사후 처분 패스를 소유한다: 사람이 run 을 관측한 뒤 돌아와 상태 (b) 또는 (c) 의 규칙으로 #2/#3/#4 를 처분한다.

**상태 (c) — 관측했고 leg 이 빨감.** #1 만 처분하고, #2/#3/#4 는 열린 채로 두며, 빨간 leg 이름·실패한 테스트 이름·run id 를 요약에 적는다. 빨간 leg 는 제품에 대한 새 정보이므로 진단으로 되돌아간다.

**상태 (b) — 관측했고 세 leg 모두 초록.** 아래 네 문단의 규칙대로 처분한다.

**#3 — darwin floor off-host (`test/process.test.ts`).** `01-VERIFICATION.md` 가 명시적으로 "이제 실제로 해결됐고 fixed 로 표시돼야 한다" 고 적었다. 근거: `the declared platform floor must match what the OS actually delivers` 는 exact `deepEqual` 이고, macos-latest leg 에서 통과했다. 과다 선언이든 과소 선언이든 그 leg 을 빨갛게 만들었을 것이므로 통과가 곧 정확성이다. Task 2 의 이번 run 에서도 macos-latest 가 초록이면 `windows fixed 3` 을 실행한다. 이번 run 의 macos leg 이 빨갛다면 열린 채로 둔다.

**#2 — EACCES `unprovable-filesystem` 분기 (`src/core/path-boundary.ts`).** Task 2 가 옮겨 적은 `path-boundary.test.js` 의 not-run 진단을 읽는다. ubuntu-latest 또는 macos-latest 중 **최소 한 leg 에서 세 픽스처가 not-run 으로 보고되지 않고 실제로 실행돼 초록**이면 `windows fixed 2` 를 실행한다. 세 leg 모두 not-run 이면 열린 채로 두고, 설명을 요약에 적는다: 픽스처는 존재하지만 어떤 러너도 그 조건을 만들지 못했으므로 분기는 여전히 관측되지 않았다. not-run 을 통과로 읽지 않는다.

**#4 — `commandProbeEnvironment` passthrough Windows 전용 (`src/core/process.ts`).** Task 2 가 옮겨 적은 `process.test.js` 의 per-command 진단을 세 leg 에서 읽는다. 세 leg 모두에서 `a version probe answers under the probe environment on this platform` 이 초록이고, 찾은 명령 중 version 과 unsupportedReason 이 둘 다 null 인 것이 없으면 `windows fixed 4` 를 실행한다. 어느 leg 에서든 조용한 null 이 있으면 열린 채로 두고, 어느 명령이 어느 플랫폼에서 그랬는지 기록한다 — 그것이 다음 plan 이 passthrough 를 넓힐 때 쓸 정확한 입력이다.

**#1 — 01-19 fixture 4 disclosure (`test/path-boundary.test.ts`).** 이것은 제품 결함이 아니라 계획이 예측한 reproducer 가 실제로는 두 번째 negative control 이었다는 정직한 공개다. 고칠 것이 없다. **이 항목만은 leg 증거가 아니라 기록에 대고 결정되므로 상태 (a), (b), (c) 모두에서 처분된다** — 그리고 아래 `<verify>` 의 두 번째 automated 커맨드가 "처분된 항목이 최소 하나" 를 요구하는 것을 만족시키는 것도 바로 이 항목이다. 그 커맨드는 다른 항목을 근거 없이 닫으라는 압력이 아니다. `windows waive 1` 을 이유와 함께 실행한다 — 이유에는 이것이 예측 기록이지 결함이 아니라는 것, `01-VERIFICATION.md` 가 이 공개를 나머지 기록을 믿을 만하게 만드는 것으로 평가했다는 것, 그리고 그 픽스처가 지금도 실행되며 초록이라는 것을 적는다. 조용히 fixed 로 바꾸지 않는다 — waive 는 이유를 요구하고, 그 이유가 이 항목의 요점이다.

**닫히지 않는 것을 닫히지 않은 채로 기록한다.** 아래 두 항목은 이 run 이 어떤 결과를 내든 해소되지 않는다. 요약에 그대로 옮겨 적고, ledger 에 새 항목으로 넣지 않는다 — 이미 `01-VERIFICATION.md` 와 `01-UAT.md` 가 소유하고 있으며 중복 기재는 두 기록을 갈라지게 한다.

  1. **Windows unknown-reparse 카나리 (SAFE-02).** `01-VALIDATION.md` 가 이 phase 유일의 manual-only verification 으로 지정했고, `01-UAT.md` 항목 2 는 `result: skipped` 다. `fsutil reparsepoint` 권한이 이 호스트에도 러너에도 없고, POSIX leg 은 이 개념을 Windows 전용으로 올바르게 보고하므로 어떤 CI leg 도 증거를 낼 수 없다. 초록 run 이 이것을 승격시키지 않는다.
  2. **file-symlink escape 카나리 (SAFE-02).** `01-UAT.md` 항목 3 은 사람이 `pass` 로 수용했지만, 수용된 것은 관측이 아니라 권한 게이트라는 주석이 함께 달려 있다. 권한 상승 셸이나 Developer Mode 에서 재실행해야 실제 관측으로 승격된다.

`01-UAT.md` 항목 4 의 judgment-tier prohibition 5개는 사람이 소스 근거와 함께 이미 승인했고, 이 라운드의 `files_modified` 에 그 파일들이 하나도 없으므로 다시 볼 일이 없다. 이 사실을 요약에 한 줄로 남긴다.
  </action>
  <acceptance_criteria>
    - The summary names which of the three states — (a) not yet observed, (b) observed and all green, (c) observed and a leg red — this execution ran under, and says how it was determined
    - Every ledger disposition of #2, #3 or #4 names the observed per-leg line it was decided on, quoted in the summary
    - `.planning/WINDOWS.md` was changed through the `gsd-tools windows` subcommands, not by hand, so its table and its JSON block agree
    - In state (a), entries #2, #3 and #4 are left open and untouched, and the summary records for each one exactly which observation it is waiting on; `G-01-1` is recorded as open, and no local green run or completed push is treated as the observation
    - In state (c), entries #2, #3 and #4 are left open, and the red leg name, the exact failing test names and the run id are recorded
    - Entry #3 is marked fixed if and only if the state is (b) and this run's macos-latest leg is green
    - Entry #2 is marked fixed if and only if the state is (b) and at least one POSIX leg reports the three new EACCES fixtures as executed rather than not-run
    - Entry #4 is marked fixed if and only if the state is (b) and all three legs report the probe assertion green with no found command carrying both a null version and a null reason
    - Entry #1 is waived with a reason stating it is a prediction disclosure rather than a defect, in every state, and is not silently marked fixed. It is the entry that satisfies the "at least one disposed" verify gate, so that gate never applies pressure to close an evidence-dependent entry
    - The Windows unknown-reparse canary and the file-symlink escape canary are each recorded in the summary as still unresolved with their reasons, and neither is added to the ledger as a new entry
    - `open_count`, `waived_count`, `fixed_count` and `total_count` in the frontmatter still sum consistently after the changes
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('node:fs');const t=fs.readFileSync('.planning/WINDOWS.md','utf8');const m=t.match(/open_count:\s*(\d+)[\s\S]*?waived_count:\s*(\d+)[\s\S]*?fixed_count:\s*(\d+)[\s\S]*?total_count:\s*(\d+)/);if(!m){console.error('ledger counters not found');process.exit(1);}const[o,w,f,tot]=[m[1],m[2],m[3],m[4]].map(Number);if(o+w+f!==tot){console.error('ledger counters disagree: '+o+'+'+w+'+'+f+' != '+tot);process.exit(1);}console.log('ledger counters consistent: open '+o+', waived '+w+', fixed '+f+', total '+tot);"</automated>
    <fails_when>non-zero exit, or the line `ledger counters consistent:` absent from the output</fails_when>
    <automated>node -e "const t=require('node:fs').readFileSync('.planning/WINDOWS.md','utf8');const j=JSON.parse(t.slice(t.indexOf('[',t.indexOf('json')),t.lastIndexOf(']')+1));const rows=j.filter(r=>r.status!=='open');if(rows.length===0){console.error('no ledger entry was disposed');process.exit(1);}for(const r of rows){if(r.status==='waived'&amp;&amp;!String(r.reason||'').trim()){console.error('waived entry '+r.id+' carries no reason');process.exit(1);}}console.log('disposed ledger entries: '+rows.map(r=>r.id+'='+r.status).join(', '));"</automated>
    <fails_when>non-zero exit, or the line `disposed ledger entries:` absent from the output — entry #1 is waived with a reason in every state, which is what this gate checks; it does NOT require #2, #3 or #4 to be disposed, because those wait on evidence that may not exist yet</fails_when>
    <human-check>If this task ran in state (a) — Task 2's three-OS observation had not been performed yet, which is the normal autonomous path — this is the deferred disposition pass. After observing the run per Task 2's `&lt;human-check&gt;`, return to `.planning/WINDOWS.md` and dispose entries #2, #3 and #4 against the transcribed per-leg lines using `gsd-tools windows fixed &lt;id&gt;`: #3 on a green macos-latest leg, #2 on at least one POSIX leg reporting the three EACCES fixtures as executed rather than not-run, #4 on all three legs reporting the probe assertion green with no found command carrying both a null version and a null reason. Any entry whose line does not say so stays open. If a leg was red, dispose nothing and record the leg, the failing test names and the run id instead. Entry #1 was already waived and is not revisited.</human-check>
    <human-check>Confirm the two SAFE-02 canaries stay unresolved and were not promoted by a green run: the Windows unknown-reparse canary still requires a privileged host with `fsutil reparsepoint`, and the file-symlink escape canary still requires an elevated shell or Developer Mode to become a real observation rather than an accepted privilege gate. If a privileged Windows host is available, run `node --test dist/test/path-boundary.test.js` there and report whether `an unclassified reparse point yields a stable unsupported refusal` and `a file link that escapes the allowed root is refused for both write and removal` executed instead of reporting not-run.</human-check>
  </verify>
  <done>Every evidence-dependent ledger entry is either disposed against a line someone actually read off a leg or left open with the observation it is waiting on named, the state this execution ran under is on the record, the two manual-only canaries are recorded as still open with their reasons, and nothing was closed because a run happened to be green or because a push happened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer host → supported operating systems | RC-5 is invisible on this host by construction: `npm_config_*` is empty here, so anything asserted only here is an assumption about the platform that produced the defect. |
| CI configuration → what is actually executed | A suite removed from the named list, a step that stops early, or a test that is absent rather than passing all read as an absence unless someone checks for presence. |
| run identity → closing evidence | Green legs from different runs, or an individually re-run leg, describe a state no single commit was ever in. |
| observation → ledger | A ledger entry marked fixed without a leg that produced the evidence converts an open question into a settled claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-26-01 | Repudiation | recording G-01-1 closed on local or partial evidence | high | mitigate | Task 2's `<human-check>` requires one run id with three green legs and is harvested into `01-UAT.md`; the rule is restated in `must_haves.prohibitions` and in the acceptance criteria. |
| T-01-26-02 | Tampering | making a red leg green by shrinking what CI runs | high | mitigate | Task 2 machine-checks the workflow contract before the push and requires the ten named files to be confirmed as executed; the per-leg step total is compared against the previous run's 125 so a smaller total reads as red. |
| T-01-26-03 | Spoofing | a test that is absent being read as a test that passed | high | mitigate | Task 2 requires the windows-latest step output to contain the invariant test reported as passing, and states explicitly that an absent line is a suite that did not run. |
| T-01-26-04 | Repudiation | closing a Broken Window on a not-run fixture | medium | mitigate | Task 3 conditions each disposition on a quoted per-leg diagnostic line and leaves the entry open when every leg reported not-run. |
| T-01-26-07 | Repudiation | an autonomous run disposing entries against an observation that has not happened yet | high | mitigate | Task 3's `<precondition>` forces an explicit state (a)/(b)/(c) determination and records it; only entry #1, decided on the written record rather than on a leg, is disposed in state (a). The `at least one disposed` verify gate is satisfied by #1 alone, so it cannot pressure an evidence-dependent entry closed, and a deferred `<human-check>` owns the post-observation disposition pass. |
| T-01-26-05 | Repudiation | a green automated run upgrading a manual-only verification | medium | mitigate | Task 3 records both SAFE-02 canaries as still unresolved with reasons and forbids promoting them; the rule is also a declared prohibition. |
| T-01-26-06 | Tampering | hand-editing the ledger so its table and its JSON block disagree | low | mitigate | Task 3 requires the `gsd-tools windows` subcommands and the second verify command parses the JSON block and checks counter consistency. |
| T-01-26-SC | Tampering | npm / pip / cargo installs | low | accept | This plan adds no dependency and runs no package-manager install task. `npm ci` in CI installs the existing lockfile only. No new legitimacy checkpoint is required. |
</threat_model>

<verification>
- The four CI steps are green locally, and green again under an injected `npm_config_prefix`, with their numbers recorded (Task 1).
- The baseline arithmetic holds: 202 + 2 + 4 = at least 208, each addend read from its own SUMMARY (Task 1).
- A NEW three-OS CI run is observed to completion, its id, URL and head sha recorded, and every leg green across all four steps (Task 2).
- The windows-latest `Safety boundary suites` output contains the runner-environment invariant reported as passing (Task 2).
- Every leg's step total is at least 125 and `wrapper families executed: 4; not-run: []` holds on all three (Task 2).
- The per-leg `path-boundary.test.js` not-run diagnostic and `process.test.js` per-command diagnostic are transcribed (Task 2) and each ledger disposition quotes the line it rests on (Task 3).
- Task 3 records which of the three states it ran under, and in state (a) leaves #2/#3/#4 open with their awaited observations named rather than disposing them (Task 3).
- The two manual-only SAFE-02 canaries are recorded as still unresolved with reasons (Task 3).
</verification>

<success_criteria>
1. Gap `G-01-1` is closed by an observed CI run id with three green legs in one run, not by local evidence and not by two of three.
2. Every one of the ten named Phase 1 suites is confirmed to have executed on every leg, with a step total no lower than the previous run's 125.
3. `the test runner environment carries no npm lifecycle injection` is seen passing inside the windows-latest `Safety boundary suites` step.
4. Each Broken Window is disposed against a line read off a leg; one that no leg could produce stays open, and so does one whose leg has not been observed yet — with the difference between those two states stated rather than blurred.
5. No red leg was closed by removing a suite, skipping a test, relaxing an assertion, or calling a failing meta-test cosmetic.
</success_criteria>

<output>
Create `.planning/phases/01-safe-operation-boundary/01-26-SUMMARY.md` when done.
</output>
