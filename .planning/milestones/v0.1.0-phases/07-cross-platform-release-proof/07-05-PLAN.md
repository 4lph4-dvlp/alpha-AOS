---
phase: 07-cross-platform-release-proof
plan: 05
type: gap_closure
wave: 1
depends_on: []
files_modified:
  - src/core/support-matrix.ts
  - test/support-matrix.test.ts
  - test/release-controls.test.ts
  - docs/SUPPORT_MATRIX.md
  - docs/RELEASE_CONTROLS.md
autonomous: true
gap_closure: true
requirements: [REL-02, REL-03]

must_haves:
  truths:
    - "src/core/support-matrix.ts represents Antigravity real-host discovery and invocation receipts without hardcoded exclusions (REL-02, D-05)."
    - "Live support matrix evaluation promotes cells to PROVEN when valid inspectable receipts exist in the capability ledger and keeps unverified cells honest (REL-02, D-06)."
    - "Optional capability invocation control executes real native Context7 intent lookup paired with an ordinary-task negative control that records zero Context7 calls (REL-03, D-07)."
    - "docs/RELEASE_CONTROLS.md and docs/SUPPORT_MATRIX.md record inspectable, credential-free SHA-256 evidence digests from real executions (REL-02, REL-03)."
  prohibitions:
    - statement: "MUST NOT hardcode harness exclusions in matchingReceipt that prevent valid capability receipts from being evaluated (REL-02)."
      category: correctness
      status: resolved
      verification: automated
    - statement: "MUST NOT use synthetic in-memory McpObservation arrays as proof of real native optional capability invocation (REL-03)."
      category: correctness
      status: resolved
      verification: automated
  artifacts:
    - path: "src/core/support-matrix.ts"
      provides: "Support matrix evaluator with full harness receipt matching including Antigravity"
      exports: ["evaluateMatrixCell", "evaluateSupportMatrix", "renderSupportMatrixMarkdown"]
    - path: "test/release-controls.test.ts"
      provides: "Paired acceptance controls with real native optional capability invocation and negative controls"
    - path: "docs/SUPPORT_MATRIX.md"
      provides: "Versioned support matrix reflecting inspectable host capability evidence"
    - path: "docs/RELEASE_CONTROLS.md"
      provides: "Inspectable receipt hashes from real paired positive and negative controls"
---

<objective>
Close Gap 2 (REL-02) and Gap 3 (REL-03) identified in 07-VERIFICATION.md:
1. Extend the support matrix capability evidence model so Antigravity real-host discovery and invocation receipts are representable in `src/core/support-matrix.ts`.
2. Replace synthetic in-memory MCP observations in `test/release-controls.test.ts` with real native Context7 invocation and an ordinary-task negative control.
3. Update `docs/SUPPORT_MATRIX.md` and `docs/RELEASE_CONTROLS.md` with genuine, credential-free evidence digests.
</objective>

<execution_context>
Execution via GSD workflow adhering to anti-shallow deep work rules.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-cross-platform-release-proof/07-CONTEXT.md
@.planning/phases/07-cross-platform-release-proof/07-VERIFICATION.md
@src/core/support-matrix.ts
@src/core/capability-ledger.ts
@test/support-matrix.test.ts
@test/release-controls.test.ts
@docs/SUPPORT_MATRIX.md
@docs/RELEASE_CONTROLS.md
</context>

<interfaces>
```typescript
// Support matrix receipt matching contract in src/core/support-matrix.ts
export interface SupportReceiptReference {
  readonly reference: string;
  readonly capability: string;
  readonly observedAt: string;
  readonly harnessVersion: string | null;
  readonly oracleCommand: string;
}

export function evaluateMatrixCell(
  entry: SupportMatrixEntry,
  inventory: Inventory,
  ledger: CapabilityLedger,
  options?: Pick<SupportMatrixEvaluationOptions, "ledgerUnavailableReason">,
): EvaluatedMatrixCell;
```
</interfaces>

<tasks>

<task type="auto" id="07-05-01">
  <name>Task 1: Enable Antigravity Receipt Representation & Evidence-Driven Support Promotion (REL-02, D-05, D-06)</name>
  <read_first>
    src/core/support-matrix.ts
    src/core/capability-ledger.ts
    test/support-matrix.test.ts
    docs/SUPPORT_MATRIX.md
    .planning/phases/07-cross-platform-release-proof/07-VERIFICATION.md
  </read_first>
  <files>src/core/support-matrix.ts, test/support-matrix.test.ts, docs/SUPPORT_MATRIX.md</files>
  <behavior>
    - `matchingReceipt` in `src/core/support-matrix.ts` does not exclude `antigravity`; it finds inspectable capability proofs where `proof.harness === entry.harnessId` and `proof.capability === entry.receiptCapability`.
    - `evaluateMatrixCell` promotes Antigravity cells (such as GSD Core and Unified Memory) from UNVERIFIED to PROVEN when a valid inspectable proof exists in the capability ledger and the harness is detected in inventory.
    - If no inspectable proof exists or the harness is undetected, the cell remains UNVERIFIED or UNSUPPORTED, maintaining fail-closed honesty.
    - `test/support-matrix.test.ts` verifies that Antigravity proofs in the ledger promote matching Antigravity cells to PROVEN, while unproven cells remain UNVERIFIED.
    - `docs/SUPPORT_MATRIX.md` is updated to reflect the evaluated status backed by genuine capability receipts.
  </behavior>
  <action>
    1. In `src/core/support-matrix.ts`:
       - Remove `|| entry.harnessId === "antigravity"` from `matchingReceipt` guard.
       - Ensure `matchingReceipt` correctly queries `ledger.proofs` for `proof.harness === entry.harnessId` and `proof.capability === entry.receiptCapability` using `receiptIsInspectable(proof)`.
       - Ensure `evaluateMatrixCell` checks harness presence in inventory, evaluates baseline eligibility, and attaches the matching receipt reference when present.
    2. In `test/support-matrix.test.ts`:
       - Add test case verifying that an inspectable Antigravity capability proof (e.g. for `GSD_CORE` or `CAPA-03`) promotes the corresponding Antigravity matrix cell to `PROVEN`.
       - Assert that when no receipt is present for Antigravity, the cell evaluates to `UNVERIFIED`.
    3. Update `docs/SUPPORT_MATRIX.md` via `npm run build && node dist/src/cli.js doctor --matrix` or test runner to reflect current real evidence.
  </action>
  <acceptance_criteria>
    - `src/core/support-matrix.ts` contains no `entry.harnessId === "antigravity"` exclusion in `matchingReceipt`.
    - `test/support-matrix.test.ts` passes and explicitly tests Antigravity cell promotion to PROVEN with a valid receipt.
    - `npm run check && npm run build && node scripts/run-tests.mjs --files dist/test/support-matrix.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node scripts/run-tests.mjs --files dist/test/support-matrix.test.js</automated>
    <fails_when>non-zero exit code, or support matrix test fails</fails_when>
  </verify>
  <done>Antigravity receipts are fully representable and tested in the support matrix evaluator.</done>
</task>

<task type="auto" id="07-05-02">
  <name>Task 2: Real Native Optional Invocation & Negative Paired Controls (REL-03, D-07)</name>
  <read_first>
    test/release-controls.test.ts
    docs/RELEASE_CONTROLS.md
    src/core/canary.ts
    src/core/mcp-fixture.ts
    .planning/phases/07-cross-platform-release-proof/07-VERIFICATION.md
  </read_first>
  <files>test/release-controls.test.ts, docs/RELEASE_CONTROLS.md</files>
  <behavior>
    - `optionalInvocationPair` in `test/release-controls.test.ts` does not construct synthetic in-memory `observations` objects.
    - The positive control executes a real Context7 invocation (e.g. querying documentation for a declared library) through the actual MCP server/bridge or native canary executor, capturing genuine observation records.
    - The negative control executes an ordinary algorithm or non-documentation task and observes zero Context7 invocations.
    - Both controls produce deterministic SHA-256 evidence digests from credential-free observation records.
    - `docs/RELEASE_CONTROLS.md` is updated with the real evidence hashes from the executed controls.
  </behavior>
  <action>
    1. In `test/release-controls.test.ts`:
       - Refactor `optionalInvocationPair()` to invoke the actual Context7 MCP tool runner or canary probe (`runCanary` or direct stdio client call) using the locked `@upstash/context7-mcp` server.
       - Verify the positive run observes real `resolve-library-id` and `query-docs` tool calls.
       - Execute the negative run against a non-library intent and confirm zero Context7 tool invocations.
       - Compute SHA-256 digests over the canonicalized evidence objects and pass them to `pair()`.
    2. Regenerate `docs/RELEASE_CONTROLS.md` with `UPDATE_RELEASE_CONTROLS=1 node scripts/run-tests.mjs --files dist/test/release-controls.test.js`.
    3. Ensure `test/release-controls.test.ts` passes in standard verification mode without `UPDATE_RELEASE_CONTROLS=1`.
  </action>
  <acceptance_criteria>
    - `test/release-controls.test.ts` does not use hardcoded synthetic `observations` in `optionalInvocationPair`.
    - `docs/RELEASE_CONTROLS.md` matches the evidence produced by the real paired control execution.
    - `npm run check && npm run build && node scripts/run-tests.mjs --files dist/test/release-controls.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node scripts/run-tests.mjs --files dist/test/release-controls.test.js</automated>
    <fails_when>non-zero exit code, or release control assertions fail</fails_when>
  </verify>
  <done>Optional capability paired controls execute real native invocation and negative checks with inspectable proof.</done>
</task>

</tasks>

## Artifacts this phase produces
- `src/core/support-matrix.ts`: Updated `matchingReceipt` supporting Antigravity capability proof representation.
- `test/support-matrix.test.ts`: Test suite verifying Antigravity receipt promotion and unverified fallback.
- `test/release-controls.test.ts`: Paired control suite with real Context7 invocation and ordinary-task negative control.
- `docs/SUPPORT_MATRIX.md`: Versioned support ledger reflecting real host receipts.
- `docs/RELEASE_CONTROLS.md`: Deterministic ledger of real paired control evidence digests.
