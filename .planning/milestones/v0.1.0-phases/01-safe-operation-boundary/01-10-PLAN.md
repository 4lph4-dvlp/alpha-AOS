---
phase: 01-safe-operation-boundary
plan: 10
type: execute
wave: 3
depends_on: [01-05, 01-06, 01-09]
files_modified:
  - schemas/install-state.schema.json
  - schemas/evidence.schema.json
  - schemas/receipt.schema.json
  - src/core/validation.ts
  - src/core/mcp.ts
  - src/core/isolation.ts
  - test/validation.test.ts
  - test/mcp.test.ts
  - test/isolation.test.ts
autonomous: true
requirements: [SAFE-04, SAFE-05]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-08/D-11: install state, evidence, receipt, and alpha-AOS-owned native configuration subtrees use closed schemas and bounded redacted validation issues."
    - "D-09: unknown extension namespaces and user-owned native fields are preserved but cannot affect planning/apply without a registered adapter."
    - "D-10: supported older operational records remain inspection-only with migration plans; unknown newer records refuse before mutation."
  artifacts:
    - path: schemas/install-state.schema.json
      provides: "Closed managed install-state contract"
    - path: schemas/evidence.schema.json
      provides: "Closed future evidence envelope contract without Phase 2 behavior"
    - path: schemas/receipt.schema.json
      provides: "Closed future receipt envelope contract without Phase 3 behavior"
    - path: src/core/mcp.ts
      provides: "Strict JSON/YAML/TOML syntax and owned-subtree validation before merge"
  key_links:
    - from: src/core/mcp.ts
      to: src/core/validation.ts
      via: "native syntax parsing and owned-subtree route"
      pattern: "parseManagedDocument|validateManagedDocument"
    - from: src/core/isolation.ts
      to: schemas/install-state.schema.json
      via: "strict operational-state load before inspection/apply"
      pattern: "install-state|schemaVersion"
  prohibitions:
    - statement: "Strict native-config handling MUST NOT delete, normalize, or claim ownership over unrelated user-managed fields while validating the alpha-AOS-owned subtree."
      requirement_id: SAFE-05
      category: safety
      status: unverified
      flagged: true
---

<objective>
남은 managed record와 native configuration input을 strict validation boundary에 연결한다.

Purpose: native files를 보존하면서 alpha-AOS가 소비하는 subtree와 future envelope만 명확히 검증한다.
Output: install-state/evidence/receipt schemas and strict native-config consumers.
</objective>

<execution_context>
@C:/Users/alpha/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safe-operation-boundary/01-CONTEXT.md
@.planning/phases/01-safe-operation-boundary/01-RESEARCH.md
@.planning/phases/01-safe-operation-boundary/01-PATTERNS.md
@.planning/phases/01-safe-operation-boundary/01-05-SUMMARY.md
@.planning/phases/01-safe-operation-boundary/01-06-SUMMARY.md
@.planning/phases/01-safe-operation-boundary/01-09-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Existing native MCP config에서 safe merged preview까지 tracer</name>
  <files>src/core/mcp.ts, test/mcp.test.ts, test/validation.test.ts</files>
  <read_first>
    src/core/mcp.ts
    src/core/validation.ts
    test/mcp.test.ts
    test/validation.test.ts
    schemas/project-stack.schema.json
  </read_first>
  <behavior>
    - JSON/YAML/TOML native config syntax and duplicate ambiguity are rejected before render/apply.
    - only alpha-AOS-owned entries receive strict semantic validation; unrelated user fields round-trip byte/semantic content.
    - unknown extension data remains inert and a malformed owned subtree returns stable redacted issues.
  </behavior>
  <action>D-08, D-09, D-11과 SAFE-05에 따라 `renderMcpConfig` 이전에 format-specific strict parser를 호출하고 alpha-AOS-owned MCP entries만 current managed schema/domain validator에 전달한다. Codex TOML은 approved smol-toml parser로 duplicate/dotted/quoted keys를 해석한다. User-owned native keys/tables를 삭제하거나 default하지 않으며 malformed/unsupported input은 plan/apply 함수가 호출되기 전에 coded validation result로 끝낸다.</action>
  <acceptance_criteria>valid mixed ownership fixtures preserve unrelated content; malformed/duplicate owned cases refuse with deterministic issues and no target mutation; unknown extension cannot alter rendered managed command.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/mcp.test.js dist/test/validation.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, or a user-owned native field is removed or changed</fails_when>
  </verify>
  <reversibility rating="costly">native input acceptance tightens, while unrelated user content remains preserved.</reversibility>
  <done>one existing native config is parsed, validated, preserved, and preview-rendered through the strict path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install-state strict loader와 version migration contract</name>
  <files>schemas/install-state.schema.json, src/core/isolation.ts, test/isolation.test.ts, test/validation.test.ts</files>
  <read_first>
    schemas/install-state.schema.json
    src/core/isolation.ts
    src/core/validation.ts
    test/isolation.test.ts
    test/validation.test.ts
  </read_first>
  <behavior>
    - current install/runtime state validates as a closed document before inspection or mutation.
    - old supported state yields read-only migration plan; unknown newer state refuses.
    - validation errors contain aliases and redacted shapes rather than raw project/environment values.
  </behavior>
  <action>D-08/D-10/D-11에 따라 install-state schema를 close하고 state readers를 exact version router에 연결한다. Existing isolation state inspection remains available, but invalid/old/newer state cannot reach sync/clean apply. This task hardens the existing state surface only; it does not add project-pack materialization, tree-off behavior, or general lifecycle recovery.</action>
  <acceptance_criteria>current fixture loads; malformed/unknown/old/newer fixtures produce the expected current/migration/refusal result and zero mutation.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/isolation.test.js dist/test/validation.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, or invalid state reaches a sync/clean mutation</fails_when>
  </verify>
  <reversibility rating="costly">managed state schema changes are versioned and old state remains read-only.</reversibility>
  <done>operational state is strict and version-aware without adding later-phase lifecycle semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Evidence/receipt envelope schemas와 inert extension registry 완성</name>
  <files>schemas/evidence.schema.json, schemas/receipt.schema.json, src/core/validation.ts, test/validation.test.ts</files>
  <read_first>
    src/core/validation.ts
    test/validation.test.ts
    schemas/lock.schema.json
    schemas/transaction-journal.schema.json
  </read_first>
  <behavior>
    - current evidence/receipt envelopes have closed core identity/version/hash/extension fields.
    - parsers can validate and display them, but no detection/materialization workflow is introduced.
    - unknown namespaces remain preserved and inert; registered adapters are explicit.
  </behavior>
  <action>D-08~D-11의 future-input boundary를 위해 evidence/receipt current schemas and validation routes를 추가한다. Fields are envelope identity, schema version, source/target hashes, producer/version, createdAt, core payload, extensions; raw credentials are disallowed by domain validation. Only parse/inspect/migration-plan APIs are exposed. Do not add Phase 2 evidence collection, Phase 3 receipts/materialization, stale deletion, or apply behavior.</action>
  <acceptance_criteria>valid envelope fixtures inspect; unknown core/secret/raw/newer cases refuse; unknown extensions preserve but cannot alter normalized core or operation digest.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/validation.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, or an evidence/receipt fixture triggers project detection or apply behavior</fails_when>
  </verify>
  <reversibility rating="reversible">schemas are introduced before live writers exist and can be revised through explicit versioning.</reversibility>
  <done>D-08 coverage includes evidence/receipt without absorbing their later-phase workflows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-owned native config→managed subtree | alpha-AOS must validate owned entries without claiming unrelated content |
| operational envelope→future consumer | evidence/receipt/state bytes may later authorize behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-22 | Tampering | native config merge | high | mitigate | strict syntax plus owned-subtree validation and preservation tests |
| T-01-23 | Elevation | operational extensions | high | mitigate | unregistered namespaces excluded from normalized domain/apply data |
</threat_model>

## Artifacts this phase produces

| Kind | Artifact | Contract |
|------|----------|----------|
| Schema | `schemas/install-state.schema.json` | closed versioned operational state |
| New schema | `schemas/evidence.schema.json` | strict inspect-only evidence envelope |
| New schema | `schemas/receipt.schema.json` | strict inspect-only receipt envelope |

## Probe coverage notes

- Generic parser/input-validation concerns stay in STRIDE; preserving unrelated user-owned native content is the phase-specific descriptor-less prohibition.

<verification>
Focused validation, MCP, and isolation suites pass; no invalid document reaches mutation and no later-phase workflow appears.
</verification>

<success_criteria>
- all D-08 document families have strict current schemas/routes.
- native user-owned content is preserved.
- old/newer/extension behavior follows D-09~D-11.
</success_criteria>

<output>
Create `.planning/phases/01-safe-operation-boundary/01-10-SUMMARY.md` when done.
</output>
