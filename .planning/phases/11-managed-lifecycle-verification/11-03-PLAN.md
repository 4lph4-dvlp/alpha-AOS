---
phase: 11-managed-lifecycle-verification
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - .planning/phases/11-managed-lifecycle-verification/evidence/probes/life01-harnesses.mjs
  - .planning/phases/11-managed-lifecycle-verification/evidence/life01-harnesses-reconcile.txt
  - .planning/phases/11-managed-lifecycle-verification/evidence/LIFE-01-findings.md
  - .planning/phases/11-managed-lifecycle-verification/evidence/probes/life08-preview.mjs
  - .planning/phases/11-managed-lifecycle-verification/evidence/life08-preview.txt
  - .planning/phases/11-managed-lifecycle-verification/evidence/probes/life08-apply.mjs
  - .planning/phases/11-managed-lifecycle-verification/evidence/life08-apply.txt
  - .planning/phases/11-managed-lifecycle-verification/evidence/LIFE-08-findings.md
  - test/tarball-fixture.test.ts
autonomous: true
requirements: [LIFE-09]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "LIFE-01 is judged for all five active harnesses (D-10): claude, antigravity, pi and hermes each get the same packed-CLI sequence the Codex tracer used (initial apply, dry-run `install --target <h>` with `CURRENT` step lines, second apply with empty `applied` and `operationIds`, sandbox byte identity), plus one combined five-target reconcile, with check ids `life01.<harness>.*` and `life01.all.*`."
    - "A harness whose initial install fails for an environmental reason (registry unreachable, harness not detected) records NOT-OBSERVED with the verbatim output and is never counted as HOLDS; a failure caused by the product (a second plan not CURRENT, a second apply that writes) is VIOLATED (D-06, empty edge)."
    - "Seeded GSD version files, pre-rendered ECC skills, pre-rendered MCP configs and the seeded Pi bridge manifest are named in `LIFE-01-findings.md` as the reconcile precondition, as the Phase 7 packed lifecycle already does (research Pitfall 5)."
    - "LIFE-08 update preview is judged against every preview surface on byte fingerprints, not journal counts (D-04, D-09): `update`, `update --check`, `update --stage` without `--apply` in a packed sandbox, and `bootstrap update`, `scripts/update.sh` and `scripts/update.ps1` without `--apply` in a fixture checkout; the fingerprinted roots cover the checkout work tree including `.git` and `node_modules` (checkout and package), the npm prefix (package and global link), the harness roots (configuration) and the state root (managed state)."
    - "LIFE-08 update apply is judged by dropping an unreviewed `catalog/candidate.lock.json` into the installed package and showing `update --apply --target codex` applies nothing from it, and by removing one managed file and showing `update --apply` restores it from the stable lock (D-09)."
    - "`LIFE-01-findings.md` and `LIFE-08-findings.md` each state whether any open gap candidate would block Phase 13 dependency promotion under D-08 (only LIFE-01 and LIFE-08 gaps block)."
    - "The packed lifecycle test in test/tarball-fixture.test.ts gains the candidate-ignored `update --apply` assertion only if `life08.apply.candidate-ignored` and `life08.apply.byte-identical` both recorded HOLDS; the Phase 10 host-immutability block and assertion stay byte-identical (D-12)."
    - "`src/` is untouched (D-05)."
  prohibitions:
    - statement: "MUST NOT run `update --apply`, `update --stage --apply`, `bootstrap update --apply` or either update script with `--apply`/`-Apply` outside a sandbox or fixture checkout, and MUST NOT run any update surface against the real repository checkout."
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT weaken the LIFE-01 or LIFE-08 verdict by excluding a fingerprinted root without a written justification in the transcript (only the sandbox `npm-cache`, `npm-logs` and `temp` directories may be excluded, as npm debug logs and fixture scratch by design)."
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT loosen or reorder the Phase 10 host-immutability oracle when extending the packed lifecycle test."
      category: safety
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/phases/11-managed-lifecycle-verification/evidence/life01-harnesses-reconcile.txt"
      provides: "LIFE-01 packed reconcile evidence for claude, antigravity, pi, hermes and all five together"
      contains: "CHECK life01.all."
    - path: ".planning/phases/11-managed-lifecycle-verification/evidence/life08-preview.txt"
      provides: "Fingerprinted no-mutation evidence for six update preview surfaces"
      contains: "life08.preview.bootstrap-update.no-mutation"
    - path: ".planning/phases/11-managed-lifecycle-verification/evidence/life08-apply.txt"
      provides: "Candidate lock ignored and stable reconcile evidence for update --apply"
      contains: "life08.apply.candidate-ignored"
    - path: ".planning/phases/11-managed-lifecycle-verification/evidence/LIFE-01-findings.md"
      provides: "LIFE-01 five-harness clause table and Phase 13 gating statement"
      contains: "Phase 13"
    - path: ".planning/phases/11-managed-lifecycle-verification/evidence/LIFE-08-findings.md"
      provides: "LIFE-08 clause table and Phase 13 gating statement"
      contains: "Phase 13"
  key_links:
    - from: "life08-apply.mjs"
      to: "installed package catalog/candidate.lock.json"
      via: "bogus candidate lock written into the sandbox package, never into the checkout"
      pattern: "candidate.lock.json"
    - from: "LIFE-01-findings.md codex row"
      to: "evidence/life01-codex-reconcile.txt"
      via: "cites the plan 11-01 tracer checks"
      pattern: "life01-codex-reconcile.txt"
---

<objective>
Produce the evidence that decides Phase 13 gating (D-08): LIFE-01 idempotent stable-lock reconcile across all five harnesses, and LIFE-08 update preview non-mutation on every preview surface plus stable-lock-only update apply.

Purpose: Phase 13 promotes dependency candidates through `update --apply`; it may proceed only if these two requirements carry no open gap. The existing oracles are weak (the LIFE-01 unit test forces a synthetic all-current plan; the LIFE-08 preview test counts journals), so this plan supplies byte-fingerprint proof through the packed CLI.

Output: three probes and transcripts, `LIFE-01-findings.md`, `LIFE-08-findings.md`, and a pass-only extension of the packed lifecycle test.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-managed-lifecycle-verification/11-CONTEXT.md
@.planning/phases/11-managed-lifecycle-verification/11-RESEARCH.md
@.planning/phases/11-managed-lifecycle-verification/evidence/README.md
@.planning/phases/11-managed-lifecycle-verification/11-01-SUMMARY.md
@.planning/phases/11-managed-lifecycle-verification/evidence/seed-selfcheck.txt
@.planning/phases/11-managed-lifecycle-verification/evidence/life01-codex-reconcile.txt
</context>

<interfaces>
Paths: EV = `.planning/phases/11-managed-lifecycle-verification/evidence`, PROBES = `EV/probes`.

From plan 11-01 (read-only here; do not modify `probe-lib.mjs` or `test/helpers/packed-sandbox.ts`): `newSandbox`, `assertSandboxEnv`, `runCli`, `fingerprintRoots`, `compareFingerprints`, `withHostGuard`, `createTranscript`, `packOnce`; `installPackedRelease`, `seedHarnessPrerequisites(sandbox, release, harness, { eccCache })`, `harnessPaths`, `hostNpmCache`, `npmInvocation`, `snapshotTargets`, `describeHostDrift`. Probes in this plan pass `eccCache: hostNpmCache()` to avoid a cold registry download per sandbox.

Product surfaces:
- `install [--target <list>] [--apply] [--json]` (src/cli.ts:489-513); dry-run step line format `<ACTION padded to 7> <step id> - <note>`; apply result `{ plan, applied, current, operationIds, externalChanges }`.
- `update` with no mode means `--check` (src/cli.ts:1009-1039); `--check` and `--stage` call `resolveCandidate(lock)` (registry fetch, src/core/update.ts:24-33); `--stage --apply` writes `catalog/candidate.lock.json` under the package root; `--apply` runs `applyManagedInstall` with the packaged stable `lock` and prints `Unverified candidate.lock.json was not applied.`
- `bootstrap update [--target] [--skip-link] [--apply] [--json]` (src/cli.ts:1316-1350, src/core/bootstrap.ts:269-349): preview inspects the build artifact, `package-lock.json`, the checkout (remote target oid), and plans external steps `git-merge-ff-only`, `npm-ci`, `npm-build`, `npm-link`.
- `scripts/update.sh` and `scripts/update.ps1`: run `node scripts/build-artifact.mjs check`, print `Showing the update plan only.`, then exec `node dist/src/cli.js bootstrap update` without `--apply` unless `--apply` / `-Apply` is given.
- Stable lock loading: src/core/catalog.ts:163-184 (`loadLockStrict`, `lockInvariants(channel)`).

Existing oracles to audit (D-04): test/lifecycle.test.ts:161-220 (synthetic `currentPlan` with every step forced to `current`, `digest: "synthetic-digest"`), 222-270, 272-292 (`pure read-only update preview` compares `listManagedTransactions` counts only), 294-310 (`candidate locks are strictly rejected` checks a plan note string); test/update.test.ts:175-235 (preflight binds the remote commit; asserts plan fields, not checkout bytes).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: LIFE-01 packed reconcile for claude, antigravity, pi, hermes and all five together</name>
  <precondition>EV/seed-selfcheck.txt from plan 11-01 exists and shows `seed.<harness>.ecc HOLDS` for all five harnesses; the npm registry is reachable or the host npm cache holds `ecc-universal@2.2.0`.</precondition>
  <files>.planning/phases/11-managed-lifecycle-verification/evidence/probes/life01-harnesses.mjs, .planning/phases/11-managed-lifecycle-verification/evidence/life01-harnesses-reconcile.txt, .planning/phases/11-managed-lifecycle-verification/evidence/LIFE-01-findings.md</files>
  <read_first>
    .planning/phases/11-managed-lifecycle-verification/evidence/probes/life01-reconcile.mjs (the tracer sequence to replicate)
    .planning/phases/11-managed-lifecycle-verification/evidence/seed-selfcheck.txt
    src/core/install.ts (lines 145-158 target selection, 373-463 plan steps, 700-878 apply and external changes)
    test/lifecycle.test.ts (lines 161-270 LIFE-01 oracles)
    .planning/phases/11-managed-lifecycle-verification/evidence/suites-local.txt
    .planning/phases/11-managed-lifecycle-verification/evidence/ci-35818198049-lifelines.txt
  </read_first>
  <action>
    Per D-01, D-04, D-08, D-09 and D-10:
    1. Write PROBES/life01-harnesses.mjs. Inside `withHostGuard`, pack once. For each harness in the order claude, antigravity, pi, hermes: a fresh sandbox, `installPackedRelease`, `seedHarnessPrerequisites(..., <harness>, { eccCache: hostNpmCache() })`, then the tracer sequence with ids `life01.<harness>.initial-apply`, `life01.<harness>.preview-current`, `life01.<harness>.preview-no-mutation`, `life01.<harness>.preview-json-current`, `life01.<harness>.second-apply-noop`, `life01.<harness>.second-apply-byte-identical` (fingerprinted roots: sandbox home, state and prefix; exclusions as in the tracer, with the same note). If the initial apply exits non-zero, record `life01.<harness>.initial-apply` as NOT-OBSERVED when the stderr names an environmental cause (network, registry, harness not detected, missing executable) and VIOLATED otherwise, and record every later check of that harness as NOT-OBSERVED with detail `initial apply did not complete`. Then one more sandbox seeded for all five harnesses with the same sequence against `--target claude,codex,antigravity,pi,hermes`, ids `life01.all.*`. Write EV/life01-harnesses-reconcile.txt.
    2. Write EV/LIFE-01-findings.md from the README template. Contract verbatim from .planning/milestones/v0.1.0-REQUIREMENTS.md:77. Clauses: install or reconcile the reviewed stable lock; idempotently (no writes, no journals, byte identity); a second unchanged plan reports `CURRENT`; one row per harness for scope (codex rows cite EV/life01-codex-reconcile.txt). Record the 'which command is the plan' decision (plan 11-01 truth: `install` without `--apply`; `alpha-aos plan` is a static table) with the `life01.plan-command-static` and `life01.plan-command-docs` checks; raise it as a gap candidate only if the docs check found a line telling users to run `alpha-aos plan` for reconcile state. Oracle audit (D-04): the `stable-lock reconciliation ... (LIFE-01, D-15)` test builds a synthetic all-current plan (test/lifecycle.test.ts:189-192), so it proves only the short-circuit, not that a real second plan is current; say which probe checks supply the missing proof. State the seeding precondition. Provisional verdict per D-02, and a `## Phase 13 gating` line reading `LIFE-01 blocks Phase 13: yes` or `no` with the gap candidate keys that cause it (D-08).
  </action>
  <verify>
    <automated>npm run build && node .planning/phases/11-managed-lifecycle-verification/evidence/probes/life01-harnesses.mjs && T=.planning/phases/11-managed-lifecycle-verification/evidence/life01-harnesses-reconcile.txt && grep -q '^host-guard: unchanged' "$T" && for h in claude antigravity pi hermes all; do test "$(grep -cE "^CHECK life01\.$h\.(initial-apply|preview-current|preview-no-mutation|preview-json-current|second-apply-noop|second-apply-byte-identical) " "$T")" -eq 6 || exit 1; done</automated>
    <fails_when>non-zero exit, no unchanged host guard line, or any of the five groups (claude, antigravity, pi, hermes, all) lacks exactly six checks</fails_when>
    <automated>F=.planning/phases/11-managed-lifecycle-verification/evidence/LIFE-01-findings.md && grep -q '^## Provisional verdict' "$F" && grep -qE 'LIFE-01 blocks Phase 13: (yes|no)' "$F" && grep -q 'life01-codex-reconcile.txt' "$F" && grep -q 'synthetic' "$F" && test -z "$(git status --porcelain -- src)"</automated>
    <fails_when>the verdict heading, the Phase 13 gating line, the codex citation or the synthetic-plan oracle note is missing, or src/ changed</fails_when>
  </verify>
  <acceptance_criteria>
    - EV/life01-harnesses-reconcile.txt has 30 `life01.<group>.*` checks (six per group) with outcomes from the four-word vocabulary only.
    - Every NOT-OBSERVED check quotes the stderr line that made it environmental.
    - LIFE-01-findings.md has one scope row per harness (five) and a `## Phase 13 gating` section.
    - `git status --porcelain -- src` prints nothing.
  </acceptance_criteria>
  <done>LIFE-01 has packed-CLI reconcile evidence for every active harness, and the findings state whether LIFE-01 blocks Phase 13.</done>
</task>

<task type="auto">
  <name>Task 2: LIFE-08 update preview non-mutation on six surfaces</name>
  <precondition>`pwsh` and Git Bash `sh` resolve on PATH (research Environment Availability); the npm registry is reachable for `update --check` (an unreachable registry is recorded, and the no-mutation checks still run).</precondition>
  <files>.planning/phases/11-managed-lifecycle-verification/evidence/probes/life08-preview.mjs, .planning/phases/11-managed-lifecycle-verification/evidence/life08-preview.txt</files>
  <read_first>
    src/cli.ts (lines 1009-1039 update, 1316-1350 bootstrap)
    src/core/update.ts (lines 1-60)
    src/core/bootstrap.ts (lines 269-360 createBootstrapOperationPlan and inspectCheckout)
    scripts/update.sh
    scripts/update.ps1
    scripts/build-artifact.mjs (the `check` mode inputs)
    test/lifecycle.test.ts (lines 272-310)
    test/update.test.ts (lines 175-235)
  </read_first>
  <action>
    Per D-01, D-04 and D-09 (the LIFE-08 text names checkout, package, global-link, configuration and managed-state mutation):
    1. Write PROBES/life08-preview.mjs. Inside `withHostGuard`, pack once.
       Scenario A (packed sandbox): `installPackedRelease`, `seedHarnessPrerequisites(..., "codex", { eccCache: hostNpmCache() })`, `install --target codex --apply --json`. Fingerprint the sandbox home, state and prefix, then run `update`, `update --check --json` and `update --stage` (no `--apply`) through `runCli`, comparing fingerprints after each: checks `life08.preview.update-default.no-mutation`, `life08.preview.update-check.no-mutation`, `life08.preview.update-stage.no-mutation`, and `life08.preview.update-check.result` OBSERVED (the candidate versions reported, or the registry error text).
       Scenario B (fixture checkout, same sandbox): `git clone --no-hardlinks --quiet <repo root> <sandbox root>/checkout`; copy `<repo root>/dist` into `<checkout>/dist`; run `npm ci --ignore-scripts --prefer-offline --no-audit --no-fund` in the checkout with the sandbox env and the host npm cache; run `node scripts/build-artifact.mjs check` in the checkout and record `life08.preview.checkout-artifact` HOLDS when it exits 0 (otherwise OBSERVED with its output: the preview will be a blocked preview, and the no-mutation checks still apply). Define in this probe file a runner for checkout commands that calls `assertSandboxEnv(env, sandbox root)` before every spawn, uses the checkout as cwd, and refuses any argument list containing `--apply` or `-Apply`. Fingerprint the checkout (whole tree including `.git`, `node_modules` and `dist`), the sandbox home, state and prefix; then run `node dist/src/cli.js bootstrap update --json`, `node dist/src/cli.js bootstrap update`, `sh scripts/update.sh` and `pwsh -NoProfile -File scripts/update.ps1`, comparing all four fingerprints after each: checks `life08.preview.bootstrap-update.no-mutation`, `life08.preview.bootstrap-update-human.no-mutation`, `life08.preview.update-sh.no-mutation`, `life08.preview.update-ps1.no-mutation`, and `life08.preview.bootstrap-update.plan` OBSERVED listing the planned external step ids and any blocked reasons.
       Excluded from every fingerprint, with a note in the transcript: the sandbox `npm-cache`, `npm-logs` and `temp` directories (npm writes debug logs and cache entries on every invocation, including read-only queries, per the scripts/run-tests.mjs header).
    2. Write EV/life08-preview.txt. Do not interpret here beyond the checks; the findings are written in Task 3.
  </action>
  <verify>
    <automated>npm run build && node .planning/phases/11-managed-lifecycle-verification/evidence/probes/life08-preview.mjs && T=.planning/phases/11-managed-lifecycle-verification/evidence/life08-preview.txt && grep -q '^host-guard: unchanged' "$T" && for c in update-default update-check update-stage bootstrap-update bootstrap-update-human update-sh update-ps1; do grep -qE "^CHECK life08\.preview\.$c\.no-mutation (HOLDS|VIOLATED|NOT-OBSERVED) " "$T" || exit 1; done</automated>
    <fails_when>non-zero exit, no unchanged host guard line, or any of the seven no-mutation checks is missing</fails_when>
  </verify>
  <acceptance_criteria>
    - EV/life08-preview.txt contains seven `life08.preview.*.no-mutation` checks and states the excluded directories with the reason.
    - Every VIOLATED no-mutation check is followed, above it, by the `describeHostDrift` lines naming the changed entries by path, kind, size and hash prefix only.
    - The real repository checkout was never a cwd for any update surface (the transcript shows only `<sandbox>` cwd aliases for scenario B).
    - `git status --porcelain -- src` prints nothing.
  </acceptance_criteria>
  <done>Every LIFE-08 preview surface has a byte-fingerprint verdict covering checkout, package, global link, configuration and managed state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: LIFE-08 update apply ignores an unreviewed candidate lock and reconciles the stable lock; findings and the pass-only lifecycle assertion</name>
  <files>.planning/phases/11-managed-lifecycle-verification/evidence/probes/life08-apply.mjs, .planning/phases/11-managed-lifecycle-verification/evidence/life08-apply.txt, .planning/phases/11-managed-lifecycle-verification/evidence/LIFE-08-findings.md, test/tarball-fixture.test.ts</files>
  <read_first>
    src/cli.ts (lines 1009-1027 update --apply)
    src/core/catalog.ts (lines 163-184)
    test/tarball-fixture.test.ts (the packed lifecycle test after plan 11-01)
    .planning/phases/11-managed-lifecycle-verification/evidence/life08-preview.txt
    .planning/phases/11-managed-lifecycle-verification/evidence/suites-local.txt
    .planning/phases/11-managed-lifecycle-verification/evidence/ci-35818198049-lifelines.txt
  </read_first>
  <behavior>
    - In the packed lifecycle test, after the reconcile assertions: with an unreviewed `catalog/candidate.lock.json` (stable lock copy, `channel` set to `candidate`, ECC version set to `9.9.9`) written into the installed package, `update --apply --target codex --json` returns empty `applied` and `operationIds`, adds no journal, and leaves the sandbox home, state and prefix digests unchanged; the bogus file is removed afterwards.
  </behavior>
  <action>
    Per D-04, D-08, D-09 and D-12:
    1. Write PROBES/life08-apply.mjs. Inside `withHostGuard`, pack once, one sandbox, `installPackedRelease`, `seedHarnessPrerequisites(..., "codex", { eccCache: hostNpmCache() })`, `install --target codex --apply --json`. Write the bogus candidate lock described in the behavior block into `<installed package>/catalog/candidate.lock.json`, then fingerprint home, state and prefix (after the write, so the bogus file is part of the baseline). Run `update --apply --target codex --json`: checks `life08.apply.candidate-ignored` (HOLDS when `applied` is empty and neither stdout nor stderr contains `9.9.9`), `life08.apply.no-new-journal` (journal file names unchanged), `life08.apply.byte-identical` (fingerprints equal). Run the human form `update --apply --target codex`: check `life08.apply.message` HOLDS when stdout contains `Unverified candidate.lock.json was not applied.` Then delete the sandbox `<CODEX_HOME>/AGENTS.md` after recording its sha256, run `update --apply --target codex --json`: check `life08.apply.reconciles-stable` HOLDS when `applied` names the codex policy step and the restored file's sha256 equals the recorded one; run it once more: check `life08.apply.reconcile-idempotent` (empty `applied`). Write EV/life08-apply.txt.
    2. If `life08.apply.candidate-ignored` and `life08.apply.byte-identical` are both HOLDS, add the behavior block to the packed lifecycle test in test/tarball-fixture.test.ts after the existing reconcile assertions and before the `status` step, using `snapshotTargets` and `describeHostDrift(..., undefined, "update --apply must ignore an unreviewed candidate lock")`, removing the bogus file with `rm` afterwards. Do not touch the `hostMutationTargets` block, `hostSnapshot`, or the final host assertion. If either check is VIOLATED, add nothing and record the reproduction in the findings instead.
    3. Write EV/LIFE-08-findings.md from the README template. Contract verbatim from .planning/milestones/v0.1.0-REQUIREMENTS.md:84. Clause rows: no checkout mutation, no package mutation, no global-link mutation, no configuration mutation, no managed-state mutation (each citing the relevant `life08.preview.*` checks per surface), update apply reconciles only a reviewed stable-lock promotion (citing `life08.apply.*`, plus src/cli.ts:1012-1026 and src/core/catalog.ts:163-184 as supporting reading, never as sole evidence). Oracle audit (D-04): the `pure read-only update preview ... (LIFE-08, D-16)` test compares journal counts and calls `planCandidateStage`, not the CLI; the update preflight test asserts plan fields, not checkout bytes; the `candidate locks are strictly rejected` test checks a note string. Provisional verdict per D-02, and a `## Phase 13 gating` line reading `LIFE-08 blocks Phase 13: yes` or `no` with the causing gap candidate keys (D-08).
  </action>
  <verify>
    <automated>npm run build && node .planning/phases/11-managed-lifecycle-verification/evidence/probes/life08-apply.mjs && T=.planning/phases/11-managed-lifecycle-verification/evidence/life08-apply.txt && grep -q '^host-guard: unchanged' "$T" && for c in candidate-ignored no-new-journal byte-identical message reconciles-stable reconcile-idempotent; do grep -qE "^CHECK life08\.apply\.$c (HOLDS|VIOLATED|NOT-OBSERVED) " "$T" || exit 1; done</automated>
    <fails_when>non-zero exit, no unchanged host guard line, or any of the six apply checks is missing</fails_when>
    <automated>npm run check && npm run build && H=$(mktemp -d) && WH=$(cygpath -w "$H" 2>/dev/null || printf '%s' "$H") && env -u ALPHA_AOS_STATE_DIR -u CODEX_HOME -u CLAUDE_CONFIG_DIR -u HERMES_HOME -u PI_CODING_AGENT_DIR -u ANTIGRAVITY_CONFIG_DIR HOME="$WH" USERPROFILE="$WH" node scripts/run-tests.mjs --files dist/test/tarball-fixture.test.js dist/test/path-literal-guard.test.js && test ! -e "$H/.alpha-aos" && diff <(git show 35d4c15:test/tarball-fixture.test.ts | sed -n '/^const hostMutationTargets = \[/,/^\];/p') <(sed -n '/^const hostMutationTargets = \[/,/^\];/p' test/tarball-fixture.test.ts) && test "$(grep -c 'assert.deepEqual(afterHost, beforeHost, describeHostDrift(beforeManifests, afterManifests));' test/tarball-fixture.test.ts)" -eq 1</automated>
    <fails_when>non-zero exit, `fail` above 0 in the summary, `.alpha-aos` in the scratch home, the targets block differs from 35d4c15, or the host deepEqual line count is not 1</fails_when>
    <automated>F=.planning/phases/11-managed-lifecycle-verification/evidence/LIFE-08-findings.md && grep -q '^## Provisional verdict' "$F" && grep -qE 'LIFE-08 blocks Phase 13: (yes|no)' "$F" && grep -q 'planCandidateStage' "$F" && test "$(grep -cE 'life08\.(preview|apply)\.' "$F")" -ge 10 && test -z "$(git status --porcelain -- src)"</automated>
    <fails_when>verdict heading, gating line or the preview-oracle audit note is missing, fewer than ten check citations, or src/ changed</fails_when>
  </verify>
  <acceptance_criteria>
    - The lifecycle test contains the header string `update --apply must ignore an unreviewed candidate lock` if and only if `life08.apply.candidate-ignored` and `life08.apply.byte-identical` are both HOLDS in EV/life08-apply.txt.
    - The bogus candidate lock was written only inside the sandbox installed package; `git status --porcelain catalog` in the repository prints nothing.
    - LIFE-08-findings.md has a row per mutation class (checkout, package, global-link, configuration, managed-state) and one for the apply clause.
    - `git status --porcelain -- src` prints nothing.
  </acceptance_criteria>
  <done>LIFE-08 apply behavior is evidenced through the packed CLI, CI guards the candidate-ignored property if it holds, and both gating findings are ready for plan 11-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe to fixture checkout | A git clone and npm ci inside the sandbox stand in for the user's alpha-AOS checkout |
| update surfaces to registry | `update --check` and `--stage` fetch from the npm registry |
| probe to installed package | The bogus candidate lock is written into the sandbox package |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-10 | Tampering | life08 checkout runner | critical | mitigate | `assertSandboxEnv` before every spawn, cwd fixed to the sandbox checkout, argument lists containing `--apply` or `-Apply` refused; host guard wraps the probe |
| T-11-11 | Tampering | bogus candidate lock | high | mitigate | Written only under the sandbox installed package; acceptance checks the repository `catalog/` is clean |
| T-11-12 | Repudiation | fingerprint exclusions | medium | mitigate | Only sandbox npm-cache, npm-logs and temp may be excluded, each with a written reason in the transcript |
| T-11-13 | Denial of service | `main` CI | medium | mitigate | The lifecycle assertion is added only when both apply checks are HOLDS (D-12) |
</threat_model>

<verification>
- All three LIFE-01/LIFE-08 transcripts end with a `RESULT:` line and an unchanged host guard.
- The scratch-home tarball-fixture run passes and the Phase 10 oracle diff gate holds.
- Host-safe full suite: `npm run build && H=$(mktemp -d) && WH=$(cygpath -w "$H" 2>/dev/null || printf '%s' "$H") && env -u ALPHA_AOS_STATE_DIR -u CODEX_HOME -u CLAUDE_CONFIG_DIR -u HERMES_HOME -u PI_CODING_AGENT_DIR -u ANTIGRAVITY_CONFIG_DIR HOME="$WH" USERPROFILE="$WH" ALPHA_AOS_REQUIRE_UPSTREAM_MCP=1 node scripts/run-tests.mjs --files $(ls dist/test/*.test.js | grep -v capability-oracle) && test ! -e "$H/.alpha-aos"` exits 0.
- `git status --porcelain -- src` prints nothing.
</verification>

<success_criteria>
- LIFE-01 and LIFE-08 findings each end with an explicit Phase 13 gating line backed by cited checks (D-08).
- Every LIFE-01 and LIFE-08 clause has a HOLDS row or a named gap candidate with a reproduction.
</success_criteria>

## Artifacts this phase produces

- Probes `PROBES/life01-harnesses.mjs`, `PROBES/life08-preview.mjs`, `PROBES/life08-apply.mjs`.
- Transcripts `EV/life01-harnesses-reconcile.txt`, `EV/life08-preview.txt`, `EV/life08-apply.txt`.
- Findings `EV/LIFE-01-findings.md`, `EV/LIFE-08-findings.md`, each with a `## Phase 13 gating` section.
- Check ids `life01.<claude|antigravity|pi|hermes|all>.*`, `life08.preview.*`, `life08.apply.*`; gap candidate keys `LIFE-01/C<k>`, `LIFE-08/C<k>`.
- Conditional new assertion in the packed lifecycle test (`update --apply must ignore an unreviewed candidate lock`).

<output>
Create `.planning/phases/11-managed-lifecycle-verification/11-03-SUMMARY.md` when done. Include the five-harness LIFE-01 table, the seven preview no-mutation outcomes, the six apply outcomes, both Phase 13 gating lines, and whether the lifecycle assertion was committed.
</output>
