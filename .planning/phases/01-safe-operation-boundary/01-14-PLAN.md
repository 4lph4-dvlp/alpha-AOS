---
phase: 01-safe-operation-boundary
plan: 14
type: execute
wave: 5
depends_on: [01-11, 01-12, 01-13]
files_modified:
  - src/core/install.ts
  - src/core/bootstrap.ts
  - test/install.test.ts
  - test/update.test.ts
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-03, SAFE-04, SAFE-06]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-02/D-03: managed install and wrapper bootstrap/update enumerate and prove every path, source identity, package root, ProcessSpec, environment name, reviewed digest, and all GSD/ECC/MCP fixture operation plans before any fixture mkdir/write/remove, package/MCP child, git/npm/build/link, or managed/native mutation."
    - "D-04/D-05: one operation-scoped MutationSession is acquired after complete preflight and is passed through every external step and session-aware component callee without nested locks."
    - "External checkout/package/link changes have durable intent and observed-state evidence, but are never described as transactionally rolled back without exact proof."
  artifacts:
    - path: src/core/install.ts
      provides: "Managed install caller wired to all session-aware fixture and component APIs"
    - path: src/core/bootstrap.ts
      provides: "Complete wrapper operation plan and one-session bootstrap/update apply"
      exports: [createBootstrapOperationPlan, applyBootstrapOperation]
  key_links:
    - from: src/core/install.ts
      to: src/core/gsd-compat.ts
      via: "same caller-owned MutationSession passed to GSD/ECC/MCP/owned-skill/policy apply APIs"
      pattern: "session|applyGsd|applyEcc|applyMcp|applyOwned|applyClaude"
    - from: src/core/install.ts
      to: src/core/gsd-fixture.ts
      via: "aggregate plan embeds GSD/ECC/MCP fixture plans and passes the same caller MutationSession to every required-session executor"
      pattern: "runGsdFixture|runEccFixture|runMcpFixture|MutationSession"
    - from: src/core/install.ts
      to: src/core/mcp.ts
      via: "applyMcpSync consumes the aggregate install session after Plan 01-13"
      pattern: "applyMcpSync.*session"
    - from: src/core/install.ts
      to: src/core/owned-skills.ts
      via: "applyOwnedSkillSync and applyClaudeSkillPolicy consume the aggregate install session after Plan 01-13"
      pattern: "applyOwnedSkillSync|applyClaudeSkillPolicy"
    - from: src/core/bootstrap.ts
      to: src/core/writer-lock.ts
      via: "complete plan revalidation followed by one MutationSession before first git/npm/build/link step"
      pattern: "createBootstrapOperationPlan|acquireMutationSession|applyBootstrapOperation"
  prohibitions:
    - statement: "A bootstrap/update result MUST NOT claim checkout, package, or global-link rollback when its journal proves only managed-file restoration or records an unknown external state."
      requirement_id: SAFE-03
      category: transparency
      status: unverified
      flagged: true
---

<objective>
모든 session-aware component API를 managed install caller에 연결하고 install/update wrapper mutation을 core one-session safe-apply 경계 안으로 이동한다.

Purpose: shell wrapper가 shared PathProof/MutationSession 경계보다 먼저 git/npm/build/link를 실행하는 우회를 제거한다.
Output: ordered managed-install caller plus complete bootstrap/update plan/apply service with honest recovery evidence.
</objective>

<execution_context>
@C:/Users/alpha/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safe-operation-boundary/01-CONTEXT.md
@.planning/phases/01-safe-operation-boundary/01-RESEARCH.md
@.planning/phases/01-safe-operation-boundary/01-PATTERNS.md
@.planning/phases/01-safe-operation-boundary/01-11-SUMMARY.md
@.planning/phases/01-safe-operation-boundary/01-12-SUMMARY.md
@.planning/phases/01-safe-operation-boundary/01-13-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Managed install caller의 one-session end-to-end tracer</name>
  <files>src/core/install.ts, test/install.test.ts</files>
  <read_first>
    src/core/install.ts
    src/core/gsd-compat.ts
    src/core/ecc-skills.ts
    src/core/gsd-fixture.ts
    src/core/ecc-fixture.ts
    src/core/mcp-fixture.ts
    src/core/mcp.ts
    src/core/owned-skills.ts
    src/core/skill-policy.ts
    test/install.test.ts
  </read_first>
  <behavior>
    - createManagedInstallPlan includes complete component and GSD/ECC/MCP fixture plans, exact fixture/temp/source/package/cache/config/target/state/journal/snapshot proofs, source/package hashes, ProcessSpecs, environment names, and one digest without mutation.
    - apply revalidates the whole plan, acquires one session, then passes the same token to every fixture executor, external step, and component apply.
    - no fixture/component callee acquires another lock, calls mkdtemp, or discovers an unplanned path/process.
  </behavior>
  <action>D-02~D-05에 따라 `createManagedInstallPlan`이 Plan 01-11의 exact GSD/ECC/MCP fixture operation plans를 component/external plans와 함께 직접 소유하고, 각 fixture의 precomputed writable/read-only paths, complete PathProof set, approved ProcessSpecs, source/package hashes 및 digest를 aggregate digest에 포함하게 한다. `applyManagedInstall`은 aggregate 전체를 재검증한 뒤 첫 mutation으로 one `MutationSession`을 acquire하고 `runGsdFixture`, `runEccFixture`, `runMcpFixture`와 Plans 01-12/01-13의 session-aware component APIs에 동일 token을 전달한다. Fixture의 첫 mkdir/write/remove/npm/npx/Pi/MCP launch와 first external installer command 및 각 source/target operation 직전에 bound proof/hash를 recheck한다. Fixture/component digest가 aggregate digest에 없거나 callee가 새 path/process를 요청하면 mutation 전에 refuse하며 어떤 callee도 standalone wrapper나 nested lock을 사용하지 않는다.</action>
  <acceptance_criteria>one-target 및 multi-component cases에서 aggregate plan이 실행될 모든 GSD/ECC/MCP fixture를 열거하고 lock acquisition은 각각 1회이며 fixture·external·component의 모든 observed token이 같다. Incomplete/drifted fixture or component plans execute zero mkdir/write/remove/package/MCP/external/file mutations, and event ordering proves session acquisition precedes the first fixture mutation/process.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/install.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, a nested lock is acquired, any fixture is absent from the aggregate proof/digest, or a directory/file/package/MCP command runs before aggregate preflight and session acquisition</fails_when>
  </verify>
  <reversibility rating="costly">install orchestration signatures and ordering change while existing component renderers remain intact.</reversibility>
  <done>managed install has one complete plan and one session spanning all fixture, external, and component callees from their first mutation onward.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install/update wrapper operation의 complete preflight plan</name>
  <files>src/core/bootstrap.ts, test/install.test.ts, test/update.test.ts</files>
  <read_first>
    src/core/install.ts
    src/core/gsd-fixture.ts
    src/core/ecc-fixture.ts
    src/core/mcp-fixture.ts
    src/core/update.ts
    src/core/path-boundary.ts
    src/core/process.ts
    scripts/install.ps1
    scripts/install.sh
    scripts/update.ps1
    scripts/update.sh
    test/install.test.ts
    test/update.test.ts
  </read_first>
  <behavior>
    - createBootstrapOperationPlan is read-only and covers checkout/.git, node_modules, dist/artifact manifest, global-link target, state/journal/snapshot/temp, install sources, package root, managed/native targets, every GSD/ECC/MCP fixture operation, and every git/npm/node/MCP ProcessSpec.
    - install binds current package-lock/source/artifact hashes; update additionally binds current HEAD, configured remote/ref, read-only resolved remote OID, and a locally inspectable target Git object/source identity.
    - missing/stale artifact, unavailable target source object, dirty checkout, incomplete proof, or unsupported filesystem returns a stable refusal with unchanged digests.
  </behavior>
  <action>SAFE-01 and D-01~D-03에 따라 `BootstrapOperationPlan`과 read-only `createBootstrapOperationPlan(kind, options)`을 `src/core/bootstrap.ts`에 만든다. Verified existing `dist` artifact and its build-input manifest are mandatory for preview and apply. Update preflight uses shell-free `git ls-remote` and locally readable target object metadata to bind the exact fast-forward OID before mutation; if the target source cannot be inspected without changing the checkout/object store, refuse and offer no unsafe override. Aggregate the complete managed-install plan—including its exact GSD/ECC/MCP fixture plans/proofs/specs—plus git/npm/build/link specs and all affected PathProof roles into one reviewed digest.</action>
  <acceptance_criteria>repeated plan output is digest-stable and byte-identical; each missing role/artifact/source-object/dirty/escape case runs no git/npm/build/link or managed mutation.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/install.test.js dist/test/update.test.js --test-name-pattern="bootstrap|preflight|artifact|source"</automated>
    <fails_when>non-zero exit, output reports 0 matching tests, or a refused case changes checkout/package/link/state/config bytes</fails_when>
  </verify>
  <reversibility rating="reversible">the new plan service is read-only and additive.</reversibility>
  <done>install/update bootstrap has a complete reviewed operation plan before any mutation is authorized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: git/npm/build/link와 managed reconcile을 one-session apply로 실행</name>
  <files>src/core/bootstrap.ts, test/install.test.ts, test/update.test.ts</files>
  <read_first>
    src/core/bootstrap.ts
    src/core/install.ts
    src/core/update.ts
    src/core/writer-lock.ts
    src/core/transaction.ts
    src/core/process.ts
    test/install.test.ts
    test/update.test.ts
  </read_first>
  <behavior>
    - applyBootstrapOperation revalidates reviewed digest/proofs/source identities, then acquires exactly one MutationSession before the first git/npm/build/link step.
    - every external step and nested apply uses the same session token and has durable intent plus before/after/unknown observed-state evidence.
    - checkout/package/link uncertainty blocks subsequent mutation, remains needs-repair evidence, and never produces a false rollback claim.
  </behavior>
  <action>D-02~D-07 and D-12에 따라 `applyBootstrapOperation(reviewedPlan)`을 구현한다. Recheck the full plan including every embedded fixture plan, acquire one session as the first mutation, journal each fixture/external intent before executing its predeclared shell-free ProcessSpec, and immediately verify bound source/path identity. Pass that session into `applyManagedInstall`, whose GSD/ECC/MCP fixture executors and component callees must use the same token; no fixture directory creation or package/MCP launch may occur earlier. Run remaining git/npm/build/link and managed reconcile in reviewed order. Record checkout HEAD, package/link observations, fingerprints and coded recovery guidance as `changed|unchanged|unknown`; only exact managed-file journal evidence may claim restoration. Release the lock only after durable final evidence, otherwise preserve needs-repair state.</action>
  <acceptance_criteria>command spies prove the session precedes all external and native mutations and every token matches; injected failures preserve honest durable evidence, redact sentinels, and never claim unproven checkout/package rollback.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/install.test.js dist/test/update.test.js --test-name-pattern="bootstrap|session|external|failure"</automated>
    <fails_when>non-zero exit, output reports 0 matching tests, any mutation precedes session acquisition, or external uncertainty is reported rolled back</fails_when>
  </verify>
  <reversibility rating="costly">external changes may not have an exact inverse; durable evidence and fail-closed recovery make that limitation explicit.</reversibility>
  <done>bootstrap/update and managed/native work execute under one operation session with truthful recovery state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewed wrapper plan→checkout/package/link/native mutation | one wrapper command coordinates external and managed mutation mechanisms |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-30 | Tampering/Elevation | bootstrap/install ordering | critical | mitigate | exact source identity, complete proof/digest, one session before every process and file action |
| T-01-31 | Repudiation | external recovery evidence | high | mitigate | durable intents plus changed/unchanged/unknown observations and no unproven rollback claim |
</threat_model>

## Artifacts this phase produces

| Kind | Artifact | Contract |
|------|----------|----------|
| New file | `src/core/bootstrap.ts` | complete wrapper plan and one-session external/managed apply |
| Extended caller | `src/core/install.ts` | embeds all three fixture operations and passes one MutationSession to every fixture/external/component callee |

<verification>
Install/update focused suites prove complete preflight, one session, immediate rechecks, and honest external-state evidence.
</verification>

<success_criteria>
- no fixture filesystem/package/MCP, git/npm/build/link, or managed/native mutation precedes complete preflight and session acquisition.
- all install component callers consume the same session-aware APIs.
- checkout/package/link recovery evidence never exceeds what hashes and observations prove.
</success_criteria>

<output>
Create `.planning/phases/01-safe-operation-boundary/01-14-SUMMARY.md` when done.
</output>
