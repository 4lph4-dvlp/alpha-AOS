---
phase: quick
plan: 260925-r2d
type: execute
autonomous: true
---

# Sync README Docs with CLI Syntax, Promoted Versions, and Update Cadence (R-2D-1)

<objective>
Update both `README.md` (English) and `README.ko.md` (Korean) to maintain complete documentation fidelity with the v0.1.1 production state.

Scope of Documentation Updates:
1. Fix tree policy CLI syntax: replace `alpha-aos tree policy set` with `alpha-aos tree set` and `alpha-aos tree remove` / `alpha-aos tree inspect`.
2. Update Global Stack Matrix versions to reflect Phase 13 promotions: GSD Core `1.14.0` (was `1.12.0`) and Firecrawl Proxy `3.25.2` (was `3.24.0`).
3. Enhance the Updating section:
   - Document direct CLI command `alpha-aos update --apply` alongside PowerShell/POSIX wrapper scripts.
   - Document the automated upstream version check cadence (`dependency-candidate.yml` scheduled every Monday at 03:17 UTC / 12:17 KST, PR automation, and 3-tier CI fixture gates).
</objective>

<constraints>
- Maintain exact 1:1 structural symmetry and heading parity between `README.md` and `README.ko.md`.
- Preserve all existing links, tables, and scenario explanations.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md (English)</name>
  <files>README.md</files>
  <action>
In `README.md`:
1. In Section "Natural Language Workspace Operations" and "Scenario D", correct `tree policy set` to `tree set` and `tree status` to `tree inspect`.
2. In Section "What is Installed (Global Stack Matrix)", update GSD Core to `1.14.0` and Firecrawl to `3.25.2`.
3. In Section "Updating", add `alpha-aos update --apply` CLI usage and document the automated upstream check cadence (scheduled Monday 03:17 UTC, automated PR generation, CI validation, and on-demand workflow_dispatch).
  </action>
  <verify>
    <automated>npm run check && npm run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Update README.ko.md (Korean)</name>
  <files>README.ko.md</files>
  <action>
In `README.ko.md`:
1. In Section "자연어 프로젝트 조작" and "시나리오 D", correct `tree policy set` to `tree set` and `tree status` to `tree inspect`.
2. In Section "설치 구성 요소 목록 (전역 매트릭스)", update GSD Core to `1.14.0` and Firecrawl to `3.25.2`.
3. In Section "업데이트", add `alpha-aos update --apply` CLI usage and document the automated upstream check cadence (매주 월요일 12:17 KST 정기 실행, 자동 PR 생성, 3단계 CI 게이트 검증 및 수동 실행 지원).
  </action>
  <verify>
    <automated>npm run check && npm run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Verify build, test suite, and record summary</name>
  <files>README.md, README.ko.md, .planning/STATE.md</files>
  <action>
1. Run `npm test`.
2. Create `260925-r2d-SUMMARY.md`.
3. Record quick task in `.planning/STATE.md`.
4. Commit and push.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
</task>

</tasks>
