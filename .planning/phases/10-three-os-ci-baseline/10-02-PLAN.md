---
phase: 10-three-os-ci-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/tarball-fixture.test.ts
  - .planning/debug/ci02-windows-alpha-aos-host-leak.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [CI-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "When the packed-lifecycle host snapshot differs, the assertion message names each changed target and lists its added, removed, changed and vanished entries by relative path, kind, size and a 12-hex sha256 prefix, bounded to 50 listed entries plus per-target totals, so a red windows leg is diagnosable from the CI log alone (D-05)."
    - "The drift report never contains file content: a content sentinel written into a changed file does not appear in the report (D-05, T-10-01)."
    - "The oracle verdict is unchanged: the `hostMutationTargets` block is byte-identical to commit c6415a2, `~/.alpha-aos` is still its first entry, and `assert.deepEqual(afterHost, beforeHost, ...)` still compares the same map of target path to aggregate digest computed from the unchanged byte stream (D-02, D-04)."
    - "An entry that vanishes mid-walk (ENOENT) is recorded as a `vanished` entry that keeps the lifecycle test red and is named in the report, instead of erroring with no diff; every other errno still propagates."
    - "`.planning/debug/ci02-windows-alpha-aos-host-leak.md` classifies the windows-latest `~/.alpha-aos` change as a test-isolation leak (D-01 class 3), names the writer (the `mcp-proxy.test.ts` pinned-upstream startups resolving `userStateRoot()` into the real `~/.alpha-aos/mcp-cache/npm-cache`), and records per-entry drift evidence from a local pair reproduction, the CI signature and timing of run 35762417138, and the onset table (D-07, ROADMAP success criterion 3)."
    - "`.planning/REQUIREMENTS.md` CI-02 and ROADMAP Phase 10 success criterion 3 name all three root-cause classes: product isolation leak, test-oracle defect, test-isolation leak (D-01)."
    - "Adjacency edge (CI-02): an entry present in both snapshots at the same relative path is reported as ONE `changed` line when its kind, size or hash differs (a file that became a directory is one changed line, not an added plus removed pair), and is not listed at all when identical."
    - "Empty edge (CI-02): a missing target fingerprints as `absent` exactly as before; an absent-to-present target lists every new entry as `added`; an existing empty directory is not `absent` (its digest is sha256 of the single `dir:` line)."
    - "Ordering edge (CI-02): drift entries are listed in hostMutationTargets order and then by relative path in plain code-unit order, so the first 50 listed entries and the truncation line are identical on every host."
  prohibitions:
    - statement: "MUST NOT loosen, narrow, serialize or reorder the packed-lifecycle host-immutability oracle: no entry removed from `hostMutationTargets`, no change to `hostStateRoot`, no `concurrency: false`, no moving the fixture last, and no `ALPHA_AOS_STATE_DIR`/HOME/USERPROFILE redirect in `.github/workflows/ci.yml` (D-02, D-04)."
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT print file contents of any host or scratch tree in diagnostics, logs, the debug record or the SUMMARY, and MUST NOT upload any `.alpha-aos` tree as a CI artifact (D-05, D-06)."
      category: privacy
      status: resolved
      verification: judgment
    - statement: "MUST NOT run any reproduction or probe against the developer's real home, and MUST NOT delete, clean or overwrite the developer's real `~/.alpha-aos` (including a polluted `mcp-cache`); every run redirects HOME/USERPROFILE to a fresh scratch directory with ALPHA_AOS_STATE_DIR unset."
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT merge any D-06 evidence branch into `main`."
      category: safety
      status: resolved
      verification: judgment
  artifacts:
    - path: "test/tarball-fixture.test.ts"
      provides: "fingerprintManifest, snapshotTargets, hostSnapshot(manifests), describeHostDrift, vanishedEntryCount, and three unit tests for the drift report; lifecycle assertion message now carries the per-entry drift"
      contains: "describeHostDrift(beforeManifests, afterManifests)"
    - path: ".planning/debug/ci02-windows-alpha-aos-host-leak.md"
      provides: "CI-02 root-cause record: classification, writer, per-entry evidence, onset, follow-ups; consumed by 10-03 (RED observations), 10-04 (resolution) and Phase 11 (LIFE-03/LIFE-04)"
      contains: "test-isolation leak"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CI-02 wording naming three root-cause classes"
      contains: "test-isolation leak"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 10 success criterion 3 naming three root-cause classes"
      contains: "test-isolation leak"
  key_links:
    - from: "test/tarball-fixture.test.ts packed release lifecycle test"
      to: "describeHostDrift"
      via: "assert.deepEqual(afterHost, beforeHost, describeHostDrift(beforeManifests, afterManifests))"
      pattern: "assert\\.deepEqual\\(afterHost, beforeHost, describeHostDrift\\(beforeManifests, afterManifests\\)\\)"
    - from: "hostSnapshot"
      to: "hostMutationTargets"
      via: "snapshotTargets(hostMutationTargets, manifests)"
      pattern: "snapshotTargets\\(hostMutationTargets"
    - from: ".planning/debug/ci02-windows-alpha-aos-host-leak.md"
      to: "test/mcp-proxy.test.ts startPinnedServer -> src/core/mcp-proxy.ts upstreamEnvironmentPolicy -> proxyCacheRoot -> src/core/paths.ts userStateRoot"
      via: "writer chain recorded as the diagnosed cause"
      pattern: "upstreamEnvironmentPolicy"
---

<objective>
Investigate CI-02 before fixing it: make the host-immutability oracle self-diagnosing, reproduce the windows-latest failure locally on a redirected home, and record the classified root cause.

In CI run 35762417138 (main @ c6415a2) only the aggregate hash of the real `C:\Users\runneradmin\.alpha-aos` moved across the packed lifecycle; the log carried two opaque hashes and nothing else. 10-RESEARCH.md reproduced the exact signature locally by running `mcp-proxy.test.js` and `tarball-fixture.test.js` concurrently: the pinned-upstream startups write the npx cache into the real host `~/.alpha-aos/mcp-cache/npm-cache` because the test never redirects `ALPHA_AOS_STATE_DIR`, while the fixture alone writes nothing.

Purpose: D-05 makes the next red windows leg diagnosable from its log; D-07 turns the research finding into a recorded, evidence-backed classification (D-01 three classes) that 10-03 fixes, 10-04 closes, and Phase 11 cites. This plan changes no leaking test and no product code.

Output: per-entry drift reporting in test/tarball-fixture.test.ts with unit coverage, the debug record `.planning/debug/ci02-windows-alpha-aos-host-leak.md`, and the three-class wording in REQUIREMENTS.md and ROADMAP.md.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-three-os-ci-baseline/10-CONTEXT.md
@.planning/phases/10-three-os-ci-baseline/10-RESEARCH.md
@.planning/phases/10-three-os-ci-baseline/10-PATTERNS.md
@.planning/phases/10-three-os-ci-baseline/10-VALIDATION.md
@.planning/debug/uat-safety-boundary-parallel.md
@test/tarball-fixture.test.ts
@src/core/paths.ts
@src/core/mcp-proxy.ts
</context>

<interfaces>
Existing (test/tarball-fixture.test.ts):
- Lines 42-44: `hostHome = homedir()`, `hostStateRoot = resolve(process.env.ALPHA_AOS_STATE_DIR?.trim() || join(hostHome, ".alpha-aos"))`, `hostCodexRoot`. Unchanged.
- Lines 151-172: `async function fingerprint(path: string): Promise<string>` — returns `absent` when the path does not exist; otherwise walks with lstat, sorted readdir (localeCompare), feeding the aggregate sha256 with `link:REL:TARGET` + newline, `dir:REL` + newline, `file:REL:SIZE` + newline and then the file bytes when size is at most 1024 * 1024. REL is empty for the root and slash-joined below it.
- Lines 174-183: `const hostMutationTargets = [ ... ];` — 10 lines from the `const` line through `];`, eight targets, `hostStateRoot` first. Byte-identical after this plan.
- Lines 185-187: `hostSnapshot()` returns a map of target path to fingerprint.
- Line 353: test `packed release completes the isolated install, reconcile, diagnose, and uninstall lifecycle` (timeout 900_000); line 354 takes `beforeHost`; lines 546-547 take `afterHost` and assert with the message `the packed lifecycle must not change any real managed host path`.

New (this plan, all module-local to test/tarball-fixture.test.ts):
- `interface HostEntry { readonly path: string; readonly kind: "dir" | "file" | "link" | "vanished"; readonly size: number | null; readonly sha256: string | null }` — path is the slash-joined relative path, empty string for the target root.
- `interface HostFingerprint { readonly digest: string; readonly entries: readonly HostEntry[] }`
- `async function fingerprintManifest(path: string): Promise<HostFingerprint>`
- `async function snapshotTargets(targets: readonly string[], manifests?: Map of string to HostFingerprint): Promise of Map of string to string`
- `async function hostSnapshot(manifests?: Map of string to HostFingerprint)` — same return type as today; calls `snapshotTargets(hostMutationTargets, manifests)`.
- `const HOST_DRIFT_LISTING_LIMIT = 50;` and `const HOST_DRIFT_HASH_PREFIX = 12;`
- `function describeHostDrift(before, after, limit = HOST_DRIFT_LISTING_LIMIT): string` over two manifest maps.
- `function vanishedEntryCount(manifests): number`

Local reproduction environment (Git Bash on the Windows developer host; research-verified, never the real home):
- scratch home: `H=$(mktemp -d)`, `WH=$(cygpath -w "$H")`
- prefix: `env -u ALPHA_AOS_STATE_DIR -u CODEX_HOME -u CLAUDE_CONFIG_DIR -u HERMES_HOME -u PI_CODING_AGENT_DIR -u ANTIGRAVITY_CONFIG_DIR HOME="$WH" USERPROFILE="$WH" ALPHA_AOS_REQUIRE_UPSTREAM_MCP=1`
- On Windows `os.homedir()` follows USERPROFILE, so every host target resolves inside the scratch home.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — per-entry host fingerprint manifest wired into the lifecycle assertion message, proven on synthetic trees</name>
  <files>test/tarball-fixture.test.ts</files>
  <read_first>
    test/tarball-fixture.test.ts (lines 1-11 imports, 40-44 host roots, 151-190 fingerprint/hostMutationTargets/hostSnapshot, 353-356 and 540-550 the lifecycle test ends)
    .planning/phases/10-three-os-ci-baseline/10-RESEARCH.md (Pattern 4, Pitfall 3, Security Domain)
    .planning/phases/10-three-os-ci-baseline/10-PATTERNS.md (tarball-fixture section)
  </read_first>
  <behavior>
    - Test `host fingerprint keeps the aggregate digest byte stream`: a scratch directory holding one file `a.txt` with content `x` fingerprints to the sha256 of the bytes `dir:` newline, `file:a.txt:1` newline, `x`; its manifest has a root `dir` entry and a `file` entry of size 1 with the full sha256 of `x`; an empty directory is not `absent`; a missing path is `absent` with no entries.
    - Test `host drift report names changed entries by metadata and never by content`: across two synthetic targets (one existing, one absent before and created after), the report lists `added`, `removed` and `changed` entries by relative path with kind, size and 12-hex hash prefixes, reports a file that became a directory as one `changed` line, shows `absent` for the created target's before side, omits the unchanged file, and contains neither content sentinel.
    - Test `host drift report bounds its listing and keeps per-target totals`: an empty target that gains 60 files lists exactly 50 `added` entry lines in code-unit order starting at the smallest name, then a line containing `10 more entries not listed`, and its header totals read `added 60`.
  </behavior>
  <action>
    Implement D-05 in test/tarball-fixture.test.ts without changing the oracle's verdict (D-02, D-04).

    1. Replace `fingerprint` with `fingerprintManifest` (interfaces block). Keep the traversal and the aggregate hash input byte-for-byte as today: same lstat, same sorted readdir with the same `localeCompare` comparator, same `link:`, `dir:` and `file:` lines, same file bytes for files up to 1 MiB, same `absent` result for a missing path. Alongside the aggregate hash, push one HostEntry per visited node: `link` with size equal to the UTF-8 byte length of the link target and sha256 of the target string; `dir` with size and sha256 null; `file` with its size and the sha256 of the same bytes when at most 1 MiB, else null. Hash the bytes and discard them; never keep content.
    2. Tolerate exactly one errno during the walk: when lstat, readdir, readFile or readlink of a node raises ENOENT (another process removed it mid-walk, 10-RESEARCH Pitfall 3), push a `vanished` entry for that relative path, feed `vanished:REL` plus a newline to the aggregate hash, and continue. Rethrow every other error unchanged (fail closed).
    3. Add `snapshotTargets` and rewrite `hostSnapshot` to call `snapshotTargets(hostMutationTargets, manifests)`, returning the same map of target to digest it returns today. Leave the `const hostMutationTargets = [` block, `hostHome`, `hostStateRoot` and `hostCodexRoot` untouched.
    4. Add `HOST_DRIFT_LISTING_LIMIT`, `HOST_DRIFT_HASH_PREFIX` and `describeHostDrift`. Output: first line `the packed lifecycle must not change any real managed host path`; then, for each target (in the before map's key order) whose digest differs or that holds a vanished entry, a header `TARGET: BEFORE -> AFTER (added A, removed R, changed C, vanished V)` where BEFORE/AFTER are the first 12 hex of the digest or `absent`; then all differing entries across those targets, ordered by target order and then relative path in plain code-unit order (not localeCompare), truncated to `limit`, one per line as two spaces, the status word (`added`, `removed`, `changed`, `vanished`), the relative path (`.` for the target root), and each side as KIND/SIZE/HASH12 with `-` for a missing side or a null value; then, when truncated, a line `  ... N more entries not listed (limit L)`. An entry is `changed` when both sides have it and kind, size or sha256 differ. Print metadata only.
    5. Add `vanishedEntryCount`. In the lifecycle test: create `beforeManifests` before `const beforeHost = await hostSnapshot(beforeManifests);` (keep it as the test's first statement), create `afterManifests` before `const afterHost = await hostSnapshot(afterManifests);`, and replace the assertion message argument so the line reads `assert.deepEqual(afterHost, beforeHost, describeHostDrift(beforeManifests, afterManifests));`. Directly after it add `assert.equal(vanishedEntryCount(beforeManifests) + vanishedEntryCount(afterManifests), 0, describeHostDrift(beforeManifests, afterManifests));`. Nothing else in the lifecycle test changes.
    6. Add the three unit tests from the behavior block, placed before the lifecycle test, using `mkdtemp(join(tmpdir(), "alpha-aos-drift-"))` scratch roots removed in `context.after` (same rm options as `createIsolatedSandbox`). Use content sentinels `alphaAOSdriftSentinel0033` (before) and `alphaAOSdriftSentinel0044` (after) in the changed file and assert neither appears in the report. Do not create symlinks (Windows runners may lack the privilege). Build roots only from `join(tmpdir(), ...)` and mkdtemp results; add no platform-absolute string literal (the 10-01 guard scans this file).
  </action>
  <verify>
    <automated>npm run check && npm run build && node --test --test-name-pattern="host (fingerprint|drift)" dist/test/tarball-fixture.test.js</automated>
    <fails_when>non-zero exit, a `not ok` line, `# fail` above 0, or `# pass` below 3 in the summary</fails_when>
    <automated>diff <(git show c6415a2:test/tarball-fixture.test.ts | sed -n '/^const hostMutationTargets = \[/,/^\];/p') <(sed -n '/^const hostMutationTargets = \[/,/^\];/p' test/tarball-fixture.test.ts) && test "$(sed -n '/^const hostMutationTargets = \[/,/^\];/p' test/tarball-fixture.test.ts | wc -l)" -eq 10</automated>
    <fails_when>diff prints any line (the targets block changed), or the extracted block is not exactly 10 lines (an empty extraction would make the diff pass vacuously)</fails_when>
    <automated>test "$(grep -c 'assert.deepEqual(afterHost, beforeHost, describeHostDrift(beforeManifests, afterManifests));' test/tarball-fixture.test.ts)" -eq 1 && grep -qF 'const hostStateRoot = resolve(process.env.ALPHA_AOS_STATE_DIR?.trim() || join(hostHome, ".alpha-aos"));' test/tarball-fixture.test.ts</automated>
    <fails_when>the deepEqual line count is not 1, or the hostStateRoot definition line is missing or altered</fails_when>
  </verify>
  <acceptance_criteria>
    - The three unit tests named in the behavior block exist verbatim and pass.
    - The `hostMutationTargets` block is byte-identical to c6415a2 and exactly 10 lines; `hostStateRoot` is still its first entry.
    - The lifecycle test's first statement still takes the before snapshot, and its host assertion reads `assert.deepEqual(afterHost, beforeHost, describeHostDrift(beforeManifests, afterManifests));`.
    - `grep -c 'vanished:' test/tarball-fixture.test.ts` is at least 1, and ENOENT is the only error code the walk tolerates (every other code rethrows).
    - `describeHostDrift` output contains no file bytes: the content sentinel assertion passes.
    - The full lifecycle test is not run in this task (the name filter excludes it); it runs in Task 2's reproduction.
  </acceptance_criteria>
  <done>The host snapshot carries a per-entry manifest, a mismatch message names changed entries by metadata only, the verdict operands are unchanged, and three unit tests prove the report on synthetic trees.</done>
</task>

<task type="auto">
  <name>Task 2: Reproduce the windows signature on a redirected home, classify the cause, and write the debug record and three-class wording</name>
  <precondition>The npm registry is reachable from the developer host (the pair reproduction downloads the pinned MCP packages cold into the scratch home), and `gh auth status` exits 0 (needed only for the D-06 fallback).</precondition>
  <files>.planning/debug/ci02-windows-alpha-aos-host-leak.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    .planning/phases/10-three-os-ci-baseline/10-RESEARCH.md (Findings 3-7, Code Examples, Assumptions A2 and A4, Open Questions 1-2)
    .planning/debug/uat-safety-boundary-parallel.md (front matter and section convention)
    .planning/REQUIREMENTS.md (CI-02 line 11)
    .planning/ROADMAP.md (Phase 10 success criterion 3, line 49)
    src/core/mcp-proxy.ts (proxyCacheRoot and upstreamEnvironmentPolicy, lines 199-246)
    src/core/paths.ts (userStateRoot, lines 93-97)
    src/core/gate-receipt.ts (lines 105-108)
    test/mcp-proxy.test.ts (startPinnedServer 702-753, the three startup tests 812-852)
  </read_first>
  <action>
    Collect the D-07 evidence with Task 1's diagnostics against the UNFIXED leaking tests (this plan does not edit mcp-proxy, gate-engine or gate-lifecycle). Every run uses a fresh scratch home and the environment prefix from the interfaces block; never the real home, and never delete anything under the real `~/.alpha-aos`.

    1. Pair reproduction (D-05): `npm run build`, then with a fresh scratch home run `node --test --test-concurrency=2 dist/test/mcp-proxy.test.js dist/test/tarball-fixture.test.js` under the prefix, saving stdout and stderr to a log in the session scratchpad. Expected per 10-RESEARCH Finding 4: exactly one failure, `packed release completes the isolated install, reconcile, diagnose, and uninstall lifecycle`, whose message is now a drift report naming only the scratch home's `.alpha-aos` target, with entries under `mcp-cache/npm-cache/` (`_npx`, `_cacache`, `_update-notifier-last-checked`), while the other seven targets are unchanged. Afterwards list the scratch home with `find "$H" -mindepth 1 -maxdepth 4` (paths only). If a run reports fail 0 (the overlap is timing-dependent), repeat with a new scratch home, up to three runs in total.
    2. D-06 fallback, only if all three local runs report fail 0: push the current HEAD (which carries Task 1's diagnostics) to a separate branch named `ci02-evidence`, trigger `gh workflow run CI --ref ci02-evidence`, watch the run, and copy the windows leg's drift report from `gh run view RUN --log`. Add no artifact upload step. Delete the remote branch afterwards and never merge it into `main`. If CI does not reproduce either, record that the per-entry evidence is the mechanism plus the research-session reproduction plus the run 35762417138 timing (Assumption A4).
    3. Control run: with another fresh scratch home, run `node --test dist/test/tarball-fixture.test.js` alone under the prefix. Expected: pass, and `test ! -e "$H/.alpha-aos"` holds (the released lifecycle alone writes nothing, 10-RESEARCH Finding 3).
    4. Classify per D-01. Test-isolation leak (class 3) when the drift is confined to entries the foreign test writes and the control run writes nothing; then D-02 governs the fix in 10-03 and the D-03 `src/` audit is not triggered. If instead the control run changes the scratch `.alpha-aos`, the cause is a product isolation leak: record the evidence, stop, and return a checkpoint to the orchestrator, because D-03 (reversibility: costly, it audits every path-resolution site in `src/core/`) needs its own plan and 10-03 does not apply. If the digests differ while the report names no changed entry, treat it as a test-oracle defect under D-04: record the evidence and stop the same way. Bisecting 638d3cc..f1ca570 is not done (Claude's discretion): the failure is timing-dependent and the leak predates the window, so record the onset table instead.
    5. Write `.planning/debug/ci02-windows-alpha-aos-host-leak.md` following the uat-safety-boundary-parallel.md convention: YAML front matter with `status: diagnosed`, a `trigger` naming CI run 35762417138 (main @ c6415a2, windows-latest, only the real runner home `.alpha-aos` hash moved while the seven other targets stayed absent) using YAML-safe quoting, `created` and `updated` dates. Then these sections:
       - `## Classification` — the line `classification: test-isolation leak` plus why it is not a product isolation leak (control run writes nothing; `userStateRoot()` honors ALPHA_AOS_STATE_DIR) and not a test-oracle defect (bytes really changed on the host).
       - `## Writer` — the chain `mcp-proxy.test.ts` startPinnedServer, then `upstreamEnvironmentPolicy` (src/core/mcp-proxy.ts:226-246, write root taken from the ambient process environment), then `proxyCacheRoot()`, then `userStateRoot()` (src/core/paths.ts:93-97), landing in the real `~/.alpha-aos/mcp-cache/npm-cache`; the overlapping foreign test in run 35762417138 (firecrawl startup 17:45:36.6 to 17:47:32.4 against the fixture's 17:47:05.5 to 17:49:35.3); and the two same-class writers `gate-engine.test.ts` (direct `writeGateReceipt`, src/core/gate-receipt.ts:106-108 hard-codes `userStateRoot()`) and `gate-lifecycle.test.ts` (spawned `gate check`, whose `commandProbeEnvironment` does not pass ALPHA_AOS_STATE_DIR, src/core/process.ts:922-958).
       - `## Evidence` — the pair reproduction command and TAP summary, the drift report excerpt exactly as printed (metadata only), the scratch-home listing, the control run result, the CI signature hashes from run 35762417138 (10-RESEARCH Finding 4), the CI timing table (Finding 5), the per-file home-write probe table from the research session (Finding 3, including the capability-oracle and preview rows), and the warm-cache corroboration.
       - `## Onset` — the Finding 6 table and the no-bisect rationale (leak introduced at 4cf9eaa, 3f77c5f and f15ad5c, all before the window).
       - `## Regression RED observations (D-08)` — one sentence stating that plan 10-03 records each regression's failing observation (command, output excerpt, RED commit) here.
       - `## Follow-ups (outside Phase 10)` — `writeGateReceipt` has no `stateRoot` option unlike every other writer; `upstreamEnvironmentPolicy` derives its write root from the ambient environment instead of its `source` argument; the capability-oracle dev-host home writes (Assumption A2) as an open audit item; the CI per-entry bytes remain inferred until a future red windows leg prints the D-05 report (Assumption A4).
    6. D-01 wording: in `.planning/REQUIREMENTS.md` CI-02, change `the root cause is classified as a product isolation leak or a test-oracle defect` to `the root cause is classified as a product isolation leak, a test-oracle defect, or a test-isolation leak (another test file writing the real host state during the lifecycle's snapshot window)`. In `.planning/ROADMAP.md` Phase 10 success criterion 3, change `classified as a product isolation leak or a test-oracle defect` to `classified as a product isolation leak, a test-oracle defect, or a test-isolation leak`. Use scoped edits; change nothing else in either file.
  </action>
  <verify>
    <automated>D=.planning/debug/ci02-windows-alpha-aos-host-leak.md && grep -q '^status: diagnosed' "$D" && grep -q '^classification: test-isolation leak' "$D" && grep -q 'mcp-cache/npm-cache' "$D" && grep -q '_npx' "$D" && grep -q '35762417138' "$D" && grep -q 'upstreamEnvironmentPolicy' "$D" && grep -q '## Regression RED observations (D-08)' "$D"</automated>
    <fails_when>any grep exits non-zero: the record is missing its status, classification line, writer path, per-entry evidence, run id, writer chain, or the D-08 section</fails_when>
    <automated>grep -n 'CI-02' .planning/REQUIREMENTS.md | grep -q 'test-isolation leak' && sed -n '/^### Phase 10:/,/^### Phase 11:/p' .planning/ROADMAP.md | grep -q 'a test-oracle defect, or a test-isolation leak'</automated>
    <fails_when>either grep exits non-zero (CI-02 or Phase 10 success criterion 3 still names only two classes)</fails_when>
  </verify>
  <acceptance_criteria>
    - The debug record exists with `status: diagnosed`, `classification: test-isolation leak`, and the six sections named in the action.
    - Its evidence section quotes a drift report that names entries under `mcp-cache/npm-cache/` with relative path, kind, size and hash prefix, and contains no file content.
    - The control run of `tarball-fixture.test.js` alone passed and left no `.alpha-aos` in its scratch home; the record says so.
    - REQUIREMENTS.md CI-02 and ROADMAP Phase 10 success criterion 3 name all three classes; `git diff --stat` shows those two files with small, scoped changes only.
    - No command in this task used the real home; the developer's real `~/.alpha-aos` was not read into any log and not modified or deleted.
    - If the classification is not class 3, the task stopped with a checkpoint instead of continuing, and the record states the class and evidence.
  </acceptance_criteria>
  <done>The windows signature is reproduced (or the D-06 fallback outcome is recorded), the cause is classified with evidence in the debug record, and the three-class wording is in REQUIREMENTS.md and ROADMAP.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host managed state → CI log | The drift report reads host state trees and prints into a public CI log |
| test process → real user home | A test that resolves `userStateRoot()` without a redirect writes the developer's or runner's real managed state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Information disclosure | describeHostDrift in test/tarball-fixture.test.ts | high | mitigate | Report prints relative path, kind, size and a 12-hex hash prefix only; content bytes are hashed and discarded; a unit test asserts two content sentinels never appear; listing bounded to 50 entries; no CI artifact upload of any `.alpha-aos` tree (D-06) |
| T-10-02 | Tampering | local reproduction and control runs | high | mitigate | Every run redirects HOME/USERPROFILE to a fresh scratch directory and unsets ALPHA_AOS_STATE_DIR and the harness root names; nothing under the real `~/.alpha-aos` is deleted or rewritten |
| T-10-03 | Repudiation | host-immutability oracle | high | mitigate | `hostMutationTargets` byte-identical to c6415a2 (diff gate), deepEqual operands unchanged, vanished entries keep the test red |
| T-10-06 | Tampering | D-06 evidence branch | medium | mitigate | Branch `ci02-evidence` is pushed only if local reproduction fails, carries no artifact upload, is deleted after capture, and is never merged into main |
</threat_model>

<verification>
- `node --test --test-name-pattern="host (fingerprint|drift)" dist/test/tarball-fixture.test.js` passes 3 tests.
- The oracle-unchanged diff gate and deepEqual-line gate pass.
- The debug record and the three-class wording pass their grep gates.
- The pair reproduction log (scratchpad) shows the D-05 drift report naming `mcp-cache/npm-cache` entries, or the D-06 fallback outcome is recorded.
</verification>

<success_criteria>
- ROADMAP Phase 10 success criterion 3 holds: the windows `~/.alpha-aos` change has a recorded root cause, classified under the D-01 three-class taxonomy, with the evidence that decided it (which entries changed and which foreign test wrote them).
- D-05 diagnostics are live for every future CI run; the oracle's verdict is unchanged.
</success_criteria>

## Artifacts this phase produces

- In `test/tarball-fixture.test.ts`: `interface HostEntry`, `interface HostFingerprint`, `fingerprintManifest`, `snapshotTargets`, `hostSnapshot(manifests)`, `HOST_DRIFT_LISTING_LIMIT` (50), `HOST_DRIFT_HASH_PREFIX` (12), `describeHostDrift`, `vanishedEntryCount`; tests `host fingerprint keeps the aggregate digest byte stream`, `host drift report names changed entries by metadata and never by content`, `host drift report bounds its listing and keeps per-target totals`.
- Drift report vocabulary: status words `added`, `removed`, `changed`, `vanished`; aggregate hash line `vanished:REL`.
- New file `.planning/debug/ci02-windows-alpha-aos-host-leak.md` with sections `## Classification`, `## Writer`, `## Evidence`, `## Onset`, `## Regression RED observations (D-08)`, `## Follow-ups (outside Phase 10)` and the marker line `classification: test-isolation leak`.
- Wording: `test-isolation leak` added to REQUIREMENTS.md CI-02 and ROADMAP Phase 10 success criterion 3.

<output>
Create `.planning/phases/10-three-os-ci-baseline/10-02-SUMMARY.md` when done. Include the pair reproduction command and TAP summary, the classification, and whether the D-06 fallback was needed.
</output>
