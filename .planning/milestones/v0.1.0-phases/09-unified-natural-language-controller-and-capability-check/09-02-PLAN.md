---
phase: 09-unified-natural-language-controller-and-capability-check
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - docs/codex-execution-policy.md
  - test/control.test.ts
  - test/pack-advisor.test.ts
  - README.md
  - README.ko.md
autonomous: true
requirements: [CTRL-01, CTRL-02, CTRL-03]

must_haves:
  truths:
    - "docs/codex-execution-policy.md embeds the capability checkpoint rule while strictly respecting the <=2000 character limit without ownership markers (CTRL-02)."
    - "test/control.test.ts provides complete behavioral coverage for all 5 domains: capability pack advisory/sync, tree-off isolation, tree-inherit, diagnostics (status/doctor), and safe rollback/repair (CTRL-01, CTRL-02, CTRL-03)."
    - "README.md and README.ko.md document alpha-aos-control and its natural language control scenarios in both English and Korean (CTRL-01, CTRL-03)."
    - "All 900+ repository tests pass cleanly, and the updated skill is materialized to user harness roots via install --apply (CTRL-01)."
  prohibitions:
    - statement: "MUST NOT exceed 2000 characters in docs/codex-execution-policy.md (CTRL-02)."
      category: architectural
      status: resolved
      verification: automated
    - statement: "MUST NOT allow silent unconfirmed mutations on any natural language command (CTRL-01)."
      category: safety
      status: resolved
      verification: automated
  artifacts:
    - path: "docs/codex-execution-policy.md"
      provides: "Codex execution guidance including the project setup capability checkpoint rule"
    - path: "test/control.test.ts"
      provides: "Comprehensive E2E test suite for alpha-aos-control and natural language operations"
    - path: "README.md"
      provides: "Updated English documentation referencing alpha-aos-control"
    - path: "README.ko.md"
      provides: "Updated Korean documentation referencing alpha-aos-control"
---

<objective>
Embed the capability checkpoint into agent execution policy, author comprehensive E2E control tests in `test/control.test.ts`, update bilingual documentation, verify zero regression with `npm test`, and deploy the unified skill to local harnesses.

Purpose: Provide airtight execution guidance and automated verification for the unified `alpha-aos-control` skill, ensuring agents automatically trigger capability checks upon environment setup and execute natural language management commands reliably.
Output: Updated `docs/codex-execution-policy.md`, new `test/control.test.ts`, updated `README.md` and `README.ko.md`, and live deployed skills across harnesses.
</objective>

<execution_context>
Execution via GSD workflow adhering to anti-shallow deep work rules.
</execution_context>

<context>
@.planning/phases/09-unified-natural-language-controller-and-capability-check/09-CONTEXT.md
@.planning/phases/09-unified-natural-language-controller-and-capability-check/09-RESEARCH.md
@.planning/phases/09-unified-natural-language-controller-and-capability-check/09-VALIDATION.md
@docs/codex-execution-policy.md
@README.md
@README.ko.md
@test/pack-advisor.test.ts
</context>

<tasks>

<task id="09-02-01" wave="2">
<title>Task 1: Add Capability Checkpoint Rule to docs/codex-execution-policy.md</title>
<read_first>
- docs/codex-execution-policy.md
- src/core/codex-policy.ts
</read_first>
<action>
1. Edit `docs/codex-execution-policy.md`:
   - Add the capability checkpoint bullet:
     `- At project setup completion (manifests, dependencies, or scaffolding), run alpha-aos-control to evaluate project capability packs before continuing.`
   - Carefully tighten existing prose so the total character count remains safely <= 2000 (around 1850-1920 chars).
2. Validate using node script that character count is between 1 and 2000.
</action>
<verify>
<automated>node -e "const fs = require('fs'); const s = fs.readFileSync('docs/codex-execution-policy.md', 'utf8'); console.log('Length:', s.length); if (s.length < 1 || s.length > 2000) process.exit(1);"</automated>
</verify>
<acceptance_criteria>
- `docs/codex-execution-policy.md` contains the capability checkpoint rule.
- Character count is strictly <= 2000.
</acceptance_criteria>
</task>

<task id="09-02-02" wave="2">
<title>Task 2: Author test/control.test.ts & Remove test/pack-advisor.test.ts</title>
<read_first>
- test/pack-advisor.test.ts
- src/core/tree-policy.ts
- src/core/project-plan.ts
- src/core/doctor.ts
- src/core/transaction.ts
</read_first>
<action>
1. Create `test/control.test.ts`:
   - Subsume all pack advisory tests from `test/pack-advisor.test.ts`:
     - Test `skills/alpha-aos-control/SKILL.md` exists and contains instructions for all 5 domains.
     - Test catalog and lock declare `alpha-aos-control` across all 5 harnesses with `automatic` invocation.
     - Test autonomous workflow: plan -> approve with exact 64-char digest -> sync -> verify `CURRENT`.
     - Test security invariants: tampered digest rejection, unapproved sync rejection, workspace drift rejection.
     - Test CLI `project plan . --json` output.
   - Add Natural Language Control domain tests:
     - Test Directory Tree Policy: setting mode `off` via tree policy updates policy store without writing any files into the target repository.
     - Test Directory Tree Policy: setting mode `inherit` restores inheritance.
     - Test Diagnostics: `status` and `doctor` report cleanly.
     - Test Rollback & Repair: `rollback` rejects drifted files and succeeds on clean preflight; `repair` handles recovery safely.
2. Remove `test/pack-advisor.test.ts`.
</action>
<verify>
<automated>npm run check && npm run build && node scripts/run-tests.mjs --files dist/test/control.test.js</automated>
</verify>
<acceptance_criteria>
- `test/control.test.ts` passes with 100% assertions green.
- `test/pack-advisor.test.ts` is cleanly removed.
</acceptance_criteria>
</task>

<task id="09-02-03" wave="2">
<title>Task 3: Update Bilingual Documentation (README.md, README.ko.md) & Live Deploy</title>
<read_first>
- README.md
- README.ko.md
</read_first>
<action>
1. Update `README.md`:
   - Replace references to `alpha-aos-pack-advisor` with `alpha-aos-control`.
   - Update agent scenario descriptions to explain the unified controller, 2 -> 3 trigger checkpoint, and natural language tree-off/status/rollback commands.
2. Update `README.ko.md`:
   - Replace references to `alpha-aos-pack-advisor` with `alpha-aos-control`.
   - Update Korean agent scenarios and natural language instructions.
3. Run full test suite:
   ```bash
   npm test
   ```
4. Run live installation to materialize `alpha-aos-control` to user harness roots:
   ```bash
   node dist/src/cli.js install --apply --target antigravity,codex,claude,pi
   ```
5. Confirm that `~/.gemini/config/skills/alpha-aos-control/SKILL.md` exists and is up to date.
</action>
<verify>
<automated>npm test</automated>
</verify>
<acceptance_criteria>
- `npm test` passes all tests with zero errors.
- `README.md` and `README.ko.md` accurately document `alpha-aos-control`.
- `alpha-aos-control` is deployed to host harness skill directories.
</acceptance_criteria>
</task>

</tasks>
