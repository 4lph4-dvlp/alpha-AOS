---
phase: 01-safe-operation-boundary
plan: 08
type: execute
wave: 2
depends_on: [01-02, 01-03]
files_modified:
  - src/core/process.ts
  - src/core/mcp-proxy.ts
  - src/core/inventory.ts
  - src/adapters/harnesses.ts
  - src/adapters/isolation.ts
  - test/process.test.ts
autonomous: true
requirements: [SAFE-04, SAFE-06]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "SAFE-06: one async ProcessSpec adapter uses an absolute executable, argv array, canonical cwd, shell:false, timeout, byte cap, and operation-specific environment-name allowlist."
    - "D-12: failed processes return only redacted bounded head/tail excerpts, whole-output fingerprints, exit code, cap and timeout metadata."
    - "Inventory and harness/isolation probes plus the SDK-backed MCP filter proxy use the same explicit environment-name, deadline, output-bound, fingerprint, and redaction policy; unsupported transports are reported rather than silently inheriting host state."
  artifacts:
    - path: src/core/process.ts
      provides: "Unified shell-free captured, interactive, and protocol-session process adapter"
      exports: [normalizeProcessSpec, runProcess, openProtocolProcess]
    - path: src/core/mcp-proxy.ts
      provides: "SDK-compatible MCP client transport backed by the bounded ProcessSpec protocol session"
  key_links:
    - from: src/adapters/harnesses.ts
      to: src/core/process.ts
      via: "probe executable/argv/env-name declarations"
      pattern: "runProcess|ProcessSpec"
    - from: src/core/process.ts
      to: src/core/redaction.ts
      via: "redaction before result/error construction"
      pattern: "createRedactedExcerpt"
    - from: src/core/mcp-proxy.ts
      to: src/core/process.ts
      via: "SDK transport adapter over an approved ProcessSpec protocol session"
      pattern: "openProtocolProcess|ProcessSpec"
  prohibitions:
    - statement: "A command adapter MUST NOT silently fall back to ComSpec, `/bin/sh`, or an inherited environment when an executable shim or required variable set is unsupported."
      requirement_id: SAFE-06
      category: safety
      status: unverified
      flagged: true
---

<objective>
기존 process helpers를 shell-free, bounded, timeout-aware, allowlisted adapter로 통합한다.

Purpose: command/data separation과 diagnostic redaction을 subprocess boundary 자체에서 보장한다.
Output: ProcessSpec/ProcessResult API and migrated inventory/harness/isolation probes.
</objective>

<execution_context>
@C:/Users/alpha/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safe-operation-boundary/01-CONTEXT.md
@.planning/phases/01-safe-operation-boundary/01-RESEARCH.md
@.planning/phases/01-safe-operation-boundary/01-PATTERNS.md
@.planning/phases/01-safe-operation-boundary/01-02-SUMMARY.md
@.planning/phases/01-safe-operation-boundary/01-03-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One captured child의 safe ProcessSpec tracer</name>
  <files>src/core/process.ts, test/process.test.ts</files>
  <read_first>
    src/core/process.ts
    src/core/paths.ts
    src/core/redaction.ts
    test/process.test.ts
  </read_first>
  <behavior>
    - a controlled Node child receives exact argv and approved environment names only with shell:false.
    - stdout/stderr are streamed into SHA-256 and bounded redacted head/tail buffers.
    - timeout/output cap terminate the child tree and return stable metadata without raw output.
  </behavior>
  <action>SAFE-06과 D-12에 따라 `ProcessSpec`, `ProcessResult`, `ProcessFailure`, `EnvironmentPolicy`를 정의하고 async `runProcess` tracer를 구현한다. executable은 absolute/proven path, cwd는 canonical directory, args는 string array, environment는 explicit required/optional names plus caller source로 받는다. Windows key names를 uppercase compare해 collision을 거부한다. stdout/stderr raw chunks는 streaming hash와 bounded overlap redactor에만 넣고 반환/throw에 raw strings를 두지 않는다.</action>
  <acceptance_criteria>argv injection sentinel, disallowed env sentinel, split secret, output cap, timeout child-tree cases all pass and ProcessResult has only coded bounded metadata.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/process.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, a sentinel side effect exists, or a disallowed environment value appears</fails_when>
  </verify>
  <reversibility rating="reversible">legacy exports remain compatibility wrappers until Plan 01-11 migrates callers.</reversibility>
  <done>one real child is executed end-to-end without shell or ambient environment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shim normalization, interactive, protocol-session 및 MCP proxy transport 완성</name>
  <files>src/core/process.ts, src/core/mcp-proxy.ts, test/process.test.ts</files>
  <read_first>
    src/core/process.ts
    src/core/mcp-proxy.ts
    src/core/mcp-fixture.ts
    src/cli.ts
    test/process.test.ts
  </read_first>
  <behavior>
    - npm/npx Windows cmd shims normalize to process.execPath plus exact npm CLI script.
    - arbitrary cmd/bat is unsupported with stable code and no ComSpec spawn.
    - interactive mode inherits approved stdio but not ambient env; protocol mode enforces same deadline/cap/redaction policy.
    - the Firecrawl filter proxy never calls getDefaultEnvironment or inherits stderr; its SDK client transport uses an explicit case-normalized environment-name allowlist, hashes the complete upstream protocol/stderr byte streams, and retains only parsed bounded protocol messages plus redacted bounded stderr evidence.
    - connect, initialize, forwarded request, idle, and shutdown deadlines terminate the upstream child tree and return stable timeout/cap/exit/fingerprint metadata without raw output.
  </behavior>
  <action>`normalizeProcessSpec`, `runProcess` modes, `openProtocolProcess`를 완성한다. 기존 npm/npx node-cli resolution을 보존하고 other cmd/bat와 relative/path-search ambiguity는 stable unsupported code로 거부한다. Interactive child exit status/signal을 반환하고 protocol session은 stdin writes plus per-message/absolute deadlines를 제공하되 stdout protocol bytes와 stderr bytes 모두 whole-stream SHA-256 및 bounded collector를 사용한다. `src/core/mcp-proxy.ts`의 `getDefaultEnvironment`와 `StdioClientTransport({ stderr: "inherit" })` 경로를 제거하고, `openProtocolProcess` 위에 MCP SDK `Transport` 계약을 구현하는 adapter를 연결한다. Adapter는 runtime 최소 변수와 Firecrawl에 승인된 환경 이름만 case-normalized allowlist로 materialize하고, parsed MCP message size/total-byte cap, connect·forwarded-call·idle deadline, graceful-close 뒤 강제 child-tree termination을 적용한다. SDK/client/server error에는 stable code와 exit/timeout/cap/fingerprint 및 bounded redacted excerpts만 전달하며 raw protocol bytes나 inherited stderr handle은 전달하지 않는다. 모든 child는 windowsHide true and shell false다.</action>
  <acceptance_criteria>known shim positive control and arbitrary script negative control pass; fake upstream MCP server를 통과하는 filter-proxy test가 disallowed environment sentinel 부재, split-secret/oversize stderr redaction·cap, complete-stream fingerprint, initialize/request timeout termination metadata를 검증하고 source에 getDefaultEnvironment 및 inherited stderr transport가 남지 않는다.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/process.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, the MCP proxy observes a disallowed environment value or raw stderr sentinel, a deadline leaves its upstream child alive, or any process mode observes shell:true/full process.env inheritance</fails_when>
  </verify>
  <reversibility rating="reversible">compatibility wrappers permit staged caller migration.</reversibility>
  <done>all three execution modes and the SDK-backed MCP proxy share one policy/result model with no ambient-environment or inherited-stderr bypass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Inventory와 harness/isolation probes를 ProcessSpec으로 이동</name>
  <files>src/core/inventory.ts, src/adapters/harnesses.ts, src/adapters/isolation.ts</files>
  <read_first>
    src/core/inventory.ts
    src/adapters/harnesses.ts
    src/adapters/isolation.ts
    src/core/process.ts
    test/install.test.ts
    test/isolation.test.ts
  </read_first>
  <behavior>
    - read-only discovery declares executable, args, cwd, timeout, byte cap, and exact environment names.
    - unsupported native surfaces remain reported rather than simulated.
    - probe failures expose stable codes and redacted metadata only.
  </behavior>
  <action>inventory/harness/isolation adapter callers를 `ProcessSpec` declarations로 전환한다. Adapter는 process execution을 구현하지 않고 executable/argv/env name/stdio intent만 선언한다. Existing harness support results and no-mutating probe semantics를 유지하며 raw stdout/stderr를 DoctorFinding message에 concatenate하지 않는다.</action>
  <acceptance_criteria>all discovery/probe call sites compile without direct spawn/spawnSync or process.env spread and existing inventory/isolation focused tests preserve supported/unsupported distinctions.</acceptance_criteria>
  <verify>
    <automated>npm run check && npm run build && node --test dist/test/install.test.js dist/test/isolation.test.js dist/test/process.test.js</automated>
    <fails_when>non-zero exit, output reports 0 tests, or a probe result contains a raw secret sentinel</fails_when>
  </verify>
  <reversibility rating="reversible">adapter declarations preserve existing public inventory contracts.</reversibility>
  <done>read-only probes no longer bypass the centralized process boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter intent→OS process | executable/argv/env/output/lifetime become operating-system effects |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-16 | Tampering/Elevation | process.ts | high | mitigate | absolute executable, argv array, shell:false, shim allowlist |
| T-01-17 | Information Disclosure | environment/output | high | mitigate | case-normalized env allowlist, bounded redacted streaming capture |
| T-01-18 | Denial of Service | child process | medium | mitigate | deadline, byte cap, descendant termination metadata |
</threat_model>

## Artifacts this phase produces

| Kind | Artifact | Contract |
|------|----------|----------|
| New types/functions | `ProcessSpec`, `ProcessResult`, `ProcessFailure`, `EnvironmentPolicy`, `normalizeProcessSpec`, `runProcess`, `openProtocolProcess` | one shell-free process boundary |
| Migrated surface | `src/core/mcp-proxy.ts` | SDK-compatible MCP protocol transport with explicit env names, bounds, deadlines, fingerprints, and redacted evidence |

## Probe coverage notes

- Generic command injection is canon and lives in STRIDE; silent shell/ambient-env fallback is retained as the product-specific descriptor-less prohibition.

<verification>
Focused process, install, and isolation suites pass with no shell fallback or raw output result.
</verification>

<success_criteria>
- all modes use one ProcessSpec contract.
- output and failures are bounded/redacted/fingerprinted.
- migrated probes and the MCP filter proxy contain no direct child-process/default-environment/inherited-stderr bypass.
</success_criteria>

<output>
Create `.planning/phases/01-safe-operation-boundary/01-08-SUMMARY.md` when done.
</output>
