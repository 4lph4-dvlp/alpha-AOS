---
phase: 01-safe-operation-boundary
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - test/path-boundary.test.ts
  - test/transaction-crash.test.ts
  - test/process.test.ts
autonomous: true
requirements: [SAFE-02, SAFE-03, SAFE-06]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Spec-less edge assumption (SAFE-02, resolved conservatively from D-01 through D-03): every configured and canonical path component, including a changed parent immediately before mutation, must remain provably within its allowed root or apply refuses."
    - "Spec-less edge assumption (SAFE-03 concurrency row, resolved explicitly from D-04 through D-07): one writer owns a state root; competitors fail immediately; interruption remains hash-diagnosable and recovery requires repair --apply."
    - "Spec-less edge assumption (SAFE-06, resolved conservatively from D-12): timeout, output cap, executable normalization, argv separation, and case-normalized environment allowlists are independently observable process outcomes."
  artifacts:
    - path: test/path-boundary.test.ts
      provides: "Cross-platform containment and parent-swap adversarial contract"
    - path: test/transaction-crash.test.ts
      provides: "Writer contention and durable failpoint contract"
    - path: test/process.test.ts
      provides: "Shell-free bounded subprocess contract"
  key_links:
    - from: test/path-boundary.test.ts
      to: src/core/transaction.ts
      via: "real filesystem writes/removals with outside sentinel"
      pattern: "symlink|junction|reparse|parent"
    - from: test/transaction-crash.test.ts
      to: src/core/transaction.ts
      via: "child process failpoints and journal/hash inspection"
      pattern: "operation|journal|repair"
    - from: test/process.test.ts
      to: src/core/process.ts
      via: "controlled child executables and environment sentinels"
      pattern: "timeout|byte|shell|env"
  prohibitions:
    - statement: "An operation whose filesystem guarantees are unknown MUST NOT offer an override that converts an unproven boundary into an applyable one."
      requirement_id: SAFE-02
      category: safety
      status: unverified
      flagged: true
    - statement: "An abandoned-looking writer record MUST NOT be presented as safely removable based only on elapsed time or PID absence."
      requirement_id: SAFE-03
      category: safety
      status: unverified
      flagged: true
---

<objective>
Wave 0에서 filesystem escape, competing/interrupted writer, subprocess policy의 adversarial evidence를 고정한다.

Purpose: race·reparse·shell·ambient environment 경계가 구현 세부사항에 가려지지 않게 한다.
Output: 실제 temp filesystem과 child process를 사용하는 세 개의 focused regression suites.
</objective>

<execution_context>
@C:/Users/alpha/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-safe-operation-boundary/01-CONTEXT.md
@.planning/phases/01-safe-operation-boundary/01-RESEARCH.md
@.planning/phases/01-safe-operation-boundary/01-VALIDATION.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Outside-sentinel path escape tracer 작성</name>
  <files>test/path-boundary.test.ts</files>
  <read_first>
    test/transaction.test.ts
    src/core/transaction.ts
    src/core/paths.ts
    src/core/isolation.ts
  </read_first>
  <behavior>
    - configured root와 canonical root 안에 남는 known symlink/junction은 허용되고 escape link는 거부된다.
    - unknown reparse kind와 canonical proof 불가 filesystem은 stable unsupported refusal을 내며 outside sentinel hash가 유지된다.
    - preflight 뒤 parent link를 바꾸는 deterministic failpoint가 rename/remove 직전 recheck에서 거부된다.
  </behavior>
  <action>D-01, D-02, D-03과 SAFE-02의 end-to-end path tracer를 작성한다. target뿐 아니라 state, journal, snapshot, temp, source, package root마다 inside/escape fixture를 만들고 apply/remove를 시도한다. Windows junction/reparse와 POSIX symlink를 host capability에 따라 생성하되 privilege가 없는 unknown-reparse case는 `not-run` evidence로 기록하고 pass count에 넣지 않는다. parent-swap hook은 preflight와 mutation 사이에서 ancestor를 교체하고 outside sentinel의 bytes/hash/metadata가 그대로인지 검사한다.</action>
  <acceptance_criteria>각 path class가 configured containment, canonical containment, immediate recheck, outside sentinel을 관찰하며 unsupported fixture가 success로 보고되지 않는다.</acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
    <fails_when>non-zero exit, or TypeScript reports that test/path-boundary.test.ts cannot be compiled</fails_when>
  </verify>
  <reversibility rating="reversible">OS temp 아래 테스트 자원만 만들고 cleanup한다.</reversibility>
  <done>SAFE-02의 link/reparse/alias/parent-swap 경계가 executable tests로 존재한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Writer contention 및 interruption failpoint matrix 작성</name>
  <files>test/transaction-crash.test.ts</files>
  <read_first>
    test/transaction.test.ts
    src/core/transaction.ts
    src/cli.ts
    schemas/install-state.schema.json
  </read_first>
  <behavior>
    - 두 번째 writer는 기다리거나 lock을 지우지 않고 active operation ID/start/PID/retry code와 함께 즉시 종료한다.
    - snapshot sync, intent journal sync, target rename/remove, step sync, final sync 사이 중단마다 current hash가 before/after/neither로 진단된다.
    - PID 부재나 오래된 timestamp만으로 lock이 사라지지 않고 needs-repair가 되며 preview는 mutation하지 않고 repair --apply만 proven transition을 수행한다.
  </behavior>
  <action>D-04, D-05, D-06, D-07과 SAFE-03을 위한 subprocess contention test와 deterministic failpoint matrix를 작성한다. child writer가 lock을 잡은 상태에서 competitor를 호출해 bounded elapsed time, diagnostic fields, unchanged target을 검사한다. 각 durable boundary에서 강제 종료한 뒤 journal/token/target hash를 별도 reader로 검사하고, diagnosis와 repair preview의 tree digest 동일성, neither-hash drift 보존, explicit apply 성공 조건을 고정한다.</action>
  <acceptance_criteria>모든 documented durable boundary에 interruption case가 있고 active, needs-repair, before, after, neither 상태가 서로 다른 stable result로 검증된다.</acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
    <fails_when>non-zero exit, or TypeScript reports that test/transaction-crash.test.ts cannot be compiled</fails_when>
  </verify>
  <reversibility rating="reversible">격리된 child process와 temp state만 사용한다.</reversibility>
  <done>SAFE-03 concurrency/interruption 행이 no-wait, no-auto-unlock, explicit-repair assertions로 해소된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Shell-free bounded process contract 작성</name>
  <files>test/process.test.ts</files>
  <read_first>
    src/core/process.ts
    src/core/mcp-fixture.ts
    src/core/gsd-fixture.ts
    src/core/ecc-fixture.ts
    src/adapters/harnesses.ts
  </read_first>
  <behavior>
    - argv metacharacters are data and never execute a sentinel side effect.
    - only allowed environment names reach the child; Windows case-colliding names are rejected deterministically.
    - timeout and byte cap terminate the child tree and return exit/timeout/cap/fingerprint plus bounded redacted excerpts without raw output.
    - known npm/npx shims normalize to node plus exact CLI script; arbitrary cmd/bat input is unsupported rather than routed through a shell.
  </behavior>
  <action>D-12 and SAFE-06의 process corpus를 작성한다. Node helper child를 argv injection sentinel, inherited environment sentinel, mixed-case key collision, 1-byte secret chunks, unbounded stdout/stderr, timeout, descendant process, interactive/protocol-session modes로 실행한다. 결과 객체에는 shell 사용 흔적이나 raw bytes가 없고 exit metadata, whole-output SHA-256, cap/timeout flags, typed redacted excerpts만 존재하는지 검사한다.</action>
  <acceptance_criteria>argv/env/timeout/cap/shim/protocol cases가 모두 real child process를 사용하고 sentinel side effect 및 disallowed environment value가 관찰되지 않는다.</acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
    <fails_when>non-zero exit, or TypeScript reports that test/process.test.ts cannot be compiled</fails_when>
  </verify>
  <reversibility rating="reversible">테스트 child와 temp sentinel만 사용한다.</reversibility>
  <done>SAFE-06의 five-dimensional process contract가 실행 가능하게 고정된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| configured path→canonical filesystem object | links/reparse/parent changes가 logical target을 바꾸는 경계 |
| concurrent command→state-root writer | 서로 다른 process가 동일 managed state를 mutate하려는 경계 |
| process spec→OS child | argv, environment, output, lifetime이 OS process로 넘어가는 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering/Elevation | path boundary | critical | mitigate | real link/junction/parent-swap outside-sentinel tests |
| T-01-05 | Tampering/Repudiation | writer transaction | high | mitigate | competitor subprocess and every durable failpoint hash inspection |
| T-01-06 | Tampering/Information Disclosure | process adapter | high | mitigate | injection/env/output/timeout corpus with sentinel controls |
</threat_model>

## Artifacts this phase produces

| Kind | Artifact | Contract |
|------|----------|----------|
| New file | `test/path-boundary.test.ts` | canonical containment and reparse test suite |
| New file | `test/transaction-crash.test.ts` | contention/crash/repair test suite |
| New file | `test/process.test.ts` | shell/env/cap/timeout test suite |

## Probe coverage notes

- Canonical path traversal and OS command injection are canon concerns covered by the STRIDE register; only product-specific no-override/no-auto-unlock rules are retained as descriptor-less prohibitions.
- SAFE-02, SAFE-03, SAFE-06 probe rows are explicitly resolved from D-01~D-07 and D-12; none is auto-dismissed.

<verification>
`npm run check` succeeds with all three Wave 0 suites compiled; later plans run each focused suite to green.
</verification>

<success_criteria>
- privileged reparse unavailability is visible not-run evidence.
- all crash stages preserve diagnosable hash evidence.
- process tests prove data/command separation and ambient-environment exclusion.
</success_criteria>

<output>
Create `.planning/phases/01-safe-operation-boundary/01-02-SUMMARY.md` when done.
</output>
