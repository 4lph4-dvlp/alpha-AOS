---
phase: 11-managed-lifecycle-verification
plan: 07
type: execute
wave: 3
depends_on: ["11-01", "11-02", "11-03", "11-04", "11-05", "11-06"]
files_modified:
  - .planning/milestones/v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/MILESTONES.md
  - .planning/milestones/v0.1.0-REQUIREMENTS.md
  - .planning/milestones/v0.1.0-ROADMAP.md
autonomous: true
requirements: [LIFE-09]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "User can open `.planning/milestones/v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md` and read, for each of LIFE-01..LIFE-08, a verdict of VERIFIED, PARTIAL or GAP backed by transcript CHECK ids, named tests with their results, or CI job ids, never by a restated SUMMARY claim (ROADMAP success criterion 1, D-01, D-02)."
    - "A requirement is VERIFIED only when every clause row HOLDS on an oracle the findings judged sound; any VIOLATED, NOT-OBSERVED or UNEVIDENCED clause makes it PARTIAL (or GAP when its core clause fails) and is listed as a named gap (ROADMAP success criterion 3, D-02, D-04)."
    - "Every named gap has a stable id `G-11-<n>` and carries the LIFE clause it fails, what is missing, what would close it, and a reproduction naming the command, the transcript path and the CHECK id with the observed lines (D-06)."
    - "Ordering edge (LIFE-09): gap ids are assigned in LIFE-01..LIFE-08 order and, within a requirement, in the order of that requirement's findings candidates, starting at `G-11-1`; the verdict matrix lists LIFE-01..LIFE-08 in numeric order."
    - "Adjacency edge (LIFE-09): one reproduced defect that fails clauses of two requirements (for example LIFE-03 and LIFE-04) receives one gap id per failed clause, each citing the same reproduction and naming the other id; candidates are never merged across requirements and never dropped."
    - "Empty edge (LIFE-09): a clause with no evidence row, or whose only evidence is NOT-OBSERVED, is a named gap of kind `unevidenced`, never VERIFIED; if the whole phase yields zero gaps the report says `No named gaps` and REQUIREMENTS.md gains no G-11 entry."
    - "The report states `Phase 13 gating: BLOCKED` when LIFE-01 or LIFE-08 has any open gap and `Phase 13 gating: NOT BLOCKED` otherwise, naming the blocking ids; gaps in LIFE-02..LIFE-07 never block (D-08)."
    - "The LIFE-03 and LIFE-04 sections cite `.planning/debug/ci02-windows-alpha-aos-host-leak.md`: the Windows `~/.alpha-aos` change was a test-isolation leak, fixed by 8f17c7e and 7a3ee28 and pinned by Phase 10 regression tests, and names no product requirement violation, while the probes' host-guard results judge product host safety afresh (ROADMAP success criterion 2, D-13)."
    - "Every G-11 id in the report is registered in `.planning/REQUIREMENTS.md` `## Future Requirements` in the existing `**REL-02 / G-07-2**` bullet style (D-07)."
    - "`.planning/MILESTONES.md` keeps the original LIFE-01..08 Known Gaps line verbatim and gains a dated sub-bullet reading `Closed` or `Narrowed to` the report's gap ids, which also records that the windows-latest lifecycle red half was resolved in Phase 10 (ROADMAP success criterion 4, D-14)."
    - "`.planning/milestones/v0.1.0-REQUIREMENTS.md` LIFE checkboxes flip to `[x]` only for VERIFIED requirements, each LIFE traceability status reads `Verified`, `Partial (G-11-…)` or `Gap (G-11-…)` with a link to the report, and the Phase 6 entry of `.planning/milestones/v0.1.0-ROADMAP.md` links the report in place of its unsupported `Verification complete (2026-09-18)` claim (D-15)."
    - "The report's `## Re-run index (D-17)` lists at least six probe commands with `--out` scratch paths and the CHECK-line diff that confirms each, so the phase verifier can re-execute a sample and compare outputs (D-17)."
    - "The report records the LIFE-09 completion criterion: all eight requirements carry evidence-backed verdicts and every gap is tracked, whether or not gaps remain (D-16)."
    - "`src/` is untouched and no test file is changed by this plan (D-05, D-12)."
  prohibitions:
    - statement: "MUST NOT mark any LIFE requirement VERIFIED, or tick its checkbox, when any of its clauses is VIOLATED, NOT-OBSERVED or UNEVIDENCED, and MUST NOT upgrade a findings file's provisional verdict without citing new evidence."
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT delete or rewrite the original v0.1.0 Known Gaps LIFE line or erase the stale 2026-09-18 claim from the audit trail; corrections are additive and dated (D-14, D-15)."
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT copy file contents, credential values or unaliased host paths from transcripts into the report; the report cites transcript paths and CHECK ids."
      category: privacy
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/milestones/v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md"
      provides: "The Phase 6 verification report: eight verdicts, gap register, Phase 13 gating, re-run index"
      contains: "Phase 13 gating:"
    - path: ".planning/REQUIREMENTS.md"
      provides: "G-11 gaps registered under Future Requirements"
      contains: "G-11-"
    - path: ".planning/MILESTONES.md"
      provides: "Dated resolution note under the v0.1.0 LIFE Known Gaps line"
      contains: "06-VERIFICATION.md"
    - path: ".planning/milestones/v0.1.0-REQUIREMENTS.md"
      provides: "LIFE checkboxes and traceability status tied to the report"
      contains: "06-VERIFICATION.md"
    - path: ".planning/milestones/v0.1.0-ROADMAP.md"
      provides: "Phase 6 entry linking the report"
      contains: "06-VERIFICATION.md"
  key_links:
    - from: "06-VERIFICATION.md skeleton written by plan 11-01 (wave 1)"
      to: "completed 06-VERIFICATION.md"
      via: "scoped in-place extension that keeps the 11-01 frontmatter keys, section order and LIFE-01 Codex rows"
      pattern: "life01\\.codex\\.second-apply-noop"
    - from: "06-VERIFICATION.md gap register"
      to: ".planning/REQUIREMENTS.md ## Future Requirements"
      via: "every G-11 id appears in both"
      pattern: "G-11-[0-9]+"
    - from: "06-VERIFICATION.md verdict matrix"
      to: "evidence/LIFE-0N-findings.md and transcripts"
      via: "each row cites transcript CHECK ids"
      pattern: "life0[1-8]\\."
    - from: ".planning/milestones/v0.1.0-REQUIREMENTS.md traceability"
      to: "06-VERIFICATION.md"
      via: "status cell links the report"
      pattern: "v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md"
---

<objective>
Compile the eight per-requirement findings and their transcripts into the Phase 6 verification report by extending, in place, the skeleton plan 11-01 created and partly filled (its LIFE-01 Codex rows stay), register every named gap, and update the milestone records so the v0.1.0 "LIFE-01..08 unverified" known gap is closed or narrowed to exactly the report's gaps.

Purpose: this is the LIFE-09 deliverable (ROADMAP Phase 11 success criteria 1-4). The report must let a reader re-run any cited command and get the same answer.

Output: completed `06-VERIFICATION.md`; edits to `.planning/REQUIREMENTS.md`, `.planning/MILESTONES.md`, `.planning/milestones/v0.1.0-REQUIREMENTS.md`, `.planning/milestones/v0.1.0-ROADMAP.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-managed-lifecycle-verification/11-CONTEXT.md
@.planning/phases/11-managed-lifecycle-verification/evidence/README.md
@.planning/phases/11-managed-lifecycle-verification/evidence/LIFE-01-findings.md
@.planning/phases/11-managed-lifecycle-verification/evidence/LIFE-02-findings.md
@.planning/phases/11-managed-lifecycle-verification/evidence/LIFE-03-findings.md
@.planning/phases/11-managed-lifecycle-verification/evidence/LIFE-04-findings.md
@.planning/phases/11-managed-lifecycle-verification/evidence/LIFE-05-findings.md
@.planning/phases/11-managed-lifecycle-verification/evidence/LIFE-06-findings.md
@.planning/phases/11-managed-lifecycle-verification/evidence/LIFE-07-findings.md
@.planning/phases/11-managed-lifecycle-verification/evidence/LIFE-08-findings.md
@.planning/milestones/v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md
@.planning/milestones/v0.1.0-phases/07-cross-platform-release-proof/07-VERIFICATION.md
@.planning/phases/10-three-os-ci-baseline/10-VERIFICATION.md
</context>

<interfaces>
Paths: EV = `.planning/phases/11-managed-lifecycle-verification/evidence`, REPORT = `.planning/milestones/v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md`.

Inputs: EV/LIFE-01..08-findings.md (headings `## Contract`, `## Clauses`, `## Core clause`, `## Provisional verdict`, `## Gap candidates` with `### LIFE-0N/C<k>` subsections carrying `clause:`, `missing:`, `close condition:`, `reproduction:`, `## Leads (not reproduced)`, `## Observations`); LIFE-01 and LIFE-08 findings also carry `## Phase 13 gating`; LIFE-03 and LIFE-04 carry `## Host safety (D-13)`. Transcripts: EV/*.txt; probes: EV/probes/*.mjs (each accepts `--out <path>`); suite and CI evidence: EV/suites-local.txt, EV/ci-35818198049-lifelines.txt.

Report frontmatter target (maps D-06 onto the 07-VERIFICATION.md gap format):
- `phase: 06-managed-lifecycle-uninstall-and-recovery`, `verified: <ISO timestamp>`, `verified_in: 11-managed-lifecycle-verification`, `status: gaps_found` (any gap) or `passed`, `score: <n>/8 VERIFIED`, `phase13_gating: BLOCKED` or `NOT BLOCKED`, `completion: LIFE-09 criterion met (D-16)`.
- `gaps:` list, one entry per gap id with keys `gap_id` (`G-11-<n>`), `requirement` (`LIFE-0N`), `candidate_key` (`LIFE-0N/C<k>`), `clause` (verbatim fragment), `kind` (`failed` or `unevidenced`), `missing`, `close_condition`, `reproduction` (command, transcript path, CHECK id, observed lines), `related` (other gap ids sharing the reproduction, or empty), `blocks_phase13` (true only for LIFE-01 and LIFE-08).

Record-file facts (verified at planning time):
- .planning/MILESTONES.md line 13 is the v0.1.0 Known Gaps LIFE line beginning `- **LIFE-01..08** (Phase 6): implemented and summarized, but Phase 6 has no` and ending `hash changes across the isolated lifecycle.`
- .planning/milestones/v0.1.0-REQUIREMENTS.md lines 77-84 are `- [ ] **LIFE-0N**: ...`; lines 179-186 are the LIFE traceability rows `| LIFE-0N | Phase 6 | <status> |`.
- .planning/milestones/v0.1.0-ROADMAP.md line 416, in the `### Phase 6: Managed Lifecycle, Uninstall, and Recovery` section, reads `**Plans**: 3/3 plans executed. Verification complete (2026-09-18).`
- .planning/REQUIREMENTS.md `## Future Requirements` starts with `- **REL-02 / G-07-2**: Claude Code real-host invocation receipt ...`.
- Relative link to the report from .planning/milestones/*.md: `v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md`; from .planning/MILESTONES.md: `milestones/v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Compile 06-VERIFICATION.md — verdicts, gap register, Phase 13 gating, host-safety citation and the re-run index</name>
  <files>.planning/milestones/v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md</files>
  <read_first>
    .planning/milestones/v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md (NOT a pristine file: plan 11-01 Task 1 created it and committed its frontmatter keys, the section order, the eight-row verdict matrix and the LIFE-01 Codex clause rows citing `evidence/life01-codex-reconcile.txt` and its `life01.codex.*` CHECK ids; this plan consumes and extends that content)
    .planning/phases/11-managed-lifecycle-verification/11-01-SUMMARY.md (what the skeleton already holds and the outcome of every `life01.codex.*` check)
    .planning/phases/11-managed-lifecycle-verification/evidence/LIFE-01-findings.md through LIFE-08-findings.md (all eight)
    .planning/phases/11-managed-lifecycle-verification/evidence/README.md
    .planning/phases/11-managed-lifecycle-verification/evidence/suites-local.txt
    .planning/phases/11-managed-lifecycle-verification/evidence/ci-35818198049-lifelines.txt
    .planning/phases/11-managed-lifecycle-verification/11-0[1-6]-SUMMARY.md (for deviations and which tests were committed)
    .planning/milestones/v0.1.0-phases/07-cross-platform-release-proof/07-VERIFICATION.md (lines 1-40)
    .planning/phases/10-three-os-ci-baseline/10-VERIFICATION.md (lines 1-60)
    .planning/debug/ci02-windows-alpha-aos-host-leak.md (lines 25-40)
  </read_first>
  <action>
    Per D-01, D-02, D-04, D-06, D-08, D-13, D-16 and D-17, complete REPORT by extending the plan 11-01 skeleton in place, replacing every plan 11-01 placeholder token.
    0. Consume the 11-01 content before writing: confirm REPORT exists with `verified_in: 11-managed-lifecycle-verification` in its frontmatter, the `## Verdict matrix` table and the `## Re-run index (D-17)` heading, and that its LIFE-01 section already holds the Codex clause rows citing `life01-codex-reconcile.txt` and `life01.codex.second-apply-noop`; if any of these is missing, stop and report that plan 11-01 did not complete rather than recreating the file. Keep the 11-01 frontmatter keys (adding the Task 1 keys from the interfaces block), the 11-01 section order (inserting `## LIFE-09 completion` after the re-run index), and the 11-01 LIFE-01 Codex rows with their transcript citations and CHECK ids; merge the remaining LIFE-01 rows from EV/LIFE-01-findings.md around them, changing a Codex row's Result only where LIFE-01-findings cites newer evidence for that clause (name that evidence in the row). Edit section by section with scoped replacements, never one whole-file write.
    1. For each requirement, fill its existing `## LIFE-0N` section from its findings file (for LIFE-01, extend the 11-01 Codex rows per step 0 instead of replacing them): the verbatim contract, the clause table (`# | Clause | Evidence | Oracle audit (D-04) | Result`), and the final verdict. Apply D-02 mechanically: VERIFIED only if every clause row is HOLDS with an oracle the findings judged sound; GAP if the findings' core clause is VIOLATED; otherwise PARTIAL. A NOT-OBSERVED or UNEVIDENCED clause becomes a gap of kind `unevidenced`. Where your verdict differs from the findings' provisional verdict, say why in one line citing the rows that decide it.
    2. Assign gap ids: walk LIFE-01 to LIFE-08 in order and each findings file's `### LIFE-0N/C<k>` candidates in their order, then every unevidenced clause of that requirement not already a candidate, numbering `G-11-1` upward. Keep one id per failed clause; where two candidates share a reproduction across requirements, fill `related` on both. Write the frontmatter keys from the interfaces block, the `gaps:` list, and a `## Gap register` table (`Gap | Requirement | Clause | Kind | Missing | Closes when | Reproduction`), each reproduction citing the transcript path and CHECK id and quoting the observed lines.
    3. `## Verdict matrix`: eight rows `| LIFE-0N | <VERIFIED/PARTIAL/GAP> | <gap ids or none> | <primary transcript and check ids> |` in numeric order, followed by the line `Score: <n>/8 VERIFIED`.
    4. `## Phase 13 gating`: one line `Phase 13 gating: BLOCKED` naming the LIFE-01 and LIFE-08 gap ids, or `Phase 13 gating: NOT BLOCKED`, with the D-08 rule stated in one sentence; the frontmatter `phase13_gating` value matches.
    5. `## Evidence records`: every transcript under EV with its probe command, its `head:` value and `tarball-sha256:` value, the suite transcript's summary, and the three CI job ids with their `ci.<os>.*` results; state that POSIX corroboration comes from run 35818198049 on 35d4c15 with the `ci.staleness` result (D-03), and that any test committed by plans 11-01..11-06 is corroborated on POSIX by the next `main` CI run.
    6. In the LIFE-03 and LIFE-04 sections keep a `### Host safety (D-13)` paragraph citing `.planning/debug/ci02-windows-alpha-aos-host-leak.md` (`classification: test-isolation leak`), commits 8f17c7e and 7a3ee28, the Phase 10 regression tests, and the host-guard lines of every Phase 11 transcript; name no product requirement violation for the Phase 10 leak.
    7. `## Leads (not reproduced)` and `## Observations`: merge the findings' sections, each item naming its source findings file; nothing here is a gap (D-06).
    8. `## Re-run index (D-17)`: a table with at least six rows, one per probe family (life01-reconcile, life02-status, life03-uninstall, life05-rollback, life06-recovery, life08-apply, suites-and-ci), each giving `npm run build && node <probe path> --out <scratch path>`, the committed transcript it is compared with, and the comparison `diff <(grep '^CHECK' <committed transcript>) <(grep '^CHECK' <scratch path>)`; note that the life03-tty probe runs under WSL.
    9. `## LIFE-09 completion`: one paragraph applying D-16 (all eight verdicts evidence-backed, every gap tracked in the report and in REQUIREMENTS.md by Task 2, zero gaps not required).
  </action>
  <verify>
    <automated>R=.planning/milestones/v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md && ! grep -q 'TO-BE-COMPILED' "$R" && test "$(grep -cE '^\| LIFE-0[1-8] \| (VERIFIED|PARTIAL|GAP) \|' "$R")" -eq 8 && test "$(grep -E '^\| LIFE-0[1-8] \| (PARTIAL|GAP) \|' "$R" | grep -v 'G-11-' | wc -l)" -eq 0 && grep -qE '^Phase 13 gating: (BLOCKED|NOT BLOCKED)' "$R" && grep -qE '^phase13_gating: (BLOCKED|NOT BLOCKED)$' "$R" && grep -q 'ci02-windows-alpha-aos-host-leak.md' "$R" && test "$(grep -cE '^\| .*--out ' "$R")" -ge 6 && grep -q '^verified_in: 11-managed-lifecycle-verification$' "$R" && grep -q 'life01-codex-reconcile.txt' "$R" && grep -q 'life01\.codex\.second-apply-noop' "$R" && grep -q '^## Re-run index (D-17)' "$R"</automated>
    <fails_when>a placeholder token remains, the matrix does not have eight verdict rows, a PARTIAL or GAP row lacks a G-11 id, the gating line or frontmatter value is missing, the D-13 citation is missing, the re-run index has fewer than six --out rows, or the 11-01 skeleton content this plan extends (the `verified_in` key, the LIFE-01 Codex rows citing the reconcile transcript and its check id, the re-run index heading) was dropped</fails_when>
    <automated>node --input-type=module -e "import { parse } from 'yaml'; import { readFileSync } from 'node:fs'; const text = readFileSync(process.argv[1], 'utf8'); const fm = parse(text.split(/^---\s*$/m)[1]); const gaps = fm.gaps ?? []; const bad = gaps.filter((g) => !/^G-11-[0-9]+$/.test(String(g.gap_id)) || !/^LIFE-0[1-8]$/.test(String(g.requirement)) || !g.clause || !g.missing || !g.close_condition || !g.reproduction || !['failed', 'unevidenced'].includes(g.kind)); const ids = gaps.map((g) => g.gap_id); if (bad.length || new Set(ids).size !== ids.length) { console.error(JSON.stringify(bad)); process.exit(1); } const blocking = gaps.some((g) => g.requirement === 'LIFE-01' || g.requirement === 'LIFE-08'); if ((fm.phase13_gating === 'BLOCKED') !== blocking) { console.error('phase13_gating disagrees with D-08'); process.exit(1); }" .planning/milestones/v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md</automated>
    <fails_when>non-zero exit: a gap entry lacks an id, requirement, clause, missing, close_condition, reproduction or valid kind; ids repeat; or phase13_gating disagrees with whether any LIFE-01 or LIFE-08 gap exists</fails_when>
  </verify>
  <acceptance_criteria>
    - REPORT has eight verdict rows in numeric order and a `Score: <n>/8 VERIFIED` line equal to the number of VERIFIED rows.
    - Gap ids run `G-11-1` to `G-11-<K>` with no hole or repeat, in LIFE order.
    - Every VIOLATED check in any Phase 11 transcript is either inside a gap's reproduction or explicitly argued in its requirement section as not failing the LIFE text.
    - The report contains no unaliased home path and no diff body line (`grep -qF` of the real home path, plain and JSON-escaped, finds nothing).
    - `git status --porcelain -- src test` prints nothing.
  </acceptance_criteria>
  <done>The Phase 6 verification report is complete, internally consistent, gated per D-08 and re-runnable per D-17.</done>
</task>

<task type="auto">
  <name>Task 2: Register gaps and update the current and archived milestone records</name>
  <reversibility rating="costly">G-11 ids become referenced from REQUIREMENTS.md, MILESTONES.md and the archived v0.1.0 records, and later phases will cite them; renumbering after this task means a coordinated edit of every reference.</reversibility>
  <files>.planning/REQUIREMENTS.md, .planning/MILESTONES.md, .planning/milestones/v0.1.0-REQUIREMENTS.md, .planning/milestones/v0.1.0-ROADMAP.md</files>
  <read_first>
    .planning/milestones/v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md (as completed by Task 1)
    .planning/REQUIREMENTS.md (## Future Requirements, ## Traceability)
    .planning/MILESTONES.md (lines 1-30, v0.1.0 Known Gaps)
    .planning/milestones/v0.1.0-REQUIREMENTS.md (lines 70-90, 170-190)
    .planning/milestones/v0.1.0-ROADMAP.md (lines 400-420, Phase 6 section)
  </read_first>
  <action>
    Per D-07, D-14, D-15 and D-16; use scoped edits only, never a whole-file rewrite.
    1. .planning/REQUIREMENTS.md `## Future Requirements`: after the existing bullets, append one bullet per gap id in id order, in the form `- **G-11-<n>** (LIFE-0N): <clause> — <missing>; closes when <close condition>. See .planning/milestones/v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md.` If the report has no gaps, append nothing. Do not edit the LIFE-09 traceability row (the phase-completion step owns it; the D-16 criterion is recorded in the report).
    2. .planning/MILESTONES.md: leave the v0.1.0 Known Gaps LIFE line byte-identical and insert directly below it one indented sub-bullet dated with today's date (`date +%F`): `Closed` when the report has no gaps, otherwise `Narrowed to G-11-1..G-11-<K>` (list the ids when they are few), then `— see [06-VERIFICATION.md](milestones/v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md) (<n>/8 VERIFIED; Phase 13 gating: <BLOCKED or NOT BLOCKED>).` followed by the sentence `The windows-latest lifecycle red half of this line was resolved in Phase 10: .planning/debug/ci02-windows-alpha-aos-host-leak.md (test-isolation leak) and .planning/phases/10-three-os-ci-baseline/CI_RUN.md (run 35818198049).`
    3. .planning/milestones/v0.1.0-REQUIREMENTS.md: in lines 77-84 change `- [ ]` to `- [x]` only on the rows whose report verdict is VERIFIED; in the traceability rows 179-186 replace only the third cell with `Verified — [06-VERIFICATION.md](v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md)`, `Partial (<gap ids>) — [06-VERIFICATION.md](...)` or `Gap (<gap ids>) — [06-VERIFICATION.md](...)` with the same link target.
    4. .planning/milestones/v0.1.0-ROADMAP.md line 416: keep `**Plans**: 3/3 plans executed.` and replace the rest of the line with `Verification recorded <date> by Phase 11 in [06-VERIFICATION.md](v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md) (<n>/8 VERIFIED, <K> named gaps); the earlier "Verification complete (2026-09-18)" statement had no report behind it.`
  </action>
  <verify>
    <automated>R=.planning/milestones/v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md && for g in $(grep -oE 'G-11-[0-9]+' "$R" | sort -u); do grep -qF "**$g** (LIFE-0" .planning/REQUIREMENTS.md || exit 1; done && V=.planning/milestones/v0.1.0-REQUIREMENTS.md && test "$(grep -cE '^- \[x\] \*\*LIFE-0[1-8]\*\*' "$V")" -eq "$(grep -cE '^\| LIFE-0[1-8] \| VERIFIED \|' "$R")" && test "$(grep -cE '^\| LIFE-0[1-8] \| Phase 6 \| (Verified|Partial \(G-11-[0-9, G-]+\)|Gap \(G-11-[0-9, G-]+\)) — \[06-VERIFICATION.md\]\(v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md\) \|' "$V")" -eq 8</automated>
    <fails_when>a report gap id is missing from REQUIREMENTS.md, the ticked LIFE count differs from the VERIFIED count, or fewer than eight traceability rows carry a verdict status with the report link</fails_when>
    <automated>grep -qF 'implemented and summarized, but Phase 6 has no `06-VERIFICATION.md`; `packed release completes the isolated install, reconcile, diagnose, and uninstall lifecycle` is red on windows-latest because the real host `~/.alpha-aos` hash changes across the isolated lifecycle.' .planning/MILESTONES.md && sed -n '/^### Known Gaps/,/^\*\*Key accomplishments/p' .planning/MILESTONES.md | grep -E '^\s+- ' | grep -qE '(Closed|Narrowed to G-11-).*06-VERIFICATION\.md' && grep -q 'ci02-windows-alpha-aos-host-leak.md' .planning/MILESTONES.md && sed -n '/^### Phase 6:/,/^### Phase 7:/p' .planning/milestones/v0.1.0-ROADMAP.md | grep -q 'v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md' && test -z "$(git status --porcelain -- src test)"</automated>
    <fails_when>the original LIFE line was altered, the dated resolution sub-bullet or the Phase 10 citation is missing, the Phase 6 roadmap entry lacks the report link, or src/ or test/ changed</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff .planning/MILESTONES.md` shows only added lines (no removed line).
    - `git diff .planning/milestones/v0.1.0-REQUIREMENTS.md` touches only LIFE checkbox lines and LIFE traceability rows.
    - `git diff .planning/milestones/v0.1.0-ROADMAP.md` touches only the Phase 6 `**Plans**` line.
    - `git diff .planning/REQUIREMENTS.md` adds only `**G-11-<n>**` bullets under `## Future Requirements`.
  </acceptance_criteria>
  <done>Every gap is registered, the v0.1.0 known gap is closed or narrowed to exactly the report's gaps, and the archived records point at the report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence to report | The report summarizes committed transcripts for a public repository |
| report to planning records | Gap ids propagate into requirements and milestone records that later phases trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-26 | Repudiation | verdicts | high | mitigate | D-02 applied mechanically from clause rows; frontmatter validated by a YAML check; Phase 13 gating cross-checked against the gap list; D-17 re-run index |
| T-11-27 | Tampering | archived records | medium | mitigate | Scoped edits only; verify gates prove the original Known Gaps line is byte-identical and each diff touches only the named lines |
| T-11-28 | Information disclosure | report | medium | mitigate | Report cites transcript paths and CHECK ids; acceptance greps for the unaliased home path and diff bodies |
</threat_model>

<verification>
- Both task verify gates pass.
- `git status --porcelain -- src test` prints nothing.
- The report's YAML frontmatter parses with the repository's `yaml` package and passes the gap-field and gating consistency check (Task 1 gate).
</verification>

<success_criteria>
- ROADMAP Phase 11 success criteria 1-4 hold: eight evidence-backed verdicts; LIFE-03/LIFE-04 reflect the Phase 10 classification; every unmet requirement is a named gap with what is missing and what closes it; the v0.1.0 known gap is closed or narrowed to exactly the report's gaps.
- LIFE-09 completion criterion (D-16) is met and stated.
</success_criteria>

## Artifacts this phase produces

- Completed `.planning/milestones/v0.1.0-phases/06-managed-lifecycle-uninstall-and-recovery/06-VERIFICATION.md` with frontmatter keys `verified_in`, `status`, `score`, `phase13_gating`, `completion`, `gaps[]` (`gap_id`, `requirement`, `candidate_key`, `clause`, `kind`, `missing`, `close_condition`, `reproduction`, `related`, `blocks_phase13`) and sections `## Verdict matrix`, `## Phase 13 gating`, `## Evidence records`, `## LIFE-01`..`## LIFE-08`, `## Gap register`, `## Leads (not reproduced)`, `## Observations`, `## Re-run index (D-17)`, `## LIFE-09 completion`.
- Gap ids `G-11-1`..`G-11-<K>` registered in `.planning/REQUIREMENTS.md` `## Future Requirements`.
- Dated resolution sub-bullet in `.planning/MILESTONES.md`; LIFE checkbox and traceability updates in `.planning/milestones/v0.1.0-REQUIREMENTS.md`; Phase 6 report link in `.planning/milestones/v0.1.0-ROADMAP.md`.

<output>
Create `.planning/phases/11-managed-lifecycle-verification/11-07-SUMMARY.md` when done. Include the verdict matrix, the gap id list with requirements, the Phase 13 gating line, and the four record edits.
</output>
