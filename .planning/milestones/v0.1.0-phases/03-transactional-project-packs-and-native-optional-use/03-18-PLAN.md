---
phase: 03-transactional-project-packs-and-native-optional-use
plan: 18
type: execute
gap_closure: true
wave: 3
depends_on: [03-17]
files_modified:
  - src/core/canary.ts
  - src/core/capability-ledger.ts
  - schemas/capability-ledger.schema.json
  - test/canary.test.ts
  - test/capability-ledger.test.ts
autonomous: true
requirements: [CAPA-01, CAPA-07]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CAPA-01 held mode fails closed when the required query-docs.libraryId identifier shape was not checked, and the run remains unverified rather than invoked."
    - "A successful Codex documentation canary proves ordered resolve-library-id then query-docs calls, a version-scoped identifier shape, a nonzero observation count, no unchecked required patterns, and held=true."
    - "The retained capability proof contains a closed, redacted projection of the observations and verdict needed to audit CAPA-01 without relying on a SUMMARY."
    - "Legacy proofs without invocation evidence remain readable but cannot satisfy the strict CAPA-01 completion gate."
    - "The live closure gate keeps the temporary Codex integration test, then runs the production CLI against a reviewed persistent LocalAppData state root and independently reads the retained ledger after the CLI process exits."
    - "The three remaining native exercises, two non-inferable backstops, and judgment-only prohibitions stay explicit end-of-phase human checks."
  artifacts:
    - path: src/core/canary.ts
      provides: "Fail-closed expectation matching and redacted canary-to-proof projection"
    - path: src/core/capability-ledger.ts
      provides: "Typed optional durable invocation evidence on CapabilityProof"
    - path: schemas/capability-ledger.schema.json
      provides: "Closed invocation-evidence schema with legacy-row compatibility"
    - path: test/canary.test.ts
      provides: "Failing-direction, ordered-observation, and live Codex completion coverage"
    - path: test/capability-ledger.test.ts
      provides: "Closed-schema and round-trip evidence coverage"
  key_links:
    - from: src/core/canary.ts
      to: src/core/capability-ledger.ts
      via: "canaryProof projects CanaryRunResult.verdict and observations into CapabilityProof.invocationEvidence"
      pattern: "canaryProof|invocationEvidence"
    - from: src/core/capability-ledger.ts
      to: schemas/capability-ledger.schema.json
      via: "Type and JSON schema accept the same closed optional evidence structure"
      pattern: "CapabilityInvocationEvidence|invocationEvidence"
    - from: test/canary.test.ts
      to: catalog/canaries.yaml
      via: "CAPA-01 documentation canary asserts query-docs.libraryId is satisfied and never unchecked"
      pattern: "query-docs\\.libraryId|identifierShape"
  prohibitions:
    - statement: "A declared required argument pattern that could not be checked must not produce held=true or nativeUse=invoked."
      status: unverified
      flagged: true
    - statement: "Durable audit evidence must not contain raw MCP arguments, MCP response bodies, model text, authentication bytes, or credential values."
      status: unverified
      flagged: true
    - statement: "A legacy proof with no retained invocation evidence must not be reinterpreted as a complete strict CAPA-01 proof."
      status: unverified
      flagged: true
---

<objective>
Close the remaining automated Phase 03 verification gap by making version-sensitive CAPA-01 matching fail closed and retaining enough redacted verdict detail to audit a successful native Codex proof from the capability ledger.

Purpose: The Codex transport and zero-filter selection paths are already real and tested by 03-16/03-17. This plan addresses only the verifier's unresolved proof-completeness finding and preserves the separately identified human verification routes.
Output: A fail-closed matcher, an additive closed ledger evidence contract, focused regression coverage, and a live Codex completion gate that checks both immediate and retained evidence.
</objective>

<execution_context>
@C:/Users/alpha/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/alpha/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-CONTEXT.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-RESEARCH.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-PATTERNS.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-VERIFICATION.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-UAT.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-08-SUMMARY.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-16-SUMMARY.md
@.planning/phases/03-transactional-project-packs-and-native-optional-use/03-17-SUMMARY.md
@catalog/canaries.yaml
@src/core/canary.ts
@src/core/capability-ledger.ts
@src/core/mcp-proxy.ts
@schemas/capability-ledger.schema.json
@test/canary.test.ts
@test/capability-ledger.test.ts
</context>

<interfaces>
- `matchExpectations(observations, declaration): ExpectationMatch` already reports satisfied, unsatisfied, and unchecked patterns, observation count, and held; this plan changes the final held predicate, not route vocabulary.
- `decideInvocation(canary, observations): InvocationVerdict` already carries the matcher verdict into native-use state.
- `canaryProof(result, options): CapabilityProof | null` is the single projection from a completed canary into a persisted `CapabilityProof`.
- `McpObservation` already contains only server, tool, timestamp, upstream version, outcome, and optional identifier shape; raw arguments and response bodies are absent.
- `CapabilityProof` is stored under the user state root, schema-validated, and upserted by `(projectId, harness, capability, polarity)` identity.
</interfaces>

<assumption_delta_decision>
Generalized primary noun: capability proof.

Decision: no-change.

Rationale: The detector matched only `optional` in the phase title. This gap adds no capability, target, scope class, identity axis, or variant family; it tightens completeness of the existing `(projectId, harness, capability, polarity)` proof identity. Promoting or adding a generalized identity model would create drift without evidence.
</assumption_delta_decision>

<flagged_assumptions>
The Phase 03 SPEC is absent. All fourteen deterministic edge-probe items are accounted for:

| Requirement | Category | Explicit disposition |
|---|---|---|
| CAPA-01 | adjacency | Existing 03-08 matching treats tools as an ordered subsequence; equal repeated observations remain separate. Task 1 preserves order and count. |
| CAPA-01 | empty | Existing coverage defines empty and single-observation behavior; null is outside the typed array contract. Task 1 adds missing-shape fail-closed coverage. |
| CAPA-01 | ordering | Existing reversed-order tests define order; Task 2 leaves cross-surface stability as a human backstop. |
| CAPA-02 | adjacency | Existing 03-08 logic deduplicates server identities and treats the declared maximum as inclusive. |
| CAPA-02 | empty | Existing 03-08 positive/control tests cover zero observations and incomplete routes. |
| CAPA-02 | ordering | Existing 03-08 reversed-order tests define discovery before extraction; native exercise remains human-verified. |
| CAPA-02 | concurrency | Existing 03-02 uses a per-run sink and closes before promotion; interruption-before-close remains a human backstop. |
| CAPA-03 | unclassified | Existing 03-12 covers signed handoff and planning immutability; receiving-harness exercise remains human-verified. |
| CAPA-04 | idempotency | Existing 03-01 proves repeat sync is a no-op for unchanged exact-hash inputs. |
| CAPA-04 | concurrency | Existing 03-01 uses one transaction and verifies rollback/no partial commit. |
| CAPA-05 | unclassified | Existing 03-04/03-05/03-11 cover discovery, provenance, and intent; native exercise remains human-verified. |
| CAPA-06 | unclassified | Existing 03-03/03-04/03-11/03-13 provide paired in/out-of-scope evidence. |
| CAPA-07 | unclassified | Existing 03-03/03-09/03-14/03-17 define honest axes; Task 1 adds required-shape and retained-proof completeness. |
| CAPA-08 | concurrency | Existing 03-10 selects fixture domains deterministically without shared mutable selection state. |
</flagged_assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one CAPA-01 canary from fail-closed matching into a durable redacted proof</name>
  <files>src/core/canary.ts, src/core/capability-ledger.ts, schemas/capability-ledger.schema.json, test/canary.test.ts, test/capability-ledger.test.ts</files>
  <read_first>
    - `.planning/phases/03-transactional-project-packs-and-native-optional-use/03-VERIFICATION.md` — implement only the unchecked-pattern and retained-verdict gaps.
    - `.planning/phases/03-transactional-project-packs-and-native-optional-use/03-CONTEXT.md` — preserve D-01, D-03, D-04, and D-10 through D-12.
    - `catalog/canaries.yaml` — use the existing CAPA-01 positive Codex declaration and `query-docs.libraryId` requirement unchanged.
    - `src/core/canary.ts` — inspect `ExpectationMatch`, `InvocationVerdict`, `matchExpectations`, `decideInvocation`, and `canaryProof`.
    - `src/core/capability-ledger.ts` — extend the closed `CapabilityProof` contract additively and preserve proof identity.
    - `src/core/mcp-proxy.ts` — reuse only the existing redacted `McpObservation` fields.
    - `schemas/capability-ledger.schema.json` — mirror the TypeScript evidence contract while retaining legacy rows.
    - `test/canary.test.ts` and `test/capability-ledger.test.ts` — follow existing Node test and ledger round-trip patterns.
  </read_first>
  <behavior>
    - A `query-docs` observation with no identifier shape produces an unchecked required pattern, held=false, and nativeUse=unverified.
    - An explicitly unscoped identifier stays unsatisfied; a version-scoped identifier can hold when all other expectations pass.
    - A successful result becomes a proof with the same observation count, ordered redacted observations, pattern sets, ordering, server-budget, reasons, and final verdict.
    - A legacy proof without invocation evidence validates and round-trips without fabricated evidence.
    - Unknown evidence keys, invalid enums, and malformed nested evidence are rejected by the closed schema.
  </behavior>
  <action>
Start RED by extending focused tests so the current code fails for an unchecked required identifier shape and for a proof whose completed verdict disappears during ledger projection.

In `matchExpectations`, require `uncheckedArgumentPatterns.length === 0` in the final held predicate. Preserve all existing missing-tool, forbidden-tool, ordering, distinct-server, and unsatisfied-pattern checks. This implements the verifier's failing direction and D-01/D-12: absence of checkable MCP evidence is unverified evidence, never successful invocation.

In `src/core/capability-ledger.ts`, add named closed types `InvocationObservationEvidence`, `InvocationVerdictEvidence`, and `CapabilityInvocationEvidence`, then add optional `invocationEvidence` to `CapabilityProof`. Observations may contain only the existing redacted fields: server, tool, at, upstreamVersion, outcome, and optional identifierShape. The verdict must retain matched, missing, forbiddenSeen, ordered, distinctServers, maxDistinctServers, withinServerBudget, satisfiedArgumentPatterns, unsatisfiedArgumentPatterns, uncheckedArgumentPatterns, observationCount, expectationsHeld, held, nativeUse, and reasons. Retain the canary name for attribution. Do not add a second ledger, identity axis, raw arguments, response bodies, model text, environment values, or credentials, per D-03 and D-04.

Update `canaryProof` to create an immutable evidence projection from `CanaryRunResult`. Copy arrays/records, preserve observation order, and keep existing bound-input and claim-note behavior. Top-level nativeUse and the retained verdict must agree.

Extend the JSON schema with the same optional, recursively closed evidence object. Existing rows remain valid when the field is absent; absence is compatibility, not affirmative evidence. Keep schema version one because no migration is needed.

Finish GREEN/REFACTOR without altering canary declarations, harness routing, or the already-proven 03-16/03-17 transport and selection behavior.
  </action>
  <reversibility rating="costly">The field is additive and legacy-compatible, but reverting after new proofs are written would discard audit detail consumed by verification.</reversibility>
  <acceptance_criteria>
    - Missing identifier-shape metadata for a declared required pattern yields held=false and nativeUse=unverified.
    - A version-scoped ordered Context7 route can still hold with an empty unchecked list.
    - `canaryProof` persists ordered redacted observations and a complete verdict whose count and native-use agree with the immediate result.
    - The schema accepts valid audited and legacy proofs and rejects malformed or open-ended evidence.
    - No raw arguments, responses, model text, authentication data, or credential values enter the proof contract or tests.
  </acceptance_criteria>
  <verify>
    <automated>npm run check &amp;&amp; npm run build &amp;&amp; node scripts/run-tests.mjs --files dist/test/canary.test.js dist/test/capability-ledger.test.js</automated>
    <fails_when>The command exits nonzero, either requested test file executes zero tests, any required-shape case reports held=true, or a ledger round trip loses or fabricates evidence.</fails_when>
  </verify>
  <done>One production CAPA-01 path runs from redacted MCP observations through fail-closed matching to a closed durable proof that is independently auditable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the strict Codex completion gate and preserve human-only verification boundaries</name>
  <files>test/canary.test.ts, test/capability-ledger.test.ts</files>
  <read_first>
    - `.planning/phases/03-transactional-project-packs-and-native-optional-use/03-VERIFICATION.md` — use its automated gap, three human-native checks, two backstops, and judgment prohibitions.
    - `.planning/phases/03-transactional-project-packs-and-native-optional-use/03-UAT.md` — preserve the user's no-Claude-account direction.
    - `.planning/phases/03-transactional-project-packs-and-native-optional-use/03-16-SUMMARY.md` and `03-17-SUMMARY.md` — retain the working Codex paths.
    - `test/canary.test.ts` — extend the documentation-canary and live-Codex assertions, not a parallel fixture path.
    - `test/capability-ledger.test.ts` — reuse state-root and schema validation helpers.
  </read_first>
  <behavior>
    - Strict completion requires a satisfied query-docs.libraryId, empty unchecked list, ordered route, consistent nonzero count, and version-scoped query observation.
    - Reversed order, no observations, one incomplete observation, explicit unscoped shape, and missing shape cannot pass.
    - A live Codex positive canary produces matching immediate and ledger-retained verdicts and no Claude result.
    - Fixture coverage is not relabeled as native evidence for CAPA-02, CAPA-03, or CAPA-05.
  </behavior>
  <action>
Expand focused tests into a strict CAPA-01 completion matrix using the declared documentation canary and existing observation helper. Assert both directions for empty, single incomplete, reversed, explicitly unscoped, unchecked-shape, and ordered version-scoped inputs. Compare the durable proof with the immediate result, including `resolve-library-id` before `query-docs` and a version-scoped query identifier.

Strengthen the opt-in live Codex test so success requires both immediate and round-tripped ledger proof fields: held=true, nativeUse=invoked, ordered=true, nonzero matching observation count, query-docs.libraryId satisfied, and no unchecked patterns. Keep this temporary-state integration test because it gives focused failure localization, but do not treat its cleanup-bound ledger as the retained closure artifact.

After the focused live test passes, run the compiled production CLI itself as `node dist/src/cli.js doctor --canary . --harness codex --capability CAPA-01 --json`. Pin `ALPHA_AOS_STATE_DIR` to the reviewed persistent root returned by `[Environment]::GetFolderPath('LocalApplicationData')` plus `alpha-aos-verification\phase-03-capa-01`; resolve it to an absolute path and refuse it if it falls under `[IO.Path]::GetTempPath()`. Treat CLI exit 0 as the only completion status; `doctor --canary` uses exit 2 for a blocked result, and the CLI's top-level refusal/error path also exits 2, so either form stops the gate before ledger acceptance. The zero-exit JSON must contain exactly one Codex `DOCUMENTATION_VERSION_SCOPED` result and no Claude result. The command deliberately leaves `[local-app-data]/alpha-aos-verification/phase-03-capa-01/capabilities/ledger.json` after the process exits rather than registering test cleanup.

In a separate post-process PowerShell command, recompute that exact state root, read `capabilities\ledger.json` from disk, select the sole `(projectId=null, harness=codex, capability=CAPA-01, polarity=positive)` proof, and assert its retained `invocationEvidence`: canary `DOCUMENTATION_VERSION_SCOPED`; top-level and verdict nativeUse `invoked`; held and expectationsHeld true; ordered true; a positive observationCount equal to the retained observation array length; `query-docs.libraryId` present in satisfiedArgumentPatterns and absent from uncheckedArgumentPatterns; `resolve-library-id` preceding `query-docs`; and at least one `query-docs` observation classified `version-scoped`. This read is the independently auditable completion gate: a legacy proof without `invocationEvidence`, an old proof at another state root, or the deleted temporary test ledger cannot satisfy it. In the SUMMARY, hand off only the redacted durable location `[local-app-data]/alpha-aos-verification/phase-03-capa-01/capabilities/ledger.json` plus the asserted field names and verdicts; do not copy the resolved user-profile path, raw CLI JSON, MCP arguments or responses, model text, authentication bytes, environment values, or credential values into repository artifacts.

Do not broaden Claude-only declarations, manufacture native evidence from fixtures, or revisit completed transport/selection work.

Run the full suite. In the SUMMARY, record the automated verdict fields and list the human checks below as pending unless the designated human records a result. This preserves D-09's later no-Claude direction, D-11's orthogonal evidence axes, and D-12's blocked-versus-unverified distinction.
  </action>
  <precondition>`codex login status` exits zero before the opt-in live gate; otherwise stop it and report the authentication blocker without substituting Claude or fixtures.</precondition>
  <reversibility rating="reversible">This task changes verification assertions and evidence reporting only; reverting it does not migrate or destroy stored state.</reversibility>
  <acceptance_criteria>
    - The strict matrix rejects every incomplete CAPA-01 route and accepts the complete version-scoped ordered route.
    - The temporary live Codex test asserts the same passing verdict immediately and after its test-local ledger round trip, with no Claude turn.
    - A separate production CLI invocation exits zero against the reviewed persistent LocalAppData state root and leaves one Codex CAPA-01 proof there after the process exits.
    - A separate post-process read of that retained ledger proves the closed invocationEvidence fields, order, counts, version-scoped identifier shape, and proof identity without accepting a legacy row.
    - The full repository test suite passes.
    - CAPA-02 native research/control, CAPA-03 receiving-harness handoff, CAPA-05 pack exercise, both backstops, and judgment prohibitions remain visibly human-verified.
  </acceptance_criteria>
  <verify>
    <automated>npm run check &amp;&amp; npm run build &amp;&amp; node scripts/run-tests.mjs --files dist/test/canary.test.js dist/test/capability-ledger.test.js</automated>
    <fails_when>The command exits nonzero, either suite executes zero tests, a negative completion case passes, or immediate and retained verdicts differ.</fails_when>
    <automated>codex login status</automated>
    <fails_when>The command exits nonzero or reports no usable Codex authentication.</fails_when>
    <automated>$env:ALPHA_AOS_LIVE_CANARY='1'; node scripts/run-tests.mjs --files dist/test/canary.test.js</automated>
    <fails_when>The command exits nonzero, the opt-in live Codex test does not execute, a Claude result is emitted, any required immediate verdict assertion fails, or the round-tripped ledger evidence differs from the immediate ordered version-scoped result.</fails_when>
    <automated>$stateRoot = [IO.Path]::GetFullPath((Join-Path ([Environment]::GetFolderPath('LocalApplicationData')) 'alpha-aos-verification\phase-03-capa-01')); $tempRoot = [IO.Path]::GetFullPath([IO.Path]::GetTempPath()); if ($stateRoot.StartsWith($tempRoot, [StringComparison]::OrdinalIgnoreCase)) { throw 'The reviewed CAPA-01 state root resolved under the OS temporary directory.' }; $env:ALPHA_AOS_STATE_DIR = $stateRoot; $liveText = (node dist/src/cli.js doctor --canary . --harness codex --capability CAPA-01 --json | Out-String); $cliExit = $LASTEXITCODE; if ($cliExit -ne 0) { throw "The production Codex canary CLI exited $cliExit." }; $live = $liveText | ConvertFrom-Json; $results = @($live.results); if ($results.Count -ne 1 -or $results[0].harness -ne 'codex' -or $results[0].canary -ne 'DOCUMENTATION_VERSION_SCOPED' -or @($results | Where-Object { $_.harness -eq 'claude' }).Count -ne 0) { throw 'The production CLI did not return exactly the selected Codex documentation canary.' }; Write-Output 'RETAINED_CAPA01_LEDGER=[local-app-data]/alpha-aos-verification/phase-03-capa-01/capabilities/ledger.json'</automated>
    <fails_when>The command exits nonzero; the reviewed state root resolves under the OS temporary directory; the compiled production CLI does not exit zero; or its parsed JSON does not contain exactly one Codex DOCUMENTATION_VERSION_SCOPED result and zero Claude results.</fails_when>
    <automated>$stateRoot = [IO.Path]::GetFullPath((Join-Path ([Environment]::GetFolderPath('LocalApplicationData')) 'alpha-aos-verification\phase-03-capa-01')); $ledgerPath = Join-Path $stateRoot 'capabilities\ledger.json'; if (-not (Test-Path -LiteralPath $ledgerPath -PathType Leaf)) { throw 'The retained CAPA-01 ledger file is absent.' }; $ledger = Get-Content -Raw -LiteralPath $ledgerPath | ConvertFrom-Json; $proofs = @($ledger.proofs | Where-Object { $null -eq $_.projectId -and $_.harness -eq 'codex' -and $_.capability -eq 'CAPA-01' -and $_.polarity -eq 'positive' }); if ($proofs.Count -ne 1) { throw 'The retained ledger does not contain exactly one Codex CAPA-01 positive proof.' }; $proof = $proofs[0]; $evidence = $proof.invocationEvidence; if ($null -eq $evidence) { throw 'The retained proof is legacy evidence without invocationEvidence.' }; $verdict = $evidence.verdict; $observations = @($evidence.observations); $tools = @($observations | ForEach-Object { $_.tool }); $resolveIndex = [Array]::IndexOf($tools, 'resolve-library-id'); $queryIndex = [Array]::IndexOf($tools, 'query-docs'); $versionQueries = @($observations | Where-Object { $_.tool -eq 'query-docs' -and $_.identifierShape -eq 'version-scoped' }); if ($evidence.canary -ne 'DOCUMENTATION_VERSION_SCOPED' -or $proof.nativeUse -ne 'invoked' -or $verdict.nativeUse -ne 'invoked' -or $verdict.held -ne $true -or $verdict.expectationsHeld -ne $true -or $verdict.ordered -ne $true -or [int]$verdict.observationCount -le 0 -or [int]$verdict.observationCount -ne $observations.Count -or @($verdict.satisfiedArgumentPatterns) -notcontains 'query-docs.libraryId' -or @($verdict.uncheckedArgumentPatterns).Count -ne 0 -or $resolveIndex -lt 0 -or $queryIndex -le $resolveIndex -or $versionQueries.Count -lt 1) { throw 'The retained Codex CAPA-01 invocationEvidence does not satisfy the strict completion contract.' }; Write-Output 'RETAINED_CAPA01_PROOF_OK [local-app-data]/alpha-aos-verification/phase-03-capa-01/capabilities/ledger.json'</automated>
    <fails_when>The command exits nonzero; the retained ledger is absent or unreadable; the exact proof identity is not unique; invocationEvidence is absent; any required verdict/count/pattern assertion differs; resolve-library-id does not precede query-docs; or no retained query-docs observation is version-scoped.</fails_when>
    <automated>npm test</automated>
    <fails_when>The command exits nonzero, any repository test fails, or no tests execute.</fails_when>
    <human-check>
      1. CAPA-02: inspect a positive native multi-source research run and ordinary-lookup control on an authorized claimed harness, or retain human-needed/blocked status; fixtures alone are not native evidence.
      2. CAPA-03: complete a real receiving-harness handoff while verifying `.planning/` bytes and authority stay unchanged, or retain human-needed/blocked status.
      3. CAPA-05: exercise a representative project pack natively in scope and confirm project-local provenance, or retain human-needed/blocked status.
      4. Review the two backstops: stable ordering of equally strong documentation proofs, and no promotion when interrupted before the observation sink closes. Record pass/fail/blocked with evidence.
      5. Review all twelve judgment-only prohibitions in `03-VERIFICATION.md`; record pass/fail/blocked rather than inferring them from source presence.
      6. Preserve the no-Claude-account direction; do not request or spend a Claude model turn.
    </human-check>
  </verify>
  <done>The automated proof-completeness gap is closed with immediate and durable evidence, while non-inferable claims remain queued for human review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Upstream MCP → observation proxy | External tool events become proof inputs; only bounded classified metadata may cross. |
| Canary result → capability ledger | Ephemeral evidence becomes durable user-state evidence used by verification. |
| Legacy ledger → strict completion gate | Older rows may lack the optional audit projection and must not be promoted by absence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-139 | Spoofing | `matchExpectations` / `decideInvocation` | high | mitigate | Require every declared argument pattern to be checkable and satisfied before held/native invocation. |
| T-03-140 | Information Disclosure | `CapabilityProof.invocationEvidence` | high | mitigate | Persist only closed redacted classifications and verdicts; exclude raw arguments, responses, model text, auth data, environment values, and credentials. |
| T-03-141 | Repudiation | legacy capability proofs | medium | mitigate | Keep absent evidence readable while requiring a newly audited proof for strict completion. |
| T-03-142 | Tampering | proof type ↔ schema ↔ ledger round trip | medium | mitigate | Test malformed nested fields, unknown keys, order, counts, and verdict agreement. |
| T-03-SC | Tampering | package supply chain | low | accept | This plan installs no package and changes no dependency lock. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

### GOAL
- COVERED: 03-01 through 03-17 implement scoped capability use; this plan closes retained proof completeness and preserves human routes for non-inferable representative use.

### REQ
- CAPA-01 — COVERED by Tasks 1-2 plus prior 03-02/03-06/03-08/03-16/03-17.
- CAPA-02 — COVERED by prior 03-02/03-07/03-08/03-13/03-14/03-15; Task 2 routes the native positive/control exercise to humans.
- CAPA-03 — COVERED by prior 03-12; Task 2 routes receiving-harness exercise to humans.
- CAPA-04 — COVERED by prior 03-01; no current gap requires new work.
- CAPA-05 — COVERED by prior 03-04/03-05/03-06/03-11; Task 2 routes native pack exercise to humans.
- CAPA-06 — COVERED by prior 03-03/03-04/03-11/03-13/03-14/03-15; no current gap requires new work.
- CAPA-07 — COVERED by prior 03-03/03-09/03-14/03-17 and Tasks 1-2.
- CAPA-08 — COVERED by prior 03-10; no current gap requires new work.

### RESEARCH
- COVERED: Existing canary orchestration, MCP evidence authority, redaction, user-state ledger, exact-input binding, additive schema evolution, and test conventions are used. No package, harness adapter, ORM path, schema push, or external integration is introduced.
- EXCLUDED: Later-phase and explicitly out-of-scope research items remain excluded.

### CONTEXT
- D-01 — COVERED by Tasks 1-2 and prior observation/canary plans; unchecked shapes fail closed.
- D-02 — COVERED by prior 03-02/03-06/03-16; proxy lifetime is unchanged.
- D-03 — COVERED by Task 1 and prior 03-03; one ledger remains authoritative.
- D-04 — COVERED by Task 1 and prior 03-03/03-11; proof identity and bound inputs stay intact.
- D-05 — COVERED by prior 03-01; pack contents are not broadened.
- D-06 — COVERED by prior 03-01; transaction boundaries are unchanged.
- D-07 — COVERED by prior 03-01/03-11; skill bytes/provenance are unchanged.
- D-08 — COVERED by prior 03-01; artifact separation is unchanged.
- D-09 — COVERED by Task 2 applying the later no-Claude UAT direction.
- D-10 — COVERED by Task 1 retaining ceiling and proof fields separately.
- D-11 — COVERED by Tasks 1-2 retaining independent verdict axes.
- D-12 — COVERED by Tasks 1-2 distinguishing unchecked/unverified from invoked.
- D-13 — COVERED by prior 03-09; no deletion behavior is added.
- D-14 — COVERED by prior paired-evidence plans and Task 2's negative matrix.
- D-15 — COVERED by prior 03-10; fixture domains are unchanged.
- D-16 — COVERED by prior 03-12; Task 2 routes remaining receiving-harness judgment.
- Deferred ideas — EXCLUDED: project MCP/policy sync, ecc-pack-router, Antigravity/Hermes project delivery, telemetry, and full OS×harness matrix are not implementation work.

Result: COVERED. No item requires another gap plan or phase split.
</source_audit>

<verification>
- Task 1 proves the failing direction and proof/schema/ledger alignment.
- Task 2 proves strict completion in both directions, keeps the focused temporary-state live test, then proves production-CLI completion and retained post-process ledger evidence at the reviewed persistent state root before running the full suite.
- Human review remains mandatory for the three native exercises, two backstops, and judgment prohibitions.
</verification>

<success_criteria>
- An unchecked query-docs.libraryId can no longer produce held/invoked.
- A passing production Codex CLI run leaves an auditable redacted ledger proof at `[local-app-data]/alpha-aos-verification/phase-03-capa-01/capabilities/ledger.json`, and a separate process reads and asserts it after the CLI exits.
- Legacy rows remain readable without becoming complete strict evidence.
- No Claude turn, dependency install, ORM/schema push, new identity model, or unrelated Phase 03 rework is introduced.
- Every non-inferable verifier item is assigned to end-of-phase human review.
</success_criteria>

<artifacts_this_phase_produces>
- `src/core/capability-ledger.ts`: `InvocationObservationEvidence`, `InvocationVerdictEvidence`, `CapabilityInvocationEvidence`, and optional `CapabilityProof.invocationEvidence`.
- `src/core/canary.ts`: fail-closed unchecked-pattern predicate and `canaryProof` evidence projection.
- `schemas/capability-ledger.schema.json`: closed optional invocation-evidence schema.
- `test/canary.test.ts`: strict completion matrix and live Codex immediate/durable assertions.
- `test/capability-ledger.test.ts`: audited-proof, legacy-proof, malformed-evidence, and round-trip coverage.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/03-transactional-project-packs-and-native-optional-use/03-18-SUMMARY.md` when done.
</output>
